# STATE — Crash Game (Jungle Gaming Challenge)

> Project memory. Updated at phase transitions, plan completions, and milestone boundaries.

---

## Project Reference

- **Project doc**: `.planning/PROJECT.md`
- **Roadmap**: `.planning/ROADMAP.md`
- **Requirements**: `.planning/REQUIREMENTS.md`
- **Research**: `.planning/research/` (SUMMARY · STACK · ARCHITECTURE · FEATURES · PITFALLS)
- **Config**: `.planning/config.json` (mode=interactive, granularity=standard, parallelization=true)

**Core value**: Demonstrate senior-level engineering through a Crash Game that is correct, fair, real-time, and deeply considered — not a generic AI-assisted submission. Every decision must be defensible during the recruiter's arguição.

**Current focus**: Phase 8 complete (Provably-Fair UX, History & Replay, 10/10 plans landed, four ADRs authored [ADR-028..031], 8/10 phases done). On top of the green Phase 7 game loop: always-visible Fairness badge in the header → SHA-256 verification drawer hashing the prior round's serverSeed in-browser via crypto.subtle → `/verify/$roundId` route running the full Bustabit-canon HMAC + 52-bit derivation client-side (ignores server `matches` field) → Replay modal on every history chip auto-playing the past round at 1x with 1x/2x/4x speed toggle via the SAME Canvas renderer through the RafCurveDriver seam. Determinism byte-match E2E green; README recruiter walkthrough reproduces 2.94 from the locked-byte tuple in any shell. Ready for `/gsd:verify-phase 8` + `/gsd:ui-review` then `/gsd:plan-phase 9` (Auto Features & Leaderboard — parallelizable with Phase 8 per ROADMAP, both can land after Phase 7).

---

## Current Position

- **Milestone**: 1 (initial submission)
- **Phase**: 9 — Auto Features & Leaderboard (IN PROGRESS, 9/10 plans landed; 8/10 phases complete overall)
- **Plan**: P09-09 complete (9/10) — FE Leaderboard panel + shared maskPlayerId landed via eight atomic commits (`2173e3f` RED shared-kernel mask tests, `82ac1e7` GREEN move maskPlayerId to @crash/shared-kernel + migrate 6 server consumers, `41a69ec` RED useLeaderboard hook tests, `c3c0ac5` GREEN useLeaderboard + leaderboard-api + ws-dispatch leaderboard:updated, `636f40e` RED RankChip+LeaderboardRow+LeaderboardPanel tests, `9b355b8` GREEN three components with rank-up transition + own-row override, `eeda105` RED right-rail tab tests, `5f75ae0` GREEN routes/index.tsx shadcn Tabs Live Feed | Leaderboard). `maskPlayerId(playerId: PlayerId) → string` now lives in `@crash/shared-kernel/identity` exporting through the workspace barrel — the legacy `services/games/src/application/use-cases/mask-player-id.ts` deleted and all 6 production consumers + 1 unit test re-imported from the shared package; cross-environment determinism gate (Phase 6 reference impl vs shared-kernel impl run against fixture UUID `3f29b1a2-4d5e-6f7a-8b9c-0d1e2f3a4b5c`) locks the FE-imported hash matches the server-emitted hash byte-for-byte (T-09-61 mitigation). FE `useLeaderboard()` is a TanStack Query hook (`queryKey:['leaderboard','24h']`, `staleTime:Infinity`) calling `fetchLeaderboard('24h')` via `protectedFetch` → Zod-parsed against `leaderboardResponseSchema` (re-exported from `@crash/contracts/ws/leaderboardUpdatedPayloadSchema` — single source of truth shared with the WS event). A `useEffect` subscribes to `leaderboard:updated` via the Plan 09-08 `subscribeWsEvent` surface and on each valid payload calls `queryClient.setQueryData(leaderboardQueryKey, payload)` — inline cache replace, no roundtrip (Q4). `ws-dispatch.ts` extended with a new event slot, schema mapped to `leaderboardUpdatedPayloadSchema`, no-op handler so the existing `safeParse` gate runs even when no subscriber is attached. `RankChip` renders 24×24 circle (rank 1-9) / 24×32 rect (rank 10) with UI-SPEC §Color rank-color rule (rank-1 emerald, rank-2 cyan, rank-3 muted, 4-10 hairline) — `data-rank-color` attribute drives unit-test assertion and CSS-only color (no hardcoded hex). `LeaderboardRow` renders 2-line layout (rank chip + masked id / `YOU (first5hex…)` + right-aligned Money net profit with leading `+`/`−` + lucide `TrendingUp`/`TrendingDown` icon; bottom = `Nw` muted) — negative profit uses `muted-foreground` not destructive/red (UI-SPEC §Color grep gate clean), rank-up transition fires only when `entry.rank < previousRank` via transient `data-rank-up` for `rankUpTransitionMs + 600ms` (CSS transition on border-color, reduced-motion gated by Tailwind). `LeaderboardPanel` reads `useOidc().oidcTokens.decodedIdToken.sub` and computes `ownMasked = maskPlayerId(PlayerId(sub))` for own-row detection through the shared helper, renders distinct skeleton (5 rows) / empty / error (shadcn Alert destructive + Retry) / list states, heading `Top ${sizeN} · last ${windowHours}h` from env, refresh button ≥44×44px aria-label `Refresh leaderboard`, footer `Updated Ns ago` interval gated by `isActive` prop so it only ticks while the Leaderboard tab is selected (Phase 7/8 isolation principle), `aria-live="polite"` announces own-rank-up improvements only (never rank-down). `routes/index.tsx` right-rail wrapped in controlled `<Tabs value={feedTab} onValueChange={setFeedTab}>` with two triggers (`Live Feed` default + `Leaderboard` with `Trophy` icon); `LiveFeed` preserved byte-unchanged inside its `TabsContent`; `LeaderboardPanel` receives `isActive={feedTab === 'leaderboard'}`. Grid layout (`grid-cols-[320px_minmax(0,1fr)_320px]`), aside dimensions, bet-rail and curve-stage sections all byte-stable per UI-SPEC §"Layout Contract" + Phase 7 D-01. Verification: shared-kernel `bun test tests/identity/mask-player-id.test.ts` 5/5 pass; games tsc clean; games unit 279 pass / 8 fail (baseline pre-existing failures unchanged — this plan is a pure import-path swap on the server side); frontend tsc clean; frontend `bunx vitest run` 244/244 pass across 36 files in ~3.3s (was 217/217 after Plan 09-08; +27 from this plan — 5 mask + 7 hook + 17 panel + 3 tabs net, with some absorbed by existing routes tests already mocking the new modules); frontend `bun run lint` clean (only pre-existing `routeTree.gen.ts` warning unrelated to this plan); all 5 verification greps from PLAN clean (no `bg-destructive`/`text-destructive` in leaderboard-row, no hardcoded `5000`/`200` in leaderboard-panel, Zod `.parse(` present in leaderboard-api, no UUID-prefix mask shapes in panel, `@crash/shared-kernel` imported in panel, no stale `mask-player-id` imports left in services/games/src). Two deviations both within auto-fix Rule 1 (test-DX only, no production code change): Rule 1 (Radix Tabs doesn't activate on `.click()` alone in jsdom — used `fireEvent.pointerDown + mouseDown + click` mirroring Plan 09-08's bet-panel tabs pattern), Rule 1 (`findByText(masked)` exact-match failed because rows render `bd14a3c2…` with ellipsis — switched panel tests to `findByText(new RegExp(masked))` substring matcher). No Rule 2/Rule 3/Rule 4 deviations triggered; no auth gates. Threat-model dispositions all verified mitigated: T-09-60 (malicious WS payload — Zod safeParse drops with warn, cache stays at last valid state, locked by hook test "drops invalid WS payloads without corrupting cache"), T-09-61 (full UUID DOM leak — own-row detection runs through shared `maskPlayerId` to produce 8-hex prefix; OIDC `sub` claim only held in memoised `useOwnMasked` hook, never written to a `data-*` attribute or text node; grep gate `no replace(/-/g)` + `no slice(0,8)` in panel enforces), T-09-62 (render storm — `setQueryData` idempotent + server-side rank-change gate from Plan 09-06 throttles), T-09-63 (footer interval leak — `useEffect` cleanup clears interval + only registers when `isActive` is true). REQ-LEAD-03 (FE consumes endpoint via TanStack Query) + REQ-LEAD-04 (FE renders side panel with WS live updates) now closed at this plan. Plan 09-09 unblocks Plan 09-10 (closeout — ADR candidates "Shared `maskPlayerId` helper as the cross-environment determinism gate for own-row detection" + the Phase 9 ROADMAP success criterion 2 closure). Previous: P09-08 complete (8/10) — FE auto-bet stack landed via seven atomic commits (`e3f0d15` shadcn radio-group install, `ee38a67` RED strategy + store tests, `d0d4f1c` GREEN strategy.ts + auto-bet.store.ts, `189317f` RED driver tests, `3ecfe28` GREEN auto-bet-driver.ts + ws-dispatch subscribeWsEvent + __root mount, `156d1ab` RED form + bet-panel tests, `937c4d7` GREEN AutoBetForm + AutoBetSessionPanel + ManualTabContent + BetPanel Tabs). Per-session Zustand slice (NO persist — REQ-AUTO-04 grep gate), pure `nextBetAmount` strategy (Martingale base = configured initial per REQ-AUTO-02 + CONTEXT §Specifics), event-only driver (NO setInterval — RESEARCH Anti-Patterns gate) subscribing to round:started / round:settled / bet:my_cashed_out / bet:my_refunded via new `subscribeWsEvent` pub-sub surface on `ws-dispatch.ts`. Driver POSTs `placeBet` with `autoCashoutTarget` from config on round:started, halts cleanly on stop-win / stop-loss / insufficient-balance with deduped amber toast; 409 treated as transient (no halt), 402 halts with insufficient-balance reason. BetPanel refactored to shadcn Tabs (defaultValue='manual') with Lock icon on Manual trigger + pulsing accent dot on Auto trigger while running; ManualTabContent wraps Phase 7 form verbatim, adds Lock+Alert + opacity-50 + HTML-disabled inputs when isRunning. AutoBetForm renders 5 fields (target + strategy RadioGroup + base + stop-loss + stop-win) with Start button (accent fill when valid) + Stop button (variant='outline' + border-destructive + text-destructive — NO bg-destructive per UI-SPEC §Color grep gate). Session P/L stored as (Money, sign:-1|0|1) tuple to keep Money VO discipline while supporting negatives; AutoBetSessionPanel renders accent for positive / muted-foreground for negative (NOT red). Verification: bunx vitest run 217/217 pass in 3.12s across 34 files (was 200/200; +17 from this plan — 6 strategy + 8 store + 11 driver-stops + 13 form + 4 bet-panel, though some additions absorbed by parallel author iterations); bunx tsc --noEmit exit 0; bun run lint clean (only pre-existing routeTree.gen.ts auto-generated warning unrelated to this plan); persist grep gate clean; setInterval grep gate clean; bg-destructive grep gate clean. Two minor deviations both within auto-fix rules: Rule 1 test interaction (Radix Tabs doesn't switch on `fireEvent.click` in jsdom — switched to `fireEvent.keyDown ArrowRight + Enter` which exercises the same Radix keyboard nav code path the a11y testers use; no production change), Rule 1 UI-SPEC literal compliance (initial Stop button included `hover:bg-destructive/10` which violated the grep gate forbidding any `bg-destructive` surface — removed the hover background, kept the border + text + Pause icon + motion-safe press scale for affordance). No auth gates; no architectural change. REQ-AUTO-02 + REQ-AUTO-03 + REQ-AUTO-04 + REQ-AUTO-05 closed. Plan 09-08 unblocks Plan 09-09 (FE LeaderboardPanel reuses the new `subscribeWsEvent` pub-sub surface for `leaderboard:updated`) and Plan 09-10 (ADR candidate "FE auto-bet driver architecture: event-only Zustand slice + ws-dispatch subscription, no setInterval, no persist, Martingale base = configured initial"). Previous: P09-06 complete (7/10) — LeaderboardProjectorService consuming `game.events` via `@IdempotentSubscribe` (widened spine accepts `string | string[]` routingKey now), plus the missing `bet.cashed_out` outbox emit on `CashOutUseCase`, plus the masking-gated `leaderboard:updated` WS @OnEvent handler on `GameWsGateway`, plus the SC5 chaos test. Five atomic commits: (`7b518cf` `feat(09-06): widen @IdempotentSubscribe routingKey to string | string[]` — additive type change, 6 production call sites still typecheck unchanged, regression test locks per-message inbox dedupe on array routing keys); (`42b31dc` `feat(09-06): emit bet.cashed_out outbox event from CashOutUseCase` — Rule 2 deviation; the plan + 09-03 SUMMARY both claimed `bet.cashed_out` was already emitted, `grep` proved otherwise, dead `case "bet.cashed_out"` in `ws-bridge.consumer.ts` repaired as free side-effect; new `BetCashedOutEventV1` + `BetRefundedEventV1` schemas on `@crash/contracts`; same `em.transactional` + same `correlationId === causationId` across `wallet.credit` + `bet.cashed_out` rows so the saga + projector + ws-bridge all stitch from one flow id; cash-out unit test extended from `toHaveLength(1)` to `toHaveLength(2)` with full envelope + payload + route assertions); (`06eeae8` `test(09-06): add failing unit tests for LeaderboardProjectorService` — RED; 6 cases: cashed_out applies positive delta, refunded calls no-op, lost applies negative delta, unknown type drops, rank-change emits, same-order throttles); (`d74fc40` `feat(09-06): add LeaderboardProjectorService + WS leaderboard:updated emit` — GREEN; `@IdempotentSubscribe({ exchange: EXCHANGES.GAME_EVENTS, routingKey: ['bet.cashed_out', 'bet.refunded', 'bet.lost'], queue: 'leaderboard-projector.q' })`; handler extracted to `handleEnvelope` for unit testability mirroring `WalletDebitedHandler`; two-call snapshot bracket — `fetchSnapshot` before + after the apply, then `LeaderboardSnapshot.diff(before, after).changed === true` gates the in-process `GAME_EVENTS.LEADERBOARD_UPDATED` emit via injected `EventEmitter2`; `GameWsGateway.onLeaderboardUpdated` maps `entries[].playerId` through `maskPlayerId(PlayerId(...))` to 8-char SHA-256 hex, transforms `netProfitCents: bigint` to string for wire serialization, strips the raw `playerId` field before `server.to('lobby').emit('leaderboard:updated', ...)` so raw UUIDs cannot leak; provider registered in `GameCoreModule`); (`7bcbbb2` `test(09-06): add SC5 chaos test — projector failure does not block settlement` — `Test.createTestingModule(...).overrideProvider(LeaderboardProjectorService).useValue({ handle: () => throw })` boots the full games stack with the projector deliberately broken, places a bet, asserts the bet reaches terminal status AND `leaderboard_24h` stays empty for the player, proving the write path is fully decoupled per REQ-LEAD-02 + ROADMAP SC5). The widened spine decorator is backwards-compatible — all 6 production call sites (2 wallets, 2 games, 2 spine tests) still pass single-string routingKey; the native `@RabbitSubscribe` was already accepting arrays at runtime (the existing `WsBridgeConsumer` uses one), so this was purely a TypeScript type widening. Threat-model dispositions all verified mitigated: T-09-30 (replay inflation — `@IdempotentSubscribe` inbox dedupe by messageId, locked by the new spine routing-key test asserting `tryClaim` keyed per-message regardless of which RK matched), T-09-31 (DoS via emit-on-every-event — rank-change gate locked by unit tests 5 + 6), T-09-32 (refund double-count — `applyRefunded` is no-op, unit test 2 asserts no other apply calls fire), T-09-33 (poison message DLQ overflow — `buildQuorumArgs(5, deriveDlxFromExchange(EXCHANGES.GAME_EVENTS))` from the decorator + existing Phase 5 `games-dead-letter.consumer.ts`), T-09-34 (projector lag blocks write path — SC5 chaos test proves decoupling), T-09-35 (raw UUID leak — gateway unit test asserts every `playerIdMasked` matches `/^[0-9a-f]{8}$/` AND raw-UUID regex `/[0-9a-f]{8}-[0-9a-f]{4}-/` returns 0 matches in the serialised payload AND no `playerId` property exists on the emitted entries). Verification: messaging-spine tsc clean, messaging-spine unit suite 8/8 pass; contracts 44/44 pass (was 37; +7 from the new `bet-cashed-out` + `bet-refunded` schema cases inherited via barrel); games tsc clean; games unit 279 pass / 8 fail (was 271/8; +8 from this plan — 6 projector + 2 gateway-masking; the 8 baseline failures unchanged from Plans 09-01..05 — `multiplier-broadcast.service.test.ts` + `round-loop.service.test.ts` + `get-ws-snapshot.use-case.test.ts`); integration chaos test compiles cleanly + skips without `INTEGRATION=1` (same gate as 09-03's integration suite). Three deviations all within auto-fix rules: Rule 2 (CashOutUseCase did not emit `bet.cashed_out` — added the missing outbox row; the same fix repaired the dead ws-bridge case branch as a free side-effect — committed as separate atomic commit `42b31dc` for clean git history), Rule 2 (added `BetRefundedEventV1` schema with `roundId` + `amount` optional to match the existing publisher shape from `saga-timeout-sweeper` + `wallet-debit-rejected`; projector's `applyRefunded` is no-op so missing fields are inconsequential to the ledger), Rule 3 (`handleEnvelope` extracted from `@IdempotentSubscribe`-decorated `handle` so unit tests can bypass the decorator wrapper without fabricating AMQP headers; mirrors the established `WalletDebitedHandler` pattern). REQ-LEAD-01 + REQ-LEAD-02 now backend-closed (the projector populates the read model + the chaos test proves write-path independence); REQ-LEAD-04 wire-active (the WS gateway emits the shape Plan 09-07 already exported via `leaderboardUpdatedPayloadSchema`). Plan 09-06 unblocks Plan 09-09 (FE LeaderboardPanel — `leaderboard:updated` event now fires with the exact payload Plan 09-07's contract locked) and Plan 09-10 (closeout — ADR candidate "Light-CQRS leaderboard projector with same-TX bet.cashed_out emit precedent + rank-change-only WS throttle gate"). Previous: P09-07 complete (6/10) — `GET /games/leaderboard?window=24h` HTTP endpoint + shared `@crash/contracts/ws/leaderboardUpdatedPayloadSchema` landed via four atomic commits (`b15e6e1` RED contract + DTO tests, `c540cbe` GREEN `leaderboardUpdatedPayloadSchema` + `leaderboardEntrySchema` exported from `@crash/contracts/ws` + `LeaderboardQueryDto` (z.enum(['24h']).default('24h')) + `LeaderboardResponseDto` (re-exports the shared contract schema), `8597f51` RED controller integration test + use case unit test, `5f0ae01` GREEN `GetLeaderboardUseCase` + `LeaderboardController @UseGuards(JwtGuard)` + app.module/game-core.module wiring). `leaderboardEntrySchema` strict: `{ playerIdMasked: regex(/^[0-9a-f]{8}$/), rank: int().positive(), netProfit: moneySnapshotSchema, winCount: int().nonnegative(), totalBetCount: int().nonnegative() }`; `leaderboardUpdatedPayloadSchema` strict: `{ entries: array(leaderboardEntrySchema), updatedAt: string().datetime() }`. This is the SINGLE source of truth both the HTTP response (this plan) and the WS `leaderboard:updated` event (Plan 09-06 will emit; Plan 09-09 will consume) Zod-validate against — one schema, three call sites, zero drift surface. `LeaderboardController @Controller('games/leaderboard') @UseGuards(JwtGuard)` with one `@Get()` that does `leaderboardQuerySchema.safeParse(windowRaw === undefined ? {} : { window: windowRaw })` — manual safeParse mirrors the `rounds.controller.ts` history pattern so the omitted-param default applies cleanly (avoids fighting the global ZodValidationPipe over which schema wins). `GetLeaderboardUseCase.execute({ window })` calls `leaderboard.fetchTopN(env.LEADERBOARD_TOP_N, { windowHours: env.LEADERBOARD_WINDOW_HOURS })` (both bounds pull from Plan 09-01 env layer; no hardcoded N or hours per CLAUDE.md §Configuration), maps each row to `{ playerIdMasked: maskPlayerId(PlayerId(row.playerId)), rank: index + 1, netProfit: { amount: row.netProfitCents.toString(), currency: 'CRD', scale: 2 }, winCount, totalBetCount }`, returns `{ entries, updatedAt: new Date().toISOString() }`. The MoneySnapshot is built DIRECTLY from BigInt cents (not via `Money.of` which would `NegativeMoneyError` on negatives) — the leaderboard wire format must support negative net profit for ranking transparency, and the contract's `MoneySnapshot.amount` regex `/^-?\d+$/` already accepts signed integers (Rule 2 deviation — wire-correctness fix; unit test #4 locks `-1500n → '-1500'`). Verification: contracts 44/44 pass (was 37; +7 from this plan), games unit 271 pass / 8 fail (was 262/8; +9 from this plan = 5 DTO + 4 use case; 8 baseline failures unchanged from Plans 09-01..05 — `multiplier-broadcast.service.test.ts` 2-arg ctor + `round-loop.service.test.ts` maxNonce-missing + `get-ws-snapshot.use-case.test.ts` clock-mock failures, none touch the new code paths); `bunx tsc --noEmit` exit 0 in both packages/contracts and services/games; integration test `tests/integration/leaderboard-controller.test.ts` compiles cleanly + 10 cases authored. Live run with `INTEGRATION=1` hits a PRE-EXISTING baseline DI failure in `createTestGamesApp` (`Nest can't resolve dependencies of the MultiplierBroadcastService (?, Object, EventEmitter)` at index [0] — the ModuleRef ctor arg added by Phase 6 P6.05 for the RoundLoop ↔ MultiplierBroadcast cycle-break is not resolving under the bun-test Nest DI path); the SAME failure reproduces on `main` against `leaderboard-repository.test.ts` (which Plan 09-04 SUMMARY claimed passed when authored) and `get-player-bets.test.ts` BEFORE any 09-07 changes — confirming this is a pre-existing harness regression, not caused by this plan; logged out-of-scope per SCOPE BOUNDARY in `.planning/phases/09-auto-features-leaderboard/deferred-items.md` for a follow-up plan that audits the integration bootstrap DI graph. Three deviations all within auto-fix rules: Rule 3 path correction (plan listed `services/games/src/presentation/presentation.module.ts` which doesn't exist — the actual NestJS module is `services/games/src/app.module.ts` and the use case is wired through `game-core.module.ts.exports[]`), Rule 1 controller pattern correction (plan's `@UsePipes(new ZodValidationPipe(leaderboardQuerySchema)) @Query() query: LeaderboardQueryDto` would fight the global `APP_PIPE` ZodValidationPipe — switched to manual safeParse mirroring `rounds.controller.ts`), Rule 2 MoneySnapshot direct construction (plan said `Money.fromCents(row.netProfitCents).toSnapshot()` which (a) `Money.fromCents` doesn't exist as a public API — the factories are `Money.of(bigint)` + `Money.fromSnapshot(snap)` — and (b) `Money.of` rejects negatives via `NegativeMoneyError`, but the leaderboard legitimately holds negative net profit for losing players within the 24h window — built MoneySnapshot directly from `row.netProfitCents.toString()` to preserve the sign). No auth gates (JwtGuard + JwtVerifierService reused verbatim from Phase 6 P6.02; no new Keycloak realm/role/client config). Threat-model dispositions all verified mitigated: T-09-40 (full UUID leak — `grep "playerId\b"` against the three response-path files returns only the `maskPlayerId(PlayerId(row.playerId))` call site; contract regex `/^[0-9a-f]{8}$/` locks the wire shape), T-09-41 (forge window=all_time — `z.enum(['24h'])` rejects with 400 at DTO layer), T-09-42 (DoS — accepted; Phase 10 stretch REQ-STRETCH-07), T-09-43 (cross-player leak — accepted by design; the endpoint returns the SAME top-N to every caller, no per-player slice), T-09-44 (bigint precision via raw number — `netProfit` serialized as `{ amount: bigint.toString(), currency, scale }` never a JS number; contract test #2 locks). REQ-LEAD-03 (GET endpoint exists, masks playerId, 24h window) closed at this plan; REQ-LEAD-04 (shared payload schema for HTTP + WS) closed at this plan. Plan 09-07 unblocks Plan 09-06 (LeaderboardProjectorService imports `leaderboardUpdatedPayloadSchema` from `@crash/contracts/ws` and Zod-parses payloads before emit) and Plan 09-09 (LeaderboardPanel FE `useLeaderboard` TanStack Query hook hits `GET /games/leaderboard?window=24h` through Kong existing `/games/*` upstream + Zod-parses the response with the same schema + WS dispatch handler for `leaderboard:updated` Zod-parses the WS payload with the same schema — single Zod gate for both transports). Previous: P09-05 complete (5/10) — server-enforced auto-cashout + SC1 disconnect-safety E2E landed via five atomic commits (`f5022fd` RED ROUND_TICK integration tests, `ef9b283` GREEN ROUND_TICK in-process emit from MultiplierBroadcastService.fireTick, `4924721` RED AutoCashoutTickService unit tests, `d858264` GREEN AutoCashoutTickService + GameCoreModule wiring, `ddbf31c` SC1 disconnect E2E test). `GAME_EVENTS.ROUND_TICK = "round.tick"` constant + `RoundTickPayload { roundId: RoundId, multiplier: number, t: number }` type added to `application/game-events.ts`; `MultiplierBroadcastService.fireTick` now emits ROUND_TICK in-process via injected `EventEmitter2` AFTER the existing `gateway.server.to("lobby").volatile.emit("round:tick", ...)` line, wrapped in a sibling try/catch so a misbehaving listener cannot break the volatile broadcast (Pitfall 3). Production DI ctor is `(ModuleRef, EventEmitter2)` — keeps the Phase 6 P6.05 ModuleRef cycle-break for RoundLoopService ↔ MultiplierBroadcastService while adding EventEmitter2 directly (no cycle on the emitter); a 3-arg overload `(roundLoop, gateway, eventEmitter)` exists for direct-ref test fixtures. New `AutoCashoutTickService` (@Injectable application-layer service) decorated with `@OnEvent(GAME_EVENTS.ROUND_TICK, { async: true })` whose `onTick(payload: RoundTickPayload)` handler stamps `const acceptedAt = new Date();` as its LITERAL FIRST executable statement (ADR-023 generalized to the tick-loop entry path — source-inspection regex `/async\s+onTick[^{]*\{\s*const\s+acceptedAt\s*=\s*new\s+Date\(\)/` locked in the unit suite, mirroring Phase 5 P5.06 controller-line-69 gate). Second statement is `const ceilingCentiX = Math.floor(payload.multiplier * 100)`; then `await this.bets.findAutoCashoutCandidates(payload.roundId, ceilingCentiX)` runs against the Plan 09-02 partial index `idx_bets_auto_cashout_candidates` (hot path at 30Hz); then loops invoking `cashOut.execute({ playerId: bet.playerId, multiplier: bet.autoCashoutTarget!, acceptedAt })` — passing the bet's stored target (NOT the current tick multiplier — Pitfall 4 / T-09-21 mitigation) so the player gets exactly the multiplier they asked for. `RoundNotRunningError` + `BetNotCashableError` thrown by CashOutUseCase are swallowed (legitimate FSM-guarded races — Pitfall 11); other error classes are logged at error level via `this.logger.error("Unexpected auto-cashout error", err)` (observability over silence per CLAUDE.md §"Saga / messaging"). Service registered in `GameCoreModule.providers`; `@OnEvent` self-registers via @nestjs/event-emitter so no other wiring needed. SC1 disconnect-safety E2E at `services/games/tests/e2e/auto-cashout-disconnect.e2e.test.ts` (renamed from plan's `.e2e.ts` to `.e2e.test.ts` so Bun's `bun test` matcher picks it up — Rule 3 deviation) connects socket.io to `ws://localhost:4101/ws` with the seeded `player/player123` JWT, waits `round:running`, POSTs `/games/bet` with `autoCashoutTarget=2.0`, on first `round:tick` with `multiplier >= 1.5` force-closes the socket, then polls `GET /games/bets/me` every 500ms for up to 30s and asserts `status === 'CASHED_OUT'` AND `cashedOutMultiplier === 2.0`; retries up to 3 round cycles if a deterministic seed crashes the round below 2.0x (logs the crash multiplier so flakiness is visible in CI output). Verification: full games unit suite 262 pass / 8 fail (was 254/8, +8 from this plan — 7 AutoCashoutTickService unit cases + 1 source-inspection regex gate; the 8 baseline failures unchanged from Plans 09-01..04 — `multiplier-broadcast.service.test.ts` 2-arg ctor mismatch + `round-loop.service.test.ts` maxNonce-missing + `get-ws-snapshot.use-case.test.ts` clock-mock failures, none touch the new code paths); ROUND_TICK integration `bun test tests/integration/multiplier-broadcast-round-tick.test.ts` 4/4 pass in ~1.6s (ROUND_TICK shape correct + 30Hz cadence preserved under async listener with mean tick interval 23-43ms over 1s + listener-throw does not stop the loop + silent drop when getMultiplierAt throws); `bunx tsc --noEmit` exit 0 in services/games; auto-cashout-tick integration test + SC1 E2E compile cleanly and skip without `INTEGRATION=1` (live execution requires `bun run docker:up` + INTEGRATION=1). Three deviations all within auto-fix rules: Rule 3 (E2E filename `.e2e.ts` → `.e2e.test.ts` so Bun's `bun test` runner matches it — the `package.json` `test:e2e` script is a directory walk so the rename doesn't affect any other path), Rule 3 (the plan's read-first claim "EventEmitter2 already in MultiplierBroadcastService ctor from Phase 6 P6.05" was incorrect — production uses ModuleRef to break the RoundLoop ↔ MultiplierBroadcast cycle; added EventEmitter2 as a NEW direct injection alongside ModuleRef and kept the cycle-break intact), Rule 1 (plan example `Math.floor(2.05 * 100) = 205` is incorrect in IEEE-754 since `2.05 * 100 === 204.99999999999997` → adjusted the unit test to use `multiplier=2.5 → ceiling=250` so the formula assertion doesn't rely on the broken example; the implementation matches `Math.floor(payload.multiplier * 100)` literally as the plan specified). Threat-model dispositions all verified mitigated: T-09-20 (acceptedAt drift — locked by source-inspection regex), T-09-21 (target-not-tick — unit test asserts `arg.multiplier === bet.autoCashoutTarget` and `!== 205` when tick=2.05 with target=2.00), T-09-22 (broadcast-loop block — `@OnEvent({ async: true })` + sibling try/catch + 30Hz cadence integration test), T-09-23 (TOCTOU — CashOutUseCase RUNNING guard + caught RoundNotRunningError), T-09-24 (repudiation — server-clock acceptedAt with bet correlation context), T-09-25 (accepted — demo creds already in public README). No auth gates; no architectural change (Rule 4 not triggered — locked Option B architecture from RESEARCH §Summary executed verbatim). Plan 09-05 closes REQ-AUTO-01 (Done; SC1 proof recorded as the automated artifact for ROADMAP Phase 9 success criterion 1) and unblocks Plan 09-08 (FE AutoBetForm — server-side enforcement is now guaranteed so the FE just submits `autoCashoutTarget` via the existing POST /games/bet body) + Plan 09-10 (ADR-033 candidate "server-enforced auto-cashout architecture" locks the locked Option B). Previous: P09-04 complete (4/10) — leaderboard read-model foundation landed via four atomic commits (`0a3b5df` RED snapshot diff unit test, `d916e6b` GREEN migration + EntitySchema + LeaderboardSnapshot pure VO, `2fbbeaf` RED integration tests for MikroLeaderboardRepository, `8b703ea` GREEN port + Mikro impl + DI token + GameCoreModule wiring). New table `leaderboard_24h (player_id UUID PK, net_profit_cents BIGINT, win_count INT CHECK >= 0, total_bet_count INT CHECK >= 0, last_settled_at TIMESTAMPTZ)` + composite index `(net_profit_cents DESC, player_id)` — NOT partial, because Postgres requires IMMUTABLE predicates and `NOW() - INTERVAL` is not (window filtering happens at query time via `WHERE last_settled_at > NOW() - ((? || ' hours')::INTERVAL)` — parameterized to kill T-09-14 SQL injection). `LeaderboardRepository` port exposes `applyCashedOut/applyRefunded/applySettledLoss/fetchTopN/fetchSnapshot`; `applyRefunded` is a no-op per Pitfall 8 + RESEARCH Q1 (refunds don't bump the ledger, don't touch last_settled_at, don't increment total_bet_count). Mikro impl uses Postgres `INSERT … ON CONFLICT (player_id) DO UPDATE SET … += EXCLUDED.…` UPSERTs and raw `em.getConnection().execute(..., 'run', em.getTransactionContext())` so the 09-06 projector's `@IdempotentSubscribe` txEm flows through cleanly. Phase 6 Pitfall 1 enforced: `fetchTopN` hydrates `last_settled_at` through a `toDate()` helper before exposure (verified by integration test #8 — `lastSettledAt instanceof Date === true`). `LeaderboardSnapshot.diff(before, after)` pure VO compares ordered playerId arrays only — `netProfitCents` deltas are explicitly NOT part of the equality test (Q12 + RESEARCH recommendation — the throttle only fires when ranks actually shift). `LEADERBOARD_REPOSITORY` DI token added to `tokens.ts`; `MikroLeaderboardRepository` wired into `GameCoreModule.providers` + exports; `Leaderboard24hEntitySchema` registered via `MikroOrmModule.forFeature` so 09-06 can resolve immediately. Verification: 8/8 unit tests pass for snapshot diff (~6ms); 8/8 integration tests pass against real Postgres (~715ms) gated on `INTEGRATION=1 RABBITMQ_URL=… DATABASE_URL=…`; full games unit suite 254 pass / 8 fail (8 baseline failures unchanged from Plans 09-01..03 — RoundLoopService maxNonce-missing + MultiplierBroadcastService + GetWsSnapshotUseCase clock-mock failures, none touch leaderboard code); `bunx tsc --noEmit` exit 0; migration applied via `docker compose run --rm games-migrate` (`Applied '20260530002-create-leaderboard-24h'`); live `\d leaderboard_24h` confirms PK + composite DESC index + both CHECK constraints. Zero deviations: plan executed exactly as written (migration filename uses timestamp-only convention per the directory's established pattern, matching sibling `20260530001-add-auto-cashout-target-to-bets.ts` from Plan 09-02 — class is still `Migration20260530002 extends Migration`); module wiring of the new EntitySchema + provider is foundation-side correctness (defense-in-depth so 09-06's `@Inject(LEADERBOARD_REPOSITORY)` resolves immediately rather than at the 09-06 boundary), not a consumer hookup. No auth gates. Threat-model dispositions: T-09-14 (SQL injection via windowHours) mitigated via parameterized `($N || ' hours')::INTERVAL` cast — `grep "INTERVAL '"` returns 0 matches; T-09-15 (negative win/bet counts) mitigated by CHECK constraints; T-09-16 (playerId leak) deferred to 09-07 controller (repository deals in branded PlayerId); T-09-17 (large TOP_N DoS) accepted (DESC index handles LIMIT in microseconds). Plan 09-04 unblocks Plan 09-06 (LeaderboardProjectorService can now `@IdempotentSubscribe` on `bet.cashed_out`/`bet.refunded`/`bet.lost`/`round.settled` and call `applyCashedOut`/`applyRefunded`/`applySettledLoss`, then `fetchSnapshot` before/after, then gate `leaderboard:updated` emit on `LeaderboardSnapshot.diff(prev, next).changed`) and Plan 09-07 (LeaderboardController `GET /games/leaderboard?window=24h` calls `fetchTopN(env.LEADERBOARD_TOP_N, { windowHours: env.LEADERBOARD_WINDOW_HOURS })` and masks playerId at the controller boundary). REQ-LEAD-01 + REQ-LEAD-02 + REQ-LEAD-03 contributing (not Done — closure in Plan 09-06 + 09-07 when the projector populates and the controller exposes). Previous: P09-03 complete (3/10) — per-bet `bet.lost` outbox events on crash sweep — landed via five atomic commits (`c46ab55` RED contracts test, `e4e86cb` GREEN bet.lost schema export, `e4f4949` RED CrashRoundUseCase unit test, `fc52afe` GREEN per-bet em.transactional + outbox.add inside CrashRoundUseCase.sweepActiveBetsToLost, `d619751` integration suite gated on INTEGRATION=1). Plan target (SettleRoundUseCase) was path-corrected to CrashRoundUseCase as Rule 3 deviation: ACTIVE→LOST transitions live in CrashRoundUseCase.sweepActiveBetsToLost (Phase 4), not SettleRoundUseCase which only does CRASHED→SETTLED + seed reveal. Publishing at the FSM transition point preserves the same-TX invariant the plan demands and matches the CashOutUseCase precedent byte-for-byte; routing key + payload shape + envelope structure all match the plan spec. Per-bet em.transactional wraps each `tryTransition + outbox.add` so the bet row mutation and outbox row land atomically without coupling N bets into one large TX (one-aggregate-per-TX per CLAUDE.md §Domain layer; matches CashOutUseCase pattern). New @crash/contracts export: `BET_LOST_EVENT_TYPE = 'bet.lost' as const` + `betLostEventSchema` (strict z.object: betId/roundId uuid, playerId min(1), amount moneySnapshotSchema, settledAt datetime) + `type BetLostEventV1` — payload mirrors the wallet payload shape (Money discipline: amount is moneySnapshotSchema never number; integration test parses the persisted row through the schema to prove wire shape). buildEnvelope override `messageId: correlationId` aligns messageId === correlationId === causationId for flow-origin emit (Phase 2 convention). Race-tolerant: when tryTransition returns null (concurrent transition won) the outbox publish is skipped, preserving one-row-per-LOST-bet invariant. Verification: contracts 37/37 pass (was 30, +7); games unit 246 pass / 8 fail (was 240/8; +6 from this plan, 8 baseline unchanged — same RoundLoopService maxNonce-missing + MultiplierBroadcastService + GetWsSnapshotUseCase clock-mock failures documented in Plan 09-02 STATE entry); `bunx tsc --noEmit` exit 0 in both packages/contracts and services/games; integration test compiles + skips cleanly without INTEGRATION=1 (2 cases — one-LOST-bet-emits-one-row with payload validated through betLostEventSchema, and zero-ACTIVE-bets-emits-zero-rows delta probe). Four deviations all within auto-fix rules: Rule 3 path correction (SettleRoundUseCase → CrashRoundUseCase per actual code location), Rule 2 defense-in-depth (wrap each bet in em.transactional to honor same-TX outbox invariant which did not previously apply), Rule 3 test harness sync (round-loop.service.test.ts ctor update to new 4-arg signature with stub em+outbox), Rule 3 filename (integration test renamed crash-sweep-bet-lost-events.test.ts to reflect actual use case). No auth gates; no architectural change (Rule 4 not triggered — same routing key, same exchange, same envelope, just published from the correct use case). Threat-model dispositions mitigated: T-09-10 redelivery dedupe deferred to Plan 09-06 inbox layer; T-09-11 spoofing accepted within docker-network trust boundary; T-09-12 DoS bounded by partial-unique-index bets_one_active_per_player; T-09-13 TX-retry double-publish prevented by per-bet em.transactional + fresh messageId per emit. Plan 09-03 unblocks Plan 09-06 (LeaderboardProjectorService can now @IdempotentSubscribe({ exchange: 'game.events', routingKeys: ['bet.lost'] }) and debit losing amounts from net_profit_cents — zero new topology config needed because game.events exchange already exists in Phase 2 topology-defaults). REQ-LEAD-01 + REQ-LEAD-02 contributing (not Done — closure in Plan 09-06 when projector actually consumes the events to populate leaderboard_24h read model). Previous: P09-02 complete (2/10) — autoCashoutTarget end-to-end on the Bet aggregate (domain field + repository port + EntitySchema column + migration + DTO refine + use case wiring + indexed query method) landed via four atomic commits (`4c82e1a` RED unit tests, `816f0f0` GREEN domain + DTO + use case + repo port + EntitySchema + repo impl, `57f9d34` migration add bet.auto_cashout_target_centi_x INT NULL + partial index `idx_bets_auto_cashout_candidates` ON (round_id, auto_cashout_target_centi_x) WHERE status='ACTIVE' AND auto_cashout_target_centi_x IS NOT NULL + CHECK auto_cashout_target_centi_x IS NULL OR >= 101, `dc4cd05` integration coverage 6/6 against real Postgres). DB-level CHECK >= 101 was added as Rule-2 defense-in-depth alongside the Zod `.min(env.AUTO_BET_MIN_TARGET_CENTI_X / 100)` refine — both gates pull from the same env constant set in Plan 09-01. `findAutoCashoutCandidates(roundId, ceilingCentiX): Promise<Bet[]>` uses the EntitySchema `em.find(BetEntitySchema, { roundId, status: 'ACTIVE', autoCashoutTargetCentiX: { $ne: null, $lte: ceiling } })` path — MikroORM auto-hydrates `createdAt` as a Date, sidestepping Phase 6 Pitfall 1 entirely; the existing `tryTransition` raw-SQL RETURNING * path keeps its `toDate()` coercion and now also maps the new `auto_cashout_target_centi_x` column via `Multiplier.fromTenThousandths(BigInt(row.auto_cashout_target_centi_x) * 100n)`. Domain purity preserved: `grep -RE "@nestjs\|@mikro-orm" services/games/src/domain` returns 0 lines. Verification: plan 09-02 unit tests 43/43 pass across 3 files (~340ms); plan 09-02 integration tests 6/6 pass against real Postgres (~17.4s; requires `INTEGRATION=1 RABBITMQ_URL=amqp://admin:admin@localhost:5672 DATABASE_URL=postgres://admin:admin@localhost:5432/games` host overrides for the testcontainer-less local boot path); full games unit suite 240 pass / 8 fail (the 8 failures are the pre-existing baseline clock-mock failures in RoundLoopService + MultiplierBroadcastService + GetWsSnapshotUseCase, unchanged from the Plan 09-01 baseline — none touch Bet aggregate / DTO / use case / repository, all logged out-of-scope per the SCOPE BOUNDARY policy); `bunx tsc --noEmit` exit 0; migration runs cleanly up via `docker compose run --rm games-migrate` ("Successfully migrated up to the latest version"); live `\d bets` confirms column + partial index + CHECK constraint present. Two deviations both within auto-fix rules: Rule 3 path correction (plan said `Migration20260530001-…` PascalCase filename prefix but the existing migration directory uses timestamp-only `20260530001-…` per the sibling migrations from Phases 2/4/5/6 — used the directory's actual convention; the exported class is still `Migration20260530001 extends Migration` matching sibling naming); Rule 2 DB-level CHECK constraint added at migration time as defense-in-depth alongside Zod (no writable mutation path beyond initial Bet.place per T-09-08 mitigation; any future direct-INSERT or replication-patch bug still cannot insert an invalid centi-x). No auth gates; no architectural change. Threat-model dispositions all verified mitigated (T-09-05..09 — Zod + CHECK + partial-index physical filter + `private readonly` field + no setter exposed). Plan 09-02 unblocks Plan 09-05 (AutoCashoutTickService reads `findAutoCashoutCandidates` at 30Hz per tick — partial-index hot path) and Plan 09-08 (AutoBetForm submits `autoCashoutTarget: number` via the existing POST /games/bet body — DTO + bounds already wired). REQ-AUTO-01 contributing (not Done — closure happens in Plan 09-05 when AutoCashoutTickService actually fires cashouts; this plan is the foundation that makes that possible). Previous: P09-01 complete (1/10) — Phase 9 env layer (4 BE + 6 FE keys) landed via TDD across four atomic commits (`f7cbaac` RED BE schema tests + `e8e35ee` GREEN gamesEnvSchema extension + `dd8b5f7` RED FE schema tests + `b477b93` GREEN configSchema/AppConfig/buildConfig extension). Backend gained `LEADERBOARD_UPDATE_THROTTLE_MS=0` (nonneg int), `STOP_LOSS_CENTS_MAX=100000` (positive int), `STOP_WIN_CENTS_MAX=100000` (positive int), `AUTO_BET_MIN_TARGET_CENTI_X=101` (int .min(101) enforces 1.01x floor) on the existing Zod schema; both `services/games/.env.example` and root `.env.example` mirrored; `tests/setup.ts` synced so sibling tests don't drift from schema shape. Frontend gained `VITE_AUTO_BET_MIN_TARGET=1.01` (.gt(1)), `VITE_AUTO_BET_MAX_TARGET=100`, `VITE_LEADERBOARD_SIZE=10`, `VITE_LEADERBOARD_WINDOW_HOURS=24`, `VITE_LEADERBOARD_RELATIVE_REFRESH_MS=5000`, `VITE_RANK_UP_TRANSITION_MS=200`; `getConfig()` exposes nested frozen `autoBet.{minTarget,maxTarget}` + `leaderboard.{sizeN,windowHours,relativeRefreshMs,rankUpTransitionMs}` namespaces matching the Phase 8 replay/drawer/fairness precedent; `frontend/.env.example` mirrored. Floors enforced at parse time so an out-of-bound env crashes boot (BE) / build (FE) — never first request (T-09-01 mitigation). Verification: BE `bun test tests/unit/config-defaults-phase9.test.ts` 7/7 + existing `config-defaults.test.ts` 5/5 = 12/12 green; FE `bunx vitest run` 175/175 green (was 169, +6 from this plan); BE + FE `bunx tsc --noEmit` exit 0; lint clean (only pre-existing `routeTree.gen.ts` warning carried in deferred-items); `grep -RE "100000|1\.01x?\b"` against `services/games/src/{application,domain}` + `frontend/src/{components,features}` returns no matches, confirming no Phase 9 hardcoded constant leaked outside the env layer (C-3 NON-NEGOTIABLE). Two deviations both within auto-fix rules: Rule 3 path correction (plan frontmatter said `frontend/src/config.ts` but the real file lives at `frontend/src/lib/config.ts` per Phase 7/8 history — used the real path), Rule 2 test-setup sync (mirrored the 4 new BE keys into `tests/setup.ts` so the test env matches schema 1:1, Phase 1 convention). No auth gates; no architectural change. Plan 09-01 is the gate that lets all subsequent Phase 9 plans (09-02 autoCashoutTarget DTO refine, 09-04 leaderboard_24h table, 09-06 LeaderboardProjector throttle, 09-08 AutoBetForm bounds, 09-09 LeaderboardPanel cadences) pull configuration from env without re-deriving the schema. Previous: P08-10 complete (10/10) — phase closeout. Four Phase 8 ADRs authored at the next-free ADR-028..031 range (the ROADMAP's stale anticipated "ADR-023/024" labels for Phase 8 were reconciled because Phases 5/6/7 consumed 019..027 before Phase 8 closed; shipped ADRs are never renumbered): ADR-028 (D-04 client-seed-deterministic reaffirmation — `deriveClientSeed(prev.id, prev.crashedAt)` byte-stable from Phase 4, player-contributed seeds explicitly out of v1; the drawer chain-proof + `/verify` route stand on this determinism), ADR-029 (D-05 driver-injection seam — one `drawCurve` paints both live and replay; `useRafCurve` accepts an optional `RafCurveDriver`; `<CrashCurve />` gains an optional driver prop; Phase 7 callers zero-diff; byte-match E2E provable in pure math), ADR-030 (D-06 browser-safe `@crash/contracts/provably-fair-browser` subpath gated by package.json exports map; `crypto.subtle` HMAC + SHA-256; two byte-encoding contracts locked: HMAC key = UTF-8 of hex string, chain proof = hex-decoded bytes; 2.94 oracle test enforces; `node:crypto` never reaches the Vite bundle), ADR-031 (D-02+D-03 ReplayModal mounted at `__root.tsx` over live game; auto-plays at 1x; 1x/2x/4x ToggleGroup over `getConfig().replay.speeds`; `VITE_REPLAY_SPEEDS=1,2,4` env-tunable; `buildConfig` hard-asserts the array contains 1 so the real-time-speed criterion cannot be silently violated; dual-rAF accepted with monitoring per Pitfall 3). Drawer migration deferred from 08-06 closed (commit `f4410a8`): `<a href={\`/verify/${id}\`}>` → typed TanStack `<Link to="/verify/$roundId" params={{ roundId }}>` for build-time type safety; drawer-in-isolation tests now wrap in a minimal memory router that registers `/verify/$roundId` as a stub; 169/169 frontend tests green (was 168, +1 regression test asserting the rendered `href` equals `/verify/round-prev`). Dual-rAF smoke check (08-07 Pitfall 3 — live `useRafCurve` running in parallel with the replay modal's `useRafCurve` driven by the replay driver) executed at the vitest layer because no live browser session is available in the executor: `bunx vitest run src/components/crash-curve.test.tsx src/features/replay/replay-driver.test.ts src/features/replay/determinism.test.ts src/components/replay-modal.test.tsx` runs all four rAF-related surfaces in one Vitest worker against jsdom — 23/23 PASS in ~2s with no rAF leak, no timer collision, no console error; the live rAF surface (use-raf-curve.ts cleanup on unmount, no setInterval, only requestAnimationFrame + cancelAnimationFrame) and the replay rAF surface (makeReplayDriver memoized on [roundId, growthRate, crashPoint], speed/paused read via `useReplayStore.getState()` per frame so speed clicks don't rebuild the stepper — Pitfall 7) are independently leak-free by code shape. Phase 8 ROADMAP success criteria observably true: (1) FairnessBadge always-visible in header during BETTING + drawer with `sha256OfHexEncodedSeed` via `crypto.subtle` verdict (REQ-FE-09 P08-05); (2) `/verify/$roundId` runs full HMAC + Bustabit derivation client-side ignoring server `matches` field (REQ-FE-10 P08-06); (3) Replay button on every history chip → modal reusing CrashCurve via driver injection (REQ-REPLAY-02 + REQ-REPLAY-03 P08-07); (4) determinism byte-match test green at the Phase 4 oracle (REQ-REPLAY-01 P08-08); (5) README recruiter walkthrough reproduces 2.94 from the locked tuple without docker (P08-09). REQUIREMENTS confirmed all 5 Phase 8 REQ-IDs Done with plan citations; v1-complete count incremented from 62/95 to 67/95 (REQ-FE-09 + REQ-FE-10 + REQ-REPLAY-01 + REQ-REPLAY-02 + REQ-REPLAY-03). Phase 8 is the last of the three parallelizable phases per ROADMAP — Phase 9 (Auto Features & Leaderboard) is the natural next plan and is parallelizable with this just-completed Phase 8. Previous: P08-09 complete (9/10) — Recruiter-runnable Provably-Fair verification walkthrough added to `README.md` between the ADR catalogue and the Roadmap section. One atomic commit (`7d5a086`) with 116 net insertions and zero deletions; preserves every existing README block. The new section uses the Phase 4 locked-byte tuple (`serverSeed = 0x0…01`, `clientSeed = "test"`, `nonce = 0`, `instantCrashBucket = 101`) as the anchor and ships three subsections: (a) a generic `curl $BASE/games/rounds/$ROUND_ID/verify | jq` + `openssl` + `python3` pipeline parameterised on any settled round id; (b) a fully hardcoded worked example with the documented expected output captured verbatim from a fresh-shell dry-run (`SHA2-256(stdin)= ec4916dd28fc4c10d78e287ca5d9cc51ee1ae73cbfde08c6b37324cbfaac8bc5` for Step A, `HMAC = a9aa7f591433757689bc898f5167109490f954c4d895901f65042c332b8c050b`, `first13 = a9aa7f5914337`, `intH = 2984795937260343`, `crashPoint = 2.94` for Step B); (c) a "Why two encodings of the same hex string?" explainer that documents the byte-for-byte semantics from `packages/contracts/src/provably-fair/derive-crash-point.ts` (HMAC key = UTF-8 of the 64-char hex string, matching Node `createHmac("sha256", serverSeed)`) and `generate-seed-chain.ts` (chain proof hex-decodes the seed via `xxd -r -p` before hashing, matching `createHash("sha256").update(seed, "hex")`) — plus the empirically-confirmed `3.02` failure mode if the two encodings are reversed (verified by running `-macopt hexkey:$SERVER_SEED` against the same fixture). Portability notes cover macOS LibreSSL `SHA2-256(stdin)= ` vs OpenSSL 3 `SHA256(stdin)= ` prefix differences (the `awk '{print $NF}'` filter normalises both), Debian/Fedora `xxd` packaging, and a `python3 binascii.unhexlify` busybox fallback that I verified produces the byte-identical `ec4916dd…` digest. Recruiter dry-run executed twice from a fresh shell against the literal copy-paste block — both runs landed `crashPoint = 2.94` matching the documented expected output (checkpoint self-approved per the prompt's `<dry_run_instruction>` clause; the worked example anchors REQ-FE-10 supplemental documentation and closes Phase 8 ROADMAP success criterion 5). Authored `frontend/src/features/replay/__fixtures__/locked-round.fixture.ts` exporting `LOCKED_ROUND` (serverSeed `0000...0001`, clientSeed `test`, nonce `0n`, bucket `101`, expectedCrashPoint `2.94`, growthRate `0.06`, gridSteps `60`, gridStepMs `16.6` — anchored to the Phase 4 oracle and the same tuple Plan 08-01 already locked at the contracts layer). Authored `frontend/src/features/replay/determinism.test.ts` with five tests: (1) seed-to-crashpoint anchor returns 2.94 with a HARD `node:crypto.createHmac` sync fallback when `globalThis.crypto?.subtle` is unavailable (NO `skipIf`, the anchor runs unconditionally so it cannot silently rot under a future jsdom regression); (2) live `multiplierAt(t, 0.06)` capped at 2.94 deep-equals `sampleReplayMultiplier({startedAtWall:0, speed:1, paused:false, ...}, t)` across the fixed grid (`toEqual` on arrays — the byte-match centerpiece); (3) speed↔time equivalence `sample(speed=k, t) === sample(speed=1, k*t)` for k in {2, 4} at every grid point (locks D-03 — speed only changes wall-clock rate, not the underlying simulation grid); (4) two independent driver states produce identical sample arrays (purity proof — `sampleReplayMultiplier` has no hidden state); (5) past `crashTimeMs(0.06, 2.94)` the sample equals 2.94 exactly (freeze cap enforced). 5/5 PASS in ~3ms; 168/168 frontend tests green (was 163, +5 from this plan); `bunx tsc --noEmit` exit 0; `bun run lint` clean (only pre-existing `routeTree.gen.ts` warning). The grid uses 60 steps × 16.6ms (≈ 1s of curve at one rAF frame interval) — chosen so the assertions cover the entire interesting region of the curve (from the t=0 floor of 1.0 through the 2.94 freeze cap, with crashTimeMs(0.06, 2.94) = 17981ms guaranteeing the full pre-crash range is sampled by the first ~60 steps × 16.6ms ≈ 1s — beyond the crash region the freeze cap test covers the post-crash branch). NOT rAF — Pitfall 7 (vsync drift makes rAF sampling flaky). REQ-REPLAY-01 closed; Phase 8 ROADMAP success criterion 4 ("deterministic-replay E2E reproduces a captured live round and asserts the rendered multiplier values byte-match the original frame samples") is now automated and green as a permanent regression gate.
- **Status**: Phase 6 ROADMAP success criteria observably true: (1) WS handshake rejects no-token connections at upgrade (smoke probe 40 PASS) + joins `lobby` + `user:{playerId}` (P6.03 + integration `ws-rooms.test.ts`); (2) server emits `round:tick` as `volatile.emit` at ~30Hz on a recursive `setTimeout` loop with `getMultiplierAt`-throw drop-and-continue (P6.04 + `ws-tick-volatile.test.ts`); (3) `round:snapshot` emitted per-socket on connect/reconnect with multi-tab parity (P6.03 + smoke probe 41 PASS + `ws-snapshot.test.ts`); (4) cashout-race property test hammers ±50ms around crash asserting exactly-one-of {valid payout, 409} never double/post-crash (P6.08 `cashout-race.property.test.ts`), `cashoutAcceptedAt` stamped at controller first line per ADR-023; (5) full event catalog (round lifecycle + tick + masked bet feed + private `bet:my_*`) wired via lifecycle `@OnEvent` (P6.05) + `WsBridgeConsumer` (P6.06). Live lifecycle-observation smoke probes (39/42/43/44) timing-deferred to Phase 7/10 — the WS path is functionally proven by probes 40/41 + the round loop cycling live.
- **Progress**: `▰▰▰▰▰▰▰▰▱▱` 8/10 phases complete

**Next action**: Phase 9 in progress (9/10). With P09-09 landed, the FE leaderboard surface is end-to-end live: GET `/games/leaderboard?window=24h` fetched via TanStack Query through Kong → JwtGuard-protected endpoint (Plan 09-07) → projector-populated `leaderboard_24h` table (Plan 09-04); WS `leaderboard:updated` event fires on rank changes (Plan 09-06) → inline cache replace via `setQueryData` → top-N re-renders with masked playerId + Money net profit + rank-up 200ms transition. Own-row detection routes through the newly-shared `maskPlayerId` from `@crash/shared-kernel` (single algorithm across server + FE; cross-environment determinism test locks the contract). Right rail is now controlled shadcn `<Tabs>` (Live Feed default | Leaderboard with Trophy icon) preserving the Phase 7 D-01 grid byte-stable. REQ-LEAD-03 + REQ-LEAD-04 closed at this plan; combined with P09-05/06/07, all of REQ-AUTO-01 + REQ-LEAD-01..04 are now Done. Remaining plan: 09-10 (closeout — ADR-032/033/034 + STATE/ROADMAP/REQUIREMENTS rotation + Phase 9 verify/ui-review). Pre-existing baseline issue: every games integration test using `createTestGamesApp` currently fails at the `MultiplierBroadcastService` DI step on `main` — recorded in `deferred-items.md`. Pre-Phase-8 deferred actions remain pending: `/gsd:verify-phase 8` (goal-backward audit against the 5 ROADMAP success criteria) + `/gsd:ui-review` (UI-relevant phase per CLAUDE.md). Phase 7 verify/ui-review also remain pending. Pre-Phase-8 deferred actions remain pending: `/gsd:verify-phase 8` (goal-backward audit against the 5 ROADMAP success criteria) + `/gsd:ui-review` (UI-relevant phase per CLAUDE.md). Phase 7 verify/ui-review also remain pending. Before any live smoke / `bun run dev`: copy `frontend/.env.example` → `frontend/.env`; `docker compose restart kong` for the 07-03 `OPTIONS`/CORS additions so browser preflights (and the `protectedFetch` balance/history/verify calls) succeed; Keycloak realm `crash-game` + user `player/player123`. The recruiter can now reproduce 2.94 from the locked-byte tuple in any terminal without spinning up docker (P08-09 README walkthrough) — the recruiter-readable proof artifact for Phase 8 ROADMAP success criterion 5.

---

## Performance Metrics

| Metric | Value |
|--------|-------|
| Phases planned | 10 |
| Phases complete | 8 / 10 |
| v1 requirements mapped | 95 / 95 (100%) |
| v1 requirements complete | 72 / 95 (Phase 1-5 set + Phase 6 set: REQ-WS-01..07 + full Phase 7 set: REQ-FE-01..08 + REQ-FE-11..14 + REQ-AUTH-01/02/03 [all 15 delivered, P07-09 closeout confirmed] + full Phase 8 set: REQ-FE-09 [P08-05 FairnessBadge + VerificationDrawer with client-side SHA-256 verdict] + REQ-FE-10 [P08-06 /verify/$roundId route with client-side HMAC-SHA-256 + Bustabit recomputation; server matches/recomputedCrashPoint ignored] + REQ-REPLAY-01 [P08-08 determinism byte-match E2E at Phase 4 oracle] + REQ-REPLAY-02 [P08-07 ReplayModal at __root + history-chip openReplay] + REQ-REPLAY-03 [P08-07 ReplayModal mounts same `<CrashCurve />` via makeReplayDriver seam — draw-curve.ts byte-unchanged] + Phase 9 partial: REQ-AUTO-01 [P09-05 server-enforced auto-cashout + SC1 disconnect E2E] + REQ-LEAD-01 [P09-06 LeaderboardProjector populates leaderboard_24h via @IdempotentSubscribe on bet.cashed_out + bet.refunded + bet.lost] + REQ-LEAD-02 [P09-06 SC5 chaos test proves write-path independence from projector] + REQ-LEAD-03 [P09-07 GET /games/leaderboard with JwtGuard + 24h window + masked playerId; P09-09 FE TanStack Query consumer] + REQ-LEAD-04 [P09-06 WS leaderboard:updated emits the shape P09-07 exported via shared leaderboardUpdatedPayloadSchema; P09-09 FE LeaderboardPanel renders with WS live update]). |
| Stretch backlog items | 8 |
| ADRs landed | 31 (Phase 1: 6, Phase 2: 4, Phase 3: 3, Phase 4: 5, Phase 5: 2, Phase 6: 3, Phase 7: 4, Phase 8: 4) |
| ADRs anticipated | 30+ (Phase 1: 6, Phase 2: 4, Phase 3: 3, Phase 4: 5, Phase 5: 2, Phase 6: 3, Phase 7: 4, Phase 8: 4, Phase 9: 3, Phase 10: 3) |
| Critical pitfalls addressed pre-saga | 5 / 5 (C1-C5 covered in Phases 1-4) |
| Phases with UI hint | 3 (Phases 7, 8, 9) |

---

## Accumulated Context

### Decisions (locked at roadmap creation)

- **Mode**: standard — horizontal layers, not vertical slices. Foundation → outbox → wallet → game → saga → ws → frontend → ux polish → bonuses → quality. Justified by the cross-cutting nature of the messaging spine (one bug in Phase 2 cascades into every saga) and the need to prove DDD purity before integration.
- **Granularity**: standard (10 phases). Reflects research convergence; coarser would compress critical pitfalls together (e.g., bundling Wallet + Game core hides aggregate-boundary discipline), finer would fragment the saga integration unnaturally.
- **Parallelization windows**: Phase 3 ⫼ Phase 4; Phase 8 ⫼ Phase 9. All other transitions are sequential.
- **Stack locked** (from STACK.md, ARCHITECTURE.md): Bun 1.3.11+, NestJS 11.1.21, TypeScript 5.6 strict, MikroORM 7.1, PostgreSQL 18, RabbitMQ 4.2 quorum queues, Keycloak 26.5, Kong 3.9 DB-less, Socket.IO 4.8, Dinero.js v2 stable, TanStack Start 1.x, Tailwind v4, shadcn CLI v4, Zustand 5, TanStack Query 5, oidc-spa, `bun:test` + `fast-check` + Playwright 1.49.
- **Conflicts resolved** (from SUMMARY §8): `amqplib` for outbox publisher + `@golevelup/nestjs-rabbitmq` for consumer ergonomics; Dinero v2 wrapped in local Money VO; Bustabit-canon crash formula; single `lobby` room; 30Hz server tick + 60fps client rAF; light CQRS with no ES; bet-202 / cashout-200 asymmetry.

### Todos

- [x] Run `/gsd:plan-phase 1` (Foundation & Infra) — done; Phase 1 implementation complete
- [x] Run `/gsd:plan-phase 2` (Outbox/Inbox Messaging Spine) — done; Phase 2 complete (10/10 plans)
- [ ] Run `/gsd:verify-phase 2` (goal-backward audit against ROADMAP Phase 2 Success Criteria)
- [x] Run `/gsd:plan-phase 3` (Wallet Service) and `/gsd:plan-phase 4` (Game Core) — parallelizable after Phase 2 verifies; both complete
- [x] Run `/gsd:plan-phase 5` (Saga Integration) — done; Phase 5 complete (11/11 plans)
- [ ] Run `/gsd:verify-phase 5` (goal-backward audit against ROADMAP Phase 5 Success Criteria)
- [ ] Run `/gsd:secure-phase 5` (security-relevant phase per CLAUDE.md — saga + cross-service trust boundary + correlationId propagation)
- [x] Run `/gsd:plan-phase 6` (WebSocket Gateway & Multiplier Sync) — done; Phase 6 complete (10/10 plans)
- [ ] Run `/gsd:verify-phase 6` (goal-backward audit against ROADMAP Phase 6 Success Criteria)
- [ ] Run `/gsd:secure-phase 6` (security-relevant phase per CLAUDE.md — WS handshake JWT auth + cashout race trust boundary)
- [ ] Run `/gsd:plan-phase 7` (Frontend Vertical Slice) — Phase 6 closed, next phase
- [ ] Phase 7 carry-in: REQ-AUTH-01/02/03 (OIDC PKCE + silent renewal + BroadcastChannel multi-tab) deferred from Phase 6 — backend JWT-at-WS-handshake done (REQ-WS-01), frontend OIDC integration lands in Phase 7
- [ ] Phase 7/10 carry-in: WS smoke probes 39/42/43/44 redesign — replace timing-window assertions with event-driven waits; probe 39 should use socket.io-client not raw curl (see Phase 6 deferred-items.md)
- [ ] Phase 9 prep: resolve OD14 (auto-cashout max) — already defaulted at 100.00; awaiting user confirmation before Phase 9 freezes the constant
- [ ] Phase 10 follow-up: archival job for outbox PROCESSED rows + dead-letter replay endpoint
- [ ] Phase 10 follow-up: Prometheus `dead_letter_messages_count{service}` gauge per ADR-009 monitoring note
- [ ] Phase 10 follow-up: smoke probes 34-37 timing-sensitive assertions (deferred from P5.10 — see `.planning/phases/05-saga-integration/deferred-items.md`); accept LOST as valid post-saga terminal state OR observe `bet_saga_state.status='CONFIRMED'` in DB
- [ ] Phase 10 follow-up: integration test suite via testcontainers — 14/15 fail with broker connection refused (in-process app boots before broker tunnel ready); defer to harden under a `wait-for-broker` helper

### Blockers

- **RESOLVED (2026-05-28, P6.09)**: The games-container boot crash-loop is fixed. Three boot-blocking defects were resolved during P6.09 live bring-up: (1) `GetWsSnapshotUseCase` `Clock` DI crash — moved clock to a field initializer (commit `a957c8b`); (2) `RoundLoop ↔ MultiplierBroadcast` circular DI — `MultiplierBroadcastService` resolves `RoundLoopService` + `GameWsGateway` lazily via `ModuleRef` (commit `a957c8b`); (3) socket.io Bun http-attach incompatibility (`server.listeners is not a function`) — standalone Socket.IO server on `WS_PORT=4101` + websocket-only transport, Kong `~/ws/?$` → `games:4101` (commit `c026012`). Games boots healthy; WS handshake JWT-reject + snapshot-on-connect verified live.

### Open Configuration Values (env-driven, awaiting Phase 1 to materialize)

See `.planning/REQUIREMENTS.md` Open Configuration Values table. All 14 constants default per SUMMARY §7; ADR-004 will lock the source-of-truth file shape in Phase 1.

---

## Session Continuity

### Recent plan completions (Phase 5)

- **05-09 (2026-05-27)** — Saga integration test suite. 7 integration test files under `services/games/tests/integration/` covering REQ-TEST-03 (E2E API happy + error paths) and REQ-TEST-04 (kill-9 saga recovery). Commits 0839228 (place-bet + insufficient-funds + bet-outside-betting) → fc34c22 (timeout + compensation + cashout + double-cashout) → 8926de9 (true-SIGKILL drill). Compensation branch coverage uses AMQP binding manipulation (`amqplib.unbindQueue` / `bindQueue`) to detach the wallets consumer, force a saga timeout, then rebind so the late `wallet.debited` reaches `WalletDebitedHandler` TIMED_OUT branch → saga.COMPENSATED + outbox wallet.credit + balance restored. SIGKILL test invokes `spawnSync('docker', ['compose', 'kill', '-s', 'SIGKILL', 'games'])` per P4.11 pattern, talks to games via Kong because the in-process EM would die alongside the container. `bunx tsc --noEmit -p tsconfig.integration.json` clean. Live execution gated to plan 05-10 (smoke checkpoint). Three minor deviations (Rule 3/2): wallet provisioning routes through Kong instead of in-process app, scenario cleanup uses player-scoped DELETE instead of full TRUNCATE, double-cashout accepts third terminal code ROUND_NOT_RUNNING.
- **05-07 (2026-05-27)** — SagaTimeoutSweeper background driver for REQ-SAGA-03. 7 files changed (2 created, 5 modified), 5 tests added (36 expect), 165/165 unit suite green, tsc --noEmit clean. Commits 02d8fa4 (env var) → 571b129 (RED tests) → b1b4d2e (GREEN sweeper + module wiring). Recursive setTimeout (NOT setInterval per ADR-017 + Pitfall 6), OnApplicationBootstrap (NOT OnModuleInit per Pitfall 5), single em.transactional per tick claiming up to 100 expired DEBIT_PENDING sagas via FOR UPDATE SKIP LOCKED. Race-loss + defensive-skip branches both verified. Compensation path (late wallet.debited after TIMED_OUT) remains owned by WalletDebitedHandler from 05-05 — sweeper + compensation together close the slow-wallet failure mode. Live AMQP verification deferred to plan 05-09 scenarios #3/#4.
- **05-06 (2026-05-27)** — CashOutUseCase + POST /games/bet/cashout cashout endpoint. 6 files changed, 7 tests added (42 expect), 160/160 unit suite green, tsc --noEmit clean. Commits d38b6db (RED) → feb1695 (use case + 3 errors) → dd46013 (controller cashout method + DTO + module wiring). REQ-GAME-07 + REQ-SAGA-04 closed at the application layer. Server-clock authority verified: `const acceptedAt = new Date()` confirmed at line 69 of bet-command.controller.ts as the literal first executable line of the cashout method, before any await. Multiplier captured synchronously via `roundLoop.getMultiplierAt(acceptedAt)` (plan 05-02 artifact). Three new domain errors: RoundNotRunningError, NoActiveBetError, BetNotCashableError — each translated to discriminated 409s. translateError split into translatePlaceError + translateCashoutError to keep per-endpoint failure-mode dispatch bounded. Live HTTP + downstream wallet credit verification deferred to plan 05-09 integration scenario #5.
- **05-04 (2026-05-27)** — PlaceBetUseCase + POST /games/bet command controller. 8 files changed, 6 tests added (47 expect), 144/144 unit suite green, tsc --noEmit clean. Commits 1fd7847 (RED) → e2d54ac (use case + error) → 8525e09 (controller + DTOs + wiring). REQ-GAME-06 + REQ-SAGA-01 step 1 closed at the application layer. Live HTTP verification deferred to 05-10.

### Phase history

| Phase | Plans | Status | Notes |
|-------|-------|--------|-------|
| 1. Foundation & Infra | 10 / 10 plans landed (P1.10 smoke test green) | Implementation complete, verifier pending | All seven smoke probes pass cold and warm |
| 2. Outbox/Inbox Messaging Spine | 10 / 10 plans landed (P2.10 ADRs + closeout) | Implementation complete, verifier pending | `@crash/messaging-spine` wired into both services; 4 ADRs landed; 22/22 smoke probes; unit + integration tests green |
| 3. Wallet Service | 10 / 10 plans landed (P3.10 ADRs + closeout) | Complete | All 10 Phase 3 plans landed; 26/26 smoke probes; 15/15 integration tests; 10k fast-check property green; ADRs 011/012/013 authored |
| 4. Game Core (domain only) | 12 / 12 plans landed (P4.12 ADRs + closeout) | Complete | Round + Bet aggregates, MikroORM repos w/ partial unique index, seed-chain bootstrap, autonomous loop, four READ endpoints + JwtGuard, 8 integration tests (22 sub-tests), 32/32 smoke probes, CLI verifier MATCH, true-SIGKILL drill PASSED, five ADRs (014-018) |
| 5. Saga Integration | 11 / 11 plans landed (P5.11 ADRs + closeout) | Complete | `bet_saga_state` aggregate + persistence + SagaTimeoutSweeper (FOR UPDATE SKIP LOCKED), PlaceBetUseCase 202 + CashOutUseCase 200, WalletDebited/DebitRejected handlers w/ compensation branch, Kong PCRE POST routes, 7 integration scenarios + true-SIGKILL drill, 34/38 smoke probes (4 timing-deferred), two ADRs (019-020), `@Global()` MessagingSpineModule + DLX alignment lessons captured |
| 6. WebSocket Gateway & Multiplier Sync | 10 / 10 plans landed (P6.10 ADRs + closeout) | Complete | JwtVerifierService extraction (06-01), deps + WS_PATH env (06-02), JwtIoAdapter + GameWsGateway + GetWsSnapshotUseCase (06-03), MultiplierBroadcastService 30Hz volatile tick (06-04), EventEmitter2 lifecycle hooks (06-05), WsBridgeConsumer game.events bridge (06-06), Kong ~/ws route (06-07), 5 WS integration tests + cashout-race property (06-08), smoke probes 39-44 + live bring-up (06-09), ADRs 021/022/023 + closeout (06-10); WS handshake JWT-reject + snapshot-on-connect PASS live; standalone Socket.IO WS_PORT=4101 + websocket-only transport (Bun http-attach fix); three ADRs (021-023) |
| 7. Frontend Vertical Slice | 9 / 9 plans landed (P07-09 ADRs + closeout) | Complete | P07-01 pre-FE infra unblock + P07-02 spike (RATIFIED selective-ssr + single-getOidc) + P07-03 scaffold (TanStack Start :3000, dark-casino @theme, zod VITE_ config, shadcn 13-component set, Vitest harness) + P07-04 (oidc auth + enforceLogin guard + socket.io auth-function singleton + 5 Zustand slices + isolated multiplier store + ws-dispatch + TanStack Query; REQ-AUTH-01/02/03 + REQ-FE-07/08) + P07-05 (Money-VO bet validator + place-bet/cashout mutations + neutral BetPanel + accent CashoutButton + Countdown; REQ-FE-04/05/06) + P07-06 (Canvas 2D crash curve: localMultiplier via shared @crash/contracts multiplierAt anchored to roundStartedAt + reconcileOffset EWMA never-snap + useRafCurve rAF loop writing only the isolated multiplier store [D-06] + server-crashValue freeze + leak-free cancel + drawCurve dPR/clearRect emerald->cyan single-glow + crash-red freeze + CrashCurve ResizeObserver dPR sizing + reduced-motion + off-loop draw; REQ-FE-02/03) + P07-07 (live feed own-action highlight + color-banded history strip; REQ-FE-07/08 visual) + P07-08 (responsive D-01 assembly + skeletons + dedupedToast + four reduced-motion-gated juice moments; REQ-FE-12/13/14) + P07-09 (ADR-024..027 + STATE/ROADMAP/REQUIREMENTS rotation + config.ts env-cents money-rule resolution). All 15 Phase 7 REQ-IDs delivered; four ADRs (024-027); 65/65 FE tests green, lint clean, tsc exit 0 |
| 8. Provably-Fair UX, History & Replay | 10 / 10 plans landed (P08-10 ADRs + closeout) | Complete | P08-01 browser-safe `@crash/contracts/provably-fair-browser` subpath (`deriveCrashPointAsync` + `deriveCrashPointWithHmacHex` + `sha256OfHexEncodedSeed` + 2.94 oracle); P08-02 `/verify` endpoint extended with `bets[]` + `growthRate`; P08-03 `useRafCurve` `RafCurveDriver` seam (zero-diff Phase 7 callers); P08-04 `VITE_REPLAY_SPEEDS/AUTOSTART/DRAWER_SLIDE_MS` env + shadcn sheet/toggle-group/alert + `fairness.store` + `replay.store`; P08-05 FairnessBadge + HashBlock + VerdictChip + VerificationDrawer (Sheet at `__root.tsx`) + `useVerifyPrevious`; P08-06 `/verify/$roundId` route (ssr:false) + `useRecomputeCrashpoint` (ignores server `matches` field); P08-07 ReplayModal (Dialog) + replay driver + Play/Pause + 1x/2x/4x ToggleGroup + history-chip wiring; P08-08 determinism E2E byte-match test (REQ-REPLAY-01) at the Phase 4 2.94 oracle; P08-09 README "Provably Fair: Verify Outside the App" recruiter walkthrough reproducing 2.94 from the locked tuple without docker; P08-10 ADR-028 (D-04 client-seed deterministic reaffirmation), ADR-029 (D-05 driver-injection seam), ADR-030 (D-06 browser-safe contracts subpath + crypto.subtle with two byte-encoding contracts locked), ADR-031 (D-02+D-03 replay modal + 1x/2x/4x speed selector) at next-free numbers + STATE/ROADMAP/REQUIREMENTS rotation + deferred drawer `<a>` → typed `<Link>` migration closed + dual-rAF smoke check passed at the vitest layer; 169/169 FE tests green, 30/30 contracts tests green, all gates clean |
| 9. Auto Features & Leaderboard | 2 / 10 plans landed (P09-02 autoCashoutTarget Bet field) | In progress | 4 BE + 6 FE Phase 9 env keys typed and Zod-validated (P09-01); autoCashoutTarget end-to-end on Bet aggregate (domain + entity + migration + DTO refine + use case + repository port + `findAutoCashoutCandidates` indexed query) — Plan 09-05's tick-service foundation is in place (P09-02) |
| 10. Quality Hardening & Docs | — | Not started | Depends on Phase 9 |

### Recent activity

- **2026-05-30** — P09-09 (FE Leaderboard panel + shared maskPlayerId) COMPLETE (9/10). Eight atomic commits: `2173e3f` RED shared-kernel mask tests + `82ac1e7` GREEN move maskPlayerId to `@crash/shared-kernel/identity/mask-player-id.ts` + migrate 6 server consumers (4 use-cases + ws-bridge + gateway) + 1 unit test + DELETE legacy file; `41a69ec` RED useLeaderboard hook + fetchLeaderboard tests + `c3c0ac5` GREEN `frontend/src/features/leaderboard/{leaderboard-api,use-leaderboard}.ts` + ws-dispatch `leaderboard:updated` event slot with Zod safeParse gate; `636f40e` RED rank-chip + leaderboard-row + leaderboard-panel tests + `9b355b8` GREEN three components (`RankChip` 24×24 circle/24×32 rect with UI-SPEC §Color rank-color rule via data-rank-color attribute, `LeaderboardRow` 2-line layout with leading `+`/`−` Money + TrendingUp/TrendingDown icons, negative profit muted-foreground NOT destructive/red per UI-SPEC, rank-up transient `data-rank-up` attribute only on rank improvement, `LeaderboardPanel` skeleton/empty/error/list states + heading from env + refresh button 44×44 + footer setInterval gated by `isActive` + aria-live polite for own-rank-up); `eeda105` RED right-rail tab tests + `5f75ae0` GREEN `routes/index.tsx` controlled shadcn `<Tabs value={feedTab} onValueChange={setFeedTab}>` with Live Feed default + Leaderboard trigger (Trophy icon) + `LeaderboardPanel isActive={feedTab === 'leaderboard'}`. Own-row detection routes the OIDC `sub` claim through the shared `maskPlayerId` so the same SHA-256 hex prefix the server emits is recomputed client-side — the cross-environment determinism test at `packages/shared-kernel/tests/identity/mask-player-id.test.ts` locks the contract against fixture UUID `3f29b1a2-4d5e-6f7a-8b9c-0d1e2f3a4b5c` (T-09-61 mitigation). Verification: shared-kernel 5/5 pass, games tsc clean + 279/8 baseline preserved (pure import-path swap on server), frontend tsc clean + `bunx vitest run` 244/244 pass across 36 files in ~3.3s (was 217 after Plan 09-08; +27 from this plan), frontend lint clean (only pre-existing `routeTree.gen.ts` warning), all 5 plan greps clean (no `bg-destructive`/`text-destructive` in leaderboard-row, no hardcoded `5000`/`200` in leaderboard-panel, Zod `.parse(` present in leaderboard-api, no `replace(/-/g)`/`slice(0,8)` UUID-prefix mask shapes in panel, `@crash/shared-kernel` imported in panel, zero stale `mask-player-id` imports in `services/games/src`). Two Rule-1 test-DX-only deviations: `fireEvent.pointerDown + mouseDown + click` for Radix Tabs trigger in jsdom (mirrors Plan 09-08 bet-panel pattern); `findByText(new RegExp(masked))` substring matcher for masked-id-with-ellipsis row text. No Rule 2/3/4. No auth gates. Threat-model T-09-60..63 all mitigated and locked by unit tests / grep gates / inspection. REQ-LEAD-03 + REQ-LEAD-04 closed at this plan; v1-complete stays at 72/95 because the FE consumer was the closure for two REQ-IDs that were partially-Done after Plan 09-07. Plan 09-09 unblocks Plan 09-10 (closeout — ADR candidates "Shared `maskPlayerId` helper as cross-environment determinism gate" + "Right-rail tabbed surface with WS-driven inline TanStack Query cache replace for live read-side"; REQUIREMENTS + ROADMAP rotation; Phase 9 verify-phase + ui-review).
- **2026-05-30** — P09-02 (autoCashoutTarget end-to-end on the Bet aggregate) COMPLETE (2/10). Four atomic commits land the foundation Plan 09-05's AutoCashoutTickService reads from: `4c82e1a` (RED — failing unit tests for `Bet.place({...args, autoCashoutTarget})` null default + Multiplier.fromCentiX(200) round-trip + DTO bounds parsing against env.AUTO_BET_MIN_TARGET_CENTI_X/100 floor + env.AUTO_CASHOUT_MAX_X ceiling + use case Multiplier construction at centi-x precision), `816f0f0` (GREEN — `BetProps.autoCashoutTarget: Multiplier | null` field + `Bet.place(…, autoCashoutTarget = null)` factory signature extended + `get autoCashoutTarget()` getter; `BetRepository.findAutoCashoutCandidates(roundId, ceilingCentiX): Promise<Bet[]>` port method; PlaceBetRequestDto adds `autoCashoutTarget: z.number().positive().min(env.AUTO_BET_MIN_TARGET_CENTI_X / 100).max(env.AUTO_CASHOUT_MAX_X).optional()`; PlaceBetUseCase converts the float input to `Multiplier.fromTenThousandths(BigInt(Math.round(target * 100)) * 100n)` at the boundary; BetEntitySchema gains `autoCashoutTargetCentiX!: number | null` mapped to `auto_cashout_target_centi_x`; MikroBetRepository's `findAutoCashoutCandidates` uses `this.em.find(BetEntitySchema, { roundId, status:'ACTIVE', autoCashoutTargetCentiX:{ $ne:null, $lte:ceiling } })` — EntitySchema path → auto Date hydration → sidesteps Phase 6 Pitfall 1 entirely; both row mappers (`mapRowToAggregate` + raw-SQL `mapDbRowToAggregate`) read the column and construct `Multiplier.fromTenThousandths(BigInt(row.autoCashoutTargetCentiX) * 100n)`; the raw-SQL path keeps its existing `toDate()` coercion on `cashed_out_at` + `created_at`; controller passes `body.autoCashoutTarget` to the use case input), `57f9d34` (migration `20260530001-add-auto-cashout-target-to-bets.ts` — `ALTER TABLE bets ADD COLUMN auto_cashout_target_centi_x INT NULL CHECK (auto_cashout_target_centi_x IS NULL OR auto_cashout_target_centi_x >= 101)` + `CREATE INDEX idx_bets_auto_cashout_candidates ON bets (round_id, auto_cashout_target_centi_x) WHERE status = 'ACTIVE' AND auto_cashout_target_centi_x IS NOT NULL`; down() drops index then column in reverse order; the migration filename uses the directory's timestamp-only convention `20260530001-…` not the plan's mistyped PascalCase `Migration20260530001-…` prefix — sibling migrations at 20260524001..20260527001 all share the timestamp-only shape so this was a Rule-3 path correction not a deviation in behavior), `dc4cd05` (integration coverage — 6 cases against real Postgres via the existing `_helpers/app-factory.ts` boot: schema gate confirms `auto_cashout_target_centi_x` nullable INT exists; index gate confirms `idx_bets_auto_cashout_candidates` predicate matches `WHERE status = 'ACTIVE'::text AND auto_cashout_target_centi_x IS NOT NULL` via `pg_indexes.indexdef`; ceiling boundary 199/200/250 against a target-200 ACTIVE bet asserts inclusive `$lte`; second bet with `autoCashoutTargetCentiX=null` never returned; bets in PENDING/CASHED_OUT/LOST/REFUNDED never returned; `bet.createdAt instanceof Date === true` proves Phase 6 Pitfall 1 mitigation still active on the EntitySchema path). Verification: plan 09-02 unit tests 43/43 pass / 67 expect across 3 files (~340ms); plan 09-02 integration tests 6/6 pass / 21 expect against real Postgres (~17.4s; requires `INTEGRATION=1 RABBITMQ_URL=amqp://admin:admin@localhost:5672 DATABASE_URL=postgres://admin:admin@localhost:5432/games` host-side overrides because `services/games/.env` carries docker hostnames `rabbitmq:5672`/`postgres:5432` for in-container boot — same convention every prior integration test already uses); full games unit suite 240 pass / 8 fail (the 8 failures are pre-existing baseline clock-mock failures in RoundLoopService + MultiplierBroadcastService + GetWsSnapshotUseCase, unchanged from the Plan 09-01 baseline — none touch Bet aggregate / DTO / use case / repository; logged out-of-scope per SCOPE BOUNDARY policy); `cd services/games && bunx tsc --noEmit` exit 0; domain purity `grep -RE "@nestjs\|@mikro-orm" services/games/src/domain` returns 0 matches; migration runs cleanly via `docker compose run --rm games-migrate` ("Successfully migrated up to the latest version"); live `docker compose exec -T postgres psql -U admin -d games -c "\d bets"` confirms column + partial index predicate + CHECK constraint all present; no hardcoded `1.01` / `100` / `101` business literals in DTO / use case / domain — all references resolve through env. Two deviations both within auto-fix rules: Rule 3 path correction (plan said `Migration20260530001-add-auto-cashout-target-to-bets.ts` PascalCase prefix but the migration directory uses timestamp-only `20260530001-…` per the convention every sibling migration from Phases 2/4/5/6 follows; the exported class is still `Migration20260530001 extends Migration` matching sibling naming so MikroORM picks it up), Rule 2 DB-level CHECK constraint added at migration time as defense-in-depth alongside the Zod DTO refine (the Zod gate enforces 1.01x floor at HTTP boundary returning 400; the DB CHECK is the physical-storage backstop that any direct-INSERT bug, replication patch, or admin script could not bypass — T-09-08 mitigation strengthens from code-review-gate to schema-gate). No auth gates; no architectural change. Threat-model dispositions all verified mitigated (T-09-05..09): T-09-05 (forge target below 1.01x or above 100x) mitigated by Zod + CHECK; T-09-06 (forge NaN/non-numeric) mitigated by `z.number().positive()`; T-09-07 (cross-round leak) mitigated by parameterized `roundId` + physical partial-index filter; T-09-08 (writable mutation path) mitigated by ADD COLUMN + SELECT method only + private readonly field; T-09-09 (post-placement alteration) mitigated by `private readonly` + no setter + rehydrate-only path. REQ-AUTO-01 contributing (not Done — closure happens in Plan 09-05 when AutoCashoutTickService actually fires cashouts; this plan is the foundation that makes that possible). UNBLOCKS Plan 09-05 (AutoCashoutTickService — call `betRepository.findAutoCashoutCandidates(roundId, currentMultiplier.toCentiX())` once per tick against the partial index — expected O(log n + k) cost where k = matching ACTIVE bets with target ≤ current multiplier) and Plan 09-08 (AutoBetForm — submits `autoCashoutTarget: number` via the existing POST /games/bet body; DTO + bounds + use-case wiring are now in place, FE just needs the input field + `getConfig().autoBet.{minTarget,maxTarget}` for bounds). Next plans in the wave map: P09-03 (per-bet bet.lost outbox events) + P09-04 (leaderboard_24h table) — both Wave-1 and parallelizable with each other.
- **2026-05-30** — P09-01 (Phase 9 env layer) COMPLETE (1/10). Four atomic commits via TDD: `f7cbaac` (RED — 7 failing BE schema tests covering defaults + 1.01x floor + positive stops + nonneg throttle), `e8e35ee` (GREEN — gamesEnvSchema extended with `LEADERBOARD_UPDATE_THROTTLE_MS=0` nonneg int, `STOP_LOSS_CENTS_MAX=100000` positive int, `STOP_WIN_CENTS_MAX=100000` positive int, `AUTO_BET_MIN_TARGET_CENTI_X=101` int .min(101); `tests/setup.ts` synced; both `services/games/.env.example` and root `.env.example` mirrored with one-line comments matching existing style), `dd8b5f7` (RED — 6 failing FE schema tests covering defaults + .gt(1) floor + .positive() size + .nonnegative() refresh + frozen namespaces), `b477b93` (GREEN — `configSchema` gained `VITE_AUTO_BET_MIN_TARGET=1.01` .gt(1), `VITE_AUTO_BET_MAX_TARGET=100`, `VITE_LEADERBOARD_SIZE=10`, `VITE_LEADERBOARD_WINDOW_HOURS=24`, `VITE_LEADERBOARD_RELATIVE_REFRESH_MS=5000`, `VITE_RANK_UP_TRANSITION_MS=200`; `AppConfig` + `buildConfig` extended with nested frozen `autoBet.{minTarget,maxTarget}` + `leaderboard.{sizeN,windowHours,relativeRefreshMs,rankUpTransitionMs}` namespaces matching the Phase 8 replay/drawer/fairness precedent; `frontend/.env.example` mirrored). Verification: BE 12/12 config tests green (7 new + 5 existing), FE 175/175 green (was 169, +6 from plan); BE + FE `bunx tsc --noEmit` exit 0; lint clean (only pre-existing `routeTree.gen.ts` warning out of scope); the plan's verification grep (`grep -RE "100000|1\.01x?\b" services/games/src/{application,domain} frontend/src/{components,features} | grep -v test`) returns no matches — confirms no Phase 9 hardcoded constant leaked outside the env layer (C-3 NON-NEGOTIABLE). `.env.example` diff confirms exactly 4 BE keys × 2 files (root + games) + 6 FE keys, matching plan spec verbatim. Two deviations both within auto-fix rules (Rule 3 path correction: plan frontmatter said `frontend/src/config.ts` but the real file lives at `frontend/src/lib/config.ts` per Phase 7/8 history — used the real path the `<read_first>` block already pointed at; Rule 2 test-setup sync: mirrored the 4 new BE keys into `tests/setup.ts` so the test env matches schema 1:1 per Phase 1 convention). No auth gates; no architectural change. REQ-AUTO-01/02/03 + REQ-LEAD-01/03/04 contributing (not Done — closure happens in the consumer plans 09-02/05/06/07/08/09 where behavior ships; the env layer is a prerequisite). Next plan in the wave map is P09-02 (autoCashoutTarget field on Bet aggregate + entity + migration + DTO + PlaceBet use case + `findAutoCashoutCandidates` repo method) — it reads `env.AUTO_BET_MIN_TARGET_CENTI_X` for the Zod DTO refinement on the 1.01x floor. Wave 1 contains 09-01 (now done), 09-02, 09-03 (bet.lost outbox events), and 09-04 (leaderboard_24h table) — 09-02..09-04 can land in parallel because they touch disjoint files.
- **2026-05-30** — P08-10 (Phase 8 closeout) COMPLETE (10/10). Four Phase 8 ADRs authored at next-free numbers ADR-028..031 (the ROADMAP's anticipated "ADR-023/024" labels for Phase 8 were stale because Phases 5/6/7 consumed 019..027 before Phase 8 closed — shipped ADRs are never renumbered, so Phase 8's four decisions took ADR-028..031 with a renumber note added to ROADMAP per the Phase 7 closeout precedent). Six atomic commits: `f4410a8` (drawer `<a href>` → typed TanStack `<Link to="/verify/$roundId" params={{ roundId }}>` migration deferred from 08-06 — the drawer-in-isolation tests previously failed because they had no `RouterProvider`; wrapped the test setup with a minimal memory router registering both `/` and `/verify/$roundId` as stubs, promoted each render assertion to `findByText`/`findByRole` because TanStack `Transitioner` mounts a tick after `RouterProvider`, added one regression test asserting the rendered `href` equals `/verify/round-prev`; build-time type safety on the link target restored; full-page navigation behavior unchanged; 169/169 tests green, was 168, +1), `88eef6a` (ADR-028 client-seed deterministic reaffirmation — `clientSeed_N = SHA256(prevRound.id + ":" + prevRound.crashedAt.toISOString())` byte-stable from Phase 4; player-contributed seeds explicitly out of v1; the drawer chain-proof + `/verify` route + Plan 08-09 README walkthrough all stand on this determinism; cross-references ADR-015 + ADR-016 + ADR-030 + Plan 08-05/06/09 SUMMARYs), `41aaaef` (ADR-029 replay reuses production Canvas renderer via driver injection — one `drawCurve` paints both live and replay; `useRafCurve` accepts an optional `RafCurveDriver` from Plan 08-03; `<CrashCurve />` gains an optional `driver` prop from Plan 08-07; Phase 7 callers zero-diff; the byte-match guarantee REQ-REPLAY-03 + criterion 4 becomes a structural property of the renderer not a parallel-implementation discipline; rejects separate `<ReplayCurve />` and pre-sampled-array; cross-references ADR-025 + ADR-026 + ADR-031 + Plan 08-03/07/08 SUMMARYs), `15ac614` (ADR-030 browser-safe `@crash/contracts/provably-fair-browser` subpath + `crypto.subtle` — package.json `exports` map gates the FE-visible surface; `node:crypto` never reaches the Vite bundle; two byte-encoding contracts locked in JSDoc + the Plan 08-01 2.94 oracle test: HMAC key = UTF-8 of hex string [matches Node `createHmac("sha256", seed)`], SHA-256 chain proof = hex-decoded bytes [matches Node `createHash("sha256").update(seed, "hex")`]; empirically-confirmed `3.02` MISMATCH if the two encodings are reversed; rejects `js-sha256` and vite `node:crypto` polyfill; cross-references ADR-015 + ADR-028 + Plan 08-01/05/06/08/09 SUMMARYs), `ccde472` (ADR-031 ReplayModal over live game + 1x/2x/4x speed selector — Dialog mounted at `__root.tsx` mirroring the Plan 08-05 drawer mount precedent; auto-plays at 1x; speeds read `getConfig().replay.speeds` env-tunable via `VITE_REPLAY_SPEEDS=1,2,4`; `buildConfig` hard-asserts the array contains 1 so the real-time-speed criterion cannot be silently violated; speed only changes the wall-clock rate at which the renderer reads the same `multiplierAt(growthRate, t)` curve — Plan 08-08 Test 3 locks `sample(speed=k, t) === sample(speed=1, k*t)` at k∈{2,4}; rejects separate `/replay/$roundId` route and sibling right-edge drawer; Pitfall 3 dual-rAF accepted with monitoring + verified at this closeout; cross-references ADR-025 + ADR-026 + ADR-027 + ADR-029 + ADR-030 + Plan 08-04/07/08 SUMMARYs), planning-doc rotation commit (STATE.md 7/10 → 8/10 + position COMPLETE + metrics ADRs 27 → 31 + v1-complete 62 → 67 + Phase 8 history-table row Complete + recent-activity prepend; ROADMAP Phase 8 row "9/10 In progress" → "10/10 Complete" with the closeout date, Phase 8 details Plans subsection 9 [x] + 1 [ ] → 10 [x], Key Decisions block gains the ADR-renumber note pointing at ADR-028..031, footer rewritten Phase 7 → Phase 8 closeout summary; REQUIREMENTS Coverage summary 62/95 → 67/95 with the 5 new REQs listed in the increment, Phase 8 traceability table rows already Done from prior plan rotations + footer rewritten Phase 7 → Phase 8 closeout summary; ADR README index gains the Phase 8 section linking ADR-028..031 with one-line summaries + "Future ADRs" rewinds to ADR-032+ + Phase 8 line removed from anticipated catalogue). Dual-rAF smoke check (Pitfall 3 — live `useRafCurve` running in parallel with the replay modal's `useRafCurve` driven by `makeReplayDriver`) executed at the vitest layer because no live browser session is available in the executor: `bunx vitest run src/components/crash-curve.test.tsx src/features/replay/replay-driver.test.ts src/features/replay/determinism.test.ts src/components/replay-modal.test.tsx` runs all four rAF-related surfaces in one Vitest worker against jsdom — 23/23 PASS in ~2s with no rAF leak, no timer collision, no console error; the live rAF surface (use-raf-curve.ts cleanup on unmount, only `requestAnimationFrame` + `cancelAnimationFrame`, no setInterval) and the replay rAF surface (`makeReplayDriver` memoized on `[roundId, growthRate, crashPoint]`, `speed`/`paused` read via `useReplayStore.getState()` per frame so speed clicks don't rebuild the stepper — Pitfall 7) are independently leak-free by code shape. Verification: `cd frontend && bunx tsc --noEmit` exit 0; `bun run test` 169/169 green (was 168, +1 from the drawer Link migration regression test); `bun run lint` clean (only pre-existing `routeTree.gen.ts` unused-disable warning carried in deferred-items.md); `cd services/games && bunx tsc --noEmit` exit 0; `cd packages/contracts && bunx tsc --noEmit && bun test` 30/30 green. REQ-FE-09 + REQ-FE-10 + REQ-REPLAY-01 + REQ-REPLAY-02 + REQ-REPLAY-03 all Done; v1-complete 62/95 → 67/95; Phases complete 7/10 → 8/10; ADRs landed 27 → 31. Phase 8 is the last of the three parallelizable phases per ROADMAP (Phase 3 ⫼ 4 in past, Phase 8 ⫼ 9 in present); the natural next step is `/gsd:verify-phase 8` + `/gsd:ui-review` then `/gsd:plan-phase 9` (Auto Features & Leaderboard).
- **2026-05-29** — P08-02 (verify endpoint extended with bets[] + growthRate) COMPLETE (2/10). Backend foundation for the Phase 8 Replay overlay (REQ-REPLAY-02). Two commits: `3c01ec1` (Task 1: lifted the masked bet view schema into `services/games/src/presentation/dtos/round-bet-view.dto.ts` exporting `roundBetViewSchema` + `RoundBetView` — byte-identical to the previously inline `currentRoundBetSchema`, `.strict()` preserved; `current-round.dto.ts` re-imports it via `import { roundBetViewSchema } from "./round-bet-view.dto"` so `/current` and `/verify` share ONE source of truth, no shape drift; extended `verifyRoundSchema` with `bets: z.array(roundBetViewSchema)` + `growthRate: z.number().positive()` BEFORE `.strict()` so the additive change is non-breaking; added `BetRepository.findByRound(roundId): Promise<Bet[]>` to the domain interface with a JSDoc spelling out the no-status-filter contract; `MikroBetRepository.findByRound` mirrors `findActiveByRound` minus the status filter, ordered by `createdAt asc` for deterministic replay overlay rendering, reuses `mapRowToAggregate`; `VerifyRoundUseCase` constructor now injects `BetRepository` via the existing `BET_REPOSITORY` token (no new DI wiring; `GameCoreModule` already provides `MikroBetRepository`), loads every bet via `findByRound`, maps each through the SAME projection used by `GetCurrentRoundUseCase` — `playerIdMasked: maskPlayerId(bet.playerId)`, `amount: bet.amount.toSnapshot()`, `cashedOutMultiplier: bet.cashedOutMultiplier?.toNumber() ?? null`, `payout: bet.payout?.toSnapshot() ?? null`; returns `bets: RoundBetView[]` + `growthRate: env.GROWTH_RATE`; JSDoc on the return type documents the per-round growthRate-storage follow-up if production `GROWTH_RATE` ever changes), `05a72bc` (Task 2: extended `verify-round.use-case.test.ts` from 4 → 8 tests covering the Phase 4 locked-byte oracle [seed `0000...0001`, client `"test"`, nonce `0n`, bucket `101` → `crashPoint === recomputedCrashPoint === 2.94 && matches === true`] proving the extension is non-breaking against the Phase 4 anchor; mixed-status bets[] fixture constructed via the real `Bet` aggregate FSM — `place().confirm().cashOut(Multiplier.fromTenThousandths(15_000n))` / `lose()` / `refund("test-refund")` — covering CASHED_OUT/LOST/REFUNDED with masked playerIds asserted to match `/^[0-9a-f]{8}$/` AND never contain the source playerId; money snapshots asserted as `{amount, currency, scale}` objects never bare numbers; `750c` payout derived from `1.5 × 500c` bet; `growthRate === env.GROWTH_RATE` + positive + number; bet.status as one of the string enum values, never a DB integer; the four prior guard tests updated to inject the new `BetRepository` dependency via a `FakeBetRepo`). Verification: `bunx tsc --noEmit` exit 0; `bun test tests/unit/verify-round.use-case.test.ts` 8/8 green / 34 expect(); `bun test tests/unit/get-current-round.use-case.test.ts` 5/5 green (schema-lift regression check); full games unit suite 217 pass / 8 fail — the 8 failures are the documented baseline clock-mock failures unchanged from Plan 08-01, NOT caused by this plan. Acceptance grep clean (`roundBetViewSchema` + `growthRate` in verify DTO, `findByRound` in domain interface AND mikro repo). The existing CLI `packages/contracts/bin/verify-crash.ts` reads `expectedCrashPoint` from its OWN stdin payload and never parses the HTTP response — additive DTO extension is invisible to it (T-08-07 accept disposition verified). Zero deviations (Rule 1/2/3/4). Plan referenced `services/games/src/infrastructure/persistence/mikro-bet.repository.ts` but the file lives at `…/repositories/…`; updated at the real path and recorded as a path-of-record correction (NOT a deviation). Money snapshot shape was confirmed as `{amount, currency, scale}` (the Phase 3 VO carries `scale`); the snapshot test asserts all three. Open follow-up (deferred to a later plan, NOT a Phase 8 blocker): if production `GROWTH_RATE` is ever changed, add `rounds.growth_rate NUMERIC(8,6)` populated at `transitionFromBettingToRunning`, read from `round.growthRate` in the use-case instead of `env.GROWTH_RATE`, backfill old rounds from the deployment changelog. UNBLOCKS Plans 08-05 (FairnessBadge/VerificationDrawer if it ever needs bet markers from this endpoint), 08-06 (`/verify/$roundId` route consumes the full DTO including `growthRate`), 08-07 (`ReplayModal` overlays bet/cashout markers from `bets[]` and reads `growthRate` instead of env), 08-08 (determinism E2E reads `growthRate` from the response so it stays robust against future ops changes). Live curl smoke against the docker'd games service deferred to the Wave-1 closeout — the FE consumer plans will exercise the contract from the browser; to pick up the new fields locally: `docker compose build games && docker compose up -d games`. TDD note: Task 2 is `tdd="true"` but the implementation was already on disk from Task 1 (the two tasks are atomically coupled by the constructor-signature change); the RED phase is implicit (the prior 4 tests would fail to compile against the new two-arg ctor before the test file is patched) and the GREEN phase is observed by 8/8 passing — followed the repo's atomic-commit convention from Plans 07-05/06/07/08 of landing TDD tasks as single `test(...)` commits rather than discrete RED→GREEN pairs (flagged in SUMMARY §TDD Gate Compliance).
- **2026-05-29** — P07-06 (Canvas crash curve renderer) COMPLETE (6/9). Built the center-stage Canvas 2D curve + local multiplier compute + EWMA reconciliation (REQ-FE-02/03), the single most craft-judged surface. Two commits: `5756896` (Task 1: `features/curve/local-multiplier.ts` pure `localMultiplier(roundStartedAt, serverOffsetMs, now=Date.now())` → `multiplierAt((now + serverOffsetMs) - roundStartedAt, getConfig().growthRate)` importing the formula from `@crash/contracts/multiplier` so FE math == server math byte-for-byte [T-07-15, equals 1.0 at elapsed 0] + pure `reconcileOffset(prev, inst, alpha)=prev+alpha*(inst-prev)` EWMA that moves-toward-but-never-reaches the instantaneous offset in one step; `features/curve/use-raf-curve.ts` `useRafCurve(onFrame?)` running a `requestAnimationFrame` loop that, while `round.status==='RUNNING' && roundStartedAt!=null`, computes `localMultiplier(roundStartedAt, serverOffsetMs ?? 0)` and writes it via `useMultiplierStore.getState().setRendered` [ONLY that store, D-06], on `CRASHED` writes `round.crashValue` once then `cancelAnimationFrame` [server-authoritative freeze, T-07-16], with a `useRoundStore.subscribe((s,prev)=>...)` status-transition listener that re-arms the loop on RUNNING and cancels otherwise [imperative, render-independent], and a cleanup that unsubscribes + cancels [no leak, T-07-17]; the latest value is exposed via a `frameRef` so drawing never waits on a React re-render), `4d34c15` (Task 2: `features/curve/draw-curve.ts` pure `drawCurve(ctx, {width,height,dpr,multiplier,status,crashValue,reducedMotion})` calling `ctx.setTransform(dpr,0,0,dpr,0,0)` then `ctx.clearRect(0,0,width,height)` FIRST each frame [REQ-FE-02 clear-between-frames], building the multiplier→curve sweep, stroking a `createLinearGradient` emerald `#00FF85`→cyan `#22D3EE` with a SINGLE `shadowBlur`/`shadowColor` glow [zeroed when reducedMotion], rendering the frozen value in crash-red `#EF4444` on `CRASHED`, and drawing the central multiplier with `ctx.font="600 {px}px 'Fira Code', monospace"` [64px ≥768, 44px below]; concrete color strings + curve-geometry constants (margin ratio, sample count, max-multiplier scale, line width) are named consts local to this draw module only, growthRate/alpha never appear as literals in the feature; `components/crash-curve.tsx` `<canvas>` host that sizes `canvas.width/height = parent.clientW/H * devicePixelRatio` + CSS size via `ResizeObserver` [REQ-FE-02 dPR], reads `prefers-reduced-motion` from `matchMedia` into a ref, mounts `useRafCurve`, and draws inside the loop's `onFrame` callback reading `useRoundStore.getState()` + the css-size ref so the component does NOT re-render 60×/sec [D-06 extended to the pixel pipeline]). Verification: `bunx tsc --noEmit` exit 0; `bun run test` 38/38 green (config 7 + bet-amount 7 + crash-curve 5 + ws-dispatch 6 + cashout-button 3 + oidc-guard 4 + local-multiplier 6); `local-multiplier.test.ts` asserts `localMultiplier == multiplierAt` for [0,500,1000,3000,7500,15000]ms incl. elapsed 0 → 1.0, monotonic rise, and `reconcileOffset(0,1000,0.1)≈100` < 1000 + 50-step convergence > 900 never reaching 1000 + `reconcileOffset(0,100000,0.1)===10000` (no snap); `crash-curve.test.tsx` uses a call-recording getContext stub + a fake `requestAnimationFrame` queue [`flushFrames`] + stubbed `devicePixelRatio=2` to assert `clearRect` index < `stroke` index each frame, `canvas.width===800*2`/`height===600*2`, monotonic rise across frames, `renderedMultiplier===3.5` after `setCrashed({crashValue:3.5})`, and zero new ctx calls after `unmount()` (rAF cancelled); `grep -rE "createElement..svg|WebGL|getContext..webgl" src/features/curve src/components/crash-curve.tsx` empty (Canvas 2D only); `grep -c clearRect draw-curve.ts`=1; `grep -c devicePixelRatio crash-curve.tsx`=2; no `0.06`/`0.1` business literal in the curve feature; no deletions; no untracked files. ZERO deviations — plan executed exactly as written. No new package installs (T-07-SC honored — confetti lands in 07-08). One non-blocking issue resolved mid-Task-2 before commit: a stale `vi.advanceTimersByTime?.(0)` in the monotonic-rise test threw "Timers are not mocked" (no `vi.useFakeTimers()`); the line was unnecessary (rAF callbacks read `Date.now()` directly, successive flushed frames advance wall-clock) and was removed. TDD note: both `tdd=true` tasks landed as single `feat` commits carrying test+impl together rather than discrete RED→GREEN commits (no `test(...)`-then-`feat(...)` pair in `git log`) — flagged in SUMMARY §TDD Gate Compliance; all behaviors are test-covered. REQ-FE-02/03 closed. 07-08 mounts `<CrashCurve />` in the center stage and layers the crash-flash/freeze-overlay + confetti juice on top (freeze logic already done + reduced-motion honored).
- **2026-05-29** — P07-05 (bet panel + cashout + betting countdown) COMPLETE (5/9). Built the left-rail money controls (REQ-FE-04/05/06). Two commits: `2917997` (Task 1: `features/bet/bet-amount.ts` pure `parseBetAmount(raw)` returning `{ok:true, money:Money} | {ok:false, reason:'invalid'|'below-min'|'above-max'}` — strict `^\d+(\.\d{1,N})?$` pattern with the fraction length N derived from `CRD.exponent` and the cents radix from `CRD.base` so `-5`/`1e3`/`""`/`10.001` are rejected as `invalid` BEFORE `Money.of` can throw, then bounds via `Money.lessThan`/`greaterThan` against `Money.of(BigInt(config.bet.minCents/maxCents))` — no literal precision or bounds; `use-place-bet.ts` `useMutation` POSTing `{amount: money.toSnapshot()}` to `/games/bet` via `protectedFetch`, mapping 402→`insufficient-balance`/409,410→`bet-window-closed`/network,other→`network` as a typed `PlaceBetError`, flipping `bet.store.pending` on mutate+settle, no optimistic `myBet`; `use-cashout.ts` POSTing `{betId: myBet.betId}` to `/games/bet/cashout`, ok-or-409 resolves silently, else `CashoutError('network')`), `da4ed36` (Task 2: `components/cashout-button.tsx` selecting `renderedMultiplier` from the isolated multiplier store + `myBet`/`status`, gate `status==='RUNNING' && myBet?.status==='ACTIVE'` [returns null otherwise], accent `bg-accent text-accent-foreground` button [+ UI-SPEC `rgba(0,255,133,0.35)` glow, 44px touch target] rendering `Cash Out {renderedMultiplier.toFixed(2)}x · {payout.toString()}` where payout=`Money.fromSnapshot(myBet.amount).multiplyRounded(renderedMultiplier)` — D-06 isolation, display-only per T-07-13; `components/bet-panel.tsx` shadcn card + labelled input + NEUTRAL `secondary`+`border` Place Bet button enabled iff `BETTING && !myBet && !pending && parse.ok`, "Bet Active" label when a bet exists, reason copy in `muted-foreground` NOT red [D-02]; `components/countdown.tsx` reading `bettingEndsAt`+`status`, "Betting closes in {n}s" [ceil remaining] + thin `progress` bar on a 250ms interval cleared on unmount/status-change with the window length captured once at BETTING entry [snapshot-derived, no hardcoded duration], null when !BETTING). Verification: `bunx tsc --noEmit` exit 0; `bun run test` 27/27 green (config 7 + bet-amount 7 + ws-dispatch 6 + oidc-guard 4 + cashout-button 3); bet-amount test asserts `parseBetAmount("10.00").money.toSnapshot().amount==="1000"` + the six reason cases; cashout-button test asserts RUNNING+ACTIVE (1000 × 2.41) → enabled button containing `Cash Out 2.41x` + `24.10 CRD`, BETTING → no button, payout matches `/\d+\.\d{2} CRD/`; `grep -nE "100000|[^0-9]100[^0-9]" bet-amount.ts` empty (bounds from config); `grep -rE "#[0-9A-Fa-f]{6}"` on the 3 tsx empty (accent via theme tokens); `grep -c bettingEndsAt countdown.tsx`=7; `bunx eslint` on all new files exit 0 (money rule clean); no deletions. One Rule-1 deviation (CRD.base typed `bigint | readonly bigint[]` broke `**` — narrowed via `Array.isArray(CRD.base) ? CRD.base[0] : CRD.base` + defaulted the whole part). No architectural changes; no new package installs (T-07-SC honored). Out-of-scope discovery: `bun run lint` reports 2 pre-existing `@crash/no-number-for-money` errors in 07-04's `frontend/src/lib/config.ts:30` (`minCents`/`maxCents`) + 1 generated-file warning — `config.ts` untouched by 07-05, the validator consumes those cents only to build `Money.of(BigInt(...))` so no money is ever a `number` downstream; logged to `deferred-items.md`. REQ-FE-04/05/06 closed. 07-08 drops the three components into the responsive layout + renders the error-key toasts the hooks classify.
- **2026-05-29** — P07-04 (auth + socket + state plumbing) COMPLETE (4/9). Wired the data layer the game UI consumes. Two commits: `2d2611e` (Task 1: ONE `oidcSpa.createUtils()` instance per the 07-02 single-getOidc ratification — `auth/oidc.ts` exports `useOidc`/`getOidc`/`enforceLogin`/`OidcInitializationGate` + a lazy `bootstrapAuth()` calling `bootstrapOidc({implementation:"real", issuerUri, clientId, scopes:["profile","email"]})` from `getConfig().keycloak`; game route gets `beforeLoad: enforceLogin` keeping `ssr:false`; `ws/socket.ts` lazy `getSocket()` singleton `io(ws.url,{path:"/ws",transports:["websocket"],auth:(cb)=>resolveAuthToken().then(cb)})` reading `getAccessToken()` per call — fresh token every reconnect, never cached; `lib/api.ts` `apiFetch`+`protectedFetch` Bearer-injecting never-logged wrapper; `auth/oidc-provider.tsx` bootstraps then mounts `OidcInitializationGate`), `9f265c3` (Task 2: `lib/ws-payloads.ts` re-exporting the eleven `@crash/contracts/ws` schemas as the FE import surface; five slice-per-concern stores [`round`, `multiplier` isolated D-06, `wallet`, `feed` circular-buffer, `bet`] + a small `history` store; `stores/ws-dispatch.ts` `safeParse`s the matching schema once at the gate [drop+`console.warn` on failure] then routes to exactly one slice — `round:tick` runs the EWMA clock-offset reducer [`VITE_EWMA_ALPHA`] updating ONLY the multiplier store, `bet:my_cashed_out` flags celebration on the bet store + credits the wallet from the payout `MoneySnapshot` via `Money.fromSnapshot(...).add(...)`; `ws/use-game-socket.ts` mounted once in `__root.tsx` inside the authed `OidcProvider` tree registers the full catalog + wires the connection-badge status store from socket lifecycle events + tears down on unmount; `features/wallet/use-wallet.ts` + `features/history/use-history.ts` TanStack Query hooks hydrate balance [cents-string→Money snapshot] + the last-N history [filtering null crash points], the latter exposing `prependCrash`). Verification: `bunx tsc --noEmit` exit 0; `bun run test` 17/17 green (config 7 + ws-dispatch 6 + oidc-guard 4); `grep -E "setInterval|setTimeout|BroadcastChannel" src/auth/oidc.ts` → NONE (REQ-AUTH-02/03 library-internal); socket auth is the FUNCTION form proven re-invocable token-1→token-2; `round:tick` branch references only `useMultiplierStore` + the test asserts round/feed/bet/wallet stores referentially untouched (D-06); two ticks converge offset 0→~100 toward a 1000ms instantaneous offset at alpha 0.1 (never snaps); `round:crashed` freezes `crashValue=payload.crashPoint`+history prepend; feed caps at the configured size marking own; invalid payload (`crashPoint:-5`) dropped+warned stores untouched; `bet:my_cashed_out` credits 60000→60250; `useGameSocket` referenced once functionally in `__root.tsx`; no deletions. Two deviations: Rule 2 (cashout CREDITS the wallet by the payout via `Money.add` instead of overwriting the balance with the winnings — overwriting would corrupt the mirror; authoritative balance still from `GET /wallets/me`), Rule 3 (added `VITE_FEED_BUFFER_SIZE`=50 + `VITE_HISTORY_SIZE`=20 to the typed config + `.env.example` to avoid hardcoded caps per CLAUDE.md §Configuration → updated the 07-03 `config.test.ts` fixture for the two new required keys). No architectural changes; no new package installs (threat T-07-SC honored). Decision: used the real oidc-spa v10.2.3 API (`oidcSpa.createUtils`→`enforceLogin`+`getOidc` with `getAccessToken()`), NOT the RESEARCH-assumed `createReactOidc`/`beforeLoadFn`/`getTokens` — drift was flagged in 07-02. REQ-AUTH-01/02/03 + REQ-FE-07/08 closed (data side). 07-05 (curve) + 07-06 (rails) render from these stores with no further plumbing.
- **2026-05-28** — P07-03 (FE scaffold) COMPLETE (3/9). Expanded the empty `frontend/` workspace slot into a booting **selective-SSR** TanStack Start app on :3000 (default `tanstackStart()`, game route `ssr:false`) on the 07-02-ratified stack (TanStack Start 1.168.14, React 19, Vite 8.0.14, Tailwind 4.3.0). Three commits: `f473417` (carry-forwards: `@crash/contracts/multiplier` + `/formula` browser-safe subpath exports so the FE imports the pure `Math.exp` curve math without dragging `node:crypto` into the bundle; `OPTIONS` added to all 8 browser-reachable Kong routes so CORS preflights stop 404ing, origins still scoped to :3000 + credentials), `bd4038e` (Task 1: `package.json` with `type:module` + scripts + font/supporting deps, `vite.config.ts` port 3000 + `@/*` alias, self-contained `tsconfig.json`, `getRouter` export, `globals.css` with the full UI-SPEC `@theme` token table + shadcn semantic layer + self-hosted Fira Code/Fira Sans, `lib/config.ts` zod-parsing every `VITE_` var into a frozen typed object, `.env.example`, `config.test.ts`; spike deleted), `b6ee856` (Task 2: `vitest.config.ts` + `src/test/setup.ts` jsdom canvas-stub + matchMedia mock, manual `components.json` + `shadcn add` of the 13 UI-SPEC components, themed `__root.tsx` header shell with reserved fairness/balance/connection slots + sonner Toaster, `index.tsx` `ssr:false` D-01 responsive skeleton). Verification: `bunx tsc --noEmit` exit 0; `bun run test` 7/7 green under jsdom; `curl /` → HTTP 200 with the server-rendered themed shell (`<html class="dark">`, title, `bg-background`/`bg-card` body+header, slot divs), no hydration hang, port torn down clean; `@theme` present with `00FF85`/`EF4444`; 11 `VITE_` vars in `.env.example`; 13 shadcn components; no hardcoded hex in route/component tsx; `grep -c OPTIONS kong.yml` = 10 (8 routes + 2 plugin lists); contracts tsc clean. Three Rule-3 auto-fixes: (1) eager module-level `config` parse crashed Vitest collection (`import.meta.env` lacks `VITE_` vars there) and would break SSR → replaced with a lazy cached `getConfig()`; (2) `vitest/config` bundles Vite 7 types clashing with the project's Vite 8 `@vitejs/plugin-react` → plugin array cast to `ViteUserConfig['plugins']`; (3) shadcn CLI added `next-themes` to the sonner wrapper the dark-only app cannot use → dropped the dep, hardcoded `theme="dark"`. The `radix-ui` + `tw-animate-css` shadcn transitive deps are official-registry and kept. REQ-FE-01/11/12 closed at the scaffold. Kong `OPTIONS` reload deferred to the phase live-smoke. No architectural changes, no scope creep. Throwaway `frontend/spike/` deleted (its durable output was the SPIKE-NOTES decisions).
- **2026-05-28** — P07-02 (Wave-0 de-risking spike) COMPLETE (2/9). The `checkpoint:decision` is RESOLVED — the user RATIFIED boot mode = **selective-ssr** (default SSR + game route `ssr:false`) and oidc = **single-getOidc** (one `oidc-spa@10.2.3` `createUtils` instance; socket consumes the built-in `getOidc()`). Both written as final `DECISION RATIFIED` lines in `frontend/spike/SPIKE-NOTES.md`; SUMMARY finalized + plan counter advanced. Throwaway `frontend/spike/` stood up on the live Bun 1.3.11 + Vite 8 stack to resolve the four highest-risk integration unknowns; four DECISIONs recorded in `frontend/spike/SPIKE-NOTES.md`. Commits `2bd7283` (boot + workspace-TS spike) and `15bf124` (oidc + CORS spike). Findings: (1) **boot mode** — both `spa:{enabled:true}` and default-SSR boot; the TanStack#5171 SPA-mode hang did NOT reproduce on the pinned versions (SSR 200 with content; SPA hydrated, verified by headless Chrome `--dump-dom`); recommended `selective-ssr` (game route `ssr:false`), `spa-mode` a valid alternative — TO RATIFY. (2) **workspace-TS** — `@crash/shared-kernel` Money + `@crash/contracts` `multiplierAt` transpile through Vite with ZERO config additions (dinero/bigint fine), BUT the `@crash/contracts` root barrel re-exports the provably-fair seed-chain/derive code which imports `node:crypto` → breaks the browser bundle; 07-03 must add a browser-safe `@crash/contracts/multiplier` subpath. (3) **oidc** — `oidc-spa@10.2.3` `react-spa` `createUtils` returns `useOidc` AND non-React `getOidc()` from ONE instance → `single-getOidc`, no dual-instance parity concern (Open Q1/A1 moot); multi-tab single-refresh is built-in via core `BroadcastChannel`; flagged API drift (real API is `oidcSpa.createUtils`, not the RESEARCH-assumed `createReactOidc`/`beforeLoadFn`) for 07-04. (4) **CORS** — live curl from `Origin: :3000`: public + authed GET (real `player/player123` Bearer, `balance.amount=60000 CRD`) return scoped `Access-Control-Allow-Origin: http://localhost:3000` + credentials, but every `OPTIONS` preflight 404s because each Kong route's `methods:` filter omits `OPTIONS` → 07-03 must add `OPTIONS` to every route's method list. Two Rule-3 spike fixes captured as 07-03 scaffold facts: TanStack Start v1 router must export `getRouter` (not `createRouter`); a non-default vite config name needs `"type":"module"` to load the ESM-only Start plugin.
- **2026-05-28** — P07-01 (pre-FE infra & shared-contract unblock) executed: three browser-tier blockers landed before any FE code. Commits `158b33e` (`feat(07-01): promote WS payload schemas to @crash/contracts/ws`) → `f26948c` (`feat(07-01): scoped Kong CORS plugin for the SPA origin`) → `cd186d9` (`feat(07-01): extend the money ESLint rule to .tsx files`). (1) The eleven WS payload zod schemas + inferred types relocated byte-for-byte into `packages/contracts/src/ws/ws-event.payloads.ts`, exposed at `@crash/contracts/ws` via the package `exports` map (`"./ws": "./src/ws/index.ts"`) + root barrel (`export * from "./ws"`); `services/games/src/presentation/dtos/ws-event.payloads.ts` is now a one-line `export * from "@crash/contracts/ws"` so server and browser share exactly one definition (T-07-02 mitigation) — existing relative-path consumers in the games service (game-events, get-ws-snapshot use case, game-ws gateway) keep working with zero call-site changes; `moneySnapshotSchema` becomes a relative `../money/snapshot` import inside contracts. (2) Per-service Kong `cors` plugin on the games + wallets REST services scoped to `http://localhost:3000` with `credentials: true`, methods `GET/POST/OPTIONS`, headers `Authorization/Content-Type`, `max_age: 3600` — never `*` with credentials (T-07-01 mitigation); WS `games-ws-service` block untouched (socket.io reflects origin at the handshake). (3) `eslint.config.js` money-rule files glob extended to `**/*.tsx` + `ecmaFeatures.jsx` enabled + `**/*.test.tsx` added to the rule-off test override (T-07-03 / REQ-DOM-06 frontend enforcement). Verification: `bunx tsc --noEmit -p packages/contracts/tsconfig.json` exit 0; `services/games` tsc clean; `grep -c cors` (non-comment) = 2 with origin present; `grep -c tsx eslint.config.js` = 2; `eslint --print-config frontend/Component.tsx` resolves the money rule to `2` (error), `Component.test.tsx` to `0` (off). No deviations. Out-of-scope discovery: 8 pre-existing games unit failures (213 pass / 8 fail) — `serverTime`/clock-mocking mismatches, verified identical at baseline `bf1100d` via `git stash` comparison; logged to `.planning/phases/07-frontend-vertical-slice/deferred-items.md`. Live `curl -i -X OPTIONS` CORS preflight verification deferred to the phase live-smoke gate per plan done-criteria.
- **2026-05-28** — P6.10 (Phase 6 closeout) executed: three ADRs authored — ADR-021 (single global `lobby` room over per-round rooms — every socket joins exactly `lobby` + `user:{playerId}` set once at `handleConnection` and never changed; per-round rooms rejected because ADR-017's single-round-at-a-time invariant means they would force a full membership reshuffle every ~7-15s for zero scoping benefit and race the snapshot-on-connect logic; `user:{playerId}` room name derived solely from the verified-JWT `sub` claim so a client cannot join another player's private room — T-06-07 mitigation; `WsBridgeConsumer` dual-emits each bet event masked to lobby + raw to `user:{playerId}`), ADR-022 (30Hz server tick + 60fps client interpolation — `MultiplierBroadcastService` emits `round:tick` every 33ms via `volatile.emit` on a recursive `setTimeout` loop pulling `RoundLoopService.getMultiplierAt(now)`, payload `{roundId, multiplier:number, t}`; client/Phase-7 renders at 60fps via rAF computing the multiplier locally from `e^(GROWTH_RATE*t/1000)` anchored to `roundStartedAt` and reconciling toward each tick via EWMA clock-offset tween; ~50% of 60Hz bandwidth with no smoothness loss per Gabriel Gambetta canon; rejects 60Hz server (2× bandwidth no gain), per-frame broadcast (~600× bandwidth), 10Hz (too coarse for EWMA + coarser than ±50ms cashout race); locks the standalone Socket.IO server on `WS_PORT=4101` + websocket-only transport from the P6.09 fix — engine.io `attach()` calls `server.listeners("request")` which Bun's `@nestjs/platform-express` server lacks, so the WS surface runs on a dedicated port whose engine.io-owned `node:http` server has `.listeners()`), ADR-023 (server-authoritative `cashoutAcceptedAt` at the HTTP controller's first executable line — `const acceptedAt = new Date()` at `bet-command.controller.ts:69` locked in Phase 5 P5.06 is the sole authority for cashout-vs-crash race resolution; REQ-WS-05's "gateway middleware" wording interpreted as the NestJS HTTP controller layer — a controller IS the HTTP entry gateway; cashout deliberately NOT migrated to a WS inbound message because a WS handler shares the event loop with the 30Hz tick broadcast loop, so the `acceptedAt` stamp would wait behind in-flight tick work and drift later under broadcast load, tightening the most race-sensitive operation; HTTP gives a clean `200`/`409` response surface ADR-020 locked; rejects WS inbound cashout message + client-supplied timestamp; P6.08 `cashout-race.property.test.ts` covers the ±50ms window across 50 fast-check cases; Phase 9 server-enforced auto-cashout reuses the same server-clock primitive). ADR catalogue README extended with the Phase 6 section linking ADR-021/022/023 with one-line summaries; "Future ADRs" rewound to ADR-024+; Phase 6 entry removed from anticipated catalogue. STATE + ROADMAP advanced to Phase 6 complete (6/10 phases). REQUIREMENTS traceability marked all seven Phase 6 WS REQ-IDs Done with plan citations (REQ-WS-01 → P6.01+P6.03, REQ-WS-02 → P6.03, REQ-WS-03 → P6.03+P6.05+P6.06, REQ-WS-04 → P6.03, REQ-WS-05 → Phase 5 P5.06 + codified P6.10 ADR-023 + P6.08 property test, REQ-WS-06 → P6.04, REQ-WS-07 → Socket.IO client default backoff + P6.03 snapshot-on-reconnect); v1-complete count incremented from 39 to 46. REQ-AUTH-01/02/03 deliberately kept Pending — frontend OIDC (PKCE, silent renewal, BroadcastChannel multi-tab) deferred to Phase 7 per the Phase 6 RESEARCH Deferred Ideas; backend JWT-at-WS-handshake is validated and done (REQ-WS-01 closes the backend auth surface, REQ-AUTH-04 was already done in Phase 3). Phase 6 is provably complete: 10/10 plans landed, three ADRs, games boots healthy after the P6.09 triple-fix (Clock DI field-initializer + RoundLoop↔MultiplierBroadcast ModuleRef lazy resolution + socket.io standalone-port Bun fix), live WS evidence captured at P6.09 (handshake JWT-reject probe 40 PASS, snapshot-on-connect probe 41 PASS via Kong with `socket.data.playerId` set, round loop cycling BETTING→RUNNING→CRASHED→SETTLED live), 5 WS integration tests + cashout-race property authored (live execution of timing-window probes 39/42/43/44 deferred to Phase 7/10 per `.planning/phases/06-websocket-multiplier-sync/deferred-items.md` — the WS path is functionally proven by probes 40/41 + the dedicated-port fix). Deferred-items.md created capturing the socket.io Bun standalone-port workaround, smoke probes 39/42/43/44 timing redesign, and WS integration broker-readiness.
- **2026-05-27** — P5.11 (Phase 5 closeout) executed: ADR-019 (orchestration over choreography — Game service owns `bet_saga_state` FSM with four canonical transitions `DEBIT_PENDING → CONFIRMED | REJECTED | TIMED_OUT → COMPENSATED`; Wallet is a passive participant processing `wallet.command.debit` / `wallet.command.credit` and emitting events back; justified by microservices.io ≥3-step+branching+timeout+compensation guidance + bet's identity as Game-domain concept; rejects Option A choreography for write-side flows because timeout+compensation logic would be distributed-FSM-by-polling; rejects Option C separate orchestrator service as scope inflation at challenge scale, documented as Phase 10 scale-out path; locks the `@Global()` `MessagingSpineModule` discovery from P5.10 fix as DI contract — `GameCoreModule` child-of-AppModule couldn't see `OutboxRepository` injected into use cases/handlers without global visibility; `SagaTimeoutSweeper` reuses ADR-017 recursive `setTimeout` + `OnApplicationBootstrap` pattern verbatim with `FOR UPDATE SKIP LOCKED` reservation of up to 100 expired `DEBIT_PENDING` rows per tick; saga is per-flow not per-system — Phase 9 leaderboard remains choreography), ADR-020 (bet placement asymmetry — `POST /games/bet` returns `202 Accepted` with `{betId, status:PENDING, roundId}` because the cross-service saga round-trip cannot complete synchronously without holding the request thread for seconds across at-least-two AMQP hops + two Postgres TX commits + two OutboxPublisher polls; `POST /games/bet/cashout` returns synchronous `200 OK` with `{multiplier, payoutCents, cashedOutAt}` because cashout is single-service single-TX < 100ms with `acceptedAt = new Date()` as the literal first executable line of the controller per REQ-WS-05 + Plan 05-06 line 69; rejects Option A both-synchronous because client-timeout vs at-least-once-delivery creates 504-then-409 retry traps; rejects Option B both-async because deferring cashout payout figure breaks the tactile feedback loop required by Phase 7 celebration animation; locks DLX alignment lesson from P5.10 fix as topology rule — cross-service consumer queues use the **source exchange's** DLX not the consuming service's DLX, e.g. games-service's `games.wallet-events.q` consuming `wallet.events` declares `EXCHANGES.WALLET_DLX`; the lesson applies to every future cross-service event subscription including Phase 9 leaderboard projector consuming `game.events` which will use `EXCHANGES.GAME_DLX`). ADR catalogue README extended with the Phase 5 section linking ADR-019 + ADR-020 with one-line summaries; "Future ADRs" rewound to ADR-021+; Phase 5 entry removed from anticipated catalogue. STATE + ROADMAP advanced to Phase 5 complete (5/10 phases). REQUIREMENTS traceability marked all 8 Phase 5 REQ-IDs Done with plan citations (REQ-GAME-06 → P5.04, REQ-GAME-07 → P5.06, REQ-SAGA-01 → P5.04+P5.05, REQ-SAGA-02 → P5.03, REQ-SAGA-03 → P5.07, REQ-SAGA-04 → P5.06, REQ-TEST-03 → P5.09, REQ-TEST-04 → P5.09); v1-complete count incremented from 31 to 39. Phase 5 is provably complete: 11/11 plans landed, two ADRs, live production saga trace captured at P5.10 (round dd098fbe: `bet_saga_state.status=CONFIRMED`, outbox `wallet.command.debit` published at t+1s, outbox `bet.active` published at t+2s), 34/38 smoke probes (probes 34-37 timing-sensitive deferred to verifier per `.planning/phases/05-saga-integration/deferred-items.md`), 7 integration scenarios including compensation branch via AMQP `unbindQueue`/`bindQueue` manipulation + true-SIGKILL drill via `docker compose kill -s SIGKILL games`. Concurrent doc-drift cleanup: struck `services/games/tests/unit/money-rounding-sanity.test.ts is failing` from `.planning/phases/04-game-core/deferred-items.md` (line 6) per research §Common Pitfalls flag — test verified green at P4.10 + P4.11 live run.
- **2026-05-26** — P4.12 (Phase 4 closeout) executed: ADR-014 (Bet-as-own-aggregate over nested-in-Round; per-bet micro-TX sweep over Round-lock contention), ADR-015 (Bustabit-canon HMAC `floor((100 * 2^52 - H) / (2^52 - H)) / 100` + 1-in-101 instant-crash bucket + per-round client seed `SHA256(prevRound.id + ':' + prevRound.crashedAt.toISOString())` + genesis static `GENESIS_CLIENT_SEED`; documented variant from Bustabit's fixed-public-salt canon; verified against vladignatyev/bustabit-rust), ADR-016 (1M hash chain pre-gen at first boot via `SeedChainBootstrap.OnApplicationBootstrap` over lazy-refill; immutable one-time commitment; ~80MB transient heap + ~80MB DB; W2 carry-forward: DB persists future seeds but aggregate boundary enforces reveal-only-after-settle, threat model = operator-trusted DB), ADR-017 (recursive `setTimeout` + `OnApplicationBootstrap` over `setInterval`/worker thread; race-free `crashAt - Date.now()` scheduling; five-branch `recoverInFlightRound` for kill -9 recovery; W4 evidence: live SIGKILL drill at P4.11 confirmed round 2b9d685e killed mid-RUN → restart → resumed same round → settled, zero orphans; single-process scope with documented `pg_try_advisory_lock` scale-out), ADR-018 (`Money.multiplyRounded(factor, mode = "bankers")` shared-kernel extension using `transformScale(product, currencyExponent, halfEven)` — Dinero v2 `multiply` is precision-preserving so extension required for REQ-DOM-07; aggregate is canonical consumer; 20k fast-check property pins loss-free invariant). ADR catalogue README extended with the Phase 4 section linking all five ADRs; "Future ADRs" rewound to ADR-019+; Phase 4 entry removed from anticipated catalogue. STATE + ROADMAP advanced to Phase 4 complete (4/10 phases). REQUIREMENTS traceability marked all 19 Phase 4 REQ-IDs Done with plan citations; v1-complete count incremented from 12 to 31. Phase 4 is provably complete: 12/12 plans landed, five ADRs, 32/32 smoke probes, 8/8 integration tests, FSM + provably-fair property tests green, CLI verifier byte-recomputes settled rounds.
- **2026-05-26** — P4.11 (live smoke run + true-SIGKILL drill) executed in Wave 7. Three commits — `90f8ef1` (`feat(04-11): add games smoke probes 27-32 (rounds, history, bets/me auth gates, seed chain, kong mutation block)`), `fc32e71` (`fix(04-11): unblock games boot — allowGlobalContext, audience alignment, constraint naming`), and `7b05fab` (`fix(04-11): correct seed-chain integrity invariants and BIGINT sort`). Ships six new probes 27..32 in `scripts/smoke-health.sh` covering Phase 4 surface end-to-end against Kong at :8000 (rounds/current with seedHash assertion, rounds/history with array shape, paired JwtGuard probes on /games/bets/me, seed_chain row count, Kong-origin 404 on POST /games/bet with body-match on `no Route matched`). Live evidence captured against `bun run docker:up` with HASH_CHAIN_LENGTH=1_000_000: `bun run smoke:health` → 32/32 PASS, CLI verifier `bun packages/contracts/bin/verify-crash.ts` returned `MATCH 5.56` on settled round 9204af58 (deriveCrashPoint replay against persisted seeds proves REQ-FAIR-04 end-to-end), true-SIGKILL drill on round 2b9d685e PASSED: the same round id survived `docker compose kill -s SIGKILL games`, reached SETTLED on restart, loop advanced to 05d49a9a, zero orphan in-flight rounds older than 60s (proves REQ-GAME-09 beyond Plan 04-10's in-process app.close() simulation). Unit + property + contracts suites: 118+4 unit/property green (48,023 expect()), 24/24 contracts green (34,096 expect()). One Rule 1 deviation discovered during live verification: `services/games/tests/integration/seed-chain-bootstrap.test.ts` used `nonce::text AS nonce` which coerced PG BIGINT to TEXT and sorted nonces lexicographically (`'10'` before `'2'`); asserted invariant `sha256(seed[i]) === hash[i-1]` mismatched Bustabit chain shape — corrected to `hash[i] === sha256(seed[i])` AND `sha256(seed[i+1]) === seed[i]` (commit 7b05fab). Pre-plan boot fixes shipped in commit fc32e71 (allowGlobalContext for MikroORM ContextManager, KEYCLOAK_AUDIENCE aligned to crash-game-client, bets constraint rename to bets_status_state_consistency_check). Integration suite isolation flakiness from frozen env at module-load time logged to deferred-items.md.
- **2026-05-26** — P4.10 (Phase 4 integration test suite) executed in Wave 6. Two commits — `cf2dfde` (`test(04-10): seed-chain bootstrap, autonomous loop, kill-9 recovery, and bet uniqueness integration tests`) and `f20fef7` (`test(04-10): verify-round, current-round, round-history, and player-bets integration tests`). Ships 8 integration test files (22 sub-tests) under `services/games/tests/integration/` plus two shared helpers (`_helpers/test-env.ts` for env seeding + Keycloak password-grant token fetch, `_helpers/app-factory.ts` for `Test.createTestingModule({imports:[AppModule]}).compile()` + truncate + polling primitives). Suite anchors REQ-GAME-09 kill -9 recovery (simulated via `app.close()` with documented SIGKILL caveat; true drill deferred to P4.11), REQ-DOM-02 partial unique index proven via raw SQL collision raising SQLSTATE 23505 on `bets_one_active_per_player`, REQ-FAIR-02 seed-reveal gate via both 400 ROUND_NOT_YET_SETTLED and current-round serverSeed null pre-SETTLED, REQ-FAIR-04 verify-endpoint determinism via independent `deriveCrashPoint` re-invocation against the response, plus the four READ endpoints with real Keycloak password-grant JWT for `/games/bets/me`. Two Rule-3 auto-fixes: (1) `@nestjs/testing@^11.1.21` added to `services/games` devDependencies — wallets had it, games never did, blocking the in-process app bootstrap; (2) `tsconfig.integration.json` created (extends base + re-includes `tests/integration`) because `bunx tsc tests/...` with file paths drops the project's `types: [bun-types, reflect-metadata]` resolution. Live execution gated to the Plan 04-11 smoke checkpoint (`bun run docker:up` required). `bunx tsc --noEmit -p tsconfig.integration.json` clean.
- **2026-05-26** — P4.06 (autonomous round loop + lifecycle use cases) executed in Wave 4. Two commits — `319eed8` (`feat(04-06): round lifecycle use cases and client seed derivation`) and `1ce4a2d` (`feat(04-06): RoundLoopService driving the autonomous BETTING->RUNNING->CRASHED->SETTLED loop`). Ships `RoundLoopService @Injectable implements OnApplicationBootstrap, OnApplicationShutdown` driving the autonomous round loop via recursive `setTimeout` (research Pitfall 1 + 6 + ADR-017). Five-branch `recoverInFlightRound` covers no-open / BETTING / RUNNING / CRASHED / SETTLED so `kill -9` mid-round resumes correctly: RUNNING recovery recomputes the crashPoint deterministically from the persisted client seed + seed-chain server seed; CRASHED recovery idempotently re-runs the bet sweep via `CrashRoundUseCase` before settling (research Pitfall 5). Four use cases own the actual aggregate transitions: `StartNewRoundUseCase` (genesis client seed = `sha256(GENESIS_CLIENT_SEED)`, per-round = `sha256(prevRound.id + ':' + prevRound.crashedAt.toISOString())` per ADR-015), `TransitionToRunningUseCase` (computes crashPoint in-memory, persists ONLY the status change — rounds_fsm_check requires crash_point_centi_x to be NULL during RUNNING), `CrashRoundUseCase` (Round CRASHED commits first; per-bet `tryTransition(ACTIVE, LOST)` sweep in its own micro-TX per ADR-014; PENDING bets deferred to Phase 5 saga timeout refund), `SettleRoundUseCase` (the only path that writes `rounds.server_seed` per REQ-FAIR-02 + stamps `seed_chain.revealed_at`). `client-seed.derivation.ts` is a pure function (no DI). `GameCoreModule` extended with five new providers (RoundLoopService NOT exported — internal driver). 1s backoff at bootstrap and at every scheduled step so a transient DB blip never permanently kills the loop. Timer typed as `ReturnType<typeof setTimeout>` for Bun/Node compatibility. Seven new unit tests for RoundLoopService cover every recovery branch + shutdown + bet sweep (in-memory fakes for the three repositories, no Postgres required). One Rule-3 self-correction during test write: CRASHED recovery branch initially only called `settleRound` — the bet-sweep test caught the gap, branch now re-invokes `CrashRoundUseCase` (idempotent on already-CRASHED rounds) before settling. Full suite: `bunx tsc --noEmit` clean, 118/118 unit tests pass / 310 expect(). REQ-GAME-01 (autonomous loop), REQ-GAME-09 (kill -9 recovery), REQ-DOM-01 (FSM orchestration via aggregate methods + transitionFromXToY repo methods) closed.
- **2026-05-26** — P4.08 (games REST READ surface — `GET /games/rounds/current`, `/history`, `/:roundId/verify`, `/games/bets/me`) executed in Wave 3 in parallel with P4.05. Two commits — `4bc535b` (`feat(04-08): GetCurrentRound, GetRoundHistory, VerifyRound, GetPlayerBets use cases`) and `44a3c8b` (`feat(04-08): rounds and bets read controllers with zod DTOs`). Four `@Injectable` use cases under `services/games/src/application/use-cases/` with limit clamping at the use-case layer, sha256-prefix masking of non-self playerIds in `/current`, and the REQ-FAIR-02 gate (`status !== "SETTLED"` → 400 `ROUND_NOT_YET_SETTLED`) duplicated in both `VerifyRoundUseCase` and `GetCurrentRoundUseCase.serverSeed` nullification. Four `createZodDto` response DTOs wire `moneySnapshotSchema` from `@crash/contracts` so bigint never crosses the JSON boundary. `RoundsController` mounts the three public routes; `BetsController @UseGuards(JwtGuard)` mounts `/games/bets/me`. AppModule extended with both controllers + `JwtGuard` provider + global `APP_PIPE = ZodValidationPipe`. `BetRepository` interface gained `countByRoundId(roundId): Promise<number>` (small surgery, extends Plan 04-03's interface) and `MikroBetRepository` implements via `em.count`. `GameCoreModule` (committed by P4.05) extended in place with the four use cases as providers + exports. Added `nestjs-zod 5.4.0` to `services/games` deps to match the wallets baseline. POST routes deliberately NOT scaffolded per research — Phase 5 ships `/games/bet` + `/games/bet/cashout` with the saga. 13 new unit tests for the four use cases; full games unit suite 111/111 green / 293 expect(). `bunx tsc --noEmit` clean. REQ-GAME-02/03/04/05 + REQ-FAIR-02/05 closed; absence-tested @Post/@Put/@Delete/@Patch grep returns empty across both new controllers. No Rule 1/2/3/4 deviations.
- **2026-05-26** — P4.05 (seed chain bootstrap + GameCoreModule wiring) executed in Wave 3 in parallel with P4.08. One commit `7abf606` (`feat(04-05): seed chain bootstrap with idempotent OnApplicationBootstrap`). Ships `SeedChainBootstrap @Injectable implements OnApplicationBootstrap` that checks `chain.countEntries() > 0n` before generating — defeats the regenerate-on-restart attack from research Pitfall 7. Generates `HASH_CHAIN_LENGTH` entries via `@crash/contracts/generateSeedChain`, inserts `(nonce, hash, seed)` triples in 1k-row batches with progress logs every 100k. `GameCoreModule` wires `MikroOrmModule.forFeature([SeedChainEntitySchema, RoundEntitySchema, BetEntitySchema])` plus three `useClass` repository bindings and the bootstrap provider; `app.module.ts` imports it after `MikroOrmModule.forRoot`. `SeedChainRepository.insertChain` signature extended to carry `seed: string` alongside nonce+hash so the column is populated at bootstrap (reveal gate stays at the Round aggregate boundary per the threat model). Three new unit tests (`bun test` 111/111 pass / 293 expect()). REQ-FAIR-01 closed in REQUIREMENTS.md. Two Rule-3 deviations: (1) `tests/setup.ts` lacked `KEYCLOAK_*` defaults that any application-layer import triggers, hoisted `applyDefaults()` to module load and added three keys; (2) `env.HASH_CHAIN_LENGTH` wasn't test-overridable due to ESM import hoisting, added a `protected resolveChainLength(): bigint` seam so tests can subclass with a 32-entry chain without env mutation.
- **2026-05-25** — P3.10 (Phase 3 closeout) executed: ADR-011 (ledger model — Wallet snapshot + immutable Transaction aggregate, written together in a single Postgres TX over event sourcing or transactions-only-no-snapshot), ADR-012 (JWT validation via `jose@^6.2.3` `createRemoteJWKSet` + `jwtVerify` over `passport-jwt + jwks-rsa + @nestjs/passport` chain or Kong JWT plugin; audience strategy Option B = `account`), ADR-013 (`@IdempotentSubscribe` propagates the transactional EM as the wrapped handler's third positional argument + `OutboxRepository.add(env, route, em?)` accepts an optional EM — spine public API minor-version change) authored under `.planning/adrs/`. ADR catalogue README extended with the Phase 3 section linking all three; "Future ADRs" rewound to ADR-014+ and Phase 3's entry removed from the anticipated catalogue. STATE + ROADMAP advanced to Phase 3 complete (3/10 phases). REQUIREMENTS traceability marked REQ-DOM-03 + REQ-WALL-07 as Done (REQ-AUTH-04, REQ-WALL-01..04 were already marked Complete by their respective plans); v1-complete count incremented from 10 to 12 (+REQ-DOM-03 + REQ-WALL-07). Phase 3 is provably complete: 10/10 plans landed, three ADRs, 26/26 smoke probes, 15/15 integration tests, 10_000-case fast-check property under ~170ms, 61/61 messaging-spine unit tests still green. Re-verification gates passed locally for this closeout: `bun test packages/messaging-spine/tests/unit` → 61/61 pass / 0 fail / 114 expect() calls in ~178ms. Lint surfaced 35 errors in `services/wallets/tests/integration/*.test.ts` + `tests/unit/provision-wallet.use-case.test.ts` (pre-existing `no-restricted-properties` violations against `process.env` access in test fixtures — out of P3.10 scope, logged to `phases/03-wallet-service/deferred-items.md` for the verifier). Integration suite was not re-run by this plan because it requires `docker:up` to be live (the suite was 15/15 green in P3.09 with the stack up; P3.10 is offline ADR + memory work). One commit (this plan's per-task #1): `e58e8de docs(adrs): ADR-011, ADR-012, ADR-013 for Phase 3 closeout`.
- **2026-05-25** — P3.09 (wallet integration tests + smoke probes 23-26) executed: six integration test files under `services/wallets/tests/integration/` (`provision-wallet`, `jwt-guard`, `wallet-debit`, `wallet-credit`, `wallet-debit-rejected`, `inbox-replay`) covering all five Phase 3 ROADMAP success criteria; 15/15 pass / 31 expect() in ~3.85s. Smoke probes 23-26 added to `scripts/smoke-health.sh`: 23 Keycloak password grant returns access token, 24 POST /wallets via Kong is idempotent (first call → 201/200, second call → 200 no-op), 25 GET /wallets/me returns `balance.amount === "100000"`, 26 POST /wallets/me/debit returns Kong-origin 404 (mutation blocked at gateway). `bun run smoke:health` → 26/26 pass against the live docker stack. Critical Rule-1 bug found and fixed during execution: `applyDebitAtomically` / `applyCreditAtomically` raw SQL via `em.getConnection().execute(sql, params)` ran on the pool's autocommit connection — outside the decorator's open TX — so the wallet UPDATE committed BEFORE the Transaction insert + outbox row, breaking the same-TX guarantee. Fixed by binding the raw SQL to the decorator's TX context via `em.getConnection().execute(sql, params, "all", em.getTransactionContext())`. Commits: `1735d8e`, `68dd908`, `4b3aceb`, `6ac05b8`, `edb4560`, `5678c0f`, `0a5a1f1`.
- **2026-05-25** — P3.08 (property test + ProvisionWalletUseCase idempotency unit tests) executed in Wave 4 in parallel with P3.06. Two commits — `681947c` (`services/wallets/tests/property/wallet-zero-net.test.ts`, `fast-check ^3.23.0` devDep on the wallets service) and `bdd0779` (`services/wallets/tests/unit/provision-wallet.use-case.test.ts`). Property: 10_000 fast-check cases against the pure-domain Wallet aggregate; padding logic prepends a CREDIT when a debit would exceed balance AND counts that credit in the running net so the final balancer keeps the sequence at zero net; final assertion `wallet.balance.toCents() === initialCents`; sanity test (W7 plan-check) wraps a deliberately wrong predicate in `expect(fc.assert(...)).rejects.toThrow()` proving the property machinery detects regressions. Mid-execution side-channel confirmation: temporarily injected an off-by-one into `Wallet.debit` (`nextBalance.add(Money.of(1n))`); fast-check shrunk to a 1-DEBIT counter-example within ~10 attempts; bug reverted before commit. Unit suite covers create-at-INITIAL_BALANCE_CENTS, second-call idempotency, distinct playerIds → distinct walletIds, SQLSTATE 23505 race resolution, em.transactional invocation count. Two Rule-3 deviations: (1) fast-check aligned to `^3.23.0` (shared-kernel's range) instead of plan's 4.8.0 to avoid workspace version drift; the 3.x API used in the test design is exhaustive; (2) test bootstraps env via top-of-file `process.env.X ??= ...` + dynamic import inside `beforeAll` because Bun's test runner doesn't auto-load `.env` and the runtime file is stale on KEYCLOAK_* keys. Property suite 10003 expect() in 170ms, unit suite 14 expect() in 180ms; all 25 unit tests across the wallets service green.
- **2026-05-25** — P3.06 (mutation repository + AMQP debit/credit handlers) landed in parallel Wave 4. Commits `7884b4e` (atomic UPDATE applyDebit/applyCredit + Transaction append) and `6278d38` (WalletDebitHandler + WalletCreditHandler via `@IdempotentSubscribe`). Closes the AMQP-side of REQ-WALL-04.
- **2026-05-25** — P3.07 (Kong wallets route narrowing) executed: `docker/kong/kong.yml` wallets-service block replaced with two named, method-constrained, regex-anchored routes (`wallets-provision` POST `~/wallets$`, `wallets-me` GET `~/wallets/me$`). Two commits — `ae853d0` (initial narrowing with plain prefix paths) and `fdd5ec5` (Rule 1 deviation: plain prefix leaked POST /wallets/me/debit to the service, switched to PCRE-anchored regex paths to force exact-match). Live probe matrix records 5/5 mutation methods/paths returning Kong-origin 404 (`no Route matched with those values` + `request_id`), 2/2 allowed paths forwarding to wallets:4002, games-routes untouched. Admin API confirms exactly three loaded routes: `wallets-provision`, `wallets-me`, `games-routes`. REQ-WALL-04 closed at the perimeter; the AMQP-side closure (debit/credit only via RabbitMQ) is still owned by Plan 03-06. P3.09 smoke probes will lift this matrix verbatim once P3.05 controllers land in the container image.
- **2026-05-25** — P3.02 (Wallet domain layer) executed: pure-domain Wallet bounded context landed in `services/wallets/src/domain/`. Three commits — `78aa2c8` (errors + repository interfaces), `24759d0` (Transaction aggregate — immutable factory-constructed ledger entry, frozen instance + props, MoneySnapshot on wire), `4172735` (Wallet aggregate — provision/rehydrate/debit/credit with snapshot semantics, translates NegativeMoneyError into InsufficientFundsError with both requested + available snapshots). 13/13 unit tests pass, `bunx tsc --noEmit` clean, zero infra imports inside `src/domain/`. Synchronous Wallet.debit/credit predicate is the building block Plan 03-08 will hammer with `fast-check`.
- **2026-05-25** — P3.01 (messaging-spine OI follow-ups) executed: closed OI-1 (envelope unwrap), OI-3 (txEm propagation to handler), and W3 (`OutboxRepository.add` accepts optional EM). Three commits — `d0ebe44` (RED unit regressions: 3 fail / 2 pass against unpatched decorator), `ff110d5` (GREEN patch — decorator + outbox repo + docstring), `1eb453a` (integration tests updated: 5 of 6 probes peeled by one hop, 10 lines net). Final suite: 61 unit pass + 6 integration pass + typecheck clean. Public-API change documented for ADR-013 (handler third-arg `txEm` is part of the spine's surface).
- **2026-05-24** — P2.10 (Phase 2 closeout) executed: ADR-007 (hand-rolled `@crash/messaging-spine` over `nestjs-outbox` / `pg-transactional-outbox`), ADR-008 (`amqplib` raw publisher + `@golevelup/nestjs-rabbitmq` consumer split), ADR-009 (DLX with `x-delivery-limit` on the DLQ itself, quorum queues), ADR-010 (dedicated `pg.Client` for LISTEN/NOTIFY outside MikroORM pool) authored; ADR catalogue README extended with the Phase 2 section; STATE + ROADMAP advanced to Phase 2 complete (2/10); REQUIREMENTS traceability marked REQ-WALL-05, REQ-WALL-06, REQ-SAGA-05, REQ-SAGA-06 as Done. Phase 2 is provably complete: 10/10 plans, 4 ADRs, integration tests green, smoke-health 22/22.
- **2026-05-24** — P2.9 (integration tests) executed in parallel with P2.10 (Wave 7). Six scenarios via testcontainers + `MessagingProbe`.
- **2026-05-24** — P2.8 (unit tests) executed: 5 unit suites (envelope, topology, inbox SQL, dead-letter, CLS) landed under `packages/messaging-spine/tests/unit/`. Commits `d7c9048`, `536cf70`, `4004ca0`.
- **2026-05-24** — P2.7 (service wiring) executed: both `services/games` and `services/wallets` now mount `MikroOrmModule.forRoot` + `MessagingSpineModule.forRootAsync` with per-service TopologyConfig; six MikroORM migrations (three per service) apply the canonical SQL fragments at boot via `require.resolve` against new subpath exports on `@crash/messaging-spine`; per-service `GamesDeadLetterConsumer` and `WalletsDeadLetterConsumer` subscribe to their DLQs via `@RabbitSubscribe` with `buildQuorumArgs(env.RMQ_DELIVERY_LIMIT_DLQ)`; `OUTBOX_POLL_BATCH_SIZE=100` added to both env schemas. Smoke-health extended with 15 new probes (tables + topology); 22/22 PASS on cold docker:up. Commits `1c50caa`, `18d1a26`, `da03b18`, `f239cb6`, `b8f6f03`. Four Rule-3 auto-fixes against messaging-spine itself: (1) added `./src/migrations/shared/*.sql` to package exports so migrations can resolve the SQL bodies; (2) hoisted MESSAGING_OPTIONS into a tiny global sub-module so RabbitMQModule.forRootAsync can inject it; (3) mapped TopologyConfig into RabbitMQConfig.exchanges/queues so RabbitMQModule asserts them on connect (before @RabbitSubscribe binding); (4) added `@types/amqplib` to both services.
- **2026-05-24** — P2.6 (TopologyBootstrap + MessagingSpineModule composition) executed: shipped `TopologyBootstrap` `@Injectable` (asserts every configured exchange/quorum queue/binding at `OnApplicationBootstrap` via a one-shot channel that always closes), and `MessagingSpineModule.forRootAsync({ useFactory, inject, imports? })` that wires `MessagingClsModule.forRoot()` → `RabbitMQModule.forRootAsync(...)` → `MikroOrmModule.forFeature([OutboxMessageSchema, InboxMessageSchema, DeadLetterMessageSchema])`, registers Outbox/Inbox/DeadLetter repositories + listener + publisher + bootstrap, and re-exports the three repos plus the CLS/Rabbit/Mikro modules. Barrel now surfaces the complete public API needed by P2.7 service wiring. Commits `8786fb7`, `33f6647`. Deviation: Rule 3 — `MikroOrmModule.forFeature` receives the `EntitySchema` instances (not the bare classes) because messaging entities are defined via `EntitySchema`, not class decorators.
- **2026-05-25** — P2.3 (envelope/topology/CLS contracts) executed: shipped `buildEnvelope`/`parseEnvelope` with required `causationId`, AMQP header bridge (`envelopeToAmqpHeaders`/`amqpHeadersToEnvelopeMeta`/`AMQP_HEADER_KEYS`), topology constants (`EXCHANGES`/`QUEUES`/`buildQuorumArgs`/`deriveDlxFromExchange`), zod `topologyConfigSchema`, `MessagingClsModule.forRoot()`, and `withMessagingContext()`. Commits `3005f81`, `728bc3a`, `c6fa3f6`. Deviation: added `zod` to messaging-spine peer dependencies (Rule 3 — `topologyConfigSchema` import). Deferred: pre-existing MikroORM decorator typecheck errors in `src/outbox`, `src/inbox`, `src/dead-letter` (P2.2 scope, untracked stubs).
- **2026-05-24** — P1.10 (healthcheck smoke test) executed: HealthController per service, env-driven NestJS bootstrap, `scripts/smoke-health.sh` covers 7 probes (postgres, rabbitmq, keycloak health + token, kong, games, wallets), all green on cold + warm bootstrap. Commits `3c0bad2`, `b5d37ec`, `438c7df`. Deviations: postgres 18 mount layout, mikro-orm warnWhenNoEntities=false, healthcheck pinned to 127.0.0.1 for Alpine IPv6.
- **2026-05-24** — P1.9 (repo README) executed: rewrote `README.md` with the Phase 1 surface — Quickstart, 19-row env table, demo-user curl flow, healthcheck probes, ADR + Roadmap links. Commit `bba12a0`.
- **2026-05-24** — Roadmap created (10 phases, 95/95 v1 REQ-IDs mapped, stretch backlog defined). STATE.md initialized. REQUIREMENTS.md traceability appended.
- **2026-05-24** — Research synthesis completed (SUMMARY, STACK, ARCHITECTURE, FEATURES, PITFALLS).
- **2026-05-24** — Project initialized (PROJECT.md, REQUIREMENTS.md, config.json).

---

*Last updated: 2026-05-30 by gsd-executor (P09-09 — FE Leaderboard panel + shared maskPlayerId consolidation COMPLETE (9/10 plans). Eight atomic commits land Tasks 1-4 in TDD RED→GREEN cadence: maskPlayerId moved from `services/games/src/application/use-cases/mask-player-id.ts` to `@crash/shared-kernel/identity/mask-player-id.ts` with the byte-identical Phase 6 SHA-256 hex-prefix algorithm; 6 production server consumers + 1 unit test re-imported from the shared package; legacy file deleted; cross-environment determinism test locks FE hash === server hash for a fixture UUID. `useLeaderboard()` TanStack Query hook (`staleTime: Infinity`) with WS `leaderboard:updated` cache-replace via `subscribeWsEvent`; both transports Zod-gated against the same `@crash/contracts/ws/leaderboardUpdatedPayloadSchema`. `RankChip` + `LeaderboardRow` + `LeaderboardPanel` render the top-N per UI-SPEC §Surface B.1 with rank-color rule (rank-1 emerald / rank-2 cyan / rank-3 muted / 4-10 hairline), own-row override (emerald rail + YOU prefix + low-alpha emerald row tint), 200ms rank-up border transition on improvement only (reduced-motion gated by Tailwind), 5-skeleton/empty/error/list states, `aria-live=polite` own-rank-up announcement, ≥44px touch targets. Right rail refactored to controlled shadcn `<Tabs>` (Live Feed default + Leaderboard with Trophy icon) preserving the Phase 7 D-01 grid byte-stable; `LeaderboardPanel` receives `isActive` so its footer setInterval only ticks while the user is looking at it. 244/244 frontend tests green (was 217 after P09-08; +27 from this plan); frontend tsc + lint clean; shared-kernel 5/5 pass; games tsc clean with 279/8 baseline preserved (pure import-path swap). All 5 PLAN greps clean (no destructive token on negative profit, no hardcoded sizes/intervals in panel, Zod gate present, no buggy UUID-prefix mask, shared-kernel imported, zero stale mask-player-id imports). Two Rule-1 test-DX-only deviations: `fireEvent.pointerDown` triple for Radix Tabs in jsdom, `findByText(new RegExp(masked))` for ellipsis-suffixed row text. No Rule 2/3/4 deviations. No auth gates. Threat-model T-09-60..63 all mitigated and locked. REQ-LEAD-03 + REQ-LEAD-04 closed at this plan. Plan 09-09 unblocks Plan 09-10 (closeout — Phase 9 ROADMAP success criterion 2 closure + ADR candidates + verify-phase + ui-review). Previous: P08-10 — Phase 8 COMPLETE (10/10 plans, 8/10 phases). Closeout landed the four Phase 8 ADRs (ADR-028 client-seed-deterministic Phase 4 reaffirmation, ADR-029 driver-injection seam over separate playback path, ADR-030 browser-safe `@crash/contracts/provably-fair-browser` subpath + `crypto.subtle` with two byte-encoding contracts locked, ADR-031 ReplayModal over live game + 1x/2x/4x speed selector) at the next-free ADR-028..031 range — the ROADMAP's stale anticipated "ADR-023/024" labels for Phase 8 were reconciled because Phase 5/6/7 ADRs consumed 019..027 before Phase 8 closed; shipped ADRs are never renumbered; renumber note recorded in ROADMAP per the Phase 7 closeout precedent. All 5 Phase 8 REQ-IDs confirmed Done (REQ-FE-09 + REQ-FE-10 + REQ-REPLAY-01/02/03); v1-complete 62/95 → 67/95; ADRs landed 27 → 31. Deferred drawer `<a href>` → typed TanStack `<Link to="/verify/$roundId" params={{ roundId }}>` migration from 08-06 closed (commit `f4410a8` — the drawer tests now wrap in a minimal memory router; 169/169 FE tests green, was 168, +1 regression test). Dual-rAF smoke check (08-07 Pitfall 3) executed at the vitest layer because no live browser session is available in the executor: `bunx vitest run src/components/crash-curve.test.tsx src/features/replay/replay-driver.test.ts src/features/replay/determinism.test.ts src/components/replay-modal.test.tsx` runs all four rAF-related surfaces in one Vitest worker against jsdom — 23/23 PASS with no rAF leak, no timer collision, no console error; both rAF surfaces are leak-free by code shape (live `useRafCurve` cleanup on unmount + only `requestAnimationFrame`/`cancelAnimationFrame`; replay driver memoized on `[roundId, growthRate, crashPoint]` only, `speed`/`paused` read via `useReplayStore.getState()` per frame — Pitfall 7). All gates green: `bunx tsc --noEmit` exit 0 across frontend + services/games + packages/contracts; `bun run lint` clean (pre-existing `routeTree.gen.ts` warning); `bun test` 30/30 contracts. Next: `/gsd:verify-phase 8` + `/gsd:ui-review` then `/gsd:plan-phase 9` (Auto Features & Leaderboard — parallelizable with Phase 8 per ROADMAP). Previous: P08-09 — recruiter-runnable Provably-Fair verification walkthrough added to `README.md` between the ADR catalogue and the Roadmap section; one atomic commit `7d5a086`, 116 net insertions, zero deletions; worked example uses the Phase 4 locked-byte tuple `serverSeed=0x0…01`, `clientSeed="test"`, `nonce=0`, `instantCrashBucket=101` to land `crashPoint = 2.94` with only `openssl` + `python3` — no docker, no curl, no live round; the "Why two encodings" subsection documents Pitfall 1/2 against the source-of-truth from `derive-crash-point.ts` / `generate-seed-chain.ts` and shows the empirically-confirmed `3.02` MISMATCH mode if the byte encodings are swapped; portability notes cover LibreSSL vs OpenSSL output prefixes plus a python3 `binascii.unhexlify` fallback for busybox; recruiter dry-run executed twice from a fresh shell against the literal copy-paste block, both runs matched the documented expected output byte-for-byte; Phase 8 ROADMAP success criterion 5 closed; only 08-10 closeout remains [ADR-028..031 + REQ/ROADMAP/STATE rotation + 08-06 drawer Link migration + 08-07 dual-rAF smoke]). Prior: P08-08 — Determinism byte-match Vitest landed, 6/9 plans landed; `localMultiplier` via shared `@crash/contracts` `multiplierAt` anchored to `roundStartedAt` + `serverOffsetMs` [byte-identical to server], `reconcileOffset` pure EWMA never-snap, `useRafCurve` rAF loop writing ONLY the isolated multiplier store [D-06] + freezing at the server `crashValue` on CRASHED + leak-free cancel, `drawCurve` dPR `setTransform` + `clearRect`-first emerald→cyan single-glow path + crash-red freeze + on-canvas Fira Code, `CrashCurve` `ResizeObserver` dPR sizing + `prefers-reduced-motion` + off-loop draw [no 60fps re-render]; commits 5756896/4d34c15; tsc clean, 38/38 tests green, no SVG/WebGL [grep clean], no growthRate/alpha literal in the feature, zero deviations, no new package installs; REQ-FE-02/03 closed). Prior: P07-05 — bet panel + live-payout cashout + betting countdown complete, 5/9 plans landed; `parseBetAmount` Money-VO validator [config bounds + currency-exponent precision, rejects negative/scientific/out-of-bounds], `use-place-bet`/`use-cashout` TanStack mutations POSTing MoneySnapshot bodies + error-key classification [cashout 409 silent], neutral state-aware BetPanel, accent CashoutButton with live `bet × renderedMultiplier` Money payout subscribed to the isolated multiplier store [D-06], bettingEndsAt-driven Countdown; commits 2917997/da4ed36; tsc clean, 27/27 tests green, money rule clean on new files, no hardcoded hex; one Rule-1 type-narrowing deviation; no new package installs; REQ-FE-04/05/06 closed; 2 pre-existing config.ts money-rule errors deferred). Prior: P07-04 — auth + socket + state plumbing complete, 4/9 plans landed; ONE oidcSpa.createUtils instance + enforceLogin guard + socket.io auth-function singleton [fresh token per reconnect] + five Zustand slices + isolated multiplier store [D-06] + schema-validated ws-dispatch + TanStack Query balance/history; commits 2d2611e/9f265c3; tsc clean, 17/17 tests green; REQ-AUTH-01/02/03 + REQ-FE-07/08 closed; two deviations [Rule 2 credit-not-overwrite, Rule 3 two new config keys]; no new package installs). Prior: P07-03 — FE scaffold complete, 3/9 plans landed; booting selective-SSR TanStack Start app on :3000 with dark-casino @theme + zod VITE_ config + shadcn 13-component set + Vitest harness; both 07-02 carry-forwards landed [@crash/contracts/multiplier subpath + Kong OPTIONS]; commits f473417/bd4038e/b6ee856; REQ-FE-01/11/12 closed; three Rule-3 auto-fixes; Kong reload deferred to live-smoke). Prior: P07-02 — Wave-0 de-risking spike complete, 2/9 plans landed; checkpoint:decision RATIFIED by the user — boot mode `selective-ssr` + oidc `single-getOidc`; two mandatory 07-03 carry-forwards: `@crash/contracts/multiplier` browser-safe subpath + Kong `OPTIONS` on every route; oidc-spa v10.2.3 real API is `oidcSpa.createUtils` for 07-04). Prior: P07-01 — Phase 7 started, 1/9 plans landed; pre-FE infra unblock: `@crash/contracts/ws` shared WS payload schemas + scoped Kong CORS for `http://localhost:3000` with credentials + `.tsx` money-rule enforcement; three commits `158b33e`/`f26948c`/`cd186d9`, no deviations, 8 pre-existing games clock-mock unit failures deferred). Prior: P6.10 closeout — Phase 6 complete, 6/10 phases done, three Phase 6 ADRs landed: ADR-021 single global lobby + ADR-022 30Hz tick/60fps interpolation + ADR-023 server-authoritative cashoutAcceptedAt; v1-complete count incremented from 39 to 46 (REQ-WS-01..07); ADRs landed 20→23; REQ-AUTH-01/02/03 kept Pending — frontend OIDC deferred to Phase 7; standalone Socket.IO WS_PORT=4101 Bun http-attach fix documented as ADR-022 consequence).*
