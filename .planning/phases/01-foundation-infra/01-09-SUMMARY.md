---
phase: 01-foundation-infra
plan: 09
subsystem: docs
tags: [readme, docs, quickstart, demo-user]
requires: [01-02, 01-03, 01-04, 01-05, 01-06, 01-07, 01-08]
provides: [readme-phase-1-surface]
affects: [recruiter-onboarding]
tech-stack:
  added: []
  patterns: [docs-as-source-of-truth]
key-files:
  created:
    - .planning/phases/01-foundation-infra/01-09-SUMMARY.md
  modified:
    - README.md
decisions:
  - "README replaces the upstream challenge brief; the brief itself is preserved in the .planning research artifacts."
  - "Env-vars table embedded directly in README (single source for recruiters) but references services/<name>/.env.example as the loader path — the README defaults must be kept in sync at Phase 10 audit."
  - "Demo-user section documents the Phase-1 caveat explicitly: Kong does not yet route /wallets — the 404 is expected until Phase 3."
metrics:
  duration: ~25 minutes
  completed: 2026-05-24
---

# Phase 1 Plan 9: Repo README Summary

Repo root README replaces Jungle Gaming's upstream challenge brief with an implementation-focused Phase 1 surface — eight sections that take a recruiter from `git clone` to a working `player/player123` token in under five minutes of reading. The brief itself is preserved as research context inside `.planning/`.

## Section structure

- `# Crash Game` — single paragraph framing the stack (Bun, NestJS, RabbitMQ saga, Keycloak, MikroORM 7, TanStack Start, Socket.IO) and the two non-negotiables (Money VO, provably fair, server-authoritative multiplier).
- `## Quickstart` — prerequisites plus the five bun scripts (`bun install`, `docker:up`, `smoke:health`, `docker:down`, `docker:prune`) with the bootstrap narrative.
- `## Environment variables` — 19-row table mirroring REQUIREMENTS §Open Configuration Values, with each row carrying the default and the owning service; explicit note that the root `.env.example` is a reference superset, not a loader.
- `## Demo user` — `player/player123` plus the manual `curl` chain (Keycloak password grant → `POST /wallets` via Kong → `GET /wallets/me`), with the Phase-1 caveat that Kong does not yet route `/wallets` (lands in Phase 3).
- `## Healthchecks` — manual `curl`/`wget`/`pg_isready` probes for all six containers (Postgres, RabbitMQ, Keycloak on the new 9000 management port, Kong, games on 4001, wallets on 4002) plus a forward reference to `bun run smoke:health` (ships in P1.10).
- `## Project structure` — ASCII tree highlighting `services/`, `packages/` (shared-kernel, contracts, eslint-plugin), `frontend/`, and `.planning/`.
- `## Architecture Decision Records` — paragraph linking `.planning/adrs/README.md` and naming what ADR-001 through ADR-006 cover.
- `## Roadmap` — paragraph linking `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` with the ten-phase shape sketched in one sentence.

## Verification

- All eight required sections present (1 H1 + 7 H2 = 7 matches against `^##`).
- File is 143 lines (plan minimum: 80).
- Env table has 19 data rows.
- All required tokens present: `bun run docker:up`, `player/player123`, `INITIAL_BALANCE_CENTS`, `.planning/adrs`, `crash-game-client`.
- AI fingerprint grep: `co-authored-by|generated by|claude|chatgpt|gpt-` → no matches.
- Emoji scan (Unicode ranges U+2600-27BF, U+1F300-1FAFF, U+1F600-1F64F, U+1F680-1F6FF, U+1F900-1F9FF) → no matches.

## Deviations from Plan

None — plan executed exactly as written. All eight sections landed in the prescribed order with the verbiage drawn from `.planning/REQUIREMENTS.md` defaults and `docker-compose.yml` healthcheck ports.

## What later phases will extend

- **Phase 7 (Frontend Vertical Slice)** — adds UI screenshots of the game page, bet panel, and history strip; documents the OIDC redirect flow as it actually works in-browser rather than via the curl fallback.
- **Phase 8 (Provably-Fair UX, History & Replay)** — adds a worked provably-fair verification example (curl `/games/rounds/:id/verify` + third-party SHA-256 reproduction) so a recruiter can verify outside the app.
- **Phase 10 (Quality Hardening & Docs)** — adds CI status badges (build / tests / coverage), the architecture and saga-flow diagrams, the Prometheus + Grafana setup notes, a troubleshooting section, and audits the env-vars table against the materialized `.env.example` defaults.

## Self-Check: PASSED

- README.md exists and is 143 lines.
- Commit `bba12a0` exists in `git log`.
- All required tokens, section headers, and the 19-row env table verified inline.
