---
phase: 01-foundation-infra
plan: 02
type: execute
wave: 3
depends_on: [01-01]
files_modified:
  - docker-compose.yml
  - docker/keycloak/realm-export.json
autonomous: true
requirements: [REQ-INFRA-01, REQ-INFRA-02, REQ-INFRA-03, REQ-AUTH-05]
tags: [docker-compose, keycloak, healthchecks, infra]
must_haves:
  truths:
    - "Keycloak healthcheck targets management port 9000 (not 8080) and turns green within `start_period`."
    - "Postgres, RabbitMQ, Keycloak, and Kong all reach `healthy` state via `docker compose up -d --wait`."
    - "`docker compose down --remove-orphans` stops the stack cleanly without leaving dangling containers."
    - "`docker compose down -v --rmi local --remove-orphans` followed by `docker volume prune -f` removes every Crash-Game volume."
    - "Realm export contains the demo user `player` with credential `player123` and the `crash-game` realm with client `crash-game-client` (PKCE S256)."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Healthcheck-correct orchestration of infra containers; container-level wiring for service + migration containers"
      contains: "KC_HTTP_MANAGEMENT_PORT"
    - path: "docker/keycloak/realm-export.json"
      provides: "Pre-seeded `crash-game` realm + `player/player123` demo user + `crash-game-client` OIDC client"
      contains: "crash-game-client"
  key_links:
    - from: "docker-compose.yml keycloak.healthcheck"
      to: "Keycloak management port 9000"
      via: "tcp/9000 /health/ready"
      pattern: "/dev/tcp/localhost/9000"
    - from: "docker-compose.yml games + wallets depends_on"
      to: "games-migrate + wallets-migrate containers (defined in P1.3)"
      via: "service_completed_successfully condition"
      pattern: "service_completed_successfully"
    - from: "Keycloak container --import-realm flag"
      to: "/opt/keycloak/data/import/realm-export.json"
      via: "volume mount + boot-time import"
      pattern: "import-realm"
---

<objective>
Fix the broken Keycloak healthcheck (currently targets port 8080 — that endpoint moved to port 9000 in Keycloak 26.x), prepare the compose file to host the multi-stage build context required by P1.3, and verify the Keycloak realm export carries the demo user `player/player123` so REQ-AUTH-05 is satisfied at boot.

Purpose: Without the healthcheck fix, `bun run docker:up` (which uses `--wait`) hangs or fails because Keycloak is reported `unhealthy` even after the realm import succeeds and the admin console is reachable. This single bug blocks every subsequent Phase 1 verification.

Output: A `docker-compose.yml` whose infrastructure section is correct for Keycloak 26.5.5; the realm export file verified to contain the demo seed; placeholder/commented service blocks updated to reference the repo-root build context that P1.3's multi-stage Dockerfile requires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-foundation-infra/01-RESEARCH.md
@docker-compose.yml
@docker/keycloak
</context>

<interfaces>
Current `docker-compose.yml` (existing wiring extracted by Read):
- `postgres`: image `postgres:18.3-alpine`, env `POSTGRES_EXTRA_DATABASES=games,wallets`, healthcheck `pg_isready -U admin`. OK as-is.
- `rabbitmq`: image `rabbitmq:4.2.4-management-alpine`, healthcheck `rabbitmq-diagnostics ping`. OK as-is.
- `keycloak`: image `quay.io/keycloak/keycloak:26.5.5`, command `start-dev --import-realm`. **Healthcheck is wrong** — targets port 8080.
- `kong`: image `kong:3.9.1`, declarative config mount. OK as-is.
- `games` and `wallets`: build context `./services/games` and `./services/wallets` — **must change to `./` (repo root)** so the multi-stage Dockerfile from P1.3 can copy workspace siblings (`packages/*`).
- `volumes`: `postgres_data`, `rabbitmq_data`.

Migration containers `games-migrate` and `wallets-migrate` are added by P1.3 (so this plan does NOT add them; this plan only prepares the compose-file shape to receive them by updating the parent service blocks' build contexts and `depends_on`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix Keycloak healthcheck and expose management port 9000</name>
  <read_first>
    - /Users/pedro/Projetos/fullstack-challenge/docker-compose.yml
    - /Users/pedro/Projetos/fullstack-challenge/.planning/phases/01-foundation-infra/01-RESEARCH.md (§Pitfall 1, §Output §3 Change 1)
  </read_first>
  <files>docker-compose.yml</files>
  <action>Edit the `keycloak:` service block in `docker-compose.yml`. Apply the four changes per RESEARCH.md §Pitfall 1 and §Output §3 Change 1:

1. In `ports:`, add a second mapping `"9000:9000"` immediately after the existing `"8080:8080"` line. Keep the 8080 mapping (admin console + OIDC endpoints stay there).

2. Under `environment:`, add a NEW key `KC_HTTP_MANAGEMENT_PORT: "9000"` immediately after the existing `KC_HEALTH_ENABLED: "true"` line. This makes the port explicit per RESEARCH Assumption A5 (a safety net if the default ever drifts).

3. Replace the entire `healthcheck:` block with the corrected version that targets port 9000:
   - `test:` becomes the multi-line list form `["CMD-SHELL", "exec 3&lt;&gt;/dev/tcp/localhost/9000 &amp;&amp; echo -e 'GET /health/ready HTTP/1.1\\r\\nHost: localhost\\r\\nConnection: close\\r\\n\\r\\n' &gt;&amp;3 &amp;&amp; cat &lt;&amp;3 | grep -q '200'"]`
   - `interval: 10s`
   - `timeout: 5s`
   - `retries: 30` (raised from 15 — RESEARCH §Output §3 Change 1 recommends 30 because the management endpoint comes up after the public endpoint)
   - `start_period: 60s` (raised from 30 — gives realm import time to complete; see RESEARCH §Phase 1 risks)

4. Leave the `command: start-dev --import-realm` line and the volume mount `./docker/keycloak:/opt/keycloak/data/import:ro` exactly as-is — they are correct.

Do NOT add `depends_on` to keycloak in this task. Do NOT touch any other service block in this task.

Do NOT inline a fenced code block in the action prose; the executor copies values from RESEARCH.md §Output §3 Change 1 which is in the `read_first` list.
  </action>
  <verify>
    <automated>grep -q 'KC_HTTP_MANAGEMENT_PORT' /Users/pedro/Projetos/fullstack-challenge/docker-compose.yml &amp;&amp; grep -q '"9000:9000"' /Users/pedro/Projetos/fullstack-challenge/docker-compose.yml &amp;&amp; grep -q '/dev/tcp/localhost/9000' /Users/pedro/Projetos/fullstack-challenge/docker-compose.yml &amp;&amp; ! grep -q '/dev/tcp/localhost/8080' /Users/pedro/Projetos/fullstack-challenge/docker-compose.yml &amp;&amp; docker compose -f /Users/pedro/Projetos/fullstack-challenge/docker-compose.yml config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config --quiet` exits 0 (YAML is parseable and schema-valid).
    - The Keycloak service's `healthcheck.test` references `/dev/tcp/localhost/9000` and does NOT reference `/dev/tcp/localhost/8080` (the old broken healthcheck must be gone).
    - The Keycloak service's `environment` has `KC_HTTP_MANAGEMENT_PORT: "9000"`.
    - The Keycloak service's `ports` includes both `"8080:8080"` and `"9000:9000"`.
    - `healthcheck.retries` is `30` and `start_period` is `60s`.
    - The `command:` line still reads `start-dev --import-realm`.
  </acceptance_criteria>
  <done>Keycloak healthcheck targets the correct management port and the realm import has 60s headroom before the gate triggers.</done>
</task>

<task type="auto">
  <name>Task 2: Reshape games and wallets service blocks for repo-root build context</name>
  <read_first>
    - /Users/pedro/Projetos/fullstack-challenge/docker-compose.yml
    - /Users/pedro/Projetos/fullstack-challenge/.planning/phases/01-foundation-infra/01-RESEARCH.md (§Pattern 2, §Output §3 Change 3, §Output §3 Change 6)
  </read_first>
  <files>docker-compose.yml</files>
  <action>Edit the `games:` and `wallets:` service blocks. Apply IDENTICAL structural changes to each (only the service name, port, and `.env` path differ):

1. Replace the `build:` block with the structured form:
   - `context: ./`
   - `dockerfile: services/games/Dockerfile` (for games; `services/wallets/Dockerfile` for wallets)
   - `target: runtime`
   Rationale: build context moves to repo root so the multi-stage Dockerfile in P1.3 can COPY `packages/*/package.json` before `bun install --frozen-lockfile` (workspace-aware install requirement — RESEARCH §Pattern 2, Pitfall 4).

2. Replace the `healthcheck.test` value from `curl -sf http://localhost:PORT/health || exit 1` to `wget -qO- http://localhost:PORT/health || exit 1` (where PORT is 4001 for games, 4002 for wallets). Rationale: `oven/bun:1.3.11-alpine` does not ship `curl` — using `wget -qO-` avoids needing `apk add curl` in the runtime stage. RESEARCH §Output §3 Change 6.

3. Expand `depends_on:` to the four-condition form for each service:
   - `postgres: { condition: service_healthy }`
   - `rabbitmq: { condition: service_healthy }`
   - `keycloak: { condition: service_healthy }`
   - `games-migrate: { condition: service_completed_successfully }` (or `wallets-migrate` for wallets)
   The migration container itself is added in P1.3; declaring the dependency here is fine — compose validates at `up` time only, and P1.3 lands before any `docker:up` is run.

4. Leave `ports`, `env_file`, `healthcheck.interval/timeout/retries/start_period` unchanged.

Do NOT modify the postgres, rabbitmq, or kong blocks in this task. Do NOT add or remove any volumes.
  </action>
  <verify>
    <automated>grep -A4 'games:' /Users/pedro/Projetos/fullstack-challenge/docker-compose.yml | grep -q 'context: ./' &amp;&amp; grep -q 'dockerfile: services/games/Dockerfile' /Users/pedro/Projetos/fullstack-challenge/docker-compose.yml &amp;&amp; grep -q 'dockerfile: services/wallets/Dockerfile' /Users/pedro/Projetos/fullstack-challenge/docker-compose.yml &amp;&amp; grep -c 'service_completed_successfully' /Users/pedro/Projetos/fullstack-challenge/docker-compose.yml | grep -qE '^[2-9]' &amp;&amp; grep -c 'wget -qO-' /Users/pedro/Projetos/fullstack-challenge/docker-compose.yml | grep -qE '^[2-9]' &amp;&amp; ! grep -q 'curl -sf http://localhost:400' /Users/pedro/Projetos/fullstack-challenge/docker-compose.yml &amp;&amp; docker compose -f /Users/pedro/Projetos/fullstack-challenge/docker-compose.yml config --quiet 2&gt;&amp;1 | grep -vE 'games-migrate|wallets-migrate' | tee /tmp/dc-config-out.txt &amp;&amp; ! grep -E 'error|invalid' /tmp/dc-config-out.txt</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config --quiet` does NOT report any error about an undefined service named `games-migrate` or `wallets-migrate` at the `depends_on` reference — compose accepts forward references at parse time and only validates at `up`.
    - Both service blocks have `build.context: ./` and the Dockerfile path includes the `services/&lt;name&gt;/` prefix.
    - Both service blocks have `build.target: runtime`.
    - Neither service block contains `curl -sf` anywhere — both use `wget -qO-`.
    - `service_completed_successfully` appears at least 2 times (once per service's depends_on block) in the compose file.
    - The `keycloak:` condition under `depends_on:` for both games and wallets is `service_healthy`.
  </acceptance_criteria>
  <done>Service blocks are wired to the repo-root build context and use alpine-native healthchecks; the structural shape required by P1.3 is in place.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Keycloak realm export carries demo user and OIDC client</name>
  <read_first>
    - /Users/pedro/Projetos/fullstack-challenge/docker/keycloak/realm-export.json
    - /Users/pedro/Projetos/fullstack-challenge/.planning/REQUIREMENTS.md (REQ-AUTH-05)
    - /Users/pedro/Projetos/fullstack-challenge/.planning/research/STACK.md (§2.8 — OIDC PKCE S256)
  </read_first>
  <files>docker/keycloak/realm-export.json</files>
  <action>Inspect `docker/keycloak/realm-export.json` (read the full file). Verify it satisfies REQ-AUTH-05 by containing ALL of the following. If any are missing or incorrect, edit the file to add/correct them (preserve the JSON shape, do not reformat unrelated keys):

1. Top-level `"realm": "crash-game"`.
2. `"enabled": true` at the realm level.
3. A `users` array containing an entry with:
   - `"username": "player"`
   - `"enabled": true`
   - `"emailVerified": true`
   - `"credentials": [{ "type": "password", "value": "player123", "temporary": false }]`
   - `"realmRoles": ["default-roles-crash-game"]` (or whatever role Keycloak 26 auto-creates — leave as-is if present)
4. A `clients` array containing an entry with:
   - `"clientId": "crash-game-client"`
   - `"publicClient": true` (browser SPA — no client secret)
   - `"standardFlowEnabled": true`
   - `"directAccessGrantsEnabled": true` (enables Resource Owner Password Credentials for the realm export smoke test in P1.10; this MUST be true for the smoke test to obtain a token without a browser)
   - `"attributes"` object containing `"pkce.code.challenge.method": "S256"` (locks PKCE to S256 per REQ-AUTH-01)
   - `"redirectUris"`: `["http://localhost:3000/*", "http://localhost:8080/*"]` (frontend dev server in Phase 7 + Keycloak self-redirect)
   - `"webOrigins"`: `["http://localhost:3000", "http://localhost:8080"]`

If the file already satisfies all of the above, make no changes — record in the task summary that the file was verified intact. If the file does NOT exist, create it with a minimal realm definition matching the schema above (refer to Keycloak realm export documentation; do not invent fields beyond what is necessary).

Do NOT add additional clients, users, or roles. Do NOT enable `serviceAccountsEnabled` (REQ-AUTH-04 needs JWKS only).
  </action>
  <verify>
    <automated>node -e "const r=require('/Users/pedro/Projetos/fullstack-challenge/docker/keycloak/realm-export.json');if(r.realm!=='crash-game')process.exit(1);const u=(r.users||[]).find(x=&gt;x.username==='player');if(!u||!u.enabled)process.exit(2);const c=(u.credentials||[]).find(x=&gt;x.type==='password'&amp;&amp;x.value==='player123');if(!c)process.exit(3);const cl=(r.clients||[]).find(x=&gt;x.clientId==='crash-game-client');if(!cl||!cl.publicClient||!cl.standardFlowEnabled||!cl.directAccessGrantsEnabled)process.exit(4);if(!cl.attributes||cl.attributes['pkce.code.challenge.method']!=='S256')process.exit(5);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `docker/keycloak/realm-export.json` parses as valid JSON.
    - `.realm === "crash-game"`.
    - `.users[]` contains a user with `username === "player"` whose `credentials[]` has `type === "password"` and `value === "player123"`.
    - `.clients[]` contains a client with `clientId === "crash-game-client"`, `publicClient === true`, `standardFlowEnabled === true`, `directAccessGrantsEnabled === true`.
    - That client's `attributes["pkce.code.challenge.method"] === "S256"`.
    - That client's `redirectUris` includes at least one of `http://localhost:3000/*` or `http://localhost:8080/*`.
  </acceptance_criteria>
  <done>Realm export will boot Keycloak with `player/player123` pre-seeded and the PKCE-S256 OIDC client ready for the Phase 7 frontend and the Phase 1 smoke test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker-compose → host network | Exposed ports (8080, 9000, 5432, 5672, 15672, 8000, 8001, 4001, 4002) are accessible to anything on the host |
| Keycloak realm export → committed git history | Realm carries seeded demo credentials |
| Compose `depends_on` chain | A misfired healthcheck can stall the entire stack or, worse, mark unhealthy services as ready |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-01 | Information Disclosure | Demo `player/player123` credential in committed realm export | accept | Play-money, local-only dev rig per spec; documented in ADR-005 + README. NOT to be reused in any deployed environment. |
| T-01.2-02 | Denial of Service | Keycloak healthcheck wrong port → entire stack hangs on `--wait` | mitigate | Healthcheck fixed in Task 1; `start_period: 60s` absorbs realm-import variance |
| T-01.2-03 | Spoofing | Public OIDC client with `directAccessGrantsEnabled: true` | accept | Enables Phase 1 smoke test password-grant validation; PKCE S256 remains required for the browser flow; documented constraint of the demo. |
| T-01.2-04 | Tampering | docker-compose YAML drift (untracked edits) | mitigate | `docker compose config --quiet` gate in each task's verify step |
| T-01.2-05 | Elevation of Privilege | Docker daemon socket mounted into a service container | mitigate | Confirmed not done; no `/var/run/docker.sock` mounts in current compose; revisit in P1.10 smoke. |
</threat_model>

<verification>
After all tasks complete (note: do NOT run `docker compose up` here — that is P1.10's job; this plan only verifies the file's STRUCTURE):
- `docker compose config --quiet` exits 0 (or warns only about the not-yet-defined `*-migrate` services).
- `grep -c '/dev/tcp/localhost/9000' docker-compose.yml` returns exactly 1.
- `grep -c '/dev/tcp/localhost/8080' docker-compose.yml` returns exactly 0.
- `node -e "JSON.parse(require('fs').readFileSync('docker/keycloak/realm-export.json'))"` exits 0.
</verification>

<success_criteria>
- Keycloak healthcheck targets port 9000 and includes `KC_HTTP_MANAGEMENT_PORT` env var.
- Both service blocks use repo-root build context, multi-stage `target: runtime`, alpine-native `wget` healthcheck, and `depends_on` includes `*-migrate` with `service_completed_successfully`.
- Realm export carries `player/player123` and the `crash-game-client` OIDC client with PKCE-S256 attribute.
- `docker compose config --quiet` parses successfully.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-infra/01-02-SUMMARY.md` documenting:
- The exact Keycloak healthcheck before/after (so ADR-003 supporting evidence is captured)
- The realm export delta (whether it was verified intact or required edits, with the set of fields added)
- Confirmation that `docker compose config --quiet` parses (do NOT run `up` here)
</output>
