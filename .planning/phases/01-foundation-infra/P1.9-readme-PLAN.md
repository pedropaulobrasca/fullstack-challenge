---
phase: 01-foundation-infra
plan: 09
type: execute
wave: 5
depends_on: [01-02, 01-03, 01-04, 01-05, 01-06, 01-07, 01-08]
files_modified:
  - README.md
autonomous: true
requirements: [REQ-DOC-03, REQ-AUTH-05]
tags: [readme, docs, quickstart, demo-user]
must_haves:
  truths:
    - "README.md exists at repo root with a Phase 1 surface: project intro, quickstart, env vars table, first-login demo note, healthcheck commands, project structure, ADR link."
    - "Quickstart documents the one-line bootstrap: `bun run docker:up`."
    - "Demo user section explicitly tells the recruiter how to log in as `player/player123` and that the wallet auto-provisions with 1000.00 CRD on first authenticated POST /wallets call."
    - "ADR catalogue is linked so the recruiter can find decision rationale."
  artifacts:
    - path: "README.md"
      provides: "Repo root README with Phase 1 surface; Phase 7 will add UI screenshots; Phase 10 will add CI badges, full architecture diagrams, troubleshooting"
      min_lines: 80
  key_links:
    - from: "README.md Quickstart section"
      to: "package.json scripts (docker:up, smoke:health)"
      via: "command references"
      pattern: "bun run docker:up"
    - from: "README.md Demo user section"
      to: "Keycloak realm export + INITIAL_BALANCE_CENTS env"
      via: "explicit citation of credentials and provisioning flow"
      pattern: "player/player123"
    - from: "README.md ADR section"
      to: ".planning/adrs/README.md"
      via: "markdown link"
      pattern: "\\[.*ADR.*\\]\\(\\.planning/adrs"
---

<objective>
Land the repo root `README.md` with the Phase 1 surface — enough for a recruiter or developer who clones the repo to bring it up, log in, and find the decision rationale. The README is intentionally lean now; Phase 7 adds UI screenshots, Phase 8 adds the provably-fair verifier example, Phase 10 adds CI badges and full architecture diagrams.

Purpose: REQ-DOC-03 requires the demo user `player/player123` to be discoverable. REQ-AUTH-05 requires the realm to be pre-imported. The README is where a recruiter learns both facts without reading source code. Without it, the recruiter wastes the first 10 minutes of their evaluation figuring out how to start the stack.

Output: A single `README.md` at the repo root with eight sections (Project, Quickstart, Env vars, Demo user, Healthchecks, Project structure, ADRs, Roadmap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CLAUDE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/adrs/README.md
@.planning/phases/01-foundation-infra/01-RESEARCH.md
@.env.example
@services/games/.env.example
@services/wallets/.env.example
@docker/keycloak/realm-export.json
</context>

<interfaces>
README is consumed by humans (recruiter, future maintainers, the developer themselves). No automated consumer. Tone: professional, terse, factual. No marketing prose. No emojis (CLAUDE.md global rule). No AI fingerprints.

Cross-references:
- ADR catalogue lives at `.planning/adrs/README.md`
- Phase plans live at `.planning/phases/`
- Roadmap at `.planning/ROADMAP.md`
- Stack details at `.planning/research/STACK.md`
- Env-var surface materialized in `.env.example` + per-service `.env.example`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md with Phase 1 surface (eight sections)</name>
  <read_first>
    - /Users/pedro/Projetos/fullstack-challenge/.planning/PROJECT.md
    - /Users/pedro/Projetos/fullstack-challenge/.planning/REQUIREMENTS.md
    - /Users/pedro/Projetos/fullstack-challenge/.planning/ROADMAP.md
    - /Users/pedro/Projetos/fullstack-challenge/CLAUDE.md
    - /Users/pedro/Projetos/fullstack-challenge/package.json
    - /Users/pedro/Projetos/fullstack-challenge/.planning/adrs/README.md
  </read_first>
  <files>README.md</files>
  <action>Write `README.md` at the repo root with EXACTLY the following eight sections, in order. Use H2 (`##`) for section headers, H3 (`###`) for sub-sections inside long sections.

**Section 1: `# Crash Game`** (H1, single heading line at top of file)

Followed by a single paragraph (3-4 sentences) describing the project:
- Multiplayer real-time Crash game submitted as the Jungle Gaming fullstack technical challenge.
- Two NestJS services (`games`, `wallets`) on Bun, RabbitMQ saga, Keycloak OIDC, PostgreSQL via MikroORM 7, TanStack Start frontend with Socket.IO 4.8.
- Money via Dinero v2 wrapped in `Money` VO. Provably fair via Bustabit-style HMAC-SHA-256 hash chain.
- Server-authoritative multiplier with client-side interpolation.

Do NOT include marketing language. Stick to the technology and intent.

**Section 2: `## Quickstart`**

Prerequisites bullet list: Docker + Docker Compose v2 (running), Bun 1.3.11 (optional locally — container has it).

Commands (use fenced bash blocks):
- `bun install` — install dev tooling and link workspaces
- `bun run docker:up` — start every container (postgres, rabbitmq, keycloak, kong, games, wallets, games-migrate, wallets-migrate); blocks until healthy
- `bun run smoke:health` — verify all services respond (this script ships in P1.10)
- `bun run docker:down` — stop cleanly
- `bun run docker:prune` — remove volumes + images (full reset)

Add a paragraph: "On first `docker:up`, Postgres creates databases `games` and `wallets` via the init script; Keycloak imports the `crash-game` realm + pre-seeded user; both service migration containers run (no-ops in Phase 1 — actual migrations ship in Phase 3 + 4). All four healthchecks must pass before `docker:up` returns."

**Section 3: `## Environment variables`**

Table with columns: `Variable | Default | Service | Description`. One row per Open Configuration Value (19 rows from REQUIREMENTS §Open Configuration Values). Use the exact defaults from the per-service `.env.example` files written in P1.7.

Below the table: "Defaults live in each service's `src/config/defaults.ts` (typed re-export via zod). Override via `.env` per service. The root `.env.example` is a superset reference — do NOT load it directly; compose reads `services/&lt;name&gt;/.env`."

**Section 4: `## Demo user`**

Plain prose section (no code blocks except for the credential):
- Keycloak realm `crash-game` is auto-imported with the OIDC client `crash-game-client` (PKCE S256) and the demo user `player` / `player123`.
- The wallet for `player` is NOT pre-seeded into the wallets database. Instead, the wallet auto-provisions with `INITIAL_BALANCE_CENTS=100000` (1000.00 CRD) on the first authenticated `POST /wallets` call — see ADR-005 for the rationale.
- During Phase 7 (frontend), this happens automatically when the user logs in. Until then, recruiters can provision the wallet manually:
  - Step 1: Obtain an access token via the Keycloak password grant (commands below).
  - Step 2: `curl -X POST -H "Authorization: Bearer $TOKEN" http://localhost:8000/wallets` (routed via Kong).
  - Step 3: `curl -H "Authorization: Bearer $TOKEN" http://localhost:8000/wallets/me` returns `{ balanceCents: "100000", ... }`.

Include the actual curl for the password grant in a fenced bash block (Keycloak 26 token endpoint at `http://localhost:8080/realms/crash-game/protocol/openid-connect/token` with `grant_type=password`, `client_id=crash-game-client`, `username=player`, `password=player123`).

The wallet provisioning endpoints ship in Phase 3; in Phase 1 the curl commands return 404 from Kong (route not yet declared). Document this caveat explicitly: "Phase 1 only verifies the auth path works; wallet endpoints land in Phase 3."

**Section 5: `## Healthchecks`**

Plain prose with manual curls a developer can run after `bun run docker:up`:
- Postgres: `docker compose exec postgres pg_isready -U admin` → expect `accepting connections`
- RabbitMQ: `curl -u admin:admin http://localhost:15672/api/overview | jq .rabbitmq_version` → expect a 4.2.x string
- Keycloak: `curl -sf http://localhost:9000/health/ready` → expect 200
- Kong: `curl -sf http://localhost:8001/status` → expect 200
- Games: `wget -qO- http://localhost:4001/health` → expect 200
- Wallets: `wget -qO- http://localhost:4002/health` → expect 200

Add a one-liner: "`bun run smoke:health` runs all of the above and exits 0 if every probe passes (ships in P1.10)."

**Section 6: `## Project structure`**

Tree-style overview using fenced text block (NOT code; just indented bullets):
```
fullstack-challenge/
├── .bun-version              # Bun 1.3.11 pin
├── docker-compose.yml        # Postgres, RabbitMQ, Keycloak, Kong, games, wallets
├── docker/                   # Container init artifacts (Postgres init script, Keycloak realm, Kong config)
├── packages/
│   ├── shared-kernel/        # Money VO, error taxonomy, event envelope, branded IDs, env schema
│   ├── contracts/            # Wire-format helpers (money snapshot); Phase 2+ adds event + DTO schemas
│   └── eslint-plugin/        # Custom no-number-for-money rule
├── services/
│   ├── games/                # NestJS service — round loop, bets, provably-fair (Phase 4+)
│   └── wallets/              # NestJS service — wallet + transaction aggregates (Phase 3+)
├── frontend/                 # Placeholder — TanStack Start app (Phase 7)
└── .planning/
    ├── PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md
    ├── adrs/                 # Architecture Decision Records
    ├── research/             # Stack + architecture + features + pitfalls + summary
    └── phases/               # Per-phase plans and research
```

**Section 7: `## Architecture Decision Records`**

Single paragraph plus a link: "Every significant decision is captured in `.planning/adrs/`. See the [ADR catalogue](.planning/adrs/README.md) for the index. Phase 1 ships ADR-001 through ADR-006 covering ORM choice, Money representation, Bun pinning, config source-of-truth, wallet seed strategy, and ESLint plugin location."

**Section 8: `## Roadmap`**

One-paragraph reference: "This is a 10-phase build. Phase 1 (Foundation & Infra) ships the bootstrap surface; subsequent phases land the outbox/inbox spine, wallet service, game core, saga integration, WebSocket gateway, frontend, provably-fair UX, auto features + leaderboard, and quality hardening. See `.planning/ROADMAP.md` for the full plan."

After section 8, end the file. NO footer. NO "Made with love" or AI attribution per CLAUDE.md rules. NO emojis.

Tone check before saving: read the whole file as if you were the recruiter. Does it answer "what is this?", "how do I start it?", "how do I log in?" without forcing a click into source? If yes, ship. If no, revise.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge &amp;&amp; test -f README.md &amp;&amp; for sec in '^# Crash Game' '^## Quickstart' '^## Environment variables' '^## Demo user' '^## Healthchecks' '^## Project structure' '^## Architecture Decision Records' '^## Roadmap'; do grep -qE "$sec" README.md || { echo "missing section $sec"; exit 1; }; done &amp;&amp; grep -q 'bun run docker:up' README.md &amp;&amp; grep -q 'player/player123\|player.*player123' README.md &amp;&amp; grep -q 'INITIAL_BALANCE_CENTS' README.md &amp;&amp; grep -q '\.planning/adrs' README.md &amp;&amp; grep -q 'crash-game-client' README.md &amp;&amp; ! grep -qiE 'co-authored-by|generated by|claude|chatgpt|gpt-' README.md &amp;&amp; ! grep -qE '[😀-🙏🚀🎉]' README.md &amp;&amp; wc -l README.md | awk '{exit ($1 &lt; 80) ? 1 : 0}'</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at repo root.
    - All eight required sections are present.
    - File is at least 80 lines.
    - Quickstart references `bun run docker:up`, `bun run docker:down`, `bun run docker:prune`, `bun run smoke:health`.
    - Demo user section explicitly mentions `player/player123` and `INITIAL_BALANCE_CENTS=100000`.
    - Demo user section provides the Keycloak password-grant curl command for token acquisition.
    - Environment variables section has a table with at least 19 rows (one per Open Configuration Value).
    - Healthchecks section provides curls for all 6 services (postgres, rabbitmq, keycloak on port 9000, kong, games on 4001, wallets on 4002).
    - ADR section links to `.planning/adrs/README.md`.
    - No emojis, no AI fingerprints (`Co-Authored-By`, `Generated by`, `Claude`, `GPT`).
    - All code blocks are properly fenced.
  </acceptance_criteria>
  <done>Phase 1 README is in place; a recruiter can clone, bootstrap, and log in without reading source code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → recruiter's first 10 minutes | Sets the entire evaluation tone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.9-01 | Information Disclosure | Demo credentials in README | accept | Already in realm export (P1.2); local-only by design; documented constraint |
| T-01.9-02 | Repudiation | AI fingerprints in README signal AI-generated submission | mitigate | Explicit grep for `Co-Authored-By`, `Generated by`, `Claude`, `GPT` in verify step |
| T-01.9-03 | Tampering | README env table drifts from `.env.example` | accept | Phase 10 verification re-syncs; treat README as living doc |
</threat_model>

<verification>
After all tasks complete:
- `README.md` exists with all eight sections.
- `grep -qE "(Co-Authored-By|Generated by|Claude|GPT)" README.md` returns nothing (exit 1).
- `grep -cE "^##" README.md` returns 7 (one H2 per section after the H1).
- Manual read: a developer cloning the repo can follow the README and reach the demo user's wallet (after Phase 3 ships) without additional guidance.
</verification>

<success_criteria>
- README is in place and complete for Phase 1.
- Demo user surface (REQ-AUTH-05 + REQ-DOC-03) is documented with the actual credentials, the auto-provisioning behavior, and the manual curl fallback.
- ADR catalogue is discoverable.
- File is free of AI fingerprints and emojis per CLAUDE.md rules.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-infra/01-09-SUMMARY.md` documenting:
- Final section structure (one bullet per H2)
- Confirmation that AI fingerprint and emoji checks passed
- Note any sections that Phase 7/8/10 will extend (UI screenshots in Phase 7, provably-fair example in Phase 8, CI badges + architecture diagrams in Phase 10)
</output>
