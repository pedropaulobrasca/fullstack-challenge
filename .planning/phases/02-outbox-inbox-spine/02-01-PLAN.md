---
phase: 02-outbox-inbox-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/messaging-spine/package.json
  - packages/messaging-spine/tsconfig.json
  - packages/messaging-spine/src/index.ts
  - packages/messaging-spine/src/outbox/.gitkeep
  - packages/messaging-spine/src/inbox/.gitkeep
  - packages/messaging-spine/src/dead-letter/.gitkeep
  - packages/messaging-spine/src/envelope/.gitkeep
  - packages/messaging-spine/src/topology/.gitkeep
  - packages/messaging-spine/src/context/.gitkeep
  - packages/messaging-spine/src/migrations/shared/.gitkeep
  - packages/messaging-spine/tests/unit/.gitkeep
  - packages/messaging-spine/tests/integration/_helpers/.gitkeep
  - package.json
  - tsconfig.base.json
autonomous: false
requirements: [REQ-WALL-05, REQ-WALL-06, REQ-SAGA-05, REQ-SAGA-06]

must_haves:
  truths:
    - "Workspace package @crash/messaging-spine resolves from both services via bun workspace linking"
    - "All Phase 2 source directories exist with a barrel index ready to receive concrete files in later plans"
    - "Every runtime dependency listed in 02-RESEARCH §Standard Stack is installed and version-pinned in messaging-spine/package.json"
    - "Each [ASSUMED] package was verified against its public registry before install via a blocking checkpoint"
  artifacts:
    - path: "packages/messaging-spine/package.json"
      provides: "Workspace package manifest (name @crash/messaging-spine, version 0.1.0, type=module)"
      contains: "@crash/messaging-spine"
    - path: "packages/messaging-spine/tsconfig.json"
      provides: "TS strict config extending tsconfig.base.json with decorators enabled"
      contains: "experimentalDecorators"
    - path: "packages/messaging-spine/src/index.ts"
      provides: "Public API barrel (empty exports placeholder)"
      min_lines: 1
  key_links:
    - from: "root package.json workspaces"
      to: "packages/messaging-spine"
      via: "bun workspaces entry"
      pattern: "packages/\\*"
    - from: "services/games/package.json"
      to: "@crash/messaging-spine"
      via: "workspace:* dependency"
      pattern: "@crash/messaging-spine.*workspace"
---

<objective>
Create the `@crash/messaging-spine` workspace package skeleton, install all runtime + dev dependencies that Phase 2 needs, and add the package as a `workspace:*` dependency in both `services/games` and `services/wallets`. No business logic ships in this plan — only the skeleton and validated installs so subsequent waves have a stable target.

Purpose: Every later plan touches files inside `packages/messaging-spine/`. Without the package skeleton existing and linked, parallel waves cannot start. The dependency install runs here (not later) because legitimacy verification of [ASSUMED] packages is a blocking checkpoint per the Phase 2 Package Legitimacy Audit — gating it once at the start avoids 5 separate human-verify points scattered through the phase.

Output: A new workspace package, populated `package.json` files in both services, populated `bun.lock`, and an empty-but-structured directory tree ready for envelope helpers, topology defaults, outbox/inbox/dead-letter code, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-outbox-inbox-spine/02-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Phase 1 verified workspace shape -->
From package.json (root):
- Workspaces include packages/*, services/*
- packageManager: bun@1.3.11
- Scripts: docker:up, docker:down, docker:prune, lint, smoke:health

From packages/shared-kernel/package.json:
- Package name: "@crash/shared-kernel"
- Exports DomainEventEnvelope (already used by other consumers via @crash/shared-kernel)

From services/games/package.json + services/wallets/package.json:
- Both declare "@crash/shared-kernel": "workspace:*" — the convention to follow
- Both already declare @mikro-orm/core, @mikro-orm/postgresql, @mikro-orm/nestjs, @mikro-orm/migrations, @nestjs/* @ ^11.1.21
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workspace package skeleton</name>
  <files>
    packages/messaging-spine/package.json,
    packages/messaging-spine/tsconfig.json,
    packages/messaging-spine/src/index.ts,
    packages/messaging-spine/src/outbox/.gitkeep,
    packages/messaging-spine/src/inbox/.gitkeep,
    packages/messaging-spine/src/dead-letter/.gitkeep,
    packages/messaging-spine/src/envelope/.gitkeep,
    packages/messaging-spine/src/topology/.gitkeep,
    packages/messaging-spine/src/context/.gitkeep,
    packages/messaging-spine/src/migrations/shared/.gitkeep,
    packages/messaging-spine/tests/unit/.gitkeep,
    packages/messaging-spine/tests/integration/_helpers/.gitkeep
  </files>
  <read_first>
    - packages/shared-kernel/package.json (mirror the field shape: name, version, type, main, exports, scripts.build, scripts.test, dependencies, peerDependencies, devDependencies)
    - packages/shared-kernel/tsconfig.json (mirror compilerOptions including experimentalDecorators, emitDecoratorMetadata, strict, module, target — these are required for MikroORM entity decorators per ADR-001 and for Nest decorators in later plans)
    - package.json (root, to confirm workspaces glob picks up packages/*)
    - tsconfig.base.json if it exists (otherwise use shared-kernel's tsconfig as the parent)
  </read_first>
  <action>Create the `packages/messaging-spine/` directory with a package.json named `@crash/messaging-spine`, version `0.1.0`, `"type": "module"`, `"main": "./src/index.ts"`, `"exports": { ".": "./src/index.ts" }`, `"scripts": { "build": "tsc -p tsconfig.json", "typecheck": "tsc --noEmit", "test": "bun test tests/unit", "test:integration": "INTEGRATION=1 bun test" }`, `peerDependencies` declaring `@nestjs/common ^11.1.21`, `@nestjs/core ^11.1.21`, `@mikro-orm/core ^7.1.0`, `@mikro-orm/postgresql ^7.1.0`, `@mikro-orm/nestjs ^7.0.0`, `@crash/shared-kernel workspace:*`. Create tsconfig.json extending `../shared-kernel/tsconfig.json` (or the root tsconfig.base.json — whichever Phase 1 used) with `compilerOptions.experimentalDecorators=true`, `emitDecoratorMetadata=true`, `strict=true`, `moduleResolution=Bundler`, `outDir=dist`, and `include: ["src/**/*.ts", "tests/**/*.ts"]`. Create `src/index.ts` containing only a comment `// @crash/messaging-spine — public API barrel. Concrete exports added by P2.2 onward.` plus `export {};`. Create empty `.gitkeep` files under every subdirectory listed in the files element so the structure survives git.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge && test -f packages/messaging-spine/package.json && test -f packages/messaging-spine/tsconfig.json && test -f packages/messaging-spine/src/index.ts && bun -e "console.log(require('./packages/messaging-spine/package.json').name)" | grep -x "@crash/messaging-spine"</automated>
  </verify>
  <done>Package directory exists with manifest, tsconfig, barrel, and all subfolders tracked via .gitkeep. `bun -e require(...)` resolves the package name correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify legitimacy of every [ASSUMED] package before install</name>
  <what-built>The Phase 2 Package Legitimacy Audit (02-RESEARCH §Package Legitimacy Audit) tagged amqplib, @types/amqplib, @golevelup/nestjs-rabbitmq, nestjs-cls, pg, @types/pg, uuid, @types/uuid as [ASSUMED]. Per the audit's protocol, every [ASSUMED] package requires a blocking-human checkpoint before `bun add`. This task surfaces the inspection output for human approval.</what-built>
  <how-to-verify>
For each of the eight packages, run the following two commands and inspect the output (do NOT install yet):

1. `bun pm info amqplib | head -25` — confirm a real maintainer, recent version, repository URL, weekly download count > 1k
2. `bun pm info amqplib | grep -E "(scripts|postinstall)"` — confirm no `postinstall` hook is present

Repeat for: `@types/amqplib`, `@golevelup/nestjs-rabbitmq`, `nestjs-cls`, `pg`, `@types/pg`, `uuid`, `@types/uuid`.

Cross-reference each package by visiting `https://www.npmjs.com/package/<name>` and confirming:
- Repository link resolves to a real GitHub/GitLab repo
- Last publish date is within the last 12 months (or the package is stable like `pg` / `uuid` with 10y+ history)
- Weekly downloads > 1,000
- No security advisories listed on npm or `https://github.com/advisories?query=<package>`

Report `MISSING`, `MISMATCH`, or `OK` for each package.
  </how-to-verify>
  <resume-signal>Type "approved" once every package is OK, "block <names>" to list rejected packages, or "investigate <name>" for any package that looks suspicious. SUS / SLOP packages must NOT be installed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Install dependencies and wire workspace links</name>
  <files>
    packages/messaging-spine/package.json,
    services/games/package.json,
    services/wallets/package.json,
    package.json,
    bun.lock
  </files>
  <read_first>
    - services/games/package.json (to know exact existing dependency versions for @nestjs/* @ ^11.1.21 — must match)
    - services/wallets/package.json (same — must match the running NestJS major)
    - The Task 2 checkpoint result (legitimacy report) — only proceed if approved
  </read_first>
  <action>Inside `packages/messaging-spine/`, run `bun add amqplib @golevelup/nestjs-rabbitmq nestjs-cls pg uuid` and `bun add -d @types/amqplib @types/pg @types/uuid testcontainers @testcontainers/postgresql @testcontainers/rabbitmq`. Then in `services/games/` and `services/wallets/` (separately, both required), run `bun add @crash/messaging-spine amqplib @golevelup/nestjs-rabbitmq nestjs-cls` to add the workspace package and the consumer-side direct deps. Confirm `bun.lock` updates at the repo root and every `package.json` lists the dependencies under the correct sections (runtime deps under `dependencies`, types under `devDependencies`). Do NOT yet wire `MessagingSpineModule` into either AppModule — that lands in plan 02-07.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge && grep -q "@crash/messaging-spine" services/games/package.json && grep -q "@crash/messaging-spine" services/wallets/package.json && grep -q "amqplib" packages/messaging-spine/package.json && grep -q "@golevelup/nestjs-rabbitmq" packages/messaging-spine/package.json && grep -q "nestjs-cls" packages/messaging-spine/package.json && grep -q "\"pg\"" packages/messaging-spine/package.json && cd packages/messaging-spine && bun run typecheck</automated>
  </verify>
  <done>All eight runtime/dev dependencies recorded in `packages/messaging-spine/package.json`; both services declare `@crash/messaging-spine: workspace:*`; `bun.lock` updated; `bun run typecheck` inside the new package exits 0 against the empty barrel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Third-party packages introduce supply-chain risk (Pitfall: T-02-SC) |
| Workspace package boundary | @crash/messaging-spine will be imported by both services in later plans; a malicious package added here becomes a transitive risk for the entire system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (amqplib, @golevelup/nestjs-rabbitmq, nestjs-cls, pg, uuid, @types/*) | mitigate | Blocking human-verify checkpoint (Task 2) inspects every [ASSUMED] package via `bun pm info` + npmjs.com manual check before `bun add` runs; no [SUS] or [SLOP] packages installed |
| T-02-01 | Tampering | postinstall hooks | mitigate | Task 2 explicitly inspects scripts.postinstall on every package; absence required for OK status |
| T-02-02 | Information Disclosure | bun.lock committed to repo | accept | bun.lock is intended to be committed (reproducible installs); contains no secrets — only package coordinates |
</threat_model>

<verification>
1. `cd /Users/pedro/Projetos/fullstack-challenge && bun install` exits 0 (resolves workspace links)
2. `cd packages/messaging-spine && bun run typecheck` exits 0 against the empty barrel
3. `cd packages/messaging-spine && bun -e "console.log(Object.keys(require('./package.json').dependencies))"` lists amqplib, @golevelup/nestjs-rabbitmq, nestjs-cls, pg, uuid
4. `cd services/games && bun -e "console.log(Object.keys(require('./package.json').dependencies))"` includes "@crash/messaging-spine"
5. `cd services/wallets && bun -e "console.log(Object.keys(require('./package.json').dependencies))"` includes "@crash/messaging-spine"
6. `bun run lint` from the repo root exits 0 (no new files violate existing rules)
</verification>

<success_criteria>
- New workspace package `@crash/messaging-spine` exists with manifest, tsconfig, barrel, and all subdirectories tracked
- All eight runtime/dev dependencies installed under the correct sections (verified by package.json grep)
- Both services declare the workspace dependency
- Legitimacy of every [ASSUMED] package was approved via human-verify checkpoint BEFORE `bun add` ran
- `bun install` at the repo root succeeds and workspace symlinks resolve in both services
- `bun run typecheck` inside the new package succeeds against the empty barrel (no spurious TS errors from peer deps)
- ROADMAP/STATE not touched (those land in the closing plan of the phase)
</success_criteria>

<output>
Create `.planning/phases/02-outbox-inbox-spine/02-01-SUMMARY.md` when done, documenting:
- The exact versions of each package installed (from `bun pm info` output)
- The legitimacy checkpoint outcome per package
- Any deviation from the planned set (e.g., if a package was rejected as SUS, what substitute was used)
- Files created and their roles
</output>
