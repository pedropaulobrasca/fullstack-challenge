---
phase: 02-outbox-inbox-spine
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/messaging-spine/src/envelope/build-envelope.ts
  - packages/messaging-spine/src/envelope/envelope-headers.ts
  - packages/messaging-spine/src/topology/topology-defaults.ts
  - packages/messaging-spine/src/topology/topology-config.ts
  - packages/messaging-spine/src/context/correlation-tokens.ts
  - packages/messaging-spine/src/context/messaging-cls.ts
  - packages/messaging-spine/src/index.ts
autonomous: true
requirements: [REQ-WALL-06, REQ-SAGA-05, REQ-SAGA-06]

must_haves:
  truths:
    - "buildEnvelope() produces a DomainEventEnvelope with messageId, correlationId, causationId, type, version, occurredAt, payload — every field non-null and ISO-typed"
    - "parseEnvelope() round-trips JSON.stringify(buildEnvelope(...)) without loss"
    - "Topology defaults expose every exchange + queue + DLX constant used by Wallet/Game services so no string literals appear in service code"
    - "buildQuorumArgs() returns the exact x-queue-type/x-delivery-limit/x-dead-letter-exchange shape that RabbitMQ 4.2 accepts"
    - "MessagingClsModule.forRoot() exposes CLS with correlationId/causationId keys and a generateId hook"
    - "withMessagingContext(cls, {correlationId, causationId}, fn) lets non-request code (round loop, recovery scans) populate CLS explicitly"
  artifacts:
    - path: "packages/messaging-spine/src/envelope/build-envelope.ts"
      provides: "buildEnvelope<TPayload>(input) → DomainEventEnvelope<TPayload>; parseEnvelope(raw) → DomainEventEnvelope"
      exports: ["buildEnvelope", "parseEnvelope", "EnvelopeBuildOptions"]
    - path: "packages/messaging-spine/src/envelope/envelope-headers.ts"
      provides: "envelopeToAmqpHeaders + amqpHeadersToEnvelopeMeta to bridge envelope <-> AMQP header bag"
      exports: ["envelopeToAmqpHeaders", "amqpHeadersToEnvelopeMeta", "AMQP_HEADER_KEYS"]
    - path: "packages/messaging-spine/src/topology/topology-defaults.ts"
      provides: "EXCHANGES + QUEUES constants + buildQuorumArgs() helper"
      exports: ["EXCHANGES", "QUEUES", "buildQuorumArgs", "deriveDlxFromExchange"]
    - path: "packages/messaging-spine/src/topology/topology-config.ts"
      provides: "zod schema describing TopologyConfig (exchanges, queues with deliveryLimit, bindings)"
      exports: ["topologyConfigSchema", "TopologyConfig"]
    - path: "packages/messaging-spine/src/context/correlation-tokens.ts"
      provides: "CORRELATION_ID_KEY, CAUSATION_ID_KEY string constants used by CLS get/set calls"
      exports: ["CORRELATION_ID_KEY", "CAUSATION_ID_KEY"]
    - path: "packages/messaging-spine/src/context/messaging-cls.ts"
      provides: "MessagingClsModule.forRoot() + withMessagingContext() helper"
      exports: ["MessagingClsModule", "withMessagingContext"]
  key_links:
    - from: "buildEnvelope payload + meta"
      to: "outbox row (headers JSONB + payload JSONB)"
      via: "OutboxRepository.add() in plan 02-04 calls envelopeToAmqpHeaders to split"
      pattern: "envelopeToAmqpHeaders"
    - from: "amqpHeadersToEnvelopeMeta"
      to: "IdempotentSubscribe decorator (plan 02-05)"
      via: "consumer extracts correlationId+causationId from AMQP headers into CLS"
      pattern: "amqpHeadersToEnvelopeMeta"
    - from: "buildQuorumArgs"
      to: "TopologyBootstrap service (plan 02-06)"
      via: "channel.assertQueue uses these args verbatim"
      pattern: "x-queue-type.*quorum"
---

<objective>
Ship the three contract layers that every other Phase 2 plan depends on but no Phase 2 plan owns alone: (1) the DomainEventEnvelope build/parse helpers + AMQP-header bridge, (2) the canonical topology constants + quorum-args helper + zod TopologyConfig schema, and (3) the nestjs-cls module + `withMessagingContext` helper for correlation/causation propagation through both HTTP-originated and scheduled-task-originated flows.

Purpose: These three concerns are the "contracts" of the messaging spine — the outbox writer (plan 02-04), the polling publisher (plan 02-04), the idempotent consumer wrapper (plan 02-05), the dead-letter consumer (plan 02-05), and the topology bootstrap (plan 02-06) all consume types declared here. Defining them first removes the scavenger hunt and lets later plans reference exact import names.

Output: Six TypeScript files implementing the envelope, topology, and CLS contracts, all re-exported from the public barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-outbox-inbox-spine/02-RESEARCH.md
@packages/shared-kernel/src/events/envelope.ts
@CLAUDE.md

<interfaces>
<!-- The DomainEventEnvelope shape is already defined in @crash/shared-kernel — we DO NOT redefine it -->
From packages/shared-kernel/src/events/envelope.ts:
```
export interface DomainEventEnvelope<TPayload = unknown> {
  readonly messageId: string;
  readonly correlationId: string;
  readonly causationId: string;
  readonly type: string;
  readonly version: number;
  readonly occurredAt: string;
  readonly payload: TPayload;
}
```

<!-- 02-RESEARCH §Code Examples confirms the exact AMQP options object the publisher will use -->
amqplib publish opts that the OutboxPublisher (plan 02-04) will pass to channel.publish:
```
{
  messageId: row.message_id,
  type: row.event_type,
  contentType: "application/json",
  persistent: true,
  headers: row.headers,             // <-- this is what envelopeToAmqpHeaders builds
}
```

<!-- 02-RESEARCH §RabbitMQ topology (TypeScript declaration) defines exact constants -->
EXCHANGES: WALLET_COMMANDS ("wallet.commands" direct durable), WALLET_EVENTS ("wallet.events" topic durable),
           GAME_EVENTS ("game.events" topic durable), WALLET_DLX ("wallet.dlx" fanout durable), GAME_DLX ("game.dlx" fanout durable)
QUEUES: WALLET_COMMANDS ("wallet.commands.q"), GAMES_WALLET_EVENTS ("games.wallet-events.q"),
        WALLET_DLQ ("wallet.dlq"), GAMES_DLQ ("games.dlq")
buildQuorumArgs(deliveryLimit, dlxName?) returns object with x-queue-type=quorum, x-delivery-limit=deliveryLimit,
                                          and x-dead-letter-exchange=dlxName if provided

<!-- 02-RESEARCH §Correlation/causation context with nestjs-cls confirms the canonical setup -->
Per 02-RESEARCH lines 741-775:
- ClsModule.forRoot({ global: true, middleware: { mount: true, generateId: true, idGenerator: () => crypto.randomUUID(), setup: (cls, req) => { cls.set('correlationId', req.headers['x-correlation-id'] ?? req.headers['x-request-id'] ?? cls.getId()); } } })
- withMessagingContext(cls, ctx, fn): cls.run(...) wrapper that sets correlationId/causationId before invoking fn
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Envelope helpers + AMQP header bridge</name>
  <files>
    packages/messaging-spine/src/envelope/build-envelope.ts,
    packages/messaging-spine/src/envelope/envelope-headers.ts
  </files>
  <read_first>
    - packages/shared-kernel/src/events/envelope.ts (the DomainEventEnvelope interface — IMPORT this, do not redefine)
    - packages/shared-kernel/src/events/domain-event.ts (to know if a related type needs re-exporting)
    - .planning/phases/02-outbox-inbox-spine/02-RESEARCH.md §Code Examples lines 569-665 (OutboxPublisher pseudocode showing how headers are used at publish time)
  </read_first>
  <action>In `build-envelope.ts`, define `export interface EnvelopeBuildOptions { type: string; version?: number; payload: unknown; correlationId?: string; causationId: string; occurredAt?: Date; messageId?: string; }`. Implement `export function buildEnvelope<TPayload>(opts: EnvelopeBuildOptions): DomainEventEnvelope<TPayload>` that imports DomainEventEnvelope from `@crash/shared-kernel`, uses `crypto.randomUUID()` (the Web Crypto API, available in Bun + Node 19+) as the default for messageId, defaults version to 1, defaults occurredAt to `new Date().toISOString()`, defaults correlationId to `opts.messageId ?? crypto.randomUUID()` IF the caller did not supply it (so a brand-new flow self-correlates), and treats causationId as REQUIRED (throw `Error('causationId is required — pass the upstream messageId or, for flow-origin events, the same value as messageId')` if missing). Implement `export function parseEnvelope<TPayload = unknown>(raw: string | Buffer): DomainEventEnvelope<TPayload>` that does `JSON.parse(typeof raw === 'string' ? raw : raw.toString('utf8'))`, validates that the seven required fields exist and are typed correctly (string/string/string/string/number/string/anything), and throws `Error('Invalid envelope: missing <field>')` on the first missing field. In `envelope-headers.ts`, declare `export const AMQP_HEADER_KEYS = { CORRELATION_ID: 'x-correlation-id', CAUSATION_ID: 'x-causation-id', MESSAGE_ID: 'x-message-id', EVENT_TYPE: 'x-event-type', EVENT_VERSION: 'x-event-version', OCCURRED_AT: 'x-occurred-at' } as const;`. Implement `export function envelopeToAmqpHeaders(env: DomainEventEnvelope): Record<string, string | number>` that returns `{ [AMQP_HEADER_KEYS.CORRELATION_ID]: env.correlationId, [AMQP_HEADER_KEYS.CAUSATION_ID]: env.causationId, [AMQP_HEADER_KEYS.MESSAGE_ID]: env.messageId, [AMQP_HEADER_KEYS.EVENT_TYPE]: env.type, [AMQP_HEADER_KEYS.EVENT_VERSION]: env.version, [AMQP_HEADER_KEYS.OCCURRED_AT]: env.occurredAt }`. Implement `export function amqpHeadersToEnvelopeMeta(headers: Record<string, unknown> | undefined): { correlationId: string; causationId: string; messageId: string; type: string; version: number; occurredAt: string }` that reads each AMQP_HEADER_KEYS entry, coerces version to number, and throws if any required header is absent (specifically correlationId, causationId, messageId, type — version and occurredAt may default to 1 / now()).</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/packages/messaging-spine && bun run typecheck && bun -e "import { buildEnvelope, parseEnvelope } from './src/envelope/build-envelope.ts'; import { envelopeToAmqpHeaders, amqpHeadersToEnvelopeMeta, AMQP_HEADER_KEYS } from './src/envelope/envelope-headers.ts'; const e = buildEnvelope({ type: 'test.thing', payload: { x: 1 }, causationId: 'abc-123' }); if (!e.messageId || !e.correlationId || e.causationId !== 'abc-123' || e.payload.x !== 1) { console.error('build broken'); process.exit(1); } const round = parseEnvelope(JSON.stringify(e)); if (round.messageId !== e.messageId) { console.error('round-trip broken'); process.exit(1); } const h = envelopeToAmqpHeaders(e); const meta = amqpHeadersToEnvelopeMeta(h); if (meta.correlationId !== e.correlationId) { console.error('header bridge broken'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <done>buildEnvelope returns a complete DomainEventEnvelope with all seven fields; parseEnvelope round-trips losslessly; AMQP header bridge maps every field bidirectionally; causationId is required (throws if missing).</done>
</task>

<task type="auto">
  <name>Task 2: Topology defaults + zod TopologyConfig schema</name>
  <files>
    packages/messaging-spine/src/topology/topology-defaults.ts,
    packages/messaging-spine/src/topology/topology-config.ts
  </files>
  <read_first>
    - .planning/phases/02-outbox-inbox-spine/02-RESEARCH.md §RabbitMQ topology (TypeScript declaration) lines 527-565
    - .planning/research/ARCHITECTURE.md §1 Component Diagram (exchanges + queues block lines 44-54) and §4.1 Message Catalog (routing keys: wallet.debit, wallet.credit, wallet.debited, wallet.debit.rejected, wallet.credited, plus game.events topics)
    - The packages/shared-kernel/src/config/env-schema.ts to know the zod conventions in use
  </read_first>
  <action>In `topology-defaults.ts`, declare `export const EXCHANGES = { WALLET_COMMANDS: 'wallet.commands', WALLET_EVENTS: 'wallet.events', GAME_EVENTS: 'game.events', WALLET_DLX: 'wallet.dlx', GAME_DLX: 'game.dlx' } as const;` and `export const QUEUES = { WALLET_COMMANDS: 'wallet.commands.q', GAMES_WALLET_EVENTS: 'games.wallet-events.q', WALLET_DLQ: 'wallet.dlq', GAMES_DLQ: 'games.dlq' } as const;`. Add `export type ExchangeName = typeof EXCHANGES[keyof typeof EXCHANGES];` and `export type QueueName = typeof QUEUES[keyof typeof QUEUES];`. Implement `export function buildQuorumArgs(deliveryLimit: number, dlxName?: string): Record<string, unknown>` that returns `{ 'x-queue-type': 'quorum', 'x-delivery-limit': deliveryLimit, ...(dlxName ? { 'x-dead-letter-exchange': dlxName } : {}) }`. Implement `export function deriveDlxFromExchange(exchange: string): string` that maps `wallet.commands -> wallet.dlx`, `wallet.events -> wallet.dlx`, `game.events -> game.dlx`, throwing on unknown — this helper is used by the IdempotentSubscribe decorator (plan 02-05) so a queue declared inline always gets the right DLX. In `topology-config.ts`, build a zod schema: `export const topologyConfigSchema = z.object({ exchangesToAssert: z.array(z.object({ name: z.string(), type: z.enum(['direct','topic','fanout']), durable: z.boolean().default(true) })), queuesToAssert: z.array(z.object({ name: z.string(), deliveryLimit: z.number().int().positive(), dlx: z.string().optional() })), bindings: z.array(z.object({ queue: z.string(), exchange: z.string(), routingKey: z.string() })) });` and `export type TopologyConfig = z.infer<typeof topologyConfigSchema>;`.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/packages/messaging-spine && bun run typecheck && bun -e "import { EXCHANGES, QUEUES, buildQuorumArgs, deriveDlxFromExchange } from './src/topology/topology-defaults.ts'; import { topologyConfigSchema } from './src/topology/topology-config.ts'; if (EXCHANGES.WALLET_COMMANDS !== 'wallet.commands') process.exit(1); const args = buildQuorumArgs(5, 'wallet.dlx'); if (args['x-queue-type'] !== 'quorum' || args['x-delivery-limit'] !== 5 || args['x-dead-letter-exchange'] !== 'wallet.dlx') process.exit(2); const noDlx = buildQuorumArgs(3); if (noDlx['x-dead-letter-exchange'] !== undefined) process.exit(3); if (deriveDlxFromExchange('wallet.commands') !== 'wallet.dlx') process.exit(4); const parsed = topologyConfigSchema.safeParse({ exchangesToAssert: [{ name: 'a', type: 'direct' }], queuesToAssert: [{ name: 'b', deliveryLimit: 5 }], bindings: [{ queue: 'b', exchange: 'a', routingKey: '' }] }); if (!parsed.success) process.exit(5); console.log('OK');"</automated>
  </verify>
  <done>EXCHANGES + QUEUES constants exported with frozen values; buildQuorumArgs produces RabbitMQ-4.2-compliant arg objects including conditional DLX; deriveDlxFromExchange covers all five exchanges; topologyConfigSchema accepts the canonical wallet+game shape.</done>
</task>

<task type="auto">
  <name>Task 3: nestjs-cls module + withMessagingContext helper</name>
  <files>
    packages/messaging-spine/src/context/correlation-tokens.ts,
    packages/messaging-spine/src/context/messaging-cls.ts,
    packages/messaging-spine/src/index.ts
  </files>
  <read_first>
    - .planning/phases/02-outbox-inbox-spine/02-RESEARCH.md lines 739-778 (the messaging-cls.ts reference block)
    - .planning/phases/02-outbox-inbox-spine/02-RESEARCH.md §Pitfall 8 (withMessagingContext rationale for non-request contexts like the round loop)
    - packages/messaging-spine/src/index.ts (current barrel; need to append new exports)
  </read_first>
  <action>In `correlation-tokens.ts`, declare `export const CORRELATION_ID_KEY = 'correlationId' as const;` and `export const CAUSATION_ID_KEY = 'causationId' as const;`. In `messaging-cls.ts`, import `ClsModule, ClsService` from `nestjs-cls`, `DynamicModule, Module` from `@nestjs/common`. Define `@Module({}) export class MessagingClsModule { static forRoot(): DynamicModule { return { module: MessagingClsModule, imports: [ClsModule.forRoot({ global: true, middleware: { mount: true, generateId: true, idGenerator: () => crypto.randomUUID(), setup: (cls, req) => { const incoming = req.headers?.['x-correlation-id'] ?? req.headers?.['x-request-id']; cls.set(CORRELATION_ID_KEY, typeof incoming === 'string' ? incoming : cls.getId()); } } })], exports: [ClsModule] }; } }`. Then export `export async function withMessagingContext<T>(cls: ClsService, ctx: { correlationId: string; causationId?: string }, fn: () => Promise<T>): Promise<T> { return cls.run(async () => { cls.set(CORRELATION_ID_KEY, ctx.correlationId); if (ctx.causationId) cls.set(CAUSATION_ID_KEY, ctx.causationId); return fn(); }); }`. Finally update `packages/messaging-spine/src/index.ts` to additionally export everything: `export { buildEnvelope, parseEnvelope, type EnvelopeBuildOptions } from './envelope/build-envelope';`, `export { envelopeToAmqpHeaders, amqpHeadersToEnvelopeMeta, AMQP_HEADER_KEYS } from './envelope/envelope-headers';`, `export { EXCHANGES, QUEUES, buildQuorumArgs, deriveDlxFromExchange, type ExchangeName, type QueueName } from './topology/topology-defaults';`, `export { topologyConfigSchema, type TopologyConfig } from './topology/topology-config';`, `export { CORRELATION_ID_KEY, CAUSATION_ID_KEY } from './context/correlation-tokens';`, `export { MessagingClsModule, withMessagingContext } from './context/messaging-cls';`.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/packages/messaging-spine && bun run typecheck && bun -e "import { MessagingClsModule, withMessagingContext, CORRELATION_ID_KEY, CAUSATION_ID_KEY, buildEnvelope, EXCHANGES } from './src/index.ts'; if (CORRELATION_ID_KEY !== 'correlationId' || CAUSATION_ID_KEY !== 'causationId') process.exit(1); if (typeof MessagingClsModule.forRoot !== 'function') process.exit(2); if (typeof withMessagingContext !== 'function') process.exit(3); if (EXCHANGES.WALLET_COMMANDS !== 'wallet.commands') process.exit(4); console.log('OK');"</automated>
  </verify>
  <done>MessagingClsModule.forRoot returns a DynamicModule that imports ClsModule with the documented middleware setup; withMessagingContext runs CLS scopes for non-request entry points; all six new public exports are reachable from the barrel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Envelope body → AMQP message | Headers carry tracing fields; an envelope missing causationId breaks REQ-SAGA-06 traceability |
| Topology constants → channel.assertQueue/assertExchange | Mismatched declarations cause PRECONDITION_FAILED on bootstrap (PITFALLS C4, 02-RESEARCH §Pitfall 7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | Envelope construction missing causationId | mitigate | buildEnvelope throws Error when causationId is absent; impossible to publish a traceless event |
| T-02-07 | Information Disclosure | correlationId/causationId logged or exposed externally | accept | These are opaque UUIDs intentionally surfaced for tracing; no PII content |
| T-02-08 | Tampering | Hardcoded string literal queue/exchange in service code (typo causes silent loss) | mitigate | EXCHANGES + QUEUES constants exported; services MUST import constants, not write literals. ESLint rule to enforce this can be added in Phase 10 if needed. |
</threat_model>

<verification>
1. `cd packages/messaging-spine && bun run typecheck` exits 0
2. Inline buildEnvelope smoke test produces all seven envelope fields with causationId required
3. envelopeToAmqpHeaders/amqpHeadersToEnvelopeMeta round-trip preserves correlationId
4. buildQuorumArgs(5, 'wallet.dlx') produces {x-queue-type, x-delivery-limit, x-dead-letter-exchange}; buildQuorumArgs(3) omits dlx
5. deriveDlxFromExchange returns correct DLX for all five known exchanges; throws for unknown
6. topologyConfigSchema accepts canonical wallet topology shape
7. CORRELATION_ID_KEY / CAUSATION_ID_KEY constants match the cls.get keys used in plan 02-05 IdempotentSubscribe
8. MessagingClsModule.forRoot() exports a DynamicModule
</verification>

<success_criteria>
- buildEnvelope / parseEnvelope round-trip via JSON without information loss
- causationId is REQUIRED (REQ-SAGA-06)
- AMQP header keys match the contract IdempotentSubscribe will consume (plan 02-05)
- Topology constants cover every exchange + queue mentioned in 02-RESEARCH + ARCHITECTURE.md
- buildQuorumArgs honors the RabbitMQ 4.2 quorum-queue argument names exactly
- TopologyConfig zod schema parses the canonical wallet topology
- MessagingClsModule is a proper NestJS DynamicModule re-exporting ClsModule globally
- withMessagingContext helper covers non-HTTP entry points (round loop, recovery scans — Phase 4/5 will consume it)
</success_criteria>

<output>
Create `.planning/phases/02-outbox-inbox-spine/02-03-SUMMARY.md` documenting:
- The exact buildEnvelope / parseEnvelope signatures
- The AMQP_HEADER_KEYS table (canonical header name per envelope field)
- The EXCHANGES + QUEUES tables (canonical name per logical concept)
- buildQuorumArgs arguments → RabbitMQ args mapping
- The CLS contract: who calls cls.set vs cls.get and where
</output>
