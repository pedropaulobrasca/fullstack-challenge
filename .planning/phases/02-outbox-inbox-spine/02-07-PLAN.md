---
phase: 02-outbox-inbox-spine
plan: 07
type: execute
wave: 5
depends_on: ["02-06"]
files_modified:
  - services/games/src/config/defaults.ts
  - services/games/.env.example
  - services/games/.env
  - services/wallets/src/config/defaults.ts
  - services/wallets/.env.example
  - services/wallets/.env
  - services/games/src/infrastructure/mikro-orm/migrations/20260524001-create-outbox.ts
  - services/games/src/infrastructure/mikro-orm/migrations/20260524002-create-inbox.ts
  - services/games/src/infrastructure/mikro-orm/migrations/20260524003-create-dead-letter-messages.ts
  - services/wallets/src/infrastructure/mikro-orm/migrations/20260524001-create-outbox.ts
  - services/wallets/src/infrastructure/mikro-orm/migrations/20260524002-create-inbox.ts
  - services/wallets/src/infrastructure/mikro-orm/migrations/20260524003-create-dead-letter-messages.ts
  - services/games/mikro-orm.config.ts
  - services/wallets/mikro-orm.config.ts
  - services/games/src/app.module.ts
  - services/wallets/src/app.module.ts
  - services/games/src/infrastructure/messaging/games-dead-letter.consumer.ts
  - services/wallets/src/infrastructure/messaging/wallets-dead-letter.consumer.ts
autonomous: false
requirements: [REQ-WALL-05, REQ-WALL-06, REQ-SAGA-05, REQ-SAGA-06]

must_haves:
  truths:
    - "Both services' typed env config exports OUTBOX_POLL_BATCH_SIZE (default 100) — added alongside the existing OUTBOX_POLL_INTERVAL_MS, RMQ_DELIVERY_LIMIT_MAIN, RMQ_DELIVERY_LIMIT_DLQ"
    - "Both services' MikroORM config registers OutboxMessage, InboxMessage, DeadLetterMessage entities (removing the Phase 1 warnWhenNoEntities=false override)"
    - "Both services have three migration files that execute the canonical SQL fragments from @crash/messaging-spine/src/migrations/shared/*.sql verbatim"
    - "Both AppModules import MessagingSpineModule.forRootAsync with the correct per-service TopologyConfig"
    - "Each service registers a concrete DeadLetterConsumer subclass bound to its DLQ (games.dlq for games, wallet.dlq for wallets) via @RabbitSubscribe with x-delivery-limit=3 on the DLQ itself"
    - "bun run docker:up cycles cleanly — migrate containers exit 0, both runtime services reach Healthy, RabbitMQ shows the declared quorum queues"
  artifacts:
    - path: "services/games/src/config/defaults.ts"
      provides: "GamesEnv extended with OUTBOX_POLL_BATCH_SIZE"
      contains: "OUTBOX_POLL_BATCH_SIZE"
    - path: "services/wallets/src/config/defaults.ts"
      provides: "WalletsEnv extended with OUTBOX_POLL_BATCH_SIZE + same outbox/RMQ envs as games"
      contains: "OUTBOX_POLL_BATCH_SIZE"
    - path: "services/games/src/infrastructure/mikro-orm/migrations/20260524001-create-outbox.ts"
      provides: "MikroORM migration that loads + executes 001-outbox.sql from the messaging-spine package"
      contains: "readFileSync"
    - path: "services/games/src/app.module.ts"
      provides: "AppModule importing MessagingSpineModule.forRootAsync with games TopologyConfig"
      contains: "MessagingSpineModule.forRootAsync"
    - path: "services/wallets/src/app.module.ts"
      provides: "AppModule importing MessagingSpineModule.forRootAsync with wallets TopologyConfig"
      contains: "MessagingSpineModule.forRootAsync"
    - path: "services/games/src/infrastructure/messaging/games-dead-letter.consumer.ts"
      provides: "Concrete DeadLetterConsumer subclass bound to games.dlq"
      contains: "extends DeadLetterConsumer"
    - path: "services/wallets/src/infrastructure/messaging/wallets-dead-letter.consumer.ts"
      provides: "Concrete DeadLetterConsumer subclass bound to wallet.dlq"
      contains: "extends DeadLetterConsumer"
  key_links:
    - from: "services/{games,wallets}/src/config/defaults.ts"
      to: "MessagingSpineModule MessagingOptions"
      via: "useFactory: (env) -> { amqpUrl: env.RABBITMQ_URL, databaseUrl: env.DATABASE_URL, ..., topology: {...} }"
      pattern: "useFactory.*env"
    - from: "migration files in both services"
      to: "packages/messaging-spine/src/migrations/shared/00*.sql"
      via: "readFileSync of the resolved path inside the messaging-spine package"
      pattern: "@crash/messaging-spine/src/migrations/shared"
    - from: "games-dead-letter.consumer.ts"
      to: "QUEUES.GAMES_DLQ asserted with x-delivery-limit=3"
      via: "@RabbitSubscribe queueOptions.arguments = buildQuorumArgs(env.RMQ_DELIVERY_LIMIT_DLQ)"
      pattern: "GAMES_DLQ.*deliveryLimit|buildQuorumArgs"
---

<objective>
Wire the new `@crash/messaging-spine` into both `services/games` and `services/wallets`: register the messaging entities with MikroORM, generate the three migrations per service from the shared SQL fragments, extend each service's typed env config with `OUTBOX_POLL_BATCH_SIZE`, mount `MessagingSpineModule.forRootAsync` in each AppModule with the correct per-service TopologyConfig, and add a concrete `DeadLetterConsumer` subclass per service bound to its DLQ.

Purpose: Plans 02-01 through 02-06 built the package. This plan makes it actually run inside the two NestJS services that Phase 1 stood up. Without this plan, no message ever crosses RabbitMQ; with it, `bun run docker:up` brings up both services and they assert their topology + start polling their (empty) outboxes.

Output: Both services' code, env, migrations, and module wiring updated; a docker:up cycle proves the spine is live end-to-end at the infrastructure level (concrete domain events come in Phase 3+).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-outbox-inbox-spine/02-RESEARCH.md
@services/games/src/config/defaults.ts
@services/wallets/src/config/defaults.ts
@services/games/mikro-orm.config.ts
@services/wallets/mikro-orm.config.ts
@services/games/src/app.module.ts
@services/wallets/src/app.module.ts
@packages/messaging-spine/src/index.ts
@docker-compose.yml
@CLAUDE.md

<interfaces>
<!-- Public API from @crash/messaging-spine that this plan consumes:
     MessagingSpineModule, MessagingOptions, EXCHANGES, QUEUES, buildQuorumArgs,
     DeadLetterConsumer, DeadLetterRepository, OutboxMessage, InboxMessage, DeadLetterMessage.
     @golevelup/nestjs-rabbitmq exports RabbitSubscribe + Nack used by the concrete DLQ consumers. -->

<!-- Per-service TopologyConfig (derived from 02-RESEARCH §How services consume it + ARCHITECTURE §1).

  wallets service owns:
    exchanges:
      - wallet.commands (direct, durable)
      - wallet.events   (topic, durable)
      - wallet.dlx      (fanout, durable)
    queues:
      - wallet.commands.q (deliveryLimit RMQ_DELIVERY_LIMIT_MAIN, dlx wallet.dlx)
      - wallet.dlq        (deliveryLimit RMQ_DELIVERY_LIMIT_DLQ)
    bindings:
      - wallet.commands.q <- wallet.commands :: 'wallet.debit'
      - wallet.commands.q <- wallet.commands :: 'wallet.credit'
      - wallet.dlq        <- wallet.dlx      :: ''

  games service owns:
    exchanges:
      - game.events    (topic, durable)
      - game.dlx       (fanout, durable)
      - wallet.events  (topic, durable; declared idempotently because games CONSUMES from it)
    queues:
      - games.wallet-events.q (deliveryLimit RMQ_DELIVERY_LIMIT_MAIN, dlx game.dlx)
      - games.dlq             (deliveryLimit RMQ_DELIVERY_LIMIT_DLQ)
    bindings:
      - games.wallet-events.q <- wallet.events :: 'wallet.*'
      - games.dlq             <- game.dlx      :: ''

  Phase 1 env layer to mirror (services/games/src/config/defaults.ts already declares
  OUTBOX_POLL_INTERVAL_MS, RMQ_DELIVERY_LIMIT_MAIN, RMQ_DELIVERY_LIMIT_DLQ via zod.coerce).
  Add OUTBOX_POLL_BATCH_SIZE: z.coerce.number().int().positive().default(100) to both schemas.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend env config + register messaging entities in mikro-orm.config.ts</name>
  <files>
    services/games/src/config/defaults.ts,
    services/games/.env.example,
    services/games/.env,
    services/wallets/src/config/defaults.ts,
    services/wallets/.env.example,
    services/wallets/.env,
    services/games/mikro-orm.config.ts,
    services/wallets/mikro-orm.config.ts
  </files>
  <read_first>
    - services/games/src/config/defaults.ts (existing zod schema — already declares OUTBOX_POLL_INTERVAL_MS, RMQ_DELIVERY_LIMIT_MAIN, RMQ_DELIVERY_LIMIT_DLQ — add OUTBOX_POLL_BATCH_SIZE alongside)
    - services/wallets/src/config/defaults.ts (the wallets equivalent — confirm whether OUTBOX_* envs are present; if not, add them all)
    - services/games/.env.example and services/games/.env (mirror new env entries; reference REQUIREMENTS.md Open Configuration Values table for canonical defaults)
    - services/games/mikro-orm.config.ts (entities and entitiesTs are currently [] with warnWhenNoEntities: false override per Phase 1 verification report — remove the override after registering entities)
    - services/wallets/mikro-orm.config.ts (same shape as games config)
    - packages/messaging-spine/src/index.ts (entities to import: OutboxMessage, InboxMessage, DeadLetterMessage)
  </read_first>
  <action>In services/games/src/config/defaults.ts, append OUTBOX_POLL_BATCH_SIZE field to gamesEnvSchema declared as `z.coerce.number().int().positive().default(100)`. Mirror in services/wallets/src/config/defaults.ts: ensure WalletsEnv schema includes OUTBOX_POLL_INTERVAL_MS, RMQ_DELIVERY_LIMIT_MAIN, RMQ_DELIVERY_LIMIT_DLQ, OUTBOX_POLL_BATCH_SIZE (add any missing; consult the file first). Update both .env.example files to add `OUTBOX_POLL_BATCH_SIZE=100` under a clearly commented section labeled "Phase 2 — Outbox publisher tuning"; also confirm OUTBOX_POLL_INTERVAL_MS=1000, RMQ_DELIVERY_LIMIT_MAIN=5, RMQ_DELIVERY_LIMIT_DLQ=3 are present (add if missing — Phase 1 should already have them per REQUIREMENTS Open Configuration Values). Update both .env files (dev-mode local) with the same defaults; do not commit secrets. In services/games/mikro-orm.config.ts, delete the `discovery: { warnWhenNoEntities: false }` line (no longer needed since real entities exist), add `import { OutboxMessage, InboxMessage, DeadLetterMessage } from '@crash/messaging-spine';`, and set both entities and entitiesTs arrays to [OutboxMessage, InboxMessage, DeadLetterMessage]. Mirror in services/wallets/mikro-orm.config.ts.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge && grep -q "OUTBOX_POLL_BATCH_SIZE" services/games/src/config/defaults.ts && grep -q "OUTBOX_POLL_BATCH_SIZE" services/wallets/src/config/defaults.ts && grep -q "OUTBOX_POLL_BATCH_SIZE=100" services/games/.env.example && grep -q "OUTBOX_POLL_BATCH_SIZE=100" services/wallets/.env.example && grep -q "OutboxMessage" services/games/mikro-orm.config.ts && grep -q "OutboxMessage" services/wallets/mikro-orm.config.ts && ! grep -q "warnWhenNoEntities" services/games/mikro-orm.config.ts && ! grep -q "warnWhenNoEntities" services/wallets/mikro-orm.config.ts && (cd services/games && bun run typecheck) && (cd services/wallets && bun run typecheck)</automated>
  </verify>
  <done>Both services env schemas + env files include OUTBOX_POLL_BATCH_SIZE; both mikro-orm.config.ts files register the three messaging entities and drop the Phase 1 warnWhenNoEntities override; both services typecheck cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Author six MikroORM migrations that consume the shared SQL fragments</name>
  <files>
    services/games/src/infrastructure/mikro-orm/migrations/20260524001-create-outbox.ts,
    services/games/src/infrastructure/mikro-orm/migrations/20260524002-create-inbox.ts,
    services/games/src/infrastructure/mikro-orm/migrations/20260524003-create-dead-letter-messages.ts,
    services/wallets/src/infrastructure/mikro-orm/migrations/20260524001-create-outbox.ts,
    services/wallets/src/infrastructure/mikro-orm/migrations/20260524002-create-inbox.ts,
    services/wallets/src/infrastructure/mikro-orm/migrations/20260524003-create-dead-letter-messages.ts
  </files>
  <read_first>
    - services/games/mikro-orm.config.ts (migrations path = ./src/infrastructure/mikro-orm/migrations, emit=ts, transactional=true)
    - packages/messaging-spine/src/migrations/shared/001-outbox.sql, 002-inbox.sql, 003-dead-letter-messages.sql (the canonical SQL bodies authored in plan 02-02)
    - .planning/phases/02-outbox-inbox-spine/02-RESEARCH.md §Migration Plan (the convention that each service migration reads the shared fragment at runtime via fs)
    - Any existing migration file in services/games/src/infrastructure/mikro-orm/migrations/ to mirror MikroORM's Migration class convention (extends Migration; override async up; call this.addSql)
  </read_first>
  <action>For each service (games, wallets), author three migration files. Each file imports `Migration` from `@mikro-orm/migrations`, `readFileSync` from `node:fs`, and uses `createRequire` from `node:module` to obtain a require capable of `require.resolve('@crash/messaging-spine/src/migrations/shared/001-outbox.sql')`. The class extends Migration and overrides `async up()` to readFileSync the resolved path and pass the SQL string to `this.addSql(sql)`. Also implement `async down()` for each: for the outbox migration, down emits three statements — DROP TRIGGER IF EXISTS outbox_notify_trigger ON outbox, DROP FUNCTION IF EXISTS outbox_notify_fn(), DROP TABLE IF EXISTS outbox. For the inbox migration, down emits DROP TABLE IF EXISTS inbox. For the dead-letter migration, down emits DROP TABLE IF EXISTS dead_letter_messages. Class names follow MikroORM convention `Migration20260524001`, `Migration20260524002`, `Migration20260524003`. All six files are conceptually identical between games and wallets; only the absolute file path differs. After authoring, run `bun run docker:down && bun run docker:up` once and confirm both `*-migrate` init containers exit code 0 and the runtime games + wallets services reach Healthy.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge && for svc in games wallets; do for n in 20260524001-create-outbox 20260524002-create-inbox 20260524003-create-dead-letter-messages; do test -f services/$svc/src/infrastructure/mikro-orm/migrations/$n.ts && grep -q "messaging-spine/src/migrations/shared" services/$svc/src/infrastructure/mikro-orm/migrations/$n.ts && grep -q "extends Migration" services/$svc/src/infrastructure/mikro-orm/migrations/$n.ts && grep -q "addSql" services/$svc/src/infrastructure/mikro-orm/migrations/$n.ts || exit 1; done; done && echo "OK"</automated>
  </verify>
  <done>Six migration files exist, each reading the shared SQL fragment via require.resolve + readFileSync, each providing up() and down(); both services typecheck.</done>
</task>

<task type="auto">
  <name>Task 3: Wire MessagingSpineModule in both AppModules + author per-service DeadLetterConsumer subclasses</name>
  <files>
    services/games/src/app.module.ts,
    services/wallets/src/app.module.ts,
    services/games/src/infrastructure/messaging/games-dead-letter.consumer.ts,
    services/wallets/src/infrastructure/messaging/wallets-dead-letter.consumer.ts
  </files>
  <read_first>
    - services/games/src/app.module.ts (current shape — must preserve existing imports: MikroOrmModule, HealthController etc.)
    - services/wallets/src/app.module.ts (same)
    - services/games/src/config/defaults.ts (the GAMES_ENV_TOKEN or env export — likely exported as `env`)
    - services/wallets/src/config/defaults.ts
    - packages/messaging-spine/src/index.ts (MessagingSpineModule, EXCHANGES, QUEUES, buildQuorumArgs, DeadLetterConsumer, DeadLetterRepository)
    - .planning/phases/02-outbox-inbox-spine/02-RESEARCH.md §How services consume it (lines 819-850) — the canonical forRootAsync invocation shape
  </read_first>
  <action>In services/wallets/src/app.module.ts, add to the imports array `MessagingSpineModule.forRootAsync({ useFactory: () => ({ amqpUrl: env.RABBITMQ_URL, databaseUrl: env.DATABASE_URL, serviceName: 'wallets', outbox: { pollIntervalMs: env.OUTBOX_POLL_INTERVAL_MS, batchSize: env.OUTBOX_POLL_BATCH_SIZE }, topology: { exchangesToAssert: [{ name: EXCHANGES.WALLET_COMMANDS, type: 'direct', durable: true }, { name: EXCHANGES.WALLET_EVENTS, type: 'topic', durable: true }, { name: EXCHANGES.WALLET_DLX, type: 'fanout', durable: true }], queuesToAssert: [{ name: QUEUES.WALLET_COMMANDS, deliveryLimit: env.RMQ_DELIVERY_LIMIT_MAIN, dlx: EXCHANGES.WALLET_DLX }, { name: QUEUES.WALLET_DLQ, deliveryLimit: env.RMQ_DELIVERY_LIMIT_DLQ }], bindings: [{ queue: QUEUES.WALLET_COMMANDS, exchange: EXCHANGES.WALLET_COMMANDS, routingKey: 'wallet.debit' }, { queue: QUEUES.WALLET_COMMANDS, exchange: EXCHANGES.WALLET_COMMANDS, routingKey: 'wallet.credit' }, { queue: QUEUES.WALLET_DLQ, exchange: EXCHANGES.WALLET_DLX, routingKey: '' }] } }) })`. Also add WalletsDeadLetterConsumer to providers. Mirror in services/games/src/app.module.ts with exchanges [GAME_EVENTS topic, GAME_DLX fanout, WALLET_EVENTS topic] (the third declared idempotently because games consumes from it), queues [GAMES_WALLET_EVENTS deliveryLimit=MAIN dlx=GAME_DLX, GAMES_DLQ deliveryLimit=DLQ], bindings [GAMES_WALLET_EVENTS <- WALLET_EVENTS :: 'wallet.*', GAMES_DLQ <- GAME_DLX :: '']. Add GamesDeadLetterConsumer to providers. Create services/wallets/src/infrastructure/messaging/wallets-dead-letter.consumer.ts: `@Injectable() export class WalletsDeadLetterConsumer extends DeadLetterConsumer { protected readonly consumerName = 'wallets.dlq'; constructor(repo: DeadLetterRepository, logger: Logger) { super(repo, logger); } @RabbitSubscribe({ exchange: EXCHANGES.WALLET_DLX, routingKey: '', queue: QUEUES.WALLET_DLQ, queueOptions: { durable: true, arguments: buildQuorumArgs(env.RMQ_DELIVERY_LIMIT_DLQ) } }) async handle(rawPayload: unknown, msg: ConsumeMessage): Promise<undefined> { return this.handleDeadLetter(rawPayload, msg); } }`. Create services/games/src/infrastructure/messaging/games-dead-letter.consumer.ts mirroring with GAME_DLX/GAMES_DLQ and consumerName 'games.dlq'. Note: both DLQ consumers use buildQuorumArgs(env.RMQ_DELIVERY_LIMIT_DLQ) — this is REQ-SAGA-05's "x-delivery-limit on the DLQ itself" requirement made concrete.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge && grep -q "MessagingSpineModule.forRootAsync" services/wallets/src/app.module.ts && grep -q "MessagingSpineModule.forRootAsync" services/games/src/app.module.ts && grep -q "extends DeadLetterConsumer" services/wallets/src/infrastructure/messaging/wallets-dead-letter.consumer.ts && grep -q "extends DeadLetterConsumer" services/games/src/infrastructure/messaging/games-dead-letter.consumer.ts && grep -q "RMQ_DELIVERY_LIMIT_DLQ" services/wallets/src/infrastructure/messaging/wallets-dead-letter.consumer.ts && grep -q "buildQuorumArgs" services/games/src/infrastructure/messaging/games-dead-letter.consumer.ts && (cd services/games && bun run typecheck) && (cd services/wallets && bun run typecheck)</automated>
  </verify>
  <done>Both AppModules import MessagingSpineModule.forRootAsync with their canonical TopologyConfig; both services expose a concrete DeadLetterConsumer subclass bound to their DLQ via @RabbitSubscribe; both subclasses declare the DLQ with x-delivery-limit=env.RMQ_DELIVERY_LIMIT_DLQ; both services typecheck.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Full-stack docker:up smoke check</name>
  <what-built>The complete Phase 2 messaging spine is now wired into both services. This checkpoint confirms `bun run docker:up` actually brings the stack up green AND the RabbitMQ management UI shows the declared quorum queues + DLX bindings.</what-built>
  <how-to-verify>
1. From the repo root: `bun run docker:down` then `bun run docker:up`. Expect both `games-migrate` and `wallets-migrate` containers to exit 0; both runtime services reach Healthy. Note any first-attempt failure and capture logs.
2. Run `bun run smoke:health` — expect 7/7 probes (same Phase 1 baseline; no regression).
3. Open RabbitMQ management UI at http://localhost:15672 (admin/admin). On the Queues tab confirm the four queues exist with type=quorum: `wallet.commands.q`, `wallet.dlq`, `games.wallet-events.q`, `games.dlq`. Click `wallet.commands.q` and confirm Arguments includes `x-queue-type: quorum`, `x-delivery-limit: 5`, `x-dead-letter-exchange: wallet.dlx`. Click `wallet.dlq` and confirm `x-queue-type: quorum`, `x-delivery-limit: 3` (no DLX — terminal).
4. On the Exchanges tab confirm: `wallet.commands` (direct), `wallet.events` (topic), `wallet.dlx` (fanout), `game.events` (topic), `game.dlx` (fanout). Each shows the expected bindings.
5. Inspect the games and wallets container logs: `docker compose logs games | grep -i "assertQueue\|outbox publisher\|topology"` and the same for wallets. Expect TopologyBootstrap to log each assert, and OutboxPublisher to indicate startup.
6. Connect to each service DB via `psql` (admin/admin against localhost:5432 db=games then db=wallets) and confirm `\dt` lists outbox, inbox, dead_letter_messages plus mikro_orm_migrations; `\d outbox` shows the partial index and the trigger; `SELECT proname FROM pg_proc WHERE proname='outbox_notify_fn'` returns one row.

Report any deviation. If a queue arg looks wrong (e.g., x-delivery-limit absent) the topology assertion code in plan 02-06 has a bug — STOP and fix before continuing the phase.
  </how-to-verify>
  <resume-signal>Type "approved" once all six checks pass, "blocked: &lt;description&gt;" to capture a regression, or "retry" if a transient docker-up race needs another cycle.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service env layer → MessagingOptions | Per-service `env` import (typed via zod) is the only source of amqpUrl/databaseUrl; no process.env reads in app.module per Phase 1 ESLint rule |
| migration runtime → file system | Migrations read SQL fragments from node_modules — symlink integrity (workspace setup) must hold inside the migrate container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-23 | Tampering | Per-service TopologyConfig drift (typo in routing key) silently misroutes | mitigate | Routing keys come from string literals here but ALWAYS pair with EXCHANGES/QUEUES constants; integration tests in plan 02-09 publish and consume real messages and would catch drift |
| T-02-24 | Denial of Service | DLQ consumer (DeadLetterConsumer subclass) throws while persisting → DLQ delivery-limit=3 then DROPPED | accept | At 3 retries the broker drops the message — acceptable failure mode for "dead letter persist also failed"; logs surface it; operator can grep |
| T-02-25 | Tampering | bun workspace symlink missing inside docker container → readFileSync('messaging-spine/.../001-outbox.sql') fails at migration time | mitigate | Phase 1 Dockerfile multi-stage build (per `01-03-PLAN`) already copies the entire workspace; verified by Task 4 docker:up smoke; if it fails, the Dockerfile workspace copy step needs fixing |
| T-02-26 | Information Disclosure | services/{games,wallets}/.env committed accidentally | accept | Phase 1 .gitignore covers .env files (verified by Phase 1 verification report); .env updated here is dev-local only |
</threat_model>

<verification>
1. Both services typecheck (`bun run typecheck` exits 0 in each)
2. `grep "MessagingSpineModule.forRootAsync" services/games/src/app.module.ts` and same for wallets returns a match
3. `grep "extends DeadLetterConsumer" services/games/src/infrastructure/messaging/games-dead-letter.consumer.ts` and same for wallets returns a match
4. `grep "buildQuorumArgs(env.RMQ_DELIVERY_LIMIT_DLQ)" services/games/src/infrastructure/messaging/games-dead-letter.consumer.ts` confirms DLQ has its own delivery limit (REQ-SAGA-05)
5. All six migration files exist; each invokes `readFileSync` on a `@crash/messaging-spine/src/migrations/shared/*` path
6. Both .env.example files contain OUTBOX_POLL_BATCH_SIZE=100
7. `bun run docker:down && bun run docker:up` brings the stack up Healthy with both migrate containers exiting 0 (Task 4 checkpoint)
8. RabbitMQ management UI lists the four quorum queues with correct arguments (Task 4 checkpoint)
9. Phase 1's `bun run smoke:health` still reports 7/7 probes (no regression)
</verification>

<success_criteria>
- Both services' env schemas include OUTBOX_POLL_BATCH_SIZE (operator-tunable per 02-RESEARCH §Open Questions answer)
- Both services register the three messaging entities and drop the Phase 1 warnWhenNoEntities placeholder
- Six migrations execute the canonical SQL fragments — shared source of truth honored
- Both AppModules consume MessagingSpineModule.forRootAsync with per-service TopologyConfig
- Both services declare a concrete DeadLetterConsumer subclass bound to their DLQ via @RabbitSubscribe with buildQuorumArgs(env.RMQ_DELIVERY_LIMIT_DLQ) — satisfying REQ-SAGA-05 "x-delivery-limit on the DLQ itself"
- `bun run docker:up` brings everything Healthy; RabbitMQ shows correct topology; Postgres shows correct tables + trigger + function
- No Phase 1 regression (smoke:health still 7/7)
</success_criteria>

<output>
Create `.planning/phases/02-outbox-inbox-spine/02-07-SUMMARY.md` documenting:
- Final TopologyConfig per service (paste the actual objects from app.module.ts)
- All env additions: OUTBOX_POLL_BATCH_SIZE confirmed in both schemas + both .env.example
- Migration filenames and which shared SQL fragment each consumes
- The checkpoint outcome from Task 4 (any drift discovered, any retry needed)
- RabbitMQ topology snapshot (queue list + their arguments) captured from the management UI
- Any deviation from the canonical config and rationale
</output>
