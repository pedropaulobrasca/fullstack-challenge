---
phase: 02-outbox-inbox-spine
plan: 08
type: execute
wave: 6
depends_on: ["02-03", "02-04", "02-05", "02-06"]
files_modified:
  - packages/messaging-spine/tests/unit/envelope.test.ts
  - packages/messaging-spine/tests/unit/topology.test.ts
  - packages/messaging-spine/tests/unit/inbox-repository-sql.test.ts
  - packages/messaging-spine/tests/unit/dead-letter-consumer.test.ts
  - packages/messaging-spine/tests/unit/messaging-cls.test.ts
autonomous: true
requirements: [REQ-WALL-05, REQ-WALL-06, REQ-SAGA-05, REQ-SAGA-06]

must_haves:
  truths:
    - "buildEnvelope produces valid envelopes; missing causationId throws; defaults fire for messageId, correlationId, version, occurredAt"
    - "parseEnvelope round-trips JSON.stringify(buildEnvelope(...)) without information loss"
    - "envelopeToAmqpHeaders + amqpHeadersToEnvelopeMeta bidirectionally preserve every envelope meta field"
    - "buildQuorumArgs produces the exact x-queue-type/x-delivery-limit/x-dead-letter-exchange shape RabbitMQ 4.2 accepts"
    - "deriveDlxFromExchange returns the documented DLX for each of the five known exchanges and throws on unknown"
    - "topologyConfigSchema accepts the canonical wallet + games topology shapes and rejects malformed configs"
    - "DeadLetterConsumer.handleDeadLetter extracts x-death metadata correctly and always returns undefined (ack), even when repository.persist throws"
    - "withMessagingContext + MessagingClsModule round-trip correlationId + causationId through a cls.run scope"
    - "InboxRepository SQL signature (statement text + parameter shape) matches the canonical ON CONFLICT pattern — verified against a stub Connection that records execute calls"
  artifacts:
    - path: "packages/messaging-spine/tests/unit/envelope.test.ts"
      provides: "Unit suite for buildEnvelope/parseEnvelope/AMQP header bridge"
      min_lines: 50
    - path: "packages/messaging-spine/tests/unit/topology.test.ts"
      provides: "Unit suite for EXCHANGES/QUEUES constants, buildQuorumArgs, deriveDlxFromExchange, topologyConfigSchema"
      min_lines: 50
    - path: "packages/messaging-spine/tests/unit/inbox-repository-sql.test.ts"
      provides: "Unit suite verifying InboxRepository emits the canonical SQL with correct parameter positions (uses a fake EntityManager whose getConnection.execute records calls)"
      min_lines: 40
    - path: "packages/messaging-spine/tests/unit/dead-letter-consumer.test.ts"
      provides: "Unit suite for DeadLetterConsumer.handleDeadLetter x-death parsing + persist-failure-still-acks behavior"
      min_lines: 40
    - path: "packages/messaging-spine/tests/unit/messaging-cls.test.ts"
      provides: "Unit suite for withMessagingContext + CLS scope semantics"
      min_lines: 30
  key_links:
    - from: "tests/unit/*.test.ts"
      to: "packages/messaging-spine/src/*.ts"
      via: "import from relative path"
      pattern: "../../src/"
    - from: "test runner"
      to: "bun:test"
      via: "no config — convention tests/**/*.test.ts"
      pattern: "import.*from.*['\"]bun:test['\"]"
---

<objective>
Land the unit test suite for the messaging-spine: lightweight, fast (sub-second total), no Docker, no testcontainers, no broker. Every artifact built in waves 2-4 gets a unit test that locks its contract. Integration tests with real Postgres + RabbitMQ are plan 02-09.

Purpose: Catches regressions instantly during plan 02-09 integration work and during Phase 3+ when the Wallet service starts wiring the decorator into its consumers. A failing unit test points to a contract violation in a single file; debugging a failing integration test is an order of magnitude more expensive.

Output: Five test files covering envelope, topology, inbox SQL shape, dead-letter consumer behavior, and CLS context propagation. All run via `bun test tests/unit` in subsecond time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-outbox-inbox-spine/02-RESEARCH.md
@packages/messaging-spine/src/index.ts
@packages/messaging-spine/src/envelope/build-envelope.ts
@packages/messaging-spine/src/envelope/envelope-headers.ts
@packages/messaging-spine/src/topology/topology-defaults.ts
@packages/messaging-spine/src/topology/topology-config.ts
@packages/messaging-spine/src/inbox/inbox-repository.ts
@packages/messaging-spine/src/dead-letter/dead-letter-consumer.service.ts
@packages/messaging-spine/src/dead-letter/dead-letter-repository.ts
@packages/messaging-spine/src/context/messaging-cls.ts

<interfaces>
<!-- bun:test API contract (from packages/shared-kernel/tests/*.test.ts in Phase 1) -->
import { describe, test, expect, mock } from 'bun:test';
describe('...', () => { test('...', () => { expect(actual).toBe(expected); }); });

<!-- Mocking strategy for InboxRepository test (no real EM needed) -->
// Construct a fake EntityManager whose getConnection() returns { execute: mock(async () => [...]) }
// Then assert mock.mock.calls[0][0] equals the canonical SQL and mock.mock.calls[0][1] equals the canonical param array.

<!-- DeadLetterConsumer test uses a fake DeadLetterRepository whose persist is mocked + sometimes throws to assert ack-on-failure. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Envelope + topology unit tests (parallel concerns, single plan task because both are pure)</name>
  <files>
    packages/messaging-spine/tests/unit/envelope.test.ts,
    packages/messaging-spine/tests/unit/topology.test.ts
  </files>
  <read_first>
    - packages/messaging-spine/src/envelope/build-envelope.ts (signature + EnvelopeBuildOptions)
    - packages/messaging-spine/src/envelope/envelope-headers.ts (AMQP_HEADER_KEYS table)
    - packages/messaging-spine/src/topology/topology-defaults.ts (EXCHANGES, QUEUES, buildQuorumArgs, deriveDlxFromExchange)
    - packages/messaging-spine/src/topology/topology-config.ts (topologyConfigSchema)
    - packages/shared-kernel/tests/money.test.ts (bun:test convention reference — describe/test/expect import style)
  </read_first>
  <action>In `envelope.test.ts`, import `{ describe, test, expect }` from `bun:test` and `{ buildEnvelope, parseEnvelope, type EnvelopeBuildOptions }` plus `{ envelopeToAmqpHeaders, amqpHeadersToEnvelopeMeta, AMQP_HEADER_KEYS }` from the package source. Write a describe block per surface:
  - 'buildEnvelope' with tests: "fills messageId/correlationId/version/occurredAt defaults", "throws when causationId omitted" (use expect(() => buildEnvelope({ type: 't', payload: {} } as EnvelopeBuildOptions)).toThrow(/causationId/)), "uses provided messageId when supplied", "uses provided correlationId when supplied", "version defaults to 1", "occurredAt is ISO-8601 string"
  - 'parseEnvelope' with tests: "round-trips JSON.stringify lossless", "throws on missing field", "throws on type mismatch (e.g., version as string)"
  - 'envelopeToAmqpHeaders + amqpHeadersToEnvelopeMeta' with tests: "maps every envelope field to AMQP_HEADER_KEYS bidirectionally", "throws when required header (e.g., x-message-id) missing"

In `topology.test.ts`, similar structure:
  - 'EXCHANGES' / 'QUEUES' constants: assert each documented value (e.g., expect(EXCHANGES.WALLET_COMMANDS).toBe('wallet.commands'))
  - 'buildQuorumArgs' tests: "returns x-queue-type=quorum + x-delivery-limit", "includes x-dead-letter-exchange when dlx provided", "omits x-dead-letter-exchange when dlx undefined", "deliveryLimit value matches input"
  - 'deriveDlxFromExchange' tests: "maps wallet.commands → wallet.dlx", "maps wallet.events → wallet.dlx", "maps game.events → game.dlx", "throws on unknown exchange"
  - 'topologyConfigSchema' tests: "accepts the canonical wallets topology", "accepts the canonical games topology", "rejects deliveryLimit ≤ 0", "rejects missing required fields"</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/packages/messaging-spine && bun test tests/unit/envelope.test.ts && bun test tests/unit/topology.test.ts</automated>
  </verify>
  <done>Both test files exist; all assertions pass; total runtime well under 1 second; coverage hits every documented surface of envelope helpers + topology helpers.</done>
</task>

<task type="auto">
  <name>Task 2: InboxRepository SQL + DeadLetterConsumer behavior unit tests</name>
  <files>
    packages/messaging-spine/tests/unit/inbox-repository-sql.test.ts,
    packages/messaging-spine/tests/unit/dead-letter-consumer.test.ts
  </files>
  <read_first>
    - packages/messaging-spine/src/inbox/inbox-repository.ts (the SQL statements to assert against)
    - packages/messaging-spine/src/dead-letter/dead-letter-consumer.service.ts (handleDeadLetter + x-death parsing)
    - packages/messaging-spine/src/dead-letter/dead-letter-repository.ts (DeadLetterRecord shape)
  </read_first>
  <action>In `inbox-repository-sql.test.ts`, use `import { mock } from 'bun:test'` and construct a fake EntityManager: `const execute = mock(async () => [{ message_id: 'abc' }]); const fakeEm = { getConnection: () => ({ execute }) } as unknown as EntityManager;`. Instantiate `const repo = new InboxRepository(fakeEm)`. Tests:
  - "tryClaim returns true when execute returns one row": call repo.tryClaim('cn','mid','mt'); assert result === true; assert execute.mock.calls.length === 1; assert execute.mock.calls[0][0].includes('INSERT INTO inbox') AND .includes('ON CONFLICT (consumer_name, message_id) DO NOTHING') AND .includes('RETURNING message_id'); assert execute.mock.calls[0][1] equals ['cn','mid','mt']
  - "tryClaim returns false when execute returns zero rows": reset mock to return []; assert false
  - "markProcessed emits UPDATE with correct params": reset; call repo.markProcessed('cn','mid'); assert execute.mock.calls[0][0].includes('UPDATE inbox SET processed_at = now()') AND .includes('WHERE consumer_name = ? AND message_id = ?'); assert execute.mock.calls[0][1] equals ['cn','mid']

In `dead-letter-consumer.test.ts`, declare a subclass for testing: `class TestDlqConsumer extends DeadLetterConsumer { protected readonly consumerName = 'test.dlq'; }`. Create a fake repo: `const persist = mock(async () => {}); const fakeRepo = { persist } as unknown as DeadLetterRepository;`. Create a fake logger: `const logger = { warn: mock(()=>{}), error: mock(()=>{}), log: mock(()=>{}) } as unknown as Logger;`. Tests:
  - "extracts x-death metadata into DeadLetterRecord": invoke handleDeadLetter(payload, fakeMsg) where fakeMsg has properties.headers['x-death'] = [{ count: 3, queue: 'wallet.commands.q', exchange: 'wallet.commands', 'routing-keys': ['wallet.debit'] }] and properties.messageId='mid-1'; assert persist called once with originalQueue='wallet.commands.q', originalExchange='wallet.commands', originalRoutingKey='wallet.debit', redeliveryCount=3, consumerName='test.dlq', originalMessageId='mid-1'
  - "always returns undefined (acks) on success": assert result === undefined
  - "always returns undefined (acks) when persist throws": configure persist mock to throw; invoke; assert result === undefined AND logger.error called
  - "handles missing x-death gracefully (defaults to 1 redelivery)": pass empty headers; assert persist called with redeliveryCount=1, originalQueue='unknown'</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/packages/messaging-spine && bun test tests/unit/inbox-repository-sql.test.ts && bun test tests/unit/dead-letter-consumer.test.ts</automated>
  </verify>
  <done>Both test files exist and pass; SQL contract for InboxRepository is locked; DeadLetterConsumer x-death parsing + ack-on-failure semantics are locked.</done>
</task>

<task type="auto">
  <name>Task 3: CLS context unit test</name>
  <files>packages/messaging-spine/tests/unit/messaging-cls.test.ts</files>
  <read_first>
    - packages/messaging-spine/src/context/messaging-cls.ts (MessagingClsModule.forRoot + withMessagingContext)
    - packages/messaging-spine/src/context/correlation-tokens.ts (CORRELATION_ID_KEY, CAUSATION_ID_KEY)
    - The nestjs-cls API: import { ClsService } from 'nestjs-cls'; instantiate directly for testing via `new ClsService(new AsyncLocalStorage())` — confirm constructor signature against the installed version
  </read_first>
  <action>Import `{ describe, test, expect }` from `bun:test`, `{ ClsService } from 'nestjs-cls'`, `{ AsyncLocalStorage } from 'node:async_hooks'`, and `{ withMessagingContext, CORRELATION_ID_KEY, CAUSATION_ID_KEY } from '../../src/index'`. Instantiate `const cls = new ClsService<Record<string, unknown>>(new AsyncLocalStorage());`. Tests:
  - "withMessagingContext sets correlationId in CLS scope": await withMessagingContext(cls, { correlationId: 'corr-1' }, async () => { expect(cls.get(CORRELATION_ID_KEY)).toBe('corr-1'); });
  - "withMessagingContext sets causationId when provided": await withMessagingContext(cls, { correlationId: 'corr-1', causationId: 'cause-1' }, async () => { expect(cls.get(CAUSATION_ID_KEY)).toBe('cause-1'); });
  - "withMessagingContext omits causationId when undefined": await withMessagingContext(cls, { correlationId: 'corr-2' }, async () => { expect(cls.get(CAUSATION_ID_KEY)).toBeUndefined(); });
  - "scopes are isolated": run two parallel withMessagingContext calls with different correlationIds via Promise.all; assert each sees its own value
  - "CORRELATION_ID_KEY equals 'correlationId' and CAUSATION_ID_KEY equals 'causationId'" — locks the key string contract that plan 02-05 IdempotentSubscribe relies on

If the nestjs-cls ClsService constructor signature differs from `new ClsService(new AsyncLocalStorage())` in the installed version, adapt minimally to obtain a usable ClsService instance (the test must not require a NestJS DI container). Document any divergence in the SUMMARY.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/packages/messaging-spine && bun test tests/unit/messaging-cls.test.ts && bun test tests/unit | grep -E "(pass|fail)"</automated>
  </verify>
  <done>CLS scope behavior verified end-to-end: correlationId + causationId set/get inside withMessagingContext, parallel scope isolation works, key constants match the integration contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production source | Tests import from `../../src/` directly so a refactor that breaks the public contract surfaces immediately |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-27 | Tampering | A refactor changes the InboxRepository SQL but the test passes a different SQL silently | mitigate | tests assert .includes() of every required SQL keyword (ON CONFLICT, RETURNING, etc.); any drift fails the test |
| T-02-28 | Repudiation | A bug in production but unit tests don't surface it | accept | This plan covers UNIT scope only; integration tests (plan 02-09) and Phase 3+ test suites cover scenarios these don't |
</threat_model>

<verification>
1. `cd packages/messaging-spine && bun test tests/unit` exits 0 with > 20 assertions across the five files (no `--bail` failures)
2. Total runtime sub-second (sanity check; bun:test is fast)
3. No test depends on a network resource (Docker, Postgres, RabbitMQ) — all mocks/stubs only
4. Every public export touched by waves 2-4 has at least one unit test (envelope helpers, topology helpers, inbox repository, dead-letter consumer, CLS helper)
</verification>

<success_criteria>
- bun:test passes for all five files
- Coverage hits: buildEnvelope (defaults + throws), parseEnvelope (round-trip + invalid), envelope-headers bridge (round-trip + missing), buildQuorumArgs (with/without dlx), deriveDlxFromExchange (5 exchanges + unknown throws), topologyConfigSchema (canonical + invalid), InboxRepository (SQL shape + params), DeadLetterConsumer (x-death extraction + ack-on-failure), withMessagingContext (set/get/isolated/keys)
- Sub-second total runtime — fits in the per-commit check loop
- No Docker, no network dependency
- All assertions use the bun:test idiom (describe/test/expect/mock)
</success_criteria>

<output>
Create `.planning/phases/02-outbox-inbox-spine/02-08-SUMMARY.md` documenting:
- Test count per file and total
- Total runtime captured from `bun test tests/unit`
- Any nestjs-cls API divergence requiring test adapter code
- The single canonical SQL statements locked by the InboxRepository test (paste them)
- The x-death header shape locked by the DeadLetterConsumer test
</output>
