---
phase: 02-outbox-inbox-spine
plan: 09
type: execute
wave: 7
depends_on: ["02-07", "02-08"]
files_modified:
  - packages/messaging-spine/tests/integration/_helpers/messaging-probe.entity.ts
  - packages/messaging-spine/tests/integration/_helpers/harness.ts
  - packages/messaging-spine/tests/integration/_helpers/probe-mikro-orm.config.ts
  - packages/messaging-spine/tests/integration/outbox-write-and-publish.test.ts
  - packages/messaging-spine/tests/integration/inbox-dedup.test.ts
  - packages/messaging-spine/tests/integration/kill-9-recovery.test.ts
  - packages/messaging-spine/tests/integration/poison-message.test.ts
  - packages/messaging-spine/tests/integration/listen-notify-wake.test.ts
  - packages/messaging-spine/tests/integration/envelope-headers.test.ts
  - packages/messaging-spine/package.json
autonomous: true
requirements: [REQ-WALL-05, REQ-WALL-06, REQ-SAGA-05, REQ-SAGA-06]

must_haves:
  truths:
    - "MessagingProbe aggregate exists ONLY in tests/integration/_helpers — never in services/games or services/wallets production source"
    - "Test harness boots a real Postgres + RabbitMQ via testcontainers; both are torn down between suites"
    - "outbox-write-and-publish: writes probe row + outbox row in same TX; poller publishes; consumer receives and inbox-dedups"
    - "inbox-dedup: same messageId delivered twice; handler invoked exactly once; Transaction-effect count = 1"
    - "kill-9-recovery: publisher killed between COMMIT and waitForConfirms; fresh publisher republishes; consumer receives exactly one effect (dedup absorbs the duplicate)"
    - "poison-message: a handler that always throws sees the message dead-lettered after 5 main + 3 dlq attempts; dead_letter_messages row count = 1"
    - "listen-notify-wake: an inserted outbox row is observed at the consumer within 250ms"
    - "envelope-headers: correlationId set on publisher side is observed at consumer side via amqpHeadersToEnvelopeMeta(msg.headers) AND the downstream outbox row's causationId equals the upstream messageId"
  artifacts:
    - path: "packages/messaging-spine/tests/integration/_helpers/messaging-probe.entity.ts"
      provides: "MessagingProbe MikroORM @Entity used only by tests"
      contains: "@Entity"
    - path: "packages/messaging-spine/tests/integration/_helpers/harness.ts"
      provides: "Test harness exporting bootHarness/shutdownHarness — manages testcontainers Postgres + RabbitMQ + ephemeral NestJS module wiring MessagingSpineModule with probe entity"
      exports: ["bootHarness", "shutdownHarness", "TestHarness"]
    - path: "packages/messaging-spine/tests/integration/_helpers/probe-mikro-orm.config.ts"
      provides: "MikroORM config for tests: includes OutboxMessage, InboxMessage, DeadLetterMessage, MessagingProbe entities — distinct from production configs (per 02-RESEARCH §Phase 2 doesn't ship a DOMAIN, Option B)"
      contains: "MessagingProbe"
  key_links:
    - from: "test harness"
      to: "testcontainers Postgres + RabbitMQ"
      via: "@testcontainers/postgresql + @testcontainers/rabbitmq Node SDK"
      pattern: "GenericContainer|PostgreSqlContainer|RabbitMqContainer"
    - from: "kill-9-recovery test"
      to: "child process publisher"
      via: "Bun.spawn with SIGKILL"
      pattern: "Bun.spawn|SIGKILL"
    - from: "poison-message test"
      to: "dead_letter_messages row"
      via: "consumer that always throws → DLX flow → DeadLetterConsumer persists"
      pattern: "dead_letter_messages|SELECT.*COUNT"
---

<objective>
Stand up the integration test harness with real Postgres + real RabbitMQ via testcontainers, ship the MessagingProbe test-only aggregate, and write the six integration tests that prove the Phase 2 success criteria end-to-end. Every Success Criterion from ROADMAP §Phase 2 is mapped to exactly one test in this plan.

Purpose: Unit tests (plan 02-08) lock contracts within single files; these integration tests prove the spine actually works under realistic broker + DB semantics. The `MessagingProbe` test aggregate solves the chicken-and-egg of "Phase 2 ships no domain aggregate but the outbox pattern requires one to write into in the same TX" — per 02-RESEARCH §Testing Approach Option B, the probe lives only in test files and is never registered with the production MikroORM config.

Output: Three helper files + six integration test files. Running `INTEGRATION=1 bun test` from the package directory boots two testcontainers, exercises every Phase 2 success criterion, and tears down cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-outbox-inbox-spine/02-RESEARCH.md
@packages/messaging-spine/src/index.ts
@packages/messaging-spine/src/migrations/shared/001-outbox.sql
@packages/messaging-spine/src/migrations/shared/002-inbox.sql
@packages/messaging-spine/src/migrations/shared/003-dead-letter-messages.sql
@packages/messaging-spine/package.json
@CLAUDE.md

<interfaces>
<!-- testcontainers Node SDK -->
import { GenericContainer, StartedTestContainer, Wait } from 'testcontainers';
import { PostgreSqlContainer } from '@testcontainers/postgresql';
import { RabbitMQContainer } from '@testcontainers/rabbitmq';
// Both return URLs (Postgres: connectionString, RabbitMQ: amqpUrl) via getter methods.

<!-- NestJS test bootstrap -->
import { Test, TestingModule } from '@nestjs/testing';
// Construct a TestingModule importing MessagingSpineModule.forRootAsync + a TestProbeModule providing the MessagingProbe handler + probe consumer.

<!-- The Phase 2 success criteria mapping (from ROADMAP §Phase 2 + 02-RESEARCH §Phase Requirements → Test Map) -->
// Criterion 1: outbox row + probe row in same TX; consumer dedup-checks inbox.       → outbox-write-and-publish.test.ts + inbox-dedup.test.ts
// Criterion 2: kill -9 publisher between commit and confirm; consumer effect count=1. → kill-9-recovery.test.ts
// Criterion 3: poison message → dead_letter_messages after 5 main + 3 dlq.            → poison-message.test.ts
// Criterion 4: envelope headers (messageId, correlationId, causationId, type, version, occurredAt) propagate end-to-end. → envelope-headers.test.ts
// Criterion 5: LISTEN/NOTIFY wakes poller; publish latency < 250ms.                   → listen-notify-wake.test.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test harness + MessagingProbe entity + probe MikroORM config + integration test npm script</name>
  <files>
    packages/messaging-spine/tests/integration/_helpers/messaging-probe.entity.ts,
    packages/messaging-spine/tests/integration/_helpers/probe-mikro-orm.config.ts,
    packages/messaging-spine/tests/integration/_helpers/harness.ts,
    packages/messaging-spine/package.json
  </files>
  <read_first>
    - packages/messaging-spine/src/outbox/outbox-message.entity.ts (entity shape — harness must register this with MikroORM)
    - packages/messaging-spine/src/inbox/inbox-message.entity.ts
    - packages/messaging-spine/src/dead-letter/dead-letter-message.entity.ts
    - packages/messaging-spine/src/migrations/shared/001-outbox.sql (the SQL the harness must execute against testcontainers Postgres before the suite runs)
    - .planning/phases/02-outbox-inbox-spine/02-RESEARCH.md §Phase 2 specifically does NOT ship a domain (Option B rationale + MessagingProbe shape lines 878-895)
    - services/games/mikro-orm.config.ts (mirror config shape)
    - testcontainers npm docs for PostgreSqlContainer + RabbitMQContainer signatures (use Context7 if needed — but the canonical API is: `await new PostgreSqlContainer('postgres:18-alpine').start()` returning a started container with `.getConnectionUri()`)
  </read_first>
  <action>In `messaging-probe.entity.ts`, declare `@Entity({ tableName: 'messaging_probe' }) export class MessagingProbe { @PrimaryKey() id!: string; @Property() label!: string; @Property({ default: 0 }) sideEffectCount!: number; }`. In `probe-mikro-orm.config.ts`, export a function `export function createProbeMikroOrmConfig(connectionString: string)` that returns a `defineConfig` object identical to services/games/mikro-orm.config.ts except `entities` and `entitiesTs` are `[OutboxMessage, InboxMessage, DeadLetterMessage, MessagingProbe]`. In `harness.ts`, build `export interface TestHarness { app: INestApplication; em: EntityManager; outboxRepo: OutboxRepository; pgContainer: StartedTestContainer; rmqContainer: StartedTestContainer; teardown: () => Promise<void>; }` and `export async function bootHarness(): Promise<TestHarness>`. bootHarness does the following sequence:
  1. Start Postgres: `const pg = await new PostgreSqlContainer('postgres:18-alpine').start(); const pgUrl = pg.getConnectionUri();`
  2. Start RabbitMQ: `const rmq = await new RabbitMQContainer('rabbitmq:4.2-management-alpine').start(); const amqpUrl = rmq.getAmqpUrl();`
  3. Execute the three SQL fragments + a CREATE TABLE messaging_probe statement against the Postgres container via a temporary pg client (read each fragment from `packages/messaging-spine/src/migrations/shared/00*.sql` with fs.readFileSync; then execute `CREATE TABLE messaging_probe (id text PRIMARY KEY, label text NOT NULL, side_effect_count int NOT NULL DEFAULT 0);`)
  4. Build a NestJS TestingModule importing MikroOrmModule.forRoot(createProbeMikroOrmConfig(pgUrl)) + MessagingSpineModule.forRootAsync({ useFactory: () => ({ amqpUrl, databaseUrl: pgUrl, serviceName: 'test', outbox: { pollIntervalMs: 1000, batchSize: 100 }, topology: { exchangesToAssert: [{ name: 'test.events', type: 'topic', durable: true }, { name: 'test.dlx', type: 'fanout', durable: true }], queuesToAssert: [{ name: 'test.q', deliveryLimit: 5, dlx: 'test.dlx' }, { name: 'test.dlq', deliveryLimit: 3 }], bindings: [{ queue: 'test.q', exchange: 'test.events', routingKey: 'test.*' }, { queue: 'test.dlq', exchange: 'test.dlx', routingKey: '' }] } }) })
  5. Compile + initialize the app; resolve EntityManager + OutboxRepository.
  6. Return TestHarness with a `teardown` function that closes app, stops containers in reverse order (rmq then pg).
Add `export async function shutdownHarness(h: TestHarness)` that simply calls `h.teardown()`. Update package.json `"scripts"`: ensure `"test:integration": "INTEGRATION=1 bun test tests/integration"` is set; also gate at the test files themselves via `if (!process.env.INTEGRATION) { console.log('skipping integration suite — set INTEGRATION=1'); process.exit(0); }` at the top of each test file (so plain `bun test` in CI doesn't try to boot Docker).</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/packages/messaging-spine && test -f tests/integration/_helpers/messaging-probe.entity.ts && test -f tests/integration/_helpers/harness.ts && test -f tests/integration/_helpers/probe-mikro-orm.config.ts && grep -q "MessagingProbe" tests/integration/_helpers/probe-mikro-orm.config.ts && grep -q "PostgreSqlContainer" tests/integration/_helpers/harness.ts && grep -q "RabbitMQContainer" tests/integration/_helpers/harness.ts && grep -q "test:integration" package.json && bun run typecheck</automated>
  </verify>
  <done>Probe entity exists test-only; probe MikroORM config registers all four entities (three production + probe); harness boots testcontainers Postgres + RabbitMQ, executes shared SQL fragments + CREATE TABLE messaging_probe, builds a TestingModule with MessagingSpineModule wired to test topology, returns a teardown closure; test:integration npm script wired.</done>
</task>

<task type="auto">
  <name>Task 2: Outbox-write-and-publish + Inbox-dedup + Envelope-headers integration tests (three correctness scenarios, single plan task because all use the same harness and similar shape)</name>
  <files>
    packages/messaging-spine/tests/integration/outbox-write-and-publish.test.ts,
    packages/messaging-spine/tests/integration/inbox-dedup.test.ts,
    packages/messaging-spine/tests/integration/envelope-headers.test.ts
  </files>
  <read_first>
    - packages/messaging-spine/tests/integration/_helpers/harness.ts (just authored — bootHarness signature)
    - packages/messaging-spine/src/index.ts (everything to import: buildEnvelope, OutboxRepository, IdempotentSubscribe, CORRELATION_ID_KEY, MessagingClsModule, etc.)
    - 02-RESEARCH §Testing Approach test descriptions (lines 866-873) — the canonical scenarios
  </read_first>
  <action>Each test file starts with `if (!process.env.INTEGRATION) { console.log('skip — INTEGRATION not set'); process.exit(0); }`. Uses `describe + beforeAll/afterAll + test` from bun:test.

  `outbox-write-and-publish.test.ts` (Success Criterion 1 first half):
  - beforeAll: harness = await bootHarness(); declare a probe handler class using @IdempotentSubscribe({ exchange: 'test.events', routingKey: 'test.probe', queue: 'test.q', consumerName: 'test.probe.handler' }) that increments harness.em-tracked messagingProbe.sideEffectCount; register handler in TestingModule (or in bootHarness extension)
  - test "writes probe + outbox row in same TX, polled and consumed": create new MessagingProbe with id='p1', label='hello', sideEffectCount=0; inside em.transactional, em.persist(probe) AND outboxRepo.add(buildEnvelope({ type:'test.probe', payload:{ probeId:'p1' }, causationId:'origin-1' }), { exchange:'test.events', routingKey:'test.probe', aggregateType:'MessagingProbe', aggregateId:'p1' }); wait for handler to be called (poll harness.em for messagingProbe.sideEffectCount === 1, timeout 5s); assert sideEffectCount === 1; assert outbox row status = 'PUBLISHED'; assert inbox row exists for (consumer_name='test.probe.handler', message_id=envelope.messageId)
  - afterAll: await harness.teardown()

  `inbox-dedup.test.ts` (Success Criterion 1 second half — REQ-WALL-05 dedup):
  - Same boot pattern + handler. Test "broker redelivery → handler called exactly once": craft a payload, manually publish the SAME envelope (same messageId) to the test.q exchange twice via a raw amqplib channel (bypassing outbox, simulating redelivery); wait briefly; assert sideEffectCount === 1, NOT 2; assert exactly one inbox row exists for that messageId

  `envelope-headers.test.ts` (Success Criterion 4 — REQ-SAGA-06):
  - Handler reads CLS via cls.get(CORRELATION_ID_KEY) inside its body and stores the observed value on the probe row (e.g., probe.label = `obs-${cls.get(CORRELATION_ID_KEY)}`)
  - Test "correlationId set on publish reaches handler via CLS": call buildEnvelope({ ..., correlationId: 'corr-xyz', causationId: 'origin-xyz' }); write to outbox; wait; assert messagingProbe.label === 'obs-corr-xyz'
  - Test "downstream outbox row written inside the handler carries causationId = upstream messageId": extend the handler to call outboxRepo.add(buildEnvelope({ type:'test.echo', payload:{}, causationId: env.messageId, correlationId: cls.get(CORRELATION_ID_KEY) as string }), {...}) for an 'echo' route; wait; SELECT raw row from outbox WHERE event_type='test.echo'; assert headers['x-causation-id'] === upstream envelope.messageId AND headers['x-correlation-id'] === 'corr-xyz'

For poll-with-timeout helper, use `const waitFor = async (predicate: () => Promise<boolean>, timeoutMs = 5000, intervalMs = 50) => { const start = Date.now(); while (Date.now() - start < timeoutMs) { if (await predicate()) return; await new Promise(r => setTimeout(r, intervalMs)); } throw new Error('waitFor timeout'); };` declared in the harness module.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/packages/messaging-spine && INTEGRATION=1 bun test tests/integration/outbox-write-and-publish.test.ts && INTEGRATION=1 bun test tests/integration/inbox-dedup.test.ts && INTEGRATION=1 bun test tests/integration/envelope-headers.test.ts</automated>
  </verify>
  <done>Three integration tests pass against real Postgres + RabbitMQ; outbox-write proves same-TX dual-write fix; inbox-dedup proves REQ-WALL-05 exactly-once; envelope-headers proves REQ-SAGA-06 correlationId + causationId chain.</done>
</task>

<task type="auto">
  <name>Task 3: kill-9-recovery + poison-message + listen-notify-wake integration tests (three failure-mode scenarios)</name>
  <files>
    packages/messaging-spine/tests/integration/kill-9-recovery.test.ts,
    packages/messaging-spine/tests/integration/poison-message.test.ts,
    packages/messaging-spine/tests/integration/listen-notify-wake.test.ts
  </files>
  <read_first>
    - packages/messaging-spine/tests/integration/_helpers/harness.ts (the bootHarness signature)
    - .planning/phases/02-outbox-inbox-spine/02-RESEARCH.md §Testing Approach (specifically kill-9-recovery + poison-message + listen-notify-wake descriptions lines 868-872)
    - .planning/phases/02-outbox-inbox-spine/02-RESEARCH.md §A5 assumption (the 250ms target — bump to 500ms if local Docker proves marginal, document in SUMMARY)
  </read_first>
  <action>Each file gates with `if (!process.env.INTEGRATION) process.exit(0);` at the top.

  `kill-9-recovery.test.ts` (Success Criterion 2 — REQ-WALL-06 + REQ-WALL-05 combined):
  Strategy: instead of spawning a child process (complex test harness), simulate kill-9 by directly creating an outbox row via raw SQL (NOT through OutboxRepository, since the publisher is local), then close the AMQP channel mid-publish to simulate the publisher process disappearing between commit and waitForConfirms. Concretely:
  - boot harness; register the standard handler (sideEffectCount++)
  - PHASE A (simulated crash): open a raw confirm channel from amqplib (bypassing the harness publisher); BEGIN a pg TX manually; INSERT INTO outbox (...) VALUES (...) RETURNING id; PUBLISH the message; then COMMIT the TX BUT call channel.close() BEFORE waitForConfirms returns. Now the outbox row is PENDING (status=PENDING because the in-TX UPDATE to PUBLISHED never ran). The message MIGHT or might not have reached the broker — both are valid.
  - PHASE B (recovery): wait 2s (gives harness's OutboxPublisher poll to run); assert sideEffectCount === 1, NOT 2 (handler MUST have run exactly once — inbox dedup absorbed any duplicate); assert outbox row eventually transitions to PUBLISHED.
  Document in inline comment that this simulates the kill-9 race per 02-RESEARCH; a true child-process kill-9 test is out of scope due to harness complexity.

  `poison-message.test.ts` (Success Criterion 3 — REQ-SAGA-05):
  - boot harness with the DeadLetterConsumer wired (subclass binding to 'test.dlq')
  - register a 'always-throws' handler bound via @IdempotentSubscribe to the test.q with consumerName='test.poison.handler' whose body unconditionally throws Error('poison')
  - publish ONE message via outbox + raw publisher; wait up to 30s
  - assert: COUNT(*) from dead_letter_messages WHERE consumer_name='test.dlq' === 1 (the DLQ consumer caught the exhausted message)
  - assert: COUNT(*) from inbox WHERE consumer_name='test.poison.handler' AND processed_at IS NOT NULL === 0 (handler never successfully completed; the inbox row may exist from the FIRST attempt then got rolled back by the throw — count both possibilities and document)
  - assert: queue depth on test.q and test.dlq via amqp-management API is 0 (everything flowed through)

  `listen-notify-wake.test.ts` (Success Criterion 5 — sub-criterion of REQ-WALL-06):
  - boot harness; register the standard handler
  - measure: const start = performance.now(); inside em.transactional do outboxRepo.add(buildEnvelope({...}), {...}); end of transactional triggers commit which fires pg_notify; wait for handler to be invoked; const elapsedMs = performance.now() - start; assert elapsedMs < 250 (per Success Criterion 5)
  - run 5 iterations and report median + max — document in SUMMARY
  - if assertion fails consistently, document the tolerance bump (per 02-RESEARCH assumption A5: bump to 500ms acceptable since semantics still met)</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/packages/messaging-spine && INTEGRATION=1 bun test tests/integration/kill-9-recovery.test.ts && INTEGRATION=1 bun test tests/integration/poison-message.test.ts && INTEGRATION=1 bun test tests/integration/listen-notify-wake.test.ts</automated>
  </verify>
  <done>Three failure-mode integration tests pass against real Postgres + RabbitMQ; kill-9 simulation proves at-least-once + dedup absorb the duplicate; poison-message proves DLQ exhaustion → dead_letter_messages row; listen-notify-wake proves sub-250ms publish latency (or documents the bump to 500ms with rationale).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → Docker daemon | testcontainers requires Docker available; tests skip cleanly (process.exit 0) when INTEGRATION env not set |
| MessagingProbe test aggregate | NEVER reaches production builds because production mikro-orm.config.ts files do not include it (verified by plan 02-07 entity list) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-29 | Tampering | MessagingProbe entity leaks into production via accidental import | mitigate | Probe entity lives only inside tests/integration/_helpers/; production mikro-orm.config.ts files list entities explicitly (per plan 02-07 Task 1) — the probe is structurally unreachable from production builds |
| T-02-30 | Denial of Service | testcontainers requires Docker socket; flaky on certain CI images | accept | Tests gated behind INTEGRATION=1; CI runs them in the docker-up job (Phase 10 CI design); locally, developers without Docker get a clean skip |
| T-02-31 | Tampering | listen-notify-wake assertion is flaky under loaded CI | mitigate | Plan documents the 250ms target with a 500ms tolerance per 02-RESEARCH A5; if flaky, the assertion bumps to 500ms but the SUMMARY records the actual median |
| T-02-32 | Information Disclosure | testcontainers logs leak Postgres password (default 'postgres') | accept | testcontainers credentials are ephemeral; no production secrets involved |
</threat_model>

<verification>
1. `INTEGRATION=1 bun test tests/integration` (from packages/messaging-spine) passes all six tests
2. Total runtime expected 30-90s depending on Docker performance — log captured in SUMMARY
3. All six success criteria from ROADMAP §Phase 2 are demonstrated by exactly one test
4. MessagingProbe is searchable ONLY in tests/integration/_helpers/* — `grep -r MessagingProbe services/` returns nothing
5. Each test file starts with the INTEGRATION env gate so plain `bun test` does not boot Docker
6. Test harness teardown stops both containers and closes the NestJS app
</verification>

<success_criteria>
- All six integration tests pass against real Postgres 18 + RabbitMQ 4.2 quorum queues
- Success Criterion 1 (outbox + inbox + dedup): covered by outbox-write-and-publish + inbox-dedup
- Success Criterion 2 (kill-9 recovery): covered by kill-9-recovery
- Success Criterion 3 (poison → dead_letter_messages after 5+3): covered by poison-message
- Success Criterion 4 (envelope correlation/causation propagation): covered by envelope-headers
- Success Criterion 5 (LISTEN/NOTIFY wake < 250ms): covered by listen-notify-wake (or documented bump per A5)
- MessagingProbe lives only in test helpers — never imported by production source
- Tests gate on INTEGRATION=1 so non-Docker environments skip cleanly
</success_criteria>

<output>
Create `.planning/phases/02-outbox-inbox-spine/02-09-SUMMARY.md` documenting:
- Each test's actual outcome + runtime
- The listen-notify-wake median + max latency over 5 runs; whether the 250ms or 500ms threshold was used and why
- Any deviation from the planned scenarios (e.g., if a third-party testcontainer API needed adaptation)
- The harness teardown contract and any caveats (e.g., RabbitMQ container slow to stop on Mac)
- Confirmation that MessagingProbe is absent from production source
</output>
