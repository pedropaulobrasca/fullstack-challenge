---
phase: 03-wallet-service
plan: 08
type: tdd
wave: 4
depends_on:
  - 03-02
  - 03-05
files_modified:
  - services/wallets/package.json
  - services/wallets/tests/property/wallet-zero-net.test.ts
  - services/wallets/tests/unit/provision-wallet.use-case.test.ts
autonomous: true
requirements:
  - REQ-DOM-03
  - REQ-WALL-03
  - REQ-WALL-01
must_haves:
  truths:
    - "Property test runs 10_000 fast-check cases against the pure-domain Wallet aggregate and confirms any zero-net sequence of debits and credits returns the wallet to its original balance"
    - "ProvisionWalletUseCase unit test confirms first call returns {created:true} with balance = INITIAL_BALANCE_CENTS, second call returns {created:false} with the same wallet (REQ-WALL-01 + REQ-WALL-03)"
    - "Property test predicate is pure (no DB) so 10_000 cases execute in under 10 seconds"
  artifacts:
    - path: services/wallets/tests/property/wallet-zero-net.test.ts
      provides: "fast-check 10k zero-net balance preservation property"
      contains: "numRuns: 10_000"
    - path: services/wallets/tests/unit/provision-wallet.use-case.test.ts
      provides: "Idempotent provisioning unit test against a mocked WalletRepository"
      contains: "ProvisionWalletUseCase"
  key_links:
    - from: services/wallets/tests/property/wallet-zero-net.test.ts
      to: services/wallets/src/domain/wallet.aggregate.ts
      via: "Wallet.debit / Wallet.credit"
      pattern: "wallet\\.debit\\("
---

<objective>
Lock the Phase 3 ROADMAP Success Criterion #5 (10k-case property test) and the unit-level idempotency contract for ProvisionWalletUseCase. Both run against pure-domain or in-memory mocks — no DB — so the suite stays under 10 seconds.

Purpose: REQ-DOM-03 (balance invariant) + ROADMAP SC#5 (zero-net property). REQ-WALL-01 + REQ-WALL-03 (idempotent provisioning at INITIAL_BALANCE_CENTS) get unit-test coverage here separately from the integration tests in Plan 03-09 which assert end-to-end HTTP behaviour.
Output: Two new test files; `fast-check@4.8.0` dev-dependency added if not already present; `bun test tests/unit tests/property` reports two suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-wallet-service/03-RESEARCH.md
@services/wallets/src/domain/wallet.aggregate.ts
@services/wallets/src/domain/transaction.aggregate.ts
@services/wallets/src/application/use-cases/provision-wallet.use-case.ts
@services/wallets/src/config/defaults.ts
@packages/shared-kernel/src/money/money.ts

<interfaces>
From Plan 03-02 (domain): `Wallet.provision(WalletId, PlayerId, Money, Date) -> Wallet`; `wallet.debit(amount, corrId, msgId, now) -> { next, transaction }`; `wallet.credit(...) -> { next, transaction }`; the call is pure and synchronous.

From `@crash/shared-kernel`: `Money.of(bigint, currency?)`.

From Plan 03-05 (application): `ProvisionWalletUseCase` constructor takes `(em, walletRepo)` where `em.transactional(cb)` invokes `cb()` and returns its result. For unit testing, mock both: the fake em simply runs the callback inline; the fake repo holds an in-memory map.

fast-check 4.x API (per 03-RESEARCH §Property Test Design):
- `fc.bigInt({ min, max })` returns a bigint arbitrary
- `fc.constantFrom("DEBIT", "CREDIT")` picks one
- `fc.array(arb, { minLength, maxLength })`
- `await fc.assert(fc.asyncProperty(arb, async (val) => { ... }), { numRuns: 10_000 })`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install fast-check (if missing) and write the 10k zero-net property test</name>
  <read_first>
    - services/wallets/package.json (confirm whether fast-check is already a devDep)
    - .planning/phases/03-wallet-service/03-RESEARCH.md "Property Test Design" (arbitrary + assertion sketch)
    - services/wallets/src/domain/wallet.aggregate.ts (confirm debit/credit signatures)
    - packages/shared-kernel/src/money/money.ts (confirm Money.of throws NegativeMoneyError on negative inputs)
  </read_first>
  <behavior>
    - For each of 10_000 cases: pick an initial balance between 1_000_000n and 1_000_000_000n cents; pick an array (length 0..100) of (op, amount) tuples; append a balancer op that makes the net zero. Apply the sequence to a `Wallet.provision(...)` starting state, padding insufficient debits with a preceding credit so `Money.subtract` never throws — and account for any padding by recording it as part of the running net (so the balancer at the end still produces zero net).
    - Final assertion: `wallet.balance.toCents() === initialCents`.
    - The predicate is synchronous (no `Promise`, no DB), but use `fc.asyncProperty` per fast-check 4.x convention. Total runtime MUST stay under 10s for the 10k runs (verify via the `--timeout` flag or `time` wrapper).
  </behavior>
  <action>From repo root, if `fast-check` is not in `services/wallets/package.json` devDependencies: `bun add -D fast-check@4.8.0 -w services/wallets`. Create `services/wallets/tests/property/wallet-zero-net.test.ts` per the sketch in 03-RESEARCH §Property Test Design. Use `import { test, expect } from "bun:test"` and `import * as fc from "fast-check"`. Use `Wallet.provision`, `wallet.debit`, `wallet.credit` from the domain aggregate; use a fresh `correlationId` + `messageId` per op (e.g. `CorrelationId(\`cid-\${i}\`)`, `\`mid-\${i}\``). Carefully handle the padding case: when a DEBIT would exceed the current balance, prepend a CREDIT of the same amount AND remember to subtract that amount from the running net (so the final balancer remains correct). Run `time bun test tests/property/wallet-zero-net.test.ts` and confirm it passes within 10 seconds. Commit as `test(wallets): fast-check 10k zero-net balance preservation property`.</action>
  <verify>
    <automated>cd services/wallets && time bun test tests/property/wallet-zero-net.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Test passes; runtime under 10 seconds; `grep -n 'numRuns: 10_000' services/wallets/tests/property/wallet-zero-net.test.ts` matches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProvisionWalletUseCase idempotency unit test (in-memory mock repo)</name>
  <read_first>
    - services/wallets/src/application/use-cases/provision-wallet.use-case.ts (final shape from Plan 03-05)
    - services/wallets/src/config/defaults.ts (INITIAL_BALANCE_CENTS = 100000n by default)
    - services/wallets/src/domain/wallet.aggregate.ts (Wallet.provision signature)
  </read_first>
  <behavior>
    - Test 1: First call to `ProvisionWalletUseCase.execute(PlayerId("p-1"))` against an empty in-memory repo returns `{ created: true }` and the resulting wallet's `balance.toCents() === env.INITIAL_BALANCE_CENTS`.
    - Test 2: Second call with the same playerId returns `{ created: false }` and the wallet is the SAME instance (or has the same id) as the first call.
    - Test 3: Calls with two different player ids each return `{ created: true }` with distinct walletIds.
    - Test 4: Simulated UNIQUE constraint race — the fake repo's `save()` throws an SQLSTATE 23505 error on the first attempt; the usecase MUST catch, re-read via findByPlayerId, and return `{ created: false }` with the pre-existing wallet (covers the two-tab race per 03-RESEARCH §Open Question 3).
    - Test 5: The usecase invokes `em.transactional` exactly once per call (mock `em.transactional` and assert call count).
  </behavior>
  <action>Create `services/wallets/tests/unit/provision-wallet.use-case.test.ts` using `bun:test`. Build a tiny `InMemoryWalletRepository` in the test file (implements the `WalletRepository` interface, backed by `Map<string, Wallet>`); its `save` method is replaceable per-test for Test 4's race simulation. Build a fake `EntityManager`-shaped object with `transactional: async (cb) => cb()` and a call-counter. Wire up the use case with these doubles, run the five tests above. Tests MUST fail if the usecase from Plan 03-05 isn't catching 23505 — that's a regression net for the open question. Commit as `test(wallets): ProvisionWalletUseCase idempotency and 23505 race resolution`.</action>
  <verify>
    <automated>cd services/wallets && bun test tests/unit/provision-wallet.use-case.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Five tests pass; mock em.transactional is called exactly once per usecase invocation; SQLSTATE 23505 race is covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test predicate ↔ domain aggregate | Property test treats the aggregate as a black box; only public methods exercised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-29 | Tampering | Domain regression sneaks in (negative balance, lost cents) | mitigate | 10k-case property test runs in CI; any regression that violates zero-net is shrunk to the minimal failing sequence by fast-check |
| T-03-30 | Repudiation | Double-provisioning under race condition | mitigate | Unit test simulates 23505 and confirms the usecase resolves the race correctly without creating two wallets |
| T-03-SC | Tampering | npm install of fast-check 4.8.0 | mitigate | fast-check is already used by Phase 1 (shared-kernel property tests); audit recorded in 03-RESEARCH §Package Legitimacy Audit |
</threat_model>

<verification>
- `cd services/wallets && time bun test tests/property/wallet-zero-net.test.ts` reports pass within 10s.
- `cd services/wallets && bun test tests/unit/provision-wallet.use-case.test.ts` reports 5 pass / 0 fail.
- `grep -n 'numRuns: 10_000' services/wallets/tests/property/wallet-zero-net.test.ts` matches.
- `grep -n '23505' services/wallets/tests/unit/provision-wallet.use-case.test.ts` matches.
</verification>

<success_criteria>
- ROADMAP SC#5 (10k property test) is observably true and runs in CI.
- ProvisionWalletUseCase idempotency is locked under unit tests independent of the DB.
- No new infra dependencies introduced.
</success_criteria>

<output>
Create `.planning/phases/03-wallet-service/03-08-SUMMARY.md` recording: property-test runtime (seconds), the shrinking behaviour if any test ever failed during development (which seeds reproduce), and the in-memory repository contract used by the unit tests.
</output>
