---
phase: 03-wallet-service
plan: 10
type: execute
wave: 6
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
  - 03-05
  - 03-06
  - 03-07
  - 03-08
  - 03-09
files_modified:
  - .planning/adrs/ADR-011-ledger-model-wallet-snapshot.md
  - .planning/adrs/ADR-012-jwt-validation-via-cached-jwks.md
  - .planning/adrs/ADR-013-idempotent-subscribe-propagates-tx-em.md
  - .planning/adrs/README.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-DOM-03
  - REQ-AUTH-04
  - REQ-WALL-01
  - REQ-WALL-02
  - REQ-WALL-03
  - REQ-WALL-04
  - REQ-WALL-07
must_haves:
  truths:
    - "ADR-011, ADR-012, ADR-013 exist at .planning/adrs/ with the canonical five-section structure (Context / Considered / Decision / Consequences / Alternatives Rejected)"
    - "ADR catalogue README.md is updated with a Phase 3 section linking all three ADRs"
    - "STATE.md advances to Phase 3 complete, Phase 4 ready, progress bar updated"
    - "ROADMAP.md Phase 3 entry checks every plan checkbox (03-01..03-10) and increments the Progress table to 3 / 10"
    - "REQUIREMENTS.md traceability table marks REQ-DOM-03, REQ-AUTH-04, REQ-WALL-01, REQ-WALL-02, REQ-WALL-03, REQ-WALL-04, REQ-WALL-07 as Done with the Phase 3 plan IDs"
  artifacts:
    - path: .planning/adrs/ADR-011-ledger-model-wallet-snapshot.md
      provides: "ADR-011: Wallet snapshot + immutable Transaction over event sourcing"
      contains: "Decision"
    - path: .planning/adrs/ADR-012-jwt-validation-via-cached-jwks.md
      provides: "ADR-012: jose JWKS validation at each service over Kong JWT plugin"
      contains: "createRemoteJWKSet"
    - path: .planning/adrs/ADR-013-idempotent-subscribe-propagates-tx-em.md
      provides: "ADR-013: spine decorator propagates txEm into handler signature"
      contains: "txEm"
  key_links:
    - from: .planning/adrs/README.md
      to: .planning/adrs/ADR-011-ledger-model-wallet-snapshot.md
      via: "Phase 3 ADR catalogue section"
      pattern: "ADR-011"
---

<objective>
Close Phase 3 with three ADRs (011 ledger model, 012 JWKS validation, 013 spine decorator txEm propagation) and update the three project memory artifacts (STATE.md, ROADMAP.md, REQUIREMENTS.md) to reflect completion. The closeout is the recruiter's reading material — every significant decision in Phase 3 gets a one-page ADR with context, alternatives rejected, and consequences.

Purpose: Phase 2 set the precedent (ADR-007 through ADR-010 with a five-section structure). Phase 3 must mirror it. REQUIREMENTS.md traceability gives the recruiter a single page showing which plan / commit landed each REQ-ID. STATE.md and ROADMAP.md keep the GSD workflow honest about progress.
Output: Three ADR files + three updated planning files + the ADR catalogue index extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/adrs/README.md
@.planning/phases/03-wallet-service/03-RESEARCH.md
@.planning/phases/03-wallet-service/03-01-PLAN.md
@.planning/phases/03-wallet-service/03-04-PLAN.md

<interfaces>
ADR template (from Phase 2 ADRs and CLAUDE.md §ADRs):
- Filename: `.planning/adrs/ADR-NNN-<kebab-slug>.md`
- Front matter / header includes: `# ADR-NNN: <Title>`, `**Status**:`, `**Date**:`, `**Phase**:`
- Sections in order: `## Context`, `## Considered`, `## Decision`, `## Consequences`, `## Alternatives Rejected`
- Phase 2 examples (ADR-007 through ADR-010) live in `.planning/adrs/`.

STATE.md update points:
- `## Current Position` block — phase / plan / status / progress bar
- `## Performance Metrics` table — phases complete, ADRs landed, v1 requirements complete
- `## Session Continuity` § Phase history — flip Phase 3 row to "Complete"
- `## Session Continuity` § Recent activity — prepend a 2026-05-25 entry covering the Phase 3 closeout

ROADMAP.md update points:
- Phase 3 plan checklist — flip all ten `- [ ]` to `- [x]`
- "Plans Complete" column in the Progress table — set to `10/10`
- Phase 3 header — `[x] Phase 3: Wallet Service`

REQUIREMENTS.md update points:
- Per-phase traceability table for Phase 3 — flip every Pending status to Done with the plan ID that landed it
- "Coverage summary" v1 completed count incremented by 7
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-011, ADR-012, ADR-013</name>
  <read_first>
    - .planning/adrs/README.md (current catalogue structure with Phase 1 and Phase 2 sections)
    - .planning/adrs/ADR-007-hand-rolled-messaging-spine.md (Phase 2 template reference for tone and depth)
    - .planning/adrs/ADR-010-dedicated-pg-client-for-listen-notify.md (another Phase 2 reference)
    - .planning/phases/03-wallet-service/03-RESEARCH.md §"Decisions to Make in Phase 3" + §"OI-1 / OI-3 Resolution Recommendation"
  </read_first>
  <action>Create `.planning/adrs/ADR-011-ledger-model-wallet-snapshot.md` with `Status: Accepted`, `Date: 2026-05-25`, `Phase: 3`. Context: REQ-WALL-07 demands an immutable ledger; REQ-DOM-03 needs an O(1) balance read on every wallet endpoint and AMQP consume. Considered: full event sourcing (rebuild balance from Transaction stream on every read), Transaction-as-child-of-Wallet (load history per debit), Wallet snapshot + immutable Transaction in same DB TX (chosen). Decision: Wallet stores a mutable `balance_cents` snapshot column; every debit/credit appends a Transaction row referencing `correlationId` + `messageId`; both rows write in the same DB TX as the inbox dedup row. Consequences: O(1) reads, audit trail intact, UNIQUE(message_id) on transactions backstops the inbox layer, no event-sourcing rebuild cost. Alternatives Rejected: event sourcing (rebuild cost on hot paths), transactions-only-no-snapshot (forces aggregation per read). Create `.planning/adrs/ADR-012-jwt-validation-via-cached-jwks.md`. Context: REQ-AUTH-04 + ARCH §1 + Kong has no JWT plugin. Considered: `passport-jwt + jwks-rsa + @nestjs/passport`, Kong JWT plugin (would need DB mode), `jose.createRemoteJWKSet + jwtVerify` (chosen). Decision: each NestJS service holds a `jose.createRemoteJWKSet` cache; `JwtGuard implements CanActivate` validates per request; Kong stays a pure router. Document the audience strategy decided in Plan 03-04 Task 3 (Option A realm mapper OR Option B accept account audience) — record which option was chosen + final KEYCLOAK_AUDIENCE value. Consequences: fewer deps, no Passport decorator metadata fighting Bun's SWC (PITFALLS M1), JWKS cache + cooldown built-in, Kong remains thin and declarative. Alternatives Rejected: Passport chain (per above), Kong JWT plugin (operational hassle), hand-rolled JWKS fetch (no audited library). Create `.planning/adrs/ADR-013-idempotent-subscribe-propagates-tx-em.md`. Context: Phase 2 OI-3 — `host.em.transactional` opened a TX but didn't pass the inner `txEm` to handlers; entity mutations went to a different EM context. Considered: Option A propagate `txEm` to handler signature (chosen), Option B mandate raw SQL in every handler, Option C internal repo flush. Decision: spine decorator changes signature to `(envelope, msg, txEm)`; handlers use `txEm` for entity persistence; raw SQL via `txEm.getConnection().execute()` is fine for the atomic UPDATE path (race-safe; same TX). Consequences: handlers use MikroORM identity map naturally; spine API is public-API-changed (semver minor); Phase 2 integration tests were re-run after Plan 03-01 and any double-wrap dependencies removed. Alternatives Rejected: Option B (defeats ADR-001 ORM benefits), Option C (em.flush mid-TX is a footgun). Commit as `docs(adrs): ADR-011, ADR-012, ADR-013 for Phase 3 closeout` (no AI attribution).</action>
  <verify>
    <automated>ls .planning/adrs/ADR-01{1,2,3}-*.md 2>&1 && grep -lE 'Status: Accepted' .planning/adrs/ADR-011-*.md .planning/adrs/ADR-012-*.md .planning/adrs/ADR-013-*.md | wc -l</automated>
  </verify>
  <done>Three ADR files exist; grep confirms each has `Status: Accepted`; commit landed without AI attribution.</done>
</task>

<task type="auto">
  <name>Task 2: Update ADR catalogue README + STATE.md</name>
  <read_first>
    - .planning/adrs/README.md (current Phase 1 + Phase 2 sections)
    - .planning/STATE.md (current Phase 2 complete state)
  </read_first>
  <action>Edit `.planning/adrs/README.md` adding a Phase 3 section after the Phase 2 section, listing ADR-011, ADR-012, ADR-013 with one-line summaries linking to each file. Edit `.planning/STATE.md`: (a) `## Current Position` — Phase: `3 — Wallet Service (complete)`, Plan: `P3.10 complete`, Status sentence summarising "All 10 Phase 3 plans landed. Wallet bounded context functional: domain aggregates, atomic UPDATE, JWT guard, REST endpoints, AMQP debit/credit consumers, ADRs 011-013 landed. STATE + ROADMAP advanced. 26/26 smoke probes, 6/6 integration tests, 10k-case property test under 10s.", Progress bar `▰▰▰▱▱▱▱▱▱▱ 3/10 phases complete`, Next action: `Run /gsd:verify-phase 3 then /gsd:plan-phase 4 (Game Core)`. (b) `## Performance Metrics` table — Phases complete `3 / 10`, ADRs landed `13`, v1 requirements complete `12 / 95` (add 7 from Phase 3). (c) `## Session Continuity` § Phase history table — flip Phase 3 row to `10/10 plans landed (P3.10 ADRs + closeout) | Complete | All 10 Phase 3 plans landed; 26/26 smoke probes; 6/6 integration tests; property test green`. (d) Prepend a `2026-05-25 — P3.10 (Phase 3 closeout) executed:` recent-activity entry summarising ADRs 011-013, smoke probe count (26/26), integration test count (6/6), and the requirements traceability flip. Commit as `docs: ADR catalogue + STATE.md advance to Phase 3 complete`.</action>
  <verify>
    <automated>grep -nE 'Phase 3|3 / 10|3/10 phases' .planning/STATE.md && grep -nE 'ADR-011|ADR-012|ADR-013' .planning/adrs/README.md</automated>
  </verify>
  <done>STATE.md reports Phase 3 complete with the right counters; ADR README links all three Phase 3 ADRs.</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md + REQUIREMENTS.md traceability</name>
  <read_first>
    - .planning/ROADMAP.md (current Phase 3 entry — Plans: 10 plans declared but plan list lines absent; per the orchestrator workflow plan-phase populates them)
    - .planning/REQUIREMENTS.md "Phase 3 — Wallet Service (7 reqs)" traceability table
  </read_first>
  <action>Edit `.planning/ROADMAP.md`: (a) flip the Phase 3 header from `[ ]` to `[x]` in the top-of-file phase list AND in the per-phase section header. (b) Replace the "Plans: 10 plans" line (or update it if previously populated) so it lists every Phase 3 plan ID and brief objective: `Plans:` followed by ten `- [x] 03-NN-PLAN.md — <objective>` lines (use the same one-liner format Phase 2 used). All ten boxes checked since this plan executes after every other Phase 3 plan. (c) Update the Progress table at the bottom: Phase 3 row → `10/10 | Complete | 2026-05-25`. Edit `.planning/REQUIREMENTS.md`: flip status from `Pending` to `Done` for REQ-DOM-03 (annotate with `(P3.2)`), REQ-AUTH-04 (`(P3.4)`), REQ-WALL-01 (`(P3.5)`), REQ-WALL-02 (`(P3.5)`), REQ-WALL-03 (`(P3.5)`), REQ-WALL-04 (`(P3.6 + P3.7)`), REQ-WALL-07 (`(P3.3 + P3.6)`). Increment the v1 completed total in the Coverage summary section by 7 (from 5 to 12). Commit as `docs: ROADMAP and REQUIREMENTS traceability — Phase 3 complete`.</action>
  <verify>
    <automated>grep -nE 'REQ-WALL-01.*Done|REQ-WALL-04.*Done|REQ-AUTH-04.*Done' .planning/REQUIREMENTS.md | head -5 && grep -nE '^- \\[x\\] \\*\\*Phase 3' .planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP shows Phase 3 checked + 10/10 plans + Progress row updated; REQUIREMENTS shows all seven Phase 3 REQ-IDs as Done with plan IDs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project memory (STATE/ROADMAP/REQUIREMENTS) ↔ recruiter / future planner | These files are read-only inputs to the next phase; misreporting completion misleads the verifier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-35 | Repudiation | ADR claim without supporting evidence | mitigate | Each ADR references the plan IDs that landed the decision and the integration test files that prove it |
| T-03-36 | Tampering | Premature "Done" flip in REQUIREMENTS.md | mitigate | Plan 03-10 explicitly depends on every other Phase 3 plan (depends_on: 01..09); the flip only happens after the integration suite and smoke probes pass |
</threat_model>

<verification>
- `ls .planning/adrs/ADR-011-*.md .planning/adrs/ADR-012-*.md .planning/adrs/ADR-013-*.md` returns three files.
- `grep -c '^## ' .planning/adrs/ADR-011-*.md` returns at least 5 (the five canonical sections).
- `grep -nE 'Phase 3' .planning/STATE.md | head -5` shows the Phase 3 complete entries.
- `grep -nE 'REQ-WALL-(01|02|03|04|07).*Done|REQ-DOM-03.*Done|REQ-AUTH-04.*Done' .planning/REQUIREMENTS.md` returns 7 matches.
</verification>

<success_criteria>
- Three ADRs exist with the canonical five-section structure.
- STATE.md advances to Phase 3 complete with updated metrics.
- ROADMAP.md Phase 3 entry is fully checked; Progress table reflects 3/10 phases complete.
- REQUIREMENTS.md traceability flips all seven Phase 3 REQ-IDs to Done with plan IDs.
- Commits land without AI attribution per CLAUDE.md.
</success_criteria>

<output>
Create `.planning/phases/03-wallet-service/03-10-SUMMARY.md` recording: the three ADR file paths and a one-line decision summary each, the diff stats for the three planning files, and the new v1-complete count (12/95).
</output>
