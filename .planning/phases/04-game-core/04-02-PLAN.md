---
phase: 04-game-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/games/src/domain/value-objects/multiplier.ts
  - services/games/src/domain/value-objects/crash-point.ts
  - services/games/src/domain/value-objects/bet-amount.ts
  - services/games/src/domain/value-objects/round-status.ts
  - services/games/src/domain/value-objects/bet-status.ts
  - services/games/src/domain/value-objects/seed.ts
  - services/games/src/domain/value-objects/index.ts
  - services/games/src/domain/errors.ts
  - services/games/src/domain/round.aggregate.ts
  - services/games/src/domain/round.repository.ts
  - services/games/src/domain/seed-chain.repository.ts
  - services/games/src/domain/round-betting-window.service.ts
  - services/games/tests/unit/value-objects.test.ts
  - services/games/tests/unit/round.aggregate.test.ts
  - services/games/tests/unit/round-betting-window.test.ts
  - services/games/tests/property/round-fsm.property.test.ts
  - services/games/tests/setup.ts
  - services/games/package.json
autonomous: true
requirements:
  - REQ-DOM-01
  - REQ-DOM-08
  - REQ-GAME-08
  - REQ-FAIR-02
  - REQ-FAIR-05
  - REQ-TEST-01
  - REQ-TEST-02

must_haves:
  truths:
    - "Round.schedule constructs a Round in BETTING with seedHash exposed and serverSeed=null (REQ-FAIR-05 commitment available before any bet)"
    - "Round.start() throws IllegalRoundTransitionError if current status is not BETTING; only path to RUNNING is from BETTING"
    - "Round.crash(at, time) throws if status is not RUNNING; sets crashPoint + crashedAt"
    - "Round.settle(serverSeed, now) throws if status is not CRASHED; this is the ONLY path that populates serverSeed (REQ-FAIR-02 reveal-after-settle)"
    - "isBettingOpen(round, now) returns true iff round.status === 'BETTING' && round.bettingEndsAt > now (REQ-GAME-08 domain-layer guard)"
    - "Multiplier.of(0.99) throws; Multiplier.of(1.00) accepted; Multiplier carries tenThousandths bigint (×10000) for lossless bet-times-multiplier math"
    - "CrashPoint.of(0.99) throws; CrashPoint stores centiX integer (×100); CrashPoint.toNumber() and CrashPoint.toCentiX() round-trip identically"
    - "BetAmount reads env.BET_MIN_CENTS and env.BET_MAX_CENTS at construction; out-of-bounds throws BetAmountOutOfBoundsError"
    - "Property test: 1000 random sequences of FSM operations never reach an illegal state (every illegal call throws)"
  artifacts:
    - path: "services/games/src/domain/round.aggregate.ts"
      provides: "Round aggregate (private constructor; schedule/rehydrate static factories; start/crash/settle behavior methods returning new Round)"
      min_lines: 80
    - path: "services/games/src/domain/round-betting-window.service.ts"
      provides: "Pure domain service isBettingOpen(round, now) used by Phase 5 saga's bet acceptance check"
    - path: "services/games/src/domain/value-objects/multiplier.ts"
      provides: "Multiplier VO with tenThousandths bigint scale + of/fromCentiX/toCentiX/toNumber"
    - path: "services/games/src/domain/value-objects/crash-point.ts"
      provides: "CrashPoint VO with centiX int scale + of/fromCentiX/toCentiX/toNumber"
    - path: "services/games/src/domain/value-objects/bet-amount.ts"
      provides: "BetAmount(amount: Money) factory reading env.BET_MIN_CENTS/BET_MAX_CENTS"
    - path: "services/games/src/domain/round.repository.ts"
      provides: "RoundRepository interface — findById, findOpen, findServerSeedByNonce, save, transitionFromBettingToRunning, transitionFromRunningToCrashed, transitionFromCrashedToSettled"
    - path: "services/games/src/domain/seed-chain.repository.ts"
      provides: "SeedChainRepository interface — countEntries, insertChain(entries), findByNonce, revealSeedAtNonce"
    - path: "services/games/src/domain/errors.ts"
      provides: "IllegalRoundTransitionError, BetAmountOutOfBoundsError, MultiplierOutOfBoundsError, CrashPointOutOfBoundsError, SeedNotYetRevealedError"
    - path: "services/games/tests/property/round-fsm.property.test.ts"
      provides: "fast-check property test, numRuns >= 200, asserts no illegal transition is reachable from random FSM command sequences"
  key_links:
    - from: "services/games/src/domain/round.aggregate.ts"
      to: "services/games/src/domain/value-objects/*.ts"
      via: "type imports"
      pattern: "from \"./value-objects"
    - from: "services/games/src/domain/round.aggregate.ts"
      to: "@nestjs/*"
      via: "MUST NOT IMPORT"
      pattern: "@nestjs"
---

<objective>
Land the pure-domain Round aggregate, value objects, repository interfaces, domain errors, and the `isBettingOpen` domain service — plus exhaustive unit tests and a fast-check FSM property test. Zero infrastructure imports.

Purpose: REQ-DOM-01 requires Round lifecycle enforced at the aggregate boundary (NOT a service-layer setStatus). REQ-DOM-08 requires rich aggregates. REQ-FAIR-02 requires "seed reveal only after settle" — encoded as: `settle()` is the only path that populates `serverSeed`. REQ-FAIR-05 requires the seedHash commitment exposed during BETTING — encoded as: `schedule()` accepts seedHash and exposes it via getter before any bet. REQ-GAME-08 at the domain layer is the `isBettingOpen` service that the Phase 5 saga will call before constructing a Bet.

Output: Domain layer for Round + its supporting VOs + repository interfaces + tests. Bet aggregate ships in Plan 04-03 (parallel). Persistence ships in Plan 04-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-game-core/04-RESEARCH.md
@CLAUDE.md
@services/games/src/config/defaults.ts
@services/wallets/src/domain/wallet.aggregate.ts
@packages/shared-kernel/src/index.ts

<interfaces>
<!-- This plan CREATES the domain surface. Plans 04-03 (Bet aggregate), 04-04 (persistence), 04-06 (round loop) consume these. -->

From services/games/src/domain/value-objects/round-status.ts (new):
```typescript
export type RoundStatus = "BETTING" | "RUNNING" | "CRASHED" | "SETTLED";
export const ROUND_STATUSES: readonly RoundStatus[] = ["BETTING", "RUNNING", "CRASHED", "SETTLED"];
```

From services/games/src/domain/round.aggregate.ts (new):
```typescript
export type RoundProps = {
  id: RoundId;
  nonce: bigint;
  status: RoundStatus;
  seedHash: string;
  clientSeed: string;
  serverSeed: string | null;
  crashPoint: CrashPoint | null;
  formulaVersion: number;
  bettingEndsAt: Date;
  startedAt: Date | null;
  crashedAt: Date | null;
  settledAt: Date | null;
  createdAt: Date;
};
export class Round {
  static schedule(id: RoundId, nonce: bigint, seedHash: string, clientSeed: string,
                  formulaVersion: number, bettingEndsAt: Date, now: Date): Round;
  static rehydrate(props: RoundProps): Round;
  start(now: Date): Round;
  crash(at: CrashPoint, time: Date): Round;
  settle(serverSeed: string, now: Date): Round;
  // getters: id, nonce, status, seedHash, clientSeed, serverSeed, crashPoint,
  //          formulaVersion, bettingEndsAt, startedAt, crashedAt, settledAt, createdAt
}
```

From services/games/src/domain/round.repository.ts (new):
```typescript
export interface RoundRepository {
  findById(id: RoundId): Promise<Round | null>;
  findOpen(): Promise<Round | null>;                 // non-SETTLED round, at most one
  findServerSeedByNonce(nonce: bigint): Promise<string | null>;
  listSettledHistory(limit: number, offset: number): Promise<Round[]>;
  saveScheduled(round: Round): Promise<void>;        // INSERT new BETTING round
  transitionFromBettingToRunning(id: RoundId, startedAt: Date, txEm?: unknown): Promise<Round | null>;
  transitionFromRunningToCrashed(id: RoundId, crashPoint: CrashPoint, crashedAt: Date, txEm?: unknown): Promise<Round | null>;
  transitionFromCrashedToSettled(id: RoundId, serverSeed: string, settledAt: Date, txEm?: unknown): Promise<Round | null>;
}
```

From services/games/src/domain/seed-chain.repository.ts (new):
```typescript
export interface SeedChainRepository {
  countEntries(): Promise<bigint>;
  insertChain(entries: { nonce: bigint; hash: string }[]): Promise<void>;
  findHashByNonce(nonce: bigint): Promise<string | null>;
  findSeedByNonce(nonce: bigint): Promise<string | null>;       // null until revealed
  revealSeedAtNonce(nonce: bigint, seed: string, revealedAt: Date, txEm?: unknown): Promise<void>;
}
```

Patterns to mirror:
- services/wallets/src/domain/wallet.aggregate.ts — private ctor + static schedule/rehydrate + immutable returns
- services/wallets/src/domain/errors.ts (if present) — domain error class pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test setup helper + value objects + domain errors</name>
  <files>services/games/tests/setup.ts, services/games/src/domain/value-objects/multiplier.ts, services/games/src/domain/value-objects/crash-point.ts, services/games/src/domain/value-objects/bet-amount.ts, services/games/src/domain/value-objects/round-status.ts, services/games/src/domain/value-objects/bet-status.ts, services/games/src/domain/value-objects/seed.ts, services/games/src/domain/value-objects/index.ts, services/games/src/domain/errors.ts, services/games/tests/unit/value-objects.test.ts, services/games/package.json</files>
  <read_first>
    - services/games/src/config/defaults.ts (env var names BET_MIN_CENTS, BET_MAX_CENTS, INSTANT_CRASH_BUCKET — BetAmount.of reads BET_MIN/MAX)
    - services/wallets/tests/property/wallet-zero-net.test.ts (top-of-file `process.env.X ??= ...` pattern Phase 3 P3.08 W6 deviation — the games tests/setup.ts extracts this into one shared helper to avoid the 35-lint-error carry-forward documented in Phase 3 VERIFICATION.md "Phase 3 carry-forwards applied opportunistically")
    - packages/shared-kernel/src/identity/branded-id.ts (PlayerId/RoundId/BetId brands available)
    - packages/shared-kernel/src/money/index.ts (Money VO API for BetAmount wrapping)
  </read_first>
  <behavior>
    - tests/setup.ts seeds `process.env.DATABASE_URL ??= "postgres://test"`, `RABBITMQ_URL ??= "amqp://test"`, every required key from sharedEnvSchema + gamesEnvSchema, then `await import("../src/config/defaults")` to validate the schema once. Exported as a named function `setupGamesTestEnv()` consumed by every test file in a top-level `setupGamesTestEnv();` call BEFORE other imports of config-dependent modules
    - Multiplier VO: `Multiplier.of(value: number)` throws MultiplierOutOfBoundsError if value < 1.00; stores tenThousandths = BigInt(Math.round(value * 10000)); exposes toNumber() = Number(tenThousandths) / 10000, toCentiX() = Number(tenThousandths / 100n), tenThousandths getter
    - CrashPoint VO: `CrashPoint.of(value: number)` throws CrashPointOutOfBoundsError if value < 1.00 or > 1e9; stores centiX = BigInt(Math.round(value * 100)); exposes toNumber(), toCentiX(), fromCentiX(centi: number) static factory
    - BetAmount factory: `BetAmount.of(money: Money): Money` reads env.BET_MIN_CENTS and env.BET_MAX_CENTS at call time (not module-load time) and throws BetAmountOutOfBoundsError if `money.toCents() < env.BET_MIN_CENTS || money.toCents() > env.BET_MAX_CENTS`. Returns Money unchanged on success (no new VO — BetAmount is a guard, not a separate type, since Money already carries the value semantics per Phase 1 ADR-002)
    - round-status.ts: union type + readonly ROUND_STATUSES array (also exported)
    - bet-status.ts: union type `"PENDING" | "ACTIVE" | "CASHED_OUT" | "LOST" | "REFUNDED"` + readonly array
    - seed.ts: type alias `Seed = string` with helper `isValidSeedHex(s: string): boolean` (length === 64 && /^[0-9a-f]+$/.test(s))
    - errors.ts: IllegalRoundTransitionError(from: RoundStatus, to: RoundStatus), IllegalBetTransitionError(from: BetStatus, to: BetStatus), BetAmountOutOfBoundsError({amountCents, min, max}), MultiplierOutOfBoundsError(value), CrashPointOutOfBoundsError(value), SeedNotYetRevealedError(roundId). All extend Error; each sets `.name` to its class name; each carries a `code` static getter returning a SCREAMING_SNAKE_CASE string
    - value-objects/index.ts barrel re-exports every public symbol
    - Unit test value-objects.test.ts covers: Multiplier.of(0.99) throws, Multiplier.of(1.00) accepted, tenThousandths round-trip; CrashPoint.of(0.5) throws, CrashPoint.of(2.34).toCentiX() === 234; BetAmount.of(Money.of(50n)) throws (below default 100); BetAmount.of(Money.of(100n)) accepted; isValidSeedHex on valid + invalid hex
    - package.json: ensure `"fast-check": "^3.23.0"` devDep present (add if missing — Phase 3 wallets has it; games may not)
  </behavior>
  <action>Create the test setup file FIRST (per Phase 3 carry-forward — single shared env bootstrap avoids 35 lint errors). Then ship value objects in their own files (one VO per file for grep-ability per CLAUDE.md "names should be self-explanatory"). errors.ts holds ALL domain errors in one file (matches services/wallets/src/domain/errors.ts pattern). VOs are immutable — every internal field `readonly`; no mutation methods. BetAmount.of() reads env LAZILY (at call time) so test fixtures that override env via setupGamesTestEnv() observe the override. Throw guard: `if (Number.isNaN(value) || !Number.isFinite(value))` BEFORE the bounds check in Multiplier.of and CrashPoint.of. Add `"fast-check": "^3.23.0"` to services/games/package.json devDependencies if not already present (align with Phase 3 W2 — do NOT pull 4.x).</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/services/games && bun test tests/unit/value-objects.test.ts</automated>
  </verify>
  <done>
    - All value-object unit tests green
    - `grep -rE "@nestjs|@mikro-orm" services/games/src/domain/` returns nothing
    - `grep -c "process.env" services/games/tests/setup.ts` >= 5 (env keys seeded)
    - `bunx tsc --noEmit` clean from services/games
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Round aggregate + repository interfaces + isBettingOpen service + FSM unit + property tests</name>
  <files>services/games/src/domain/round.aggregate.ts, services/games/src/domain/round.repository.ts, services/games/src/domain/seed-chain.repository.ts, services/games/src/domain/round-betting-window.service.ts, services/games/tests/unit/round.aggregate.test.ts, services/games/tests/unit/round-betting-window.test.ts, services/games/tests/property/round-fsm.property.test.ts</files>
  <read_first>
    - services/wallets/src/domain/wallet.aggregate.ts (private ctor + static factories + immutable returns — Round mirrors this exactly)
    - .planning/phases/04-game-core/04-RESEARCH.md "Pattern 1: Round aggregate" code sample
    - .planning/phases/04-game-core/04-RESEARCH.md "Anti-Patterns to Avoid" (Round MUST NOT have acceptBet or a Bets collection — Bet is its own aggregate per ADR-014)
    - services/wallets/tests/property/wallet-zero-net.test.ts (fast-check command-pattern reference for FSM property test)
  </read_first>
  <behavior>
    - Round aggregate per <interfaces> block: private constructor, schedule/rehydrate static factories, getters for every prop, start/crash/settle behavior methods that throw IllegalRoundTransitionError on illegal source status and return a NEW Round instance (immutable update via `new Round({ ...this.props, ... })`)
    - schedule(id, nonce, seedHash, clientSeed, formulaVersion, bettingEndsAt, now): returns Round with status=BETTING, serverSeed=null, crashPoint=null, startedAt=null, crashedAt=null, settledAt=null, createdAt=now
    - start(now): requires status === BETTING; returns Round with status=RUNNING, startedAt=now
    - crash(at: CrashPoint, time: Date): requires status === RUNNING; returns Round with status=CRASHED, crashPoint=at, crashedAt=time
    - settle(serverSeed: string, now: Date): requires status === CRASHED; throws if !isValidSeedHex(serverSeed); returns Round with status=SETTLED, serverSeed=serverSeed, settledAt=now
    - NO acceptBet method; NO bets[] collection (ADR-014 enforced by absence)
    - round.repository.ts: interface only per <interfaces> block — pure-domain, no MikroORM types (txEm typed as `unknown` so MikroEntityManager can pass through without leaking)
    - seed-chain.repository.ts: interface only per <interfaces> block
    - round-betting-window.service.ts: pure function `isBettingOpen(round: Round, now: Date): boolean` returning `round.status === "BETTING" && round.bettingEndsAt.getTime() > now.getTime()`. Exported as a NAMED FUNCTION, not a class — it has no state, no DI need
    - Unit test round.aggregate.test.ts covers: schedule produces BETTING with seedHash exposed (REQ-FAIR-05); start from BETTING returns RUNNING; start from RUNNING throws IllegalRoundTransitionError with `.code === "ILLEGAL_ROUND_TRANSITION"`; crash from RUNNING returns CRASHED with crashPoint set; crash from BETTING throws; settle from CRASHED returns SETTLED with serverSeed set; settle from RUNNING throws (REQ-FAIR-02 — seed cannot be revealed pre-crash); settle with invalid seed hex throws; rehydrate round-trip preserves every prop
    - Unit test round-betting-window.test.ts covers: isBettingOpen returns true for BETTING with bettingEndsAt in future, false for BETTING with past bettingEndsAt, false for RUNNING/CRASHED/SETTLED regardless of bettingEndsAt
    - Property test round-fsm.property.test.ts using fast-check: `numRuns >= 200`, generate random sequences of `{action: "start"|"crash"|"settle", arg?: CrashPoint|string}` against a Round, assert: every illegal action call throws an IllegalRoundTransitionError (caught and asserted); legal action sequences only reach the four valid statuses; serverSeed is null in BETTING/RUNNING/CRASHED and only non-null in SETTLED (REQ-FAIR-02 property-level proof)
  </behavior>
  <action>Create round.aggregate.ts mirroring services/wallets/src/domain/wallet.aggregate.ts structure: `private constructor(private readonly props: RoundProps) {}`, static factories, getters via `get x(): T { return this.props.x; }`, behavior methods returning `new Round({...this.props, ...patch})`. Repository interfaces are TYPE-only files — no class bodies, no concrete implementations. The Bet aggregate ships in Plan 04-03 (parallel wave) and brings its own BetRepository interface — DO NOT create bet.repository.ts here. The fast-check property test uses fc.commands or a hand-rolled command generator (per Phase 3 wallet-zero-net.test.ts precedent — choose whichever matches the team style). Every test file starts with `import { setupGamesTestEnv } from "../setup"; setupGamesTestEnv();` BEFORE other imports. The IllegalRoundTransitionError message includes `from` and `to` in the format `Illegal Round transition: ${from} → ${to}` (the arrow character is a normal ASCII `->`, NOT a unicode arrow — keep dependency-free / grep-friendly). Use `RoundId(crypto.randomUUID())` from `@crash/shared-kernel` for test fixtures.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/services/games && bun test tests/unit/round.aggregate.test.ts tests/unit/round-betting-window.test.ts tests/property/round-fsm.property.test.ts</automated>
  </verify>
  <done>
    - All three test files green
    - Property test numRuns >= 200 in config; total expect() calls > 200
    - `grep -rE "@nestjs|@mikro-orm|socket\\.io|axios" services/games/src/domain/round.aggregate.ts services/games/src/domain/round.repository.ts services/games/src/domain/seed-chain.repository.ts services/games/src/domain/round-betting-window.service.ts` returns nothing
    - `grep -c "acceptBet\|bets:" services/games/src/domain/round.aggregate.ts` returns 0 (ADR-014 absence-test)
    - `bunx tsc --noEmit` clean from services/games
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain layer (no external input) | Pure functions; inputs come from application layer which has its own validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | Round FSM bypass via service-layer setStatus | mitigate | No setStatus method exists; only start/crash/settle paths; property test asserts no illegal state reachable |
| T-04-02-02 | Information Disclosure | serverSeed leak before settle | mitigate | settle() is the ONLY method that mutates serverSeed; REQ-FAIR-02 property test asserts serverSeed === null pre-SETTLED |
| T-04-02-03 | Tampering | Reviver of persisted round bypasses FSM | mitigate | rehydrate accepts a full RoundProps but the only callers are repositories restoring DB state; no untrusted input crosses the boundary |
| T-04-02-04 | Repudiation | Round FSM not verifiable | mitigate | Property test with numRuns >= 200 explores random command sequences |
</threat_model>

<verification>
- `bun test services/games/tests/unit tests/property` from services/games runs the three test files green
- `bunx tsc --noEmit` from services/games clean
- `grep -rE "@nestjs|@mikro-orm" services/games/src/domain/` returns nothing
- Round aggregate has no `acceptBet`, no `bets:` collection field (ADR-014 absence-test)
- Property test asserts no illegal FSM transition reachable across 200+ random sequences
</verification>

<success_criteria>
- Round aggregate ships in pure-domain folder with zero infra imports
- Round FSM enforced at aggregate boundary (REQ-DOM-01) — verified by unit + property tests
- Seed reveal only on settle (REQ-FAIR-02) — verified by property test
- Pre-round seedHash exposed via getter during BETTING (REQ-FAIR-05) — verified by unit test
- isBettingOpen domain service ships for Phase 5 saga consumption (REQ-GAME-08 domain layer)
- Value objects + errors + repository interfaces complete
- Property test with numRuns >= 200 green
</success_criteria>

<output>
Create `.planning/phases/04-game-core/04-02-SUMMARY.md` with: files created, fast-check numRuns, total unit test count, anti-pattern absence-test commands.
</output>
