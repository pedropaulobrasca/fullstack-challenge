---
phase: 04-game-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - services/games/src/domain/bet.aggregate.ts
  - services/games/src/domain/bet.repository.ts
  - services/games/tests/unit/bet.aggregate.test.ts
  - services/games/tests/unit/bet-amount.value-object.test.ts
  - services/games/tests/property/money-rounding.property.test.ts
  - services/games/tests/unit/money-rounding-sanity.test.ts
autonomous: true
requirements:
  - REQ-DOM-02
  - REQ-DOM-04
  - REQ-DOM-07
  - REQ-DOM-08
  - REQ-TEST-01
  - REQ-TEST-02

must_haves:
  truths:
    - "Bet.place constructs a Bet in PENDING status — only Phase 5 saga calls this (no Phase 4 controller exists for POST /games/bet)"
    - "Bet.confirm() throws IllegalBetTransitionError if status is not PENDING"
    - "Bet.cashOut(multiplier, time) throws if status is not ACTIVE; returns {next, payout} where payout = bet.amount.multiply(multiplier) with banker's rounding"
    - "Bet.lose() requires status === ACTIVE; sets status=LOST"
    - "Bet.refund(reason) requires status === PENDING; sets status=REFUNDED + refundReason"
    - "BetAmount guard rejects values < env.BET_MIN_CENTS or > env.BET_MAX_CENTS"
    - "Money rounding sanity: 100 × 1.005 rounds to 100 (banker's half-to-even) and 100 × 1.015 rounds to 102 (banker's half-to-even, 1.5 → even = 2) — OR if Money.multiply truncates, the test FAILS and the next task in this plan adds Money.multiplyRounded to shared-kernel"
    - "Property test: 10k random (betCents, multiplierTenThousandths) pairs — payout = floor(bet * multiplier / 10000) when banker resolves to floor in the chosen Money semantics; cashout × inverse multiplier never exceeds original bet"
  artifacts:
    - path: "services/games/src/domain/bet.aggregate.ts"
      provides: "Bet aggregate — private ctor + static place/rehydrate factories + confirm/cashOut/lose/refund behavior methods"
      min_lines: 100
    - path: "services/games/src/domain/bet.repository.ts"
      provides: "BetRepository interface — findById, findActiveByRoundAndPlayer, findActiveByRound, listByPlayer, save, tryTransition"
    - path: "services/games/tests/unit/money-rounding-sanity.test.ts"
      provides: "Banker's-rounding sanity test (REQ-DOM-07) — resolves research Open Q1 by hammering Money.multiply with known half-cent cases"
    - path: "services/games/tests/property/money-rounding.property.test.ts"
      provides: "fast-check 10k-case loss-free monetary rounding property"
  key_links:
    - from: "services/games/src/domain/bet.aggregate.ts"
      to: "@crash/shared-kernel"
      via: "Money type import"
      pattern: "from \"@crash/shared-kernel\""
    - from: "services/games/src/domain/bet.aggregate.ts"
      to: "services/games/src/domain/value-objects/multiplier.ts"
      via: "Multiplier type import"
      pattern: "from \"./value-objects/multiplier\""
---

<objective>
Land the pure-domain Bet aggregate as a sibling-of-Round aggregate (per ADR-014 — Bet is NOT nested in Round), plus the BetRepository interface, exhaustive Bet FSM unit tests, the Money rounding sanity test (resolves research Open Q1), the BetAmount bounds unit test, and the 10k-case fast-check property test for monetary rounding.

Purpose: REQ-DOM-02 — Bet is its own aggregate so per-cashout locking (Phase 5 saga) touches one Bet row, not the entire Round. REQ-DOM-04 — bet bounds. REQ-DOM-07 — banker's rounding on `bet × multiplier`. REQ-DOM-08 — rich aggregate with behavior methods.

CRITICAL: This plan does NOT scaffold POST /games/bet — that ships in Phase 5 with its saga (per research §Architectural Responsibility Map). `Bet.place` static factory exists but is called only by the Phase 5 saga.

Output: Bet aggregate + repo interface + unit tests + property test + money-rounding sanity test that resolves Open Q1 BEFORE Wave 4 (round loop) needs it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-game-core/04-RESEARCH.md
@CLAUDE.md
@services/wallets/src/domain/wallet.aggregate.ts
@services/wallets/src/domain/transaction.aggregate.ts
@packages/shared-kernel/src/index.ts
@services/games/src/config/defaults.ts

<interfaces>
<!-- This plan CREATES Bet domain surface. Plan 04-04 (persistence) and Phase 5 (saga) consume these. -->

From services/games/src/domain/bet.aggregate.ts (new):
```typescript
export type BetProps = {
  id: BetId;
  roundId: RoundId;
  playerId: PlayerId;
  amount: Money;
  status: BetStatus;
  cashedOutAt: Date | null;
  cashedOutMultiplier: Multiplier | null;
  payout: Money | null;
  refundReason: string | null;
  createdAt: Date;
};
export type BetCashOutResult = { next: Bet; payout: Money };
export class Bet {
  static place(id: BetId, roundId: RoundId, playerId: PlayerId, amount: Money, now: Date): Bet;
  static rehydrate(props: BetProps): Bet;
  confirm(): Bet;
  cashOut(multiplier: Multiplier, time: Date): BetCashOutResult;
  lose(): Bet;
  refund(reason: string): Bet;
  // getters for every prop
}
```

From services/games/src/domain/bet.repository.ts (new):
```typescript
export interface BetRepository {
  findById(id: BetId): Promise<Bet | null>;
  findActiveByRoundAndPlayer(roundId: RoundId, playerId: PlayerId): Promise<Bet | null>;
  findActiveByRound(roundId: RoundId): Promise<Bet[]>;
  listByPlayer(playerId: PlayerId, limit: number, offset: number): Promise<Bet[]>;
  save(bet: Bet, txEm?: unknown): Promise<void>;
  tryTransition(id: BetId, fromStatus: BetStatus, toStatus: BetStatus, patch: Partial<BetProps>, txEm?: unknown): Promise<Bet | null>;
}
```

Existing shared-kernel surface (DO NOT modify in this plan unless money-rounding-sanity fails):
- `Money.of(cents: bigint): Money`
- `Money.multiply({ numerator: bigint, denominator: bigint }): Money` — Dinero v2 backend, default truncate-toward-zero per Dinero docs
- `Money.toCents(): bigint`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Money rounding sanity test (resolves Open Q1) + BetAmount bounds unit test</name>
  <files>services/games/tests/unit/money-rounding-sanity.test.ts, services/games/tests/unit/bet-amount.value-object.test.ts</files>
  <read_first>
    - .planning/phases/04-game-core/04-RESEARCH.md "Open Questions > 1. Banker's rounding implementation" (the recommendation is to write the sanity test FIRST and discover Dinero's actual default before assuming)
    - packages/shared-kernel/src/money/index.ts (existing Money.multiply signature + Dinero backend)
    - services/games/src/domain/value-objects/bet-amount.ts (from Plan 04-02 — BetAmount.of reads env at call time)
  </read_first>
  <behavior>
    - money-rounding-sanity.test.ts ASSERTS banker's (half-to-even) rounded literals — W3 plan-check fix. REQ-DOM-07 is explicit; the test must enforce the requirement, not pin observed Dinero behavior. Test cases: `Money.of(100n).multiply({numerator:1005n,denominator:1000n}).toCents() === 100n` (1.005 → half-to-even → 1.00), `Money.of(100n).multiply({numerator:1015n,denominator:1000n}).toCents() === 102n` (1.015 → half-to-even → 1.02), `Money.of(100n).multiply({numerator:1025n,denominator:1000n}).toCents() === 102n` (1.025 → half-to-even → 1.02), `Money.of(100n).multiply({numerator:1035n,denominator:1000n}).toCents() === 104n` (1.035 → half-to-even → 1.04). If test fails (Dinero default ≠ banker's), this plan EXTENDS `packages/shared-kernel/src/money/money.ts` with `multiplyRounded(factor, mode: "bankers" | "half-up" = "bankers"): Money` using Dinero v2's `multiply(factor, { round: halfEven })` import, AND updates Bet.cashOut to call multiplyRounded. REQ-DOM-07 MUST ship satisfied in Phase 4, not deferred.
    - The test covers: 100 × 1.005 (half-cent up), 100 × 1.015 (half-cent up), 100 × 1.995 (near-integer), 100 × 0.5 (half-only), 1n × 100n/3n (low-precision residual), 7n × 333n/1000n (irrational-ish)
    - bet-amount.value-object.test.ts covers: BetAmount.of(Money.of(99n)) throws BetAmountOutOfBoundsError with the right code; BetAmount.of(Money.of(100n)) accepted; BetAmount.of(Money.of(100_000n)) accepted; BetAmount.of(Money.of(100_001n)) throws; env-override via setupGamesTestEnv with `process.env.BET_MIN_CENTS = "500"` propagates so 100 throws after override (test uses Bun's module cache reset OR re-imports config inside the test to honor the override)
  </behavior>
  <action>W3 plan-check fix: ASSERT banker's literal values, do NOT pin observed Dinero behavior. Write the money-rounding-sanity test with EXPLICIT banker's-rounded expected values (see behavior block). Run the test. Branch on result: (a) GREEN → Dinero default is already banker's, REQ-DOM-07 satisfied by `Money.multiply` as shipped, move on. (b) RED → Dinero default differs from banker's. Within THIS plan (scope expansion approved by plan-check), extend `packages/shared-kernel/src/money/money.ts` with `multiplyRounded(factor: { numerator: bigint; denominator: bigint }, mode: "bankers" | "half-up" = "bankers"): Money` that wraps Dinero v2's `multiply` with appropriate `round: halfEven` option from `dinero.js` (research the v2.x import path during execution — likely `import { halfEven, halfUp } from "dinero.js"` then `dinero.multiply(d, factor, { round: halfEven })`). Add `multiplyRounded` to the shared-kernel `index.ts` barrel. Update Bet.cashOut to call `bet.amount.multiplyRounded(multiplier)` instead of `bet.amount.multiply(multiplier)`. Re-run test until GREEN. Document the chosen path (a or b) in 04-03-SUMMARY.md. ADR-018 is no longer conditional — if path (b) is taken, ADR-018 in 04-12 documents the shared-kernel extension as a real architectural change. For bet-amount tests, the env override path requires calling `setupGamesTestEnv({ BET_MIN_CENTS: "500" })` — if Plan 04-02's setup helper doesn't accept overrides, extend it within this plan to accept an optional `overrides?: Record<string, string>` parameter that gets applied AFTER the defaults `??=` block.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/services/games && bun test tests/unit/money-rounding-sanity.test.ts tests/unit/bet-amount.value-object.test.ts</automated>
  </verify>
  <done>
    - Both test files green
    - money-rounding-sanity.test.ts contains a top-of-file comment block documenting observed Money.multiply semantics
    - 04-03-SUMMARY.md (created at plan completion) records the observed banker's-vs-truncate result for ADR-018 input
    - BetAmount env override path verified (setup helper accepts overrides)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bet aggregate + repository interface + Bet FSM unit + cashout property tests</name>
  <files>services/games/src/domain/bet.aggregate.ts, services/games/src/domain/bet.repository.ts, services/games/tests/unit/bet.aggregate.test.ts, services/games/tests/property/money-rounding.property.test.ts</files>
  <read_first>
    - services/wallets/src/domain/transaction.aggregate.ts (immutable factory-constructed pattern)
    - services/wallets/src/domain/wallet.aggregate.ts (private ctor + behavior methods returning new instance)
    - .planning/phases/04-game-core/04-RESEARCH.md "Pattern 2: Bet aggregate" code sample (mirrors this structure)
    - services/games/tests/unit/money-rounding-sanity.test.ts (Task 1 output — pinned Money.multiply semantics inform cashOut payout test expectations)
  </read_first>
  <behavior>
    - bet.aggregate.ts per <interfaces> block: private ctor, static place(id, roundId, playerId, amount, now) returns PENDING bet with null cashedOutAt/Multiplier/payout/refundReason; rehydrate(props) returns Bet
    - confirm(): requires status===PENDING; returns new Bet with status=ACTIVE
    - cashOut(multiplier, time): requires status===ACTIVE; computes payout = `this.props.amount.multiply({ numerator: multiplier.tenThousandths, denominator: 10_000n })`; returns `{ next: new Bet({...props, status:"CASHED_OUT", cashedOutAt: time, cashedOutMultiplier: multiplier, payout}), payout }`
    - lose(): requires status===ACTIVE; returns new Bet with status=LOST
    - refund(reason): requires status===PENDING; returns new Bet with status=REFUNDED + refundReason (note: ADR-014 + research §Pitfall 4 — REFUNDED is only reachable from PENDING; an ACTIVE bet cannot be refunded — it can only be CASHED_OUT or LOST)
    - bet.repository.ts: interface only per <interfaces> block. `tryTransition` is the optimistic-locking primitive Phase 5 saga uses to atomically transition Bet states with concurrent-cashout safety (acquired-row count = 1 on success, 0 on race-loss)
    - Bet FSM unit test bet.aggregate.test.ts: every transition (place→confirm→cashOut, place→confirm→lose, place→refund) has a passing case and an illegal-source case asserting IllegalBetTransitionError; cashOut payout equality test against the Money-multiply pinned semantics from Task 1; cashOut returns BOTH next AND payout (destructured)
    - Property test money-rounding.property.test.ts with fast-check numRuns=10000: generates (betCents in [100..100000], multiplierTenThousandths in [10000..1000000_0]) — i.e., 1.00x to 1000.00x — calls Bet.place + confirm + cashOut, asserts: (a) payout.toCents() is finite and >= 0; (b) payout.toCents() === amount.toCents() × multiplier.tenThousandths / 10000n with the observed-Dinero rounding (call the same Money.multiply in the assertion to make the property tautological in the chosen rounding); (c) the loss-free property — `cashedOut.payout.divide({numerator: 10_000n, denominator: multiplier.tenThousandths})` rounds back to <= original amount (favours player at most by 1 cent in the chosen rounding direction)
  </behavior>
  <action>Create bet.aggregate.ts mirroring wallet.aggregate.ts structure. The cashOut method MUST use `multiplier.tenThousandths` (bigint, ×10000) as numerator — this is the lossless integer scale chosen in Plan 04-02 for Multiplier. The repository interface in bet.repository.ts is type-only — no class, no concrete implementations (those ship in Plan 04-04). The property test imports `fc` from `fast-check` (^3.23.x aligned). All tests start with `setupGamesTestEnv();` BEFORE other imports. The IllegalBetTransitionError uses the same format/code pattern as IllegalRoundTransitionError (Plan 04-02 errors.ts) — message: `Illegal Bet transition: ${from} -> ${to}`, code: `ILLEGAL_BET_TRANSITION`. Property test numRuns=10000 to match Phase 3 wallet-zero-net.test.ts precision; if runtime exceeds 5s, scale down to 5000 and document in SUMMARY.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/services/games && bun test tests/unit/bet.aggregate.test.ts tests/property/money-rounding.property.test.ts</automated>
  </verify>
  <done>
    - Both test files green
    - Property test numRuns >= 5000 (preferably 10000)
    - `grep -rE "@nestjs|@mikro-orm" services/games/src/domain/bet.aggregate.ts services/games/src/domain/bet.repository.ts` returns nothing
    - `grep -c "round:" services/games/src/domain/bet.aggregate.ts` returns 0 — Bet references Round by roundId only, no nav property (ADR-014 absence-test)
    - `bunx tsc --noEmit` from services/games clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bet domain (no external input) | Phase 4 has no controller wiring this; Phase 5 saga is the only future caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | Cashout payout manipulation | mitigate | Multiplier × bet computed via Money.multiply with bigint scale; no client-supplied payout |
| T-04-03-02 | Tampering | Bet FSM bypass | mitigate | Behavior methods are the only path; tryTransition is the only atomic infrastructure-level transition |
| T-04-03-03 | Information Disclosure | Cross-player bet data | accept | Domain layer; query masking lives in Plan 04-08 controller/use-case |
| T-04-03-04 | Repudiation | Cashout dispute (player claims they hit higher multiplier) | mitigate | cashedOutAt + cashedOutMultiplier persisted; Phase 6 WS-stamped cashoutAcceptedAt resolves race (out of scope for Phase 4 but ADR-018 in Phase 6 records mechanism) |
</threat_model>

<verification>
- `bun test services/games/tests/unit/bet.aggregate.test.ts tests/property/money-rounding.property.test.ts tests/unit/money-rounding-sanity.test.ts tests/unit/bet-amount.value-object.test.ts` from services/games green
- `bunx tsc --noEmit` clean
- Money.multiply observed semantics documented in money-rounding-sanity.test.ts top comment
- Bet has no Round navigation property (ADR-014 absence-test)
</verification>

<success_criteria>
- Bet aggregate ships pure-domain
- BetRepository interface defined
- Bet FSM enforced at aggregate boundary (REQ-DOM-08 + per-status REQ-DOM-04)
- Cashout math wired to Money.multiply with observed-banker's-or-truncate semantics documented (REQ-DOM-07 — finalization in Plan 04-12 ADR-018)
- Property test 10k cases (or 5k with documented timing) green
- BetAmount bounds enforced (REQ-DOM-04)
</success_criteria>

<output>
Create `.planning/phases/04-game-core/04-03-SUMMARY.md` with: files created, observed Money.multiply rounding mode (banker's vs truncate), property test numRuns + duration, ADR-018 input data.
</output>
