---
phase: 04-game-core
plan: 04
type: execute
wave: 2
depends_on:
  - 04-02
  - 04-03
files_modified:
  - services/games/src/infrastructure/persistence/seed-chain.entity.ts
  - services/games/src/infrastructure/persistence/round.entity.ts
  - services/games/src/infrastructure/persistence/bet.entity.ts
  - services/games/src/infrastructure/repositories/mikro-seed-chain.repository.ts
  - services/games/src/infrastructure/repositories/mikro-round.repository.ts
  - services/games/src/infrastructure/repositories/mikro-bet.repository.ts
  - services/games/src/infrastructure/mikro-orm/migrations/20260526001-create-seed-chain.ts
  - services/games/src/infrastructure/mikro-orm/migrations/20260526002-create-rounds.ts
  - services/games/src/infrastructure/mikro-orm/migrations/20260526003-create-bets.ts
  - services/games/src/application/tokens.ts
  - services/games/mikro-orm.config.ts
autonomous: true
requirements:
  - REQ-DOM-01
  - REQ-DOM-02
  - REQ-DOM-08

must_haves:
  truths:
    - "Three migrations apply at `bun run docker:up` against a fresh postgres volume — seed_chain first (rounds FKs to it), then rounds, then bets (bets FKs to rounds)"
    - "rounds CHECK constraint rejects illegal FSM tuples — e.g., status='SETTLED' with NULL server_seed is rejected at INSERT/UPDATE time"
    - "bets partial unique index `(player_id, round_id) WHERE status IN ('PENDING','ACTIVE')` prevents double-active-bet at DB level — INSERT of a second PENDING row for the same (player, round) returns Postgres SQLSTATE 23505"
    - "bets CHECK constraint on amount_cents BETWEEN 100 AND 100000 (spec defaults — env overrides at app layer; DB is defense-in-depth)"
    - "crash_point_centi_x INT (scale ×100) — avoids pg NUMERIC → JS Number precision drift"
    - "Repository methods bind raw SQL to em.getTransactionContext() per Phase 3 ADR-013 precedent (critical fix from P3.09)"
  artifacts:
    - path: "services/games/src/infrastructure/persistence/seed-chain.entity.ts"
      provides: "EntitySchema<SeedChainRow> mapping seed_chain table; nonce BIGINT PK"
    - path: "services/games/src/infrastructure/persistence/round.entity.ts"
      provides: "EntitySchema<RoundRow> mapping rounds table; status, seedHash, clientSeed, serverSeed nullable, crashPointCentiX nullable INT"
    - path: "services/games/src/infrastructure/persistence/bet.entity.ts"
      provides: "EntitySchema<BetRow> mapping bets table; FK round_id, partial-unique index expressed at DB layer"
    - path: "services/games/src/infrastructure/mikro-orm/migrations/20260526001-create-seed-chain.ts"
      provides: "seed_chain DDL with nonce BIGINT PRIMARY KEY, unrevealed partial index"
    - path: "services/games/src/infrastructure/mikro-orm/migrations/20260526002-create-rounds.ts"
      provides: "rounds DDL with 4-arm CHECK FSM constraint + status-index + history-index"
    - path: "services/games/src/infrastructure/mikro-orm/migrations/20260526003-create-bets.ts"
      provides: "bets DDL with partial unique index (REQ-DOM-02), bounds CHECK, payout CHECK, status-conditional CHECK"
    - path: "services/games/src/infrastructure/repositories/mikro-round.repository.ts"
      provides: "MikroRoundRepository implementing RoundRepository — atomic UPDATE bound to em.getTransactionContext() on all transition methods"
    - path: "services/games/src/infrastructure/repositories/mikro-bet.repository.ts"
      provides: "MikroBetRepository implementing BetRepository — tryTransition uses atomic UPDATE ... WHERE id=? AND status=? RETURNING *"
    - path: "services/games/src/infrastructure/repositories/mikro-seed-chain.repository.ts"
      provides: "MikroSeedChainRepository — bulk insert via batched COPY/INSERT, findSeedByNonce, revealSeedAtNonce"
    - path: "services/games/src/application/tokens.ts"
      provides: "DI tokens ROUND_REPOSITORY, BET_REPOSITORY, SEED_CHAIN_REPOSITORY"
  key_links:
    - from: "services/games/src/infrastructure/repositories/mikro-round.repository.ts"
      to: "em.getTransactionContext()"
      via: "em.getConnection().execute(sql, params, 'all', em.getTransactionContext())"
      pattern: "em\\.getTransactionContext\\(\\)"
    - from: "services/games/mikro-orm.config.ts"
      to: "RoundEntitySchema, BetEntitySchema, SeedChainEntitySchema"
      via: "entities array"
      pattern: "RoundEntitySchema\\|BetEntitySchema\\|SeedChainEntitySchema"
---

<objective>
Ship the three MikroORM EntitySchemas, three migrations (with the partial unique index for REQ-DOM-02 and the 4-arm FSM CHECK for REQ-DOM-01 defense-in-depth), and three MikroORM-backed repositories implementing the Plan-04-02/03 interfaces using the Phase 3 atomic-UPDATE-bound-to-txEm pattern.

Purpose: REQ-DOM-02 partial unique index at the DB layer (the CRITICAL anti-double-bet guard, even before Phase 5 saga code exists). REQ-DOM-01 FSM CHECK constraint at the DB layer is defense-in-depth — the Round aggregate is the primary guard, the DB CHECK is the safety net. The crash_point_centi_x INT scale (×100) avoids Postgres NUMERIC → JS Number precision drift documented in research §9 note 1.

Output: All persistence-layer plumbing for Round, Bet, and SeedChain. Migrations apply cleanly on a fresh `docker:up`. Repositories pass typecheck. Integration tests against this code ship in Plan 04-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-game-core/04-RESEARCH.md
@CLAUDE.md
@.planning/phases/04-game-core/04-02-PLAN.md
@.planning/phases/04-game-core/04-03-PLAN.md
@services/wallets/src/infrastructure/persistence/wallet.entity.ts
@services/wallets/src/infrastructure/repositories/mikro-wallet.repository.ts
@services/wallets/src/infrastructure/mikro-orm/migrations/20260525001-create-wallets.ts
@services/games/mikro-orm.config.ts

<interfaces>
<!-- This plan IMPLEMENTS the interfaces created in 04-02 and 04-03. -->

DDL contracts (from research §9 Postgres Schema):
```sql
-- seed_chain (nonce BIGINT PK, hash NOT NULL, seed NULL until revealed)
-- rounds (id UUID PK; nonce BIGINT UNIQUE FK to seed_chain; 4-arm CHECK FSM; crash_point_centi_x INT)
-- bets (id UUID PK; FK round_id; amount_cents BIGINT CHECK 100..100000; partial unique index on (player_id, round_id) WHERE status IN ('PENDING','ACTIVE'); status-conditional CHECK on cashed_out_* fields)
```

Repository contracts from Plan 04-02 / 04-03 (must be implemented byte-for-byte):
- RoundRepository (findById, findOpen, findServerSeedByNonce, listSettledHistory, saveScheduled, transitionFromBettingToRunning, transitionFromRunningToCrashed, transitionFromCrashedToSettled)
- BetRepository (findById, findActiveByRoundAndPlayer, findActiveByRound, listByPlayer, save, tryTransition)
- SeedChainRepository (countEntries, insertChain, findHashByNonce, findSeedByNonce, revealSeedAtNonce)

Pattern reused VERBATIM from Phase 3 (P3.09 critical fix commit 5678c0f):
```typescript
const rows = await em.getConnection().execute<Row[]>(
  `UPDATE ... WHERE ... RETURNING *`,
  [params],
  "all",
  em.getTransactionContext(),    // CRITICAL — binds to decorator-opened TX
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: EntitySchemas + three migrations + mikro-orm.config wiring</name>
  <files>services/games/src/infrastructure/persistence/seed-chain.entity.ts, services/games/src/infrastructure/persistence/round.entity.ts, services/games/src/infrastructure/persistence/bet.entity.ts, services/games/src/infrastructure/mikro-orm/migrations/20260526001-create-seed-chain.ts, services/games/src/infrastructure/mikro-orm/migrations/20260526002-create-rounds.ts, services/games/src/infrastructure/mikro-orm/migrations/20260526003-create-bets.ts, services/games/mikro-orm.config.ts, services/games/src/application/tokens.ts</files>
  <read_first>
    - services/wallets/src/infrastructure/persistence/wallet.entity.ts (EntitySchema pattern with BigIntType, fieldName mapping)
    - services/wallets/src/infrastructure/persistence/transaction.entity.ts (EntitySchema with FK + JSON fields)
    - services/wallets/src/infrastructure/mikro-orm/migrations/20260525001-create-wallets.ts (migration up/down + CHECK constraint pattern)
    - services/wallets/src/infrastructure/mikro-orm/migrations/20260525002-create-transactions.ts (FK + UNIQUE constraint pattern)
    - .planning/phases/04-game-core/04-RESEARCH.md §"Postgres Schema DDL" (the canonical DDL for all three tables)
    - services/games/mikro-orm.config.ts (current entities array — extend with the three new schemas)
  </read_first>
  <behavior>
    - seed-chain.entity.ts exports `SeedChainRow` class with fields `nonce: bigint, hash: string, seed: string | null, revealedAt: Date | null, createdAt: Date` and `SeedChainEntitySchema = new EntitySchema<SeedChainRow>({class: SeedChainRow, tableName: "seed_chain", properties: {nonce: {type: new BigIntType(), primary: true}, hash: {type: "string"}, seed: {type: "string", nullable: true}, revealedAt: {type: "Date", fieldName: "revealed_at", nullable: true}, createdAt: {type: "Date", fieldName: "created_at", defaultRaw: "now()", onCreate: () => new Date()}}})`
    - round.entity.ts exports `RoundRow` and `RoundEntitySchema` mapping id (uuid PK), nonce (BigIntType, unique), status (string), seedHash (string), clientSeed (string), serverSeed (string, nullable), crashPointCentiX (number, nullable, fieldName "crash_point_centi_x"), formulaVersion (number), bettingEndsAt (Date), startedAt (Date, nullable), crashedAt (Date, nullable), settledAt (Date, nullable), createdAt (Date defaultRaw now())
    - bet.entity.ts exports `BetRow` and `BetEntitySchema` mapping id (uuid PK), roundId (uuid, fieldName "round_id"), playerId (string, fieldName "player_id"), amountCents (BigIntType, fieldName "amount_cents"), currencyCode (string), status (string), cashedOutAt (Date, nullable), cashedOutMultiplierCentiX (number, nullable, fieldName "cashed_out_multiplier_centi_x"), payoutCents (BigIntType, nullable, fieldName "payout_cents"), refundReason (string, nullable, fieldName "refund_reason"), createdAt (Date defaultRaw now())
    - Migration 20260526001-create-seed-chain.ts: `CREATE TABLE seed_chain (nonce BIGINT PRIMARY KEY, hash TEXT NOT NULL, seed TEXT NULL, revealed_at TIMESTAMPTZ NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now())` + `CREATE INDEX seed_chain_unrevealed_idx ON seed_chain(nonce) WHERE seed IS NULL`
    - Migration 20260526002-create-rounds.ts: `CREATE TABLE rounds (id UUID PRIMARY KEY, nonce BIGINT NOT NULL UNIQUE REFERENCES seed_chain(nonce), status TEXT NOT NULL CHECK (status IN ('BETTING','RUNNING','CRASHED','SETTLED')), seed_hash TEXT NOT NULL, client_seed TEXT NOT NULL, server_seed TEXT NULL, crash_point_centi_x INT NULL, formula_version INT NOT NULL, betting_ends_at TIMESTAMPTZ NOT NULL, started_at TIMESTAMPTZ NULL, crashed_at TIMESTAMPTZ NULL, settled_at TIMESTAMPTZ NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), CONSTRAINT rounds_fsm_check CHECK ((status='BETTING' AND started_at IS NULL AND crash_point_centi_x IS NULL AND server_seed IS NULL) OR (status='RUNNING' AND started_at IS NOT NULL AND crash_point_centi_x IS NULL AND server_seed IS NULL) OR (status='CRASHED' AND started_at IS NOT NULL AND crash_point_centi_x IS NOT NULL AND server_seed IS NULL AND crashed_at IS NOT NULL) OR (status='SETTLED' AND started_at IS NOT NULL AND crash_point_centi_x IS NOT NULL AND server_seed IS NOT NULL AND settled_at IS NOT NULL)))` + `CREATE INDEX rounds_status_idx ON rounds(status) WHERE status IN ('BETTING','RUNNING','CRASHED')` + `CREATE INDEX rounds_history_idx ON rounds(settled_at DESC) WHERE status='SETTLED'`
    - Migration 20260526003-create-bets.ts: `CREATE TABLE bets (id UUID PRIMARY KEY, round_id UUID NOT NULL REFERENCES rounds(id), player_id TEXT NOT NULL, amount_cents BIGINT NOT NULL CHECK (amount_cents >= 100 AND amount_cents <= 100000), currency_code TEXT NOT NULL CHECK (length(currency_code) = 3), status TEXT NOT NULL CHECK (status IN ('PENDING','ACTIVE','CASHED_OUT','LOST','REFUNDED')), cashed_out_at TIMESTAMPTZ NULL, cashed_out_multiplier_centi_x INT NULL, payout_cents BIGINT NULL CHECK (payout_cents IS NULL OR payout_cents >= 0), refund_reason TEXT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), CONSTRAINT bets_status_check CHECK ((status IN ('PENDING','ACTIVE','REFUNDED','LOST') AND cashed_out_at IS NULL AND cashed_out_multiplier_centi_x IS NULL AND payout_cents IS NULL) OR (status='CASHED_OUT' AND cashed_out_at IS NOT NULL AND cashed_out_multiplier_centi_x IS NOT NULL AND payout_cents IS NOT NULL)))` + `CREATE UNIQUE INDEX bets_one_active_per_player ON bets(player_id, round_id) WHERE status IN ('PENDING','ACTIVE')` + `CREATE INDEX bets_player_history_idx ON bets(player_id, created_at DESC)` + `CREATE INDEX bets_round_active_idx ON bets(round_id) WHERE status IN ('PENDING','ACTIVE')` + `CREATE INDEX bets_round_player_status_idx ON bets(player_id, round_id, status)` (the research §Pitfall 4 secondary covering index)
    - All three migrations have a corresponding `down()` that DROP TABLE IF EXISTS in reverse order (bets first, then rounds, then seed_chain)
    - mikro-orm.config.ts entities array extended with the three EntitySchemas (alongside existing OutboxMessageSchema, InboxMessageSchema, DeadLetterMessageSchema)
    - tokens.ts: `export const ROUND_REPOSITORY = "ROUND_REPOSITORY"`, `export const BET_REPOSITORY = "BET_REPOSITORY"`, `export const SEED_CHAIN_REPOSITORY = "SEED_CHAIN_REPOSITORY"`
  </behavior>
  <action>Create the three EntitySchema files first (each in its own file under infrastructure/persistence/), then the three migrations (one DDL block per migration, plus indexes). Migrations are TIMESTAMPED 20260526001..003 so they sort AFTER the Phase 1/2/3 migrations already present (20260524001-create-outbox.ts through 20260525002-create-transactions.ts). The 4-arm FSM CHECK in the rounds migration is the critical defense-in-depth piece — write it as a single `CONSTRAINT rounds_fsm_check CHECK ((...) OR (...) OR (...) OR (...))` block with status-specific column nullability. The partial unique index name `bets_one_active_per_player` MUST be exact — Phase 5 saga error handling will introspect by name on SQLSTATE 23505. Add a SECONDARY covering index `bets_round_player_status_idx` per research §Pitfall 4 to support the `findActiveByRoundAndPlayer` query without scanning. Migrations: extend `Migration20260526001` class to `extends Migration` from `@mikro-orm/migrations` — match Phase 3 pattern. Wire all three EntitySchemas into mikro-orm.config.ts in BOTH `entities` and `entitiesTs` arrays. DO NOT create repositories in this task — Task 2 owns repository implementations.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/services/games && bunx tsc --noEmit</automated>
  </verify>
  <done>
    - Three migration files compile with correct timestamp prefixes
    - Three EntitySchema files compile
    - mikro-orm.config.ts entities array contains the three new schemas (verify via `grep -c "RoundEntitySchema\|BetEntitySchema\|SeedChainEntitySchema" services/games/mikro-orm.config.ts` >= 3 — once in entities, once in entitiesTs, once in import = 9 matches across two arrays + import; minimum 3)
    - tokens.ts exports three DI tokens
    - DDL strings contain the canonical CHECK constraint names (`rounds_fsm_check`, `bets_status_check`) and partial-unique-index name (`bets_one_active_per_player`) verbatim
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Three MikroORM repositories with atomic-UPDATE-bound-to-txEm pattern</name>
  <files>services/games/src/infrastructure/repositories/mikro-seed-chain.repository.ts, services/games/src/infrastructure/repositories/mikro-round.repository.ts, services/games/src/infrastructure/repositories/mikro-bet.repository.ts</files>
  <read_first>
    - services/wallets/src/infrastructure/repositories/mikro-wallet.repository.ts (CRITICAL template — atomic UPDATE with em.getTransactionContext() bound to the connection.execute call; this is the P3.09 fix commit 5678c0f and the wallet rep is the canonical reference)
    - services/games/src/domain/round.repository.ts (Plan 04-02 — interface signatures the repo implements byte-for-byte)
    - services/games/src/domain/bet.repository.ts (Plan 04-03 — interface signatures)
    - services/games/src/domain/seed-chain.repository.ts (Plan 04-02)
    - services/games/src/domain/round.aggregate.ts (Round.rehydrate for mapRowToAggregate)
    - services/games/src/domain/bet.aggregate.ts (Bet.rehydrate)
  </read_first>
  <behavior>
    - MikroSeedChainRepository: countEntries via `em.findCount(SeedChainEntitySchema, {})`; insertChain batches via `em.getConnection().execute("INSERT INTO seed_chain (nonce, hash) VALUES " + multi-row VALUES placeholders, params, "run", em.getTransactionContext())` — batches of 1000 entries (the 1M chain bootstrap in Plan 04-05 calls this 1000 times); findHashByNonce/findSeedByNonce via em.findOne; revealSeedAtNonce via atomic UPDATE WHERE seed IS NULL RETURNING (idempotent — second call no-ops)
    - MikroRoundRepository: findById/findOpen via em.findOne (findOpen filters status IN ('BETTING','RUNNING','CRASHED')); findServerSeedByNonce via em.findOne returning row.serverSeed; listSettledHistory via em.find ordered by settledAt DESC; saveScheduled inserts a BETTING row via em.persistAndFlush(em.create(...)); transitionFromBettingToRunning/RunningToCrashed/CrashedToSettled use raw SQL UPDATE ... WHERE id=? AND status=? RETURNING * bound to em.getTransactionContext() — returns Round.rehydrate(mappedRow) on success or null if WHERE clause matched zero rows (already transitioned by a peer — future scale-out path)
    - MikroBetRepository: findById/findActiveByRoundAndPlayer/findActiveByRound/listByPlayer via em.find/em.findOne; save via em.upsert(BetEntitySchema, row) + em.flush; tryTransition via raw SQL UPDATE bets SET status=?, cashed_out_at=?, cashed_out_multiplier_centi_x=?, payout_cents=?, refund_reason=? WHERE id=? AND status=? RETURNING * — returns Bet.rehydrate(row) on hit, null on miss (status race-loss)
    - mapRowToAggregate helpers in each repo translate row.amountCents (bigint via BigIntType but MikroORM returns string) → Money.of(BigInt(row.amountCents)), row.crashPointCentiX (number) → CrashPoint.fromCentiX(row.crashPointCentiX), row.cashedOutMultiplierCentiX → Multiplier.fromCentiX, status field → BetStatus/RoundStatus union narrowing with runtime check (throw if unknown status — should never happen due to DB CHECK)
    - Every repo class is `@Injectable()` and constructor-injects `EntityManager` from `@mikro-orm/postgresql` (matches wallets pattern)
    - Every raw-SQL execute call MUST pass `em.getTransactionContext()` as the 4th arg — this is the Rule-1 fix from P3.09 and the absence of it is a CRITICAL bug (the writes would commit on autocommit OUTSIDE the decorator's TX)
  </behavior>
  <action>Create the three repository files mirroring services/wallets/src/infrastructure/repositories/mikro-wallet.repository.ts. Every raw-SQL call goes `em.getConnection().execute<Row[]>(sql, params, "all", em.getTransactionContext())` or `..., "run", em.getTransactionContext())` for INSERTs that don't need RETURNING. The `txEm?: unknown` parameter on transition methods means: if caller passes one (Phase 5 saga will), it overrides `this.em`; else use `this.em`. Mirror wallet's `private resolveEm(txEm?: unknown): EntityManager { return (txEm as EntityManager) ?? this.em; }` helper. The insertChain method for the 1M seed chain MUST batch (1000 rows per INSERT) — single 1M-row INSERT will exceed Postgres parameter limit (~65k placeholders). Build the multi-VALUES SQL string at call time: `INSERT INTO seed_chain (nonce, hash) VALUES (?, ?), (?, ?), ...` with 2 placeholders per entry. The findActiveByRoundAndPlayer query uses the secondary covering index `bets_round_player_status_idx` (Task 1) by filtering `status IN ('PENDING','ACTIVE')`. DO NOT register these repos in any module yet — Plan 04-06 (round loop module wiring) owns DI registration.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/services/games && bunx tsc --noEmit</automated>
  </verify>
  <done>
    - All three repository files compile
    - `grep -c "em.getTransactionContext()" services/games/src/infrastructure/repositories/mikro-round.repository.ts` >= 3 (one per FSM transition method)
    - `grep -c "em.getTransactionContext()" services/games/src/infrastructure/repositories/mikro-bet.repository.ts` >= 1 (tryTransition)
    - `grep -c "em.getTransactionContext()" services/games/src/infrastructure/repositories/mikro-seed-chain.repository.ts` >= 2 (insertChain + revealSeedAtNonce)
    - All three classes are `@Injectable()` decorated
    - mapRowToAggregate functions present in all three (handles the row-to-domain conversion)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → Postgres | All parameters are bigint/string from domain layer; no string-concat SQL; parameterized via execute() |
| DB CHECK constraints | Defense-in-depth: domain aggregate is primary guard, DB CHECK catches the unreachable-by-code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | SQL injection via raw execute() | mitigate | All queries use parameter placeholders ?; never string concat of user input |
| T-04-04-02 | Tampering | FSM bypass at DB layer | mitigate | rounds_fsm_check 4-arm CHECK catches any code path that tries to write SETTLED with NULL server_seed |
| T-04-04-03 | Tampering | Double-active-bet race | mitigate | `bets_one_active_per_player` partial unique index — second concurrent PENDING insert returns SQLSTATE 23505 |
| T-04-04-04 | Tampering | Tx-context unbound writes commit prematurely | mitigate | Every raw-SQL execute passes em.getTransactionContext() as 4th arg per P3.09 critical fix (commit 5678c0f) |
| T-04-04-05 | Information Disclosure | Cross-player bet read | accept | Repository layer is unscoped; Plan 04-08 controllers add JwtGuard + per-player filter |
</threat_model>

<verification>
- `bunx tsc --noEmit` clean from services/games
- Three migrations file-named with correct timestamp prefix
- mikro-orm.config.ts wires all three schemas
- `em.getTransactionContext()` appears in every raw-SQL execute call
- DDL strings contain `rounds_fsm_check`, `bets_status_check`, `bets_one_active_per_player` verbatim
</verification>

<success_criteria>
- Three EntitySchemas + three migrations + three repositories ship
- REQ-DOM-02 partial unique index live at DB layer
- REQ-DOM-01 4-arm FSM CHECK at DB layer
- Repository contracts (RoundRepository, BetRepository, SeedChainRepository) fully implemented
- Atomic UPDATE bound to em.getTransactionContext() on every FSM transition method (Phase 3 critical-fix pattern reused)
- crash_point_centi_x INT scale ×100 (no NUMERIC precision drift)
</success_criteria>

<output>
Create `.planning/phases/04-game-core/04-04-SUMMARY.md` with: migration filenames, DDL constraint names committed, index list, repository method count, em.getTransactionContext() occurrence count, mikro-orm.config diff.
</output>
