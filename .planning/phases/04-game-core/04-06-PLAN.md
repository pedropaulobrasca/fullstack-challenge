---
phase: 04-game-core
plan: 06
type: execute
wave: 4
depends_on:
  - 04-01
  - 04-04
  - 04-05
files_modified:
  - services/games/src/application/round-loop.service.ts
  - services/games/src/application/use-cases/start-new-round.use-case.ts
  - services/games/src/application/use-cases/crash-round.use-case.ts
  - services/games/src/application/use-cases/settle-round.use-case.ts
  - services/games/src/application/use-cases/transition-to-running.use-case.ts
  - services/games/src/application/client-seed.derivation.ts
  - services/games/src/application/game-core.module.ts
autonomous: true
requirements:
  - REQ-GAME-01
  - REQ-GAME-09
  - REQ-FAIR-02
  - REQ-FAIR-05
  - REQ-DOM-01

must_haves:
  truths:
    - "RoundLoopService implements OnApplicationBootstrap (NOT OnModuleInit) and OnApplicationShutdown — the autonomous loop starts after every other module is ready"
    - "On boot, recoverInFlightRound() reads the latest non-SETTLED round; if RUNNING with crash time in past, retroactively crashes; if RUNNING with crash time in future, schedules setTimeout(crashAt - now); if BETTING, schedules transitionToRunning at bettingEndsAt; if CRASHED, settles immediately; if no open round or last open is SETTLED, starts a new BETTING round"
    - "Round lifecycle: schedule new round in BETTING with seedHash exposed (REQ-FAIR-05); recursive setTimeout fires transitionToRunning after BETTING_WINDOW_MS; setTimeout fires crashRound at exactly the precomputed crashTimeMs; setTimeout fires settleRound immediately after crash; setTimeout fires startNewRound after COOLDOWN_MS"
    - "Crash point is computed via deriveCrashPoint({serverSeed, clientSeed, nonce, instantCrashBucket}) at TRANSITION-TO-RUNNING time (not at BETTING start) so the seed is consulted but NOT exposed — only the hash is exposed during BETTING (REQ-FAIR-05)"
    - "Settle persists serverSeed onto the round (REQ-FAIR-02) — settle is the only path that writes server_seed; before settle, only seedHash is exposed"
    - "Active bets on the round transition to LOST in one sweep TX after the round transitions to CRASHED — per ADR-014, this is a Bets-only sweep, NOT a multi-aggregate TX"
    - "Client seed for round N derives via SHA256(prevRound.id + ':' + prevRound.crashedAt.toISOString()); round 1 (genesis) uses SHA256(GENESIS_CLIENT_SEED)"
    - "kill -9 mid-round → on restart, recovery reads DB state and resumes (RUNNING with crashTime in past → retroactive crash; RUNNING with crashTime in future → reschedule remaining setTimeout)"
  artifacts:
    - path: "services/games/src/application/round-loop.service.ts"
      provides: "RoundLoopService — OnApplicationBootstrap/OnApplicationShutdown lifecycle; recursive setTimeout loop; recoverInFlightRound"
      min_lines: 150
    - path: "services/games/src/application/use-cases/start-new-round.use-case.ts"
      provides: "Creates a new BETTING round; derives client seed from previous round (or genesis); reads next-nonce from seed_chain; computes seedHash = sha256(serverSeed)"
    - path: "services/games/src/application/use-cases/transition-to-running.use-case.ts"
      provides: "Atomic UPDATE rounds SET status=RUNNING + computes crashPoint via deriveCrashPoint and pre-computes crashTimeMs"
    - path: "services/games/src/application/use-cases/crash-round.use-case.ts"
      provides: "Atomic UPDATE rounds SET status=CRASHED + sweep update all (PENDING|ACTIVE) bets to LOST"
    - path: "services/games/src/application/use-cases/settle-round.use-case.ts"
      provides: "Atomic UPDATE rounds SET status=SETTLED + server_seed (REQ-FAIR-02 reveal gate)"
    - path: "services/games/src/application/client-seed.derivation.ts"
      provides: "Pure function deriveClientSeed(prevRound | null) per ADR-015"
  key_links:
    - from: "services/games/src/application/round-loop.service.ts"
      to: "@crash/contracts/provably-fair"
      via: "deriveCrashPoint, crashTimeMs imports"
      pattern: "from \"@crash/contracts"
    - from: "services/games/src/application/round-loop.service.ts"
      to: "setTimeout (recursive)"
      via: "private scheduleAt(ms, fn)"
      pattern: "setTimeout"
    - from: "services/games/src/application/round-loop.service.ts"
      to: "OnApplicationShutdown"
      via: "implements interface; clearTimeout(this.timer) on shutdown"
      pattern: "clearTimeout"
---

<objective>
Ship the autonomous round loop: a single in-process recursive-setTimeout-driven service that transitions Round through BETTING → RUNNING → CRASHED → SETTLED → BETTING without external triggers, with full `kill -9` recovery via DB state reconstruction. Plus the four use cases (startNewRound, transitionToRunning, crashRound, settleRound) that own the actual aggregate transitions.

Purpose: REQ-GAME-01 autonomous round loop. REQ-GAME-09 `kill -9` survives and resumes. REQ-FAIR-02 seed reveal gate (settle path is the only writer). REQ-FAIR-05 seedHash exposed during BETTING. ADR-014 enforcement: when a round crashes, active bets transition to LOST via a Bets-only sweep TX (NOT a multi-aggregate TX with Round).

Output: RoundLoopService + four use cases + client seed derivation helper + GameCoreModule provider extensions. The service starts running the moment AppModule finishes bootstrap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-game-core/04-RESEARCH.md
@CLAUDE.md
@.planning/phases/04-game-core/04-02-PLAN.md
@.planning/phases/04-game-core/04-04-PLAN.md
@.planning/phases/04-game-core/04-05-PLAN.md
@services/games/src/config/defaults.ts

<interfaces>
<!-- This plan IMPLEMENTS the round loop. Plan 04-08 (REST) and Phase 5 (saga) read state via the same repositories — no direct loop API. -->

Consumed from @crash/contracts/provably-fair (Plan 04-01):
- `deriveCrashPoint(input): number`
- `crashTimeMs(growthRate, crashPoint): number`
- `FORMULA_VERSION: number`
- `GENESIS_CLIENT_SEED: "genesis"`
- `multiplierAt(elapsedMs, growthRate): number` (used by Plan 04-08 GET /current; not by loop)

Consumed from Plan 04-02 (Round aggregate + RoundRepository + SeedChainRepository):
- `Round.schedule(id, nonce, seedHash, clientSeed, formulaVersion, bettingEndsAt, now): Round`
- `RoundRepository.findOpen() / saveScheduled / transitionFromBettingToRunning / transitionFromRunningToCrashed / transitionFromCrashedToSettled`
- `SeedChainRepository.findHashByNonce / findSeedByNonce`

Consumed from Plan 04-03 (Bet aggregate + BetRepository):
- `BetRepository.findActiveByRound(roundId): Promise<Bet[]>`
- `BetRepository.tryTransition(id, from, to, patch, txEm): Promise<Bet | null>`

Consumed from services/games/src/config/defaults.ts:
- `env.BETTING_WINDOW_MS` (default 5000)
- `env.COOLDOWN_MS` (default 2000)
- `env.GROWTH_RATE` (default 0.06)
- `env.INSTANT_CRASH_BUCKET` (default 101)

NestJS lifecycle (research §Pitfall 1 — MUST use OnApplicationBootstrap not OnModuleInit).

Hash helper: `import { createHash } from "node:crypto"` for SHA-256 of (prevRound.id + ':' + prevRound.crashedAt.toISOString()) to derive client seed (ADR-015). Genesis fallback uses `createHash("sha256").update(GENESIS_CLIENT_SEED).digest("hex")`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Use cases — startNewRound, transitionToRunning, crashRound, settleRound + client seed derivation</name>
  <files>services/games/src/application/use-cases/start-new-round.use-case.ts, services/games/src/application/use-cases/transition-to-running.use-case.ts, services/games/src/application/use-cases/crash-round.use-case.ts, services/games/src/application/use-cases/settle-round.use-case.ts, services/games/src/application/client-seed.derivation.ts, services/games/src/application/game-core.module.ts</files>
  <read_first>
    - services/games/src/domain/round.aggregate.ts (Round.schedule signature)
    - services/games/src/domain/round.repository.ts (transition methods + saveScheduled)
    - services/games/src/domain/seed-chain.repository.ts (findHashByNonce + findSeedByNonce)
    - services/games/src/domain/bet.repository.ts (findActiveByRound + tryTransition)
    - .planning/phases/04-game-core/04-RESEARCH.md "Open Questions > 2. Client seed derivation" (recommendation: SHA256(prevRound.id + ':' + prevRound.crashedAt.toISOString()); genesis = SHA256(GENESIS_CLIENT_SEED))
    - .planning/phases/04-game-core/04-RESEARCH.md "Code Examples > Round loop with OnApplicationBootstrap" (skeleton)
    - .planning/phases/04-game-core/04-05-PLAN.md (GameCoreModule providers — extend this module)
  </read_first>
  <behavior>
    - client-seed.derivation.ts exports pure function `deriveClientSeed(prevRound: { id: RoundId; crashedAt: Date } | null): string`. Genesis (prevRound === null): returns `createHash("sha256").update(GENESIS_CLIENT_SEED).digest("hex")`. Otherwise: `createHash("sha256").update(prevRound.id + ":" + prevRound.crashedAt.toISOString()).digest("hex")`. ADR-015 records the exact concatenation format
    - StartNewRoundUseCase: `@Injectable()`; injects ROUND_REPOSITORY, SEED_CHAIN_REPOSITORY; `async execute(now: Date): Promise<Round>`: finds the latest SETTLED round to derive client seed (or genesis); computes next nonce = lastNonce + 1n (or 0n if genesis); fetches `seedHash = await seedChain.findHashByNonce(nextNonce)` — throw if null (chain not bootstrapped); generates new RoundId via crypto.randomUUID(); computes bettingEndsAt = new Date(now.getTime() + env.BETTING_WINDOW_MS); constructs Round.schedule(id, nextNonce, seedHash, clientSeed, FORMULA_VERSION, bettingEndsAt, now); calls roundRepo.saveScheduled(round); returns round
    - TransitionToRunningUseCase: injects ROUND_REPOSITORY, SEED_CHAIN_REPOSITORY; `async execute(round: Round, now: Date): Promise<{ round: Round; crashPoint: CrashPoint; crashTimeMs: number }>`: fetches serverSeed = await seedChain.findSeedByNonce(round.nonce); computes `crashPointValue = deriveCrashPoint({serverSeed, clientSeed: round.clientSeed, nonce: round.nonce, instantCrashBucket: env.INSTANT_CRASH_BUCKET})`; constructs CrashPoint.of(crashPointValue); computes `crashAt = crashTimeMs(env.GROWTH_RATE, crashPointValue)`; PERSISTS only the status transition (status=RUNNING + started_at) via roundRepo.transitionFromBettingToRunning(round.id, now) — the crashPoint is intentionally NOT persisted yet (it's derived deterministically and only stored at CRASHED transition); returns { round: nextRound, crashPoint, crashTimeMs: crashAt }
    - CrashRoundUseCase: injects ROUND_REPOSITORY, BET_REPOSITORY; `async execute(round: Round, crashPoint: CrashPoint, now: Date): Promise<Round>`: persists via roundRepo.transitionFromRunningToCrashed(round.id, crashPoint, now); then SWEEPS active bets: `const activeBets = await betRepo.findActiveByRound(round.id);` then for each `await betRepo.tryTransition(bet.id, "ACTIVE", "LOST", {})` — this is a Bets-only sweep, NO Round mutation in the same TX (ADR-014). The sweep happens AFTER the Round transition commits — if kill -9 between Round=CRASHED and bet sweep, the recoverInFlightRound branch for CRASHED status re-runs the sweep (idempotent — only ACTIVE bets get touched; already-LOST bets remain). PENDING bets are left to the Phase 5 saga timeout to refund — Phase 4 does NOT settle PENDING bets
    - SettleRoundUseCase: injects ROUND_REPOSITORY, SEED_CHAIN_REPOSITORY; `async execute(round: Round, now: Date): Promise<Round>`: fetches `serverSeed = await seedChain.findSeedByNonce(round.nonce)` — throw if null; persists via roundRepo.transitionFromCrashedToSettled(round.id, serverSeed, now); calls `await seedChain.revealSeedAtNonce(round.nonce, serverSeed, now)` — sets revealed_at on the chain row (so the verify endpoint can show when the seed was revealed)
    - Update game-core.module.ts: add the four use cases + client-seed.derivation (if exported via a token, otherwise just import where needed) to GameCoreModule's providers + exports arrays
  </behavior>
  <action>Create each use case in its own file. Inject repositories via `@Inject(TOKEN)` from tokens.ts (Plan 04-04). The TransitionToRunningUseCase deliberately splits "compute crash point" from "persist crash point" — at RUNNING transition only the status changes; crashPoint is held in-memory by RoundLoopService and persisted at CRASHED transition. This matches the FSM CHECK (rounds_fsm_check) which requires crash_point_centi_x to be NULL in RUNNING state. The CrashRoundUseCase's bet sweep runs AFTER the Round transition commits — research §Pitfall 5 documents this exact recovery branch for "CRASHED but bets not yet swept" → recoverInFlightRound's CRASHED branch re-runs the sweep AND THEN settles. Use crypto.randomUUID() for RoundId generation (Bun-native). FORMULA_VERSION comes from `@crash/contracts/provably-fair`. The `seedChain.revealSeedAtNonce` call inside SettleRoundUseCase must be idempotent — repo's UPDATE WHERE seed IS NULL semantics handle this. Extend GameCoreModule providers + exports — Plan 04-05 created the module skeleton, this task adds the four use cases.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/services/games && bunx tsc --noEmit</automated>
  </verify>
  <done>
    - All four use case files compile
    - client-seed.derivation.ts exports `deriveClientSeed(prevRound | null)` pure function with the exact concatenation `id + ":" + crashedAt.toISOString()`
    - GameCoreModule providers array includes the four use cases
    - TransitionToRunningUseCase only persists status (crashPoint held in-memory, persisted on CRASHED) — verify by `grep "transitionFromBettingToRunning" services/games/src/application/use-cases/transition-to-running.use-case.ts` AND ensure no `crashPoint` reference is passed to that call
    - CrashRoundUseCase runs bet sweep AFTER round transition; no multi-aggregate TX
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: RoundLoopService with OnApplicationBootstrap + recursive setTimeout + kill-9 recovery</name>
  <files>services/games/src/application/round-loop.service.ts, services/games/src/application/game-core.module.ts</files>
  <read_first>
    - services/games/src/application/use-cases/*.ts (Task 1 outputs — RoundLoopService injects all four)
    - .planning/phases/04-game-core/04-RESEARCH.md "Code Examples > Round loop with OnApplicationBootstrap" (full skeleton)
    - .planning/phases/04-game-core/04-RESEARCH.md "Common Pitfalls > Pitfall 1, Pitfall 5" (OnApplicationBootstrap + every-status recovery branch)
    - .planning/phases/04-game-core/04-RESEARCH.md "Phase 4 Risks > R1" (single-process; pg_try_advisory_lock documented but not implemented)
    - services/games/src/application/tokens.ts (DI tokens)
  </read_first>
  <behavior>
    - RoundLoopService: `@Injectable()` class `implements OnApplicationBootstrap, OnApplicationShutdown`. Fields: `private readonly log = new Logger(RoundLoopService.name); private timer: NodeJS.Timeout | null = null; private running = false;`. Constructor injects ROUND_REPOSITORY (for findOpen + findById + listSettledHistory), BET_REPOSITORY (only for tests/integration; the use cases own bet sweeps), plus the four use cases (StartNewRound, TransitionToRunning, CrashRound, SettleRound)
    - `async onApplicationBootstrap(): Promise<void>`: sets `this.running = true`, awaits `await this.recoverInFlightRound()`. NEVER throws — wraps inner logic in try/catch that logs and re-schedules with a 1s backoff on unexpected error (so a transient DB blip doesn't permanently kill the loop)
    - `async onApplicationShutdown(signal?: string): Promise<void>`: logs `"Round loop shutting down on ${signal}"`; sets `this.running = false`; `if (this.timer) clearTimeout(this.timer); this.timer = null;`
    - `private async recoverInFlightRound(): Promise<void>`: `const open = await this.rounds.findOpen()`; if open is null → `await this.startNewRound(new Date())`; else switch on open.status: BETTING → if `open.bettingEndsAt.getTime() < Date.now()` await transitionToRunning(open) else scheduleAt(open.bettingEndsAt.getTime() - Date.now(), () => transitionToRunning(open)); RUNNING → recompute crashPoint via deriveCrashPoint (fetch serverSeed via seedChain.findSeedByNonce) AND crashTimeMs; const elapsed = Date.now() - open.startedAt.getTime(); if (elapsed >= crashTimeMs) await crashRound(open, crashPoint); else scheduleAt(crashTimeMs - elapsed, () => crashRound(open, crashPoint)); CRASHED → await settleRound(open) (the CRASHED branch re-runs the bet sweep idempotently via tryTransition before settling); SETTLED → await startNewRound (this is a no-op-from-state — the latest SETTLED is treated as "ready to start next")
    - Private helpers (each is a thin wrapper that calls the corresponding use case THEN schedules the next setTimeout to drive the next transition):
      - `private async startNewRound(now: Date): Promise<void>`: calls startNewRoundUseCase.execute(now) → result is a BETTING round; schedules `setTimeout(env.BETTING_WINDOW_MS, () => transitionToRunning(round))`
      - `private async transitionToRunning(round: Round): Promise<void>`: calls transitionToRunningUseCase.execute(round, new Date()) → result is {round: runningRound, crashPoint, crashTimeMs}; schedules `setTimeout(crashTimeMs, () => crashRound(runningRound, crashPoint))`
      - `private async crashRound(round: Round, crashPoint: CrashPoint): Promise<void>`: calls crashRoundUseCase.execute(round, crashPoint, new Date()) → result is crashed round; schedules `setTimeout(0, () => settleRound(crashedRound))` (immediate settle — no delay between crash and settle in Phase 4; Phase 6 WS gateway will add a short broadcast delay if needed)
      - `private async settleRound(round: Round): Promise<void>`: calls settleRoundUseCase.execute(round, new Date()) → result is settled round; schedules `setTimeout(env.COOLDOWN_MS, () => startNewRound(new Date()))`
    - `private scheduleAt(ms: number, fn: () => Promise<void>): void`: if `!this.running` return; this.timer = setTimeout(() => { fn().catch((e) => { this.log.error(e); this.scheduleAt(1000, fn); }); }, Math.max(0, ms))
    - Register RoundLoopService in GameCoreModule providers (do NOT export — it's an internal driver, not consumed by other modules)
  </behavior>
  <action>Create round-loop.service.ts using the research §Code Examples skeleton as the structural template. CRITICAL: `implements OnApplicationBootstrap` (NOT OnModuleInit) — research §Pitfall 1. Every recovery branch in the switch statement must handle the corresponding status — research §Pitfall 5 lists all five (BETTING, RUNNING, CRASHED, SETTLED, no-round). The CRASHED branch deliberately re-enters settleRound which calls the use case — the use case's tryTransition is idempotent on already-LOST bets (the SQL UPDATE WHERE status='ACTIVE' returns 0 rows for non-active bets), so the sweep is safe to re-run. The crashRound helper handles the case where the timer fires while running===false (process is shutting down): the `scheduleAt` guard at the top of every chained setTimeout catches it. Use cases inject via constructor (NOT via @Inject — use direct class names since they're @Injectable classes registered in the same module). The error-recovery 1s-backoff branch in onApplicationBootstrap and scheduleAt prevents a transient DB error from killing the loop permanently — log and retry. Register the service in GameCoreModule providers but NOT exports (loop driver is private). DO NOT add the controller wiring here — Plan 04-08 owns REST.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/services/games && bunx tsc --noEmit</automated>
  </verify>
  <done>
    - round-loop.service.ts compiles
    - `grep -c "OnApplicationBootstrap" services/games/src/application/round-loop.service.ts` >= 1
    - `grep -c "OnModuleInit" services/games/src/application/round-loop.service.ts` returns 0
    - `grep -c "setTimeout" services/games/src/application/round-loop.service.ts` >= 1 (at least the scheduleAt private helper)
    - `grep -c "clearTimeout" services/games/src/application/round-loop.service.ts` >= 1 (shutdown)
    - All five recovery branches present (BETTING, RUNNING, CRASHED, SETTLED, no-open) — verify by counting `case` in the switch
    - GameCoreModule providers includes RoundLoopService (not exported)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Round loop → DB | Internal driver; no external input flows into the loop |
| Round loop → seed chain | Reads serverSeed pre-RUNNING for crash-point computation; never exposes the seed before SETTLED transition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | Crash-point manipulation via setTimeout drift | mitigate | crashTimeMs precomputed at transitionToRunning from formula; setTimeout drift is bounded; Phase 6 WS adds server-authoritative tick |
| T-04-06-02 | Information Disclosure | Server seed leak before settle | mitigate | TransitionToRunningUseCase consults seedChain.findSeedByNonce internally but does NOT expose; SettleRoundUseCase is the only path that writes round.server_seed (REQ-FAIR-02) |
| T-04-06-03 | Denial of Service | Transient DB error kills loop permanently | mitigate | 1s backoff retry in scheduleAt error handler; OnApplicationBootstrap wrap-and-log pattern |
| T-04-06-04 | Tampering | Multi-instance double-tick (operator deploys two replicas) | accept | Research §Risk R1 — single-process per ADR-017; scale-out path via pg_try_advisory_lock documented but not implemented in v1 |
| T-04-06-05 | Repudiation | kill -9 mid-transition leaves inconsistent state | mitigate | Five-branch recoverInFlightRound covers every non-terminal state; idempotent bet sweep on CRASHED re-entry; research §Pitfall 5 |
</threat_model>

<verification>
- `bunx tsc --noEmit` clean
- RoundLoopService uses OnApplicationBootstrap + OnApplicationShutdown
- Five recovery branches in recoverInFlightRound
- Four use cases compile and are registered in GameCoreModule providers
- client-seed derivation uses exact concatenation format from ADR-015
</verification>

<success_criteria>
- REQ-GAME-01 autonomous round loop landed (BETTING → RUNNING → CRASHED → SETTLED → BETTING)
- REQ-GAME-09 kill -9 recovery via every-status recoverInFlightRound branch
- REQ-FAIR-02 seed reveal gate (only Settle path writes server_seed)
- REQ-FAIR-05 seedHash exposed during BETTING (Round.schedule sets it)
- REQ-DOM-01 FSM enforced (loop uses aggregate methods + repository atomic transitions)
- ADR-014 enforced (bet sweep is Bets-only TX, never multi-aggregate)
- ADR-017 enforced (recursive setTimeout + OnApplicationBootstrap)
</success_criteria>

<output>
Create `.planning/phases/04-game-core/04-06-SUMMARY.md` with: use case method signatures, recovery branch coverage matrix (every status → recovery action), 1s-backoff error path, ADR-015 client-seed concatenation format committed verbatim.
</output>
