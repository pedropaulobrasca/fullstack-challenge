---
phase: 04-game-core
plan: 09
type: execute
wave: 5
depends_on:
  - 04-08
files_modified:
  - docker/kong/kong.yml
autonomous: true
requirements:
  - REQ-GAME-02
  - REQ-GAME-03
  - REQ-GAME-04
  - REQ-GAME-05

must_haves:
  truths:
    - "Kong exposes exactly four games endpoints: GET ~/games/rounds/current$, GET ~/games/rounds/history$, GET ~/games/rounds/[^/]+/verify$, GET ~/games/bets/me$"
    - "Every other path under /games/* returns Kong-origin 404 (e.g., POST /games/bet, POST /games/bet/cashout, GET /games/admin)"
    - "All four allowed routes are method-restricted to GET — POST/PUT/DELETE/PATCH on any of them returns Kong-origin 404"
    - "Mirrors the Phase 3 P3.07 wallets pattern (PCRE-anchored regex paths + explicit methods array) for narrowing discipline"
  artifacts:
    - path: "docker/kong/kong.yml"
      provides: "Updated games-service block with four named, method-constrained, regex-anchored routes"
  key_links:
    - from: "docker/kong/kong.yml games-service block"
      to: "services/games/src/presentation/controllers/{rounds,bets}.controller.ts"
      via: "route paths match controller route prefixes + method whitelist"
      pattern: "~/games/rounds"
---

<objective>
Replace the existing `games-routes` Kong block (currently a wide-open `/games` prefix) with four narrowly-scoped, PCRE-anchored, method-restricted routes — mirroring the Phase 3 P3.07 wallets narrowing pattern. Only the four READ endpoints shipped in Plan 04-08 are reachable through Kong; all mutation paths (including the future POST /games/bet from Phase 5) return Kong-origin 404 until Phase 5's Kong update opens them.

Purpose: Defense-in-depth at the gateway. Phase 5 may inadvertently expose a controller before its saga is ready; the Kong narrow scope means that even if Phase 5 wires a POST route without ALSO updating Kong, the route is unreachable from the public side. This is the same discipline Phase 3 P3.07 applied to wallets.

Output: Updated `docker/kong/kong.yml` with four named, regex-anchored, method-whitelisted routes. The change applies on `bun run docker:up` (Kong DB-less reloads on container restart). Smoke probes (Plan 04-11) verify the narrowing live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-game-core/04-RESEARCH.md
@CLAUDE.md
@.planning/phases/04-game-core/04-08-PLAN.md
@docker/kong/kong.yml

<interfaces>
<!-- Current games-service block in docker/kong/kong.yml (verbatim from the file): -->

```yaml
- name: games-service
  url: http://games:4001
  routes:
    - name: games-routes
      paths:
        - /games
```

<!-- Replace with the four-route pattern from Phase 3 wallets P3.07 (PCRE-anchored + methods array). Phase 3 wallets reference for the regex-path + methods pattern: -->

```yaml
- name: wallets-service
  url: http://wallets:4002
  routes:
    - name: wallets-provision
      paths: ["~/wallets$"]
      methods: [POST]
    - name: wallets-me
      paths: ["~/wallets/me$"]
      methods: [GET]
```

<!-- Phase 4 four READ routes: -->
- games-current: ~/games/rounds/current$, methods [GET]
- games-history: ~/games/rounds/history$, methods [GET]
- games-verify: ~/games/rounds/[^/]+/verify$, methods [GET]
- games-bets-me: ~/games/bets/me$, methods [GET]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace games-routes with four narrow routes in kong.yml</name>
  <files>docker/kong/kong.yml</files>
  <read_first>
    - docker/kong/kong.yml (current full content — locate the existing games-service block, study the wallets-service block immediately above/below it for the narrowing pattern)
    - .planning/phases/04-game-core/04-08-PLAN.md (the four endpoints shipped + their Nest controller route paths)
    - Phase 3 P3.07 commit history mention in STATE.md (commits `ae853d0` initial narrowing + `fdd5ec5` PCRE-anchor switch)
  </read_first>
  <behavior>
    - Replace the existing games-service block's `routes` array with four route entries: (1) games-current — paths `["~/games/rounds/current$"]`, methods `[GET]`; (2) games-history — paths `["~/games/rounds/history$"]`, methods `[GET]` (route accepts query string ?limit=&offset=); (3) games-verify — paths `["~/games/rounds/[^/]+/verify$"]`, methods `[GET]` (the `[^/]+` pattern matches a single path segment for roundId, anchored before `/verify$`); (4) games-bets-me — paths `["~/games/bets/me$"]`, methods `[GET]`
    - Keep the existing `name: games-service` + `url: http://games:4001` (service definition unchanged — only routes change)
    - Route names use the `games-` prefix (matches the `wallets-` prefix pattern from P3.07 — admin API introspection will show all routes namespaced by service)
    - PCRE anchors: every path starts with `~` (regex prefix) and ends with `$` (end-of-path anchor). Without `$`, Kong would match prefix-only (e.g., `/games/rounds/current/foo` would also match) — research P3.07 deviation `fdd5ec5` documents this exact failure mode
    - Do NOT add any POST routes — Phase 5 owns POST /games/bet + POST /games/bet/cashout AND the corresponding Kong route additions
  </behavior>
  <action>Open docker/kong/kong.yml and locate the `name: games-service` block. Replace its `routes:` list with four entries per the <interfaces> spec. Each route has three keys: name, paths (array with one PCRE-anchored regex), methods (array with single uppercase HTTP method). Preserve YAML indentation and structure (study wallets-service block for the exact column alignment). Do NOT touch the wallets-service block. Do NOT add any logging/auth/rate-limit plugins — bare narrowing. Verify the regex `~/games/rounds/[^/]+/verify$` matches `/games/rounds/abc-123-uuid/verify` but NOT `/games/rounds/abc/verify/extra`. The `[^/]+` segment captures any non-slash sequence — Kong's PCRE engine evaluates this server-side per Plan 04-08 controller's @Param("roundId").</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge && grep -c "name: games-" docker/kong/kong.yml</automated>
  </verify>
  <done>
    - `grep -c "name: games-" docker/kong/kong.yml` returns >= 5 (the service block `name: games-service` + four routes `games-current`, `games-history`, `games-verify`, `games-bets-me`)
    - `grep -c "~/games/" docker/kong/kong.yml` returns >= 4
    - `grep -c "\\$\"" docker/kong/kong.yml` returns >= 6 (four games routes plus two wallets routes already present)
    - No POST/PUT/DELETE/PATCH methods declared on any games route
    - Live verification deferred to Plan 04-11 smoke probes (requires docker:up)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Internet → Kong | Untrusted; narrow allowlist of method+path pairs |
| Kong → games upstream | Trusted (broker-level boundary per ARCHITECTURE) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09-01 | Tampering | Future controller exposes admin route accidentally | mitigate | Kong default-deny — any new path requires explicit route entry |
| T-04-09-02 | Tampering | HTTP method override (e.g., POST tunneled via X-Method-Override) | accept | Kong validates HTTP method natively; no method-override plugin loaded |
| T-04-09-03 | Information Disclosure | Round verify URL enumeration via brute-force UUID guessing | accept | Round IDs are UUID v4 (128 bits); brute force is computationally infeasible; future v2 may add rate limiting (REQ-STRETCH-07) |
| T-04-09-04 | Spoofing | Bypass JwtGuard by hitting /games/bets/me directly on the games container | accept | Internal container network only; production deployment is out of scope (research §Out-of-scope k8s/mTLS) |
</threat_model>

<verification>
- grep counts confirm four games- routes with regex-anchored paths and GET methods
- Live verification via Plan 04-11 smoke probes (post-docker:up)
</verification>

<success_criteria>
- Kong exposes exactly four games READ endpoints
- All other paths under /games return Kong 404
- Method whitelist enforces GET-only per endpoint
- Defense-in-depth pattern from P3.07 applied consistently
</success_criteria>

<output>
Create `.planning/phases/04-game-core/04-09-SUMMARY.md` with: the four route names, their PCRE paths verbatim, methods array, defer note for Plan 04-11 live verification.
</output>
