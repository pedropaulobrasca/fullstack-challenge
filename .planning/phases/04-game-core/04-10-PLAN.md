---
phase: 04-game-core
plan: 10
type: execute
wave: 6
depends_on:
  - 04-04
  - 04-05
  - 04-06
  - 04-08
files_modified:
  - services/games/tests/integration/_helpers/test-env.ts
  - services/games/tests/integration/_helpers/app-factory.ts
  - services/games/tests/integration/seed-chain-bootstrap.test.ts
  - services/games/tests/integration/round-loop-autonomous.test.ts
  - services/games/tests/integration/kill-9-recovery.test.ts
  - services/games/tests/integration/bet-uniqueness.test.ts
  - services/games/tests/integration/verify-round.test.ts
  - services/games/tests/integration/get-current-round.test.ts
  - services/games/tests/integration/get-round-history.test.ts
  - services/games/tests/integration/get-player-bets.test.ts
autonomous: true
requirements:
  - REQ-GAME-01
  - REQ-GAME-02
  - REQ-GAME-03
  - REQ-GAME-04
  - REQ-GAME-05
  - REQ-GAME-09
  - REQ-FAIR-01
  - REQ-FAIR-02
  - REQ-FAIR-04
  - REQ-DOM-02
  - REQ-TEST-01

must_haves:
  truths:
    - "seed-chain-bootstrap test: cold start with empty seed_chain populates HASH_CHAIN_LENGTH rows; second cold start with rows present is a no-op (idempotent)"
    - "round-loop-autonomous test: starting a games app instance, waiting BETTING_WINDOW_MS + small slack, then asserting a round exists in DB with status RUNNING; waiting until crashTimeMs and asserting status CRASHED then SETTLED"
    - "kill-9-recovery test: start the round loop, wait until status RUNNING is observed, then forcibly destroy the Nest app, restart, and assert the round resumes (RUNNING with future crashTime → reschedule; RUNNING with past crashTime → retroactive crash; CRASHED → settle)"
    - "bet-uniqueness test: insert two concurrent PENDING bets for the same (player_id, round_id) → second INSERT fails with SQLSTATE 23505 from bets_one_active_per_player partial unique index"
    - "verify-round test: SETTLED round returns matches=true with recomputed === stored crashPoint; unsettled round returns 400 ROUND_NOT_YET_SETTLED; non-existent round returns 404"
    - "get-current-round test: live round returns seedHash and serverSeed=null pre-settle; settled latest returns serverSeed populated"
    - "get-round-history test: ?limit=5 returns 5 most recent SETTLED rounds"
    - "get-player-bets test: JwtGuard rejects missing-bearer with 401; valid JWT returns only the authenticated player's bets"
  artifacts:
    - path: "services/games/tests/integration/_helpers/test-env.ts"
      provides: "process.env seeding for INTEGRATION mode + dynamic import sequence"
    - path: "services/games/tests/integration/_helpers/app-factory.ts"
      provides: "createTestGamesApp() that boots AppModule with HASH_CHAIN_LENGTH=100 (fast for integration tests, not 1M) + isolated DB schema"
    - path: "services/games/tests/integration/round-loop-autonomous.test.ts"
      provides: "Full lifecycle test using HASH_CHAIN_LENGTH=10, BETTING_WINDOW_MS=200, COOLDOWN_MS=100 for fast iteration"
    - path: "services/games/tests/integration/kill-9-recovery.test.ts"
      provides: "Recovery test that simulates kill -9 by close()-ing the app, then creating a fresh app and asserting state continuity"
  key_links:
    - from: "services/games/tests/integration/*.test.ts"
      to: "services/games/src/app.module.ts"
      via: "Test module via @nestjs/testing or direct AppModule boot"
      pattern: "AppModule"
    - from: "tests/integration/_helpers/test-env.ts"
      to: "Phase 3 wallets pattern"
      via: "top-of-file process.env.X ??= ... + dynamic import"
      pattern: "process.env"
---

<objective>
Ship eight integration tests via testcontainers + Bun test runner covering the full Phase 4 surface: seed-chain bootstrap idempotency, autonomous round loop, kill-9 recovery, DB-level double-bet block, verify endpoint determinism, current-round seedHash exposure, round history pagination, and JWT-gated player bets.

Purpose: REQ-TEST-01 unit + integration test coverage. REQ-GAME-09 kill -9 recovery is the headline test — exactly the property a recruiter will inspect during arguição. REQ-DOM-02 partial unique index proven at the DB layer with two concurrent INSERTs.

Output: Eight integration test files + two helpers. All run via `cd services/games && INTEGRATION=1 bun test tests/integration` against a live docker stack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-game-core/04-RESEARCH.md
@CLAUDE.md
@.planning/phases/03-wallet-service/VERIFICATION.md
@services/wallets/tests/integration/provision-wallet.test.ts

<interfaces>
<!-- These tests EXERCISE the Phase 4 surface end-to-end. They depend on a live Postgres (RabbitMQ not strictly required since Phase 4 has no AMQP publish) — Bun + docker:up brings up the stack. -->

Test environment knobs (small values for fast integration runs):
- HASH_CHAIN_LENGTH=10 (not 1M — keeps bootstrap < 100ms)
- BETTING_WINDOW_MS=200 (small window)
- COOLDOWN_MS=100
- GROWTH_RATE=0.06 (production value — crash time depends on this)

Pattern reused from Phase 3 (services/wallets/tests/integration):
- top-of-file process.env.X ??= ... bootstrap via _helpers/test-env.ts (avoids the 35-lint-error carry-forward via single shared helper)
- @nestjs/testing Test.createTestingModule({imports: [AppModule]}).compile() OR direct AppModule boot
- Per-test DB schema isolation: each test uses a random schema name or truncates tables in afterEach
- INTEGRATION=1 env guard at top of every integration file: `if (process.env.INTEGRATION !== "1") { console.log("Skipping integration"); process.exit(0); }` (matches Phase 3 pattern)

Documented workspace quirk (Phase 3 P3.09 + research §Pitfall 8):
- Tests run via `cd services/games && INTEGRATION=1 bun test tests/integration`, NOT from repo root
- Document this in services/games/README.md if not already

Authentication for /games/bets/me test:
- Use a real Keycloak token via the smoke-health.sh probe_wallets_keycloak_token helper (extract the token-fetch into _helpers/test-env.ts)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Integration test helpers (test-env + app-factory) + seed-chain + round-loop-autonomous + kill-9-recovery + bet-uniqueness tests</name>
  <files>services/games/tests/integration/_helpers/test-env.ts, services/games/tests/integration/_helpers/app-factory.ts, services/games/tests/integration/seed-chain-bootstrap.test.ts, services/games/tests/integration/round-loop-autonomous.test.ts, services/games/tests/integration/kill-9-recovery.test.ts, services/games/tests/integration/bet-uniqueness.test.ts</files>
  <read_first>
    - services/wallets/tests/integration/provision-wallet.test.ts (Phase 3 integration test pattern — INTEGRATION=1 guard + Test.createTestingModule + DB cleanup)
    - services/wallets/tests/integration/inbox-replay.test.ts (multi-step state assertion pattern)
    - .planning/phases/04-game-core/04-RESEARCH.md "Common Pitfalls > Pitfall 8" (workspace integration test quirk)
    - .planning/phases/04-game-core/04-RESEARCH.md "Wave 0 Gaps" (full list of test files this plan creates)
    - services/games/src/application/round-loop.service.ts (Plan 04-06 — observable behavior: emits status transitions persisting to DB)
    - services/games/src/infrastructure/mikro-orm/migrations/20260526003-create-bets.ts (Plan 04-04 — the partial unique index name `bets_one_active_per_player` is the constraint to assert on SQLSTATE 23505)
  </read_first>
  <behavior>
    - _helpers/test-env.ts: exports `setupIntegrationEnv(overrides?: Record<string, string>)` — top-of-file seeds `process.env.NODE_ENV ??= "test"`, `process.env.DATABASE_URL ??= "postgres://postgres:postgres@localhost:5432/games"`, `process.env.RABBITMQ_URL ??= "amqp://guest:guest@localhost:5672"`, `process.env.HASH_CHAIN_LENGTH ??= "10"`, `process.env.BETTING_WINDOW_MS ??= "200"`, `process.env.COOLDOWN_MS ??= "100"`, and the three KEYCLOAK_* keys. Applies overrides AFTER defaults. Also exports `fetchPlayerToken()` that POSTs to Keycloak token endpoint with player/player123 and returns the access_token (mirrors smoke-health.sh probe_wallets_keycloak_token logic)
    - _helpers/app-factory.ts: exports `async function createTestGamesApp(): Promise<INestApplication>` that calls Test.createTestingModule({imports: [AppModule]}).compile() then app.init() and returns the instance. Caller is responsible for app.close() in afterAll. Helper `truncateGamesTables(app)` that runs `TRUNCATE rounds, bets, seed_chain RESTART IDENTITY CASCADE` via the app's EntityManager
    - seed-chain-bootstrap.test.ts: INTEGRATION=1 guard; beforeAll → setupIntegrationEnv + create app + truncate; test 1: assert seed_chain has exactly HASH_CHAIN_LENGTH rows after first boot; test 2: close app, recreate app with same env, assert seed_chain row count UNCHANGED (idempotency, REQ-FAIR-01)
    - round-loop-autonomous.test.ts: INTEGRATION=1 guard; setupIntegrationEnv({BETTING_WINDOW_MS: "200", COOLDOWN_MS: "100", HASH_CHAIN_LENGTH: "20"}); create app; wait 50ms then assert a BETTING round exists (`SELECT * FROM rounds WHERE status = 'BETTING'` returns 1 row with seedHash populated, server_seed NULL); wait BETTING_WINDOW_MS + 100ms; assert the same round transitioned to RUNNING (started_at IS NOT NULL); poll every 50ms up to crashTimeMs upper bound + 500ms for status='CRASHED' (then SETTLED). Verify server_seed populated only in SETTLED. Then assert a NEW BETTING round appears within COOLDOWN_MS + slack (REQ-GAME-01 — autonomous transition cycle)
    - kill-9-recovery.test.ts: INTEGRATION=1 guard; setupIntegrationEnv with small windows; create app, wait for status=RUNNING via polling; CAPTURE round.id, round.started_at, round.crash_point_centi_x via raw query (these will not be in DB yet — RUNNING has crash_point_centi_x=NULL by FSM CHECK; instead compute the expected crash_point via deriveCrashPoint reading seed_chain.seed at round.nonce; that's the recovery story). Close the app forcibly (`await app.close()`). Wait 500ms. Create a fresh app — assert the same round.id is now in CRASHED or SETTLED status (recovery branch ran). For the more rigorous variant: choose BETTING_WINDOW_MS small, GROWTH_RATE high so crashTime is > 1s, close app at 500ms into RUNNING, restart 200ms later, assert reschedule honored (round.crashed_at within expected window). REQ-GAME-09
    - bet-uniqueness.test.ts: INTEGRATION=1 guard; create app; wait for a BETTING round; via raw EntityManager INSERT INTO bets directly (bypass aggregate to test the DB constraint), insert one PENDING bet for (playerA, roundA); then attempt to insert ANOTHER PENDING bet for (playerA, roundA) → expect Postgres throws with SQLSTATE 23505 (catch via try/catch — check `err.code === "23505"` AND `err.constraint === "bets_one_active_per_player"`). Insert a third bet for a DIFFERENT player on same round → succeeds. Update the first bet to REFUNDED, then insert a new PENDING bet for (playerA, roundA) → succeeds (partial index permits — research §Pitfall 4 confirms). REQ-DOM-02 DB-layer proof
  </behavior>
  <action>Create _helpers/ FIRST (both files). setupIntegrationEnv uses `process.env.X ??= "default"` pattern so overrides via the function argument can be applied AFTER the defaults (assign with `=` for overrides). Helper functions live in _helpers/ to avoid the per-file process.env pollution from the Phase 3 W6 deviation. Every test file starts with: `import { setupIntegrationEnv } from "./_helpers/test-env"; setupIntegrationEnv(); if (process.env.INTEGRATION !== "1") { console.log("Skipping — set INTEGRATION=1"); process.exit(0); }` BEFORE any import of AppModule/contracts. The round-loop-autonomous test uses small env values (HASH_CHAIN_LENGTH=20, BETTING_WINDOW_MS=200) so the full lifecycle completes in seconds; do NOT use the production 1M chain. The kill-9 test uses `app.close()` as the kill simulation — true SIGKILL via process.kill cannot be tested in-process; documenting in the test header comment that "kill -9 is simulated by app.close() which exits all lifecycle hooks; the real-process kill-9 would skip OnApplicationShutdown, making recovery harder; this test asserts recovery from the easier case — true SIGKILL recovery is tested manually via the Plan 04-11 smoke checkpoint." For bet-uniqueness, BYPASS the Bet aggregate's status guard by raw INSERT — the test verifies the DB constraint independent of application code (defense in depth). The third sub-test (REFUNDED then re-insert PENDING) directly verifies research §Pitfall 4's "partial index doesn't cover REFUNDED" semantic.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/services/games && bunx tsc --noEmit tests/integration/_helpers/test-env.ts tests/integration/_helpers/app-factory.ts tests/integration/seed-chain-bootstrap.test.ts tests/integration/round-loop-autonomous.test.ts tests/integration/kill-9-recovery.test.ts tests/integration/bet-uniqueness.test.ts</automated>
  </verify>
  <done>
    - All six files compile under tsc --noEmit
    - Each test file contains the INTEGRATION=1 guard
    - _helpers/test-env.ts seeds at least 10 env keys (DATABASE_URL, RABBITMQ_URL, all KEYCLOAK_*, BETTING_WINDOW_MS, COOLDOWN_MS, HASH_CHAIN_LENGTH, GROWTH_RATE, CURRENCY_CODE, NODE_ENV)
    - bet-uniqueness.test.ts asserts `err.code === "23505"` AND constraint name in catch block
    - kill-9-recovery.test.ts header comment documents the SIGKILL simulation caveat
    - Live execution deferred to Plan 04-11 (requires docker:up). The Bash command verifies the test SOURCE compiles, not that tests pass — passing requires docker:up which the executor will run during checkpoint
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: verify-round + get-current-round + get-round-history + get-player-bets integration tests</name>
  <files>services/games/tests/integration/verify-round.test.ts, services/games/tests/integration/get-current-round.test.ts, services/games/tests/integration/get-round-history.test.ts, services/games/tests/integration/get-player-bets.test.ts</files>
  <read_first>
    - services/games/tests/integration/_helpers/test-env.ts (Task 1 output — fetchPlayerToken helper for the bets/me test)
    - services/games/tests/integration/_helpers/app-factory.ts (createTestGamesApp helper)
    - .planning/phases/04-game-core/04-08-PLAN.md (the four endpoint contracts)
    - services/wallets/tests/integration/jwt-guard.test.ts (Phase 3 integration auth pattern — extracts a real Keycloak token via the password grant)
  </read_first>
  <behavior>
    - verify-round.test.ts: create app via createTestGamesApp (HASH_CHAIN_LENGTH small, fast loop); wait until at least one round transitions to SETTLED via polling; GET /games/rounds/{roundId}/verify against the SETTLED round via `app.getHttpServer()` + `supertest` OR via direct fetch on the app's listening port. Assert: response.matches === true, recomputedCrashPoint === stored crashPoint, formulaVersion === 1, serverSeed is a 64-hex string, previousServerSeed is either null (genesis) or 64-hex. Second sub-test: GET /games/rounds/{roundId}/verify against a non-SETTLED round returns 400 with code ROUND_NOT_YET_SETTLED (REQ-FAIR-02 gate). Third sub-test: GET /games/rounds/00000000-0000-0000-0000-000000000000/verify returns 404 ROUND_NOT_FOUND
    - get-current-round.test.ts: create app; wait for a BETTING round; GET /games/rounds/current. Assert: status === BETTING, seedHash is non-empty 64-hex (REQ-FAIR-05 commitment), serverSeed === null, crashPoint === null, currentMultiplier === null, bets array is empty. Wait until status transitions to RUNNING; GET /games/rounds/current again; assert currentMultiplier is a number >= 1.0 (REQ-GAME-02). Wait until SETTLED; assert that GET /current now returns the NEXT BETTING round (NOT the SETTLED one — the endpoint always returns the open/in-flight round)
    - get-round-history.test.ts: create app with very small windows; wait until at least 5 rounds have SETTLED (poll on `SELECT COUNT(*) FROM rounds WHERE status='SETTLED'`); GET /games/rounds/history?limit=3 → assert rounds.length === 3, each entry has crashPoint > 0 and settledAt populated. GET /games/rounds/history?limit=200 → asserts limit clamped to 100 at use-case layer (response.limit === 100). GET /games/rounds/history → defaults to limit=20
    - get-player-bets.test.ts: create app; fetch player token via fetchPlayerToken() helper; GET /games/bets/me with `Authorization: Bearer <token>` → assert 200 with bets array (initially empty since Phase 4 has no POST /bet — the test verifies the endpoint shape + JWT acceptance, NOT bet placement). Second sub-test: GET /games/bets/me without Authorization → 401 MISSING_BEARER_TOKEN. Third sub-test: GET /games/bets/me with invalid token → 401 INVALID_TOKEN. Then directly INSERT a bet row for the authenticated playerId via raw SQL; GET /games/bets/me again → asserts the bet appears in the response
  </behavior>
  <action>Use Bun's native `fetch` against `app.getUrl()` (or `app.listen(0); const port = (app.getHttpServer().address() as AddressInfo).port` if getUrl unavailable). Avoid supertest if Phase 3 wallets didn't use it — match the established pattern. For verify-round, poll with 50ms intervals up to 5s for SETTLED transition (small env knobs make this fast). For get-round-history, the test deliberately runs longer (~3-5s) to accumulate 5+ SETTLED rounds — annotate the test description with the expected runtime so executors don't think it's hung. For get-player-bets, the bet INSERT in the third sub-test bypasses the (non-existent in Phase 4) POST /bet by going directly to the bets table — explicitly note this in a comment "Phase 4 has no POST /bet; bypassing for test setup; Phase 5 will replace this insert with a real saga-driven flow". The JwtGuard error codes (`MISSING_BEARER_TOKEN`, `INVALID_TOKEN`) match Plan 04-07's guard. Each test file ends with afterAll: app.close() + truncateGamesTables (truncate AFTER close not strictly necessary if next test uses fresh schema; pick one strategy and document).</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/services/games && bunx tsc --noEmit tests/integration/verify-round.test.ts tests/integration/get-current-round.test.ts tests/integration/get-round-history.test.ts tests/integration/get-player-bets.test.ts</automated>
  </verify>
  <done>
    - All four files compile
    - verify-round.test.ts covers all three sub-cases (settled match, unsettled 400, missing 404)
    - get-current-round.test.ts asserts seedHash exposure during BETTING (REQ-FAIR-05) and serverSeed null pre-SETTLED (REQ-FAIR-02)
    - get-player-bets.test.ts uses real Keycloak token via fetchPlayerToken
    - Live execution deferred to Plan 04-11
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test → live stack | Tests run against docker:up; no production data; ephemeral DB schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10-01 | Repudiation | Test passes silently in non-INTEGRATION mode | mitigate | Every file starts with `if (process.env.INTEGRATION !== "1") { console.log("Skipping"); process.exit(0); }` — visible skip signal |
| T-04-10-02 | Tampering | Test pollutes DB causing flaky downstream tests | mitigate | truncate in afterEach/afterAll; or random schema name per test |
| T-04-10-03 | Information Disclosure | Test logs leak Keycloak password | accept | Player password is `player123` from a public dev realm; documented in REQ-AUTH-05 |
</threat_model>

<verification>
- All eight integration test files compile under tsc --noEmit
- INTEGRATION=1 guard present in every file
- _helpers/test-env.ts seeds full env
- Live run deferred to Plan 04-11 (post-docker:up checkpoint)
</verification>

<success_criteria>
- Eight integration tests cover Phase 4 surface end-to-end
- REQ-GAME-09 kill-9 recovery test ships (with documented SIGKILL simulation caveat)
- REQ-DOM-02 DB-level double-bet block proven by SQLSTATE 23505 assertion
- REQ-FAIR-02 verify endpoint gate proven (settled-only)
- All four READ endpoints exercised
- Compilation clean; live execution gated to Plan 04-11
</success_criteria>

<output>
Create `.planning/phases/04-game-core/04-10-SUMMARY.md` with: test file list, REQ-ID coverage matrix, SIGKILL simulation caveat note, docker:up dependency for execution.
</output>
