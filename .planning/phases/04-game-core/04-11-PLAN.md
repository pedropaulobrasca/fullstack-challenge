---
phase: 04-game-core
plan: 11
type: execute
wave: 7
depends_on:
  - 04-09
  - 04-10
files_modified:
  - scripts/smoke-health.sh
autonomous: false
requirements:
  - REQ-GAME-01
  - REQ-GAME-02
  - REQ-GAME-04
  - REQ-FAIR-01
  - REQ-FAIR-05

must_haves:
  truths:
    - "Smoke probe 27: GET http://localhost:8000/games/rounds/current returns 200 with status in {BETTING,RUNNING,CRASHED,SETTLED} and a non-empty seedHash"
    - "Smoke probe 28: GET http://localhost:8000/games/rounds/history?limit=5 returns 200 with rounds array (may be empty initially)"
    - "Smoke probe 29: GET http://localhost:8000/games/bets/me without Authorization returns Kong-origin 401 (JwtGuard fires)"
    - "Smoke probe 30: GET http://localhost:8000/games/bets/me with valid player token returns 200 with bets array"
    - "Smoke probe 31: Postgres seed_chain table has HASH_CHAIN_LENGTH rows after docker:up (REQ-FAIR-01)"
    - "Smoke probe 32: POST http://localhost:8000/games/bet returns Kong-origin 404 (Phase 5 not yet wired, narrowing holds at gateway)"
    - "All probes 1-32 PASS on a fresh docker:up"
  artifacts:
    - path: "scripts/smoke-health.sh"
      provides: "Extended with six new probes (27-32) covering Phase 4 surface live against the docker stack"
  key_links:
    - from: "scripts/smoke-health.sh"
      to: "docker/kong/kong.yml games-routes block"
      via: "probe 27/28/29/32 hit Kong at localhost:8000"
      pattern: "localhost:8000/games"
---

<objective>
Extend `scripts/smoke-health.sh` with six new probes (27-32) covering Phase 4 surface live against the running docker stack, then run `bun run smoke:health` end-to-end after a `docker:up` and capture 32/32 PASS as the human-verify checkpoint signal.

Purpose: Phase 4 closeout requires LIVE verification — code that compiles and passes unit/integration tests is not the same as code that works against the real Postgres + Keycloak + Kong stack. The smoke probes are the executable acceptance proof a recruiter (or future you) can run on any clone.

Output: `scripts/smoke-health.sh` updated + a blocking human-verify checkpoint that requires `bun run docker:down && bun run docker:up` followed by `bun run smoke:health` returning 32/32 PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-game-core/04-RESEARCH.md
@CLAUDE.md
@.planning/phases/04-game-core/04-09-PLAN.md
@scripts/smoke-health.sh

<interfaces>
<!-- Current smoke-health.sh probe count: 14 active (15 if we count the wallets_kong_mutation_block). Per STATE.md Phase 3 reports "26/26 probes" — there may be 12 additional inline probes not surfaced in my partial read. The Phase 4 probes are numbered 27-32 to follow the existing convention. -->

Probe pattern (verbatim from existing file):
```bash
probe_<name>() {
  local name="<human-readable>"
  local code
  code=$(curl -s -o /dev/null -w "%{http_code}" "<url>")
  if [[ "${code}" == "<expected>" ]]; then record_pass "${name}"
  else record_fail "${name}" "expected <expected>, got ${code}"; fi
}
```

The keycloak token fetch pattern already exists for wallets:
- `probe_wallets_keycloak_token()` — sets WALLETS_TOKEN env var with the access token
- Reuse: rename to `probe_player_keycloak_token()` if no token-shared step exists, OR reuse WALLETS_TOKEN directly (same player → same audience-validated JWT works for both services since KEYCLOAK_AUDIENCE=account on both)

Endpoint surface (via Kong at localhost:8000 per Phase 1 compose):
- GET http://localhost:8000/games/rounds/current
- GET http://localhost:8000/games/rounds/history?limit=5
- GET http://localhost:8000/games/bets/me (JwtGuard)
- POST http://localhost:8000/games/bet (NOT in Kong allowlist — expected Kong 404)

Postgres seed_chain check:
- `docker compose exec -T postgres psql -U postgres -d games -tAc "SELECT COUNT(*) FROM seed_chain"` returns HASH_CHAIN_LENGTH
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add six Phase 4 smoke probes (27-32) to scripts/smoke-health.sh</name>
  <files>scripts/smoke-health.sh</files>
  <read_first>
    - scripts/smoke-health.sh (full file — observe existing probe_<name> function pattern, the WALLETS_TOKEN fetch, the record_pass / record_fail helpers, the probe invocation order at the bottom)
    - .planning/phases/04-game-core/04-09-PLAN.md (Kong route names + paths — the probe URLs must hit through Kong at :8000, not the games container at :4001)
    - .planning/phases/04-game-core/04-05-PLAN.md (HASH_CHAIN_LENGTH env var — probe 31 reads this from env or uses the default 1_000_000)
  </read_first>
  <behavior>
    - probe_games_rounds_current(): curl GET http://localhost:8000/games/rounds/current; expects 200; additionally parses JSON via `jq` (already used elsewhere in the file per Phase 3 pattern) and asserts `.status` in ("BETTING","RUNNING","CRASHED","SETTLED") and `.seedHash` is non-empty (length === 64 — `jq -r '.seedHash | length'`)
    - probe_games_rounds_history(): curl GET http://localhost:8000/games/rounds/history?limit=5; expects 200; asserts response has `.rounds` array (may be empty — `jq -e 'has("rounds")'`)
    - probe_games_bets_me_unauth(): curl GET http://localhost:8000/games/bets/me WITHOUT Authorization; expects 401 (JwtGuard from Plan 04-07 fires — Kong forwards to games which returns 401; this is NOT a Kong-origin error, it's the upstream service's response). NOTE: Kong does NOT block unauthenticated requests at the gateway — Kong allows them through and the upstream JwtGuard responds 401. The probe verifies JwtGuard wired correctly
    - probe_games_bets_me_auth(): use WALLETS_TOKEN from probe_wallets_keycloak_token (already set earlier in the run); curl GET http://localhost:8000/games/bets/me with Authorization Bearer ${WALLETS_TOKEN}; expects 200; asserts `.bets` is an array
    - probe_games_seed_chain_initialized(): docker compose exec -T postgres psql -U postgres -d games -tAc "SELECT COUNT(*) FROM seed_chain"; expects a count > 0 (use the env var if exposed, else just assert > 0 since the production HASH_CHAIN_LENGTH=1M is the value docker:up uses). The probe records the actual count in the success message for ops visibility
    - probe_games_kong_mutation_block(): curl POST http://localhost:8000/games/bet with empty body; expects 404 (Kong-origin no-route-matched, mirrors the wallets mutation block probe 26 pattern from Phase 3). The probe additionally asserts the response body contains "no Route matched with those values" (Kong's default 404 message — proves it's Kong-origin not games-origin)
    - Invocation order at the bottom of the file: add the six new `probe_*` calls AFTER `probe_wallets_kong_mutation_block` (which is currently last)
    - Probes 27-32 are added in this strict numeric order; the human-readable name strings start with the number for ops clarity (e.g., `"27: GET /games/rounds/current returns 200 with seedHash"`)
  </behavior>
  <action>Open scripts/smoke-health.sh. Add the six probe functions in the same shell function style as the existing probes (no fancy bash, no associative arrays — match Phase 3 P3.09's style). The seed_chain probe uses `docker compose exec -T postgres` (not `docker exec` — the project uses compose-managed containers per Phase 1). The kong-mutation-block probe greps the response body for "no Route matched" — capture both the status code (curl -w "%{http_code}") and the body (separate curl -s call to the same URL OR use curl -i to combine). For the auth probe (30), check that WALLETS_TOKEN is set before invoking; if not set, record_fail with "no token". For probe 31 (seed_chain count), the success criterion is COUNT > 0 — the actual expected count is HASH_CHAIN_LENGTH (default 1M from .env.example) but reading that value reliably in bash is fragile; assert > 0 and log the actual count for human inspection. Append the six probe invocations to the bottom of the file in numeric order, AFTER the existing `probe_wallets_kong_mutation_block` line, BEFORE the `TOTAL=$((PASS + FAIL))` final summary line.</action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge && bash -n scripts/smoke-health.sh</automated>
  </verify>
  <done>
    - scripts/smoke-health.sh parses cleanly under `bash -n`
    - Six new probe_<name> functions added
    - Six probe invocations added in numeric order at the bottom
    - Probe names start with the probe number (27, 28, 29, 30, 31, 32)
    - WALLETS_TOKEN reuse pattern wired for probe 30
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live full-stack smoke run — docker:down + docker:up + smoke:health</name>
  <what-built>Phase 4 lands: domain aggregates (Round + Bet), pure-function provably-fair module in @crash/contracts, 1M hash chain bootstrap, autonomous round loop with kill-9 recovery, four REST READ endpoints, Kong narrowing to four GET-only routes, JwtGuard on /games/bets/me, eight integration tests, six new smoke probes (27-32).</what-built>
  <how-to-verify>
    Run these commands in order from the repo root /Users/pedro/Projetos/fullstack-challenge:

    1. `bun run docker:down` — clean teardown of any prior stack
    2. `bun run docker:up` — fresh bring-up; wait for healthchecks (the script waits internally; if not, watch `docker compose ps` until every service is healthy)
    3. Wait an additional ~15s after all containers are healthy so the games-service finishes the 1M hash chain bootstrap (research §Risk R3 estimates ~2s gen + insert time; allow slack)
    4. `bun run smoke:health` — runs all 32 probes; expected output `Smoke summary: 32/32 probes passed`
    5. Manually inspect `curl -s http://localhost:8000/games/rounds/current | jq` and confirm:
       - status field is one of BETTING, RUNNING, CRASHED, SETTLED
       - seedHash is a 64-character hex string
       - serverSeed is null unless status === SETTLED
    6. Verify Kong route narrowing: `curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:8000/games/bet` returns 404
    7. Verify seed chain populated: `docker compose exec -T postgres psql -U postgres -d games -tAc "SELECT COUNT(*) FROM seed_chain"` returns 1000000 (or HASH_CHAIN_LENGTH override)
    8. Verify a round lifecycle observed: `docker compose exec -T postgres psql -U postgres -d games -tAc "SELECT status, COUNT(*) FROM rounds GROUP BY status ORDER BY status"` shows rounds at multiple statuses (proves loop is running)
    9. Run the games integration suite: `cd services/games && INTEGRATION=1 bun test tests/integration` — expected all eight files green
    10. Run the games unit + property suites: `cd services/games && bun test tests/unit tests/property` — expected all green
    11. Run the contracts suite: `cd packages/contracts && bun test` — expected all green
    12. Optional: `cd /Users/pedro/Projetos/fullstack-challenge && echo '{"serverSeed":"<from a settled round>","clientSeed":"<from same round>","nonce":"<from same round>","instantCrashBucket":101,"expectedCrashPoint":<from same round>}' | bun packages/contracts/bin/verify-crash.ts` — should print MATCH (the recruiter-facing CLI verifier)
    13. **True-SIGKILL kill-9 recovery drill (W4 plan-check fix — proves REQ-GAME-09 beyond app.close() simulation):**
        a. Capture the current round id: `BEFORE_ROUND=$(curl -s http://localhost:8000/games/rounds/current | jq -r .id)` and current status `BEFORE_STATUS=$(curl -s http://localhost:8000/games/rounds/current | jq -r .status)`
        b. Wait until status is `RUNNING` (re-query every 500ms if needed)
        c. `docker compose kill -s SIGKILL games` — sends real SIGKILL, OnApplicationShutdown is SKIPPED (mimics OS-level crash, the actually-hard case)
        d. `docker compose start games` and wait for healthcheck (`docker compose ps games`)
        e. Wait 5s for round-loop to recover
        f. Capture `AFTER=$(curl -s http://localhost:8000/games/rounds/current)`
        g. Assert: the loop CONTINUED — `AFTER` shows a round id that is either the same as `BEFORE_ROUND` (recovered mid-round) OR a strictly later round (recovered + settled + advanced). Either is acceptable; what's NOT acceptable is the service stuck or DB row in an impossible state.
        h. Verify rounds table integrity: `docker compose exec -T postgres psql -U postgres -d games -tAc "SELECT COUNT(*) FROM rounds WHERE status = 'RUNNING' AND started_at < now() - interval '60 seconds'"` returns 0 (no orphan in-flight rounds)
        i. Verify the BEFORE_ROUND eventually reached a terminal state (CRASHED or SETTLED): `docker compose exec -T postgres psql -U postgres -d games -tAc "SELECT status FROM rounds WHERE id = '$BEFORE_ROUND'"` returns CRASHED or SETTLED

    If any probe fails or any test suite is red OR the SIGKILL drill leaves a stuck/orphan state, capture the failing output, do NOT approve, and describe the failure to the planner so a follow-up plan can address it.
  </how-to-verify>
  <resume-signal>Type "approved" if 32/32 probes pass AND all three test suites green AND CLI verifier prints MATCH; OR describe failures to escalate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke probe → live Kong | Public-network-equivalent path; what a recruiter would test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11-01 | Repudiation | Smoke probes pass while real flow broken | mitigate | Probes assert response BODY shape, not just status code (probe 27 checks .seedHash length === 64) |
| T-04-11-02 | Tampering | Kong allows unintended POST through | mitigate | Probe 32 explicitly checks POST /games/bet returns 404 + "no Route matched" body |
</threat_model>

<verification>
- bash -n smoke-health.sh syntactic check
- Human verifier confirms 32/32 PASS
- Three test suites green (services/games unit + property + integration; packages/contracts)
- CLI verifier MATCH on a settled round payload
</verification>

<success_criteria>
- Six new smoke probes added covering all Phase 4 endpoints + seed chain bootstrap + Kong narrowing
- Live verification checkpoint requires 32/32 PASS
- All test suites green against the live docker stack
- CLI verifier byte-recomputes a real settled round
</success_criteria>

<output>
Create `.planning/phases/04-game-core/04-11-SUMMARY.md` with: probe count delta (26 → 32), all six probe outputs from the live run, seed_chain row count, recovery-test result, CLI verifier MATCH evidence.
</output>
