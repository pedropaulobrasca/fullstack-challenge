---
phase: 05-saga-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/games/src/domain/round.aggregate.ts
  - services/games/src/domain/errors.ts
  - services/games/tests/unit/round.aggregate.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-GAME-08
must_haves:
  truths:
    - "Round.acceptBet(now) on a BETTING round returns the same Round without mutation."
    - "Round.acceptBet on a non-BETTING round throws RoundNotInBettingPhaseError carrying the actual status."
    - "REQUIREMENTS.md REQ-GAME-08 row cites the new aggregate method, closing the prior doc drift."
  artifacts:
    - path: services/games/src/domain/round.aggregate.ts
      provides: "Round.acceptBet aggregate-boundary FSM guard"
      contains: "acceptBet"
    - path: services/games/src/domain/errors.ts
      provides: "RoundNotInBettingPhaseError"
      contains: "RoundNotInBettingPhaseError"
  key_links:
    - from: services/games/src/domain/round.aggregate.ts
      to: services/games/src/domain/errors.ts
      via: "named import of RoundNotInBettingPhaseError"
      pattern: "RoundNotInBettingPhaseError"
---

<objective>
Close REQ-GAME-08 at the aggregate boundary by introducing `Round.acceptBet(now)` plus the named domain error it throws. The method is a pure FSM guard — it returns the same Round when status is BETTING and otherwise throws `RoundNotInBettingPhaseError`. This is preparation for plan 05-04 (PlaceBetUseCase) and resolves the REQUIREMENTS.md doc drift that cited a method which did not yet exist.

Purpose: DDD-pure aggregate-level FSM enforcement instead of use-case-level guard, matching the spirit of REQ-GAME-08 ("enforced at aggregate boundary, not service layer") and the existing Round.start/crash/settle pattern.

Output: Round.acceptBet method, RoundNotInBettingPhaseError error class, unit tests, and REQUIREMENTS.md citation update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-saga-integration/05-RESEARCH.md
@services/games/src/domain/round.aggregate.ts
@services/games/src/domain/errors.ts

<interfaces>
<!-- Existing Round aggregate already has start(now)/crash(at,time)/settle(seed,now) — all pure: return new Round or throw IllegalRoundTransitionError. -->
<!-- Domain errors file already exports IllegalRoundTransitionError. New error follows the same shape. -->

From services/games/src/domain/round.aggregate.ts:
- private constructor consuming RoundProps
- get status(): RoundStatus
- start(now: Date): Round  (throws IllegalRoundTransitionError if status !== 'BETTING')
- crash(at: CrashPoint, time: Date): Round
- settle(serverSeed: string, now: Date): Round

From services/games/src/domain/value-objects/round-status.ts:
- export type RoundStatus = "BETTING" | "RUNNING" | "CRASHED" | "SETTLED";
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RoundNotInBettingPhaseError and Round.acceptBet method</name>
  <files>services/games/src/domain/errors.ts, services/games/src/domain/round.aggregate.ts</files>
  <read_first>
    - services/games/src/domain/round.aggregate.ts (entire file)
    - services/games/src/domain/errors.ts
    - services/games/src/domain/value-objects/round-status.ts
  </read_first>
  <behavior>
    - Test 1: `round.acceptBet(now)` when status === 'BETTING' returns the SAME Round reference shape (no prop mutation), proven by `next.status === 'BETTING'` and equal `id`/`nonce`/`bettingEndsAt`.
    - Test 2: `round.acceptBet(now)` when status === 'RUNNING' throws `RoundNotInBettingPhaseError` and the error carries `actual === 'RUNNING'`.
    - Test 3: Same for 'CRASHED' and 'SETTLED' (one parameterised test or three).
    - Test 4: `RoundNotInBettingPhaseError` extends `Error`, has `name === 'RoundNotInBettingPhaseError'`, and exposes `actual: RoundStatus` (or `actual: string` with the actual status value).
  </behavior>
  <action>
    1. In `services/games/src/domain/errors.ts`, add `export class RoundNotInBettingPhaseError extends Error` with a constructor taking `actual: RoundStatus | "NO_OPEN_ROUND"`. Set `this.name = 'RoundNotInBettingPhaseError'`. Store `actual` as a public readonly field so callers (controllers in plan 05-04) can map it to the 409 discriminated payload `{ code: 'ROUND_NOT_IN_BETTING_PHASE', phase: <actual> }`. The error message should include the actual status for log readability.
    2. In `services/games/src/domain/round.aggregate.ts`, add an `acceptBet(now: Date): Round` instance method. Pure FSM guard. If `this.props.status !== 'BETTING'` throw `new RoundNotInBettingPhaseError(this.props.status)`. Otherwise return `this` (or `new Round({ ...this.props })` — caller must not depend on identity; document the chosen semantics in a single doc-comment line). Match style of existing `start(now)` method — no mutation, no emission of events, pure validation. Do NOT consume `now` for anything other than future signature symmetry with `start(now)`/`crash(at, time)`/`settle(seed, now)` (per REQ-GAME-08 the method is purely a guard).
    3. Import `RoundNotInBettingPhaseError` from `./errors` at the top of `round.aggregate.ts`.
    4. Update existing tests in `services/games/tests/unit/round.aggregate.test.ts` only if test names overlap; otherwise add a new `describe('Round.acceptBet')` block with the four behavior tests listed above. Use the same factory helpers used by the existing Round tests.
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bun test tests/unit/round.aggregate.test.ts</automated>
  </verify>
  <done>
    - `Round.acceptBet(now)` exists, is pure, throws RoundNotInBettingPhaseError for non-BETTING states.
    - `errors.ts` exports `RoundNotInBettingPhaseError` with a readonly `actual` field.
    - Bun unit test suite for `round.aggregate.test.ts` passes including the four new behavior tests.
    - `bunx tsc --noEmit` passes inside `services/games`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md REQ-GAME-08 citation</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (just the REQ-GAME-08 row + Phase 4 traceability table — read the file in one pass and locate "REQ-GAME-08")
  </read_first>
  <action>
    1. Find the REQ-GAME-08 row inside the Phase 4 traceability table (currently cites "Round.acceptBet throws RoundNotInBettingPhaseError" — a method that did NOT exist at Phase 4 close).
    2. Replace the status cell so it reads: "Done (P4.02 aggregate FSM via existing Round.start/crash/settle + Phase 5 plan 05-01 adds Round.acceptBet(now) aggregate-boundary guard surfacing RoundNotInBettingPhaseError; Phase 5 plan 05-04 surfaces the 409 ConflictException with discriminated code ROUND_NOT_IN_BETTING_PHASE via POST /games/bet)".
    3. Do NOT change the `- [x]` checkbox state of REQ-GAME-08 (it stays complete; this is doc fidelity, not status change).
    4. Do NOT touch other rows. Do NOT touch the v1-complete count summary.
  </action>
  <verify>
    <automated>grep -c "Round.acceptBet" .planning/REQUIREMENTS.md | awk '$1 &gt;= 1 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    - REQUIREMENTS.md REQ-GAME-08 row now references both the existing Phase 4 FSM enforcement and the Phase 5 05-01 Round.acceptBet addition.
    - No checkbox toggles; no v1-complete count changes; no other rows altered.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure domain change; no external surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Round FSM bypass via direct entity mutation | accept | Aggregate keeps `private constructor` + immutable props; ORM rehydrates via static factory only. No new attack surface in this plan. |
</threat_model>

<verification>
- `bun test tests/unit/round.aggregate.test.ts` — green
- `bunx tsc --noEmit` inside `services/games` — clean
- `grep "Round.acceptBet" .planning/REQUIREMENTS.md` — at least one match
</verification>

<success_criteria>
- Pure aggregate-level FSM guard `Round.acceptBet(now)` exists and is unit-tested.
- `RoundNotInBettingPhaseError` is the single named error thrown.
- REQUIREMENTS.md no longer drifts: the cited method now exists.
</success_criteria>

<output>
Create `.planning/phases/05-saga-integration/05-01-SUMMARY.md` when done.
</output>
