---
phase: 05-saga-integration
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - services/games/src/infrastructure/mikro-orm/migrations/20260527001-create-bet-saga-state.ts
  - services/games/src/infrastructure/persistence/bet-saga-state.entity.ts
  - services/games/src/domain/bet-saga-state.aggregate.ts
  - services/games/src/domain/bet-saga-state.repository.ts
  - services/games/src/domain/errors.ts
  - services/games/src/infrastructure/repositories/mikro-bet-saga-state.repository.ts
  - services/games/src/application/tokens.ts
  - services/games/src/application/game-core.module.ts
  - services/games/tests/unit/bet-saga-state.aggregate.test.ts
autonomous: true
requirements:
  - REQ-SAGA-02
must_haves:
  truths:
    - "A `bet_saga_state` row exists in postgres after migration with the five-status CHECK constraint."
    - "BetSagaState aggregate transitions DEBIT_PENDING -> { CONFIRMED, REFUNDED, TIMED_OUT } and TIMED_OUT -> COMPENSATED only; all other transitions throw."
    - "MikroBetSagaStateRepository.claimExpired returns rows via FOR UPDATE SKIP LOCKED, scoped to status='DEBIT_PENDING' AND deadline_at < $now, ORDER BY deadline_at LIMIT $n."
    - "MikroBetSagaStateRepository.transition is atomic (UPDATE ... WHERE bet_id = $id AND status = $from RETURNING ...) and returns null on losing race."
    - "BET_SAGA_REPOSITORY token registered in GameCoreModule; concrete repo wired via useClass."
  artifacts:
    - path: services/games/src/infrastructure/mikro-orm/migrations/20260527001-create-bet-saga-state.ts
      provides: "DDL for bet_saga_state table + indexes"
      contains: "bet_saga_state"
    - path: services/games/src/domain/bet-saga-state.aggregate.ts
      provides: "BetSagaState aggregate (pure FSM)"
      contains: "class BetSagaState"
    - path: services/games/src/domain/bet-saga-state.repository.ts
      provides: "BetSagaStateRepository interface"
      contains: "BetSagaStateRepository"
    - path: services/games/src/infrastructure/repositories/mikro-bet-saga-state.repository.ts
      provides: "Mikro implementation with atomic UPDATE-RETURNING and FOR UPDATE SKIP LOCKED"
      contains: "claimExpired"
    - path: services/games/src/application/tokens.ts
      provides: "BET_SAGA_REPOSITORY DI token"
      contains: "BET_SAGA_REPOSITORY"
  key_links:
    - from: services/games/src/application/game-core.module.ts
      to: services/games/src/infrastructure/repositories/mikro-bet-saga-state.repository.ts
      via: "useClass binding under BET_SAGA_REPOSITORY token + MikroOrmModule.forFeature([BetSagaStateEntitySchema])"
      pattern: "BET_SAGA_REPOSITORY"
---

<objective>
Land the persistent saga-state foundation: DB migration, EntitySchema, BetSagaState aggregate (minimal FSM-only), repository interface + Mikro implementation, DI token, and module wiring. This is the single new persistent concept in Phase 5 — every subsequent plan composes on top of it.

Purpose: REQ-SAGA-02 ("System persists saga state in a `bet_saga_state` row so a service restart can recover and resume in-flight sagas"). The Mikro repo's `claimExpired` (`FOR UPDATE SKIP LOCKED LIMIT 100`) backs the sweeper in plan 05-07; the atomic `transition(from, to)` UPDATE-WHERE backs the handlers in plan 05-05.

Output: Migration runs at boot, schema asserts the 5-status CHECK + correlation_id unique index + partial deadline index, aggregate enforces FSM transitions, repo offers idempotent atomic operations, module wiring exposes the token.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-saga-integration/05-RESEARCH.md
@services/games/src/infrastructure/mikro-orm/migrations/20260526003-create-bets.ts
@services/games/src/infrastructure/persistence/bet.entity.ts
@services/games/src/infrastructure/repositories/
@services/games/src/application/game-core.module.ts
@services/games/src/application/tokens.ts

<interfaces>
<!-- Existing migrations format (timestamp-prefixed) and EntitySchema style. Mirror these. -->
<!-- Existing bets entity uses EntitySchema (Phase 4). MikroORM 7.1 forFeature receives EntitySchema instances. -->

From services/games/src/application/tokens.ts (existing):
```ts
export const ROUND_REPOSITORY = "ROUND_REPOSITORY";
export const BET_REPOSITORY = "BET_REPOSITORY";
export const SEED_CHAIN_REPOSITORY = "SEED_CHAIN_REPOSITORY";
```

From services/games/src/application/game-core.module.ts:
- @Module decorated, imports MikroOrmModule.forFeature([RoundEntitySchema, BetEntitySchema, SeedChainEntitySchema]), provides three useClass bindings + the bootstrap + RoundLoopService + four lifecycle use cases.

From @crash/shared-kernel:
- type BetId (branded string), CorrelationId (branded string).

Status FSM (target):
- 'DEBIT_PENDING' → 'CONFIRMED'   (wallet.debited arrived in time)
- 'DEBIT_PENDING' → 'REFUNDED'    (wallet.debit.rejected arrived)
- 'DEBIT_PENDING' → 'TIMED_OUT'   (sweeper claimed expired row)
- 'TIMED_OUT'     → 'COMPENSATED' (late wallet.debited; compensation credit emitted)
All other transitions illegal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + EntitySchema for bet_saga_state</name>
  <files>services/games/src/infrastructure/mikro-orm/migrations/20260527001-create-bet-saga-state.ts, services/games/src/infrastructure/persistence/bet-saga-state.entity.ts</files>
  <read_first>
    - services/games/src/infrastructure/mikro-orm/migrations/20260526003-create-bets.ts (mirror style — Migration class, up()/down(), this.addSql, exact SQL idioms)
    - services/games/src/infrastructure/persistence/bet.entity.ts (EntitySchema style with EntitySchema constructor, properties block, indexes/constraints)
  </read_first>
  <action>
    1. Create migration `20260527001-create-bet-saga-state.ts` extending `Migration`. In `up()` emit one `this.addSql(...)` for the table per the research DDL block:
       ```
       CREATE TABLE bet_saga_state (
         bet_id          UUID PRIMARY KEY REFERENCES bets(id) ON DELETE CASCADE,
         correlation_id  TEXT NOT NULL,
         status          TEXT NOT NULL CHECK (status IN ('DEBIT_PENDING','CONFIRMED','REFUNDED','TIMED_OUT','COMPENSATED')),
         deadline_at     TIMESTAMPTZ NOT NULL,
         updated_at      TIMESTAMPTZ NOT NULL DEFAULT now()
       );
       ```
       Then two index addSql calls: `CREATE UNIQUE INDEX bet_saga_state_correlation_id_idx ON bet_saga_state(correlation_id);` and `CREATE INDEX bet_saga_state_deadline_idx ON bet_saga_state(deadline_at) WHERE status = 'DEBIT_PENDING';`. The CHECK constraint must be named `bet_saga_state_status_check` (explicit name for forensics — match the `bets_status_state_consistency_check` naming style fixed in P4.11). If MikroORM autonames, leave it implicit and document. Implement `down()` with `DROP TABLE bet_saga_state CASCADE;`.
    2. Create `bet-saga-state.entity.ts` exporting `BetSagaStateEntitySchema` as a new `EntitySchema<BetSagaStateRow>` with tableName `'bet_saga_state'`. Properties: `betId` (primaryKey, fieldName: 'bet_id', type: 'uuid'), `correlationId` (type: 'string', fieldName: 'correlation_id'), `status` (type: 'string', fieldName: 'status'), `deadlineAt` (type: Date, fieldName: 'deadline_at'), `updatedAt` (type: Date, fieldName: 'updated_at', onUpdate: () => new Date()). Use the same EntitySchema construction shape as `bet.entity.ts`.
    3. Do NOT add a FK target object to the EntitySchema — keep the FK enforced at the DB layer only (mirrors how `bets.round_id` is treated in P4).
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bunx tsc --noEmit</automated>
  </verify>
  <done>
    - Migration file compiles and follows the existing Phase 4 migration style.
    - EntitySchema exports the row type and is importable from the new path.
    - down() drops the table cleanly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BetSagaState aggregate + errors + repository interface + unit tests</name>
  <files>services/games/src/domain/bet-saga-state.aggregate.ts, services/games/src/domain/bet-saga-state.repository.ts, services/games/src/domain/errors.ts, services/games/tests/unit/bet-saga-state.aggregate.test.ts</files>
  <read_first>
    - services/games/src/domain/bet.aggregate.ts (pure aggregate pattern with private ctor + static factory + transition methods returning new Bet)
    - services/games/src/domain/round.aggregate.ts (mirror)
    - services/games/src/domain/errors.ts (existing error class shape)
  </read_first>
  <behavior>
    - Test 1: `BetSagaState.create({ betId, correlationId, deadlineAt })` returns an aggregate with status === 'DEBIT_PENDING' and the supplied fields.
    - Test 2: `state.confirm()` on DEBIT_PENDING returns a new aggregate with status === 'CONFIRMED'.
    - Test 3: `state.refund()` on DEBIT_PENDING returns REFUNDED.
    - Test 4: `state.timeOut()` on DEBIT_PENDING returns TIMED_OUT.
    - Test 5: `state.compensate()` on TIMED_OUT returns COMPENSATED.
    - Test 6: `state.compensate()` on CONFIRMED throws `IllegalBetSagaTransitionError` with `from='CONFIRMED'` and `to='COMPENSATED'`.
    - Test 7: `state.confirm()` after a terminal state (CONFIRMED, REFUNDED, COMPENSATED) throws `IllegalBetSagaTransitionError`.
    - Test 8: `BetSagaState.rehydrate(row)` returns an aggregate matching the row's status — no transitions trigger from rehydrate.
  </behavior>
  <action>
    1. In `errors.ts`, add `IllegalBetSagaTransitionError extends Error` with `from: BetSagaStatus` + `to: BetSagaStatus` readonly fields. Also export `BetSagaNotFoundError extends Error` carrying a `correlationId: string` field (used by handlers in plan 05-05 when no saga exists for an incoming envelope). Also export `SagaTimeoutError extends Error` (zero-arg or string-msg constructor — used by 05-04 to flag the refund reason).
    2. Create `bet-saga-state.aggregate.ts`. Export `type BetSagaStatus = 'DEBIT_PENDING' | 'CONFIRMED' | 'REFUNDED' | 'TIMED_OUT' | 'COMPENSATED'`. Export `class BetSagaState` with private constructor over props `{ betId: BetId; correlationId: string; status: BetSagaStatus; deadlineAt: Date; updatedAt: Date }`. Public getters for each. Static factory `create({ betId, correlationId, deadlineAt })` -> initial state DEBIT_PENDING + updatedAt = new Date(). Static `rehydrate(props)` -> bypass FSM. Instance methods: `confirm()` (from=DEBIT_PENDING), `refund()` (from=DEBIT_PENDING), `timeOut()` (from=DEBIT_PENDING), `compensate()` (from=TIMED_OUT). Each throws `IllegalBetSagaTransitionError(this.status, target)` if precondition fails; otherwise returns `new BetSagaState({ ...this.props, status: target, updatedAt: new Date() })`. ZERO infrastructure imports (per CLAUDE.md "Zero infrastructure imports in domain/").
    3. Create `bet-saga-state.repository.ts` with the interface from research §"BetSagaStateRepository interface sketch":
       - `create(input, txEm): Promise<void>`
       - `findByCorrelationId(correlationId, txEm?): Promise<BetSagaState | null>`
       - `findByBetId(betId, txEm?): Promise<BetSagaState | null>`
       - `transition(betId, fromStatus, toStatus, txEm): Promise<BetSagaState | null>` — returns null on race lose
       - `claimExpired(limit, now, txEm): Promise<BetSagaState[]>`
       Document each method with a one-line JSDoc explaining txEm semantics (required for writes, optional for reads).
    4. Write the unit tests in `bet-saga-state.aggregate.test.ts` covering the 8 behaviors above. Use plain `BetId` brand cast for the test betId (mirror `bet.aggregate.test.ts` if it does the same).
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bun test tests/unit/bet-saga-state.aggregate.test.ts</automated>
  </verify>
  <done>
    - Aggregate enforces the 5-state FSM with only 4 legal transitions (DEBIT_PENDING→{CONFIRMED, REFUNDED, TIMED_OUT}, TIMED_OUT→COMPENSATED).
    - All 8 behavior tests pass.
    - Zero infra imports in `bet-saga-state.aggregate.ts` (no `@nestjs`, `@mikro-orm`, etc).
    - `bunx tsc --noEmit` clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mikro repository implementation + module wiring</name>
  <files>services/games/src/infrastructure/repositories/mikro-bet-saga-state.repository.ts, services/games/src/application/tokens.ts, services/games/src/application/game-core.module.ts</files>
  <read_first>
    - services/games/src/infrastructure/repositories/ (one of the existing mikro repos to mirror the EM-bind pattern; specifically the one that does atomic UPDATE-RETURNING for tryTransition on the Bet repo, AND the seed-chain or wallets repo that uses `em.getTransactionContext()` per P3.09 fix)
    - services/games/src/application/game-core.module.ts (current provider registration shape)
    - services/games/src/application/tokens.ts
  </read_first>
  <action>
    1. In `tokens.ts`, add `export const BET_SAGA_REPOSITORY = "BET_SAGA_REPOSITORY";`.
    2. Create `mikro-bet-saga-state.repository.ts`:
       - `@Injectable()` class `MikroBetSagaStateRepository implements BetSagaStateRepository`.
       - Constructor injects `EntityManager` from `@mikro-orm/postgresql`.
       - `create`: `(txEm ?? this.em).persistAndFlush(...)` OR `(txEm ?? this.em).getRepository(BetSagaStateEntitySchema).nativeInsert({ ... })`. Match the style used by `MikroBetRepository.save`. Always prefer the supplied `txEm` when present (required for handler-driven inserts).
       - `findByCorrelationId(correlationId, txEm?)`: `(txEm ?? this.em).findOne(BetSagaStateEntitySchema, { correlationId })` and map row → `BetSagaState.rehydrate(row)` or null.
       - `findByBetId(betId, txEm?)`: similar via primary key.
       - `transition(betId, fromStatus, toStatus, txEm)`: raw SQL bound to the TX context — `UPDATE bet_saga_state SET status = $1, updated_at = now() WHERE bet_id = $2 AND status = $3 RETURNING bet_id, correlation_id, status, deadline_at, updated_at`. Use `txEm.getConnection().execute(sql, params, "all", txEm.getTransactionContext())` (P3.09 carry-forward; the connection autocommits otherwise). If `result.length === 0` return null (race lost); else rehydrate and return. NEVER use the bare `this.em.getConnection().execute` here — it must be on the supplied `txEm` per the same-TX invariant.
       - `claimExpired(limit, now, txEm)`: raw SQL `SELECT bet_id, correlation_id, status, deadline_at, updated_at FROM bet_saga_state WHERE status = 'DEBIT_PENDING' AND deadline_at < $1 ORDER BY deadline_at LIMIT $2 FOR UPDATE SKIP LOCKED`. Same `getTransactionContext()` bind. Map each row → `BetSagaState.rehydrate(...)`.
    3. In `game-core.module.ts`:
       - Extend `MikroOrmModule.forFeature([RoundEntitySchema, BetEntitySchema, SeedChainEntitySchema])` to also include `BetSagaStateEntitySchema`.
       - Add `{ provide: BET_SAGA_REPOSITORY, useClass: MikroBetSagaStateRepository }` to the providers array.
       - Add `BET_SAGA_REPOSITORY` to the `exports` array so dependent modules can consume it (Phase 5 handlers + use cases will live in submodules or be added to this module directly in later plans).
       - Add the migration file under whatever migration glob the service already uses; if migrations are auto-discovered by filename pattern, the new file is picked up automatically — confirm by reading the service's mikro-orm config and add it explicitly only if not.
    4. Do NOT create a separate "saga module" yet — keep everything inside `GameCoreModule` to mirror Phase 4. The Phase 5 application providers (use cases, handlers, sweeper) will all extend this single module in subsequent plans.
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bunx tsc --noEmit &amp;&amp; bun test tests/unit</automated>
  </verify>
  <done>
    - `MikroBetSagaStateRepository` implements all 5 interface methods.
    - `transition` and `claimExpired` use `getTransactionContext()`-bound raw SQL.
    - `claimExpired` includes `FOR UPDATE SKIP LOCKED`.
    - `GameCoreModule` provides + exports `BET_SAGA_REPOSITORY`.
    - `MikroOrmModule.forFeature` lists the new EntitySchema.
    - `bunx tsc --noEmit` clean; existing unit suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → DB | All saga reads/writes scoped to the games DB; correlation_id is opaque UUID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | Saga row mutated outside TX | mitigate | `transition` + `claimExpired` always use `txEm.getTransactionContext()`-bound SQL (P3.09 fix carry-forward) |
| T-05-03-R | Tampering | Two sweepers claiming same row | mitigate | `FOR UPDATE SKIP LOCKED` — Phase 5 single-instance scope, but SQL is scale-out forward-compatible |
| T-05-03-I | Information Disclosure | Cross-player saga read | accept | `correlation_id` is opaque UUID; no PII; callers in 05-05 scope reads to a single `req.user.playerId` |
</threat_model>

<verification>
- `bunx tsc --noEmit` clean inside `services/games`.
- `bun test tests/unit` green (8 new aggregate tests + zero regressions).
- Migration up()/down() compiles; will run automatically on next `bun run docker:up` (live verification deferred to plan 05-10 smoke checkpoint).
</verification>

<success_criteria>
- `BET_SAGA_REPOSITORY` is a registered DI token resolving to `MikroBetSagaStateRepository`.
- Aggregate enforces the FSM; repo offers atomic operations bound to a TX.
- Phase 5 downstream plans (05-04, 05-05, 05-07) can inject the token and rely on its contract.
</success_criteria>

<output>
Create `.planning/phases/05-saga-integration/05-03-SUMMARY.md` when done.
</output>
