---
phase: 05-saga-integration
plan: 04
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-03
files_modified:
  - services/games/src/application/use-cases/place-bet.use-case.ts
  - services/games/src/domain/errors.ts
  - services/games/src/presentation/controllers/bet-command.controller.ts
  - services/games/src/presentation/dtos/place-bet.request.dto.ts
  - services/games/src/presentation/dtos/place-bet.response.dto.ts
  - services/games/src/application/game-core.module.ts
  - services/games/src/app.module.ts
  - services/games/tests/unit/place-bet.use-case.test.ts
autonomous: true
requirements:
  - REQ-GAME-06
  - REQ-SAGA-01
must_haves:
  truths:
    - "POST /games/bet during BETTING phase returns 202 with body { betId, status: 'PENDING' }."
    - "POST /games/bet during RUNNING/CRASHED/SETTLED returns 409 with body { code: 'ROUND_NOT_IN_BETTING_PHASE', phase: <actual> }."
    - "POST /games/bet when player already has an ACTIVE/PENDING bet returns 409 with body { code: 'BET_ALREADY_ACTIVE', existingBetId: <uuid> } (single-bet-per-round invariant)."
    - "POST /games/bet without a valid Keycloak JWT returns 401."
    - "Each successful PlaceBet writes bets(status=PENDING) + bet_saga_state(status=DEBIT_PENDING, deadlineAt=now+SAGA_TIMEOUT_MS) + outbox(wallet.commands, wallet.debit) in ONE em.transactional callback."
  artifacts:
    - path: services/games/src/application/use-cases/place-bet.use-case.ts
      provides: "PlaceBetUseCase — orchestrates Round.acceptBet, Bet INSERT, saga INSERT, wallet.debit outbox in a single TX"
      contains: "PlaceBetUseCase"
    - path: services/games/src/presentation/controllers/bet-command.controller.ts
      provides: "POST /games/bet command controller (separate from BetsController which is /games/bets read-only)"
      contains: "@Controller(\"games/bet\")"
    - path: services/games/src/presentation/dtos/place-bet.request.dto.ts
      provides: "Request DTO + zod schema with amountCents bigint snapshot"
      contains: "placeBetRequestSchema"
    - path: services/games/src/presentation/dtos/place-bet.response.dto.ts
      provides: "Response DTO for 202"
      contains: "PlaceBetResponseDto"
  key_links:
    - from: services/games/src/presentation/controllers/bet-command.controller.ts
      to: services/games/src/application/use-cases/place-bet.use-case.ts
      via: "constructor injection + execute({ playerId, amount, now: new Date() })"
      pattern: "placeBet.execute"
    - from: services/games/src/application/use-cases/place-bet.use-case.ts
      to: "@crash/messaging-spine OutboxRepository.add"
      via: "txEm-bound outbox.add(buildEnvelope({ type: 'wallet.debit' }), { exchange: EXCHANGES.WALLET_COMMANDS, routingKey: 'wallet.debit' }, txEm)"
      pattern: "wallet.debit"
---

<objective>
Land the bet placement entry point: zod-validated request DTO, JwtGuard-protected POST `/games/bet` controller, and the orchestration use case. Use case opens a single `em.transactional` callback that loads the open Round, calls `Round.acceptBet(now)` (FSM guard from plan 05-01), checks single-bet invariant, places a `Bet(PENDING)`, inserts the `bet_saga_state(DEBIT_PENDING)` row with a deadline, and writes the `wallet.debit` outbox envelope — all under the same `txEm`. Returns 202 with `{ betId, status: 'PENDING' }`.

Purpose: REQ-GAME-06 + REQ-SAGA-01 step 1. Honors ADR-020 (bet-202 / cashout-200 asymmetry). The controller path is the literal spec text `/games/bet` (singular) per research Open Question 2 — new controller is `BetCommandController` mounted at `games/bet`; the existing `BetsController` keeps `/games/bets/me` read-only.

Output: Use case + controller + DTOs + module wiring + unit tests. No live AMQP delivery yet — outbox row is sufficient; OutboxPublisher (Phase 2) will ship it asynchronously.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-saga-integration/05-RESEARCH.md
@.planning/phases/05-saga-integration/05-01-PLAN.md
@.planning/phases/05-saga-integration/05-03-PLAN.md
@services/games/src/domain/round.aggregate.ts
@services/games/src/domain/bet.aggregate.ts
@services/games/src/domain/round.repository.ts
@services/games/src/domain/bet.repository.ts
@services/games/src/presentation/controllers/bets.controller.ts
@services/games/src/presentation/guards/jwt.guard.ts
@services/games/src/config/defaults.ts
@services/wallets/src/application/handlers/wallet-debit.handler.ts
@packages/contracts/src/wallet/payloads.ts
@packages/messaging-spine/src/outbox/outbox-repository.ts

<interfaces>
<!-- Existing patterns to mirror exactly. -->

From services/games/src/presentation/controllers/bets.controller.ts:
- `@Controller("games/bets") @UseGuards(JwtGuard)`
- `req.user!.playerId` extraction pattern
- `PlayerId(req.user!.playerId)` brand cast
- zod-parsed DTO via `.safeParse` + BadRequestException

From @crash/messaging-spine OutboxRepository.add signature:
- `add<TPayload>(envelope, route: { exchange, routingKey, aggregateType, aggregateId }, txEm: EntityManager): Promise<void>`

From @crash/contracts walletDebitPayloadSchema (packages/contracts/src/wallet/payloads.ts):
- payload = { playerId: string, amount: MoneySnapshot } — confirm exact field names during read_first.

From @crash/shared-kernel:
- `BetId`, `PlayerId`, `CorrelationId` brand casts.
- `Money.fromSnapshot({ amount, currency, scale })`.

From services/games/src/config/defaults.ts:
- env.SAGA_TIMEOUT_MS (default 5000).
- env.CURRENCY_CODE, env.CURRENCY_EXPONENT.
- env.BET_MIN_CENTS, env.BET_MAX_CENTS.

From plan 05-01:
- Round.acceptBet(now: Date): Round — throws RoundNotInBettingPhaseError if !BETTING.

From plan 05-03:
- BET_SAGA_REPOSITORY token; BetSagaStateRepository.create({ betId, correlationId, deadlineAt, status: 'DEBIT_PENDING' }, txEm).
- BetAlreadyActiveError must be added in this plan's errors update (with existingBetId field).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlaceBetUseCase + BetAlreadyActiveError + BetAmountOutOfBoundsError + unit tests</name>
  <files>services/games/src/application/use-cases/place-bet.use-case.ts, services/games/src/domain/errors.ts, services/games/tests/unit/place-bet.use-case.test.ts</files>
  <read_first>
    - services/games/src/application/use-cases/crash-round.use-case.ts (existing transactional use case pattern with em.transactional + repo injection)
    - services/games/src/domain/errors.ts (current shape after plan 05-01 + 05-03 additions)
    - services/wallets/src/application/handlers/wallet-debit.handler.ts (canonical outbox.add usage with txEm)
    - packages/contracts/src/wallet/payloads.ts (walletDebitPayloadSchema)
    - packages/messaging-spine/src/outbox/outbox-repository.ts (add signature)
  </read_first>
  <behavior>
    - Test 1: Happy path — round.findOpen returns BETTING round, no existing bet → returns `{ betId, status: 'PENDING' }`. Stub repos record: `bets.save(Bet with status PENDING)`, `sagas.create({ status: 'DEBIT_PENDING', deadlineAt = now + 5000ms })`, `outbox.add(envelope with type='wallet.debit', exchange=WALLET_COMMANDS, routingKey='wallet.debit')`. correlationId is a UUID distinct from betId (per research Open Question 4).
    - Test 2: No open round → throws `RoundNotInBettingPhaseError` with `actual === 'NO_OPEN_ROUND'`. (Reuse existing error from 05-01 with the NO_OPEN_ROUND variant.)
    - Test 3: Open round in RUNNING → throws `RoundNotInBettingPhaseError` with `actual === 'RUNNING'` (delegated to Round.acceptBet — confirms the aggregate-boundary call).
    - Test 4: Player already has an active/pending bet on this round → throws `BetAlreadyActiveError` with `existingBetId` = the existing bet's id.
    - Test 5: Amount below env.BET_MIN_CENTS or above env.BET_MAX_CENTS → throws `BetAmountOutOfBoundsError` (or the existing Phase 4 BetAmount VO error if it already enforces bounds — choose whichever exists; if both exist, prefer the VO).
    - Test 6: All four writes (bets.save, sagas.create, outbox.add, optional Round-touched) happen INSIDE a single em.transactional callback — assert by stub-recording the order: em.transactional invoked exactly once; bets.save called with the txEm argument before sagas.create; both before outbox.add.
  </behavior>
  <action>
    1. Extend `services/games/src/domain/errors.ts` with `BetAlreadyActiveError extends Error` (readonly `existingBetId: BetId`) and `BetAmountOutOfBoundsError extends Error` (readonly `amountCents: bigint`, `minCents: bigint`, `maxCents: bigint`). If a comparable VO-level error already exists from Phase 4, keep both: VO error guards construction, this error is the application-layer translation.
    2. Create `place-bet.use-case.ts`:
       ```
       @Injectable()
       export class PlaceBetUseCase {
         constructor(
           private readonly em: EntityManager,
           private readonly outbox: OutboxRepository,
           @Inject(ROUND_REPOSITORY) private readonly rounds: RoundRepository,
           @Inject(BET_REPOSITORY) private readonly bets: BetRepository,
           @Inject(BET_SAGA_REPOSITORY) private readonly sagas: BetSagaStateRepository,
         ) {}
         async execute(input: { playerId: PlayerId; amount: Money; now: Date }): Promise<{ betId: BetId; status: 'PENDING' }>
       }
       ```
    3. Inside `execute`, wrap in `this.em.transactional(async (txEm) => { ... })`. Step-by-step:
       a. `const open = await this.rounds.findOpen(txEm)`. If null, throw `new RoundNotInBettingPhaseError('NO_OPEN_ROUND')`.
       b. Call `open.acceptBet(input.now)` (aggregate FSM guard — throws RoundNotInBettingPhaseError if !BETTING). Discard the returned Round (this method is a pure guard, no mutation).
       c. Bounds check: `if (amount.toCents() < env.BET_MIN_CENTS || amount.toCents() > env.BET_MAX_CENTS) throw new BetAmountOutOfBoundsError(...)`. Skip if BetAmount VO already enforces this on construction (the controller will construct via VO).
       d. `const existing = await this.bets.findActiveByRoundAndPlayer(open.id, input.playerId, txEm)`. If non-null, throw `new BetAlreadyActiveError(existing.id)`.
       e. `const betId = BetId(randomUUID()); const correlationId = randomUUID();` (fresh UUID per research Open Question 4).
       f. `const bet = Bet.place(betId, open.id, input.playerId, input.amount, input.now); await this.bets.save(bet, txEm);`.
       g. `const deadlineAt = new Date(input.now.getTime() + env.SAGA_TIMEOUT_MS); await this.sagas.create({ betId, correlationId, deadlineAt, status: 'DEBIT_PENDING' }, txEm);`.
       h. `await this.outbox.add(buildEnvelope({ type: 'wallet.debit', version: 1, correlationId, causationId: correlationId, payload: { playerId: input.playerId, amount: input.amount.toSnapshot() } }), { exchange: EXCHANGES.WALLET_COMMANDS, routingKey: 'wallet.debit', aggregateType: 'Bet', aggregateId: betId }, txEm);`.
       i. Return `{ betId, status: 'PENDING' as const }`.
    4. Import `randomUUID` from `node:crypto`. Import `buildEnvelope`, `EXCHANGES`, `OutboxRepository` from `@crash/messaging-spine`. Import `BetId`, `PlayerId`, `Money` from `@crash/shared-kernel`. Import `env` from `../../config/defaults`. Import `Bet` aggregate + repos via tokens.
    5. Write unit tests with in-memory stub repos that record calls. Use `jest`-style or `bun:test` `mock` — match `place-bet.use-case.test.ts` companion patterns from Phase 4 tests. Stub `em.transactional` to invoke the callback inline with the same em as txEm.
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bun test tests/unit/place-bet.use-case.test.ts</automated>
  </verify>
  <done>
    - All 6 behavior tests pass.
    - Use case opens exactly one em.transactional; all four writes (bet, saga, outbox) take txEm.
    - correlationId and betId are distinct UUIDs.
    - bunx tsc --noEmit clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: BetCommandController + DTOs + JwtGuard + module wiring</name>
  <files>services/games/src/presentation/controllers/bet-command.controller.ts, services/games/src/presentation/dtos/place-bet.request.dto.ts, services/games/src/presentation/dtos/place-bet.response.dto.ts, services/games/src/application/game-core.module.ts, services/games/src/app.module.ts</files>
  <read_first>
    - services/games/src/presentation/controllers/bets.controller.ts (existing BetsController pattern — JwtGuard, req.user, BadRequestException)
    - services/games/src/presentation/guards/jwt.guard.ts (AuthenticatedRequest shape)
    - services/games/src/presentation/dtos/player-bets.dto.ts (existing zod DTO style + createZodDto export)
    - services/games/src/app.module.ts (controller registration pattern)
    - services/games/src/application/game-core.module.ts (provider/exports pattern)
  </read_first>
  <action>
    1. Create `place-bet.request.dto.ts`:
       - `export const placeBetRequestSchema = z.object({ amountCents: z.string().regex(/^\d+$/).transform((s) => BigInt(s)) })` (string-bigint snapshot to avoid JSON precision loss).
       - `export class PlaceBetRequestDto extends createZodDto(placeBetRequestSchema) {}`.
    2. Create `place-bet.response.dto.ts`:
       - `export type PlaceBetResponseDto = { betId: string; status: 'PENDING' }` (plain type — response shape is small; or a class via createZodDto for symmetry — pick the codebase convention).
    3. Create `bet-command.controller.ts`:
       ```ts
       @Controller('games/bet')
       @UseGuards(JwtGuard)
       export class BetCommandController {
         constructor(private readonly placeBet: PlaceBetUseCase) {}

         @Post()
         @HttpCode(202)
         async place(@Req() req: AuthenticatedRequest, @Body() body: PlaceBetRequestDto): Promise<PlaceBetResponseDto> {
           const playerId = PlayerId(req.user!.playerId);
           const amount = Money.fromSnapshot({ amount: body.amountCents, currency: env.CURRENCY_CODE, scale: env.CURRENCY_EXPONENT });
           try {
             const result = await this.placeBet.execute({ playerId, amount, now: new Date() });
             return { betId: result.betId as unknown as string, status: 'PENDING' };
           } catch (err) {
             this.translateError(err);
           }
         }

         private translateError(err: unknown): never {
           if (err instanceof RoundNotInBettingPhaseError) throw new ConflictException({ code: 'ROUND_NOT_IN_BETTING_PHASE', phase: err.actual });
           if (err instanceof BetAlreadyActiveError) throw new ConflictException({ code: 'BET_ALREADY_ACTIVE', existingBetId: err.existingBetId });
           if (err instanceof BetAmountOutOfBoundsError) throw new BadRequestException({ code: 'BET_AMOUNT_OUT_OF_BOUNDS' });
           throw err;
         }
       }
       ```
       Note: the `@Post()` with no path argument lives at the controller root `/games/bet`. Cashout endpoint lives in plan 05-06 — do NOT add it here.
    4. In `game-core.module.ts`:
       - Add `PlaceBetUseCase` to providers AND to exports (so the controller — which lives in app.module's controllers list — can resolve it via module composition; alternatively register the controller inside GameCoreModule controllers — match the pattern Phase 4 used for BetsController).
    5. In `app.module.ts`:
       - Add `BetCommandController` to the `controllers: []` array (mirror how `BetsController` is currently registered).
    6. Do NOT add Kong route here — that is plan 05-08's job. Live verification deferred to plan 05-10.
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bunx tsc --noEmit &amp;&amp; bun test tests/unit</automated>
  </verify>
  <done>
    - Controller mounted at `games/bet` (singular), POST returns 202 status code via @HttpCode(202).
    - DTOs use zod via createZodDto + the existing ZodValidationPipe.
    - All three domain errors translate to the discriminated 409/400 payloads.
    - bun test tests/unit green (no regressions; new unit tests from Task 1 pass).
    - bunx tsc --noEmit clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Kong → games-service | Untrusted POST body; JWT validated by JwtGuard via cached JWKS |
| games-service → outbox/RabbitMQ | Trusted; envelope signed by buildEnvelope; same-TX write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-S | Spoofing | Client-supplied playerId in body | mitigate | Controller uses `req.user!.playerId` from JWT, never body.playerId. DTO has no playerId field. |
| T-05-04-T | Tampering | Negative/oversized amount | mitigate | zod schema parses non-negative bigint; BetAmount VO + BET_MIN/MAX_CENTS bounds in use case; DB CHECK constraint (Phase 4) is the last line of defense. |
| T-05-04-A | Authentication bypass | POST without JWT | mitigate | `@UseGuards(JwtGuard)` mandatory; smoke probe 34 (plan 05-10) verifies 401 without bearer. |
| T-05-04-R | Replay | Double-POST creating two bets | mitigate | `bets_one_active_per_player` partial unique index (Phase 4) + use case's `findActiveByRoundAndPlayer` pre-check; concurrent INSERTs raise SQLSTATE 23505 → translate to BetAlreadyActiveError 409. |
</threat_model>

<verification>
- `bun test tests/unit/place-bet.use-case.test.ts` — 6 behavior tests green.
- `bunx tsc --noEmit` — clean.
- `bun test tests/unit` — full suite green (no regressions).
- Live POST /games/bet smoke verification deferred to plan 05-10 (needs Kong + saga handlers from plans 05-05/05-06/05-07/05-08).
</verification>

<success_criteria>
- POST /games/bet exists, JwtGuard-protected, returns 202 with `{ betId, status: 'PENDING' }`.
- All three error paths (no-round / wrong-phase / double-bet) return ConflictException 409 with discriminated codes.
- Single em.transactional spans bet + saga + outbox writes (verifiable by inspecting bets / bet_saga_state / outbox row counts after a POST).
</success_criteria>

<output>
Create `.planning/phases/05-saga-integration/05-04-SUMMARY.md` when done.
</output>
