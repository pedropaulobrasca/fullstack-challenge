---
phase: 05-saga-integration
plan: 08
type: execute
wave: 6
depends_on:
  - 05-04
  - 05-06
files_modified:
  - docker/kong/kong.yml
autonomous: true
requirements:
  - REQ-GAME-06
  - REQ-GAME-07
must_haves:
  truths:
    - "Kong's games-service block declares exactly six routes after this plan: games-current, games-history, games-verify, games-bets-me, games-bet-place, games-bet-cashout."
    - "Each new route is method-constrained (POST only) and PCRE-anchored (~/games/bet$ and ~/games/bet/cashout$)."
    - "POST /games/bet at Kong forwards to games:4001."
    - "POST /games/bet/cashout at Kong forwards to games:4001."
    - "Mutation paths NOT in this list (e.g. PUT /games/bet, DELETE /games/bet) return Kong-origin 404."
  artifacts:
    - path: docker/kong/kong.yml
      provides: "Two new POST routes (games-bet-place, games-bet-cashout) on the games-service block"
      contains: "games-bet-place"
  key_links:
    - from: docker/kong/kong.yml
      to: services/games/src/presentation/controllers/bet-command.controller.ts
      via: "Kong route /games/bet$ + /games/bet/cashout$ -> http://games:4001 -> BetCommandController"
      pattern: "games:4001"
---

<objective>
Open Kong's gateway to the two new POST endpoints from plans 05-04 and 05-06. Routes use PCRE-anchored regex paths (`~/games/bet$`, `~/games/bet/cashout$`) and method constraints (`POST`) so that mutation methods on other paths (or other methods on these paths) return Kong-origin 404 — the same pattern that closed REQ-WALL-04 perimeter in P3.07.

The cashout path `/games/bet/cashout` must be declared BEFORE the placement path `/games/bet` in the routes list so Kong's regex matcher tries the more specific route first. (Actually Kong's PCRE matcher uses path-length-based priority so order is not strictly required, but ordering by specificity is a readability/maintenance contract — document this in a comment if non-obvious.)

Output: Updated `docker/kong/kong.yml`. Live perimeter verification (allowed methods/paths forward; everything else returns 404) is plan 05-10's smoke probe matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-saga-integration/05-RESEARCH.md
@docker/kong/kong.yml

<interfaces>
From docker/kong/kong.yml (current state — 4 games routes, 2 wallets routes):
- games-current: ~/games/rounds/current$, GET
- games-history: ~/games/rounds/history$, GET
- games-verify: ~/games/rounds/[^/]+/verify$, GET
- games-bets-me: ~/games/bets/me$, GET

Wallets unchanged: wallets-provision (POST ~/wallets$), wallets-me (GET ~/wallets/me$).

Phase 3 P3.07 (commit fdd5ec5) documented why plain prefix paths leak — Kong forwards POST /wallets/me/debit to the service if path is /wallets prefix. PCRE-anchored regex with $ terminator is the only reliable shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add games-bet-place + games-bet-cashout PCRE routes to Kong</name>
  <files>docker/kong/kong.yml</files>
  <read_first>
    - docker/kong/kong.yml (entire file — current 4 routes + 2 wallets routes)
  </read_first>
  <action>
    1. Edit the games-service `routes:` list (currently 4 entries: games-current, games-history, games-verify, games-bets-me). Append two new entries at the END of that list (or place them adjacent — order does not affect Kong's regex match priority because regex paths in Kong are matched by priority/length; document the chosen order with a YAML comment if it deviates from the existing style):
       ```yaml
       - name: games-bet-cashout
         paths:
           - ~/games/bet/cashout$
         methods:
           - POST
         strip_path: false
       - name: games-bet-place
         paths:
           - ~/games/bet$
         methods:
           - POST
         strip_path: false
       ```
       Placing `games-bet-cashout` first is a maintenance/readability convention only — Kong's regex priority handles disambiguation via path-length-based scoring; both anchored ($ terminator) paths are mutually exclusive anyway. Confirm with the existing style: research §"Updated Kong block" lists `games-bet-place` first then `games-bet-cashout` — match research order if there is no other constraint.
    2. Do NOT touch any other section. Do NOT modify the wallets-service block. Do NOT modify the four existing games routes.
    3. Verify the YAML file is still valid (e.g. `yq` or `python -c "import yaml; yaml.safe_load(open('docker/kong/kong.yml'))"`); record the exact validation invocation in the verify step.
  </action>
  <verify>
    <automated>python3 -c "import yaml; cfg = yaml.safe_load(open('docker/kong/kong.yml')); routes = [r['name'] for r in cfg['services'][0]['routes']]; assert 'games-bet-place' in routes and 'games-bet-cashout' in routes and len(routes) == 6, routes"</automated>
  </verify>
  <done>
    - `docker/kong/kong.yml` parses as valid YAML.
    - The games-service block has exactly 6 routes: the original 4 + the 2 new POST routes.
    - Both new routes use `~/games/bet$` and `~/games/bet/cashout$` PCRE patterns with `methods: [POST]`.
    - No changes to wallets-service block.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → Kong → games-service | Kong is the perimeter; PCRE-anchored paths + method constraints minimize attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-S | Spoofing | Path traversal to other services | mitigate | PCRE $ terminator + dedicated route per path prevents `/games/bet/../wallets` style attacks at Kong. |
| T-05-08-M | Tampering | Unexpected HTTP methods on bet path | mitigate | `methods: [POST]` constraint — PUT/PATCH/DELETE return Kong-origin 404. Smoke probe 33 (plan 05-10) verifies. |
| T-05-08-X | Information Disclosure | Kong admin API exposure | accept | Out of scope — admin API binding is configured in Phase 1 docker-compose. |
</threat_model>

<verification>
- YAML parse + route name assertion via python3 inline script (above).
- Live perimeter probe matrix (POST allowed, other methods 404) is plan 05-10 smoke probes.
</verification>

<success_criteria>
- Kong proxy will route POST /games/bet and POST /games/bet/cashout to games-service when the stack is brought up.
- All other method/path combinations on the bet paths are rejected at Kong.
</success_criteria>

<output>
Create `.planning/phases/05-saga-integration/05-08-SUMMARY.md` when done.
</output>
