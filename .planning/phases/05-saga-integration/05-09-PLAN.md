---
phase: 05-saga-integration
plan: 09
type: execute
wave: 7
depends_on:
  - 05-05
  - 05-06
  - 05-07
  - 05-08
files_modified:
  - services/games/tests/integration/place-bet.test.ts
  - services/games/tests/integration/saga-insufficient-funds.test.ts
  - services/games/tests/integration/saga-timeout-and-compensation.test.ts
  - services/games/tests/integration/cash-out.test.ts
  - services/games/tests/integration/bet-outside-betting.test.ts
  - services/games/tests/integration/double-cashout.test.ts
  - services/games/tests/integration/kill-9-saga-recovery.test.ts
autonomous: true
requirements:
  - REQ-TEST-03
  - REQ-TEST-04
must_haves:
  truths:
    - "place-bet.test.ts asserts the full bet → wallet.debit → wallet.debited → bet.ACTIVE round trip within SAGA_TIMEOUT_MS (REQ-SAGA-01 happy path)."
    - "saga-insufficient-funds.test.ts asserts the rejection path: bet → wallet.debit.rejected{INSUFFICIENT_FUNDS} → bet.REFUNDED + saga.REFUNDED."
    - "saga-timeout-and-compensation.test.ts asserts both the timeout refund AND the compensation branch when wallet.debited arrives AFTER timeout."
    - "cash-out.test.ts asserts bet → ACTIVE → cashout 200 → bet.CASHED_OUT + downstream wallet credit (eventual)."
    - "bet-outside-betting.test.ts asserts POST /games/bet outside BETTING returns 409 ROUND_NOT_IN_BETTING_PHASE."
    - "double-cashout.test.ts asserts second cashout returns 409 (NO_ACTIVE_BET or BET_NOT_CASHABLE depending on race outcome)."
    - "kill-9-saga-recovery.test.ts asserts a true `docker compose kill -s SIGKILL games` mid-saga reaches a consistent terminal state on restart (REQ-TEST-04)."
  artifacts:
    - path: services/games/tests/integration/place-bet.test.ts
      provides: "End-to-end bet happy-path coverage"
      contains: "wallet.debited"
    - path: services/games/tests/integration/saga-timeout-and-compensation.test.ts
      provides: "Compensation branch coverage — the trickiest correctness point"
      contains: "COMPENSATED"
    - path: services/games/tests/integration/kill-9-saga-recovery.test.ts
      provides: "REQ-TEST-04 SIGKILL drill"
      contains: "SIGKILL"
  key_links:
    - from: services/games/tests/integration/saga-timeout-and-compensation.test.ts
      to: services/games/src/application/handlers/wallet-debited.handler.ts
      via: "asserts the TIMED_OUT branch emits wallet.credit and transitions saga to COMPENSATED"
      pattern: "COMPENSATED"
---

<objective>
Land the integration test suite that proves REQ-TEST-03 (E2E API happy + error paths) and REQ-TEST-04 (kill-9 saga recovery). All seven scenarios run against the live docker stack via the Phase 4 test-env helpers (`_helpers/test-env.ts`, `_helpers/app-factory.ts`).

The compensation scenario is the highest-value test in the suite — it exercises the TIMED_OUT → COMPENSATED transition in `WalletDebitedHandler` (plan 05-05), which is the only path that prevents silent balance loss when Wallet succeeded but games didn't see the reply in time. The kill-9 scenario carries forward the P4.11 SIGKILL pattern (`docker compose kill -s SIGKILL games`) rather than relying on in-process `app.close()` which is not equivalent.

Output: 7 integration test files. Each is gated by `INTEGRATION=1` and requires `bun run docker:up`. Smoke probes 33-38 are NOT included here — they ship in plan 05-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-saga-integration/05-RESEARCH.md
@services/games/tests/integration/_helpers/test-env.ts
@services/games/tests/integration/_helpers/app-factory.ts
@services/games/tests/integration/seed-chain-bootstrap.test.ts
@services/games/tsconfig.integration.json

<interfaces>
From Phase 4 P4.10 test infrastructure:
- `_helpers/test-env.ts` — env seeder + Keycloak password-grant token fetch via `get_token('player', 'player123')`.
- `_helpers/app-factory.ts` — `Test.createTestingModule({ imports: [AppModule] }).compile()` + truncate helpers + polling primitives like `waitFor(predicate, timeoutMs)`.
- INTEGRATION=1 gate convention.

From plan 05-04: POST /games/bet at Kong; @Body schema requires { amountCents: string }.
From plan 05-06: POST /games/bet/cashout; no body.
From plan 05-05: WalletDebitedHandler with two branches (CONFIRM, TIMED_OUT-compensation).
From plan 05-07: SagaTimeoutSweeper polling every SAGA_SWEEP_INTERVAL_MS.

Wallet endpoint: GET /wallets/me (Phase 3) returns balance MoneySnapshot.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three foundational scenarios — happy path, insufficient funds, bet outside BETTING</name>
  <files>services/games/tests/integration/place-bet.test.ts, services/games/tests/integration/saga-insufficient-funds.test.ts, services/games/tests/integration/bet-outside-betting.test.ts</files>
  <read_first>
    - services/games/tests/integration/_helpers/test-env.ts (token fetch helper signatures)
    - services/games/tests/integration/_helpers/app-factory.ts (waitFor + truncate helpers)
    - services/games/tests/integration/seed-chain-bootstrap.test.ts (canonical Phase 4 integration test shape — beforeAll/afterAll/describe layout)
  </read_first>
  <action>
    1. `place-bet.test.ts` — REQ-GAME-06 + REQ-SAGA-01 happy:
       - beforeAll: bootstrap app, truncate bets / bet_saga_state / outbox / inbox / wallet rows, provision wallet at 1000.00 CRD (POST /wallets via Keycloak token).
       - test: `waitFor` until rounds/current.status === 'BETTING' (poll GET /games/rounds/current). POST /games/bet with `{ amountCents: '10000' }` (100.00 CRD). Assert response status 202 + body `{ betId: <uuid>, status: 'PENDING' }`. Capture `betId`.
       - waitFor: GET /games/bets/me returns the bet with status === 'ACTIVE' (poll up to SAGA_TIMEOUT_MS + 2s grace). Also assert DB-side `bet_saga_state.status === 'CONFIRMED'` for that betId (use mikro orm em from the test app, raw SQL fine).
       - Assert GET /wallets/me balance amount === '99000' (10000 cent debit applied — eventual; poll up to grace).
    2. `saga-insufficient-funds.test.ts` — REQ-SAGA-01 rejection:
       - beforeAll: provision wallet at 50.00 (5000 cents).
       - test: waitFor BETTING. POST /games/bet with amountCents='10000'. Assert 202 PENDING.
       - waitFor: GET /games/bets/me bet.status === 'REFUNDED' and refundReason ⊇ 'INSUFFICIENT_FUNDS'; DB saga.status === 'REFUNDED'; GET /wallets/me balance unchanged === '5000'.
    3. `bet-outside-betting.test.ts` — REQ-GAME-08 (Phase 5 surface):
       - beforeAll: provision wallet 1000.00.
       - test: waitFor rounds/current.status === 'RUNNING'. POST /games/bet with amountCents='10000'. Assert 409 + body `{ code: 'ROUND_NOT_IN_BETTING_PHASE', phase: 'RUNNING' }`.
       - Same test or sub-test: when no round is open (NO_OPEN_ROUND scenario is hard to force; document as covered by use-case unit test from plan 05-04 instead and OMIT here).
    4. All three tests gated by `if (!process.env.INTEGRATION) { test.skip(); return; }` at the describe boundary — match the existing Phase 4 integration test convention. Use the test-env + app-factory helpers; do NOT spawn fresh Postgres containers (the live `docker:up` stack is used).
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bunx tsc --noEmit -p tsconfig.integration.json</automated>
  </verify>
  <done>
    - Three test files compile under `tsconfig.integration.json`.
    - Files follow the Phase 4 integration test shape (describe + beforeAll + truncate + waitFor + assert).
    - Live execution gated to plan 05-10 smoke checkpoint (which runs INTEGRATION=1 after `docker:up`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Timeout + compensation + cashout + double-cashout scenarios</name>
  <files>services/games/tests/integration/saga-timeout-and-compensation.test.ts, services/games/tests/integration/cash-out.test.ts, services/games/tests/integration/double-cashout.test.ts</files>
  <read_first>
    - services/games/tests/integration/place-bet.test.ts (just-created — share idioms)
    - services/games/tests/integration/_helpers/app-factory.ts (any test-hook to pause wallets consumer; if none, use docker network-level isolation OR direct AMQP binding manipulation)
  </read_first>
  <action>
    1. `saga-timeout-and-compensation.test.ts` — REQ-SAGA-03 + compensation branch (THE critical correctness test):
       - This scenario requires temporarily blocking the wallets-service consumer so that wallet.debit is NOT processed in time, then unblocking it AFTER the sweeper has refunded. Implementation options (pick ONE based on which the existing test infra supports — check `_helpers` first):
         a. **AMQP binding manipulation** (preferred): use `amqplib` directly in the test to unbind the `wallet.debit.q` queue from `wallet.commands` exchange before the test, then rebind after the sweeper has run. The wallets-service WalletDebitHandler simply receives nothing until rebind. After rebind, the queued (or re-published) message lands and `wallet.debited` fires → games' WalletDebitedHandler sees `saga.status === 'TIMED_OUT'` → emits compensation → saga.status='COMPENSATED'.
         b. **docker compose stop wallets** (simpler but coarser): `docker compose stop wallets` before POST /games/bet, wait > SAGA_TIMEOUT_MS, `docker compose start wallets`. Risk: bun:test runner cannot easily exec docker; would need a child_process.spawn shell call. Document the constraint and prefer option (a) if amqplib is already in test deps.
       - Sub-test 1 (timeout-only): block wallets consumer, POST bet 100, waitFor (SAGA_TIMEOUT_MS + sweep interval + grace) until bet.status === 'REFUNDED' with refundReason ⊇ 'SAGA_TIMEOUT' and saga.status === 'TIMED_OUT'. Wallet balance unchanged.
       - Sub-test 2 (compensation): same setup but AFTER the timeout refund completes, unblock the wallets consumer (or rebind the queue). waitFor: saga.status === 'COMPENSATED'. Assert an outbox row of type='wallet.credit' was written for that correlationId. Assert wallet balance is eventually back at original (wallets debited THEN credited via compensation; or wallets never debited because broker replay handled it — both terminal states are acceptable, but the test verifies the saga reaches COMPENSATED and the player's balance is whole).
    2. `cash-out.test.ts` — REQ-GAME-07 + REQ-SAGA-04:
       - beforeAll: wallet 1000.00.
       - test: waitFor BETTING. POST /games/bet 100.00. waitFor bet → ACTIVE. waitFor rounds/current.status === 'RUNNING'. POST /games/bet/cashout. Assert 200 + body `{ multiplier: <number > 1>, payoutCents: { amount: <string >= '10000'>, currency: 'CRD', scale: 2 } }`. waitFor bet.status === 'CASHED_OUT'. waitFor GET /wallets/me balance includes the credit (balance >= 900 + payout).
    3. `double-cashout.test.ts` — error path:
       - Same setup as cash-out happy. After successful first cashout, immediately POST /games/bet/cashout again. Assert 409 + body code ∈ { 'NO_ACTIVE_BET', 'BET_NOT_CASHABLE' } (depending on whether the use case detects via findActiveByRoundAndPlayer returning null vs the bet existing but not ACTIVE; document both as acceptable terminal codes).
    4. Same INTEGRATION=1 gate, same helper conventions.
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bunx tsc --noEmit -p tsconfig.integration.json</automated>
  </verify>
  <done>
    - Three test files compile.
    - Compensation test file CLEARLY documents the wallets-consumer-blocking technique in a comment block at the top.
    - Live execution deferred to plan 05-10.
  </done>
</task>

<task type="auto">
  <name>Task 3: True-SIGKILL saga recovery test (REQ-TEST-04)</name>
  <files>services/games/tests/integration/kill-9-saga-recovery.test.ts</files>
  <read_first>
    - services/games/tests/integration/place-bet.test.ts (token + bet setup from Task 1)
    - .planning/STATE.md line on P4.11 — the canonical SIGKILL drill: `docker compose kill -s SIGKILL games` then container restart, assert state consistency.
  </read_first>
  <action>
    1. `kill-9-saga-recovery.test.ts`:
       - Skip if INTEGRATION=1 is unset OR if `docker` CLI is unavailable in PATH (run `which docker` via child_process).
       - Scenario A — kill AFTER wallet.debited but BEFORE games-side handler runs:
         a. Provision wallet 1000.00. waitFor BETTING.
         b. POST /games/bet 100. Capture betId.
         c. Immediately `docker compose kill -s SIGKILL games` via child_process. The wallets-service is untouched and will process the wallet.debit asynchronously; the broker will retry delivery to the (now-dead) games queue or land it in DLQ depending on x-delivery-limit. (Document the broker behavior: with quorum queue and `x-delivery-limit=5`, the message will retry up to 5 times then DLQ. Since games-service is dead the consumer doesn't ack and the message stays in `games.wallet-events.q` until games restarts.)
         d. `docker compose start games` (or `docker compose up -d games`). waitFor health endpoint.
         e. waitFor (up to SAGA_TIMEOUT_MS + sweep interval + grace) until the saga reaches a consistent terminal state. Accept any of: `{ saga.status='CONFIRMED', bet.status='ACTIVE' }`, `{ saga.status='TIMED_OUT', bet.status='REFUNDED' }`, `{ saga.status='COMPENSATED', bet.status='REFUNDED', wallet eventually restored }`. ASSERT the state is one of these three terminal shapes — never a stuck PENDING + DEBIT_PENDING.
       - Scenario B — kill BEFORE wallet processes:
         a. Provision wallet 1000.00. waitFor BETTING.
         b. Block wallets consumer via the technique from Task 2 (binding unbind).
         c. POST /games/bet 100.
         d. `docker compose kill -s SIGKILL games`.
         e. Restart games. Rebind wallets consumer.
         f. waitFor: same terminal-state assertions as Scenario A. Outbox poll will re-publish the wallet.debit envelope to RMQ post-restart (Phase 2 contract); wallets processes; either confirms in time (CONFIRMED) or sweeper times out and compensation fires.
       - Both scenarios MUST run sequentially within the same describe block; afterAll truncates state.
       - Document SIGKILL caveat at the top of the file: "If `docker` CLI is unavailable, this test self-skips. The P4.11 pattern is canonical; in-process `app.close()` is NOT equivalent because it allows graceful shutdown handlers to run."
    2. Use `child_process.spawnSync('docker', ['compose', 'kill', '-s', 'SIGKILL', 'games'])` with `cwd` pointing at the repo root.
    3. Health-check polling: after restart, poll `http://localhost:8000/games/rounds/current` (Kong route from Phase 4) until 200 OK before continuing.
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bunx tsc --noEmit -p tsconfig.integration.json</automated>
  </verify>
  <done>
    - File compiles.
    - Self-skips when docker CLI missing.
    - Two scenarios (kill-after-debit, kill-before-debit) cover the two race windows REQ-TEST-04 specifies.
    - Live execution deferred to plan 05-10 (which will also document SIGKILL drill results in the SUMMARY).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → docker daemon | Local-only; running tests has full host access — accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-D | DoS | Tests leave wallets-consumer in broken state on crash | mitigate | afterEach/afterAll always rebinds queues + clears blocking state; tests truncate DB tables on entry. |
| T-05-09-FLAKE | (not security) | Sweeper interval timing race in tests | mitigate | Use `waitFor(predicate, SAGA_TIMEOUT_MS * 3)` grace; document the timing budget at the top of each timeout-sensitive test. |
</threat_model>

<verification>
- `bunx tsc --noEmit -p tsconfig.integration.json` clean.
- Live `INTEGRATION=1 bun test tests/integration` execution is plan 05-10's job (after docker:up + smoke probes).
</verification>

<success_criteria>
- REQ-TEST-03 satisfied: happy + 4 error paths (insufficient funds, bet-outside-betting, double-cashout, timeout/compensation) covered.
- REQ-TEST-04 satisfied: true-SIGKILL drill scripted; runs against live docker stack.
- Compensation branch has dedicated test that exercises both timeout AND late-debited-after-timeout transitions to COMPENSATED.
</success_criteria>

<output>
Create `.planning/phases/05-saga-integration/05-09-SUMMARY.md` when done.
</output>
