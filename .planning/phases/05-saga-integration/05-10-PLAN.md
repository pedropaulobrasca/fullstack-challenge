---
phase: 05-saga-integration
plan: 10
type: execute
wave: 8
depends_on:
  - 05-08
  - 05-09
files_modified:
  - scripts/smoke-health.sh
autonomous: false
requirements:
  - REQ-GAME-06
  - REQ-GAME-07
must_haves:
  truths:
    - "scripts/smoke-health.sh extends from 32 probes to 38 probes."
    - "Probe 33 confirms POST /games/bet outside BETTING returns 409 with ROUND_NOT_IN_BETTING_PHASE."
    - "Probe 34 confirms POST /games/bet during BETTING returns 202 PENDING and the bet eventually shows in /games/bets/me as ACTIVE or REFUNDED within SAGA_TIMEOUT_MS + grace."
    - "Probe 35 confirms POST /games/bet/cashout during RUNNING returns 200 with multiplier > 1.0 and payoutCents shape."
    - "Probe 36 confirms wallet balance decreases by bet amount after a successful bet confirmation."
    - "Probe 37 confirms wallet balance is credited by payoutCents after a successful cashout (eventually consistent — polling)."
    - "Probe 38 confirms POST /games/bet/cashout without an ACTIVE bet returns 409 with NO_ACTIVE_BET (or BET_NOT_CASHABLE)."
    - "Full smoke run shows 38/38 PASS against the live docker stack."
    - "Live integration suite INTEGRATION=1 bun test tests/integration passes (8/8 scenarios green) including the kill-9 SIGKILL drill."
  artifacts:
    - path: scripts/smoke-health.sh
      provides: "Six new probes 33-38 covering Phase 5 surface end-to-end at Kong perimeter"
      contains: "probe_games_bet_place_happy"
  key_links:
    - from: scripts/smoke-health.sh
      to: docker/kong/kong.yml
      via: "Probes hit http://localhost:8000 (Kong) — exercises the routes added in plan 05-08"
      pattern: "localhost:8000"
---

<objective>
Land the smoke probe matrix that proves the Phase 5 surface works end-to-end against the live docker stack, and run the full integration suite to capture the SIGKILL drill evidence. This plan has a BLOCKING `checkpoint:human-verify` task at the end because Phase 5 ships the first cross-service saga and the recruiter's arguição will exercise it live — a human must observe a successful bet → cashout → balance update cycle before the phase is declared complete.

Probes 33-38 follow the P4.11 pattern: bash-driven HTTP probes against `http://localhost:8000` (Kong) using the player Keycloak token from probe 19. Each probe is one shell function returning PASS / FAIL with diagnostic curl output.

Output: 6 new probes appended to `scripts/smoke-health.sh`; live smoke run 38/38 PASS captured in the plan SUMMARY; live integration suite green; SIGKILL drill outcome recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-saga-integration/05-RESEARCH.md
@scripts/smoke-health.sh

<interfaces>
From scripts/smoke-health.sh (current — 32 probes):
- get_token helper (probe 19 fetches player/player123 access token via Keycloak password grant)
- probe_* functions returning PASS/FAIL
- Kong base URL http://localhost:8000
- jq for JSON assertion

From plans 05-04, 05-06, 05-08:
- POST /games/bet returns 202 / 409.
- POST /games/bet/cashout returns 200 / 409.

From SAGA_TIMEOUT_MS=5000 + SAGA_SWEEP_INTERVAL_MS=1000:
- Bet confirmation polling budget: ~6-8s (timeout + sweep tick + grace).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add probes 33-38 to scripts/smoke-health.sh</name>
  <files>scripts/smoke-health.sh</files>
  <read_first>
    - scripts/smoke-health.sh (entire file — current 32 probes, helper functions, output formatting, exit-status accumulation)
  </read_first>
  <action>
    1. Append 6 new probe functions in the order specified in research §"Smoke probes 33-38". Each probe:
       a. Fetches the player token via `get_token` (idempotent).
       b. Issues the documented HTTP call against `http://localhost:8000`.
       c. Asserts response status + JSON body shape via `jq`.
       d. On failure prints the response body for diagnostics.
       e. Updates the pass/fail counter using the existing convention.
    2. Probe contracts:
       - Probe 33 — `probe_games_bet_place_outside_betting`: POST /games/bet during RUNNING — assert 409 + `.code == "ROUND_NOT_IN_BETTING_PHASE"`. Strategy: poll GET /games/rounds/current; when status === RUNNING fire the POST; retry up to 3x if round flips.
       - Probe 34 — `probe_games_bet_place_happy`: poll for BETTING, POST /games/bet with body `{ amountCents: "10000" }`, assert 202 + `.status == "PENDING"` + `.betId` is uuid. Capture betId. Then poll GET /games/bets/me up to 8s; assert one bet matches betId AND its status is one of ACTIVE / REFUNDED.
       - Probe 35 — `probe_games_bet_cashout_during_running`: needs an ACTIVE bet. Wait for RUNNING. POST /games/bet/cashout. Assert 200 + `.multiplier > 1.0` + `.payoutCents.amount` parses to bigint string + `.payoutCents.scale == 2`.
       - Probe 36 — `probe_games_balance_decreased_after_bet`: GET /wallets/me before placing bet, capture `.balance.amount`. Place a bet. Poll GET /wallets/me until balance amount decreased by the bet's cent value (timeout 8s).
       - Probe 37 — `probe_games_balance_credited_after_cashout`: GET /wallets/me before cashout, capture amount. Cashout. Poll GET /wallets/me until amount increases by at least the payoutCents (budget 5s grace for downstream credit).
       - Probe 38 — `probe_games_bet_cashout_without_active`: ensure no ACTIVE bet. POST /games/bet/cashout. Assert 409 + `.code` in `{"NO_ACTIVE_BET", "BET_NOT_CASHABLE", "ROUND_NOT_RUNNING"}` (document all three as acceptable depending on round phase at probe time).
    3. Register each new probe in the master probe list at the bottom of the script (the existing for-loop driver). Update the total expected count from 32 to 38.
    4. Do NOT modify probes 1-32. Do NOT touch any other file in this task.
  </action>
  <verify>
    <automated>bash -n scripts/smoke-health.sh &amp;&amp; grep -c "probe_games_bet_place_happy" scripts/smoke-health.sh | awk '$1 &gt;= 1 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    - scripts/smoke-health.sh syntax-checks clean.
    - All 6 new probe function names present.
    - The driver loop expects 38 probes total.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live smoke + integration walkthrough (BLOCKING human verify)</name>
  <what-built>
    Phase 5 ships POST /games/bet (202) + POST /games/bet/cashout (200) end-to-end across games and wallets services with persistent saga state, timeout refund, and compensation. Integration tests cover happy + 4 error paths + true-SIGKILL drill. Smoke probes 33-38 cover the same surface at Kong perimeter.
  </what-built>
  <how-to-verify>
    1. Ensure `bun run docker:down &amp;&amp; bun run docker:prune` then `bun run docker:up` from a clean slate. Wait for all healthchecks green.
    2. Run `bun run smoke:health` — expect 38/38 PASS. If any probe fails, paste the diagnostic output.
    3. Run `cd services/games &amp;&amp; INTEGRATION=1 bun test tests/integration` — expect all 8 scenarios green (place-bet, saga-insufficient-funds, saga-timeout-and-compensation, cash-out, bet-outside-betting, double-cashout, kill-9-saga-recovery [2 sub-tests], plus the carryover Phase 4 integration tests). Note duration; expect &gt;1 minute due to saga timeouts + SIGKILL restart.
    4. Manual flow probe via curl + jq (use the `get_token` helper or run probe 19 first):
       a. POST /wallets to provision (200 or 409 idempotent).
       b. GET /wallets/me — capture starting balance.
       c. Wait for /games/rounds/current.status === 'BETTING'. POST /games/bet with amountCents='10000'. Expect 202 + betId + status PENDING.
       d. Wait for /games/bets/me to show that bet as ACTIVE (poll, &lt;6s typical).
       e. Wait for /games/rounds/current.status === 'RUNNING'. POST /games/bet/cashout. Expect 200 + multiplier &gt; 1.0 + payoutCents.
       f. GET /wallets/me — expect balance higher than the value captured in step (b) by `payout - bet` (net win, since multiplier &gt; 1.0).
    5. Inspect `bet_saga_state` table directly via `docker compose exec postgres psql ...`. Confirm at least one row with status='CONFIRMED' from the happy path; if the suite included a timeout test, also confirm rows with status='TIMED_OUT' and/or 'COMPENSATED'.
    6. Inspect `outbox` table: confirm rows of type 'wallet.debit', 'wallet.debited', 'bet.active', 'wallet.credit', 'wallet.credited', and (when timeout was exercised) 'bet.refunded'.
    7. Inspect `wallets` service logs: confirm `WalletDebitHandler` and `WalletCreditHandler` processed the relevant messages; no DLX entries.
    8. SIGKILL drill confirmation: re-read the test output for `kill-9-saga-recovery.test.ts` — assert both sub-tests passed (kill-after-debit and kill-before-debit) with terminal state assertions green.
    9. If ANY probe / test / manual flow fails, capture: (a) the failing assertion, (b) the relevant service logs (`docker compose logs games wallets --tail=200`), (c) the bet_saga_state row state, (d) RabbitMQ queue depths (`docker compose exec rabbitmq rabbitmqctl list_queues name messages messages_dlq`).
  </how-to-verify>
  <resume-signal>
    Type "approved" with the smoke pass count and integration suite outcome (e.g. "approved — smoke 38/38, integration 8/8, SIGKILL drill 2/2"), OR describe failures with the diagnostic bundle from step 9.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live stack | Smoke + manual probes; running locally; full host access — accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-OBS | Information | Probe logs may leak betId / playerId | accept | Local-only diagnostics; not committed to repo. |
</threat_model>

<verification>
- bash -n scripts/smoke-health.sh syntactically clean.
- All six new probe names present.
- bun run smoke:health prints 38/38 PASS (recorded in SUMMARY).
- INTEGRATION=1 bun test tests/integration green (recorded in SUMMARY).
</verification>

<success_criteria>
- 38/38 smoke probes green against a fresh docker:up.
- 8/8 integration scenarios green (including SIGKILL drill sub-tests).
- Manual bet → cashout → balance-delta curl flow demonstrated and approved.
</success_criteria>

<output>
Create `.planning/phases/05-saga-integration/05-10-SUMMARY.md` when done. Include: smoke result count, integration suite outcome, SIGKILL drill outcome, sample bet_saga_state rows showing CONFIRMED + (if exercised) TIMED_OUT / COMPENSATED rows.
</output>
