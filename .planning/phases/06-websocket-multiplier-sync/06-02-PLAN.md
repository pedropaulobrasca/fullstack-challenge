---
phase: 06-websocket-multiplier-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/games/package.json
  - bun.lock
  - services/games/src/config/defaults.ts
  - .env.example
autonomous: false
requirements:
  - REQ-WS-01
  - REQ-WS-02
  - REQ-WS-03
  - REQ-WS-04
  - REQ-WS-06
must_haves:
  truths:
    - "services/games declares @nestjs/websockets, @nestjs/platform-socket.io, socket.io, @nestjs/event-emitter as production dependencies and socket.io-client as a devDependency"
    - "bun.lock is rewritten and committed alongside package.json"
    - "env.WS_PATH default ('/ws') is exported from services/games/src/config/defaults.ts"
    - "Each new package has been blocking-human-verified against its canonical GitHub repository per Package Legitimacy Audit"
  artifacts:
    - path: "services/games/package.json"
      provides: "Pinned WebSocket stack for Phase 6"
      contains: "@nestjs/websockets"
    - path: "services/games/src/config/defaults.ts"
      provides: "Typed WS_PATH env constant"
      contains: "WS_PATH"
    - path: ".env.example"
      provides: "Documented WS_PATH default"
      contains: "WS_PATH"
  key_links:
    - from: "services/games/package.json"
      to: "bun.lock"
      via: "lockfile rewrite"
      pattern: "bun.lock"
---

<objective>
Install the five Phase 6 WebSocket dependencies into `services/games` and add the `WS_PATH` env constant so subsequent plans (06-03 gateway, 06-04 broadcast service, 06-06 bridge consumer) compile and import the canonical packages — without bypassing the Package Legitimacy Audit gate.

Purpose: Phase 6 RESEARCH §Standard Stack identifies five new packages (4 prod, 1 dev). slopcheck was unavailable at research time, so each install must be gated behind a blocking human checkpoint that verifies the npm registry source against the GitHub repo (per planner authority rules + Phase 6 RESEARCH §Package Legitimacy Audit). After approval, run a single `bun add` per dependency category and lock the patch versions.

Output: package.json with five new entries; refreshed bun.lock; `WS_PATH` env constant available service-wide; `.env.example` documents the default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-websocket-multiplier-sync/06-RESEARCH.md
@services/games/package.json
@services/games/src/config/defaults.ts
@.env.example

<interfaces>
<!-- Existing services/games/package.json dependency block already pins @nestjs/* at ^11.1.21, jose at 6.2.3, @golevelup/nestjs-rabbitmq at ^9.0.2 -->
<!-- Phase 6 RESEARCH §Standard Stack — required additions: -->

Production deps to add:
- `@nestjs/websockets@^11.1.21` (match existing @nestjs/* baseline)
- `@nestjs/platform-socket.io@^11.1.21` (match existing @nestjs/* baseline)
- `socket.io@^4.8.1` (per STACK §2.3)
- `@nestjs/event-emitter@^3.0.0` — exact patch must be resolved via `npm view @nestjs/event-emitter version` first (Assumption A9)

Dev dep to add:
- `socket.io-client@^4.8.1` (integration test client)

env.WS_PATH:
- Default: `"/ws"`
- Validation: non-empty string starting with `/`
- Consumer: GameWsGateway @WebSocketGateway({path: env.WS_PATH}) in Plan 06-03
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package Legitimacy Gate — verify five new packages against canonical sources</name>
  <what-built>
    Five packages tagged [ASSUMED] in Phase 6 RESEARCH §Package Legitimacy Audit need manual verification before `bun add`. The slopcheck CLI was unavailable at research time.
  </what-built>
  <how-to-verify>
    Run these five commands and confirm the `repository.url` matches the official GitHub source listed below. Report any mismatch as a hard stop.

    1. `npm view @nestjs/websockets repository` — expect `github.com/nestjs/nest` (monorepo)
    2. `npm view @nestjs/platform-socket.io repository` — expect `github.com/nestjs/nest` (monorepo)
    3. `npm view socket.io repository` — expect `github.com/socketio/socket.io`
    4. `npm view socket.io-client repository` — expect `github.com/socketio/socket.io-client`
    5. `npm view @nestjs/event-emitter repository version` — expect `github.com/nestjs/event-emitter`; also CAPTURE the latest version string to pin the patch level in Task 3

    For each, confirm:
    - npm scope is `@nestjs/*` (official) or `socket.io*` (official org `socketio`)
    - publisher / maintainer is the canonical org (`nestjsofficial` for @nestjs, `darrachequesne`/`socketio` for socket.io packages)
    - the repo URL above resolves to the real GitHub project (open in browser if uncertain)

    Approve only if all five match. Reject and stop the plan if any package looks unofficial — surface the discrepancy with the actual `repository.url` value seen.
  </how-to-verify>
  <resume-signal>Type "approved: nestjs/event-emitter=X.Y.Z" with the actual latest patch from `npm view`, OR describe the discrepancy and reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add WS_PATH env constant and .env.example documentation</name>
  <files>
    services/games/src/config/defaults.ts,
    .env.example,
    services/games/tests/unit/config-defaults.test.ts
  </files>
  <read_first>
    services/games/src/config/defaults.ts,
    .env.example
  </read_first>
  <behavior>
    - env.WS_PATH defaults to "/ws" when unset
    - env.WS_PATH parses any string starting with "/" (non-empty) when set
    - env.WS_PATH rejects empty string or strings not starting with "/" via zod refinement
    - .env.example has a documented `WS_PATH=/ws` entry alongside the existing SERVER_TICK_HZ block
  </behavior>
  <action>
    In `services/games/src/config/defaults.ts`, add a `WS_PATH` zod field to the schema: `z.string().regex(/^\/.+$/, "WS_PATH must start with '/'").default("/ws")` (regex chosen to forbid empty path while keeping `/ws` shape; document the rule inline by name only — no comment block). Place it in the same env block as `SERVER_TICK_HZ` (alphabetical placement is not enforced here; follow the existing file ordering).

    In repo-root `.env.example`, add `WS_PATH=/ws` next to the existing `SERVER_TICK_HZ` entry under a `# Phase 6 — WebSocket gateway` section header line (the only new section header — keep formatting consistent with the existing per-phase grouping convention).

    If `services/games/tests/unit/config-defaults.test.ts` already exists, append the WS_PATH cases; otherwise create it with three cases: default, valid override, invalid (empty or no-leading-slash) throws.
  </action>
  <verify>
    <automated>cd services/games && bunx tsc --noEmit && bun test tests/unit/config-defaults.test.ts</automated>
  </verify>
  <done>
    `grep -c 'WS_PATH' services/games/src/config/defaults.ts` returns at least 2 (schema entry + export); `.env.example` contains the documented line; tsc clean; new test cases pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Install the five WebSocket dependencies</name>
  <files>
    services/games/package.json,
    bun.lock
  </files>
  <read_first>
    services/games/package.json
  </read_first>
  <behavior>
    - After installation: `bun pm ls --filter=@crash/games | grep -E '(nestjs/websockets|nestjs/platform-socket.io|nestjs/event-emitter|socket.io|socket.io-client)'` lists all five
    - bun.lock contains new entries for each package
    - `cd services/games && bunx tsc --noEmit` continues to pass (no peerDep type conflict)
    - The `@nestjs/event-emitter` patch level is the one returned in the checkpoint resume signal — do NOT use `^3.0.0` blindly; pin to the actual latest minor
  </behavior>
  <action>
    From repo root run (uses Bun workspaces, scoped to the games service via `--filter`):
    `bun add --filter=@crash/games @nestjs/websockets@^11.1.21 @nestjs/platform-socket.io@^11.1.21 socket.io@^4.8.1 @nestjs/event-emitter@^X.Y.Z`
    where `X.Y.Z` is the version captured in the Task 1 checkpoint resume signal.

    Then add the dev dependency:
    `bun add --filter=@crash/games -d socket.io-client@^4.8.1`

    Confirm bun.lock changes are limited to additions for these five packages (no transitive upgrades of unrelated trees beyond their peer-deps). If `bun.lock` shows broader diffs, halt and surface the unexpected upgrades — do NOT commit.

    Do NOT modify any source file yet. The gateway, adapter, bridge consumer, and broadcast service are scaffolded in later plans (06-03 through 06-06).
  </action>
  <verify>
    <automated>cd services/games && bunx tsc --noEmit && grep -c '@nestjs/websockets' package.json && grep -c '@nestjs/platform-socket.io' package.json && grep -c 'socket.io' package.json && grep -c '@nestjs/event-emitter' package.json</automated>
  </verify>
  <done>
    package.json contains the five new entries in dependencies/devDependencies; bun.lock rewritten and diff is limited to these packages + their peer-deps; tsc clean against the existing source tree; full games unit suite still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → services/games | Untrusted JavaScript crosses here on install; supply-chain risk gated by Package Legitimacy checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-SC | Tampering | npm installs of 5 new packages | mitigate | Blocking human checkpoint verifies repository.url against canonical GitHub orgs (`nestjs`, `socketio`) before `bun add` runs; all [ASSUMED] tags resolved at checkpoint, no [SLOP] packages permitted |
| T-06-04 | Tampering | env.WS_PATH | mitigate | zod refinement rejects empty or non-`/`-prefixed values; defaults to `/ws` so misconfiguration in production cannot route WS to an arbitrary path |
</threat_model>

<verification>
- `grep -c '@nestjs/websockets' services/games/package.json` >= 1
- `grep -c 'socket.io' services/games/package.json` >= 2 (server + client dev)
- `grep -c 'WS_PATH' services/games/src/config/defaults.ts` >= 2
- `grep -c 'WS_PATH' .env.example` >= 1
- `cd services/games && bunx tsc --noEmit && bun test tests/unit` clean and green
</verification>

<success_criteria>
- Five new packages installed only after Package Legitimacy checkpoint approved
- bun.lock rewritten; no surprise transitive upgrades
- env.WS_PATH typed and consumable by GameWsGateway in Plan 06-03
- No source files under `services/games/src/presentation/` or `services/games/src/application/` modified in this plan
</success_criteria>

<output>
Create `.planning/phases/06-websocket-multiplier-sync/06-02-SUMMARY.md` when done
</output>
