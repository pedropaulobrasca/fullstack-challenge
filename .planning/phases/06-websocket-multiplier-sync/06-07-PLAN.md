---
phase: 06-websocket-multiplier-sync
plan: 07
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - docker/kong/kong.yml
autonomous: true
requirements:
  - REQ-WS-01
must_haves:
  truths:
    - "Kong declares a games-ws route at PCRE pattern ~/ws$ targeting games-service"
    - "The games-ws route is declared FIRST in games-service.routes[] so its specific path wins over any broader matcher"
    - "After kong reload, curl -i -H 'Connection: Upgrade' -H 'Upgrade: websocket' http://localhost:8000/ws does NOT return a Kong-origin 'no Route matched' 404"
    - "Existing six game routes (games-current, games-history, games-verify, games-bets-me, games-bet-place, games-bet-cashout) continue to match and forward unchanged"
  artifacts:
    - path: "docker/kong/kong.yml"
      provides: "Kong DB-less declarative config with WS upgrade route"
      contains: "games-ws"
  key_links:
    - from: "docker/kong/kong.yml"
      to: "games:4001"
      via: "kong service URL"
      pattern: "url: http://games:4001"
---

<objective>
Add a Kong DB-less route `games-ws` that proxies WebSocket upgrade requests at path `~/ws$` to the existing `games-service` upstream. The route declaration is positioned FIRST in `games-service.routes[]` to avoid any PCRE ambiguity with later route patterns.

Purpose: REQ-WS-01 requires WS connections to flow through Kong (the perimeter) as well as direct to `games:4001` in tests. Kong 3.9 handles WS over `http`/`https` protocols transparently — no plugin is required (per RESEARCH §Architecture Patterns + Kong proxying docs).

Output: `docker/kong/kong.yml` with the new games-ws route; existing routes untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-websocket-multiplier-sync/06-RESEARCH.md
@docker/kong/kong.yml

<interfaces>
<!-- Current docker/kong/kong.yml structure (read in planning phase) -->
services:
  - name: games-service
    url: http://games:4001
    routes:
      - name: games-current
        paths: [~/games/rounds/current$]
      - name: games-history
        paths: [~/games/rounds/history$]
      - name: games-verify
        paths: [~/games/rounds/[^/]+/verify$]
      - name: games-bets-me
        paths: [~/games/bets/me$]
      - name: games-bet-place
        paths: [~/games/bet$]
      - name: games-bet-cashout
        paths: [~/games/bet/cashout$]
  - name: wallets-service
    ...

<!-- The new route MUST come first in games-service.routes[] per RESEARCH Pitfall 6 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert games-ws route at top of games-service.routes[]</name>
  <files>
    docker/kong/kong.yml
  </files>
  <read_first>
    docker/kong/kong.yml
  </read_first>
  <behavior>
    - games-service.routes[0] is `games-ws` with paths `[~/ws$]`, protocols `[http, https]`, strip_path: false
    - games-service.routes[1..6] preserve the existing six routes in their current order
    - wallets-service block unchanged
    - YAML still parses (`_format_version: "3.0"` retained at top)
  </behavior>
  <action>
    Edit `docker/kong/kong.yml`. Between the line `routes:` (under `games-service`) and the existing `- name: games-current`, insert (4-space indented under `routes:` — matches existing indentation):

    ```yaml
      - name: games-ws
        paths:
          - ~/ws$
        protocols:
          - http
          - https
        strip_path: false
    ```

    Do NOT include a `methods:` array — WS handshake is GET with Upgrade headers; constraining methods breaks the upgrade. Do NOT include any plugin block. Do NOT alter the existing six game routes. Do NOT alter the wallets-service routes.

    After the edit, the file's games-service routes block should list `games-ws` first followed by the previous six routes in their existing order.
  </action>
  <verify>
    <automated>grep -n 'games-ws' docker/kong/kong.yml && awk '/games-service/,/wallets-service/' docker/kong/kong.yml | grep -E '^\s+- name:' | head -1 | grep -q 'games-ws'</automated>
  </verify>
  <done>
    `awk '/games-service/,/wallets-service/' docker/kong/kong.yml | grep -E '^\s+- name:' | head -1` outputs `- name: games-ws` (confirming it is the FIRST route under games-service); all existing routes preserved; YAML valid (Kong will reject on `docker compose up` if malformed — this is verified live in Plan 06-09 smoke probe 39).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public network → Kong → games:4001 | Kong is the public perimeter; WS upgrade transits via Kong route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-19 | Spoofing | Anonymous WS upgrade | mitigate | Kong forwards the upgrade unchanged to games:4001; the JwtIoAdapter (Plan 06-03) rejects at io.use middleware — Kong does NOT validate JWT (consistent with Phase 3 perimeter policy of services-validate-JWT-themselves) |
| T-06-20 | Tampering | Route shadowing | mitigate | `games-ws` declared at the TOP of routes[] with anchored `~/ws$` PCRE — cannot be shadowed by later routes; verified by smoke probe 39 in Plan 06-09 |
| T-06-21 | Information Disclosure | Token in Kong access log | mitigate | JwtIoAdapter extracts token from `auth.token` payload or `Authorization` header (Plan 06-03 Task 3) — never from query string; Kong logs the request line which contains neither |
</threat_model>

<verification>
- `grep -c 'games-ws' docker/kong/kong.yml` >= 1
- `grep -c '~/ws\$' docker/kong/kong.yml` >= 1
- `awk '/games-service/,/wallets-service/' docker/kong/kong.yml | grep -E '^\s+- name:' | head -1 | grep -c 'games-ws'` returns 1
- Live verification deferred to Plan 06-09 smoke probe 39
</verification>

<success_criteria>
- Kong reloads cleanly on `docker compose restart kong` (verified in Plan 06-09)
- `curl -i http://localhost:8000/ws` reaches games:4001 (returns 400 or 101, NOT Kong 404)
- All six prior game routes continue to forward unchanged
</success_criteria>

<output>
Create `.planning/phases/06-websocket-multiplier-sync/06-07-SUMMARY.md` when done
</output>
