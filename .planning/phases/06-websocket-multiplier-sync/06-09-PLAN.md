---
phase: 06-websocket-multiplier-sync
plan: 09
type: execute
wave: 6
depends_on:
  - 06-08
files_modified:
  - scripts/smoke-health.sh
autonomous: false
requirements:
  - REQ-WS-01
  - REQ-WS-02
  - REQ-WS-03
  - REQ-WS-04
  - REQ-WS-05
  - REQ-WS-06
  - REQ-WS-07
must_haves:
  truths:
    - "Smoke probe 39: GET /ws via Kong with Upgrade: websocket reaches games:4001 (returns 101 Switching Protocols or socket.io engine-specific code — never Kong-origin 404 'no Route matched')"
    - "Smoke probe 40: connecting to /ws via socket.io-client WITHOUT a token causes a connect_error event"
    - "Smoke probe 41: connecting to /ws via socket.io-client WITH a valid Keycloak token receives a round:snapshot event within 3s"
    - "Smoke probe 42: connected client observes ≥ 30 round:tick events during a 2s window of RUNNING phase"
    - "Smoke probe 43: connected client observes the full round:started → round:running → round:crashed → round:settled sequence within 30s"
    - "Smoke probe 44: connected client observing the same player's bet placement also receives bet:my_active within 5s of the 202 response"
    - "Live integration + property suite (Plan 06-08) runs against `bun run docker:up` with all tests green before checkpoint approves"
  artifacts:
    - path: "scripts/smoke-health.sh"
      provides: "Phase 6 WS smoke probes 39-44 appended to the existing 38-probe script"
      contains: "probe_games_ws"
  key_links:
    - from: "scripts/smoke-health.sh"
      to: "docker/kong/kong.yml games-ws route"
      via: "curl http://localhost:8000/ws"
      pattern: "/ws"
---

<objective>
Wire smoke probes 39-44 into `scripts/smoke-health.sh` covering Phase 6 WS surface end-to-end, then run the full Phase 6 test ladder against the live `bun run docker:up` stack and gate the phase exit behind a blocking human checkpoint.

Purpose: REQ-WS-01 through REQ-WS-07 must be observably true against the real perimeter (Kong → games:4001), not just in-process. The smoke probes are the recruiter-facing demo evidence; the live integration suite from Plan 06-08 is the regression net.

Output: Six new probes in smoke-health.sh; blocking checkpoint verifies all 44 probes PASS plus the new integration + property tests green; SUMMARY captures the live trace evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-websocket-multiplier-sync/06-RESEARCH.md
@.planning/phases/06-websocket-multiplier-sync/06-03-PLAN.md
@.planning/phases/06-websocket-multiplier-sync/06-07-PLAN.md
@scripts/smoke-health.sh

<interfaces>
<!-- Existing smoke probe patterns (see Phase 5 probes 33-38 in scripts/smoke-health.sh) -->
- WALLETS_TOKEN is captured via probe_wallets_keycloak_token early in the script
- record_pass / record_fail helpers exist
- jq is the JSON-parsing dependency
- `wait_for_round_phase "RUNNING" 15` is already available

<!-- New helper needed -->
- `probe_games_ws_via_curl`: curl with `Connection: Upgrade` + `Upgrade: websocket` headers to http://localhost:8000/ws; expect HTTP 4xx from games (NOT 404 from Kong); the 4xx indicates Kong forwarded and games rejected the non-socketio handshake
- `probe_games_ws_via_bun`: a `bun -e` one-liner using `socket.io-client` (devDep installed in plan 06-02 via the workspace) to perform a full handshake and record events

<!-- Bun one-liner shape (verbose pseudo for the smoke script — exact escaping handled in implementation) -->
bun -e 'import {io} from "socket.io-client"; const s = io("http://localhost:8000/", { path: "/ws", auth: { token: process.env.WALLETS_TOKEN }, transports: ["websocket"], reconnection: false }); s.on("connect", () => process.stdout.write("CONNECTED\n")); s.on("connect_error", (e) => { process.stdout.write("CONNECT_ERROR " + e.message + "\n"); process.exit(0); }); s.on("round:snapshot", () => { process.stdout.write("SNAPSHOT\n"); s.close(); process.exit(0); }); setTimeout(() => { process.stdout.write("TIMEOUT\n"); process.exit(1); }, 5000);'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append probes 39-44 to scripts/smoke-health.sh</name>
  <files>
    scripts/smoke-health.sh
  </files>
  <read_first>
    scripts/smoke-health.sh
  </read_first>
  <behavior>
    - Probe 39 — Kong WS upgrade route open: curl with Connection: Upgrade + Upgrade: websocket to http://localhost:8000/ws; expect HTTP status NOT 404 (Kong-origin) AND NOT "no Route matched" in body; passing values include 400 (games rejected non-socketio handshake), 401 (JwtIoAdapter rejected before upgrade — depending on path), 426 (engine.io upgrade-required)
    - Probe 40 — handshake denied without token: use `bun -e` one-liner connecting via socket.io-client WITHOUT auth.token; expect connect_error within 5s; record_pass if event message includes "UNAUTHORIZED"
    - Probe 41 — handshake accepted with token + snapshot delivered: connect with WALLETS_TOKEN; expect round:snapshot event within 5s; record_pass on receipt; assert payload contains a `round` key + `serverTime` key
    - Probe 42 — tick frequency during RUNNING: connect with WALLETS_TOKEN; wait_for_round_phase RUNNING; record round:tick events for 2s window; expect count >= 30 (allowing 50% drop from theoretical 60 at 30Hz to absorb scheduling jitter and any volatile drops on first 100ms of RUNNING)
    - Probe 43 — lifecycle sequence: connect; record round:started, round:running, round:crashed, round:settled over 30s; expect all four observed at least once in order
    - Probe 44 — bet event arrives over WS: connect; wait_for_round_phase BETTING; POST /games/bet via Kong; expect bet:my_active event on the connected socket within 5s with the matching betId
    - Each probe uses the existing record_pass / record_fail helpers
    - All six probes appended to the bottom of the script's run section and tallied into the final summary
  </behavior>
  <action>
    Append six new bash functions to `scripts/smoke-health.sh` after `probe_games_bet_cashout_without_active` (the last existing probe). Pattern follows the existing probes 33-38 verbatim — `local name`, `record_pass`/`record_fail` calls, jq for JSON parsing, `wait_for_round_phase` reuse.

    For probes 40-44 that need a real WS client, use a `bun -e "..."` one-liner that imports `socket.io-client` (must be present in the workspace — installed via `--filter=@crash/games -d` in plan 06-02; the smoke runner invokes `bun -e` from repo root which can resolve workspace deps via `--cwd services/games` flag if needed). Alternative if cwd resolution fails: shell out to `cd services/games && bun -e "..."`.

    Probe 40 implementation outline:
    ```
    probe_games_ws_handshake_denied() {
      local name="40: WS handshake without token returns UNAUTHORIZED"
      local output
      output=$(cd services/games && bun -e '
        import { io } from "socket.io-client";
        const s = io("http://localhost:8000/", { path: "/ws", transports: ["websocket"], reconnection: false });
        s.on("connect_error", (e) => { console.log("CONNECT_ERROR " + e.message); s.close(); process.exit(0); });
        s.on("connect", () => { console.log("UNEXPECTED_CONNECT"); s.close(); process.exit(1); });
        setTimeout(() => { console.log("TIMEOUT"); process.exit(1); }, 5000);
      ' 2>&1 || echo "RUN_FAIL")
      if echo "${output}" | grep -q "CONNECT_ERROR.*UNAUTHORIZED"; then
        record_pass "${name}"
      else
        record_fail "${name}" "expected CONNECT_ERROR UNAUTHORIZED, got: ${output}"
      fi
    }
    ```

    Probes 41-44 follow the same shape; pass `auth: { token: process.env.WALLETS_TOKEN }` after exporting `WALLETS_TOKEN` to the bun subprocess (`WALLETS_TOKEN="${WALLETS_TOKEN}" bun -e '...'`).

    For probe 42 (tick count): increment a counter on each `round:tick`, exit with the count after 2s; bash compares against threshold 30.

    For probe 43 (lifecycle): record presence-set of four event names; exit-success if all four observed within 30s; bash records pass.

    For probe 44 (bet event): wait_for_round_phase BETTING in bash FIRST; then issue POST /games/bet via curl (re-use Phase 5 probe 34 pattern); then run the bun one-liner that listens for `bet:my_active` with the matching betId; bash matches the betId in the bun output.

    Add each probe function name to the run section at the bottom of the script (in order) and re-tally `PASS` / `FAIL`.

    No code-side test for shell — verification is manual smoke run in Task 2.
  </action>
  <verify>
    <automated>bash -n scripts/smoke-health.sh && grep -c 'probe_games_ws' scripts/smoke-health.sh</automated>
  </verify>
  <done>
    `bash -n scripts/smoke-health.sh` exits 0 (syntax valid); `grep -c 'probe_games_ws\|probe_games_lifecycle\|probe_games_bet_ws' scripts/smoke-health.sh` returns >= 6; new probe function names appear in the final run section.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live Phase 6 walkthrough — smoke probes 39-44 + integration + property suite</name>
  <what-built>
    The Phase 6 WebSocket surface is complete: JwtIoAdapter, GameWsGateway, MultiplierBroadcastService, RoundLoopService lifecycle hooks, WsBridgeConsumer outbox-to-WS bridge, Kong games-ws route, six smoke probes, five integration tests, one property test.
  </what-built>
  <how-to-verify>
    Run from repo root (`/Users/pedro/Projetos/fullstack-challenge`):

    1. `bun run docker:down` — clean any prior stack
    2. `bun run docker:up` — bring up the full Phase 1+2+3+4+5+6 stack; wait for all healthy
    3. `bash scripts/smoke-health.sh` — expect 44/44 probes PASS (the prior 38 from Phases 1-5 plus the new 39-44 from this plan)
    4. `cd services/games && bunx tsc --noEmit -p tsconfig.integration.json` — typecheck clean
    5. `cd services/games && bun test tests/unit` — unit suite green (full count from prior phases + 06-01 through 06-06 additions)
    6. `cd services/games && bun test tests/integration/ws-*.test.ts` — five new WS integration tests green
    7. `cd services/games && bun test tests/property/cashout-race.property.test.ts` — property test green (50 cases, expect <90s runtime)
    8. Manual `wscat` or `bun -e` connection: open a WS to `ws://localhost:8000/ws` with a fresh Keycloak token; observe round:snapshot, then round:tick at ~30Hz, then a full round lifecycle, then a placed bet's bet:my_active

    Approve only if EVERY check passes. If a non-trivial timing flake appears (e.g., tick count 28 instead of 30), record it in the SUMMARY as a deferred-items.md candidate but DO NOT silently accept — surface the gap to the user with the actual observed count.
  </how-to-verify>
  <resume-signal>Type "approved: 44/44 probes pass, integration green, property green, manual WS confirmed" OR describe the failure observed with the specific probe number / test name / WS event missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live perimeter Kong → games:4001 | Smoke probes exercise the real WS upgrade path through Kong; mirrors production attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-25 | Spoofing | Smoke probe 40 missing token | mitigate | Probe asserts connect_error UNAUTHORIZED — equivalent to integration test ws-handshake-jwt but exercises the LIVE Kong-proxied path |
| T-06-26 | Tampering | Kong route shadowing | mitigate | Probe 39 confirms the games-ws route is the FIRST match for `/ws` (Kong returns non-404; if route ordering were wrong, Kong-origin 404 would surface) |
| T-06-27 | Denial of Service | Live property-test load | accept | numRuns=50 takes <90s on the dev box; checkpoint requires the run to complete; if CI timeout becomes a concern in Phase 10 it can drop to 20 cases with a documented compromise |
</threat_model>

<verification>
- `bash -n scripts/smoke-health.sh` clean
- `bash scripts/smoke-health.sh` reports 44/44 PASS against live stack (gated by checkpoint approval)
- `bun test tests/integration/ws-*.test.ts tests/property/cashout-race.property.test.ts` green against live stack (gated by checkpoint approval)
- Manual WS observation captured in SUMMARY with at least: snapshot received, tick count ≥ 30 over 2s, full lifecycle observed, bet:my_active arrived
</verification>

<success_criteria>
- All Phase 6 REQs observable end-to-end through Kong
- No silent gaps: any timing flake or missing event surfaces a deferred-items.md entry with the actual observed value
- Phase 6 ready for closeout in Plan 06-10
</success_criteria>

<output>
Create `.planning/phases/06-websocket-multiplier-sync/06-09-SUMMARY.md` when done, including the captured live trace (probe outputs, key event timestamps from the manual WS run).
</output>
