---
phase: 06-websocket-multiplier-sync
plan: 10
type: execute
wave: 7
depends_on:
  - 06-09
files_modified:
  - .planning/adrs/ADR-021-single-global-lobby-room.md
  - .planning/adrs/ADR-022-30hz-server-tick-60fps-client-interpolation.md
  - .planning/adrs/ADR-023-server-authoritative-cashout-accepted-at.md
  - .planning/adrs/README.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-WS-01
  - REQ-WS-02
  - REQ-WS-03
  - REQ-WS-04
  - REQ-WS-05
  - REQ-WS-06
  - REQ-WS-07
must_haves:
  truths:
    - "Three ADRs landed: ADR-021 single global lobby, ADR-022 30Hz tick + 60fps interpolation, ADR-023 server-authoritative cashoutAcceptedAt"
    - "ADR catalogue README has a Phase 6 section linking the three ADRs with one-line summaries"
    - "STATE.md updated to Phase 6 complete (6/10 phases)"
    - "ROADMAP.md Phase 6 marked [x] with Plans landed list"
    - "REQUIREMENTS.md traceability marks REQ-WS-01 through REQ-WS-07 as Done with plan citations; v1-complete count incremented from 39 to 46"
    - "REQ-AUTH-01/02/03 remain Pending (Phase 7 frontend scope per Phase 6 RESEARCH Deferred Ideas)"
  artifacts:
    - path: ".planning/adrs/ADR-021-single-global-lobby-room.md"
      provides: "Locks the lobby-vs-per-round-room decision"
      contains: "ADR-021"
    - path: ".planning/adrs/ADR-022-30hz-server-tick-60fps-client-interpolation.md"
      provides: "Locks tick rate + client interpolation pattern"
      contains: "ADR-022"
    - path: ".planning/adrs/ADR-023-server-authoritative-cashout-accepted-at.md"
      provides: "Codifies first-line acceptedAt invariant for cashout race"
      contains: "ADR-023"
    - path: ".planning/STATE.md"
      provides: "Project memory advanced to Phase 6 complete"
      contains: "Phase 6"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 marked complete + Plans landed list"
      contains: "Phase 6"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability for REQ-WS-01..07 marked Done"
      contains: "Done (P6"
  key_links:
    - from: ".planning/adrs/README.md"
      to: ".planning/adrs/ADR-021-single-global-lobby-room.md"
      via: "ADR catalogue index entry"
      pattern: "ADR-021"
---

<objective>
Phase 6 closeout: author the three locked Phase 6 ADRs (021/022/023), extend the ADR catalogue README, advance STATE.md and ROADMAP.md to Phase 6 complete, mark all seven Phase 6 REQ-IDs Done in REQUIREMENTS.md with plan citations.

Purpose: The arguição requires every significant decision to be defensible via an ADR. Phase 6 anticipated three ADRs in the ROADMAP (per Phase 6 RESEARCH ADRs Anticipated section). The closeout also advances project memory so subsequent `/gsd:plan-phase 7` work starts from an accurate baseline.

Output: Three new ADR files; ADR catalogue extended; STATE.md / ROADMAP.md / REQUIREMENTS.md updated; v1-complete REQ count incremented from 39 to 46.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/adrs/README.md
@.planning/phases/06-websocket-multiplier-sync/06-RESEARCH.md
@.planning/phases/06-websocket-multiplier-sync/06-09-PLAN.md

<interfaces>
ADR template (from CLAUDE.md):
- Title line: # ADR-NNN: Title
- Status: Accepted
- Date: YYYY-MM-DD
- Phase: 6
- Sections: Context, Considered, Decision, Consequences

ADR-019 / ADR-020 (Phase 5) are the immediate prior reference for tone, length, and "Alternatives Rejected" style.

REQUIREMENTS.md Phase 6 section currently has all 10 REQ-IDs marked Pending; this plan flips REQ-WS-01..07 to Done with plan citations and leaves REQ-AUTH-01/02/03 as Pending (Phase 7).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-021, ADR-022, ADR-023</name>
  <files>
    .planning/adrs/ADR-021-single-global-lobby-room.md,
    .planning/adrs/ADR-022-30hz-server-tick-60fps-client-interpolation.md,
    .planning/adrs/ADR-023-server-authoritative-cashout-accepted-at.md
  </files>
  <read_first>
    .planning/adrs/README.md,
    .planning/adrs/ADR-019-orchestration-over-choreography.md,
    .planning/adrs/ADR-020-bet-202-cashout-200-asymmetry.md
  </read_first>
  <behavior>
    - ADR-021: Context describes the choice between single global `lobby` room and per-round `round:{roundId}` rooms; cites ARCHITECTURE 7.2 + RESEARCH Architecture Patterns; Considered lists Option A per-round room + Option B single lobby + Option C per-player segmentation only; Decision is Option B; Consequences include eliminated 7-15s join/leave churn + simplified handleConnection wiring + acceptable scope (single round at a time, lobby = the public broadcast surface)
    - ADR-022: Context describes 30Hz server tick vs 60Hz vs per-frame broadcast; cites Gabriel Gambetta client-side prediction + SUMMARY 8; Considered lists 60Hz server + per-frame server-push + 10Hz tick; Decision is 30Hz server + 60fps client rAF interpolation via `e^(GROWTH_RATE*t/1000)`; Consequences include ~50% bandwidth vs 60Hz + smooth render via rAF + EWMA clock-offset reconciliation deferred to Phase 7 FE
    - ADR-023: Context describes the cashout race window (PITFALLS C2); REQ-WS-05 literal wording "stamped at gateway middleware" interpreted as HTTP controller layer (NestJS controller IS a kind of gateway) — explains why cashout was NOT migrated to a WS inbound message; cites Phase 5 plan 05-06 bet-command.controller.ts:69 verbatim as the locked implementation; Considered lists WS inbound `cashout` message + HTTP controller server-stamp + client-supplied timestamp; Decision is HTTP controller server-stamp `acceptedAt = new Date()` as the FIRST EXECUTABLE LINE before any await; Consequences include single isolated request thread per cashout + REQ-WS-05 satisfied + Phase 6 property test (Plan 06-08 cashout-race.property.test.ts) covers the ±50ms window across 50 cases; explicitly REJECTS a WS inbound migration because the WS handler shares the event loop with the 30Hz tick broadcast — competing for the same loop ticks tightens the race rather than relaxing it
    - All three ADRs follow the existing 4-section template (Status / Date / Phase / Context / Considered / Decision / Consequences) and match the prose density of ADR-019/020
  </behavior>
  <action>
    Read ADR-019 and ADR-020 first to anchor the writing style — Phase 5 ADRs run ~80-120 lines each with explicit "Alternatives Rejected" subsections under Decision. Match that density.

    Author each ADR with the following structural emphasis:
    - Context section names the REQ-IDs being satisfied and cites the research source (ARCHITECTURE section, SUMMARY section, PITFALLS C/H number)
    - Considered section lists at least 3 options (A/B/C) with concrete pros/cons in bullet form
    - Decision section names the chosen option and quotes the verbatim code or config that locks it (e.g., for ADR-023, quote `bet-command.controller.ts:69` line `const acceptedAt = new Date();`)
    - Consequences section enumerates: what this locks in (positive), what it forecloses (foreclosed), and what downstream phase carries which residual concern (e.g., ADR-022 -> Phase 7 FE EWMA reconciliation; ADR-023 -> Phase 9 auto-cashout reuses the same server-clock primitive)

    For ADR-023 specifically: include a "Why NOT a WS inbound message" subsection citing RESEARCH OQ context — the HTTP request thread is isolated, the WS handler shares the event loop with the tick broadcast loop, and HTTP gives a clean 200/409 response surface that the FE consumes uniformly.

    No code or test changes in this task; ADRs are documentation only.
  </action>
  <verify>
    <automated>ls .planning/adrs/ADR-021-*.md .planning/adrs/ADR-022-*.md .planning/adrs/ADR-023-*.md && wc -l .planning/adrs/ADR-021-*.md .planning/adrs/ADR-022-*.md .planning/adrs/ADR-023-*.md</automated>
  </verify>
  <done>
    Three ADR files exist with the canonical filename pattern; each is at least 60 lines (matches the prior Phase 5 ADR density); every file has Status/Date/Phase/Context/Considered/Decision/Consequences headings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend ADR catalogue README; advance STATE.md, ROADMAP.md, REQUIREMENTS.md</name>
  <files>
    .planning/adrs/README.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/adrs/README.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    - ADR catalogue README gains a "Phase 6 — WebSocket Gateway and Multiplier Sync" section with three one-line links to ADR-021/022/023
    - The README's "Future ADRs" / anticipated catalogue rewinds — ADR-021/022/023 entries removed from Phase 6 anticipated, and the anticipated counter advances to ADR-024+
    - STATE.md: Current Position phase becomes "6 — WebSocket Gateway and Multiplier Sync (complete, 10/10 plans landed; 6/10 phases complete overall)"; Progress bar advances to `▰▰▰▰▰▰▱▱▱▱` 6/10; Phase history row for Phase 6 fills in with the 10/10 plans + 44/44 smoke probes + integration suite green
    - STATE.md Performance Metrics table: Phases complete 6/10; v1 complete 46/95 (+7: REQ-WS-01..07); ADRs landed 23 (+3); ADRs anticipated unchanged (30+ overall)
    - STATE.md Recent activity entry for 2026-MM-DD captures: P6.10 closeout, three ADRs, live smoke trace summary (snapshot received, tick count over 2s, full lifecycle observed), key per-plan deltas
    - ROADMAP.md Phase 6 checkbox flipped to [x]; Plans landed list populated with 06-01 through 06-10 plan titles + one-line summaries; Phase 6 progress row 10/10 Complete; date set to today
    - REQUIREMENTS.md Phase 6 traceability table: REQ-WS-01 -> Done (P6.03+P6.01); REQ-WS-02 -> Done (P6.03); REQ-WS-03 -> Done (P6.03+P6.05+P6.06); REQ-WS-04 -> Done (P6.03); REQ-WS-05 -> Done (Phase 5 P5.06 + codified in P6.10 ADR-023); REQ-WS-06 -> Done (P6.04); REQ-WS-07 -> Done (Socket.IO client default backoff + P6.03 snapshot-on-reconnect; FE wiring deferred to Phase 7)
    - REQUIREMENTS.md Phase 6 REQ-AUTH-01/02/03 explicitly remain Pending (deferred to Phase 7 per RESEARCH Deferred Ideas)
    - REQUIREMENTS.md Coverage summary: v1 complete 46 / 95; +7 from Phase 6 (REQ-WS-01..07)
  </behavior>
  <action>
    Edit `.planning/adrs/README.md` — add a new "Phase 6 — WebSocket Gateway and Multiplier Sync" section between the existing Phase 5 section and the "Future ADRs" / anticipated list. Include three bullet links: ADR-021 single global lobby; ADR-022 30Hz server tick + 60fps client rAF interpolation; ADR-023 server-authoritative cashoutAcceptedAt at HTTP controller's first executable line. In the "Future ADRs" / anticipated catalogue, remove the three Phase 6 entries and rewind any "next ADR" counter to ADR-024 onward.

    Edit `.planning/STATE.md`:
    - Current Position block: update Phase to 6 complete, plan to P6.10 complete, progress bar to `▰▰▰▰▰▰▱▱▱▱` 6/10
    - Performance Metrics: Phases complete 6 / 10; v1 requirements complete 46 / 95; ADRs landed 23 (+3 in Phase 6)
    - Phase history table row for Phase 6: "10 / 10 plans landed (P6.10 ADRs + closeout) — Complete — 06-01 JwtVerifierService extraction, 06-02 deps + WS_PATH env, 06-03 JwtIoAdapter + GameWsGateway + GetWsSnapshotUseCase, 06-04 MultiplierBroadcastService volatile tick, 06-05 EventEmitter2 lifecycle hooks, 06-06 WsBridgeConsumer game.events bridge, 06-07 Kong /ws route, 06-08 integration + property tests, 06-09 smoke probes 39-44 + live walkthrough, 06-10 ADRs 021/022/023 + closeout; 44/44 smoke probes; 5 WS integration tests + cashout-race property green; three ADRs"
    - Recent activity: insert a 2026-MM-DD entry summarizing P6.10 closeout — three ADRs authored, REQ-WS-01..07 marked Done, live smoke trace evidence captured from Plan 06-09 SUMMARY
    - Next action: "Run /gsd:verify-phase 6 then /gsd:plan-phase 7 (Frontend Vertical Slice)"

    Edit `.planning/ROADMAP.md`:
    - Phases checkbox section: flip Phase 6 entry to `[x]`
    - Phase 6 detail block: append a "Plans landed" subsection listing all ten plans with one-line summaries
    - Progress table: Phase 6 -> 10/10 Complete (date)
    - Parallelization Map: no change

    Edit `.planning/REQUIREMENTS.md`:
    - Phase 6 traceability table (search for `#### Phase 6 — WebSocket Gateway and Multiplier Sync (10 reqs)`):
      - REQ-AUTH-01 -> Pending (Phase 7 OIDC PKCE)
      - REQ-AUTH-02 -> Pending (Phase 7 silent renewal)
      - REQ-AUTH-03 -> Pending (Phase 7 BroadcastChannel)
      - REQ-WS-01 -> Done (P6.01 JwtVerifierService extraction + P6.03 JwtIoAdapter handshake middleware)
      - REQ-WS-02 -> Done (P6.03 handleConnection auto-joins lobby + user:{playerId})
      - REQ-WS-03 -> Done (P6.03 round:snapshot + P6.05 lifecycle @OnEvent broadcasts + P6.06 WsBridgeConsumer bet event bridging)
      - REQ-WS-04 -> Done (P6.03 GetWsSnapshotUseCase + handleConnection emit)
      - REQ-WS-05 -> Done (Phase 5 P5.06 bet-command.controller.ts:69 acceptedAt = new Date() + P6.10 ADR-023 codifies invariant; Phase 6 P6.08 cashout-race property test covers ±50ms window)
      - REQ-WS-06 -> Done (P6.04 MultiplierBroadcastService volatile.emit + P6.08 ws-tick-volatile integration test)
      - REQ-WS-07 -> Done (Socket.IO client default exponential backoff + P6.03 round:snapshot on every handleConnection; FE wiring deferred to Phase 7)
    - Top "v1 Requirements" checkbox flips for REQ-WS-01..07 to `[x]`; REQ-AUTH-01/02/03 remain `[ ]`
    - Coverage summary: v1 complete count incremented from 39 to 46; add Phase 6 contribution line listing the seven WS REQ-IDs

    Footer date stamps in each file updated to today's date with the P6.10 closeout citation referencing all three ADRs and the +7 REQ-count delta.
  </action>
  <verify>
    <automated>grep -c 'ADR-021' .planning/adrs/README.md && grep -c 'ADR-022' .planning/adrs/README.md && grep -c 'ADR-023' .planning/adrs/README.md && grep -c 'Phase 6 complete' .planning/STATE.md && grep -E '^- \[x\] \*\*Phase 6' .planning/ROADMAP.md && grep -c 'Done (P6' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    ADR catalogue README lists all three Phase 6 ADRs with hyperlinks; STATE.md, ROADMAP.md, REQUIREMENTS.md all carry the Phase 6 closeout deltas; v1 complete count is 46/95 in REQUIREMENTS.md coverage summary AND STATE.md Performance Metrics.
  </done>
</task>

<task type="auto">
  <name>Task 3: Git commit and SUMMARY</name>
  <files>
    .planning/phases/06-websocket-multiplier-sync/06-10-SUMMARY.md
  </files>
  <read_first>
    .planning/phases/05-saga-integration/05-11-SUMMARY.md
  </read_first>
  <behavior>
    - All seven files from Tasks 1-2 are committed in a single atomic commit
    - Commit message follows the Phase 5 closeout convention (see git log for the actual P5.11 commit subject)
    - 06-10-SUMMARY.md is written with the same density as 05-11-SUMMARY.md, listing the three ADRs + the seven REQ-IDs closed + the +3 ADR / +7 REQ delta
  </behavior>
  <action>
    Stage and commit the seven modified/created files (ADR-021, ADR-022, ADR-023, adrs/README.md, STATE.md, ROADMAP.md, REQUIREMENTS.md) in a single atomic commit. Commit message subject follows the Phase 5 convention — `docs(06-10): Phase 6 closeout — ADR-021 + ADR-022 + ADR-023 + STATE/ROADMAP/REQUIREMENTS advance`. NO Co-Authored-By, NO emoji, NO AI attribution (per CLAUDE.md + global user rules). Body is a 3-4 line summary of what changed.

    Write `.planning/phases/06-websocket-multiplier-sync/06-10-SUMMARY.md` capturing:
    - Three ADR titles and one-line rationale each
    - REQ-IDs closed (WS-01 through WS-07) with plan citations
    - REQ-IDs deliberately left Pending (AUTH-01/02/03) with Phase 7 forward reference
    - v1 complete count delta (39 -> 46)
    - ADR count delta (20 -> 23)
    - Phases complete delta (5 -> 6)
    - Any Rule-3 deviations encountered during the closeout (record them at SUMMARY tail for the verifier agent)
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -c '06-10' && ls .planning/phases/06-websocket-multiplier-sync/06-10-SUMMARY.md</automated>
  </verify>
  <done>
    Single git commit lands with all closeout files staged; SUMMARY exists with the standard structure; next action is `/gsd:verify-phase 6` per the updated STATE.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation files in `.planning/` | No runtime trust boundary crossed; project memory update only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-28 | Repudiation | Undocumented decision later contested in arguição | mitigate | Three ADRs lock the three Phase 6 architectural choices (lobby model, tick rate, cashout race authority) with explicit Alternatives Rejected sections |
| T-06-29 | Information Disclosure | Stale STATE.md drift | mitigate | Closeout commit advances STATE.md atomically alongside ROADMAP.md + REQUIREMENTS.md so the next /gsd:plan-phase 7 invocation sees a coherent snapshot |
</threat_model>

<verification>
- `ls .planning/adrs/ADR-02[123]-*.md` lists three files
- `grep -c 'ADR-021\|ADR-022\|ADR-023' .planning/adrs/README.md` returns at least 3
- `grep -E '^- \[x\] \*\*Phase 6' .planning/ROADMAP.md` matches one line
- `grep -c '46 / 95' .planning/REQUIREMENTS.md` returns at least 1
- `git log --oneline -1` shows the closeout commit
</verification>

<success_criteria>
- Three Phase 6 ADRs landed and indexed
- STATE.md, ROADMAP.md, REQUIREMENTS.md all reflect Phase 6 complete with the +7 REQ delta
- Phase 6 SUMMARY exists at the standard path
- Project ready for `/gsd:verify-phase 6` next per STATE.md
</success_criteria>

<output>
Create `.planning/phases/06-websocket-multiplier-sync/06-10-SUMMARY.md` when done
</output>
