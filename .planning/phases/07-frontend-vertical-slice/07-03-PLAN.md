---
phase: 07-frontend-vertical-slice
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/vitest.config.ts
  - frontend/tsconfig.json
  - frontend/components.json
  - frontend/index.html
  - frontend/.env.example
  - frontend/src/router.tsx
  - frontend/src/routes/__root.tsx
  - frontend/src/routes/index.tsx
  - frontend/src/styles/globals.css
  - frontend/src/lib/config.ts
  - frontend/src/lib/config.test.ts
  - frontend/src/test/setup.ts
  - frontend/src/components/ui/.gitkeep
autonomous: true
requirements:
  - REQ-FE-01
  - REQ-FE-11
  - REQ-FE-12
must_haves:
  truths:
    - "frontend dev server boots on port 3000 using the boot mode ratified in 07-02 (SPA mode or selective ssr:false) and renders a styled shell with no hydration hang"
    - "The dark-casino Tailwind v4 @theme tokens from 07-UI-SPEC.md (background #0A0F14, card #111827, accent #00FF85→#22D3EE, destructive #EF4444) are applied via CSS variables"
    - "Every business constant (GROWTH_RATE, EWMA alpha, history thresholds, bet min/max, issuer, client id, WS url) is read from a VITE_-prefixed env via a single typed zod-parsed config module — none hardcoded"
    - "Vitest runs a green unit test in the frontend workspace"
    - "shadcn is initialized with the UI-SPEC component set installed"
  artifacts:
    - path: "frontend/vite.config.ts"
      provides: "tanstackStart + viteReact + tailwindcss; server.port 3000; workspace-TS transpile per 07-02 decision"
      contains: "tanstackStart"
    - path: "frontend/src/styles/globals.css"
      provides: "@import tailwindcss + @theme dark-casino tokens"
      contains: "@theme"
    - path: "frontend/src/lib/config.ts"
      provides: "Typed zod-parsed VITE_ env — single source of truth for all FE business constants"
      contains: "import.meta.env"
    - path: "frontend/tsconfig.json"
      provides: "Self-contained tsconfig (no root tsconfig exists) with @/* alias"
      contains: "paths"
  key_links:
    - from: "frontend/src/styles/globals.css"
      to: "07-UI-SPEC.md color/typography tokens"
      via: "@theme CSS variables"
      pattern: "00FF85"
    - from: "frontend/src/lib/config.ts"
      to: "frontend/.env.example"
      via: "VITE_ env vars surfaced + zod-parsed"
      pattern: "VITE_"
---

<objective>
Scaffold the `frontend/` workspace slot (currently `.gitkeep` + a stub `package.json`) into a booting TanStack Start app with the dark-casino theme, typed env config, shadcn, and a Vitest harness — the foundation every later plan builds on (REQ-FE-01/11/12).

This plan consumes the 07-02 ratified decisions: boot mode (SPA vs selective `ssr:false`) and the workspace-TS transpile config. It establishes file ownership of the `frontend/` tree foundation so 07-04/05/06 extend it without churning config.

Purpose: A clean, themed, env-driven, test-capable shell. No game logic yet — just that `bun run dev` boots on :3000, the theme tokens are live, the typed config rejects bad env, and one Vitest test passes.
Output: vite/vitest/tsconfig/components.json, `globals.css` with UI-SPEC `@theme` tokens, `lib/config.ts` typed env, `.env.example`, `__root.tsx` layout shell (header with balance-pill slot + connection-badge slot + reserved Fairness-badge space + `<Toaster/>`), `index.tsx` game-route placeholder, Vitest setup with jsdom + canvas stub + matchMedia mock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-frontend-vertical-slice/07-RESEARCH.md
@.planning/phases/07-frontend-vertical-slice/07-UI-SPEC.md
@.planning/phases/07-frontend-vertical-slice/07-CONTEXT.md
@.planning/phases/07-frontend-vertical-slice/07-02-PLAN.md
@CLAUDE.md
@frontend/package.json
@packages/contracts/src/index.ts

<interfaces>
<!-- Boot mode + workspace-TS transpile config come from 07-02 SPIKE-NOTES DECISIONs — READ frontend/spike/SPIKE-NOTES.md FIRST -->

<!-- VITE_ env surface this config module must define + zod-parse (defaults from REQUIREMENTS Open Configuration Values + RESEARCH + UI-SPEC) -->
VITE_KEYCLOAK_ISSUER        = http://localhost:8080/realms/crash-game
VITE_KEYCLOAK_CLIENT_ID     = crash-game-client
VITE_WS_URL                 = http://localhost:8000          // Kong; socket path '/ws'
VITE_REST_BASE              = http://localhost:8000
VITE_GROWTH_RATE            = 0.06                            // matches games GROWTH_RATE; multiplierAt() throws if <= 0 — zod .positive()
VITE_EWMA_ALPHA             = 0.1                             // Claude's-discretion default (RESEARCH A4); zod .min(0).max(1); confirm/tune in build
VITE_HISTORY_RED_MAX_X      = 1.5                             // <= red band (UI-SPEC)
VITE_HISTORY_YELLOW_MAX_X   = 2.0                             // <= amber band; > = emerald
VITE_BET_MIN_CENTS          = 100                             // 1.00 CRD (REQUIREMENTS BET_MIN_CENTS)
VITE_BET_MAX_CENTS          = 100000                          // 1000.00 CRD (REQUIREMENTS BET_MAX_CENTS)
VITE_CURRENCY_CODE          = CRD

<!-- UI-SPEC dark-casino tokens (07-UI-SPEC.md §Color / §Typography) -->
--color-background #0A0F14  --color-card #111827  --color-popover #1F2937
--color-accent #00FF85  --color-accent-cyan #22D3EE  --color-destructive #EF4444 (CRASH ONLY)
--color-border #1E3A5F  --color-muted-foreground #94A3B8  --color-foreground #F8FAFC
--font-mono "Fira Code" (tabular display)  --font-sans "Fira Sans"

<!-- shadcn component set (07-UI-SPEC §Component Inventory) -->
button card dialog input label tabs sonner tooltip skeleton badge separator scroll-area progress
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold TanStack Start + Vite + Tailwind v4 + typed config + .env.example</name>
  <files>
    frontend/package.json,
    frontend/vite.config.ts,
    frontend/tsconfig.json,
    frontend/index.html,
    frontend/.env.example,
    frontend/src/router.tsx,
    frontend/src/styles/globals.css,
    frontend/src/lib/config.ts,
    frontend/src/lib/config.test.ts
  </files>
  <read_first>
    frontend/spike/SPIKE-NOTES.md,
    frontend/package.json,
    .planning/phases/07-frontend-vertical-slice/07-RESEARCH.md,
    .planning/phases/07-frontend-vertical-slice/07-UI-SPEC.md,
    packages/contracts/src/index.ts
  </read_first>
  <action>
    Delete the throwaway `frontend/spike/` directory once its SPIKE-NOTES decisions are read (the decisions are the only durable output). Expand `frontend/package.json` (keep name `@crash/frontend`, private) with the RESEARCH-verified dependency set and scripts: `dev` (vite, port 3000), `build`, `typecheck` (`tsc --noEmit`), `test` (`vitest run`), `lint`. Add deps at the RESEARCH §Installation pins; add `@crash/contracts@workspace:*` and `@crash/shared-kernel@workspace:*`.

    Create `frontend/vite.config.ts` per RESEARCH Pattern 1: `defineConfig` with `server.port: 3000` (MUST be 3000 — realm redirectUris only allow :3000/:8080), plugins `tanstackStart()`, `viteReact()`, `tailwindcss()`. Apply the boot-mode DECISION from SPIKE-NOTES: if `spa-mode` was ratified, add `spa: { enabled: true }` to `tanstackStart()`; if `selective-ssr`, leave `tanstackStart()` plain and mark the game route `ssr: false` (done in Task 2). Apply the workspace-TS transpile DECISION (add the packages to `optimizeDeps.include` / `ssr.noExternal` only if SPIKE-NOTES said it was needed). Configure the `@/*` alias to `frontend/src`.

    Create `frontend/tsconfig.json` SELF-CONTAINED (no root tsconfig exists — RESEARCH Pitfall 8): strict, bundler moduleResolution, `lib` including DOM + bigint-capable target (ES2020+), `paths` mapping `@/*` → `./src/*`, `types` for vite/vitest. Create `frontend/index.html` (root div + module script to `src/router` entry). Create `frontend/src/router.tsx` with `createRouter(routeTree)` per TanStack Start build-from-scratch.

    Create `frontend/src/styles/globals.css`: `@import "tailwindcss";` then an `@theme { ... }` block defining EVERY token from the UI-SPEC Color + Typography tables (the `--color-*` and `--font-*` vars in the interfaces block, exact hex values). Add the `:root`/`.dark` CSS variable layer shadcn expects, mapping shadcn's semantic vars onto these tokens. Self-host Fira Code + Fira Sans (RESEARCH Environment Availability: add `@fontsource/fira-code` + `@fontsource/fira-sans` or vendor woff2 in `public/`) and import them. Force dark by default (the app is dark-casino only).

    Create `frontend/src/lib/config.ts`: a zod schema parsing `import.meta.env` for EVERY `VITE_` var in the interfaces block. `VITE_GROWTH_RATE` uses `z.coerce.number().positive()` (multiplierAt throws if <= 0); `VITE_EWMA_ALPHA` `z.coerce.number().min(0).max(1)`; history thresholds + bet bounds coerced numbers; issuer/client/url strings `.url()` where applicable. Export a single frozen `config` object. NO business constant may be hardcoded anywhere else in the FE — this module is the sole source (CLAUDE.md §Configuration). Materialize `frontend/.env.example` listing every var with the default values from the interfaces block + a comment per line.

    Create `frontend/src/lib/config.test.ts` (Vitest): assert the parsed config exposes the expected keys with coerced types (numbers are numbers, not strings); assert a `VITE_GROWTH_RATE=0` or negative input would fail the schema (test the schema function directly with a bad object, since import.meta.env is build-time).
  </action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/frontend && bun install && bunx tsc --noEmit && grep -c '@theme' src/styles/globals.css && grep -c 'VITE_' .env.example</automated>
  </verify>
  <done>
    `frontend/` boots config-wise: tsc clean; `globals.css` has an `@theme` block with the UI-SPEC hex tokens (`grep -c 00FF85` and `EF4444` each >= 1); `lib/config.ts` zod-parses all VITE_ vars and is the only place constants live; `.env.example` lists every VITE_ var; `server.port: 3000` set in vite config; boot mode matches the SPIKE-NOTES decision; `frontend/spike/` deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Vitest harness + root layout shell + game-route placeholder + shadcn init</name>
  <files>
    frontend/vitest.config.ts,
    frontend/src/test/setup.ts,
    frontend/components.json,
    frontend/src/routes/__root.tsx,
    frontend/src/routes/index.tsx,
    frontend/src/components/ui/.gitkeep
  </files>
  <read_first>
    frontend/vite.config.ts,
    frontend/src/lib/config.ts,
    .planning/phases/07-frontend-vertical-slice/07-UI-SPEC.md
  </read_first>
  <action>
    Create `frontend/vitest.config.ts` reusing the Vite config (so the `@/*` alias, Tailwind, and workspace-TS transpile apply in tests): `test.environment = 'jsdom'`, `test.globals = true`, `test.setupFiles = ['src/test/setup.ts']`. Add devDeps `vitest @testing-library/react @testing-library/jest-dom jsdom` (RESEARCH §Validation Architecture — first Vitest in the repo; backend stays on bun:test). Create `frontend/src/test/setup.ts` importing `@testing-library/jest-dom`, stubbing `HTMLCanvasElement.prototype.getContext` (jsdom has no canvas), and mocking `window.matchMedia` (for `prefers-reduced-motion` gating in later plans — RESEARCH Pitfall 6).

    Run `bunx shadcn@latest init` against the TanStack Start + Tailwind v4 project (RESEARCH Assumption A5 — leave the tailwind config blank in `components.json` per shadcn tailwind-v4 guidance; if the CLI lacks a clean path, fall back to manual `components.json` + manual component files). Then `bunx shadcn@latest add button card dialog input label tabs sonner tooltip skeleton badge separator scroll-area progress`. Components land in `frontend/src/components/ui/` (keep a `.gitkeep` so the dir is tracked even if the CLI writes elsewhere — adjust `components.json` aliases to `@/components/ui`).

    Create `frontend/src/routes/__root.tsx`: the layout shell — a header row with (a) a logo/title, (b) a RESERVED empty slot for the Phase 8 "Fairness ✔" badge (UI-SPEC: reserve space only, do NOT implement the drawer), (c) a balance-pill slot (placeholder component or empty div 07-06 fills), (d) a connection-badge slot (07-04 fills), and the `<Toaster/>` from sonner mounted once. Apply the dark-casino surfaces (`bg-background`, card rails) and the D-01 layout scaffold containers (center stage + left/right rail regions for desktop ≥lg, single stacked column < md) as empty placeholder regions — later plans fill them. Use semantic Tailwind classes bound to the `@theme` tokens; NO hardcoded hex in components (UI-SPEC theming discipline).

    Create `frontend/src/routes/index.tsx`: the game route. If SPIKE-NOTES ratified `selective-ssr`, set `ssr: false` on this route. Render the D-01 responsive layout skeleton (history strip top, curve center, bet/feed rails) as labeled empty regions with the UI-SPEC empty-state copy where applicable — a visual shell that proves the theme + responsive breakpoints, with no live data yet. Do NOT add the auth guard here (07-04 adds `beforeLoad: beforeLoadFn`).
  </action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/frontend && bunx tsc --noEmit && bun run test 2>&1 | tail -5 && test -f components.json && echo COMPONENTS_OK</automated>
  </verify>
  <done>
    Vitest runs green (the config.test.ts from Task 1 passes under jsdom); `components.json` exists and shadcn components are installed under `src/components/ui/`; `__root.tsx` renders the themed header shell with reserved Fairness slot + Toaster + balance/connection slots; `index.tsx` renders the D-01 responsive layout skeleton with the boot-mode-appropriate `ssr` flag; tsc clean; no hardcoded hex in `.tsx` (`grep -rE "#[0-9A-Fa-f]{6}" src/routes src/components --include=*.tsx | grep -v globals.css` returns nothing).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time env (VITE_) | `VITE_`-prefixed vars are embedded in the client bundle — must contain NO secrets (issuer/client-id/urls are public OIDC config; client is a publicClient) |
| Browser → static assets | Pure client rendering; theme compiled at build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information Disclosure | VITE_ env in bundle | accept | Only public OIDC config (issuer, public client id, public Kong urls) is surfaced; no client secret exists (PKCE public client per realm-export); RESEARCH Security V2 |
| T-07-07 | Tampering | Hardcoded business constant bypassing config | mitigate | All constants flow through `lib/config.ts` zod-parsed env; `done` greps for hardcoded hex; CLAUDE.md §Configuration |
| T-07-SC | Tampering | npm installs (shadcn, vitest, fontsource) | mitigate | shadcn components are official-registry (UI-SPEC Registry Safety: no third-party); vitest/jsdom/testing-library/fontsource are mainstream MIT packages; FE deps already vetted Approved in RESEARCH Package Legitimacy Audit; no postinstall scripts |
</threat_model>

<verification>
- `cd frontend && bunx tsc --noEmit` clean
- `cd frontend && bun run test` green (config.test.ts passes)
- `grep -c '@theme' frontend/src/styles/globals.css` >= 1; contains `00FF85` and `EF4444`
- `grep -c 'VITE_' frontend/.env.example` covers all 10 vars from the interfaces block
- `frontend/components.json` exists; `frontend/src/components/ui/` populated
- `server.port: 3000` in `frontend/vite.config.ts`
- no hardcoded hex in `.tsx` route/component files (only in globals.css)
</verification>

<success_criteria>
- `bun run dev` (manual, at phase smoke) boots on :3000 with no hydration hang in the ratified boot mode and shows the themed dark-casino shell responsively
- Typed env config is the single source for every business constant
- Vitest + shadcn + theme foundation ready for 07-04/05/06 to extend
</success_criteria>

<output>
Create `.planning/phases/07-frontend-vertical-slice/07-03-SUMMARY.md` when done
</output>
