---
phase: 07-frontend-vertical-slice
plan: 08
type: execute
wave: 5
depends_on:
  - 07-05
  - 07-06
  - 07-07
files_modified:
  - frontend/src/components/balance-pill.tsx
  - frontend/src/components/connection-badge.tsx
  - frontend/src/components/game-skeletons.tsx
  - frontend/src/lib/toast.ts
  - frontend/src/features/juice/use-reduced-motion.ts
  - frontend/src/features/juice/celebrate.ts
  - frontend/src/features/juice/crash-flash.tsx
  - frontend/src/features/juice/use-count-up.ts
  - frontend/src/routes/index.tsx
  - frontend/src/routes/index.test.tsx
autonomous: true
requirements:
  - REQ-FE-12
  - REQ-FE-13
  - REQ-FE-14
must_haves:
  truths:
    - "The game route assembles the D-01 layout: history strip top, CrashCurve center stage, BetPanel+Cashout+Countdown left rail, LiveFeed right rail at lg+; single stacked column (history -> curve -> sticky controls -> feed) below md, with >=44px touch targets"
    - "Every async surface shows a skeleton while loading (initial round snapshot wait, history fetch) and the layout works on desktop + mobile breakpoints with working touch interactions"
    - "Error toasts are deduped by message key: insufficient balance, network/WS drop, bet-window-closed — the same key fired repeatedly shows one toast"
    - "Balance change animates a counter-up tween (Fira Code tabular); a cashout win triggers an accent glow + small confetti burst; a crash triggers a red flash + brief freeze overlay; the rising curve carries the emerald glow"
    - "prefers-reduced-motion disables confetti, crash flash, and counter-up (snap to final value instead); the curve still renders"
  artifacts:
    - path: "frontend/src/routes/index.tsx"
      provides: "Assembled responsive game page wiring all 07-05/06/07 components into the D-01 layout + skeletons + juice mounts"
      contains: "CrashCurve"
    - path: "frontend/src/lib/toast.ts"
      provides: "Deduped sonner toast helper keyed by message (insufficient-balance, network, bet-window-closed)"
      contains: "dedupe"
    - path: "frontend/src/features/juice/celebrate.ts"
      provides: "canvas-confetti single-burst cashout celebration, gated by reduced-motion"
      contains: "confetti"
    - path: "frontend/src/features/juice/crash-flash.tsx"
      provides: "Red flash + brief freeze overlay on round:crashed, gated by reduced-motion"
      contains: "CRASHED"
  key_links:
    - from: "frontend/src/routes/index.tsx"
      to: "frontend/src/components/crash-curve.tsx"
      via: "center-stage mount of the curve + rails"
      pattern: "CrashCurve"
    - from: "frontend/src/lib/toast.ts"
      to: "frontend/src/features/bet/use-place-bet.ts"
      via: "error key -> deduped toast"
      pattern: "toast"
    - from: "frontend/src/features/juice/celebrate.ts"
      to: "frontend/src/stores/bet.store.ts"
      via: "bet:my_cashed_out celebration flag triggers the burst"
      pattern: "celebrat"
---

<objective>
Assemble the full responsive game page (REQ-FE-12) and layer the cross-cutting UX: loading skeletons + deduped error toasts (REQ-FE-13) and the four sanctioned juice moments (REQ-FE-14). This is the wiring plan — it composes the components built in 07-05 (bet/cashout/countdown), 07-06 (curve), and 07-07 (feed/history) into the D-01 layout and fills the header slots reserved in 07-03 (balance pill, connection badge), then adds the motion + feedback layer.

Disciplines:
- D-01 layout: center-stage curve + left/right rails at `lg+`; single stacked column (history → curve ~40vh → sticky-bottom controls → capped feed) below `md`; touch targets ≥44px (UI-SPEC Spacing exception). Tailwind default breakpoints.
- Juice budget (D-04): counter-up balance, cashout glow + small confetti (≤~40 particles, single burst), crash red flash (≤200ms) + freeze overlay (~600–800ms). No screen shake, no particle storms. ALL gated behind `prefers-reduced-motion` (snap instead).
- Toasts deduped by message key; amber for warnings (insufficient balance, network) — red stays crash-only (D-02). Cashout-too-late is silent (no toast).
- canvas-confetti is already installed (07-03 dep set, Approved in RESEARCH audit) — no new install.

Purpose: turn the disjoint components into one playable, responsive, polished page — the deliverable the recruiter judges.
Output: balance-pill (counter-up) + connection-badge; game-skeletons; deduped `toast.ts`; reduced-motion hook + `celebrate.ts` (confetti) + `crash-flash.tsx` overlay + `use-count-up.ts`; the assembled `index.tsx` + a layout/behavior test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-frontend-vertical-slice/07-RESEARCH.md
@.planning/phases/07-frontend-vertical-slice/07-UI-SPEC.md
@.planning/phases/07-frontend-vertical-slice/07-CONTEXT.md
@.planning/phases/07-frontend-vertical-slice/07-03-PLAN.md
@.planning/phases/07-frontend-vertical-slice/07-05-PLAN.md
@.planning/phases/07-frontend-vertical-slice/07-06-PLAN.md
@.planning/phases/07-frontend-vertical-slice/07-07-PLAN.md
@CLAUDE.md
@frontend/src/lib/config.ts

<interfaces>
<!-- Components to compose (built in 07-05/06/07) -->
<CrashCurve/>        // center stage (07-06)
<BetPanel/> <CashoutButton/> <Countdown/>   // left rail / controls (07-05)
<LiveFeed/>          // right rail (07-07)
<HistoryStrip/>      // top strip (07-07)

<!-- Header slots reserved in 07-03 __root.tsx: balance-pill slot, connection-badge slot, Fairness placeholder (P8), <Toaster/> already mounted -->

<!-- Stores (07-04) the cross-cutting layer reads -->
wallet.store     : { balance: MoneySnapshot | null }                 // counter-up source
bet.store        : { myBet, celebrate?: boolean }                    // 07-04 sets a celebration flag on bet:my_cashed_out
round.store      : { status }                                        // CRASHED triggers flash; BETTING/snapshot-pending triggers skeleton
connection slice : connected / reconnecting (07-04 use-game-socket wires socket lifecycle)  // read frontend/src/ws/use-game-socket.ts

<!-- sonner (mounted in __root.tsx 07-03) -->
import { toast } from 'sonner'   // toast(...) ; toast.dismiss(id) — dedupe via a Set/Map of active keys

<!-- canvas-confetti (07-03 dep; Approved RESEARCH audit) -->
import confetti from 'canvas-confetti'   // confetti({ particleCount, spread, colors, origin })

<!-- UI-SPEC juice budget + copy -->
counter-up ~400ms ease-out (Fira Code tabular) ; confetti <=~40 particles ~800ms emerald/cyan ; crash flash <=200ms + freeze 600-800ms
toasts: insufficient-balance "Not enough balance. Lower your bet or wait for a win." ; bet-window-closed "Betting window closed - your bet was refunded." ; network "Connection lost. Reconnecting..."
mobile: controls sticky-bottom ; curve ~40vh ; rails -> full-width stacked cards
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Header slots (balance counter-up + connection badge) + skeletons + deduped toast helper</name>
  <files>
    frontend/src/components/balance-pill.tsx,
    frontend/src/components/connection-badge.tsx,
    frontend/src/components/game-skeletons.tsx,
    frontend/src/lib/toast.ts,
    frontend/src/features/juice/use-reduced-motion.ts,
    frontend/src/features/juice/use-count-up.ts
  </files>
  <read_first>
    frontend/src/routes/__root.tsx,
    frontend/src/stores/wallet.store.ts,
    frontend/src/ws/use-game-socket.ts,
    frontend/src/test/setup.ts,
    .planning/phases/07-frontend-vertical-slice/07-UI-SPEC.md
  </read_first>
  <behavior>
    - dedupedToast('insufficient-balance', msg) called 3x in quick succession results in ONE active toast for that key
    - dedupedToast clears a key when its toast is dismissed/auto-closed so the next occurrence can show again
    - useReducedMotion() returns true when matchMedia('(prefers-reduced-motion: reduce)') matches (mockable)
    - useCountUp(target) tweens from prev to target over the budget when motion allowed; snaps instantly to target when reduced-motion
    - BalancePill renders the balance via Money.fromSnapshot(...).toString() in Fira Code tabular and counts up on change
    - ConnectionBadge shows "Live" (accent dot) when connected, "Reconnecting..." when not
  </behavior>
  <action>
    Create `frontend/src/features/juice/use-reduced-motion.ts`: a hook reading `window.matchMedia('(prefers-reduced-motion: reduce)')` with a listener (the `matchMedia` mock from `src/test/setup.ts` makes this testable). Create `frontend/src/features/juice/use-count-up.ts`: `useCountUp(targetCents: bigint)` returning a Money to render — tween the displayed cents from the previous to the target over ~400ms ease-out via rAF when motion is allowed; when `useReducedMotion()` is true, snap to target immediately (UI-SPEC reduced-motion). Render values as Money (`Money.of(currentCents)`), never a raw number.

    Create `frontend/src/components/balance-pill.tsx`: subscribe to `wallet.store.balance`; feed `useCountUp` the balance cents; render `Money.of(displayedCents).toString()` in Fira Code tabular (the `badge`/custom pill). Create `frontend/src/components/connection-badge.tsx`: read the connection state exposed by `use-game-socket` (07-04); render a shadcn `badge` with an accent "Live" dot when connected and amber "Reconnecting…" when not (red stays crash-only).

    Create `frontend/src/components/game-skeletons.tsx`: composed shadcn `skeleton` blocks for (a) the initial round-snapshot wait (curve + multiplier placeholder) and (b) the history-fetch (chip-row placeholders). Export named skeletons the route renders while the respective data is pending (REQ-FE-13).

    Create `frontend/src/lib/toast.ts`: a `dedupedToast(key: string, message: string, opts?)` wrapping sonner `toast`. Maintain a `Map<key, toastId>` of active toasts; if a key is already active, do nothing (or refresh duration) instead of stacking; clear the key on dismiss/auto-close (via sonner's `onDismiss`/`onAutoClose` or a timeout matching the toast duration). Warnings use the amber/warning sonner variant (never red). Export typed helpers for the three keys (insufficient-balance, network, bet-window-closed) with the UI-SPEC copy.

    Test these in the route test (Task 2) plus a small unit assertion on `dedupedToast` dedup behavior (mock sonner's `toast`, call the same key 3x, assert `toast` invoked once) — put that unit test inline in `index.test.tsx` or a focused `toast` describe block.
  </action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/frontend && bunx tsc --noEmit && grep -c 'matchMedia' src/features/juice/use-reduced-motion.ts</automated>
  </verify>
  <done>
    `dedupedToast` shows one toast per active key and clears on close; warnings amber (no red); `useReducedMotion` reads matchMedia; `useCountUp` tweens or snaps per reduced-motion and renders Money (no bare number); BalancePill counts up in Fira Code tabular; ConnectionBadge reflects connected/reconnecting; skeletons exist for snapshot + history; tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confetti celebrate + crash flash/freeze overlay + assembled responsive game route</name>
  <files>
    frontend/src/features/juice/celebrate.ts,
    frontend/src/features/juice/crash-flash.tsx,
    frontend/src/routes/index.tsx,
    frontend/src/routes/index.test.tsx
  </files>
  <read_first>
    frontend/src/components/crash-curve.tsx,
    frontend/src/components/bet-panel.tsx,
    frontend/src/components/cashout-button.tsx,
    frontend/src/components/countdown.tsx,
    frontend/src/components/live-feed.tsx,
    frontend/src/components/history-strip.tsx,
    frontend/src/components/balance-pill.tsx,
    frontend/src/components/connection-badge.tsx,
    frontend/src/components/game-skeletons.tsx,
    frontend/src/features/juice/use-reduced-motion.ts,
    frontend/src/stores/round.store.ts,
    frontend/src/stores/bet.store.ts
  </read_first>
  <behavior>
    - celebrate() fires a single canvas-confetti burst (<=~40 particles, emerald/cyan colors) when motion allowed; no-op when reduced-motion
    - the celebration triggers exactly once per bet:my_cashed_out (the 07-04 celebrate flag), not in a loop
    - CrashFlash renders a red flash + freeze overlay only while round.store.status === 'CRASHED' (and briefly after), then clears; no-op visuals (no flash) when reduced-motion but the freeze value still shows
    - at lg+ the page renders history(top) + left rail (bet/cashout/countdown) + center CrashCurve + right rail (feed); below md it renders a single stacked column with sticky-bottom controls
    - while the initial snapshot is pending the curve area shows the snapshot skeleton; while history is pending the strip shows chip skeletons
    - the cashout-too-late path produces NO toast (silent)
  </behavior>
  <action>
    Create `frontend/src/features/juice/celebrate.ts`: a `celebrate()` calling `confetti({ particleCount: <=40, spread, colors: [emerald, cyan], origin })` — a SINGLE burst (no loop, no interval), gated: return early if `prefersReducedMotion()` (read via a non-hook accessor to matchMedia so it works outside React). Colors are the accent tokens (concrete strings allowed here as confetti needs literal colors — centralize as named consts). Create `frontend/src/features/juice/crash-flash.tsx`: an overlay component subscribing to `round.store.status`; when it transitions to `CRASHED`, render a red (`#EF4444` crash token) flash (≤200ms fade) then a brief freeze overlay (~600–800ms) holding the crashed value, then clear; honor reduced-motion (skip the flash animation, still show the frozen value briefly). One absolutely-positioned overlay over the curve stage; no screen shake.

    Rewrite `frontend/src/routes/index.tsx` (replacing the 07-03 placeholder skeleton, keeping the 07-04 `beforeLoad: beforeLoadFn` guard and any `ssr:false` flag) to assemble the D-01 layout: a CSS grid/flex that at `lg+` places `HistoryStrip` across the top, `BetPanel`+`CashoutButton`+`Countdown` in the ~320px left rail, `CrashCurve` (+ `CrashFlash` overlay) in the dominant center stage, `LiveFeed` in the ~320px right rail; below `md` collapse to a single column ordered history → curve (~40vh) → controls (sticky-bottom) → feed (capped, scroll), with ≥44px touch targets (REQ-FE-12). Render the snapshot skeleton in the curve slot while `round.store` has no snapshot yet and the history skeleton while `useHistory` is loading (REQ-FE-13). Subscribe to `bet.store.celebrate` and call `celebrate()` once per cash-out event (clear the flag after firing). Wire the three error keys from the 07-05 mutation hooks to `dedupedToast`; keep cashout-too-late silent. Mount `BalancePill` + `ConnectionBadge` into the header slots reserved in `__root.tsx`. NO hardcoded hex in the route (only the centralized confetti/flash color consts in the juice modules).

    Test `frontend/src/routes/index.test.tsx` (Vitest + @testing-library/react, mock canvas-confetti + the stores): (a) with a pending snapshot the curve-area skeleton is shown, then with a seeded RUNNING snapshot the CrashCurve mounts; (b) setting `bet.store.celebrate=true` fires `confetti` exactly once and the flag clears; with reduced-motion mocked true, `confetti` is NOT called; (c) status CRASHED renders the crash-flash overlay; (d) the layout renders both rails (assert presence of bet panel + live feed regions) — breakpoint-specific stacking is asserted via the responsive class markers; (e) firing the same toast key twice yields one toast (dedupe).
  </action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge/frontend && bunx tsc --noEmit && bun run test src/routes/index.test.tsx 2>&1 | tail -8</automated>
  </verify>
  <done>
    `celebrate()` fires a single gated confetti burst (no-op under reduced-motion); `CrashFlash` shows red flash + freeze only on CRASHED honoring reduced-motion; `index.tsx` assembles the D-01 two-rail lg+ / stacked mobile layout with ≥44px touch targets, snapshot+history skeletons, header balance/connection slots, deduped error toasts, silent cashout-too-late; all route test cases green; tsc clean; no hardcoded hex in `index.tsx` (`grep -rE "#[0-9A-Fa-f]{6}" src/routes/index.tsx` empty); confetti is a single burst (`grep -c 'setInterval' src/features/juice/celebrate.ts` == 0).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS connection state → UI | Connection badge reflects socket lifecycle; reconnect re-handshakes with a fresh token (07-04) |
| Celebration trigger | Driven by the server-authoritative `bet:my_cashed_out` flag (07-04), not a client guess |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-21 | Denial of Service | Toast storm on repeated network errors | mitigate | `dedupedToast` keys by message; one active toast per key; cleared on close (REQ-FE-13 dedupe) |
| T-07-22 | Denial of Service | Confetti loop / heavy particles janking the rAF curve | mitigate | Single burst ≤~40 particles, no interval; gated behind reduced-motion; `done` greps no setInterval (D-04 budget) |
| T-07-23 | Spoofing | Celebrating a cashout the server did not credit | mitigate | Celebration fires only on the `bet:my_cashed_out` WS flag (server-authoritative payout snapshot), never on the optimistic client cashout click |
| T-07-24 | Information Disclosure | Red used for non-crash (semantic spoofing of "crash") | mitigate | Toasts/warnings use amber; red reserved for the crash flash/freeze only (D-02); `done` greps route for no stray hex |
| T-07-SC | Tampering | npm installs (canvas-confetti) | accept | canvas-confetti@1.9.4 already installed in 07-03; Approved in RESEARCH Package Legitimacy Audit (13.5M wk dl, no postinstall) — no new install here |
</threat_model>

<verification>
- `cd frontend && bunx tsc --noEmit && bun run test src/routes/index.test.tsx` green
- `index.tsx` mounts CrashCurve + BetPanel + CashoutButton + Countdown + LiveFeed + HistoryStrip in the D-01 layout; header slots filled with BalancePill + ConnectionBadge
- snapshot + history skeletons render while pending (REQ-FE-13)
- `dedupedToast` shows one toast per key; warnings amber, red crash-only
- `celebrate()` single burst, gated by reduced-motion; CrashFlash only on CRASHED; counter-up snaps under reduced-motion (REQ-FE-14)
- responsive: two rails at lg+, stacked + sticky controls below md, ≥44px touch targets (REQ-FE-12)
- no hardcoded hex in `index.tsx`; no setInterval in celebrate.ts
- (live desktop+mobile smoke + Playwright critical-loop E2E is the phase verify/Phase 10 gate)
</verification>

<success_criteria>
- The full game page is assembled and responsive on desktop + mobile with working touch interactions
- Loading skeletons + deduped toasts cover every async/error surface
- The four juice moments fire correctly and all honor prefers-reduced-motion
- The phase vertical slice is playable end-to-end pending the live smoke gate
</success_criteria>

<output>
Create `.planning/phases/07-frontend-vertical-slice/07-08-SUMMARY.md` when done
</output>
