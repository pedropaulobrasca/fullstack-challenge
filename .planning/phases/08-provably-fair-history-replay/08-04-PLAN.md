---
phase: 08-provably-fair-history-replay
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/.env.example
  - frontend/src/lib/config.ts
  - frontend/src/lib/config.test.ts
  - frontend/src/features/fairness/fairness.store.ts
  - frontend/src/features/fairness/fairness.store.test.ts
  - frontend/src/features/replay/replay.store.ts
  - frontend/src/features/replay/replay.store.test.ts
  - frontend/components.json
autonomous: true
requirements:
  - REQ-FE-09
  - REQ-FE-10
  - REQ-REPLAY-02

must_haves:
  truths:
    - "`VITE_REPLAY_SPEEDS`, `VITE_REPLAY_AUTOSTART`, `VITE_DRAWER_SLIDE_MS`, `VITE_INSTANT_CRASH_BUCKET` are parsed by getConfig() with defaults [1,2,4] / true / 220 / 101 and observable on the AppConfig type"
    - "AppConfig exposes a `fairness: { instantCrashBucket: number }` section consumed by Plan 08-06's /verify/:roundId route (deriveCrashPointAsync requires the bucket)"
    - "Fairness store tracks per-roundId verdict cache (Map<roundId, 'MATCH'|'MISMATCH'>) + a transient recentlyVerifiedRoundId with a 4-second TTL"
    - "Fairness store ALSO tracks `drawerOpen: boolean` from initial design with `openDrawer()` / `closeDrawer()` actions so the FairnessBadge (Plan 08-05) and VerificationDrawer (Plan 08-05) share open-state without prop drilling"
    - "Replay store tracks the open roundId (string | null), playing flag (boolean), and active speed (number); openReplay(id) and closeReplay() are the only mutators"
    - "shadcn sheet + toggle-group + alert components are installed (dialog already from P7)"
  artifacts:
    - path: "frontend/src/lib/config.ts"
      provides: "Extended AppConfig with replay + drawer + fairness config sections"
      contains: "replay:"
    - path: "frontend/src/features/fairness/fairness.store.ts"
      provides: "Zustand store for cached verification verdicts + transient checkmark state + drawerOpen UI state"
      exports: ["useFairnessStore"]
    - path: "frontend/src/features/replay/replay.store.ts"
      provides: "Zustand store for the replay modal (roundId, playing, speed)"
      exports: ["useReplayStore"]
    - path: "frontend/src/components/ui/sheet.tsx"
      provides: "shadcn Sheet component"
    - path: "frontend/src/components/ui/toggle-group.tsx"
      provides: "shadcn ToggleGroup component"
    - path: "frontend/src/components/ui/alert.tsx"
      provides: "shadcn Alert component"
  key_links:
    - from: "frontend/.env.example"
      to: "frontend/src/lib/config.ts"
      via: "zod parse of VITE_REPLAY_SPEEDS/VITE_REPLAY_AUTOSTART/VITE_DRAWER_SLIDE_MS/VITE_INSTANT_CRASH_BUCKET"
      pattern: "VITE_REPLAY_SPEEDS|VITE_REPLAY_AUTOSTART|VITE_DRAWER_SLIDE_MS|VITE_INSTANT_CRASH_BUCKET"
    - from: "frontend/src/features/fairness/fairness.store.ts (drawerOpen state)"
      to: "frontend/src/components/fairness-badge.tsx + verification-drawer.tsx (Plan 08-05 consumers)"
      via: "openDrawer/closeDrawer actions read by both surfaces"
      pattern: "drawerOpen|openDrawer|closeDrawer"
---

<objective>
Land the four env additions surfaced by UI-SPEC (`VITE_REPLAY_SPEEDS`, `VITE_REPLAY_AUTOSTART`, `VITE_DRAWER_SLIDE_MS`, `VITE_INSTANT_CRASH_BUCKET`) through the typed `getConfig()` layer with defaults `[1,2,4]` / `true` / `220` / `101` per D-03 + UI-SPEC § Config-driven (the bucket value mirrors the server's INSTANT_CRASH_BUCKET so Plan 08-06's /verify route can run deriveCrashPointAsync with the correct config; landing the env additions here keeps Plan 08-06's files_modified disjoint from this plan's). Install the three new shadcn components (`sheet`, `toggle-group`, `alert`) — `dialog` already present from Phase 7 (T-07-SC package legitimacy honored: only shadcn-official registry blocks, no third-party). Land two new Zustand slices used by the FE surfaces in Wave 2/3: `fairness.store.ts` (verdict cache + transient MATCH checkmark — resolves RESEARCH Open Question 1) and `replay.store.ts` (replay modal open/close + play/pause + speed).

Per CLAUDE.md: NO hardcoded business constants — replay speeds, autostart, drawer slide duration, instant-crash bucket MUST live in env. Per REQ-INFRA-05 enforcement.

Purpose: All Wave 2/3 UI plans (08-05 drawer, 08-06 verify route, 08-07 replay modal) read these env vars and these stores. Landing them together in Wave 2 makes the UI plans clean and parallelisable.

Output: Extended `getConfig()` with `replay: { speeds: number[]; autostart: boolean }`, `drawer: { slideMs: number }`, and `fairness: { instantCrashBucket: number }` sections; three shadcn primitives installed and committed; two Zustand stores with unit tests (fairness.store includes drawerOpen UI state from initial design).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-provably-fair-history-replay/08-CONTEXT.md
@.planning/phases/08-provably-fair-history-replay/08-RESEARCH.md
@.planning/phases/08-provably-fair-history-replay/08-UI-SPEC.md

@frontend/.env.example
@frontend/src/lib/config.ts
@frontend/src/lib/config.test.ts
@frontend/components.json
@frontend/package.json

<interfaces>
<!-- Phase 7 shapes the additions must extend, not replace. -->

From frontend/src/lib/config.ts:
- `configSchema` (zod) with `VITE_*` keys
- `AppConfig` Readonly type with sections: `keycloak`, `ws`, `rest`, `growthRate`, `ewmaAlpha`, `history`, `bet`, `currencyCode`, `feedBufferSize`, `historySize`
- `buildConfig(env)` returns Object.freeze of nested Readonly sections
- `getConfig()` lazy singleton

From shadcn components.json: existing components in Phase 7 set — sheet/toggle-group/alert are NEW per UI-SPEC § Cross-References.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend env schema + config with replay/drawer sections (env-tunable, no hardcoded defaults in code)</name>
  <files>
    frontend/.env.example,
    frontend/src/lib/config.ts,
    frontend/src/lib/config.test.ts
  </files>
  <read_first>
    frontend/src/lib/config.ts (zod schema + buildConfig + AppConfig type),
    frontend/src/lib/config.test.ts (existing test pattern + fixture),
    frontend/.env.example (current var set + style)
  </read_first>
  <behavior>
    - `VITE_REPLAY_SPEEDS=1,2,4` parses to `number[]` with each element `> 0` and `Number.isFinite`; default `[1,2,4]` per D-03 only applies when the env key is absent.
    - `VITE_REPLAY_AUTOSTART=true` parses to `boolean`; default `true` per D-03.
    - `VITE_DRAWER_SLIDE_MS=220` parses to a positive integer; default `220` per UI-SPEC.
    - `VITE_INSTANT_CRASH_BUCKET=101` parses to a positive integer; default `101` (mirrors server INSTANT_CRASH_BUCKET; Plan 08-06 /verify route consumes this).
    - `getConfig()` returns `AppConfig` with `replay: { speeds: readonly number[]; autostart: boolean }`, `drawer: { slideMs: number }`, AND `fairness: { instantCrashBucket: number }` sections frozen.
    - Existing config keys are UNCHANGED — additive only.
    - `VITE_REPLAY_SPEEDS` parsing rejects malformed inputs (`"1,abc,4"` throws); the test asserts this.
  </behavior>
  <action>
    Patch `frontend/src/lib/config.ts`:

    1. Add to `configSchema` (zod): `VITE_REPLAY_SPEEDS: z.string().optional().default("1,2,4").transform((raw) => raw.split(",").map((s) => Number(s.trim()))).pipe(z.array(z.number().positive().finite()).nonempty())`; `VITE_REPLAY_AUTOSTART: z.coerce.boolean().default(true)` (note: `z.coerce.boolean` treats any truthy string as true — confirm semantics work for `"true"`/`"false"` env values by reading Phase 7 patterns; if needed, fall back to a `z.string().optional().default("true").transform((s) => s === "true")` form to match the strict `"true"`-only contract); `VITE_DRAWER_SLIDE_MS: z.coerce.number().int().positive().default(220)`; `VITE_INSTANT_CRASH_BUCKET: z.coerce.number().int().positive().default(101)`.

    2. Extend the `AppConfig` Readonly type:
       - `readonly replay: Readonly<{ speeds: readonly number[]; autostart: boolean }>`
       - `readonly drawer: Readonly<{ slideMs: number }>`
       - `readonly fairness: Readonly<{ instantCrashBucket: number }>`

    3. Extend `buildConfig(env)` to populate the three new sections via `Object.freeze` + `Object.freeze([...env.VITE_REPLAY_SPEEDS])` to make the speeds array immutable. The 1x default constraint per D-03 (1 must be present): inside `buildConfig`, after parsing, assert `env.VITE_REPLAY_SPEEDS.includes(1)` — if false, throw with a clear error message (RESEARCH Anti-pattern: "if parsed array literal contains 1, treat 1 as the default" — this hard-asserts the invariant). Populate `fairness: Object.freeze({ instantCrashBucket: env.VITE_INSTANT_CRASH_BUCKET })`.

    Patch `frontend/.env.example`: append a new "Replay & Verification UX" section after the existing history block with 4 keys, each preceded by a one-line `#` comment:
    - `# Replay speed selector options (D-03 / UI-SPEC). Must include 1.`
    - `VITE_REPLAY_SPEEDS=1,2,4`
    - `# Replay modal auto-plays on open at 1x.`
    - `VITE_REPLAY_AUTOSTART=true`
    - `# Verification drawer slide-in duration (MCP motion band 150-300ms).`
    - `VITE_DRAWER_SLIDE_MS=220`
    - `# Backend INSTANT_CRASH_BUCKET mirror; must match server env (Bustabit canon 1-in-N). Consumed by /verify/:roundId route.`
    - `VITE_INSTANT_CRASH_BUCKET=101`

    Extend `frontend/src/lib/config.test.ts` to cover the new fields:
    - Existing 7 tests stay green.
    - New test: with `VITE_REPLAY_SPEEDS="1,2,4"`, `config.replay.speeds` equals `[1, 2, 4]`.
    - New test: with `VITE_REPLAY_AUTOSTART="true"`, `config.replay.autostart === true`.
    - New test: with `VITE_DRAWER_SLIDE_MS="220"`, `config.drawer.slideMs === 220`.
    - New test: with `VITE_INSTANT_CRASH_BUCKET="101"`, `config.fairness.instantCrashBucket === 101`.
    - New test: with `VITE_INSTANT_CRASH_BUCKET` overridden to `"77"`, `config.fairness.instantCrashBucket === 77`.
    - New test: malformed speeds `"1,abc,4"` throws on parse.
    - New test: speeds missing 1 (`"2,4"`) throws on build with the expected message.
    - New test: defaults apply when env vars are absent (`config.replay.speeds === [1,2,4]`, `config.replay.autostart === true`, `config.drawer.slideMs === 220`, `config.fairness.instantCrashBucket === 101`).

    The test fixture builder (whatever `config.test.ts` uses to construct an env object) needs the four new keys threaded through — preserve the existing fixture utility shape.

    No code fences in this action body.
  </action>
  <acceptance_criteria>
    - `frontend/.env.example` contains all four new keys (grep all four): `grep -q VITE_REPLAY_SPEEDS frontend/.env.example && grep -q VITE_REPLAY_AUTOSTART frontend/.env.example && grep -q VITE_DRAWER_SLIDE_MS frontend/.env.example && grep -q VITE_INSTANT_CRASH_BUCKET frontend/.env.example`
    - `frontend/src/lib/config.ts` declares `replay:`, `drawer:`, AND `fairness:` Readonly sections on the AppConfig type
    - `cd frontend && bunx vitest run src/lib/config.test.ts` reports >= 14 tests (original 7 + 8 new: speeds-from-env, autostart, drawer-slide, bucket-default, bucket-override, malformed-speeds, speeds-missing-1, all-defaults) all PASS
    - `cd frontend && bunx tsc --noEmit` exit 0
    - No business constant `1,2,4` / `220` / `101` literal appears in any source TSX/TS file outside `config.ts`, `.env.example`, and the config test (grep audit at plan-end)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; bunx tsc --noEmit &amp;&amp; bunx vitest run src/lib/config.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    Four env keys parsed, defaulted, validated; AppConfig surface extended with replay/drawer/fairness; the "must include 1" invariant is asserted at build time; tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn sheet + toggle-group + alert components (dialog already present)</name>
  <files>
    frontend/components.json,
    frontend/src/components/ui/sheet.tsx,
    frontend/src/components/ui/toggle-group.tsx,
    frontend/src/components/ui/alert.tsx,
    frontend/package.json
  </files>
  <read_first>
    frontend/components.json (current shadcn config — style: new-york, lucide icon set),
    frontend/src/components/ui/ (existing Phase 7 components — confirm dialog.tsx is present, sheet/toggle-group/alert are absent),
    frontend/package.json (existing deps — confirm @radix-ui/react-dialog is installed via Phase 7)
  </read_first>
  <behavior>
    - `frontend/src/components/ui/sheet.tsx` exists and exports `Sheet`, `SheetTrigger`, `SheetContent`, `SheetHeader`, `SheetFooter`, `SheetTitle`, `SheetDescription`, `SheetClose` (canonical shadcn surface).
    - `frontend/src/components/ui/toggle-group.tsx` exists and exports `ToggleGroup`, `ToggleGroupItem`.
    - `frontend/src/components/ui/alert.tsx` exists and exports `Alert`, `AlertTitle`, `AlertDescription`.
    - `frontend/package.json` includes `@radix-ui/react-dialog` (already there per Phase 7 dialog install — sheet reuses it) AND `@radix-ui/react-toggle-group` (added by shadcn CLI as a transitive). No third-party shadcn registry blocks (T-08-SC: only the shadcn official registry per UI-SPEC § Registry Safety).
    - All three component files are dark-casino-token compatible — use `bg-popover`, `text-popover-foreground`, `border-border` etc., NOT hardcoded hex.
  </behavior>
  <action>
    Run `cd frontend && bunx shadcn@latest add sheet toggle-group alert --yes` (or the project's interactive equivalent — match Phase 7 install style; check `package.json` scripts).

    After install:
    1. Verify the three files exist in `frontend/src/components/ui/`. If shadcn added them under a different path due to `components.json` config, leave them in place but log the actual path in the SUMMARY.
    2. Open each file and confirm theme-token usage (no `#xxxxxx` literals in JSX — the new-york shadcn template should use `bg-popover` / `bg-card` / `border-border` / `text-foreground` etc. directly).
    3. Confirm `package.json` shows new transitive deps. If `@radix-ui/react-toggle-group` did NOT get auto-installed, run `bun add @radix-ui/react-toggle-group@^1.1.11` (version per RESEARCH Package Legitimacy Audit row).
    4. Run `cd frontend && bunx tsc --noEmit` — confirm 0 errors after install.
    5. Run `cd frontend && bun run test` — full suite must remain green.

    Do NOT install any third-party registry block. Do NOT install `crypto-js`, `js-sha256`, or any hand-rolled crypto package (RESEARCH § Don't Hand-Roll — `crypto.subtle` is the standard).

    Per Phase 7 T-07-SC convention: this install rides through the package-legitimacy gate inline because all three are shadcn-official + Radix (the audit table in RESEARCH already shows OK for both `@radix-ui/react-toggle-group@1.1.11` and `@radix-ui/react-dialog@1.1.15`). No separate package-legitimacy human checkpoint is needed for this plan (RESEARCH Package Legitimacy Audit closed).
  </action>
  <acceptance_criteria>
    - `ls frontend/src/components/ui/sheet.tsx frontend/src/components/ui/toggle-group.tsx frontend/src/components/ui/alert.tsx` all exist
    - `grep -q '"@radix-ui/react-toggle-group"' frontend/package.json` — toggle-group radix dep is present
    - `cd frontend && bunx tsc --noEmit` exit 0
    - `cd frontend && bun run test` exit 0 (full suite green; sheet/toggle-group/alert have no behavior tests yet — those land with their consumer surfaces in 08-05/06/07)
    - No `#[0-9a-fA-F]{3,6}` hex literal in any of the three new component files: `! grep -RIn '#[0-9a-fA-F]\{3,6\}' frontend/src/components/ui/sheet.tsx frontend/src/components/ui/toggle-group.tsx frontend/src/components/ui/alert.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; ls src/components/ui/sheet.tsx src/components/ui/toggle-group.tsx src/components/ui/alert.tsx &amp;&amp; bunx tsc --noEmit &amp;&amp; bun run test</automated>
  </verify>
  <done>
    All three shadcn components present, theme-token compatible, FE suite still green, no third-party registry contamination.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author fairness.store.ts + replay.store.ts Zustand slices with unit tests</name>
  <files>
    frontend/src/features/fairness/fairness.store.ts,
    frontend/src/features/fairness/fairness.store.test.ts,
    frontend/src/features/replay/replay.store.ts,
    frontend/src/features/replay/replay.store.test.ts
  </files>
  <read_first>
    frontend/src/stores/round.store.ts (Zustand convention used in the repo — create/subscribe patterns),
    frontend/src/stores/multiplier.store.ts (slice-per-concern pattern),
    frontend/src/stores/history.store.ts (newest-first + cap pattern, in case useful as reference),
    .planning/phases/08-provably-fair-history-replay/08-UI-SPEC.md (Surface A state matrix — transient MATCH 4s; Surface D state matrix — speed/play/open)
  </read_first>
  <behavior>
    fairness.store:
    - State shape: `{ verdicts: Map<string, "MATCH" | "MISMATCH">; recentlyVerifiedRoundId: string | null; recentlyVerifiedExpiresAt: number | null; drawerOpen: boolean }`
    - Initial state includes `drawerOpen: false`.
    - Action `recordVerdict(roundId, verdict)` sets `verdicts.get(roundId) = verdict` AND if verdict is MATCH sets `recentlyVerifiedRoundId = roundId` + `recentlyVerifiedExpiresAt = Date.now() + 4000` (4-second transient per UI-SPEC Surface A "transient checkmark after recent verified MATCH" + State Matrix "brief CheckCircle2 swap (4s)"); MISMATCH does NOT set the transient.
    - Action `clearRecentlyVerified()` resets `recentlyVerifiedRoundId = null` + `recentlyVerifiedExpiresAt = null`.
    - Action `openDrawer()` sets `drawerOpen = true`. Action `closeDrawer()` sets `drawerOpen = false`. Both leave verdicts/recentlyVerified untouched.
    - Selector `selectVerdict(roundId): "MATCH" | "MISMATCH" | undefined` reads from `verdicts`.
    - Selector `selectRecentlyVerified(roundId, now=Date.now()): boolean` returns true iff `recentlyVerifiedRoundId === roundId AND recentlyVerifiedExpiresAt !== null AND now < recentlyVerifiedExpiresAt`.
    - No autostarted timers (the 4s expiry is read-as-needed; consumers poll via the selector or set up their own `setTimeout` cleanup).
    - drawerOpen ships in the INITIAL store design (this plan) so Plan 08-05 only CONSUMES openDrawer/closeDrawer — no retroactive edit of fairness.store.ts in Plan 08-05.

    replay.store:
    - State shape: `{ roundId: string | null; playing: boolean; speed: number }`
    - Initial state: `{ roundId: null, playing: false, speed: 1 }` — 1x default per D-03.
    - Action `openReplay(roundId, autostart, defaultSpeed)`: sets `roundId = roundId`, `playing = autostart`, `speed = defaultSpeed`. Consumers pass `getConfig().replay.autostart` and the first element of `getConfig().replay.speeds` (or explicitly 1) — the store has NO direct config dependency to keep it test-clean.
    - Action `closeReplay()`: resets to initial state.
    - Action `setPlaying(playing: boolean)`.
    - Action `setSpeed(speed: number)` — must validate the value is in `getConfig().replay.speeds`? — keep the store config-agnostic: validation is the caller's responsibility (Plan 08-07 wires the toggle to legal speeds only).
    - Selector `selectIsReplayOpen(state): boolean` = `state.roundId !== null`.

    Tests for fairness.store:
    - Test 1: initial state has empty verdicts Map + null transient + `drawerOpen === false`.
    - Test 2: `recordVerdict("r1", "MATCH")` → `verdicts.get("r1") === "MATCH"`; `recentlyVerifiedRoundId === "r1"`; `recentlyVerifiedExpiresAt > Date.now()`.
    - Test 3: `recordVerdict("r2", "MISMATCH")` → `verdicts.get("r2") === "MISMATCH"`; `recentlyVerifiedRoundId` NOT changed (stays previous).
    - Test 4: `selectRecentlyVerified("r1", Date.now())` returns true right after MATCH; returns false after `Date.now() + 5000`.
    - Test 5: `clearRecentlyVerified()` resets both transient fields, verdicts Map untouched.
    - Test 6: `openDrawer()` flips `drawerOpen` from false to true; verdicts Map untouched.
    - Test 7: `closeDrawer()` flips `drawerOpen` back to false; verdicts Map untouched.

    Tests for replay.store:
    - Test 1: initial state `{ roundId: null, playing: false, speed: 1 }`.
    - Test 2: `openReplay("r1", true, 1)` → `{ roundId: "r1", playing: true, speed: 1 }`.
    - Test 3: `openReplay("r2", false, 2)` → `{ roundId: "r2", playing: false, speed: 2 }`.
    - Test 4: `closeReplay()` resets to initial.
    - Test 5: `setPlaying(true)` then `setPlaying(false)` toggles `playing` accordingly without affecting roundId/speed.
    - Test 6: `setSpeed(4)` updates `speed` to 4 without affecting roundId/playing.
    - Test 7: `selectIsReplayOpen` returns false initially, true after openReplay, false after closeReplay.
  </behavior>
  <action>
    Create `frontend/src/features/fairness/fairness.store.ts` using the same `create<T>()(...)` Zustand pattern as `frontend/src/stores/round.store.ts` (read that file to confirm the exact `create` import + middleware style — likely no middleware, just plain create). Use a plain JS `Map` for `verdicts` and mutate by spreading into a new Map per Zustand-immutability convention (`set((s) => ({ verdicts: new Map(s.verdicts).set(id, verdict), ... }))`).

    Create `frontend/src/features/replay/replay.store.ts` using the same pattern with the smaller state shape.

    Author both test files using Vitest + the same harness style as existing store tests in the repo. After each test, reset state via `useFairnessStore.setState(initialState)` (or whatever Zustand reset pattern the existing tests use — match style).

    Both stores live under `frontend/src/features/<feature>/` (RESEARCH Recommended Project Structure) rather than `frontend/src/stores/` because they are feature-specific (used only by fairness/replay components), not global state slices used across the live game.

    Export the hook + named selectors so consumers can do `useFairnessStore((s) => selectRecentlyVerified(s, "r1"))` from any component.
  </action>
  <acceptance_criteria>
    - `frontend/src/features/fairness/fairness.store.ts` exports `useFairnessStore`
    - `frontend/src/features/replay/replay.store.ts` exports `useReplayStore`
    - `cd frontend && bunx vitest run src/features/fairness/fairness.store.test.ts src/features/replay/replay.store.test.ts --reporter=verbose` reports >= 14 tests (7 + 7) all PASS
    - `cd frontend && bunx tsc --noEmit` exit 0
    - Full FE suite still green: `cd frontend && bun run test`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; bunx tsc --noEmit &amp;&amp; bunx vitest run src/features/fairness/fairness.store.test.ts src/features/replay/replay.store.test.ts --reporter=verbose &amp;&amp; bun run test</automated>
  </verify>
  <done>
    Two Zustand slices ready for Wave 2/3 consumers (FairnessBadge + VerificationDrawer + ReplayModal); 4-second transient checkmark logic is observably correct; replay store is config-agnostic and testable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `import.meta.env` ← bundler | Env values validated by zod at config build time; malformed values surface as a runtime parse error, not a silent fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | Hardcoded `[1,2,4]` / `220` slipping into a TSX file later (violates REQ-INFRA-05) | mitigate | Wave 3 grep audit (Plan 08-05/06/07 acceptance criteria); `config.ts` becomes the only source of truth |
| T-08-12 | Tampering | Env-parsed speeds array does NOT include 1 (breaks D-03 default-1x contract) | mitigate | `buildConfig` throws when `VITE_REPLAY_SPEEDS` does not contain 1 (Task 1 behavior + test) |
| T-08-13 | Tampering | Third-party shadcn registry block introducing a supply-chain risk | mitigate | UI-SPEC § Registry Safety: only `sheet`, `toggle-group`, `alert` from the OFFICIAL shadcn registry; Package Legitimacy Audit OK |
</threat_model>

<verification>
- `cd frontend && bunx tsc --noEmit` exit 0
- `cd frontend && bun run test` exit 0 (full suite, including the new config tests and store tests)
- `grep -RIn '\\b220\\b' frontend/src --include='*.tsx' --include='*.ts' | grep -v 'config.ts\\|config.test.ts'` returns no hits (no hardcoded drawer slide ms outside config)
- `grep -RIn '\\[1,2,4\\]\\|\\[1, 2, 4\\]' frontend/src --include='*.tsx' --include='*.ts' | grep -v 'config.ts\\|config.test.ts'` returns no hits (no hardcoded speeds literal outside config)
</verification>

<success_criteria>
- All three new env keys parse with defaults and feed into `AppConfig`; downstream surfaces in 08-05/06/07 read them via `getConfig().replay.*` and `getConfig().drawer.slideMs` — no hardcoded constants in TSX.
- shadcn surface for Wave 2/3 is fully provisioned (sheet/toggle-group/alert/dialog).
- Two Zustand stores are testable, mock-able, and config-agnostic — ready for the consumer plans to wire into FairnessBadge + VerificationDrawer + ReplayModal.
</success_criteria>

<output>
Create `.planning/phases/08-provably-fair-history-replay/08-04-SUMMARY.md` documenting: the three new env keys + defaults + invariants, the shadcn install result (which radix transitives landed), the two Zustand store shapes, and any test-fixture refactor needed for `config.test.ts`. Note any unexpected default behaviors of `z.coerce.boolean` if encountered.
</output>
