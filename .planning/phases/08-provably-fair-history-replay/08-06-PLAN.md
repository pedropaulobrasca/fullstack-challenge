---
phase: 08-provably-fair-history-replay
plan: 06
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-04
  - 08-05
files_modified:
  - frontend/src/features/verify/use-recompute-crashpoint.ts
  - frontend/src/features/verify/use-recompute-crashpoint.test.ts
  - frontend/src/routes/verify.$roundId.tsx
  - frontend/src/routes/verify.roundid.test.tsx
autonomous: true
requirements:
  - REQ-FE-10

must_haves:
  truths:
    - "`/verify/:roundId` is a TanStack Router file-based route at routes/verify.$roundId.tsx with ssr:false (matches routes/index.tsx precedent)"
    - "The route fetches GET /games/rounds/:id/verify and computes HMAC-SHA-256(serverSeed,clientSeed:nonce) + the Bustabit formula via deriveCrashPointWithHmacHex from @crash/contracts/provably-fair-browser (single SHARED byte-encoding anchor — Plan 08-01) — NEVER reading the server's `matches` or `recomputedCrashPoint` fields, NEVER duplicating crypto.subtle.sign/importKey inline"
    - "MATCH verdict renders 'MATCH · Computed crash point equals reported crash point' with role=status aria-live=polite"
    - "MISMATCH verdict renders 'MISMATCH · Computed crash point differs from reported' with role=status aria-live=assertive"
    - "Round-not-yet-revealed (400 ROUND_NOT_YET_SETTLED) renders the default Alert with locked copy + Refresh + Go-to-live-game CTAs (NOT destructive variant)"
    - "404 ROUND_NOT_FOUND renders a destructive Alert with the locked copy + back-to-game button"
  artifacts:
    - path: "frontend/src/routes/verify.$roundId.tsx"
      provides: "Surface C — full client-side HMAC + Bustabit verification page"
    - path: "frontend/src/features/verify/use-recompute-crashpoint.ts"
      provides: "Hook: fetch verify endpoint + recompute crashPoint via deriveCrashPointWithHmacHex (shared 08-01 helper) + emit verdict"
      exports: ["useRecomputeCrashpoint"]
  key_links:
    - from: "frontend/src/features/verify/use-recompute-crashpoint.ts"
      to: "@crash/contracts/provably-fair-browser (deriveCrashPointWithHmacHex — shared byte-encoding anchor)"
      via: "import { deriveCrashPointWithHmacHex } from '@crash/contracts/provably-fair-browser'"
      pattern: "deriveCrashPointWithHmacHex"
    - from: "frontend/src/routes/verify.$roundId.tsx"
      to: "Route.useParams (TanStack Router)"
      via: "const { roundId } = Route.useParams()"
      pattern: "Route\\.useParams"
---

<objective>
Implement the `/verify/:roundId` route per UI-SPEC Surface C: a standalone TanStack Router page that fetches `GET /games/rounds/:id/verify` and runs the provably-fair algorithm fully in-browser via `crypto.subtle` — IGNORING the server's `matches` and `recomputedCrashPoint` fields per REQ-FE-10 ("no server recomputation"). The page reads only the inputs the server cannot fake (`serverSeed`, `clientSeed`, `nonce`, `crashPoint`), recomputes locally via `deriveCrashPointAsync` from the browser-safe subpath landed in Plan 08-01, and renders `MATCH ✓` or `MISMATCH ✗`.

Per RESEARCH Pattern 3: file-based route `routes/verify.$roundId.tsx` with `ssr: false` (matches `routes/index.tsx:23` precedent). `Route.useParams()` returns `{roundId: string}` and is validated via `z.string().uuid()` before the fetch.

Per UI-SPEC Surface C: three Cards (Inputs / Computed in your browser / Reported by server) + VerdictChip + 1-paragraph explainer + recruiter README example panel (the panel mandate; the EXACT shell commands are authored in Plan 08-09, this plan renders a placeholder reference).

Purpose: REQ-FE-10 (client-side recomputation via crypto.subtle, no server trust); Phase 8 ROADMAP success criterion 2 (MATCH ✓ / MISMATCH ✗ per the client-side compute).

Output: A new TanStack Router file-based route + a `useRecomputeCrashpoint` hook + tests asserting: (1) MATCH for the Phase 4 locked-byte fixture round, (2) MISMATCH for a tampered crashPoint, (3) ROUND_NOT_YET_SETTLED renders the default Alert with locked copy, (4) ROUND_NOT_FOUND renders the destructive Alert with locked copy, (5) the route ignores the server's `matches` field even when set to true.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-provably-fair-history-replay/08-CONTEXT.md
@.planning/phases/08-provably-fair-history-replay/08-RESEARCH.md
@.planning/phases/08-provably-fair-history-replay/08-UI-SPEC.md
@.planning/phases/08-provably-fair-history-replay/08-01-SUMMARY.md
@.planning/phases/08-provably-fair-history-replay/08-02-SUMMARY.md
@.planning/phases/08-provably-fair-history-replay/08-04-SUMMARY.md
@.planning/phases/08-provably-fair-history-replay/08-05-SUMMARY.md

@frontend/src/routes/index.tsx
@frontend/src/lib/api.ts
@frontend/src/lib/config.ts
@frontend/src/components/ui/card.tsx
@frontend/src/components/ui/alert.tsx
@frontend/src/components/ui/skeleton.tsx
@frontend/src/components/hash-block.tsx
@frontend/src/components/verdict-chip.tsx

<interfaces>
<!-- Contracts the route + hook consume. -->

From @crash/contracts/provably-fair-browser (Plan 08-01):
- `deriveCrashPointAsync({serverSeed, clientSeed, nonce, instantCrashBucket}): Promise<number>`
  - serverSeed: hex string (UTF-8-bytes-as-HMAC-key — Pitfall 1)
  - nonce: bigint
  - returns crash point (e.g. 2.94)

From services/games verify endpoint (Plan 08-02 extended):
- 200: `{ roundId, nonce: string, serverSeed: string, serverSeedHash: string, clientSeed: string, crashPoint: number, recomputedCrashPoint: number, matches: boolean, formulaVersion: number, previousServerSeed: string | null, bets: RoundBetView[], growthRate: number }`
- 400 with `{code: "ROUND_NOT_YET_SETTLED"}` for non-SETTLED rounds
- 404 with `{code: "ROUND_NOT_FOUND"}` for unknown roundIds
- 400 with `{code: "INVALID_ROUND_ID"}` for non-UUID

From frontend/src/routes/index.tsx (Phase 7):
- `createFileRoute(...)({ ssr: false, ... })` precedent.

From frontend/src/lib/api.ts:
- `protectedFetch` (Bearer-attached fetch); used here for the verify endpoint call.

From getConfig() (Plan 08-04 extended):
- `getConfig().rest.base` → `${base}/games/rounds/${roundId}/verify`
- The `instantCrashBucket` for the algorithm: CONSUMED from `getConfig().fairness.instantCrashBucket` (default 101) landed by Plan 08-04. This plan does NOT touch `frontend/.env.example`, `frontend/src/lib/config.ts`, or `frontend/src/lib/config.test.ts` — those are owned by Plan 08-04 (which now ships VITE_INSTANT_CRASH_BUCKET + the fairness section). Plan 08-04 lands in wave 1 / this plan lands in wave 4, guaranteeing the config is in place before this plan runs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author useRecomputeCrashpoint hook (consumes 08-04 config + 08-01 helper)</name>
  <files>
    frontend/src/features/verify/use-recompute-crashpoint.ts,
    frontend/src/features/verify/use-recompute-crashpoint.test.ts
  </files>
  <read_first>
    frontend/src/lib/config.ts (Plan 08-04 output — already exposes fairness.instantCrashBucket; read-only consumption),
    frontend/src/lib/api.ts (protectedFetch signature + error shape),
    packages/contracts/src/provably-fair-browser/derive-crash-point.async.ts (Plan 08-01 output — input shape for deriveCrashPointAsync AND deriveCrashPointWithHmacHex),
    packages/contracts/src/provably-fair-browser/index.ts (Plan 08-01 — confirm deriveCrashPointWithHmacHex is exported from the barrel),
    services/games/src/presentation/dtos/verify-round.dto.ts (Plan 08-02 — response shape with bets + growthRate),
    frontend/src/features/fairness/use-verify-previous.ts (Plan 08-05 Task 2 — fetching pattern + abort + Pitfall 4 try/catch style to mirror)
  </read_first>
  <behavior>
    No config modifications in this plan. `getConfig().fairness.instantCrashBucket` is consumed read-only — Plan 08-04 already shipped the env key + zod schema + AppConfig section + tests in wave 1.

    useRecomputeCrashpoint hook:
    - Input: `roundId: string | null` (null = render the route's empty/idle state).
    - Output: `{ status: "idle" | "loading" | "computing" | "match" | "mismatch" | "not-settled" | "not-found" | "error"; verifyResponse: VerifyRoundView | null; computedCrashPoint: number | null; computedHmacHex: string | null; computedFirst13Hex: string | null; errorMessage: string | null; retry: () => void }`.
    - On mount or `roundId` change: idle → loading.
    - protectedFetch GET `${getConfig().rest.base}/games/rounds/${roundId}/verify`:
      - 200 → status `computing`. Parse response with `verifyRoundSchema` (re-export from `@crash/contracts` or define a local zod schema mirroring it — keep narrow to fields the hook reads). Call `const { crashPoint, hmacHex, first13Hex } = await deriveCrashPointWithHmacHex({serverSeed: response.serverSeed, clientSeed: response.clientSeed, nonce: BigInt(response.nonce), instantCrashBucket: getConfig().fairness.instantCrashBucket})`. Store `computedCrashPoint = crashPoint`, `computedHmacHex = hmacHex`, `computedFirst13Hex = first13Hex`. Compare `computedCrashPoint === response.crashPoint` (number equality — both are floats from the Bustabit formula divided by 100; the Plan 08-01 oracle gives exact 2.94 byte-identity). Status → `"match"` or `"mismatch"`.
      - 400 with `{code: "ROUND_NOT_YET_SETTLED"}` → status `"not-settled"`.
      - 404 (or response code `"ROUND_NOT_FOUND"`) → status `"not-found"`.
      - Other errors → status `"error"` with `errorMessage`.
    - Ignore `response.matches` and `response.recomputedCrashPoint` (anti-pattern per RESEARCH § Anti-Patterns to Avoid + UI-SPEC test: "Verify route does not trust server's `matches` field").
    - The HMAC hex display (UI-SPEC Surface C Card 2 "Computed in your browser") consumes `computedHmacHex` + `computedFirst13Hex` from the SHARED `deriveCrashPointWithHmacHex` helper (Plan 08-01) — NO inline crypto.subtle.importKey/sign duplication. The byte-encoding contract (Pitfall 1: UTF-8 bytes of hex string as HMAC key) is anchored ONCE in Plan 08-01's `computeHmacHexInternal` and consumed here as a typed return value. Eliminates the silent-divergence risk identified by the checker.
    - Pitfall 4 fallback (crypto.subtle undefined in non-secure context): wrap the `deriveCrashPointWithHmacHex(...)` call in try/catch (err): if `err instanceof TypeError` set status = "error" and errorMessage = "Browser cryptography unavailable. Open the app via http://localhost or HTTPS." (matches Plan 08-05's locked fallback copy — single source of truth).

    Tests for useRecomputeCrashpoint:
    - Test 1: with roundId null, status === "idle".
    - Test 2: with the Phase 4 locked-byte fixture mocked (serverSeed `"0000...0001"`, clientSeed `"test"`, nonce `"0"`, crashPoint `2.94`), state transitions to `"match"`, `computedCrashPoint === 2.94`, `computedHmacHex.length === 64`, `computedFirst13Hex === computedHmacHex.substring(0,13)`.
    - Test 3: with a tampered response (crashPoint mutated to `3.50` but seeds intact), state ends in `"mismatch"` and `computedCrashPoint === 2.94`.
    - Test 4: with response.matches set to `false` BUT the seeds correctly producing the reported crashPoint, the hook STILL reports `"match"` (proves the server's `matches` field is ignored).
    - Test 5: with response 400 {code: "ROUND_NOT_YET_SETTLED"}, status === "not-settled".
    - Test 6: with response 404, status === "not-found".
    - Test 7: with network error, status === "error" + errorMessage non-null.
    - Test 8: retry() re-fires the fetch.
    - Test 9 (REQUIRED, Pitfall 4 anchor — parallel to Plan 08-05 Test 7): with `deriveCrashPointWithHmacHex` mocked to throw `TypeError("Cannot read properties of undefined (reading 'sign')")` (simulating `crypto.subtle === undefined` in a non-secure context), state.status === "error" AND errorMessage === "Browser cryptography unavailable. Open the app via http://localhost or HTTPS." — locks the secure-context fallback contract; matches the copy/behavior in 08-05's use-verify-previous Test 7.
  </behavior>
  <action>
    Author `frontend/src/features/verify/use-recompute-crashpoint.ts` per the behavior spec. Use `useEffect([roundId])` + AbortController pattern from Plan 08-05's `use-verify-previous.ts` (style consistency). Import `deriveCrashPointWithHmacHex` from `@crash/contracts/provably-fair-browser` (Plan 08-01 barrel export) — do NOT import `deriveCrashPointAsync` separately and do NOT call `crypto.subtle.importKey`/`sign` inline. The Pitfall 4 try/catch wraps the helper call and surfaces the locked fallback copy.

    Author the test file mocking `protectedFetch` (or injecting it — match Phase 7 mocking style). For Test 4 (ignoring server `matches`), explicitly set `response.matches = false` and `response.recomputedCrashPoint = 1.0` and assert the hook still reports "match" because the in-browser recomputation against the seeds yields 2.94 (or whatever the locked fixture produces). For Test 9 (Pitfall 4), use `vi.mock("@crash/contracts/provably-fair-browser", ...)` to stub `deriveCrashPointWithHmacHex` with a throwing implementation, or use `vi.spyOn` on the module to override that single export — match Phase 7 mocking style.

    No code fences in this action body.
  </action>
  <acceptance_criteria>
    - `frontend/src/features/verify/use-recompute-crashpoint.ts` exports `useRecomputeCrashpoint`
    - Hook source imports `deriveCrashPointWithHmacHex` from `@crash/contracts/provably-fair-browser` (grep): `grep -q 'deriveCrashPointWithHmacHex' frontend/src/features/verify/use-recompute-crashpoint.ts && grep -q '@crash/contracts/provably-fair-browser' frontend/src/features/verify/use-recompute-crashpoint.ts`
    - Hook source does NOT call `crypto.subtle.importKey` or `crypto.subtle.sign` inline (byte-encoding NOT duplicated — Plan 08-01's helper is the single anchor): `! grep -E 'crypto\\.subtle\\.(importKey|sign)' frontend/src/features/verify/use-recompute-crashpoint.ts`
    - Hook source does NOT reference `@crash/contracts/provably-fair` (server-only path): `! grep -q '@crash/contracts/provably-fair"' frontend/src/features/verify/use-recompute-crashpoint.ts && ! grep -q "@crash/contracts/provably-fair'" frontend/src/features/verify/use-recompute-crashpoint.ts`
    - Hook source does NOT read `response.matches` or `response.recomputedCrashPoint` (anti-pattern enforcement; grep): `! grep -E '\\.matches\\b|\\.recomputedCrashPoint' frontend/src/features/verify/use-recompute-crashpoint.ts`
    - This plan does NOT touch `frontend/.env.example`, `frontend/src/lib/config.ts`, or `frontend/src/lib/config.test.ts` (those are owned by Plan 08-04): `! git diff --name-only HEAD | grep -E 'frontend/\\.env\\.example|frontend/src/lib/config\\.(ts|test\\.ts)'` (confirm at execution)
    - `cd frontend && bunx vitest run src/features/verify/use-recompute-crashpoint.test.ts --reporter=verbose` >= 9 tests all PASS (including Test 9 Pitfall 4)
    - `cd frontend && bunx tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; bunx tsc --noEmit &amp;&amp; bunx vitest run src/features/verify/use-recompute-crashpoint.test.ts --reporter=verbose &amp;&amp; grep -q 'deriveCrashPointWithHmacHex' src/features/verify/use-recompute-crashpoint.ts &amp;&amp; grep -q '@crash/contracts/provably-fair-browser' src/features/verify/use-recompute-crashpoint.ts &amp;&amp; ! grep -E 'crypto\.subtle\.(importKey|sign)' src/features/verify/use-recompute-crashpoint.ts &amp;&amp; ! grep -E '\.matches\b|\.recomputedCrashPoint' src/features/verify/use-recompute-crashpoint.ts</automated>
  </verify>
  <done>
    Hook lands; trust-the-browser contract is enforced by source-grep (no read of `matches`/`recomputedCrashPoint`) AND by Test 4 (mismatched server field, browser still produces correct verdict); byte-encoding is shared via Plan 08-01's `deriveCrashPointWithHmacHex` (NO inline crypto.subtle calls — grep gate enforces); Pitfall 4 fallback is test-enforced (Test 9 parallel to 08-05 Test 7).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the verify.$roundId.tsx route with three Cards + verdict + locked error copy</name>
  <files>
    frontend/src/routes/verify.$roundId.tsx,
    frontend/src/routes/verify.roundid.test.tsx
  </files>
  <read_first>
    .planning/phases/08-provably-fair-history-replay/08-UI-SPEC.md (Surface C lines 187-243 + § Copywriting Surface C),
    frontend/src/routes/index.tsx (createFileRoute + ssr:false precedent),
    frontend/src/features/verify/use-recompute-crashpoint.ts (Task 1 output),
    frontend/src/components/hash-block.tsx (Plan 08-05 Task 1),
    frontend/src/components/verdict-chip.tsx (Plan 08-05 Task 1),
    frontend/src/components/ui/card.tsx,
    frontend/src/components/ui/alert.tsx,
    frontend/src/components/ui/button.tsx,
    frontend/src/features/fairness/verification-explainer.tsx (Plan 08-05 Task 2 — reuse the same locked-copy 1-paragraph explainer here)
  </read_first>
  <behavior>
    Route file shape:
    - `import { createFileRoute, Link } from "@tanstack/react-router"` (or equivalent for the project's TanStack Router version).
    - `export const Route = createFileRoute("/verify/$roundId")({ ssr: false, component: VerifyPage })`.
    - `function VerifyPage() { const { roundId } = Route.useParams(); ... }` — guard against non-UUID by validating with `z.string().uuid().safeParse(roundId)` and short-circuiting to a 404 Alert if invalid.

    Page layout (per UI-SPEC Surface C):
    - Outer container: max-width `min(720px, 100vw - 64px)` centered, py-12 (2xl).
    - Heading "Verifying Round #{shortId}" (use last 8 chars of roundId, or full UUID — choose what looks readable; document choice).
    - "← Live game" Link to `/` (TanStack Router Link).
    - When status === "loading" or "computing": render three Cards each containing skeleton blocks (curve/inputs/verdict sections), no fetch spinner; the skeleton IS the loading state per UI-SPEC.
    - When status === "match" or "mismatch":
      - Card 1 (heading "Inputs (from server)"): 4 rows — HashBlock for serverSeed, HashBlock for clientSeed, plain mono row for nonce, plain mono row for formula (e.g. "bustabit-52bit-instant-101"). Use `verifyResponse.serverSeed`, `.clientSeed`, `.nonce`, hardcode the formula string OR derive from `verifyResponse.formulaVersion` mapped to a display name in a constant.
      - Card 2 (heading "Computed in your browser", subhead "HMAC-SHA-256(serverSeed, clientSeed:nonce)"): HashBlock for `computedHmacHex` (full), display the first-13 hex slice, display `computedCrashPoint.toFixed(2) + "x"` as a Fira Code tabular number.
      - Card 3 (heading "Reported by server"): display `verifyResponse.crashPoint.toFixed(2) + "x"` as Fira Code tabular.
      - VerdictChip with:
        - matchText="MATCH · Computed crash point equals reported crash point"
        - mismatchText="MISMATCH · Computed crash point differs from reported"
        - For MISMATCH, override the `aria-live` to `"assertive"` per UI-SPEC § Copywriting (MISMATCH is a serious failure — escalate the announcement).
    - When status === "not-settled": shadcn `Alert variant="default"` (NOT destructive — per UI-SPEC: "this is expected"). Title "Round not yet revealed". Body "This round has not crashed yet. The serverSeed will be revealed automatically after it settles." + a "Refresh" button (calls `retry()`) + a "Go to live game" Link to `/`.
    - When status === "not-found": shadcn `Alert variant="destructive"`. Title `Round #{shortId} not found.` Body short. + Back-to-game button (Link to `/`).
    - When status === "error": shadcn `Alert variant="destructive"`. Title "Couldn't load round data." Body "Check your connection and try again." + Retry button calling `retry()`.

    - Below the verification panel: an `<section>` with heading "How this works" and the same `<VerificationExplainer />` component used in the drawer (single source of truth — locked copy).

    - At page bottom: a `<section>` with heading "Verify outside the app" and a placeholder mono-block + Copy button containing a comment like `# README example will land in Plan 08-09 — see README.md` (the UI-SPEC mandates this panel exists; the exact shell commands are authored in Plan 08-09; this plan ships the panel scaffold).

    Tests for the verify route:
    - Test 1: with the Phase 4 locked-byte fixture mocked through `useRecomputeCrashpoint`, the page renders three Cards + a MATCH chip with exact locked copy "MATCH · Computed crash point equals reported crash point".
    - Test 2: with mismatched fixture, MISMATCH chip renders with `aria-live="assertive"` + locked copy "MISMATCH · Computed crash point differs from reported".
    - Test 3: with status "not-settled", the default Alert renders with EXACT copy "This round has not crashed yet. The serverSeed will be revealed automatically after it settles." + a Refresh button + a "Go to live game" link.
    - Test 4: with status "not-found", the destructive Alert renders with `"Round #` substring + back-to-game button.
    - Test 5: invalid roundId param (e.g. "not-a-uuid") triggers the not-found path WITHOUT firing the fetch (mocked protectedFetch spy.callCount === 0).
    - Test 6: the page renders the locked explainer paragraph (re-using `<VerificationExplainer />`).

    Use TanStack Router test utilities — read the existing `frontend/src/routes/index.test.tsx` for the harness pattern. If creating a router test is complex, render `<VerifyPage />` directly while mocking `Route.useParams` (acceptable shortcut; mark the choice in SUMMARY).
  </behavior>
  <action>
    Author `frontend/src/routes/verify.$roundId.tsx` per the behavior spec. Theme tokens only. Use `<HashBlock />` + `<VerdictChip />` + `<VerificationExplainer />` for consistency with the drawer (single visual language).

    Author `frontend/src/routes/verify.roundid.test.tsx` with mocked `useRecomputeCrashpoint` for each scenario (idle/match/mismatch/not-settled/not-found/error). Use Vitest's `vi.mock` on the hook module.

    Run `bunx tsc --noEmit` after each save — TanStack Router auto-generates route tree types; ensure the new file lands in the generated `routeTree.gen.ts` (re-run dev once if needed; the generated artifact is committed to the repo).

    No code fences in this action body.
  </action>
  <acceptance_criteria>
    - `frontend/src/routes/verify.$roundId.tsx` exists, exports `Route`, declares `ssr: false`
    - `frontend/src/routeTree.gen.ts` (generated) references the new route after a dev build OR the route compiles standalone in tsc — both must work
    - `cd frontend && bunx vitest run src/routes/verify.roundid.test.tsx --reporter=verbose` >= 6 tests all PASS
    - The MATCH test asserts exact copy "MATCH · Computed crash point equals reported crash point"
    - The not-settled test asserts exact copy "This round has not crashed yet. The serverSeed will be revealed automatically after it settles." (UI-SPEC § Copywriting)
    - `! grep -E '#[0-9a-fA-F]{3,6}' frontend/src/routes/verify.$roundId.tsx` (no hex literals in the route file)
    - `cd frontend && bunx tsc --noEmit` exit 0
    - Full FE suite still green: `cd frontend && bun run test`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; bunx tsc --noEmit &amp;&amp; bunx vitest run src/routes/verify.roundid.test.tsx --reporter=verbose &amp;&amp; grep -q 'createFileRoute' src/routes/verify.\$roundId.tsx &amp;&amp; grep -q 'ssr: false' src/routes/verify.\$roundId.tsx &amp;&amp; ! grep -E '#[0-9a-fA-F]{3,6}' src/routes/verify.\$roundId.tsx &amp;&amp; bun run test</automated>
  </verify>
  <done>
    `/verify/:roundId` is reachable; runs HMAC-SHA-256 + Bustabit fully in the browser; ignores server's `matches`/`recomputedCrashPoint`; locked error copy verified for both expected (not-settled) and unexpected (not-found / network) paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ← server `/verify` response | Only `serverSeed`, `clientSeed`, `nonce`, `crashPoint` consumed; `matches` and `recomputedCrashPoint` ignored |
| Browser ← `Route.useParams()` | roundId param validated via `z.string().uuid()` before any fetch (prevents Pitfall path-injection attacks per RESEARCH § Security Domain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-18 | Tampering | Route relies on server's `matches` boolean | mitigate | grep gate in Task 1 acceptance + Test 4 (server `matches=false` but browser still computes correct match) |
| T-08-18b | Tampering | Inline HMAC byte-encoding silently diverges from Plan 08-01's anchored implementation (Pitfall 1) | mitigate | Grep gate forbids `crypto.subtle.importKey`/`sign` in use-recompute-crashpoint.ts; the SHARED `deriveCrashPointWithHmacHex` helper (08-01) is the single anchor; Test 2 + Test 6 in 08-01 lock the 2.94 oracle for the helper itself |
| T-08-18c | Information Disclosure | `crypto.subtle` undefined in non-secure context (Pitfall 4) → /verify route breaks silently | mitigate | Test 9 (REQUIRED) asserts the locked fallback copy renders when `deriveCrashPointWithHmacHex` throws TypeError; parallel to Plan 08-05 Test 7 — single source of truth for the copy |
| T-08-19 | Tampering | Crafted roundId path traversal | mitigate | UUID validation at `Route.useParams` boundary; Test 5 asserts no fetch fired for invalid roundId |
| T-08-20 | Information Disclosure | Page displays MISMATCH copy color-only | mitigate | VerdictChip Test 4 (Plan 08-05) confirmed icon+text+color; route reuses VerdictChip — inherits the gate |
| T-08-21 | Tampering | Future maintainer reads `response.recomputedCrashPoint` to "save a compute" | mitigate | Source-grep gate in Task 1 acceptance + Test 4 catches behavioral regression |
</threat_model>

<verification>
- `cd frontend && bunx tsc --noEmit` exit 0
- `cd frontend && bun run test` exit 0 (FE suite green incl. route tests)
- Manual navigation sanity (live smoke): visit `http://localhost:3000/verify/<settled-round-id>` → 3 Cards + MATCH verdict
- Manual sanity: visit `/verify/<bogus-uuid>` → not-found Alert renders
- Manual sanity: visit `/verify/<current-running-round-id>` → not-settled Alert renders with locked copy
</verification>

<success_criteria>
- REQ-FE-10 observable: route runs HMAC + Bustabit fully client-side via crypto.subtle; verdict is browser-derived (test 4 proves the server's `matches` field is ignored); MATCH ✓ / MISMATCH ✗ rendered with icon + text + color + aria-live.
- The not-settled error path uses the DEFAULT (non-destructive) Alert variant — UI-SPEC explicitly says this is expected, not an error.
- The "Open full verification" link from the drawer (Plan 08-05 Task 3) lands here; smoke test the path during the closeout walkthrough.
</success_criteria>

<output>
Create `.planning/phases/08-provably-fair-history-replay/08-06-SUMMARY.md` documenting: route file naming + ssr:false decision, the INSTANT_CRASH_BUCKET env addition + rationale, the choice to compute the HMAC hex inline for display vs extending Plan 08-01's helper, and any TanStack Router routeTree regen caveats. Note the locked copy assertions that passed.
</output>
