---
phase: 08-provably-fair-history-replay
plan: 07
type: execute
wave: 4
depends_on:
  - 08-02
  - 08-03
  - 08-04
  - 08-05
files_modified:
  - frontend/src/features/replay/use-round-detail.ts
  - frontend/src/features/replay/use-round-detail.test.ts
  - frontend/src/features/replay/replay-driver.ts
  - frontend/src/features/replay/replay-driver.test.ts
  - frontend/src/features/replay/replay-overlays.tsx
  - frontend/src/components/replay-speed-toggle.tsx
  - frontend/src/components/replay-modal.tsx
  - frontend/src/components/replay-modal.test.tsx
  - frontend/src/components/crash-curve.tsx
  - frontend/src/components/history-strip.tsx
  - frontend/src/components/history-strip.test.tsx
  - frontend/src/routes/__root.tsx
autonomous: true
requirements:
  - REQ-REPLAY-02
  - REQ-REPLAY-03
  - REQ-REPLAY-01

must_haves:
  truths:
    - "Clicking any HistoryStrip chip opens the Replay modal with that chip's roundId; the modal is mounted in __root.tsx so route changes do not unmount it"
    - "Replay modal auto-plays at 1x on open (D-03); Play/Pause toggle works; speed ToggleGroup 1x/2x/4x updates the driver wall-clock multiplier without changing the underlying multiplierAt grid"
    - "Replay reuses <CrashCurve /> verbatim — the modal passes a deterministic RafCurveDriver (Plan 08-03 seam) so draw-curve.ts is unchanged"
    - "Replay overlays render bets[] from the extended /verify endpoint (Plan 08-02) with masked playerId and Money snapshots"
    - "Inline Alert (not Sonner toast) for fetch failures (UI-SPEC § Copywriting Replay modal)"
    - "useMultiplierStore is NOT mutated by the replay driver (Plan 08-03 contract is preserved through the modal lifecycle)"
  artifacts:
    - path: "frontend/src/components/replay-modal.tsx"
      provides: "Surface D — shadcn Dialog hosting <CrashCurve /> driven by the replay driver + controls + overlays"
    - path: "frontend/src/features/replay/replay-driver.ts"
      provides: "Pure function sampleReplayMultiplier + makeReplayDriver factory returning a RafCurveDriver"
      exports: ["sampleReplayMultiplier", "makeReplayDriver"]
    - path: "frontend/src/features/replay/use-round-detail.ts"
      provides: "TanStack Query hook for GET /games/rounds/:id/verify (reuses verify endpoint per RESEARCH Architectural Map)"
      exports: ["useRoundDetail"]
    - path: "frontend/src/components/history-strip.tsx"
      provides: "Phase 7 history strip — chips now wired to openReplay(roundId) per UI-SPEC Surface E"
    - path: "frontend/src/components/replay-speed-toggle.tsx"
      provides: "ToggleGroup wrapper rendering getConfig().replay.speeds as 1x/2x/4x options"
  key_links:
    - from: "frontend/src/components/replay-modal.tsx"
      to: "frontend/src/features/curve/use-raf-curve.ts (driver param from Plan 08-03)"
      via: "<CrashCurve /> mounted with a custom driver from makeReplayDriver"
      pattern: "makeReplayDriver|RafCurveDriver"
    - from: "frontend/src/components/history-strip.tsx"
      to: "frontend/src/features/replay/replay.store.ts (Plan 08-04)"
      via: "onClick triggers useReplayStore.getState().openReplay(roundId, autostart, defaultSpeed)"
      pattern: "openReplay"
    - from: "frontend/src/routes/__root.tsx"
      to: "frontend/src/components/replay-modal.tsx"
      via: "<ReplayModal /> mounted as sibling of <Outlet /> (Pitfall 5)"
      pattern: "ReplayModal"
---

<objective>
Ship Surface D (Replay modal) and Surface E (history-chip Replay affordance) per UI-SPEC. The Replay modal opens over the live game (which keeps animating behind a blurred scrim per D-02), mounts a `<CrashCurve />` driven by a deterministic time-stepper (`makeReplayDriver` — the RafCurveDriver seam from Plan 08-03), and reconstructs bet/cashout overlays from `bets[]` returned by the extended verify endpoint (Plan 08-02). Speed selector 1x/2x/4x from `getConfig().replay.speeds` (Plan 08-04). Auto-play at 1x on open per D-03 + `getConfig().replay.autostart`.

Per D-05 (renderer reuse): the modal MUST NOT duplicate any rendering logic — it supplies a driver to the SAME `useRafCurve` + `<CrashCurve />`. The replay driver computes `multiplierAt(elapsedWall * speed, growthRate)` from `@crash/contracts/multiplier` (browser-safe subpath already in the bundle).

Per Pitfall 5: mount the modal in `__root.tsx` as a sibling of `<Outlet />` so route navigation (e.g. opening the drawer's "Open full verification" link to `/verify/...`) does not dismiss the modal mid-state.

Purpose: REQ-REPLAY-02 (Replay button on each history entry); REQ-REPLAY-03 (renderer reuse, no separate code path); REQ-REPLAY-01 (deterministic reproduction — the driver's per-frame value is the pure `multiplierAt`).

Output: ReplayModal component + replay driver + round-detail TanStack Query hook + speed toggle + history-chip wiring + root mount; tests assert opening, autostart, speed update, pause, byte-identical sampling at different speeds, and the same `<CrashCurve />` component instance is reused.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-provably-fair-history-replay/08-CONTEXT.md
@.planning/phases/08-provably-fair-history-replay/08-RESEARCH.md
@.planning/phases/08-provably-fair-history-replay/08-UI-SPEC.md
@.planning/phases/08-provably-fair-history-replay/08-02-SUMMARY.md
@.planning/phases/08-provably-fair-history-replay/08-03-SUMMARY.md
@.planning/phases/08-provably-fair-history-replay/08-04-SUMMARY.md

@frontend/src/components/history-strip.tsx
@frontend/src/components/crash-curve.tsx
@frontend/src/features/curve/use-raf-curve.ts
@frontend/src/features/curve/draw-curve.ts
@frontend/src/features/curve/local-multiplier.ts
@frontend/src/components/ui/dialog.tsx
@frontend/src/components/ui/toggle-group.tsx
@frontend/src/components/ui/alert.tsx
@frontend/src/components/ui/skeleton.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/lib/api.ts
@frontend/src/lib/config.ts
@frontend/src/features/replay/replay.store.ts
@frontend/src/routes/__root.tsx

<interfaces>
<!-- Contracts the modal and driver consume. -->

From frontend/src/features/curve/use-raf-curve.ts (Plan 08-03):
- `export type RafCurveDriver = { multiplier: () => number; status: () => RoundStatus | string; crashValue: () => number | null; shouldStop: () => boolean }`
- `useRafCurve(onFrame?, driver?: RafCurveDriver = liveDriver): React.MutableRefObject<CurveFrame>`
- When driver !== liveDriver, hook does NOT write useMultiplierStore.setRendered.

From frontend/src/components/crash-curve.tsx (Phase 7):
- Current call site: `useRafCurve((frame) => { draw(frame.multiplier, ...); })` — no driver arg.
- The replay modal will mount its OWN `<CrashCurve />` instance, but the current component DOES NOT accept a `driver` prop today. Either (a) extend `<CrashCurve />` to optionally accept a `driver` prop and pass it through to `useRafCurve`, or (b) author a thin wrapper `<ReplayCrashCurve />` that calls `useRafCurve` directly with the driver and reuses `draw-curve.ts` for the actual canvas painting.
  - RECOMMENDED (a) — extend `<CrashCurve />` with an optional `driver?: RafCurveDriver` prop, default omitted = live. This preserves D-05 (same component, no separate playback code path) and is one extra prop.

From @crash/contracts/multiplier:
- `multiplierAt(elapsedMs: number, growthRate: number): number` — pure, browser-safe.

From frontend/src/features/replay/replay.store.ts (Plan 08-04):
- `useReplayStore` with state `{ roundId, playing, speed }` + actions `openReplay`, `closeReplay`, `setPlaying`, `setSpeed`.

From server `/games/rounds/:id/verify` (Plan 08-02 extended):
- Response shape includes `serverSeed, clientSeed, nonce, crashPoint, bets[], growthRate`. Replay uses `crashPoint` (when to stop), `growthRate` (the formula's rate), `bets[]` (overlays).

Note for the driver: per RESEARCH Pattern 2 / Pitfall 7, the replay driver MUST be deterministic in the sense that `sampleReplayMultiplier(state, t)` is a PURE function of (state.growthRate, state.crashPoint, state.speed, state.startedAtWall, t). The determinism E2E in Plan 08-08 hammers this property.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author replay-driver.ts pure function + makeReplayDriver factory + round-detail TanStack Query hook</name>
  <files>
    frontend/src/features/replay/replay-driver.ts,
    frontend/src/features/replay/replay-driver.test.ts,
    frontend/src/features/replay/use-round-detail.ts,
    frontend/src/features/replay/use-round-detail.test.ts
  </files>
  <read_first>
    packages/contracts/src/provably-fair/multiplier.ts (multiplierAt signature),
    frontend/src/features/curve/use-raf-curve.ts (RafCurveDriver type from Plan 08-03),
    frontend/src/features/curve/local-multiplier.ts (existing pattern for invoking multiplierAt),
    frontend/src/features/replay/replay.store.ts (state shape — Plan 08-04),
    services/games/src/presentation/dtos/verify-round.dto.ts (response shape — Plan 08-02 extended),
    frontend/src/lib/api.ts (protectedFetch),
    Existing Phase 7 TanStack Query usages e.g. use-wallet / use-history for the mocking + retry style
  </read_first>
  <behavior>
    sampleReplayMultiplier(state: ReplayDriverState, now: number): number — pure (per RESEARCH § Code Examples):
    - If state.paused returns state.lastMultiplier (or 1 if no prior).
    - `elapsedWall = now - state.startedAtWall`
    - `replayElapsed = elapsedWall * state.speed`
    - `m = multiplierAt(replayElapsed, state.growthRate)`
    - returns `Math.min(m, state.crashPoint)`

    makeReplayDriver(args: { growthRate, crashPoint, speed: () => number, paused: () => boolean }): RafCurveDriver:
    - Closures over a `startedAtWall = performance.now()` captured at factory time, plus mutable `lastMultiplier` (initially 1) and a `stopped` boolean.
    - `multiplier()` uses sampleReplayMultiplier with current state and `performance.now()`; updates `lastMultiplier` and sets `stopped=true` when sampled value >= crashPoint.
    - `status()` returns `"CRASHED"` when stopped, else `"RUNNING"`.
    - `crashValue()` returns `crashPoint` when stopped, else `null`.
    - `shouldStop()` returns `stopped`.
    - Speed and paused are read via the caller-supplied getter functions so the driver reflects live store changes without rebuild.

    useRoundDetail(roundId: string | null) hook (TanStack Query):
    - queryKey: `["round-detail", roundId]`
    - queryFn: `protectedFetch(`${getConfig().rest.base}/games/rounds/${roundId}/verify`).then(r => r.json())` — REUSES the verify endpoint (the Plan 08-02 extension; no new endpoint).
    - Disabled when `roundId === null`.
    - returns `{ data: VerifyRoundResponse | undefined, isLoading, isError, error }`.
    - Discriminates 400 ROUND_NOT_YET_SETTLED into a `{kind: "not-settled"}` typed error; consumer renders a different message than fetch failure.

    Tests for replay-driver:
    - Test 1: sampleReplayMultiplier returns 1.0 when elapsedWall === 0 (multiplierAt(0) === 1).
    - Test 2: sampleReplayMultiplier at 1x and 2x speeds produces values where `value@speed=2 at t === value@speed=1 at 2t` for the same growthRate (the determinism property — locks D-03).
    - Test 3: sampleReplayMultiplier caps at crashPoint when computed value would exceed it.
    - Test 4: makeReplayDriver().multiplier() advances over time (call twice with synthetic performance.now() — second value > first).
    - Test 5: when the driver's multiplier reaches crashPoint, status() returns "CRASHED" and shouldStop() returns true.
    - Test 6: paused getter returning true freezes the multiplier (subsequent calls return same value).

    Tests for use-round-detail:
    - Test 1: with roundId null, isLoading = false, data = undefined (disabled).
    - Test 2: with mocked protectedFetch returning the Phase 4 fixture, data.crashPoint === 2.94 and data.bets is an array (length depends on mock — assert length >= 0).
    - Test 3: 400 ROUND_NOT_YET_SETTLED surfaces as a typed error with `kind === "not-settled"`.
  </behavior>
  <action>
    Author `frontend/src/features/replay/replay-driver.ts` exporting the pure `sampleReplayMultiplier(state, now)` function and the `makeReplayDriver(args)` factory. Match RESEARCH § Code Examples (Replay time-stepper driver).

    Author `frontend/src/features/replay/use-round-detail.ts` using TanStack Query — import `useQuery` from `@tanstack/react-query` (already in the project; Phase 7 use-history precedent). queryKey + queryFn as specified. enabled = `roundId !== null`. Map the fetch error → typed error per behavior.

    Author both test files with Vitest. For the replay-driver tests, control `performance.now()` by mocking `globalThis.performance.now = vi.fn().mockReturnValueOnce(...)` or by injecting a fake-now into the function signature (the pure-function form makes this easy — pass `now` directly).

    For use-round-detail tests, use `@testing-library/react`'s render with a fresh QueryClient per test (the pattern Phase 7 uses for query hooks).

    No code fences in this action body.
  </action>
  <acceptance_criteria>
    - `frontend/src/features/replay/replay-driver.ts` exports `sampleReplayMultiplier`, `makeReplayDriver`, and a `ReplayDriverState` type
    - `frontend/src/features/replay/use-round-detail.ts` exports `useRoundDetail`
    - `cd frontend && bunx vitest run src/features/replay/replay-driver.test.ts src/features/replay/use-round-detail.test.ts --reporter=verbose` reports >= 9 tests (6 + 3) all PASS
    - The determinism property test (Test 2 of replay-driver) explicitly asserts the speed↔time equivalence — load-bearing for Plan 08-08
    - `cd frontend && bunx tsc --noEmit` exit 0
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; bunx tsc --noEmit &amp;&amp; bunx vitest run src/features/replay/replay-driver.test.ts src/features/replay/use-round-detail.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    Pure driver primitive + Query hook ready for ReplayModal consumption; determinism property has a passing unit test.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend CrashCurve with a driver prop, author ReplaySpeedToggle, author ReplayOverlays</name>
  <files>
    frontend/src/components/crash-curve.tsx,
    frontend/src/components/replay-speed-toggle.tsx,
    frontend/src/features/replay/replay-overlays.tsx
  </files>
  <read_first>
    frontend/src/components/crash-curve.tsx (current implementation — extend with one optional driver prop),
    frontend/src/features/curve/use-raf-curve.ts (Plan 08-03 — driver param signature),
    frontend/src/components/ui/toggle-group.tsx (Plan 08-04 install),
    .planning/phases/08-provably-fair-history-replay/08-UI-SPEC.md (Surface D controls lines 270-285),
    frontend/src/lib/config.ts (Plan 08-04 — getConfig().replay.speeds),
    frontend/src/features/replay/replay.store.ts (setSpeed action),
    services/games/src/presentation/dtos/round-bet-view.dto.ts (Plan 08-02 — RoundBetView shape for overlays),
    frontend/src/components/feed-row.tsx (Phase 7 — money snapshot + masked playerId render style to mirror)
  </read_first>
  <behavior>
    CrashCurve extension:
    - Accept an optional `driver?: RafCurveDriver` prop. Default omitted = live (Phase 7 unchanged).
    - When driver is provided, pass it through to `useRafCurve(onFrame, driver)`.
    - Optionally accept an `ariaLabel` prop so the replay instance reports "Replay curve" instead of "Live curve" (a11y nicety; document in SUMMARY).
    - The Canvas + ResizeObserver + dPR setup logic is UNCHANGED — the only delta is forwarding the driver.

    ReplaySpeedToggle:
    - Props: `{ speeds: number[]; currentSpeed: number; onChange: (speed: number) => void; disabled?: boolean }`.
    - Renders `<ToggleGroup type="single" value={String(currentSpeed)} onValueChange={(v) => onChange(Number(v))}>` with one `<ToggleGroupItem value={String(s)}>{`${s}x`}</ToggleGroupItem>` per speed.
    - Each item: `aria-label={`Replay at ${s}x speed`}` per UI-SPEC § Copywriting Replay modal.
    - Theme classes: accent ring on active, neutral on inactive; min-h-11 min-w-11 touch target per UI-SPEC § Spacing.

    ReplayOverlays:
    - Props: `{ bets: RoundBetView[] }`.
    - Renders a scrollable list of bets WITH the same row shape as Phase 7 LiveFeed (own bet emerald rail, foreign muted), but for replay context: every bet is a SETTLED outcome so each row shows: masked playerId, bet amount via Money.fromSnapshot, status badge (CASHED_OUT / LOST / REFUNDED), and the cashedOutMultiplier+payout when cashed out.
    - Position: stacked below or alongside the canvas inside the Dialog (per UI-SPEC Surface D layout — the overlay area is a side panel; the planner authors the layout flex to honor the 16:11 modal canvas aspect).
    - NOTE: UI-SPEC says "appear at their `t` anchor" — but UI-SPEC also notes "all bets effectively appear at `t = 0`" (all bets placed during BETTING). For v1 scope, render the overlay as a static panel listing the bets (not animated per-tick markers on the curve). The curve renders identically to live; the overlay panel shows the bet/cashout outcomes. Document this scope decision in SUMMARY (it satisfies REQ-REPLAY-02 "bet/cashout overlays" without requiring per-frame canvas markers).
  </behavior>
  <action>
    Patch `frontend/src/components/crash-curve.tsx` to accept the optional `driver` prop and forward it. NO change to the canvas / dPR / draw logic. Patch any TSX usage if needed (Phase 7 callers in `routes/index.tsx` simply don't pass the prop — verified by grep).

    Author `frontend/src/components/replay-speed-toggle.tsx` per behavior. Theme tokens only. Hit area ≥ 44×44px (UI-SPEC § Spacing).

    Author `frontend/src/features/replay/replay-overlays.tsx` per behavior. Reuse the FeedRow visual primitive if it cleanly accepts a RoundBetView (or compose a lightweight inline row matching FeedRow style — confirm by reading FeedRow).

    Tests:
    - For ReplaySpeedToggle: render with speeds [1,2,4] currentSpeed=1; assert 3 items render; assert clicking "2x" calls `onChange(2)`. (Add a small replay-speed-toggle.test.tsx — keep light.)
    - For ReplayOverlays: render with 3 fake bets; assert all three rows render; assert money formatted; assert masked playerId shown. (Add a small test if time permits — otherwise covered by ReplayModal Test in Task 3.)
    - For CrashCurve extension: confirm the existing Phase 7 tests for crash-curve still pass (no API break per Plan 08-03 contract — driver is optional).

    No code fences.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/crash-curve.tsx` exports the component with an optional `driver?: RafCurveDriver` prop (grep type)
    - `frontend/src/components/replay-speed-toggle.tsx` exists and renders a ToggleGroup
    - `frontend/src/features/replay/replay-overlays.tsx` exists and renders a list of bets
    - Phase 7 crash-curve tests still green: `cd frontend && bunx vitest run src/components/crash-curve.test.tsx` exit 0 (if the file exists; if not, the regression is covered by the existing 5 curve tests landed in P07-06)
    - `cd frontend && bunx tsc --noEmit` exit 0
    - `! grep -E '#[0-9a-fA-F]{3,6}' frontend/src/components/replay-speed-toggle.tsx frontend/src/features/replay/replay-overlays.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; bunx tsc --noEmit &amp;&amp; bun run test &amp;&amp; ! grep -E '#[0-9a-fA-F]{3,6}' src/components/replay-speed-toggle.tsx src/features/replay/replay-overlays.tsx</automated>
  </verify>
  <done>
    CrashCurve driver prop wired (D-05 enforceable from the modal); speed toggle + overlay primitives ready for modal composition; Phase 7 zero-diff for crash-curve consumers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author ReplayModal, mount at __root.tsx, wire HistoryStrip chip clicks to openReplay</name>
  <files>
    frontend/src/components/replay-modal.tsx,
    frontend/src/components/replay-modal.test.tsx,
    frontend/src/components/history-strip.tsx,
    frontend/src/components/history-strip.test.tsx,
    frontend/src/routes/__root.tsx
  </files>
  <read_first>
    .planning/phases/08-provably-fair-history-replay/08-UI-SPEC.md (Surface D lines 246-287 + § Copywriting Replay modal + Surface E lines 289-296),
    frontend/src/components/ui/dialog.tsx (existing P7),
    frontend/src/components/history-strip.tsx (Phase 7 — chips are <button> already; wire onClick),
    frontend/src/components/replay-speed-toggle.tsx (Task 2 output),
    frontend/src/features/replay/replay-overlays.tsx (Task 2 output),
    frontend/src/features/replay/replay-driver.ts (Task 1 output),
    frontend/src/features/replay/use-round-detail.ts (Task 1 output),
    frontend/src/features/replay/replay.store.ts (Plan 08-04 — open/close/play/pause/speed actions),
    frontend/src/components/crash-curve.tsx (Task 2 extended with driver prop),
    frontend/src/routes/__root.tsx (Plan 08-05 already wired drawer here — add modal as next sibling)
  </read_first>
  <behavior>
    ReplayModal (Surface D):
    - Reads `useReplayStore` for `roundId, playing, speed` + actions `setPlaying, setSpeed, closeReplay`.
    - When `roundId === null`: renders null (modal closed).
    - When `roundId !== null`: renders shadcn `<Dialog open={true} onOpenChange={(open) => !open && closeReplay()}>` with `<DialogContent>` configured per UI-SPEC Surface D spacing (`max-w-[min(960px,calc(100vw-64px))]` x `max-h-[min(720px,calc(100vh-64px))]`).
    - DialogTitle: `Replay · Round #{shortRoundId}` with suffix `@ {crashPoint}x` in muted-foreground (read from data.crashPoint once loaded).
    - DialogDescription: "Reproduced from serverSeed + clientSeed + bets[]. Same renderer as the live game."
    - useRoundDetail(roundId) fetches the round.
    - Loading state: Skeleton in the canvas area + Skeleton in the control bar.
    - Error state: shadcn `Alert variant="destructive"` inline (NOT a sonner toast) with "Couldn't load replay data for Round #{shortRoundId}." + Retry button calling `useRoundDetail.refetch()`.
    - On data ready:
      - `const driver = useMemo(() => makeReplayDriver({ growthRate: data.growthRate, crashPoint: data.crashPoint, speed: () => useReplayStore.getState().speed, paused: () => !useReplayStore.getState().playing }), [data.growthRate, data.crashPoint])` — memo deps exclude speed/playing so the driver instance is stable; the closures read live state each frame.
      - Render `<CrashCurve driver={driver} ariaLabel="Replay curve" />` in the canvas region.
      - Render `<ReplayOverlays bets={data.bets} />` in a side or below panel.
      - Render controls row at the bottom: Play/Pause Button (toggles via `setPlaying`; lucide `Play` icon when paused as accent CTA, `Pause` icon when playing as neutral) + ReplaySpeedToggle (currentSpeed=speed, onChange=setSpeed, speeds=getConfig().replay.speeds) + elapsed/total time as Fira Code tabular "{elapsed} / {total}".
    - elapsed/total: derived from `performance.now() - startedAtWall` and the analytic crash time via `crashTimeMs(crashPoint, growthRate)` from `@crash/contracts/multiplier` (or compute inversely from the formula). Format `m:ss / m:ss`. Update via a lightweight rAF or setInterval inside the modal (acceptable; minimal CPU; tracking display only — no impact on the canvas driver).

    Mount in __root.tsx as a sibling of `<Outlet />` (same precedent as VerificationDrawer from Plan 08-05).

    HistoryStrip wiring (UI-SPEC Surface E):
    - Each chip becomes an interactive trigger. Add an `onClick` that calls `useReplayStore.getState().openReplay(entry.roundId, getConfig().replay.autostart, getConfig().replay.speeds[0])`.
    - Add a lucide `History` 12px icon at the right edge of each chip (muted-foreground by default, accent on hover/focus). Touch: always visible.
    - Tooltip extension: append "Click to replay" line after the existing "Crashed @ ..." line.
    - aria-label: `Replay Round #{shortId}, crashed at {crashPoint}x`.
    - Hit area: chip is already min-h-11 from Phase 7; ensure min-w-11 is preserved if not already.

    Tests for ReplayModal:
    - Test 1: when replay store roundId === null, the modal does not render (querySelector for `[role="dialog"]` returns null).
    - Test 2: with roundId set + mocked useRoundDetail returning Phase 4 fixture, the modal renders DialogTitle with the locked text "Replay · Round #" (substring) and DialogDescription exact "Reproduced from serverSeed + clientSeed + bets[]. Same renderer as the live game."
    - Test 3: autostart=true → `playing` is true initially, Pause button visible.
    - Test 4: clicking the speed toggle "2x" calls setSpeed(2) (spy or store assertion).
    - Test 5: with the round-detail hook in error state, an Alert variant=destructive renders with "Couldn't load replay data" substring (NOT a sonner toast — sonner-mounted root spy stays at 0 toast calls).
    - Test 6: with autostart=false (override in test), the modal renders with Play button instead of Pause; clicking it calls setPlaying(true).

    Tests for HistoryStrip:
    - Test 1: existing P07-07 tests still green (regression).
    - Test 2: clicking a chip triggers `openReplay(roundId, autostart, defaultSpeed)` — spy on the store action.
    - Test 3: chips have aria-label `Replay Round #...`.
    - Test 4: tooltip text contains "Click to replay".
    - Test 5: lucide History icon rendered inside the chip.

    __root.tsx changes: add `<ReplayModal />` as a sibling of `<Outlet />`, after `<VerificationDrawer />`.
  </behavior>
  <action>
    Author ReplayModal per behavior. Theme tokens only. Use `getConfig().replay.speeds` for the toggle and `getConfig().replay.autostart` for the autostart wiring. Mount via shadcn `Dialog`.

    Memoize the driver per the behavior — the modal must NOT recreate the driver on every render (rAF instability + lost startedAtWall). Use `useMemo` with deps including roundId/growthRate/crashPoint so changing the round closes/reopens the driver correctly.

    Patch HistoryStrip per behavior. Keep theme-token discipline (no hex). The chip remains a `<button>`; just wire the click and add the icon.

    Patch __root.tsx to mount `<ReplayModal />`.

    Author the test files using Vitest + RTL. Mock `useRoundDetail` for the modal tests (don't exercise the network in unit tests). Use `vi.spyOn` on `useReplayStore.getState()` actions for click assertions.

    For Test 5 (no sonner): query the Toaster portal (sonner mounts under a known testid) and assert no toast rendered.

    No code fences.
  </action>
  <acceptance_criteria>
    - `frontend/src/components/replay-modal.tsx` exists and exports `ReplayModal`
    - `frontend/src/routes/__root.tsx` contains `<ReplayModal />` (grep) AND retains `<VerificationDrawer />` from Plan 08-05
    - `frontend/src/components/history-strip.tsx` chip onClick references `openReplay` (grep)
    - `cd frontend && bunx vitest run src/components/replay-modal.test.tsx src/components/history-strip.test.tsx --reporter=verbose` reports >= 11 tests (6 + 5) all PASS
    - Test 2 of ReplayModal asserts exact DialogDescription "Reproduced from serverSeed + clientSeed + bets[]. Same renderer as the live game."
    - Test 5 asserts the error path uses inline Alert (NOT sonner toast)
    - `! grep -E '#[0-9a-fA-F]{3,6}' frontend/src/components/replay-modal.tsx frontend/src/components/history-strip.tsx`
    - `cd frontend && bunx tsc --noEmit` exit 0
    - Full FE suite green: `cd frontend && bun run test`
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; bunx tsc --noEmit &amp;&amp; bunx vitest run src/components/replay-modal.test.tsx src/components/history-strip.test.tsx --reporter=verbose &amp;&amp; grep -q '&lt;ReplayModal' src/routes/__root.tsx &amp;&amp; grep -q 'openReplay' src/components/history-strip.tsx &amp;&amp; ! grep -E '#[0-9a-fA-F]{3,6}' src/components/replay-modal.tsx src/components/history-strip.tsx &amp;&amp; bun run test</automated>
  </verify>
  <done>
    REQ-REPLAY-02 observable end-to-end: click history chip → modal opens → auto-plays at 1x using the production CrashCurve component → speed toggle + Play/Pause work → overlays show bets[] → close returns to live game; D-05 enforced (one CrashCurve component, two drivers).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Replay driver ← useMultiplierStore (LIVE) | Replay driver must NOT write to the live multiplier store (Plan 08-03 contract) — enforced by use-raf-curve isLive gate; Plan 08-07 reinforces by passing a non-live driver |
| Modal ← /verify endpoint | bets[] and growthRate are read; server's `recomputedCrashPoint`/`matches` are NOT read by the replay path (replay only needs the raw inputs + outputs to drive the deterministic curve) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-22 | Tampering | Modal duplicates rendering logic instead of reusing CrashCurve (violates D-05) | mitigate | Task 2 extends CrashCurve with optional driver prop; Task 3 uses the same component; SUMMARY explicit confirmation grep "ReplayModal renders <CrashCurve" |
| T-08-23 | Denial of Service | Two simultaneous rAF loops drain CPU on the modal-open transition (Pitfall 3) | accept | RESEARCH § Pitfall 3 documents option (a) "accept it"; smoke probe during 08-10 closeout — if CPU climbs > 5% fall back to option (b) (gate live loop while modal open) |
| T-08-24 | Denial of Service | Replay modal lost on route change (Pitfall 5) | mitigate | Mounted at __root.tsx sibling of <Outlet /> — Task 3; tested via the test 1 (closed initially) + render-survives navigation manual sanity in 08-10 |
| T-08-25 | Tampering | Player-id leak via unmasked bets[] | mitigate | bets[] uses Plan 08-02's masked CurrentRoundBetView shape verbatim; replay-overlays render `playerIdMasked` only |
</threat_model>

<verification>
- `cd frontend && bunx tsc --noEmit` exit 0
- `cd frontend && bun run test` exit 0 (full suite, all prior + new)
- Manual sanity (live smoke during 08-10): click a history chip → modal opens → curve animates → speed 2x makes it visibly faster → speed 4x faster still → pause halts → close returns to live game (no rAF leak; DevTools Performance panel can verify)
- Manual sanity: open drawer (Plan 08-05) → click "Open full verification" → navigates to /verify/<id> WITHOUT dismissing the replay modal if it was open (mount-at-root contract)
</verification>

<success_criteria>
- REQ-REPLAY-02 + REQ-REPLAY-03 observable: history-chip → modal → SAME CrashCurve component → deterministic replay at 1x/2x/4x.
- D-05 enforced by code shape (one component) and by test (Task 3 Test 4 verifies the modal uses CrashCurve with a driver prop).
- D-02 visual contract honored: dialog scrim + backdrop blur shows live game faintly behind; auto-play at 1x per D-03.
- Bet overlays render masked bets[] — replay's "proof" surface for REQ-REPLAY-02 includes bet outcomes, not just the curve.
</success_criteria>

<output>
Create `.planning/phases/08-provably-fair-history-replay/08-07-SUMMARY.md` documenting: ReplayModal layout decisions, the CrashCurve driver-prop extension, the memoization decision for the driver factory, any visible CPU impact observed (Pitfall 3 — option (a) accepted vs option (b) needed), and the history-strip wiring (icon placement + tooltip extension + aria-label).
</output>
