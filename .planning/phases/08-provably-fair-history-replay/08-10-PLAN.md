---
phase: 08-provably-fair-history-replay
plan: 10
type: execute
wave: 6
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
  - 08-05
  - 08-06
  - 08-07
  - 08-08
  - 08-09
files_modified:
  - .planning/adrs/ADR-028-client-seed-derivation-deterministic.md
  - .planning/adrs/ADR-029-replay-reuses-canvas-renderer.md
  - .planning/adrs/ADR-030-browser-safe-contracts-subpath-crypto-subtle.md
  - .planning/adrs/ADR-031-replay-modal-speed-selector.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - REQ-FE-09
  - REQ-FE-10
  - REQ-REPLAY-01
  - REQ-REPLAY-02
  - REQ-REPLAY-03

must_haves:
  truths:
    - "Four ADRs land at the NEXT FREE numbers ADR-028..031 (ROADMAP anticipated 'ADR-023/024' labels were stale — Phase 5/6/7 consumed 019..027); shipped ADRs are NEVER renumbered"
    - "STATE.md, ROADMAP.md, REQUIREMENTS.md mark all 5 Phase 8 REQ-IDs as Done (REQ-FE-09, REQ-FE-10, REQ-REPLAY-01, REQ-REPLAY-02, REQ-REPLAY-03)"
    - "v1-complete count in REQUIREMENTS.md increments from 62/95 to 67/95"
    - "ROADMAP Phase 8 row updates from '0/0 Not started' to '10/10 Complete' with the closeout date"
    - "Live-smoke checkpoint observably confirms: drawer opens + computes SHA-256 + verdict shows MATCH for a freshly settled round; /verify/:roundId loads + MATCH appears; click history chip → replay modal opens + auto-plays at 1x + speeds work; oidc silent renewal continues during drawer (Pitfall 6) — verified by waiting past renew window"
  artifacts:
    - path: ".planning/adrs/ADR-028-client-seed-derivation-deterministic.md"
      provides: "ADR for D-04 — reaffirms Phase 4 deterministic client-seed derivation in Phase 8 context"
    - path: ".planning/adrs/ADR-029-replay-reuses-canvas-renderer.md"
      provides: "ADR for D-05 — driver-injection seam over separate playback code path"
    - path: ".planning/adrs/ADR-030-browser-safe-contracts-subpath-crypto-subtle.md"
      provides: "ADR for D-06 — browser-safe @crash/contracts/provably-fair-browser subpath + crypto.subtle"
    - path: ".planning/adrs/ADR-031-replay-modal-speed-selector.md"
      provides: "ADR for D-02 + D-03 — modal over live game vs separate route + 1x/2x/4x speed selector"
    - path: ".planning/STATE.md"
      provides: "Updated Current Position + Progress + Performance Metrics + Todos for Phase 8 close"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 8 row Complete + Plans landed summary + ADR-renumber reconciliation note"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All 5 Phase 8 REQ-IDs status Done + v1-complete 67/95 + Phase 8 traceability table"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 8 status synchronized: both show 10/10 plans Complete"
      pattern: "Phase 8.*Complete"
---

<objective>
Close out Phase 8 with the four ADRs at next-free numbers (ADR-028..031 — Phase 5 took 019/020, Phase 6 took 021/022/023, Phase 7 took 024..027; ROADMAP's "anticipated ADR-023/024" labels are stale and must be reconciled with a renumber note); update STATE.md / ROADMAP.md / REQUIREMENTS.md to reflect all 5 Phase 8 REQ-IDs Done (REQ-FE-09, REQ-FE-10, REQ-REPLAY-01, REQ-REPLAY-02, REQ-REPLAY-03), v1-complete moves from 62/95 to 67/95, Phase 8 ROADMAP row from "0/0 Not started" to "10/10 Complete"; perform a blocking live-smoke walkthrough that observably exercises the drawer, verify route, replay modal, and oidc silent-renew compatibility (Assumption A3 + Pitfall 6) under real `bun run docker:up` + Keycloak conditions.

Per Phase 7 closeout precedent (P07-09 SUMMARY): documentation-only behavior; no source change; lint + tsc + tests stay green.

Purpose: Record the four Phase 8 decisions formally (Context / Decision / Consequences / Alternatives Rejected per CLAUDE.md ADR template); make project state observably complete (Phase 8 is the last of three parallelizable phases per ROADMAP — Phase 9 + Phase 10 land after this).

Output: 4 ADR files + 3 planning-doc updates + a passing live-smoke walkthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-provably-fair-history-replay/08-CONTEXT.md
@.planning/phases/08-provably-fair-history-replay/08-RESEARCH.md
@.planning/phases/08-provably-fair-history-replay/08-UI-SPEC.md
@.planning/phases/08-provably-fair-history-replay/08-01-SUMMARY.md
@.planning/phases/08-provably-fair-history-replay/08-02-SUMMARY.md
@.planning/phases/08-provably-fair-history-replay/08-03-SUMMARY.md
@.planning/phases/08-provably-fair-history-replay/08-04-SUMMARY.md
@.planning/phases/08-provably-fair-history-replay/08-05-SUMMARY.md
@.planning/phases/08-provably-fair-history-replay/08-06-SUMMARY.md
@.planning/phases/08-provably-fair-history-replay/08-07-SUMMARY.md
@.planning/phases/08-provably-fair-history-replay/08-08-SUMMARY.md
@.planning/phases/08-provably-fair-history-replay/08-09-SUMMARY.md

@.planning/adrs/ADR-015-crash-point-formula-and-client-seed-derivation.md
@.planning/adrs/ADR-025-canvas-2d-crash-curve.md
@.planning/adrs/ADR-027-oidc-spa-broadcastchannel-multi-tab-refresh.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-028..031 capturing the four Phase 8 decisions at next-free numbers</name>
  <files>
    .planning/adrs/ADR-028-client-seed-derivation-deterministic.md,
    .planning/adrs/ADR-029-replay-reuses-canvas-renderer.md,
    .planning/adrs/ADR-030-browser-safe-contracts-subpath-crypto-subtle.md,
    .planning/adrs/ADR-031-replay-modal-speed-selector.md
  </files>
  <read_first>
    .planning/adrs/ (full directory listing to confirm 001..027 are taken — re-read at execution start to be safe; if any Phase 8 work elsewhere claimed 028..031 land between plan and execution, shift up to next-free),
    .planning/adrs/ADR-015-crash-point-formula-and-client-seed-derivation.md (the original Phase 4 ADR that locked the seed derivation — ADR-028 cross-references it),
    .planning/adrs/ADR-025-canvas-2d-crash-curve.md (Phase 7 ADR that locked Canvas 2D — ADR-029 cross-references it),
    .planning/adrs/ADR-027-oidc-spa-broadcastchannel-multi-tab-refresh.md (most recent ADR — file structure and tone to mirror),
    CLAUDE.md (ADR template requirement: Status / Date / Phase / Context / Considered / Decision / Consequences / Alternatives Rejected),
    .planning/phases/08-provably-fair-history-replay/08-CONTEXT.md (D-01 through D-06 — source of the four decisions),
    All Plan 08-01..08-09 SUMMARYs (the actual implementations to cite in the Consequences sections)
  </read_first>
  <behavior>
    Each ADR follows the CLAUDE.md template: H1 "ADR-NNN: Title", then bolded `**Status**: Accepted`, `**Date**: 2026-05-29` (or actual execution date), `**Phase**: 8`. Then sections H2 Context, H2 Considered (with bulleted Options A/B/C with pros/cons), H2 Decision (single chosen option + why), H2 Consequences (what this locks in, what it forecloses, cross-references to plans).

    ADR-028 — Client-seed derivation strategy: deterministic from previous round close (`deriveClientSeed(prev.id, prev.crashedAt)`) — REAFFIRMS Phase 4's decision in the Phase 8 context. Title: "Client-Seed Derivation: Deterministic from Previous Round Close (Reaffirmed)". Context: D-04 in 08-CONTEXT.md; player-contributed seeds out of v1 scope; the drawer + /verify route rely on this determinism. Considered: (A) deterministic-from-previous (chosen — already locked in Phase 4 ADR-015), (B) player-contributed (stretch only — preserves player agency but adds UX surface), (C) randomBytes-each-round (forecloses chain verification). Decision: A. Consequences: Plan 08-05 drawer's chain proof works (compute SHA-256(prev.serverSeed) and compare against current.seedHash); Plan 08-06's verify route shows the deterministic clientSeed in the inputs card; future "let player supply seed" stretch (REQ-STRETCH adjacent) requires a saga + ADR-supersede. Cross-reference ADR-015.

    ADR-029 — Replay reuses production Canvas renderer over separate playback code path. Title: "Replay Reuses Production Canvas Renderer via Driver Injection". Context: D-05; REQ-REPLAY-03 mandates renderer reuse; the modal needs the same dPR/clearRect/glow discipline as live to make REQ-REPLAY-01 byte-equivalence a property of the renderer itself, not a duplicate implementation. Considered: (A) Driver-injection seam on use-raf-curve (chosen — Plan 08-03 + 08-07), (B) Separate <ReplayCurve /> component with copy-pasted draw logic (forecloses determinism guarantee; doubles maintenance), (C) Pre-sampled-array fed to draw-curve (forecloses speed selector flexibility — RESEARCH § Anti-Patterns). Decision: A. Consequences: 08-03 Plan refactor; CrashCurve gains optional `driver` prop; useRafCurve adds optional `RafCurveDriver` param defaulting to live; the determinism E2E (Plan 08-08) is provable in pure math, not in a browser fixture. Cross-reference ADR-025.

    ADR-030 — Browser-safe `@crash/contracts/provably-fair-browser` subpath + crypto.subtle. Title: "Browser-Safe Provably-Fair Subpath with crypto.subtle (HMAC + SHA-256)". Context: D-06; REQ-FE-10 mandates client-side recomputation; the existing `@crash/contracts/provably-fair` imports `node:crypto` and CANNOT load in the FE bundle (Phase 7 boot-spike confirmed root barrel leaks node:crypto). Two byte-encoding traps (Pitfalls 1+2 in 08-RESEARCH): HMAC key is UTF-8-bytes-of-hex-string (NOT hex-decoded); SHA-256 chain proof IS hex-decoded. Considered: (A) New `provably-fair-browser` subpath with async crypto.subtle implementation (chosen — Plan 08-01), (B) Pure-JS `js-sha256` npm + sync API (rejected — adds bundle, hand-rolled crypto), (C) Node polyfill via vite plugin (rejected — masks the leak instead of fixing it). Decision: A. Consequences: Plan 08-01 ships the new subpath + Phase 4 locked-byte 2.94 oracle test; package.json `exports` map adds `"./provably-fair-browser"`; both byte-encoding contracts are documented in JSDoc with the matching test fixture; future-maintainer regression caught by Test 1.

    ADR-031 — Replay modal over live game + 1x/2x/4x speed selector. Title: "Replay Modal Over Live Game with 1x/2x/4x Speed Selector". Context: D-02 + D-03; alternative was a dedicated `/replay/:roundId` route. UI continuity argument: drawer + history strip live on the game route; replay-as-modal preserves the player's mental model of the live loop; the live game keeps animating behind the blurred scrim. Considered: (A) Modal mounted at __root.tsx sibling of Outlet, auto-play at 1x, 1x/2x/4x ToggleGroup (chosen — Plan 08-07), (B) Separate route + linked chip click (forecloses "live continues behind" UX, doubles auth context surface, breaks player flow), (C) Drawer-shaped replay panel sliding from the right (cluttered against the verification drawer; UI-SPEC committed to modal). Decision: A. Speed selector source: `getConfig().replay.speeds` env-tunable per UI-SPEC § Config-driven; default `[1,2,4]`; D-03 invariant "must include 1" hard-asserted in `buildConfig`. Consequences: Plan 08-04 lands the env + store; Plan 08-07 lands the modal + speed wiring; Plan 08-08 locks the speed-time equivalence property (sampleReplayMultiplier(s@2, t) === sampleReplayMultiplier(s@1, 2t)) in source; Pitfall 3 (two rAF loops) accepted with monitoring during 08-10 smoke.

    Each ADR's "Alternatives Rejected" section explicitly enumerates the rejected options with one-sentence reasons.
  </behavior>
  <action>
    Re-confirm next-free ADR number at execution time:
    `ls .planning/adrs/ADR-*.md | sed 's/.*ADR-\\([0-9]\\+\\).*/\\1/' | sort -n | tail -1`
    If the result is 027 the plan numbering (028..031) holds; if any earlier work shifted, recompute and rename consistently (always next-free, never reusing).

    Author the four ADRs in order, citing the related plan SUMMARYs in the Consequences sections (08-01 for ADR-030, 08-03+08-07 for ADR-029, 08-07 for ADR-031, all + Phase 4 ADR-015 for ADR-028).

    Cross-reference: each ADR's "Date" is the execution date; "Status" is Accepted; "Phase" is 8.

    Match the prose tone of ADR-027 (Phase 7's most recent close ADR) — senior, defensible, no marketing. No emojis.

    No code fences in this action body.
  </action>
  <acceptance_criteria>
    - Four ADR files exist with the documented filenames and slug suffixes
    - Each ADR contains the seven required sections: Title, Status, Date, Phase, Context, Considered, Decision, Consequences, Alternatives Rejected (per CLAUDE.md template — Alternatives Rejected may be inside Consequences or a sibling section; the template tolerates both)
    - Each ADR cross-references at least one prior ADR (ADR-015 for ADR-028, ADR-025 for ADR-029, no prior for ADR-030, ADR-027 + ADR-025 for ADR-031 if relevant) AND at least one Plan SUMMARY (08-01..08-09)
    - `grep -c "Accepted" .planning/adrs/ADR-028*.md .planning/adrs/ADR-029*.md .planning/adrs/ADR-030*.md .planning/adrs/ADR-031*.md` returns 1 for each
    - No ADR file uses numbers already taken (verify ls counts: `ls .planning/adrs/ADR-{028,029,030,031}-*.md` shows exactly 4 files; no duplicates from prior runs)
  </acceptance_criteria>
  <verify>
    <automated>ls .planning/adrs/ADR-028-*.md .planning/adrs/ADR-029-*.md .planning/adrs/ADR-030-*.md .planning/adrs/ADR-031-*.md &amp;&amp; for f in .planning/adrs/ADR-{028,029,030,031}-*.md; do grep -q '^\*\*Status\*\*: Accepted' "$f" || { echo "MISSING STATUS in $f"; exit 1; }; done</automated>
  </verify>
  <done>
    Four ADRs at next-free numbers; each captures Context/Considered/Decision/Consequences for the Phase 8 decision; cross-references prior ADRs + the implementing Plan SUMMARYs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md + ROADMAP.md + REQUIREMENTS.md to mark Phase 8 Complete and reconcile ADR numbering</name>
  <files>
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/STATE.md (current Position / Progress / Todos / Performance Metrics — the structure to preserve while incrementing),
    .planning/ROADMAP.md (Phase 8 row in the Progress table; "Plans" line under Phase 8; the ADR-renumber note pattern from Phase 7's closeout — line 220 in ROADMAP and the closing notes),
    .planning/REQUIREMENTS.md (Phase 8 traceability table at the bottom; v1-complete count line),
    All Plan 08-NN SUMMARYs to extract implementation citations for the REQUIREMENTS table updates
  </read_first>
  <behavior>
    STATE.md updates (preserve existing structure, append/replace targeted sections):
    - Current Position: change Phase to "8 — Provably-Fair UX, History & Replay (COMPLETE, 10/10 plans landed; 8/10 phases complete overall)"; update Plan line to "P08-10 complete (10/10) — phase closeout: four Phase 8 ADRs authored + planning docs rotated. ADR-028 (D-04 client-seed-deterministic reaffirmation), ADR-029 (D-05 driver-injection seam over separate playback code path), ADR-030 (D-06 browser-safe @crash/contracts/provably-fair-browser subpath + crypto.subtle for HMAC + SHA-256 with two locked byte-encoding contracts), ADR-031 (D-02+D-03 replay modal over live game + 1x/2x/4x speed selector with VITE_REPLAY_SPEEDS env-tunable). The ROADMAP's stale anticipated 'ADR-023/024' Phase 8 labels were reconciled to the actual next-free ADR-028..031 (019/020 Phase 5, 021/022/023 Phase 6, 024..027 Phase 7) with a renumber note; shipped ADRs were not renumbered. REQUIREMENTS confirmed all 5 Phase 8 REQ-IDs Done."
    - Append a short summary line for each P08-NN (briefly mirroring the Phase 7 closeout style in STATE.md lines around 25), citing key implementation facts: P08-01 browser-safe subpath + 2.94 oracle; P08-02 verify endpoint extension; P08-03 driver-seam refactor; P08-04 env+stores; P08-05 FairnessBadge + VerificationDrawer; P08-06 verify route; P08-07 ReplayModal; P08-08 determinism E2E byte-match; P08-09 README example; P08-10 closeout.
    - Status: update to reflect Phase 8 ROADMAP success criteria observably true: (1) FairnessBadge in header + drawer with crypto.subtle SHA-256 verdict; (2) /verify/:roundId runs HMAC + Bustabit locally + ignores server's matches field; (3) Replay button on every chip → modal reusing CrashCurve via driver injection; (4) determinism test byte-equality green; (5) README recruiter example reproduces 2.94 from the locked tuple.
    - Progress: update bar to `▰▰▰▰▰▰▰▰▱▱` 8/10 phases complete.
    - Next action: update to "Phase 8 is COMPLETE (10/10 plans landed). Phase 8 is parallelizable with Phase 9 per ROADMAP — both can run after Phase 7 / Phase 8. Run `/gsd:verify-phase 8` + `/gsd:ui-review` then `/gsd:plan-phase 9`."
    - Performance Metrics: increment "Phases complete" from 7/10 to 8/10; "v1 requirements complete" from 62/95 to 67/95 (REQ-FE-09 + REQ-FE-10 + REQ-REPLAY-01 + REQ-REPLAY-02 + REQ-REPLAY-03); "ADRs landed" from 27 to 31 with Phase 8 row added: 4.
    - Todos: mark Phase 8 plan/verify items as done; preserve all unchanged items.
    - Last updated footer: today's date + "P08-10 — Phase 8 COMPLETE (10/10 plans, 8/10 phases). ..." mirroring Phase 7's footer style.

    ROADMAP.md updates:
    - Phase 8 row in the Progress table: from "0/0 Not started" to "10/10 Complete" + completion date.
    - Phase 8 details block "**Plans:** 0 plans" → "**Plans:** 10 plans" with the 10 plan entries marked [x]:
      - [x] 08-01-PLAN.md — Browser-safe @crash/contracts/provably-fair-browser subpath (crypto.subtle HMAC + SHA-256) + Phase 4 2.94 oracle test
      - [x] 08-02-PLAN.md — Extend VerifyRoundDto with bets[] + growthRate; BetRepository.findByRound (no status filter)
      - [x] 08-03-PLAN.md — Refactor use-raf-curve.ts to accept optional RafCurveDriver (Phase 7 callers zero-diff)
      - [x] 08-04-PLAN.md — VITE_REPLAY_SPEEDS/AUTOSTART/DRAWER_SLIDE_MS env + shadcn sheet/toggle-group/alert + fairness.store + replay.store
      - [x] 08-05-PLAN.md — FairnessBadge + HashBlock + VerdictChip + VerificationDrawer (Sheet, mount at __root.tsx) + useVerifyPrevious
      - [x] 08-06-PLAN.md — /verify/$roundId route (ssr:false) + useRecomputeCrashpoint (ignores server matches field)
      - [x] 08-07-PLAN.md — ReplayModal (Dialog) + replay driver + Play/Pause + 1x/2x/4x ToggleGroup + history-chip wiring
      - [x] 08-08-PLAN.md — Determinism E2E byte-match test (REQ-REPLAY-01)
      - [x] 08-09-PLAN.md — README "Provably Fair: Verify Outside the App" recruiter example
      - [x] 08-10-PLAN.md — ADR-028..031 + STATE/ROADMAP/REQUIREMENTS closeout
    - The "Key decisions to make (ADRs)" labels "ADR-023" and "ADR-024" are stale. Replace with reconciliation note: "**ADR-renumber note:** these four were anticipated under labels ADR-023/024, but Phase 5/6/7 ADRs consumed 019..027 before Phase 8 closed. Shipped ADRs are never renumbered; Phase 8 ADRs took the next-free range ADR-028..031: ADR-028 (D-04 client-seed-deterministic reaffirmation), ADR-029 (D-05 driver-injection seam), ADR-030 (D-06 browser-safe contracts subpath + crypto.subtle), ADR-031 (D-02+D-03 replay modal + speed selector). Phase 9/10 anticipated-ADR labels will likewise resolve to next-free numbers when those phases ship."
    - Footer "Last updated" line: today's date + a one-sentence Phase 8 closeout summary mirroring Phase 7's footer style.

    REQUIREMENTS.md updates:
    - REQ-FE-09 status: from Pending → Done (P08-05). Add citation in the Status column or Phase 8 table: "Done (P08-05 — FairnessBadge fills the Phase 7 header data-slot; VerificationDrawer Sheet computes SHA-256(prev serverSeed) via @crash/contracts/provably-fair-browser; verdict chip renders MATCH/MISMATCH with icon+text+theme color; mounted at __root.tsx sibling of <Outlet />)".
    - REQ-FE-10 status: Done (P08-06 — /verify/$roundId TanStack Router file-based route with ssr:false; useRecomputeCrashpoint runs deriveCrashPointAsync against @crash/contracts/provably-fair-browser; ignores server's `matches`/`recomputedCrashPoint` fields verified by source-grep gate + Test 4; locked Alert copy for not-settled / not-found / fetch-error paths).
    - REQ-REPLAY-01 status: Done (P08-08 — determinism.test.ts byte-equality assertion across live vs replay derivations at fixed t grid + speed-time equivalence at 1x/2x/4x; anchored to Phase 4 locked-byte 2.94 fixture).
    - REQ-REPLAY-02 status: Done (P08-07 — ReplayModal mounted at __root.tsx; auto-plays at 1x per D-03 + getConfig().replay.autostart; Play/Pause + 1x/2x/4x speed via getConfig().replay.speeds; bet/cashout overlays from extended verify endpoint's bets[]).
    - REQ-REPLAY-03 status: Done (P08-03 + P08-07 — use-raf-curve.ts accepts optional RafCurveDriver, CrashCurve gains optional driver prop, ReplayModal mounts the SAME CrashCurve with makeReplayDriver — no separate code path; Phase 7 callers zero-diff verified by Plan 08-03 Tests 1+2).
    - v1-complete summary line: "62/95" → "67/95" with the 5 new REQs listed in the increment.
    - Phase 8 traceability table at the bottom: replace "Pending" rows with the Done citations above.
    - Footer "Last updated" line: today's date + Phase 8 closeout summary.

    DO NOT edit any other phase's REQ rows.
  </behavior>
  <action>
    Read each of the three files in full first, then perform targeted edits matching the existing formatting verbatim (column alignment, h2/h3 hierarchy, list bullet style).

    For ROADMAP's Phase 8 details block, the existing detail content (Goal / Depends on / Requirements / Success Criteria / Blocks / Parallelizable with / UI hint / Key decisions to make) stays unchanged; only the Plans subsection grows from "(empty)" to the 10-entry checked list; and the ADR-renumber note is added inside the "Key decisions to make" subsection per the Phase 7 precedent.

    For STATE.md and REQUIREMENTS.md, mirror the Phase 7 closeout footer style (lines around 349 in ROADMAP and lines around 385 in REQUIREMENTS).

    No code fences in this action body.
  </action>
  <acceptance_criteria>
    - STATE.md shows Phase 8 COMPLETE in Current Position, progress bar updated to 8/10
    - ROADMAP.md Progress table row for Phase 8 shows "10/10" + "Complete" + a date
    - ROADMAP.md Phase 8 detail block "Plans" subsection has 10 [x] entries
    - ROADMAP.md Key Decisions block contains the ADR-renumber note referencing ADR-028..031
    - REQUIREMENTS.md REQ-FE-09 + REQ-FE-10 + REQ-REPLAY-01/02/03 all show Done with plan citations
    - REQUIREMENTS.md Coverage summary "v1 complete" reads 67/95
    - All three file edits preserve existing content (git diff review: no orphaned sections; no Phase 1-7 rows mutated)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Phase 8.*Complete\|8/10' .planning/STATE.md &amp;&amp; grep -q '10/10' .planning/ROADMAP.md &amp;&amp; grep -q 'ADR-028' .planning/ROADMAP.md &amp;&amp; grep -q 'REQ-FE-09.*Done\|Done.*P08-05' .planning/REQUIREMENTS.md &amp;&amp; grep -q '67 / 95\|67/95' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    Three planning docs synchronized; ADR numbering reconciled; v1 progress incremented; phase 8 archive is internally consistent.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Blocking live-smoke walkthrough — observe REQ-FE-09 + REQ-FE-10 + REQ-REPLAY-02 + Pitfall 6 in the running app</name>
  <what-built>
    Phase 8 frontend surfaces (drawer, /verify route, replay modal) on top of the green Phase 7 dark-casino game. Backend: extended /verify endpoint shipping bets[] + growthRate. Contracts: browser-safe provably-fair subpath. Determinism E2E green in Vitest.
  </what-built>
  <how-to-verify>
    Setup: `bun run docker:up` brings the stack online; `docker compose restart kong` if the Phase 7 CORS additions are not yet loaded; `cd frontend && bun run dev`; open `http://localhost:3000`; log in as `player/player123`.

    REQ-FE-09 (Fairness badge + drawer):
    1. Confirm the header shows the Fairness badge with ShieldCheck icon + label + emerald pulse dot during BETTING phase.
    2. Click the badge. The drawer slides in from the right; the live curve keeps animating behind a slightly blurred scrim.
    3. Wait for one round to settle. Open the drawer again. The "Previous round" panel shows the revealed serverSeed + the computed SHA-256 hash; the verdict chip reads "MATCH · Previous commitment confirmed" with CheckCircle2 icon.
    4. Close the drawer (Esc or click outside). The live game remains uninterrupted.

    REQ-FE-10 (/verify/:roundId route):
    5. From the drawer or by manually visiting `http://localhost:3000/verify/<a-settled-round-id-from-history>` — confirm three Cards (Inputs / Computed in your browser / Reported by server) render; MATCH verdict appears; the page does NOT show "MISMATCH" for any healthy settled round.
    6. Visit `/verify/<a-running-round-id>` (a current/RUNNING round) — confirm the default Alert appears with locked copy "This round has not crashed yet. The serverSeed will be revealed automatically after it settles."
    7. Visit `/verify/not-a-uuid` — confirm the destructive not-found Alert appears.

    REQ-REPLAY-02 (Replay modal):
    8. Click any chip on the history strip. The Replay modal opens; the curve auto-plays at 1x; the live game continues animating behind the scrim.
    9. Click the 2x speed toggle. The curve visibly speeds up. Click 4x — even faster. Click 1x — back to real-time.
    10. Pause: the curve halts. Resume: it continues from the same multiplier.
    11. Close the modal (Esc). Live game resumes seamlessly.

    Pitfall 6 (oidc-spa silent renewal during drawer/modal):
    12. Open the verification drawer.
    13. Wait at least 5 minutes (Keycloak default access-token lifetime; longer if KEYCLOAK_ACCESS_TOKEN_LIFESPAN is overridden).
    14. While the drawer is still open, click "Open full verification" link (navigates to /verify/...). Confirm no Sonner toast "Session expired" appears; the page loads with the verdict; protectedFetch succeeded with a renewed token (verifiable in DevTools Network panel — Authorization header has a fresh JWT).
    15. Open the replay modal (still no logout). Confirm the round-detail fetch succeeds.

    Render-survives-navigation contract (Pitfall 5):
    16. Open the replay modal. Click "Open full verification" in the drawer (or manually navigate to /verify/...). Confirm the replay modal STAYS OPEN through the route transition (does NOT close).

    Determinism cross-check (sanity for REQ-REPLAY-01):
    17. `cd frontend && bunx vitest run src/features/replay/determinism.test.ts` → all tests PASS.

    README recruiter example:
    18. From a fresh shell paste the README "Worked example (no live stack required)" block. Confirm both Step A (seedHash digest) and Step B (crashPoint = 2.94) reproduce.

    Reduced-motion gate:
    19. In OS settings enable "Reduce motion" (macOS: Settings → Accessibility → Display → Reduce motion). Reload the page. Open the drawer + modal — confirm both snap instead of slide; the dot pulse stops; the curve still renders content but glow is reduced.
  </how-to-verify>
  <resume-signal>Type "approved" if all 19 steps observably pass. Otherwise list the step number(s) that failed with what you observed (e.g. "Step 4: drawer dimmed the live game — looked frozen, not animating").</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Atomic commits per Phase 7 closeout style + final sanity gate</name>
  <files>
    .planning/adrs/ADR-028-client-seed-derivation-deterministic.md,
    .planning/adrs/ADR-029-replay-reuses-canvas-renderer.md,
    .planning/adrs/ADR-030-browser-safe-contracts-subpath-crypto-subtle.md,
    .planning/adrs/ADR-031-replay-modal-speed-selector.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    git log --oneline -n 30 (to confirm the project commit style — `docs:`, `feat:`, `fix:`, `chore:` prefixes; no AI attribution per CLAUDE.md and user global rules)
  </read_first>
  <behavior>
    - Each ADR lands in its own atomic commit with a clear `docs(adr-NNN):` prefix and a short body explaining what was locked.
    - Planning doc updates land in one consolidated commit `docs(phase-8): close phase 8 — REQ-FE-09/10 + REQ-REPLAY-01/02/03 done`.
    - NO AI attribution, NO Co-Authored-By, NO emojis (CLAUDE.md + global rules).
    - tsc + tests + lint stay green after each commit (per Phase 7 closeout precedent).
  </behavior>
  <action>
    Run a final sanity gate:
    - `cd frontend && bunx tsc --noEmit && bun run test && bun run lint` (all must exit 0 — Phase 7 standard 65+/65+ tests + new Phase 8 tests; lint clean except for pre-existing `routeTree.gen.ts` warning carried in deferred-items.md).
    - `cd services/games && bunx tsc --noEmit && bun test tests/unit/` (verify-round.use-case.test.ts + others green from Plan 08-02).
    - `cd packages/contracts && bunx tsc --noEmit && bun test` (Plan 08-01 oracle + Phase 4 fixture).

    Author commits:
    1. `docs(adr-028): client-seed derivation deterministic (Phase 4 reaffirm)`
    2. `docs(adr-029): replay reuses production Canvas renderer via driver injection`
    3. `docs(adr-030): browser-safe @crash/contracts/provably-fair-browser subpath + crypto.subtle`
    4. `docs(adr-031): replay modal over live game + 1x/2x/4x speed selector`
    5. `docs(phase-8): close phase 8 — REQ-FE-09/10 + REQ-REPLAY-01/02/03 done`

    Each commit body explains the decision in 2-4 lines + cross-references the Plan SUMMARY.

    No AI attribution. Match the existing repo prefix style observed in `git log`.

    No code fences in this action body.
  </action>
  <acceptance_criteria>
    - `cd frontend && bunx tsc --noEmit` exit 0
    - `cd frontend && bun run test` exit 0 (full suite green)
    - `cd frontend && bun run lint` exit 0 (no NEW lint errors; pre-existing routeTree.gen.ts warning still present per deferred-items.md)
    - `cd services/games && bunx tsc --noEmit` exit 0
    - `cd packages/contracts && bunx tsc --noEmit && bun test` exit 0
    - `git log -n 5 --oneline` shows the 5 atomic commits in order with the listed messages
    - No commit contains "Co-Authored-By", "Generated by", "Claude", or emoji (grep `git log -n 5 --format=%B | grep -i 'co-authored\\|generated by\\|claude'` returns nothing)
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; bunx tsc --noEmit &amp;&amp; bun run test &amp;&amp; bun run lint &amp;&amp; cd ../services/games &amp;&amp; bunx tsc --noEmit &amp;&amp; cd ../../packages/contracts &amp;&amp; bunx tsc --noEmit &amp;&amp; bun test &amp;&amp; cd ../.. &amp;&amp; ! git log -n 5 --format=%B | grep -i 'co-authored\|generated by\|claude\|🤖'</automated>
  </verify>
  <done>
    Five atomic commits land in repo style; all gates green; Phase 8 archive is committed and reviewable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live smoke ← real oidc-spa silent renewal | Assumption A3 (drawer/modal at __root.tsx does not break renewal) is empirically verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-32 | Denial of Service | oidc silent renewal blocked by drawer/modal (Pitfall 6) | mitigate | Task 3 step 12-14 explicitly waits past renew window with drawer open + confirms next protected fetch succeeds |
| T-08-33 | Tampering | ADR numbering collides with shipped ADR (would corrupt the audit trail) | mitigate | Task 1 re-confirms next-free at execution; renumber note in ROADMAP records the reconciliation |
| T-08-34 | Information Disclosure | Plan SUMMARYs cite the wrong implementation file | mitigate | Atomic commit-per-ADR makes each citation independently reviewable |
</threat_model>

<verification>
- All 19 live-smoke steps pass in Task 3
- All gates green in Task 4 (tsc + tests + lint across all 3 workspaces)
- ADR numbering verified next-free (Task 1)
- Planning docs internally consistent: Phase 8 row Complete in ROADMAP + Progress 8/10 in STATE + 67/95 in REQUIREMENTS
- No AI attribution in commits
</verification>

<success_criteria>
- Phase 8 is OBSERVABLY complete: drawer + verify route + replay modal all work in the running app; determinism test green; README recruiter example reproducible.
- ADR catalogue reflects four new decisions at next-free numbers; ROADMAP/STATE/REQUIREMENTS are synchronized.
- Project is ready to plan Phase 9 (Auto Features & Leaderboard) — parallelizable with the just-completed Phase 8 per ROADMAP parallelization map.
</success_criteria>

<output>
Create `.planning/phases/08-provably-fair-history-replay/08-10-SUMMARY.md` documenting: the four ADRs landed (titles + cross-references), the planning-doc deltas (Phase 8 Complete + ADR-renumber reconciliation note + 67/95), the live-smoke checkpoint results (any deviations observed at the 19 steps), and any deferred items (e.g. Pitfall 3 CPU monitoring outcome — accepted vs needs Plan 9 follow-up). Note the final commit hashes for the Phase 8 archive.
</output>
