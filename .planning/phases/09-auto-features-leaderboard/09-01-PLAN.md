---
phase: 09-auto-features-leaderboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/games/src/config/defaults.ts
  - services/games/.env.example
  - .env.example
  - frontend/src/config.ts
  - frontend/.env.example
autonomous: true
requirements:
  - REQ-AUTO-01
  - REQ-AUTO-02
  - REQ-AUTO-03
  - REQ-LEAD-01
  - REQ-LEAD-03
  - REQ-LEAD-04

must_haves:
  truths:
    - "Backend gamesEnvSchema parses 4 new env vars (LEADERBOARD_UPDATE_THROTTLE_MS, STOP_LOSS_CENTS_MAX, STOP_WIN_CENTS_MAX, AUTO_BET_MIN_TARGET_CENTI_X) with locked defaults"
    - "Frontend getConfig() exposes 5 new VITE_ vars typed (auto_bet target bounds, leaderboard size/window/refresh, rank-up transition)"
    - "No hardcoded business constant for any Phase 9 value lives outside the env layer"
  artifacts:
    - path: "services/games/src/config/defaults.ts"
      provides: "4 new env keys typed and re-exported via existing gamesEnvSchema"
      contains: "LEADERBOARD_UPDATE_THROTTLE_MS"
    - path: "services/games/.env.example"
      provides: "Per-service env example with new keys + defaults"
    - path: ".env.example"
      provides: "Root env example mirrors per-service additions"
    - path: "frontend/src/config.ts"
      provides: "Zod-parsed VITE_ env mirrors for FE"
      contains: "VITE_AUTO_BET_MIN_TARGET"
    - path: "frontend/.env.example"
      provides: "FE env example with VITE_* additions"
  key_links:
    - from: "services/games/src/application/auto-cashout-tick.service.ts (09-05 consumer)"
      to: "env.AUTO_BET_MIN_TARGET_CENTI_X via getConfig"
      via: "Zod-parsed typed import"
      pattern: "env\\.AUTO_BET_MIN_TARGET_CENTI_X"
    - from: "frontend/src/components/auto-bet-form.tsx (09-08 consumer)"
      to: "getConfig().autoBet.minTarget / maxTarget"
      via: "typed config import"
      pattern: "getConfig\\(\\)\\.autoBet"
---

<objective>
Land the 9 new Phase 9 env vars (4 backend + 5 frontend) through the existing typed-config pipelines (Zod-parsed defaults.ts + Vite getConfig). Establishes the configuration layer that every downstream Phase 9 plan reads from — no plan may hardcode a stop ceiling, leaderboard size, auto-bet target bound, throttle interval, or refresh cadence.

Purpose: CLAUDE.md §Configuration is NON-NEGOTIABLE — "No hardcoded business constants". This plan is the gate that lets all subsequent Phase 9 work pull configuration from env without re-deriving the schema.
Output: gamesEnvSchema extended with 4 keys; frontend config.ts extended with 5 keys; three .env.example files updated; existing schema tests remain green and pick up the new keys via the schema's strict mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-auto-features-leaderboard/09-CONTEXT.md
@.planning/phases/09-auto-features-leaderboard/09-RESEARCH.md
@.planning/phases/09-auto-features-leaderboard/09-UI-SPEC.md
@CLAUDE.md

# Source of truth for existing env schema patterns
@services/games/src/config/defaults.ts
@frontend/src/config.ts
@services/games/.env.example
@.env.example
@frontend/.env.example
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend backend gamesEnvSchema with 4 new Phase 9 keys</name>
  <files>services/games/src/config/defaults.ts, services/games/.env.example, .env.example</files>
  <read_first>
    - services/games/src/config/defaults.ts (study the existing gamesEnvSchema shape, the Zod coercions used for LEADERBOARD_WINDOW_HOURS / LEADERBOARD_TOP_N / AUTO_CASHOUT_MAX_X already in the schema, the typed re-export, and any env-schema unit test that asserts shape)
    - services/games/.env.example (existing variables + comment style)
    - .env.example (root mirror conventions)
    - .planning/REQUIREMENTS.md §"Open Configuration Values" (existing patterns: AUTO_CASHOUT_MAX_X=100.00, LEADERBOARD_WINDOW_HOURS=24, LEADERBOARD_TOP_N=10 are already declared and must not be redeclared)
    - .planning/phases/09-auto-features-leaderboard/09-RESEARCH.md §"User Constraints" + §"Assumptions Log" A10 (defaults locked: LEADERBOARD_UPDATE_THROTTLE_MS=0, STOP_LOSS_CENTS_MAX=100000, STOP_WIN_CENTS_MAX=100000, AUTO_BET_MIN_TARGET_CENTI_X=101)
  </read_first>
  <behavior>
    - Test: `parse({ ...validBase, LEADERBOARD_UPDATE_THROTTLE_MS: "500" })` returns `{ leaderboardUpdateThrottleMs: 500, ... }`
    - Test: `parse({ ...validBase })` (none of the 4 new keys provided) returns defaults `{ leaderboardUpdateThrottleMs: 0, stopLossCentsMax: 100000, stopWinCentsMax: 100000, autoBetMinTargetCentiX: 101 }`
    - Test: `parse({ ...validBase, AUTO_BET_MIN_TARGET_CENTI_X: "100" })` throws (must be > 100 because 1.00x is unreachable — Zod `.min(101)`)
    - Test: `parse({ ...validBase, STOP_LOSS_CENTS_MAX: "0" })` throws (`.positive()` / `.min(1)`)
    - Test: `parse({ ...validBase, LEADERBOARD_UPDATE_THROTTLE_MS: "-1" })` throws (`.nonnegative()`)
  </behavior>
  <action>
    Extend the existing Zod schema in services/games/src/config/defaults.ts by adding 4 new keys (do NOT touch the existing keys, do NOT relax any existing validation). Use the SAME coercion style the existing schema uses (z.coerce.number().int() with defaults). The 4 keys + locked defaults (per RESEARCH §Assumptions Log A10):
      - `LEADERBOARD_UPDATE_THROTTLE_MS` → number, integer, nonnegative, default `0` (the round-settle natural debounce + top-N diff gate provide the throttle; no additional cooldown needed per D-04).
      - `STOP_LOSS_CENTS_MAX` → number, integer, .positive(), default `100000` (1000.00 CRD ceiling — generous play-money cap).
      - `STOP_WIN_CENTS_MAX` → number, integer, .positive(), default `100000` (1000.00 CRD ceiling — symmetry with stop-loss).
      - `AUTO_BET_MIN_TARGET_CENTI_X` → number, integer, `.min(101)`, default `101` (1.01x minimum — below 1.00x is unreachable per UI-SPEC §Component Inventory).
    Then update the typed re-export so consumers see camelCased keys (`leaderboardUpdateThrottleMs`, `stopLossCentsMax`, `stopWinCentsMax`, `autoBetMinTargetCentiX`) matching the existing pattern.
    Update services/games/.env.example to add the 4 keys with their default values and a short one-line comment each (style match existing). Update root .env.example identically (root mirrors per-service additions).
    NO new env-schema test file is required if defaults.ts already has a colocated env-schema test — extend the existing test with the 5 behavior cases above. If no test file exists, create services/games/tests/unit/config-defaults-phase9.test.ts using the existing test framework (bun:test) and assert the 5 cases.
    Per CLAUDE.md §Coding: no emojis, no AI attribution, names self-explanatory.
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bun test tests/unit/config-defaults-phase9.test.ts</automated>
  </verify>
  <done>
    All 4 keys parsed with defaults; out-of-bound inputs throw; typed re-export consumable by downstream Phase 9 services; .env.example files contain the 4 new lines with defaults; tsc --noEmit clean for services/games.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend frontend getConfig with 5 new VITE_ keys</name>
  <files>frontend/src/config.ts, frontend/.env.example</files>
  <read_first>
    - frontend/src/config.ts (study the existing Zod-parsed VITE_ env schema, the `getConfig()` typed re-export, and how Phase 8 added VITE_REPLAY_SPEEDS / VITE_REPLAY_AUTOSTART / VITE_DRAWER_SLIDE_MS — same pattern)
    - frontend/.env.example (existing entries + comment style)
    - .planning/phases/09-auto-features-leaderboard/09-UI-SPEC.md §"Cross-References" (locks the 5 FE keys and defaults)
    - .planning/phases/09-auto-features-leaderboard/09-RESEARCH.md §"Common Pitfalls" Pitfall 4 (min target > 1.00x is hard rule)
  </read_first>
  <behavior>
    - Test: with all 5 keys set in import.meta.env, getConfig() returns `autoBet: { minTarget: 1.01, maxTarget: 100 }`, `leaderboard: { sizeN: 10, windowHours: 24, relativeRefreshMs: 5000, rankUpTransitionMs: 200 }`
    - Test: with none set, defaults apply (1.01 / 100 / 10 / 24 / 5000 / 200) — matches UI-SPEC §Cross-References
    - Test: `VITE_AUTO_BET_MIN_TARGET=1.00` parses but the schema enforces .gt(1) (planner per D-06 + UI-SPEC requirement that 1.00x is unreachable) → throws
    - Test: `VITE_LEADERBOARD_SIZE=0` throws (.positive().int())
  </behavior>
  <action>
    Extend the Zod-parsed VITE_ env schema in frontend/src/config.ts by adding (defaults from UI-SPEC §Cross-References):
      - `VITE_AUTO_BET_MIN_TARGET` → z.coerce.number().gt(1), default `1.01`
      - `VITE_AUTO_BET_MAX_TARGET` → z.coerce.number().positive(), default `100.00` (mirrors backend `AUTO_CASHOUT_MAX_X`)
      - `VITE_LEADERBOARD_SIZE` → z.coerce.number().int().positive(), default `10` (mirrors backend `LEADERBOARD_TOP_N`)
      - `VITE_LEADERBOARD_WINDOW_HOURS` → z.coerce.number().int().positive(), default `24` (mirrors backend `LEADERBOARD_WINDOW_HOURS`)
      - `VITE_LEADERBOARD_RELATIVE_REFRESH_MS` → z.coerce.number().int().nonnegative(), default `5000` (footer tick interval)
      - `VITE_RANK_UP_TRANSITION_MS` → z.coerce.number().int().nonnegative(), default `200` (sub-300ms MCP band)
    Then update the typed `getConfig()` re-export so consumers see nested namespaces: `autoBet: { minTarget, maxTarget }` and `leaderboard: { sizeN, windowHours, relativeRefreshMs, rankUpTransitionMs }`. This nesting matches the Phase 8 replay/drawer nesting precedent.
    Update frontend/.env.example to add the 6 new lines (note: 6 VITE_ keys but they roll up to 5 logical config namespaces — count by env entries) with default values and one-line comments. Match the existing comment style verbatim.
    Extend the existing frontend config test (or create frontend/src/config.phase9.test.ts using vitest) with the 4 behavior cases. Follow the Phase 8 P08-04 test pattern.
    Per CLAUDE.md §Coding + UI-SPEC §"Registry Safety": no new dependencies.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bunx vitest run src/config.phase9.test.ts</automated>
  </verify>
  <done>
    All 6 VITE_ keys parsed with defaults; 1.00x rejected; getConfig() exposes autoBet + leaderboard namespaces; .env.example carries the new lines; bunx tsc --noEmit clean for frontend; existing 169/169 FE tests remain green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env → service | env values cross from OS into the games-service via Zod schema |
| import.meta.env → browser | env values cross from build-time Vite into the FE bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | env injection of out-of-bound numeric defaults (e.g., negative throttle, zero stop ceiling) | mitigate | Zod schema enforces .positive() / .nonnegative() / .gt(1) / .min(101) at parse time; service crashes at boot rather than running with invalid config (fail-fast principle inherited from Phase 1). |
| T-09-02 | Information Disclosure | VITE_ env vars are baked into the FE bundle (Vite default) — must contain no secrets | accept | All 6 new VITE_ keys are tunables (bounds + cadences), never secrets. The shape mirrors P07/P08 conventions; no Phase 9 key carries credential material. |
| T-09-03 | Tampering | FE user manipulates VITE_AUTO_BET_MIN_TARGET in browser DevTools to set target=0.5x | mitigate | Backend Zod on PlaceBetRequestDto.autoCashoutTarget enforces `>= env.AUTO_BET_MIN_TARGET_CENTI_X / 100` and `<= env.AUTO_CASHOUT_MAX_X`. FE config is the UI hint; server is the trust boundary. (Plan 09-02 lands the server-side validation.) |
| T-09-04 | DoS | malicious operator sets STOP_LOSS_CENTS_MAX to a value so large it allows huge martingale doubling | accept | Operator-level config; play-money demo; the per-bet `BET_MAX_CENTS` (env, already in place) also caps any single POST. Phase 9 driver halts on insufficient-balance before runaway. |
</threat_model>

<verification>
- tsc --noEmit clean for services/games and frontend
- Existing services/games env-schema test green (and the 5 new Phase 9 cases pass)
- Existing frontend config tests green (and the 4 new Phase 9 cases pass)
- grep for any new hardcoded numeric default OUTSIDE config.ts / defaults.ts files: `grep -RE "100000|1\\.01x?\\b" services/games/src/application services/games/src/domain frontend/src/components frontend/src/features 2>/dev/null | grep -v test` returns no Phase 9 occurrences (the constants live ONLY in the env layer)
- .env.example diff shows exactly 4 new lines in services/games + 4 new lines in root + 6 new lines in frontend
</verification>

<success_criteria>
Downstream Phase 9 plans can `import { env } from "@/config/defaults"` (BE) or `getConfig()` (FE) and consume the 4 + 6 new keys with no need to redefine schema or invent defaults. The env layer is the single source of truth for every Phase 9 numeric parameter.
</success_criteria>

<output>
Create `.planning/phases/09-auto-features-leaderboard/09-01-SUMMARY.md` when done.
</output>
