---
phase: 09-auto-features-leaderboard
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - services/games/src/domain/leaderboard.repository.ts
  - services/games/src/domain/leaderboard-snapshot.value-object.ts
  - services/games/src/infrastructure/persistence/leaderboard-24h.entity.ts
  - services/games/src/infrastructure/repositories/mikro-leaderboard.repository.ts
  - services/games/src/infrastructure/mikro-orm/migrations/Migration20260530002-create-leaderboard-24h.ts
  - services/games/src/application/tokens.ts
  - services/games/tests/unit/leaderboard-snapshot.test.ts
  - services/games/tests/integration/leaderboard-repository.test.ts
autonomous: true
requirements:
  - REQ-LEAD-01
  - REQ-LEAD-02
  - REQ-LEAD-03

must_haves:
  truths:
    - "leaderboard_24h table exists with player_id PK + net_profit_cents BIGINT + win_count INT + total_bet_count INT + last_settled_at TIMESTAMPTZ"
    - "Partial index on (net_profit_cents DESC) WHERE last_settled_at > NOW() - INTERVAL '{N} hours' (or equivalent supported predicate) enables fast top-N query"
    - "LeaderboardRepository port + Mikro implementation: applyCashedOut, applyRefunded, applySettledLoss, fetchTopN, fetchSnapshot"
    - "LeaderboardSnapshot.diff(before, after) pure function returns { changed: boolean } based on ordered playerId arrays"
    - "Raw-SQL timestamp hydration (last_settled_at) coerced via toDate() helper (Pitfall 1)"
  artifacts:
    - path: "services/games/src/infrastructure/mikro-orm/migrations/Migration20260530002-create-leaderboard-24h.ts"
      provides: "CREATE TABLE + index + down() drop"
      contains: "leaderboard_24h"
    - path: "services/games/src/infrastructure/persistence/leaderboard-24h.entity.ts"
      provides: "MikroORM EntitySchema for leaderboard_24h"
    - path: "services/games/src/domain/leaderboard.repository.ts"
      provides: "Port: applyCashedOut, applyRefunded, applySettledLoss, fetchTopN, fetchSnapshot"
    - path: "services/games/src/infrastructure/repositories/mikro-leaderboard.repository.ts"
      provides: "Implementation backed by UPSERTs on leaderboard_24h"
    - path: "services/games/src/domain/leaderboard-snapshot.value-object.ts"
      provides: "Pure VO: LeaderboardSnapshot.diff(before, after) → { changed, before, after }"
    - path: "services/games/src/application/tokens.ts"
      provides: "LEADERBOARD_REPOSITORY symbol for DI"
  key_links:
    - from: "LeaderboardProjectorService (09-06)"
      to: "LeaderboardRepository.applyCashedOut / applyRefunded / applySettledLoss"
      via: "DI port"
      pattern: "leaderboardRepository\\.apply"
    - from: "LeaderboardController (09-07)"
      to: "LeaderboardRepository.fetchTopN"
      via: "DI port"
      pattern: "fetchTopN"
---

<objective>
Land the leaderboard read-model schema + repository port + Mikro implementation + pure-function snapshot diff VO. This is the foundation Plan 09-06 (LeaderboardProjectorService) writes to and Plan 09-07 (LeaderboardController) reads from. Per D-05 + RESEARCH §Pattern 2: denormalized `leaderboard_24h` table, on-read window filter (no cron), idempotent UPSERT writes, indexed top-N reads.

Purpose: REQ-LEAD-01..03. The light-CQRS read model is the single source of truth for top-N + net-profit semantics. The pure diff VO isolates the "did ranks change" decision from the projector's side-effect path, simplifying both testing and the round-settle throttle of `leaderboard:updated` emits (Plan 09-06).
Output: Migration + EntitySchema + Repository port/impl + Snapshot VO + their tests. ZERO consumers wired here — Plan 09-06 wires the projector, Plan 09-07 wires the controller.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-auto-features-leaderboard/09-CONTEXT.md
@.planning/phases/09-auto-features-leaderboard/09-RESEARCH.md
@CLAUDE.md

# Source-of-truth files
@services/games/src/infrastructure/persistence/bet.entity.ts
@services/games/src/infrastructure/repositories/mikro-bet.repository.ts
@services/games/src/infrastructure/mikro-orm/migrations/
@services/games/src/application/tokens.ts

# Phase 6 raw-SQL timestamp pitfall — commit 439e5b4 toDate() helper reference
# Pitfall 1 (RESEARCH §Common Pitfalls): any raw SQL RETURNING * MUST coerce TIMESTAMPTZ via toDate()
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration + EntitySchema + LeaderboardSnapshot pure VO</name>
  <files>services/games/src/infrastructure/mikro-orm/migrations/Migration20260530002-create-leaderboard-24h.ts, services/games/src/infrastructure/persistence/leaderboard-24h.entity.ts, services/games/src/domain/leaderboard-snapshot.value-object.ts, services/games/tests/unit/leaderboard-snapshot.test.ts</files>
  <read_first>
    - services/games/src/infrastructure/mikro-orm/migrations/ (latest Phase 5/6 migration shape — `up()`/`down()` with this.addSql; pick next free numeric slot after 09-02's 20260530001 — use 20260530002)
    - services/games/src/infrastructure/persistence/bet.entity.ts (EntitySchema conventions — column mapping style, branded ID columns, bigint mapping for cents)
    - .planning/phases/09-auto-features-leaderboard/09-RESEARCH.md §"Pattern 2" (schema definition: `player_id PK, net_profit_cents BIGINT, win_count INT, total_bet_count INT, last_settled_at TIMESTAMPTZ`) + §"Pitfall 8" (last_settled_at bumped ONLY on bet.cashed_out + bet.lost — NOT on bet.refunded)
    - .planning/phases/09-auto-features-leaderboard/09-CONTEXT.md (last_settled_at column name disambiguation — semantic is "last LEDGER-affecting event", refunds excluded)
  </read_first>
  <behavior>
    - Test (unit): `LeaderboardSnapshot.diff([{playerId:'A',rank:1}, {playerId:'B',rank:2}], [{playerId:'B',rank:1}, {playerId:'A',rank:2}]).changed === true` (reorder)
    - Test (unit): same input twice → `.changed === false`
    - Test (unit): `[A,B]` vs `[A,B,C]` → `.changed === true` (new entry in top-N)
    - Test (unit): `[A,B,C]` vs `[A,B]` → `.changed === true` (entry dropped from top-N)
    - Test (unit): empty before, [A] after → `.changed === true`
    - Test (unit): empty vs empty → `.changed === false`
    - Test (unit): comparison ignores `netProfit` deltas if ordering identical (rank ordering is the source of truth for "did top-N change")
  </behavior>
  <action>
    Create the migration services/games/src/infrastructure/mikro-orm/migrations/Migration20260530002-create-leaderboard-24h.ts:
      - `up()`:
        - `CREATE TABLE leaderboard_24h ( player_id UUID PRIMARY KEY, net_profit_cents BIGINT NOT NULL DEFAULT 0, win_count INT NOT NULL DEFAULT 0 CHECK (win_count >= 0), total_bet_count INT NOT NULL DEFAULT 0 CHECK (total_bet_count >= 0), last_settled_at TIMESTAMPTZ NOT NULL );`
        - `CREATE INDEX idx_leaderboard_24h_profit_desc ON leaderboard_24h (net_profit_cents DESC, player_id);` (composite includes player_id for stable ordering on ties)
      - NOTE on the partial-index option (RESEARCH §"Pattern 2"): Postgres allows the index predicate only on `IMMUTABLE` expressions, and `NOW() - INTERVAL` is NOT immutable. The plain composite (DESC, player_id) above is the correct choice; window filtering happens at query time via `WHERE last_settled_at > NOW() - INTERVAL 'N hours'`. Document this in a one-line migration comment so reviewers don't propose adding a partial WHERE that Postgres rejects.
      - `down()`: drop the index then drop the table (reverse order)
    Create the MikroORM EntitySchema services/games/src/infrastructure/persistence/leaderboard-24h.entity.ts mapping each column. Column-to-property mapping: snake_case columns → camelCase properties (`net_profit_cents` → `netProfitCents: bigint`, `last_settled_at` → `lastSettledAt: Date`). Mark `playerId` as the primary key.
    Create the pure VO services/games/src/domain/leaderboard-snapshot.value-object.ts:
      - Define `type LeaderboardEntry = { playerId: string; rank: number; netProfitCents: bigint }` (rank is 1-based)
      - Class `LeaderboardSnapshot` with a static `diff(before: LeaderboardEntry[], after: LeaderboardEntry[]): { changed: boolean; before: LeaderboardEntry[]; after: LeaderboardEntry[] }`
      - `.changed` is true iff the ordered list of `playerId`s differs (length differs OR same length but any positional inequality)
      - Per Q12 + RESEARCH recommendation: ranks-only comparison; netProfit values are NOT part of the diff (only the ordered set matters for "should we emit leaderboard:updated")
      - Pure-function, zero side effects, zero infrastructure imports
    Create services/games/tests/unit/leaderboard-snapshot.test.ts with the 7 cases above.
    Per CLAUDE.md §Money: netProfitCents is `bigint`, never number. Per CLAUDE.md §Domain layer: VO has zero infrastructure imports.
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bun test tests/unit/leaderboard-snapshot.test.ts &amp;&amp; bunx mikro-orm migration:up --dry-run 2>/dev/null || true &amp;&amp; bunx tsc --noEmit</automated>
  </verify>
  <done>
    Migration up/down clean; index visible; EntitySchema compiles + maps; LeaderboardSnapshot.diff returns correct boolean for all 7 cases; bunx tsc --noEmit clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LeaderboardRepository port + Mikro implementation + DI token</name>
  <files>services/games/src/domain/leaderboard.repository.ts, services/games/src/infrastructure/repositories/mikro-leaderboard.repository.ts, services/games/src/application/tokens.ts, services/games/tests/integration/leaderboard-repository.test.ts</files>
  <read_first>
    - services/games/src/domain/bet.repository.ts (existing port style — interface in domain/, no infrastructure imports)
    - services/games/src/infrastructure/repositories/mikro-bet.repository.ts (existing Mikro impl style — em.transactional, raw SQL where needed, toDate() coercion for timestamps per Pitfall 1)
    - services/games/src/application/tokens.ts (existing DI symbol pattern — `export const X_REPOSITORY = Symbol('X_REPOSITORY')`)
    - .planning/phases/09-auto-features-leaderboard/09-RESEARCH.md §"Pattern 2" (UPSERT semantics — Postgres `INSERT ... ON CONFLICT (player_id) DO UPDATE SET ...`), §"Pitfall 1" (raw-SQL timestamp coercion), §"Pitfall 8" (refund semantics — refunds do NOT bump last_settled_at and do NOT increment total_bet_count per Q1 recommendation)
    - .planning/phases/09-auto-features-leaderboard/09-CONTEXT.md §"Specifics" (env LEADERBOARD_WINDOW_HOURS drives the on-read filter)
  </read_first>
  <behavior>
    - Test (integration): `applyCashedOut({ playerId:'A', betAmount: Money.fromCents(1000), payout: Money.fromCents(2500), settledAt: now })` inserts a new row with net_profit_cents=1500, win_count=1, total_bet_count=1, last_settled_at=now
    - Test (integration): a SECOND applyCashedOut for the same player adds to the existing row: net_profit_cents += 1500 (UPSERT)
    - Test (integration): `applyRefunded({ playerId:'A', betAmount: Money.fromCents(500), settledAt: now })` is a NO-OP for net_profit_cents and total_bet_count (refunds excluded per Q1); last_settled_at is NOT bumped (per Pitfall 8)
    - Test (integration): `applySettledLoss({ playerId:'A', betAmount: Money.fromCents(2000), settledAt: now })` decrements net_profit_cents by 2000, increments total_bet_count by 1, bumps last_settled_at, does NOT increment win_count
    - Test (integration): `fetchTopN(10, { windowHours: 24 })` returns rows where `last_settled_at > NOW() - INTERVAL '24 hours'`, ordered by net_profit_cents DESC, capped at 10
    - Test (integration): rows older than the window are EXCLUDED from fetchTopN (insert a row with last_settled_at = 25h ago — must not appear)
    - Test (integration): `fetchSnapshot(10, { windowHours: 24 })` returns the same data shape as fetchTopN but mapped to LeaderboardEntry[] for use by the Snapshot VO
    - Test (integration): all rows returned have `lastSettledAt instanceof Date === true` (Pitfall 1 verification — raw SQL hydration coerced via toDate())
  </behavior>
  <action>
    Create the port services/games/src/domain/leaderboard.repository.ts:
      - `interface LeaderboardRepository { applyCashedOut(input: { playerId: PlayerId; betAmount: Money; payout: Money; settledAt: Date }, txEm: EntityManager): Promise<void>; applyRefunded(input: { playerId: PlayerId; betAmount: Money; settledAt: Date }, txEm: EntityManager): Promise<void>; applySettledLoss(input: { playerId: PlayerId; betAmount: Money; settledAt: Date }, txEm: EntityManager): Promise<void>; fetchTopN(size: number, opts: { windowHours: number }, txEm?: EntityManager): Promise<LeaderboardRow[]>; fetchSnapshot(size: number, opts: { windowHours: number }, txEm?: EntityManager): Promise<LeaderboardEntry[]>; }`
      - `txEm` is optional on read methods, required on write methods (projector always operates inside a TX per @IdempotentSubscribe convention).
      - NOTE: EntityManager is a Mikro TYPE — but it's structurally a port the domain consumes. To preserve domain purity, declare a minimal `TxEntityManager` interface in this file OR re-export the Mikro EntityManager type from a shared infra-types shim. Match whatever Phase 5 does for BetRepository (which is the canon).
    Create the implementation services/games/src/infrastructure/repositories/mikro-leaderboard.repository.ts:
      - `applyCashedOut`: UPSERT via raw SQL — `INSERT INTO leaderboard_24h (player_id, net_profit_cents, win_count, total_bet_count, last_settled_at) VALUES ($1, $2, 1, 1, $3) ON CONFLICT (player_id) DO UPDATE SET net_profit_cents = leaderboard_24h.net_profit_cents + EXCLUDED.net_profit_cents, win_count = leaderboard_24h.win_count + 1, total_bet_count = leaderboard_24h.total_bet_count + 1, last_settled_at = EXCLUDED.last_settled_at;`. The signed delta is `payout.toCents() - betAmount.toCents()` (positive if player won).
      - `applyRefunded`: NO-OP. Per Pitfall 8 + Q1: refunds don't touch the ledger. Just return; do NOT INSERT a zero-net row (would create a spurious player entry).
      - `applySettledLoss`: UPSERT with delta `-betAmount.toCents()` (negative for the loser), `win_count += 0`, `total_bet_count += 1`, `last_settled_at = settledAt`.
      - `fetchTopN`: parameterized SELECT `SELECT player_id, net_profit_cents, win_count, total_bet_count, last_settled_at FROM leaderboard_24h WHERE last_settled_at > NOW() - INTERVAL '{N} hours' ORDER BY net_profit_cents DESC, player_id LIMIT $1`. INJECTION SAFETY: use parameterized window — `... > NOW() - ($2 || ' hours')::INTERVAL` with `$2 = String(windowHours)`. NEVER string-interpolate windowHours directly. Map each row's `last_settled_at` via the Phase 6 `toDate()` helper before returning.
      - `fetchSnapshot`: thin wrapper over fetchTopN that flattens to `LeaderboardEntry[]` with `rank = 1 + index`.
    Extend services/games/src/application/tokens.ts: `export const LEADERBOARD_REPOSITORY = Symbol('LEADERBOARD_REPOSITORY')` — used by 09-06's projector and 09-07's controller.
    Create services/games/tests/integration/leaderboard-repository.test.ts using the Phase 5 integration test patterns (testcontainers Postgres). 8 cases listed in <behavior>. Use real Postgres; the partial-window query semantics are not unit-testable.
    Per CLAUDE.md §Money: never `number` for monetary amounts in the public API of the repository — Money VO at the boundary; bigint on the wire via Money.toCents. Per CLAUDE.md §Domain: port has zero infra decorators. Per RESEARCH §Pitfall 1: every raw-SQL TIMESTAMPTZ read goes through toDate() before exposure.
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bun test tests/integration/leaderboard-repository.test.ts</automated>
  </verify>
  <done>
    LeaderboardRepository port + Mikro impl + DI token in place; all 8 integration cases green; refunds correctly no-op; window cutoff correctly excludes old rows; timestamps hydrate as Date; bunx tsc --noEmit clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Projector input → leaderboard_24h table | event payloads from RabbitMQ (already envelope-validated by @IdempotentSubscribe in 09-06) |
| LeaderboardController query → leaderboard_24h | windowHours sourced from controller env (LEADERBOARD_WINDOW_HOURS), never from user input directly (Plan 09-07 enforces) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-14 | Tampering | SQL injection via windowHours parameter | mitigate | windowHours is parameterized via `$2 = String(windowHours)` and cast `::INTERVAL`; NOT string-concatenated. Plan 09-07's controller passes the env-bound integer; never user input. |
| T-09-15 | Tampering | Projector applies a negative win_count via double-refund logic error | mitigate | CHECK constraints in the migration: `CHECK (win_count >= 0)` + `CHECK (total_bet_count >= 0)` — Postgres rejects any UPDATE that violates. |
| T-09-16 | Information Disclosure | Top-N query leaks the FULL player_id (UUID) when called from the controller | mitigate | Plan 09-07's controller applies the existing `maskPlayerId()` helper before serializing — the masking gate is at the controller boundary, not the repository (repository deals in branded PlayerId values). |
| T-09-17 | DoS | A malicious operator sets LEADERBOARD_TOP_N to a very large value (e.g., 10000) | accept | Operator config; the index ORDER BY DESC handles the LIMIT efficiently in microseconds even for large N. WS payload sizing is the FE concern (Plan 09-07 caps via env). |
</threat_model>

<verification>
- All 8 integration cases green
- Migration up + down clean (run twice to confirm idempotency)
- CHECK constraints prevent negative counts (verify by attempting a manual UPDATE that violates)
- bunx tsc --noEmit clean for services/games
- grep for `INTERVAL` string concatenation in mikro-leaderboard.repository.ts: returns 0 matches with raw windowHours literal (parameterized only)
- grep `services/games/src/domain/leaderboard*` for `@nestjs|@mikro-orm`: returns 0 (Domain purity)
</verification>

<success_criteria>
Plan 09-06 has a working LeaderboardRepository to write through; Plan 09-07 has fetchTopN to query. The pure-function LeaderboardSnapshot.diff is unit-tested and ready to gate the leaderboard:updated emit throttle. No projector wired here — strict separation of foundation (this plan) from consumer (09-06).
</success_criteria>

<output>
Create `.planning/phases/09-auto-features-leaderboard/09-04-SUMMARY.md` when done.
</output>
