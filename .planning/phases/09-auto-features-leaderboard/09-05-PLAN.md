---
phase: 09-auto-features-leaderboard
plan: 05
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - services/games/src/application/game-events.ts
  - services/games/src/application/multiplier-broadcast.service.ts
  - services/games/src/application/auto-cashout-tick.service.ts
  - services/games/src/application/game-core.module.ts
  - services/games/tests/unit/auto-cashout-tick.spec.ts
  - services/games/tests/integration/auto-cashout-tick.test.ts
  - services/games/tests/e2e/auto-cashout-disconnect.e2e.ts
autonomous: true
requirements:
  - REQ-AUTO-01

must_haves:
  truths:
    - "MultiplierBroadcastService.fireTick() emits an in-process GAME_EVENTS.ROUND_TICK event after each volatile.emit (broadcast loop never blocks)"
    - "AutoCashoutTickService @OnEvent(ROUND_TICK, { async: true }) listens, stamps acceptedAt = new Date() as its literal first executable statement (ADR-023)"
    - "Auto-cashout invokes EXISTING CashOutUseCase with bet.autoCashoutTarget as the multiplier (NOT current tick) — player's contract honored (Pitfall 4)"
    - "SC1 disconnect E2E: socket dropped at 1.5x with target 2.0x; GET /games/bets/me confirms CASHED_OUT at 2.0x within saga timeout"
    - "Existing manual cashout flow + 30Hz tick cadence unaffected (Phase 6 ws-tick-volatile.test.ts still green)"
  artifacts:
    - path: "services/games/src/application/auto-cashout-tick.service.ts"
      provides: "AutoCashoutTickService with @OnEvent ROUND_TICK handler"
      exports: ["AutoCashoutTickService"]
    - path: "services/games/src/application/game-events.ts"
      provides: "GAME_EVENTS.ROUND_TICK constant + RoundTickPayload type"
      contains: "ROUND_TICK"
    - path: "services/games/tests/e2e/auto-cashout-disconnect.e2e.ts"
      provides: "SC1 proof — drop socket, confirm server cashes at target"
    - path: "services/games/tests/integration/auto-cashout-tick.test.ts"
      provides: "Tick handler dispatches CashOutUseCase per matching ACTIVE bet"
  key_links:
    - from: "MultiplierBroadcastService.fireTick"
      to: "EventEmitter2.emit(GAME_EVENTS.ROUND_TICK)"
      via: "in-process pub-sub"
      pattern: "eventEmitter\\.emit.*ROUND_TICK"
    - from: "AutoCashoutTickService.onTick"
      to: "BetRepository.findAutoCashoutCandidates"
      via: "Plan 09-02 indexed query"
      pattern: "findAutoCashoutCandidates"
    - from: "AutoCashoutTickService.onTick"
      to: "CashOutUseCase.execute"
      via: "existing Phase 5 use case"
      pattern: "cashOut\\.execute"
---

<objective>
Land server-enforced auto-cashout per REQ-AUTO-01 + D-06 via the locked Option B architecture from RESEARCH §"Summary": MultiplierBroadcastService emits an in-process GAME_EVENTS.ROUND_TICK event; a new AutoCashoutTickService listens and triggers the EXISTING CashOutUseCase for every ACTIVE bet whose target is crossed. The proof is the SC1 disconnect E2E — drop the WS client at 1.5x with target 2.0x and confirm the server cashes the bet at 2.0x.

Purpose: REQ-AUTO-01 + the SC1 ROADMAP success criterion are the CENTERPIECE of this phase. Server enforcement (not client-driven) means disconnects do not cost the player. The architecture preserves Phase 6 single-responsibility for MultiplierBroadcastService (volatile tick broadcast only) — the auto-cashout logic lives in a dedicated service that subscribes off the same 30Hz cadence.
Output: ROUND_TICK constant; one-line extension to fireTick; new AutoCashoutTickService with first-line acceptedAt stamp; unit + integration + E2E tests (the E2E runs against the live docker stack and IS the SC1 proof).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-auto-features-leaderboard/09-CONTEXT.md
@.planning/phases/09-auto-features-leaderboard/09-RESEARCH.md
@CLAUDE.md
@.planning/adrs/ADR-023-server-authoritative-cashout-accepted-at.md

# Source-of-truth files
@services/games/src/application/multiplier-broadcast.service.ts
@services/games/src/application/round-loop.service.ts
@services/games/src/application/use-cases/cash-out.use-case.ts
@services/games/src/application/game-events.ts
@services/games/src/application/game-core.module.ts
@services/games/src/application/tokens.ts
@services/games/src/presentation/gateways/game-ws.gateway.ts

# Existing test patterns
@services/games/tests/integration/cashout-race.property.test.ts
@services/games/tests/integration/ws-tick-volatile.test.ts
@services/games/tests/e2e/saga-recovery-sigkill.e2e.ts

# Plan 09-02 interfaces this plan consumes
@services/games/src/domain/bet.aggregate.ts (autoCashoutTarget field)
@services/games/src/domain/bet.repository.ts (findAutoCashoutCandidates port)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MultiplierBroadcastService.fireTick with in-process ROUND_TICK event</name>
  <files>services/games/src/application/game-events.ts, services/games/src/application/multiplier-broadcast.service.ts, services/games/tests/integration/multiplier-broadcast-round-tick.test.ts</files>
  <read_first>
    - services/games/src/application/multiplier-broadcast.service.ts (existing 30Hz recursive-setTimeout fireTick — full body; the volatile.emit('round:tick', ...) call; the scheduleNext() chain; the EventEmitter2 import — already present from Phase 6 because the service emits lifecycle events)
    - services/games/src/application/game-events.ts (existing constants like ROUND_STARTED, ROUND_RUNNING, ROUND_CRASHED, ROUND_SETTLED — add ROUND_TICK in the same style)
    - .planning/phases/09-auto-features-leaderboard/09-RESEARCH.md §"Pattern 1" (precise code shape — emit AFTER the volatile.emit, NEVER await emit, listener marked `{ async: true }`) + §"Pitfall 3" (broadcast loop must not block on listener)
    - services/games/tests/integration/ws-tick-volatile.test.ts (the Phase 6 cadence regression gate — must still pass after the change)
  </read_first>
  <behavior>
    - Test (integration): with an EventEmitter2 spy listener attached, fireTick triggers EXACTLY one ROUND_TICK emit per tick, with `{ roundId, multiplier: number, t: number }` payload shape
    - Test (integration): 30Hz cadence preserved — measure tick interval over 1 second of ticks with the auto-cashout listener (mock/spy) attached; mean cadence within ±10ms of 33ms (Pitfall 3)
    - Test (integration): if the listener throws, the next tick still fires (broadcast loop never blocked)
    - Test (integration): no ROUND_TICK emit when fireTick is called outside RUNNING (silent drop matches the existing try/catch around resolveRoundLoop().getMultiplierAt)
  </behavior>
  <action>
    Extend services/games/src/application/game-events.ts: add `export const ROUND_TICK = 'game.round.tick' as const` to the GAME_EVENTS object (or wherever the existing constants live; match the casing style verbatim). Add a TypeScript type export: `export interface RoundTickPayload { roundId: RoundId; multiplier: number; t: number }` (matches the WS volatile.emit payload exactly — single source of truth for the in-process emit).
    Extend services/games/src/application/multiplier-broadcast.service.ts:
      - Inside fireTick(), AFTER the existing `gateway.server.to("lobby").volatile.emit("round:tick", {...})` line, add `this.eventEmitter.emit(GAME_EVENTS.ROUND_TICK, { roundId, multiplier: multiplier.toNumber(), t: now.getTime() })`. Do NOT await this call. EventEmitter2 emit returns boolean immediately when listeners are registered with `{ async: true }`; even if a listener is sync, do not propagate its rejection (the existing try/catch around `getMultiplierAt` already isolates fireTick from listener crashes — verify the surrounding try/catch covers the new emit, otherwise add a sibling try/catch around the new line only).
      - The constructor already has `private readonly eventEmitter: EventEmitter2` from Phase 6 P6.05 — reuse, don't re-inject.
      - Single-line addition. Do NOT refactor the surrounding code shape; the cadence loop is byte-stable except for this one statement.
    Create services/games/tests/integration/multiplier-broadcast-round-tick.test.ts using the Phase 6 ws-tick-volatile.test.ts pattern (bootstrap a Nest test module with the broadcast service + a real EventEmitter2). 4 cases listed in <behavior>. Use bun:test.
    Per CLAUDE.md §WebSocket: ticks remain volatile.emit; the new in-process emit is parallel, never serial. Per RESEARCH §"Don't Hand-Roll": use @nestjs/event-emitter (already in spine).
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bun test tests/integration/multiplier-broadcast-round-tick.test.ts tests/integration/ws-tick-volatile.test.ts</automated>
  </verify>
  <done>
    fireTick emits ROUND_TICK in-process; cadence preserved; existing ws-tick-volatile.test.ts still green (no regression); all 4 new cases pass; bunx tsc --noEmit clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AutoCashoutTickService — first-line acceptedAt + target-honoring CashOutUseCase invocation</name>
  <files>services/games/src/application/auto-cashout-tick.service.ts, services/games/src/application/game-core.module.ts, services/games/tests/unit/auto-cashout-tick.spec.ts, services/games/tests/integration/auto-cashout-tick.test.ts</files>
  <read_first>
    - services/games/src/application/handlers/wallet-debited.handler.ts (canonical @OnEvent handler shape — Phase 6 precedent; the `{ async: true }` decorator option)
    - services/games/src/application/use-cases/cash-out.use-case.ts (CashOutUseCase.execute input shape — playerId, multiplier (Multiplier VO), acceptedAt (Date); error contract — throws RoundNotRunningError | BetNotCashableError on race; the existing `acceptedAt = new Date()` first-line invariant per ADR-023)
    - services/games/src/application/game-core.module.ts (existing provider registrations — add AutoCashoutTickService here)
    - services/games/src/application/tokens.ts (BET_REPOSITORY symbol — DI for the new repository method)
    - .planning/phases/09-auto-features-leaderboard/09-RESEARCH.md §"Pattern 1" (FULL code shape for AutoCashoutTickService — acceptedAt as literal first statement, ceilingCentiX = Math.floor(payload.multiplier * 100), loop over candidates invoking cashOut.execute({ playerId, multiplier: bet.autoCashoutTarget, acceptedAt }), try/catch swallows RoundNotRunningError + BetNotCashableError per Pitfall 11), §"Pitfall 2" (acceptedAt MUST be first executable statement, NOT after the query), §"Pitfall 4" (pay at the player's target, NOT the current tick multiplier), §"Pitfall 11" (CRASHED-round race is already handled by CashOutUseCase's RUNNING guard — swallow the error)
    - .planning/adrs/ADR-023-server-authoritative-cashout-accepted-at.md (the invariant generalization that Phase 9 ratifies)
  </read_first>
  <behavior>
    - Test (unit, source inspection): the FIRST executable line of `onTick` is `const acceptedAt = new Date();` — assert by reading the source file string and checking the regex `/async\s+onTick[^{]*\{\s*const\s+acceptedAt\s*=\s*new\s+Date\(\)/` matches (mirrors Phase 5 P5.06 acceptedAt position check)
    - Test (unit): given a bet with `autoCashoutTarget = Multiplier.fromCentiX(200)` and a tick payload `multiplier = 2.05` → CashOutUseCase.execute called with `multiplier === bet.autoCashoutTarget` (NOT 2.05 — Pitfall 4)
    - Test (unit): findAutoCashoutCandidates called with `ceilingCentiX = Math.floor(2.05 * 100) = 205`
    - Test (unit): when CashOutUseCase.execute throws RoundNotRunningError → caught silently, no rethrow (Pitfall 11)
    - Test (unit): when CashOutUseCase.execute throws BetNotCashableError → caught silently
    - Test (unit): when CashOutUseCase.execute throws an UNEXPECTED error (e.g., RangeError) → logged at error level (do NOT silently swallow unknown failures); per CLAUDE.md §"Saga / messaging": observability matters
    - Test (integration): place an ACTIVE bet with target=2.0x; emit a ROUND_TICK with multiplier=1.99 → no cashout. Emit ROUND_TICK with multiplier=2.00 → bet transitions to CASHED_OUT with cashedOutMultiplier=2.00 (NOT 1.99, NOT 2.00001 — Pitfall 9)
    - Test (integration): emit ROUND_TICK with multiplier=2.50 with three ACTIVE bets targeting 2.00, 2.25, 2.75 → first two cash, third stays ACTIVE
    - Test (integration): emit ROUND_TICK twice in quick succession for the same crossing bet → only one CASHED_OUT (CashOutUseCase tryTransition is FSM-guarded, second invocation is a no-op per Pitfall 11)
  </behavior>
  <action>
    Create services/games/src/application/auto-cashout-tick.service.ts following RESEARCH §"Pattern 1" verbatim:
      - `@Injectable() export class AutoCashoutTickService`
      - Constructor: `@Inject(BET_REPOSITORY) private readonly bets: BetRepository, private readonly cashOut: CashOutUseCase, private readonly logger = new Logger(AutoCashoutTickService.name)`
      - `@OnEvent(GAME_EVENTS.ROUND_TICK, { async: true })` decorated `async onTick(payload: RoundTickPayload): Promise<void>`
      - LITERAL FIRST STATEMENT: `const acceptedAt = new Date();` (ADR-023 — code review gate + source-inspection unit test)
      - SECOND statement: `const ceilingCentiX = Math.floor(payload.multiplier * 100);`
      - Then: `const candidates = await this.bets.findAutoCashoutCandidates(payload.roundId, ceilingCentiX);`
      - Loop: for each bet, `try { await this.cashOut.execute({ playerId: bet.playerId, multiplier: bet.autoCashoutTarget!, acceptedAt }); } catch (err) { if (err instanceof RoundNotRunningError || err instanceof BetNotCashableError) { /* race — swallow */ } else { this.logger.error('Unexpected auto-cashout error', err); } }`. Honor the player's target, NOT payload.multiplier (Pitfall 4).
    Register the service in services/games/src/application/game-core.module.ts: add `AutoCashoutTickService` to providers; nothing else changes (the @OnEvent decorator self-registers via @nestjs/event-emitter — no other wiring required).
    Create services/games/tests/unit/auto-cashout-tick.spec.ts with the 6 unit cases above. The source-inspection test reads `services/games/src/application/auto-cashout-tick.service.ts` as a string at test time and runs the regex; this is the same gate Phase 5 used to lock controller-line-69 acceptedAt.
    Create services/games/tests/integration/auto-cashout-tick.test.ts with the 3 integration cases above. Bootstrap a Nest test module with real EventEmitter2 + real BetRepository (testcontainers Postgres) + real CashOutUseCase. Place bets via the PlaceBetUseCase to ensure the autoCashoutTarget column wiring works end-to-end with Plan 09-02.
    Per CLAUDE.md §WebSocket: server-authoritative timestamp before any await — STRICT. Per CLAUDE.md §Domain: AutoCashoutTickService is application layer, NOT domain (it depends on CashOutUseCase). Per RESEARCH §Pitfall 5: do not introduce balance checks here — the existing CashOutUseCase semantics apply.
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bun test tests/unit/auto-cashout-tick.spec.ts tests/integration/auto-cashout-tick.test.ts</automated>
  </verify>
  <done>
    AutoCashoutTickService source has acceptedAt as first line (regex test green); target is honored not current tick; race errors swallowed; integration test proves auto-cashout fires only when target crossed; no double-cashout on concurrent tick floods; all 9 cases green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SC1 disconnect E2E proof (live docker stack)</name>
  <files>services/games/tests/e2e/auto-cashout-disconnect.e2e.ts</files>
  <read_first>
    - services/games/tests/integration/ws-rooms.test.ts (Phase 6 socket.io-client connection pattern — JWT bearer in auth header, lobby join)
    - services/games/tests/integration/cashout-race.property.test.ts (cashout race assertion patterns; how it asserts a 200 vs 409 outcome)
    - services/games/tests/e2e/saga-recovery-sigkill.e2e.ts (Phase 5 e2e test that runs against `docker compose up` stack; bootstrap shape — how it gets a player JWT, places a bet, polls /games/bets/me)
    - .planning/phases/09-auto-features-leaderboard/09-RESEARCH.md §"Disconnect-Safety E2E (SC1)" + §"Validation Architecture" (the test IS the SC1 proof — must run against the LIVE docker stack, not testcontainers) + §"Don't Hand-Roll" — disconnect-safety SC1 row (Bun test + socket.io-client + force-close + poll GET /games/bets/me until CASHED_OUT)
  </read_first>
  <behavior>
    - Test (e2e): Bootstrap — acquire a player JWT (`player/player123` via Keycloak; reuse the existing Phase 5 helper if present)
    - Test (e2e): Connect socket.io-client to `ws://localhost:4101/ws` (or the env-driven WS URL) with the JWT in handshake auth
    - Test (e2e): Wait for `round:started` event (BETTING phase begins)
    - Test (e2e): POST /games/bet with `{ amountCents: '1000', autoCashoutTarget: 2.0 }` via the existing /games/bet endpoint (through Kong at :8000); assert 202 response with betId
    - Test (e2e): Wait for `round:running` event (BETTING ends, RUNNING begins)
    - Test (e2e): When the first round:tick reports `multiplier >= 1.5` → `socket.disconnect()` (force-close client)
    - Test (e2e): Poll `GET /games/bets/me` (HTTP, fresh request — no socket) every 500ms for up to 20 seconds; assert the placed bet appears with `status === 'CASHED_OUT'` and `cashedOutMultiplier === 2.00` (the target, NOT a higher value — Pitfall 4)
    - Test (e2e): Edge case — if the round crashes BEFORE reaching 2.0x (rare in deterministic test rounds but possible if seed produces a sub-2.0x crash), assert status === 'LOST' AND log this as a flaky-seed warning; the test should re-run against a different round (loop or retry). Document this in a test comment; the test is REQ-AUTO-01's centerpiece proof and must be robust.
  </behavior>
  <action>
    Create services/games/tests/e2e/auto-cashout-disconnect.e2e.ts following the Phase 5 saga-recovery-sigkill.e2e.ts pattern:
      - Test relies on `bun run docker:up` being live (skipped if not — use existing `requireDockerUp()` helper or equivalent guard from Phase 5)
      - Import socket.io-client; import fetch (Bun global); import the existing test-helper that mints a player JWT
      - Implement the 8-step flow above; assert at each step
      - For seed-deterministic round selection: if the project supports a TEST_SEED env override, use it to force a round that reaches 2.0x. Otherwise, loop up to 3 round cycles and skip rounds that crash below 2.0x (logging the crash multiplier in the test output so flakiness is visible).
      - Timeouts: BETTING is 5s, RUNNING is variable; poll budget 20s; total test budget 60s. Use bun:test's `test.timeout` if available.
    Per RESEARCH §"Validation Architecture": this test IS REQ-AUTO-01's SC1 proof + the centerpiece of the phase verify. It MUST live in Phase 9 (not deferred to Phase 10). Per CLAUDE.md §Tests: bun test runner.
    Document in a header comment: "SC1 disconnect-safety E2E — proves server-enforced auto-cashout per REQ-AUTO-01 + ROADMAP §Phase 9 success criterion 1. Drops the WS client at multiplier=1.5x with target=2.0x; asserts CASHED_OUT at 2.0x via REST poll. Requires `bun run docker:up`."
  </action>
  <verify>
    <automated>cd services/games &amp;&amp; bun test tests/e2e/auto-cashout-disconnect.e2e.ts</automated>
  </verify>
  <done>
    E2E test executes against the live stack; bet placed with target=2.0x cashes at 2.0x server-side after client disconnect at 1.5x; test passes in at most 3 round cycles; verbose log shows the disconnect timing + the final bet status from /games/bets/me; this is the recorded REQ-AUTO-01 proof for the phase verify.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 30Hz tick → AutoCashoutTickService | in-process event; broadcast loop must not block on listener |
| AutoCashoutTickService → CashOutUseCase | trusted internal call; acceptedAt server-stamped per ADR-023 |
| WS client → server | client disconnect must NOT affect server-side cashout enforcement (SC1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-20 | Tampering | acceptedAt drifts past tick instant by query latency | mitigate | ADR-023 invariant: acceptedAt = new Date() as LITERAL first executable statement; source-inspection unit test asserts position via regex (mirrors Phase 5 P5.06 controller-line-69 gate). |
| T-09-21 | Tampering | Auto-cashout pays at current tick multiplier (e.g., 2.05x) instead of player target (2.00x) — exploits the gap | mitigate | RESEARCH §Pitfall 4: pass `bet.autoCashoutTarget` to CashOutUseCase, NOT `payload.multiplier`. Unit test verifies the multiplier passed equals the target. |
| T-09-22 | DoS | EventEmitter2.emit blocks broadcast loop under listener await | mitigate | RESEARCH §Pitfall 3: @OnEvent({ async: true }); never await emit. Integration test verifies 30Hz cadence preserved with listener active. |
| T-09-23 | TOCTOU | Tick fires for round R, then R crashes between tick and CashOutUseCase invocation | mitigate | CashOutUseCase already does `if (open.status !== 'RUNNING') throw RoundNotRunningError` inside its TX (Pitfall 11). AutoCashoutTickService swallows the error — no harm. |
| T-09-24 | Repudiation | Player disputes auto-cashout server timestamp | mitigate | acceptedAt is server clock at the tick handler entry; same race-resolution authority as manual cashout per ADR-023. Logged with bet correlation context. |
| T-09-25 | Information Disclosure | E2E test leaks player credentials to test log | accept | E2E uses the seeded demo user `player/player123`; no PII exposure beyond what Phase 1 README already documents publicly. |
</threat_model>

<verification>
- All unit tests green (6 cases + source-inspection gate)
- All integration tests green (4 multiplier-broadcast cases + 3 auto-cashout-tick cases)
- E2E test green against live docker stack (this IS REQ-AUTO-01's SC1 proof)
- Phase 6 ws-tick-volatile.test.ts STILL GREEN (no cadence regression)
- Phase 5 cashout-race.property.test.ts STILL GREEN (no race regression)
- bunx tsc --noEmit clean for services/games
- grep `services/games/src/application/auto-cashout-tick.service.ts` for `acceptedAt`: first match line is `const acceptedAt = new Date();` inside onTick (visual code review)
</verification>

<success_criteria>
REQ-AUTO-01 closed via server-enforced auto-cashout. SC1 proof — the disconnect E2E test — runs green against the live docker stack as the recorded artifact for ROADMAP Phase 9 success criterion 1. ADR-023's race-resolution invariant generalizes to the tick-loop entry path. Plans 09-06 + 09-07 are independent — they do not depend on this plan's outputs.
</success_criteria>

<output>
Create `.planning/phases/09-auto-features-leaderboard/09-05-SUMMARY.md` when done.
</output>
