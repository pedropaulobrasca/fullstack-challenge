---
phase: 09-auto-features-leaderboard
plan: 08
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - frontend/src/components/ui/radio-group.tsx
  - frontend/src/features/auto-bet/strategy.ts
  - frontend/src/features/auto-bet/strategy.test.ts
  - frontend/src/features/auto-bet/auto-bet.store.ts
  - frontend/src/features/auto-bet/auto-bet.store.test.ts
  - frontend/src/features/auto-bet/auto-bet-driver.ts
  - frontend/src/features/auto-bet/driver-stops.test.ts
  - frontend/src/components/auto-bet-form.tsx
  - frontend/src/components/auto-bet-form.test.tsx
  - frontend/src/components/auto-bet-session-panel.tsx
  - frontend/src/components/manual-tab-content.tsx
  - frontend/src/components/bet-panel.tsx
  - frontend/src/components/bet-panel.test.tsx
  - frontend/src/routes/__root.tsx
  - frontend/src/stores/ws-dispatch.ts
autonomous: false
requirements:
  - REQ-AUTO-02
  - REQ-AUTO-03
  - REQ-AUTO-04
  - REQ-AUTO-05

must_haves:
  truths:
    - "BetPanel is a shadcn Tabs with defaultValue='manual' + Manual tab + Auto tab (per D-03)"
    - "Auto tab form: target multiplier, strategy radio (fixed | martingale), base bet (Money VO), stop-loss (Money VO), stop-win (Money VO), Start/Stop button"
    - "Auto-bet store is per-session (NO persist middleware) — survives navigation but dies on reload per REQ-AUTO-04"
    - "Strategy.nextBetAmount(strategy, base, lastOutcome): Martingale base is CONFIGURED initial bet (NOT previous bet) — RE-CONFIRMED PER REQ-AUTO-02 + CONTEXT §Specifics"
    - "Driver subscribes to round:started + bet:my_cashed_out + bet:my_refunded; POSTs next bet on round:started (per Q3 recommendation, avoids 409 race vs round:settled)"
    - "Driver halts on stop-loss, stop-win, or insufficient-balance with amber deduped toast (NOT red — red is reserved for crash per UI-SPEC inherited D-02)"
    - "Manual tab disabled (opacity-50, inputs disabled, inline Lock Alert) while auto-bet running per D-03"
    - "Driver POSTs use autoCashoutTarget from store config (server-enforced per 09-05)"
  artifacts:
    - path: "frontend/src/features/auto-bet/strategy.ts"
      provides: "nextBetAmount(strategy, baseAmount: Money, lastOutcome): Money"
      contains: "nextBetAmount"
    - path: "frontend/src/features/auto-bet/auto-bet.store.ts"
      provides: "Zustand slice: isRunning, config, sessionPL: Money, lastBetAmount, lastOutcome, roundCount, halted, start/stop/recordOutcome actions"
      contains: "create"
    - path: "frontend/src/features/auto-bet/auto-bet-driver.ts"
      provides: "useAutoBetDriver hook mounted once in __root.tsx"
    - path: "frontend/src/components/bet-panel.tsx"
      provides: "shadcn Tabs Manual | Auto refactor"
    - path: "frontend/src/components/auto-bet-form.tsx"
      provides: "5-field form + Start/Stop + session panel"
    - path: "frontend/src/components/ui/radio-group.tsx"
      provides: "shadcn radio-group primitive (new install)"
  key_links:
    - from: "AutoBetForm Start button"
      to: "auto-bet.store start()"
      via: "Zustand action"
      pattern: "useAutoBetStore.*start"
    - from: "useAutoBetDriver hook"
      to: "ws-dispatch round:started subscription"
      via: "WS dispatcher subscription"
      pattern: "round:started"
    - from: "Driver POST /games/bet"
      to: "Plan 09-02 autoCashoutTarget field"
      via: "PlaceBetRequestDto"
      pattern: "autoCashoutTarget"
---

<objective>
Land the frontend Auto-bet stack per REQ-AUTO-02..05 + D-01 + D-03: shadcn Tabs (Manual | Auto) on BetPanel + the 5-field Auto form + the per-session Zustand store + the driver hook subscribed to WS events + the cumulative-P/L stop enforcement. The Martingale base discipline (base = configured initial, NOT previous bet) is a hard contract per REQ-AUTO-02 + CONTEXT §Specifics — locked at acceptance.

> **Scope note (borderline — 15 files, accepted as-is per checker):** Three logical units are bundled in this plan because each is too small to justify a dedicated plan and they share the same `frontend/src/components/bet-panel.tsx` consumer file. Task 1 owns the store + strategy unit (4 files: pure-function + Zustand + 2 tests). Task 2 owns the driver + WS dispatcher integration unit (4 files: driver + driver test + ws-dispatch extension + __root mount). Task 3 owns the form + tabbed panel UI install unit (6 files: radio-group primitive + auto-bet-form + session panel + manual-tab-content wrapper + bet-panel refactor + 2 form/panel tests). Splitting further would either fragment file ownership (e.g., Task 3's bet-panel refactor cannot land before manual-tab-content + auto-bet-form exist) or force a Wave 0 scaffold step that is not warranted at this size. Context cost target ~50% holds because each task is touching exclusive files within its unit.

Purpose: REQ-AUTO-02..05. The store is the per-session Zustand slice with NO persist (REQ-AUTO-04). The driver fires POSTs on `round:started` (Q3 recommendation to avoid `409 ROUND_NOT_IN_BETTING_PHASE` race) using the `autoCashoutTarget` field landed by 09-02. The Manual tab disabled-while-running treatment + the Stop button border-only destructive treatment + the Session P/L Mono Data display all follow UI-SPEC §Surface A verbatim.
Output: Auto-bet form + driver + store + strategy with full test coverage; tabbed BetPanel preserves the existing Manual flow byte-unchanged; useAutoBetDriver hook mounted in __root.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-auto-features-leaderboard/09-CONTEXT.md
@.planning/phases/09-auto-features-leaderboard/09-RESEARCH.md
@.planning/phases/09-auto-features-leaderboard/09-UI-SPEC.md
@CLAUDE.md

# Source-of-truth files
@frontend/src/components/bet-panel.tsx
@frontend/src/components/cashout-button.tsx
@frontend/src/features/bet/use-place-bet.ts
@frontend/src/features/bet/parse-bet-amount.ts
@frontend/src/stores/bet.store.ts
@frontend/src/stores/round.store.ts
@frontend/src/stores/wallet.store.ts
@frontend/src/stores/ws-dispatch.ts
@frontend/src/components/ui/tabs.tsx
@frontend/src/components/ui/alert.tsx
@frontend/src/components/ui/tooltip.tsx
@frontend/src/lib/toast.ts
@frontend/src/routes/__root.tsx
@frontend/src/config.ts

# Plan 09-01 interfaces this plan consumes
# getConfig().autoBet.minTarget / maxTarget, env-bound

# Plan 09-02 interfaces this plan consumes
# PlaceBetRequestDto.autoCashoutTarget (optional number, multiplier coefficient)
@services/games/src/presentation/dtos/place-bet.request.dto.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: shadcn radio-group install (legitimacy gate)</name>
  <what-built>
    Single shadcn component install via the official registry: `bunx shadcn@latest add radio-group`. Scaffolds frontend/src/components/ui/radio-group.tsx (Radix-backed). No npm package added directly — the transitive dep @radix-ui/react-radio-group lands automatically.
  </what-built>
  <how-to-verify>
    1. Confirm at https://ui.shadcn.com/docs/components/radio-group that radio-group is the official shadcn block (legitimacy gate per RESEARCH §Package Legitimacy Audit).
    2. Confirm at https://www.npmjs.com/package/@radix-ui/react-radio-group that the transitive Radix primitive is the canonical Radix package (same provenance as @radix-ui/react-tabs, @radix-ui/react-dialog already in the project).
    3. After install, verify frontend/src/components/ui/radio-group.tsx exists and exports RadioGroup + RadioGroupItem. Run `bunx tsc --noEmit` from frontend/ — clean.
    4. Run the existing 169/169 FE test suite — green, no regression from the install.
  </how-to-verify>
  <resume-signal>Type "approved" after legitimacy verified and tests green.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Auto-bet store + strategy pure function</name>
  <files>frontend/src/features/auto-bet/strategy.ts, frontend/src/features/auto-bet/strategy.test.ts, frontend/src/features/auto-bet/auto-bet.store.ts, frontend/src/features/auto-bet/auto-bet.store.test.ts</files>
  <read_first>
    - frontend/src/stores/bet.store.ts (Phase 7 P07-04 Zustand slice-per-concern pattern — NO persist middleware, simple `create<T>((set) => ({...}))` shape)
    - frontend/src/stores/round.store.ts (slice-per-concern reference)
    - packages/shared-kernel/src/money/money.ts (Money VO API — `Money.fromCents(bigint)`, `Money.fromSnapshot(MoneySnapshot)`, `.toCents(): bigint`, `.add(Money): Money`, `.subtract(Money): Money`, `.multiplyRounded({ numerator, denominator }): Money`)
    - .planning/phases/09-auto-features-leaderboard/09-RESEARCH.md §"Pattern 3" (strategy.ts shape) + §"Anti-Patterns" (Martingale base = configured initial, NOT previous bet — REQ-AUTO-02 + CONTEXT §Specifics) + §"Pitfall 10" (driver state syncs from server events, not optimistic FE) + §"Common Pitfalls" Pitfall 5 (martingale doubling overflows wallet — driver checks before POST)
    - .planning/phases/09-auto-features-leaderboard/09-UI-SPEC.md §"Cross-References" (auto-bet.store state shape)
  </read_first>
  <behavior>
    - Test (strategy, fixed): `nextBetAmount('fixed', Money.fromCents(1000n), null) === Money.fromCents(1000n)` (no last outcome → base)
    - Test (strategy, fixed): `nextBetAmount('fixed', Money.fromCents(1000n), 'win') === Money.fromCents(1000n)`
    - Test (strategy, fixed): `nextBetAmount('fixed', Money.fromCents(1000n), 'loss') === Money.fromCents(1000n)` (fixed never changes amount)
    - Test (strategy, martingale, win): `nextBetAmount('martingale', Money.fromCents(1000n), 'win', { lastBet: Money.fromCents(8000n) }) === Money.fromCents(1000n)` — reset to BASE (not previous bet) per REQ-AUTO-02 + CONTEXT §Specifics + Anti-Patterns
    - Test (strategy, martingale, loss): `nextBetAmount('martingale', Money.fromCents(1000n), 'loss', { lastBet: Money.fromCents(2000n) }) === Money.fromCents(4000n)` — double the previous bet
    - Test (strategy, martingale, first round): `nextBetAmount('martingale', Money.fromCents(1000n), null, { lastBet: null }) === Money.fromCents(1000n)` — no previous outcome → base
    - Test (store, no persist): no persist middleware imported; verify by reading the store source and grep for `persist` — returns 0 (REQ-AUTO-04)
    - Test (store, start/stop): `store.start(config)` sets `isRunning=true, config, sessionPL=Money.fromCents(0n), roundCount=0, halted=null`
    - Test (store, recordOutcome win): `store.recordOutcome('win', betAmount=Money.fromCents(1000n), payout=Money.fromCents(2500n))` adds delta +1500 to sessionPL, increments roundCount, sets lastBetAmount + lastOutcome='win'
    - Test (store, recordOutcome loss): `store.recordOutcome('loss', betAmount=Money.fromCents(1000n), payout=Money.fromCents(0n))` subtracts 1000 from sessionPL, increments roundCount, sets lastOutcome='loss'
    - Test (store, recordOutcome refund): `store.recordOutcome('refund', betAmount=Money.fromCents(1000n), ...)` is a NO-OP for sessionPL + roundCount (refund is a cancelled bet, not a played round per Pitfall 8 + Q1)
    - Test (store, halt): `store.halt({ reason: 'stop-win' })` sets isRunning=false, halted={reason:'stop-win'}; subsequent recordOutcome is ignored
  </behavior>
  <action>
    Create frontend/src/features/auto-bet/strategy.ts:
      - Pure function `nextBetAmount(strategy: 'fixed' | 'martingale', baseAmount: Money, lastOutcome: 'win' | 'loss' | null, ctx?: { lastBet: Money | null }): Money`
      - `if (strategy === 'fixed') return baseAmount;`
      - `if (lastOutcome === 'loss' && ctx?.lastBet) return ctx.lastBet.multiplyRounded({ numerator: 2n, denominator: 1n });` // double on loss
      - `return baseAmount;` // win or first-round → reset to BASE (REQ-AUTO-02)
      - No side effects, no infrastructure imports, fully unit-testable.
    Create frontend/src/features/auto-bet/auto-bet.store.ts (Zustand slice, NO persist):
      - Types per UI-SPEC §Cross-References:
        - `interface AutoBetConfig { target: number; strategy: 'fixed' | 'martingale'; baseAmount: Money; stopLoss: Money; stopWin: Money }`
        - `interface AutoBetState { isRunning: boolean; config: AutoBetConfig | null; sessionPL: Money; lastBetAmount: Money | null; lastOutcome: 'win' | 'loss' | null; roundCount: number; halted: { reason: 'user' | 'stop-loss' | 'stop-win' | 'insufficient-balance' } | null; start: (config: AutoBetConfig) => void; stop: (haltReason?: AutoBetState['halted']) => void; recordOutcome: (outcome: 'win' | 'loss' | 'refund', betAmount: Money, payout: Money) => void; recordPostedBet: (amount: Money) => void; }`
      - `export const useAutoBetStore = create<AutoBetState>((set, get) => ({ ... }))` — no persist, no localStorage.
      - `recordOutcome` uses Money VO arithmetic: `sessionPL.add(payout).subtract(betAmount)` for win, `sessionPL.subtract(betAmount)` for loss, no-op for refund. Money — never `number`.
      - `stop({ reason })` sets isRunning=false + halted. `recordOutcome` early-returns if `!isRunning` so post-halt events don't corrupt state.
    Create frontend/src/features/auto-bet/strategy.test.ts + frontend/src/features/auto-bet/auto-bet.store.test.ts (vitest + jsdom; mirror Phase 7 P07-04 store test patterns). All 12 cases above.
    Per CLAUDE.md §Money: every monetary field is Money VO, never number. The ESLint custom rule blocks `number` on amount-like fields — verify lint clean. Per RESEARCH §Pitfall 10: store state is hydrated from server events only (no optimistic state).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bunx vitest run src/features/auto-bet/strategy.test.ts src/features/auto-bet/auto-bet.store.test.ts</automated>
  </verify>
  <done>
    12 cases green; Martingale base discipline locked + tested; store has no persist (grep gate); store actions use Money VO throughout; bunx tsc --noEmit + bun run lint clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-bet driver hook + stops + ws-dispatch subscriptions</name>
  <files>frontend/src/features/auto-bet/auto-bet-driver.ts, frontend/src/features/auto-bet/driver-stops.test.ts, frontend/src/stores/ws-dispatch.ts, frontend/src/routes/__root.tsx</files>
  <read_first>
    - frontend/src/stores/ws-dispatch.ts (existing WS dispatcher — how it subscribes to events; the auto-bet driver subscribes via the same surface)
    - frontend/src/features/bet/use-place-bet.ts (Phase 7 PlaceBet mutation hook — TanStack Query mutation; reuse — the driver fires the SAME mutation, just with autoCashoutTarget populated)
    - frontend/src/stores/wallet.store.ts (balance source — driver checks balance before POST)
    - frontend/src/stores/round.store.ts (round phase source — driver checks BETTING before POST as defense in depth)
    - frontend/src/lib/toast.ts (dedupedToast surface — Phase 7 amber warning toasts; the driver uses this for halt reasons)
    - frontend/src/routes/__root.tsx (where to mount the driver hook — alongside existing dispatcher subscriptions)
    - .planning/phases/09-auto-features-leaderboard/09-RESEARCH.md §"Pattern 3" (driver shape) + §"Pitfall 5" (martingale wallet overflow — halt with insufficient-balance reason) + §"Pitfall 10" (driver state syncs from server events) + §"Open Q3" + §"Recommendation" (POST on round:started not round:settled — avoids 409 race) + §"Pitfall 12" alt (driver POST rejection — partial unique index handles double-POSTs; driver treats 23505 as "next round, don't halt")
    - .planning/phases/09-auto-features-leaderboard/09-UI-SPEC.md §"Copywriting Contract" (toast copy: "Stop-win reached. Auto-bet halted at +X.XX CRD." amber, etc.)
    - .planning/phases/09-auto-features-leaderboard/09-CONTEXT.md §D-01 (cumulative-since-Start P/L; FE-driven; backend stateless)
  </read_first>
  <behavior>
    - Test (driver, on round:started while running): driver computes nextBet via strategy + checks stops + balance + POSTs PlaceBet mutation with `{ amountCents, autoCashoutTarget: config.target }`
    - Test (driver, on round:started while NOT running): no POST fired (driver idle)
    - Test (driver, on bet:my_cashed_out): calls store.recordOutcome('win', betAmount, payout); driver does NOT POST here (POST happens on next round:started)
    - Test (driver, on bet:my_refunded): calls store.recordOutcome('refund', betAmount, Money.zero) — refunds don't extend session P/L per Q1
    - Test (driver, on round:started, sessionPL >= stopWin): driver halts via store.halt({reason:'stop-win'}) + dedupedToast amber "Stop-win reached..."; NO POST fired
    - Test (driver, on round:started, sessionPL <= -stopLoss): driver halts via store.halt({reason:'stop-loss'}) + dedupedToast amber "Stop-loss reached..."; NO POST fired
    - Test (driver, on round:started, nextBet > wallet.balance): driver halts via store.halt({reason:'insufficient-balance'}) + dedupedToast amber "Auto-bet halted: insufficient balance..."; NO POST fired (Pitfall 5)
    - Test (driver, on round:started, nextBet > env BET_MAX_CENTS via getConfig().bet.maxCents): halts with insufficient-balance reason (Pitfall 5 alternative) OR a dedicated "bet-cap-overflow" reason — pick one consistent code; UI-SPEC §Copywriting allows the insufficient-balance message to cover this case
    - Test (driver, POST 409 ROUND_NOT_IN_BETTING_PHASE): does NOT halt; treats as a transient race; the next round:started triggers a new attempt (Pitfall 10 + Q3 — POSTing on round:started should prevent this but defense in depth)
    - Test (driver, POST 23505 partial-unique-index race): does NOT halt; treats as next-round-already (Pitfall 10)
    - Test (driver, POST 402 insufficient balance from server): halts with insufficient-balance reason (Pitfall 5 alternate path)
  </behavior>
  <action>
    Create frontend/src/features/auto-bet/auto-bet-driver.ts:
      - `export function useAutoBetDriver(): void`
      - Subscribes to the ws-dispatch event surface (or whatever Phase 7 exposes) for `round:started`, `bet:my_cashed_out`, `bet:my_refunded`.
      - On `bet:my_cashed_out`: `useAutoBetStore.getState().recordOutcome('win', Money.fromSnapshot(payload.amount), Money.fromSnapshot(payload.payout))`.
      - On `bet:my_refunded`: `useAutoBetStore.getState().recordOutcome('refund', Money.fromSnapshot(payload.amount), Money.fromCents(0n))`.
      - On `round:started`:
        1. `const state = useAutoBetStore.getState(); if (!state.isRunning || !state.config) return;`
        2. Check stops: `if (state.sessionPL.toCents() >= state.config.stopWin.toCents()) { state.stop({reason:'stop-win'}); dedupedToast.amber('Stop-win reached. Auto-bet halted at +' + state.sessionPL.toFormat() + ' CRD.'); return; }`. Same shape for stop-loss (sessionPL <= -stopLoss). UI-SPEC §Copywriting locks the exact copy.
        3. Compute next bet: `const next = nextBetAmount(state.config.strategy, state.config.baseAmount, state.lastOutcome, { lastBet: state.lastBetAmount });`
        4. Check balance: `const balance = useWalletStore.getState().balance; if (next.toCents() > balance.toCents()) { halt('insufficient-balance'); return; }`. Same for bet cap.
        5. Fire PlaceBet mutation: `placeBet.mutate({ amount: next, autoCashoutTarget: state.config.target })`. On error 402/insufficient → halt; on 409 (ROUND_NOT_IN_BETTING_PHASE) or 23505 → log + continue (next round will retry); on success → `state.recordPostedBet(next)` (updates lastBetAmount BEFORE the outcome event arrives).
    Extend frontend/src/stores/ws-dispatch.ts: the driver hook needs subscription points. If the existing dispatcher already exposes pub-sub for these 3 events (round:started, bet:my_cashed_out, bet:my_refunded), reuse — verify A9 from RESEARCH §"Assumptions Log". If the dispatcher is closed (switch over fixed types), extend it minimally with new dispatch slots. The change is additive.
    Extend frontend/src/routes/__root.tsx: add `useAutoBetDriver();` inside the root component (alongside existing dispatcher subscriptions). One-line addition.
    Create frontend/src/features/auto-bet/driver-stops.test.ts using vitest + jsdom + the existing Phase 7 store mocking patterns. 11 cases listed in <behavior>.
    Per CLAUDE.md §Money: every monetary read/write goes through Money VO (no `number`). Per RESEARCH §"Anti-Patterns": no setInterval — only WS-event-driven. Per UI-SPEC §"Copywriting Contract": exact halt-reason toast copy.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bunx vitest run src/features/auto-bet/driver-stops.test.ts</automated>
  </verify>
  <done>
    11 driver cases green; driver mounted in __root; ws-dispatch reused (or extended additively); driver halts cleanly on every halt condition without fail-loop on POST errors; no setInterval; Money VO discipline; bunx tsc --noEmit + bun run lint clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AutoBetForm + ManualTabContent + BetPanel tabs refactor</name>
  <files>frontend/src/components/auto-bet-form.tsx, frontend/src/components/auto-bet-form.test.tsx, frontend/src/components/auto-bet-session-panel.tsx, frontend/src/components/manual-tab-content.tsx, frontend/src/components/bet-panel.tsx, frontend/src/components/bet-panel.test.tsx</files>
  <read_first>
    - frontend/src/components/bet-panel.tsx (existing Phase 7 form body — to be preserved verbatim inside the new ManualTabContent wrapper; the BetPanel SHELL is what becomes the Tabs root)
    - frontend/src/components/ui/tabs.tsx (shadcn Tabs primitive — TabsList, TabsTrigger, TabsContent)
    - frontend/src/components/ui/radio-group.tsx (just installed in Task 0 — RadioGroup, RadioGroupItem)
    - frontend/src/components/ui/alert.tsx (existing Alert primitive — used for Manual-tab-disabled explainer)
    - frontend/src/components/ui/tooltip.tsx (existing Tooltip — strategy radio explainer tooltips)
    - frontend/src/features/bet/parse-bet-amount.ts (existing Money-VO validator — REUSE for base, stop-loss, stop-win inputs)
    - .planning/phases/09-auto-features-leaderboard/09-UI-SPEC.md §"Surface A" (entire tab structure) + §"Surface A.1" (Auto form field-by-field spec) + §"State Matrix" (Manual tab disabled while Auto running; Auto trigger dot indicator) + §"Color" (Start button accent fill when valid; Stop button destructive-border outline) + §"Copywriting Contract" (every label, helper, error, button text — VERBATIM)
  </read_first>
  <behavior>
    - Test (BetPanel): renders shadcn Tabs root with defaultValue="manual"; TabsList has exactly 2 triggers (`Manual`, `Auto`); Auto trigger has lucide `Bot` icon
    - Test (BetPanel, Manual default): Manual TabsContent renders the existing Phase 7 bet form (assert by presence of the Place Bet button or the amount Input)
    - Test (BetPanel, Auto tab click): switching to Auto reveals AutoBetForm; the existing Manual form is no longer mounted in the DOM (TabsContent unmounts)
    - Test (BetPanel, while auto running): switch to Manual tab → Manual TabsContent shows the disabled Alert with Lock icon + copy "Auto-bet is running. Switch to the Auto tab to stop."; inputs are HTML-disabled + aria-disabled
    - Test (AutoBetForm, idle): renders 5 inputs (target, strategy RadioGroup, base, stop-loss, stop-win) + Start button; Start button is disabled (form invalid by default until target/base/stops are entered)
    - Test (AutoBetForm, valid): with all 5 fields valid, Start button enabled + accent fill + lucide Play icon
    - Test (AutoBetForm, strategy radio): default selection is `fixed`; tooltip on each option shows the locked copy ("Same bet amount every round." / "Double the bet after each loss; reset to base after each win.")
    - Test (AutoBetForm, target out of bounds): typing 0.5 in target on blur shows error below input "Target must be at least 1.01x." in muted-foreground (NOT red — UI-SPEC §Color)
    - Test (AutoBetForm, base out of bounds): typing 2000 (above max) shows the Phase 7 parseBetAmount error
    - Test (AutoBetForm, Start click): calls useAutoBetStore.start with the parsed config; button transitions to Loading state then Running state (Stop button + destructive border + Pause icon)
    - Test (AutoBetForm, running): all 5 inputs HTML-disabled + aria-disabled; AutoBetSessionPanel renders below showing Session P/L and Next bet preview
    - Test (AutoBetForm, Stop click): calls store.stop({reason:'user'}); no toast (UI-SPEC §Copywriting: "Halted by user (Stop click — no toast, just state change)")
    - Test (AutoBetSessionPanel, sessionPL positive): renders "Session P/L: +12.50 CRD (3 rounds)" in Mono Data accent
    - Test (AutoBetSessionPanel, sessionPL negative): renders "Session P/L: -4.30 CRD (2 rounds)" in Mono Data muted (NOT red — UI-SPEC §Color rule for negative profit)
    - Test (AutoBetSessionPanel, Next bet preview): shows "Next bet: 10.00 CRD (fixed)" or "Next bet: 20.00 CRD (martingale, after loss)" based on strategy + last outcome
    - Test (Touch targets): every interactive element ≥ 44px height (UI-SPEC §Spacing + Accessibility — critical)
    - Test (Reduced motion): no animation runs under `prefers-reduced-motion: reduce` (Loader2 spin replaced with static icon; Stop button no `active:scale-95`)
  </behavior>
  <action>
    Create frontend/src/components/auto-bet-form.tsx per UI-SPEC §Surface A.1 verbatim:
      - 5 fields stacked vertically (UI-SPEC spacing): Target multiplier Input (type=number step=0.01 min=getConfig().autoBet.minTarget max=getConfig().autoBet.maxTarget) + Strategy RadioGroup (Fixed | Martingale) + Base bet Input + Stop-loss Input + Stop-win Input
      - Money inputs reuse parseBetAmount validator with config bounds; stop-loss/stop-win bounded by env STOP_LOSS_CENTS_MAX / STOP_WIN_CENTS_MAX (via getConfig — NOT hardcoded)
      - Inline validation on blur — errors in muted-foreground (NOT red); aria-describedby wires error to input
      - All <Label htmlFor>s present (MCP "Input Labels" sev:High — no placeholder-as-label)
      - Start/Stop button: 4 states per UI-SPEC §Color discipline (idle-disabled, idle-valid, loading, running)
      - When running: form fields all HTML-disabled + aria-disabled (config locked); AutoBetSessionPanel renders below
    Create frontend/src/components/auto-bet-session-panel.tsx:
      - Reads `sessionPL`, `roundCount`, `lastOutcome`, `lastBetAmount`, `config` from auto-bet.store
      - Renders Session P/L line (Money.toFormat + sign + accent if positive, muted otherwise — NOT red)
      - Renders Next bet preview using strategy.ts to compute the same value the driver will use
    Create frontend/src/components/manual-tab-content.tsx:
      - Wraps the existing Phase 7 BetPanel form body verbatim (extract into this wrapper — DO NOT change the form behavior)
      - When `useAutoBetStore.getState().isRunning === true`: render an inline shadcn Alert at the top with lucide Lock icon + accent border (UI-SPEC §Color) + copy "Auto-bet is running. Switch to the Auto tab to stop."; the wrapped form gets `opacity-50` + all inputs HTML-disabled + aria-disabled
    Refactor frontend/src/components/bet-panel.tsx:
      - Outer surface (Card with --card background, --border hairline, Phase 7 padding) preserved
      - Body becomes `<Tabs defaultValue="manual"><TabsList grid grid-cols-2>...</TabsList><TabsContent value="manual"><ManualTabContent /></TabsContent><TabsContent value="auto"><AutoBetForm /></TabsContent></Tabs>`
      - Manual TabsTrigger shows lucide Lock icon 12px (muted) when `useAutoBetStore.getState().isRunning === true` — visual signal without preventing nav
      - Auto TabsTrigger shows lucide Bot icon + pulsing accent dot when running (animate-pulse Tailwind utility, reduced-motion static)
    Create test files:
      - frontend/src/components/auto-bet-form.test.tsx (vitest + RTL + the Phase 7/8 test conventions; mock useAutoBetStore where needed): the 12 form cases above
      - frontend/src/components/bet-panel.test.tsx (extend the existing bet-panel test if present, else create): the 4 BetPanel cases above
    Per UI-SPEC §Accessibility: every input has a visible Label; touch targets ≥44px; aria-disabled on disabled controls; aria-describedby for errors. Per UI-SPEC §Motion: all transitions reduced-motion gated. Per CLAUDE.md §Money: no `number` for amount-shaped fields anywhere (ESLint custom rule enforces). Per CLAUDE.md §"No hardcoded business constants": every numeric bound from getConfig().
    Acceptance asserts (test-list above includes): NO `setInterval` in driver path (grep). NO destructive-fill (red surface) on the Stop button (only border + text); the destructive-fill rule is reserved for crash (UI-SPEC §Color). NO confirmation dialog on Stop (UI-SPEC §"Destructive actions in this phase" forbids).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bunx vitest run src/components/auto-bet-form.test.tsx src/components/bet-panel.test.tsx</automated>
  </verify>
  <done>
    All ~18 form + BetPanel cases green; Manual tab disabled-while-running treatment present (Alert + opacity + disabled); Start/Stop button color discipline correct; UI-SPEC copy verbatim; touch targets verified; reduced-motion respected; 169+ FE tests still green; bunx tsc --noEmit + bun run lint clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → WS event subscription | server-emitted events drive driver state; FE never trusts optimistic state |
| Browser → POST /games/bet | driver POSTs through existing authenticated path; autoCashoutTarget validated server-side (09-02) |
| Browser localStorage | NOT used — REQ-AUTO-04 forbids persist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-50 | Tampering | User tampers store via DevTools to bypass stops | accept | Auto-bet stops are a UX safety, NOT a security control. Server has no stop knowledge (D-01 explicit). The defense is BET_MAX_CENTS + balance check + wallet refusal at the server (Plan 09-02 + Phase 3 wallet). |
| T-09-51 | DoS | Driver fail-loop POSTs continuously on POST errors | mitigate | Driver only POSTs on round:started (one POST per round at most); Pitfall 5 wallet-overflow check + Pitfall 10 saga-race tolerance prevent spinning. |
| T-09-52 | Information Disclosure | Auto-bet config persists across reloads exposing strategy | mitigate | NO persist middleware (REQ-AUTO-04). Grep-gate test asserts no `persist` import in auto-bet.store.ts. |
| T-09-53 | Tampering | User submits target=0.5x via store mutation, bypassing form validation | mitigate | Server Zod (Plan 09-02) rejects with 400 before saga begins. FE validation is UX hint only. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | shadcn radio-group install gated by Task 0 checkpoint (human-verified legitimacy per RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- All vitest cases green (strategy + store + driver + form + bet-panel)
- 169+ existing FE tests still green (regression gate — Phase 7/8 untouched)
- bunx tsc --noEmit clean for frontend
- bun run lint clean (no `number` for monetary fields anywhere in auto-bet/)
- grep `frontend/src/features/auto-bet/auto-bet.store.ts` for `persist`: returns 0 (REQ-AUTO-04 gate)
- grep `frontend/src/features/auto-bet/auto-bet-driver.ts` for `setInterval`: returns 0 (RESEARCH Anti-Patterns)
- grep `frontend/src/components/auto-bet-form.tsx` for `bg-destructive` or `destructive-fill` on the Stop button: returns 0 (only border + text destructive per UI-SPEC §Color)
- Manual visual smoke (after Task 3): open the game page, click Auto tab, fill the form, click Start, observe a POST fires on the next round:started with autoCashoutTarget in the body (browser devtools Network panel) — captured during phase verify, not gated here
</verification>

<success_criteria>
REQ-AUTO-02..05 delivered: tabbed BetPanel with the 5-field Auto form, per-session store with no persist, driver firing POSTs on round:started with autoCashoutTarget. The Martingale base discipline + the cumulative-since-Start stop semantic + the FE-only stop enforcement (D-01) are all under test and locked.
</success_criteria>

<output>
Create `.planning/phases/09-auto-features-leaderboard/09-08-SUMMARY.md` when done.
</output>
