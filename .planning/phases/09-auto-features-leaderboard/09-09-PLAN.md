---
phase: 09-auto-features-leaderboard
plan: 09
type: execute
wave: 4
depends_on:
  - 09-01
  - 09-07
  - 09-08
files_modified:
  - packages/shared-kernel/src/identity/mask-player-id.ts
  - packages/shared-kernel/src/index.ts
  - packages/shared-kernel/tests/identity/mask-player-id.test.ts
  - services/games/src/application/use-cases/mask-player-id.ts
  - services/games/src/application/use-cases/get-current-round.use-case.ts
  - services/games/src/application/use-cases/get-ws-snapshot.use-case.ts
  - services/games/src/application/use-cases/verify-round.use-case.ts
  - services/games/src/infrastructure/messaging/ws-bridge.consumer.ts
  - services/games/src/presentation/gateways/game-ws.gateway.ts
  - frontend/src/features/leaderboard/use-leaderboard.ts
  - frontend/src/features/leaderboard/leaderboard-api.ts
  - frontend/src/features/leaderboard/use-leaderboard.test.ts
  - frontend/src/components/leaderboard-panel.tsx
  - frontend/src/components/leaderboard-panel.test.tsx
  - frontend/src/components/leaderboard-row.tsx
  - frontend/src/components/rank-chip.tsx
  - frontend/src/stores/ws-dispatch.ts
  - frontend/src/routes/index.tsx
autonomous: true
requirements:
  - REQ-LEAD-03
  - REQ-LEAD-04

must_haves:
  truths:
    - "maskPlayerId helper lives in @crash/shared-kernel (moved from services/games/src/application/use-cases/mask-player-id.ts); server consumers + FE both import the SAME function (SHA256 → 8 hex)"
    - "FE own-row detection computes mask via the SAME shared maskPlayerId function — fixture UUID produces an identical 8-hex prefix on both sides (unit test gate)"
    - "GET /games/leaderboard?window=24h fetched via TanStack Query hook useLeaderboard()"
    - "WS leaderboard:updated event invalidates (or inline-updates) the TanStack Query cache; Zod-validated against @crash/contracts/ws leaderboardUpdatedPayloadSchema"
    - "Right rail in routes/index.tsx becomes shadcn Tabs defaultValue='live-feed' with Live Feed (existing) + Leaderboard tabs"
    - "LeaderboardPanel renders top-N rows with rank chip (color rule per UI-SPEC §Color), masked playerId, net profit Money + Trend icon, win count"
    - "Own-row highlight (emerald rail + low-alpha emerald tint) when authenticated playerId appears in top-N"
    - "Rank-up CSS transition (200ms border-color flash) reduced-motion gated"
    - "Empty / loading / error states distinct per UI-SPEC §Surface B.1"
  artifacts:
    - path: "frontend/src/features/leaderboard/use-leaderboard.ts"
      provides: "TanStack Query hook + WS-event invalidation"
    - path: "frontend/src/components/leaderboard-panel.tsx"
      provides: "Tabbed-content body with heading, refresh, list, empty/loading/error, footer"
    - path: "frontend/src/components/leaderboard-row.tsx"
      provides: "Row with rank chip, masked id, net profit, win count, rank-up transition, own-row override"
    - path: "frontend/src/components/rank-chip.tsx"
      provides: "24×24 circle (rank 1-9) / 24×32 rect (rank 10) with rank-color rule"
  key_links:
    - from: "FE LeaderboardPanel own-row detection"
      to: "@crash/shared-kernel maskPlayerId"
      via: "import { maskPlayerId } from '@crash/shared-kernel'"
      pattern: "from\\s+['\"]@crash/shared-kernel['\"]"
    - from: "useLeaderboard"
      to: "GET /games/leaderboard (Plan 09-07 endpoint)"
      via: "TanStack Query fetch through Kong"
      pattern: "/games/leaderboard"
    - from: "ws-dispatch"
      to: "leaderboardUpdatedPayloadSchema (Zod gate)"
      via: "@crash/contracts/ws import"
      pattern: "leaderboardUpdatedPayloadSchema"
    - from: "routes/index.tsx right rail"
      to: "shadcn Tabs (Live Feed | Leaderboard)"
      via: "shadcn primitive (inherited)"
      pattern: "Tabs.*defaultValue=\"live-feed\""
---

<objective>
Land the frontend Leaderboard panel per REQ-LEAD-03 + REQ-LEAD-04 + D-02: tabbed right rail (Live Feed | Leaderboard) with the existing LiveFeed preserved verbatim and a new LeaderboardPanel rendering the top-N from GET /games/leaderboard with WS-driven live updates. The rank-color rule, the own-row override, and the 200ms rank-up transition all follow UI-SPEC §Surface B.1.

Purpose: REQ-LEAD-03 (FE consumes the new endpoint via TanStack Query) + REQ-LEAD-04 (FE renders the side panel with WS-driven live updates). The rail-as-Tabs refactor preserves the Phase 7 D-01 grid byte-identically — same width, same height, same dark-casino tokens. The leaderboard:updated WS event is Zod-validated against @crash/contracts/ws (single source of truth shared with the server emit in 09-06).
Output: useLeaderboard hook + LeaderboardPanel + LeaderboardRow + RankChip + routes/index.tsx tabbed-rail refactor + ws-dispatch extension for the new event. Zero changes to LiveFeed (preserved verbatim inside the Live Feed tab).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-auto-features-leaderboard/09-CONTEXT.md
@.planning/phases/09-auto-features-leaderboard/09-RESEARCH.md
@.planning/phases/09-auto-features-leaderboard/09-UI-SPEC.md
@CLAUDE.md

# Source-of-truth files
@frontend/src/routes/index.tsx
@frontend/src/components/live-feed.tsx
@frontend/src/components/ui/tabs.tsx
@frontend/src/components/ui/scroll-area.tsx
@frontend/src/components/ui/skeleton.tsx
@frontend/src/components/ui/alert.tsx
@frontend/src/components/ui/tooltip.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/stores/ws-dispatch.ts
@frontend/src/lib/auth.ts
@frontend/src/lib/api.ts
@frontend/src/features/history/use-history.ts

# Plan 09-07 interfaces this plan consumes
@packages/contracts/src/ws/leaderboard-updated.payload.ts
@services/games/src/presentation/controllers/leaderboard.controller.ts

# Plan 09-01 interfaces this plan consumes
# getConfig().leaderboard.{sizeN, windowHours, relativeRefreshMs, rankUpTransitionMs}
@frontend/src/config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move maskPlayerId helper to @crash/shared-kernel (Phase 6 consolidation)</name>
  <files>packages/shared-kernel/src/identity/mask-player-id.ts, packages/shared-kernel/src/index.ts, packages/shared-kernel/tests/identity/mask-player-id.test.ts, services/games/src/application/use-cases/mask-player-id.ts, services/games/src/application/use-cases/get-current-round.use-case.ts, services/games/src/application/use-cases/get-ws-snapshot.use-case.ts, services/games/src/application/use-cases/verify-round.use-case.ts, services/games/src/infrastructure/messaging/ws-bridge.consumer.ts, services/games/src/presentation/gateways/game-ws.gateway.ts</files>
  <read_first>
    - services/games/src/application/use-cases/mask-player-id.ts (the existing Phase 6 implementation — `createHash('sha256').update(playerId).digest('hex').substring(0, 8)`)
    - services/games/src/application/use-cases/get-current-round.use-case.ts (one of the 4 application-layer consumers; imports via `./mask-player-id`)
    - services/games/src/application/use-cases/get-ws-snapshot.use-case.ts (consumer)
    - services/games/src/application/use-cases/verify-round.use-case.ts (consumer)
    - services/games/src/infrastructure/messaging/ws-bridge.consumer.ts (consumer; imports via `../../application/use-cases/mask-player-id`)
    - services/games/src/presentation/gateways/game-ws.gateway.ts (Plan 09-06 added the LEADERBOARD_UPDATED `@OnEvent` handler that uses maskPlayerId from the legacy path — this task updates that import to `@crash/shared-kernel` as part of the consolidation)
    - packages/shared-kernel/src/index.ts (the workspace re-export barrel — add the new module export alphabetically/logically next to `./identity/branded-id`)
    - packages/shared-kernel/src/identity/branded-id.ts (sibling module — match the file convention, no `node:crypto` dependency exists yet in shared-kernel; verify `node:crypto` is available to packages/shared-kernel's bundler / TS target; the existing `cash-out.use-case.ts` uses node crypto so the runtime target supports it; the package.json of shared-kernel may need no change because `node:` imports are first-class in Node-target tsconfig — confirm during execution; if a peer dependency declaration is needed, add `node:crypto` to the allowed-import list)
  </read_first>
  <behavior>
    - Test: `maskPlayerId(PlayerId("3f29b1a2-4d5e-6f7a-8b9c-0d1e2f3a4b5c"))` returns an 8-char lowercase hex string matching `/^[0-9a-f]{8}$/`
    - Test: cross-environment determinism — the SAME UUID input produces the SAME 8-hex output when invoked from (a) the server-side use-case test harness AND (b) the FE-imported shared-kernel module (the test imports the function from `@crash/shared-kernel` and runs it in a vitest jsdom environment against the same fixture UUID; both outputs must be identical)
    - Test: input branded `PlayerId` type is accepted (TypeScript compile-time gate; runtime input is a raw UUID string)
    - Test: empty string input throws OR returns a defined hash (whichever the existing Phase 6 impl does — preserve byte-identically)
    - Regression: every existing Phase 6 server consumer still produces the SAME masked value after the move (snapshot test — fixture UUIDs from Phase 6 tests yield byte-identical mask outputs)
  </behavior>
  <action>
    Move the existing Phase 6 helper to shared-kernel:
      1. Create `packages/shared-kernel/src/identity/mask-player-id.ts` with the SAME body as the existing `services/games/src/application/use-cases/mask-player-id.ts`:
         `import { createHash } from "node:crypto"; import type { PlayerId } from "./branded-id"; export function maskPlayerId(playerId: PlayerId): string { return createHash("sha256").update(playerId as unknown as string).digest("hex").substring(0, 8); }`
         Note: the existing impl imports `PlayerId` from `@crash/shared-kernel` — inside shared-kernel itself, import from the sibling `./branded-id`.
      2. Add `export * from "./identity/mask-player-id";` to `packages/shared-kernel/src/index.ts` (sibling of the existing `./identity/branded-id` export).
      3. Update `services/games/src/application/use-cases/mask-player-id.ts` to a re-export shim: `export { maskPlayerId } from "@crash/shared-kernel";` — preserves the legacy import path during transition AND ensures any consumer not updated in step 4 keeps compiling. Alternatively, DELETE the file and update all 4 application-layer + 1 infrastructure consumer imports + 1 gateway import directly. Pick deletion for cleanliness UNLESS a consumer outside `files_modified` is discovered during the grep audit (run: `grep -rn "from.*mask-player-id" services/`).
      4. Update each cataloged consumer's import statement from `./mask-player-id` / `../../application/use-cases/mask-player-id` to `@crash/shared-kernel`. The 6 consumers identified by grep:
         - services/games/src/application/use-cases/get-current-round.use-case.ts
         - services/games/src/application/use-cases/get-ws-snapshot.use-case.ts
         - services/games/src/application/use-cases/verify-round.use-case.ts
         - services/games/src/infrastructure/messaging/ws-bridge.consumer.ts (3 call sites, single import)
         - services/games/src/presentation/gateways/game-ws.gateway.ts (Plan 09-06 added this consumer — verify Plan 09-06 has landed before this task runs; if not yet landed, omit the gateway line and 09-06 must coordinate the import-from-shared-kernel directly when it lands — but per wave 4 sequencing, 09-06 (wave 3) has already shipped so this consumer EXISTS and uses the legacy import)
      5. Create `packages/shared-kernel/tests/identity/mask-player-id.test.ts` with the 5 cases listed in <behavior>. Use a fixture UUID array shared between this test and the FE use-leaderboard / leaderboard-panel test fixtures (export a const `MASK_FIXTURES = [...]` from the test file or a sibling `tests/identity/fixtures.ts`).
      6. Run the full `services/games` test suite to assert NO regression — the 6 consumers must produce byte-identical masked output. Run `bunx tsc --noEmit` across the workspace.
    Per CLAUDE.md §"Zero infrastructure imports in domain/": shared-kernel is allowed `node:crypto` (it is the language platform, not an infrastructure dependency); the existing `packages/shared-kernel/src/money/money.ts` already uses external (dinero.js) imports — verify no domain-purity ESLint rule blocks `node:crypto` here. If it does, escalate as a CHECKPOINT in this task BEFORE making the move.
    Per CLAUDE.md §"No AI attribution": no `Co-Authored-By`, no "Generated by" in any commit message landing this move.
  </action>
  <verify>
    <automated>cd packages/shared-kernel &amp;&amp; bun test tests/identity/mask-player-id.test.ts &amp;&amp; cd ../../services/games &amp;&amp; bun test tests/unit &amp;&amp; bunx tsc --noEmit</automated>
  </verify>
  <done>
    maskPlayerId lives in `@crash/shared-kernel`; all 6+ services/games consumers updated to import from there; legacy `services/games/src/application/use-cases/mask-player-id.ts` either deleted or shimmed; cross-env determinism test green; all existing Phase 6 server consumers produce byte-identical mask output (regression-free); FE can now import the same helper for own-row detection (consumed by renumbered Task 3).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useLeaderboard TanStack Query hook + WS invalidation + leaderboard-api fetch</name>
  <files>frontend/src/features/leaderboard/leaderboard-api.ts, frontend/src/features/leaderboard/use-leaderboard.ts, frontend/src/features/leaderboard/use-leaderboard.test.ts, frontend/src/stores/ws-dispatch.ts</files>
  <read_first>
    - frontend/src/features/history/use-history.ts (canonical TanStack Query hook + WS event handler pattern from Phase 7 — staleTime, queryKey shape, invalidation on WS event)
    - frontend/src/lib/api.ts or equivalent (the authenticated fetch helper that wires the Keycloak bearer through to /games/* endpoints via Kong on :8000)
    - frontend/src/stores/ws-dispatch.ts (existing dispatcher; extend with leaderboard:updated subscription point — the schema gate is leaderboardUpdatedPayloadSchema from @crash/contracts/ws)
    - packages/contracts/src/ws/leaderboard-updated.payload.ts (the shared schema from 09-07)
    - .planning/phases/09-auto-features-leaderboard/09-RESEARCH.md §"Pattern 2" + §"Open Q4" (inline payload — FE consumes the full top-N from the WS event, no separate refetch needed when the event arrives; the manual refresh button remains as a defensive UX) + §"Pitfall 7" (server throttles emits to top-N rank changes; FE just consumes)
    - .planning/phases/09-auto-features-leaderboard/09-UI-SPEC.md §"Cross-References" (use-leaderboard.ts behavior: staleTime: Infinity; WS event invalidates or replaces)
  </read_first>
  <behavior>
    - Test (api): `fetchLeaderboard()` calls `${API_BASE}/games/leaderboard?window=24h` with Authorization Bearer header (the existing protectedFetch)
    - Test (api): response is Zod-parsed against leaderboardResponseSchema; malformed response throws
    - Test (hook, initial fetch): useLeaderboard returns `{ data, isLoading, isError, refetch }`; initial render → isLoading=true; after fetch resolves → data populated
    - Test (hook, WS event arrives): when ws-dispatch dispatches leaderboard:updated with a valid payload, the TanStack Query cache for `['leaderboard', '24h']` is REPLACED inline with the event payload (per Q4 — inline payload, no refetch); subsequent render shows new data without re-fetching
    - Test (hook, WS event with invalid payload): Zod validation throws; dispatch logs the error; cache is NOT corrupted (stays at previous valid state)
    - Test (hook, manual refresh): calling `refetch()` triggers a fresh GET (the refresh button defensive path)
    - Test (hook, staleTime: Infinity): no automatic refetch on focus/reconnect (per UI-SPEC; WS event is the live signal)
  </behavior>
  <action>
    Create frontend/src/features/leaderboard/leaderboard-api.ts:
      - `export async function fetchLeaderboard(window: '24h' = '24h'): Promise<LeaderboardResponse>` — uses the existing protectedFetch helper (or equivalent) to GET `${apiBase}/games/leaderboard?window=${window}`; parses response via `leaderboardResponseSchema` from `@crash/contracts/ws` (re-exported).
    Create frontend/src/features/leaderboard/use-leaderboard.ts:
      - `export function useLeaderboard(): UseQueryResult<LeaderboardResponse>`
      - `useQuery({ queryKey: ['leaderboard', '24h'], queryFn: () => fetchLeaderboard('24h'), staleTime: Infinity })`
      - Subscribes to the ws-dispatch leaderboard:updated event in a useEffect; on each valid payload, calls `queryClient.setQueryData(['leaderboard', '24h'], payload)` (inline replace per Q4 recommendation — no roundtrip)
    Extend frontend/src/stores/ws-dispatch.ts: add a dispatch slot for `leaderboard:updated` using the existing dispatcher pattern (whether case-statement or pub-sub registry — match Phase 7 style). Zod-validate the inbound payload against `leaderboardUpdatedPayloadSchema` from @crash/contracts/ws before propagating. Invalid payloads logged + dropped (do not crash the WS connection).
    Create frontend/src/features/leaderboard/use-leaderboard.test.ts (vitest + jsdom + the existing Phase 7 TanStack Query test patterns; use `@tanstack/react-query`'s test utilities + a mock QueryClient). 7 cases listed in <behavior>.
    Per CLAUDE.md §Money: netProfit on the wire is MoneySnapshot; hydration to Money happens at the row-render boundary (Task 2). Per RESEARCH §Pitfall 1: timestamps from the WS payload arrive as ISO8601 strings — coerce via `new Date(updatedAt)` at the FE consumer; the Phase 6 toDate() helper applies to raw SQL, not WS payloads (Zod's z.string().datetime() validates the format).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bunx vitest run src/features/leaderboard/use-leaderboard.test.ts</automated>
  </verify>
  <done>
    Hook + API + WS subscription all green (7 cases); cache inline-update from WS event works; invalid payloads safely rejected; manual refresh works; bunx tsc --noEmit + bun run lint clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RankChip + LeaderboardRow + LeaderboardPanel components</name>
  <files>frontend/src/components/rank-chip.tsx, frontend/src/components/leaderboard-row.tsx, frontend/src/components/leaderboard-panel.tsx, frontend/src/components/leaderboard-panel.test.tsx</files>
  <read_first>
    - frontend/src/components/live-feed.tsx (rail height / ScrollArea / dimension reference for the leaderboard panel — they share the same surface)
    - frontend/src/components/history-strip.tsx (color-banded chip pattern reference for the rank chip color rule)
    - frontend/src/components/ui/scroll-area.tsx, ui/skeleton.tsx, ui/alert.tsx, ui/button.tsx, ui/tooltip.tsx (inherited primitives)
    - frontend/src/lib/auth.ts (where to get the authenticated playerId for own-row detection — Phase 7 OIDC adapter exposes the sub claim)
    - packages/shared-kernel/src/money/money.ts (Money.fromSnapshot + Money.toFormat / .toString for the net profit display)
    - .planning/phases/09-auto-features-leaderboard/09-UI-SPEC.md §"Surface B.1" (full row anatomy + heading + footer + states), §"Color" (rank chip color rule + own-row override + net profit color rule), §"Typography" (Mono Data 14px for rank + id + profit; Label 14px Fira Sans for win count), §"Spacing" (row 44px height, padding values), §"Copywriting Contract" (every label, masked id format, footer relative timestamp logic, empty/error copy)
    - .planning/phases/09-auto-features-leaderboard/09-RESEARCH.md §"Pitfall 8" (window cutoff handled server-side; FE just renders what arrives)
    - frontend/src/features/juice/celebrate.ts or similar (reduced-motion gate pattern reference)
  </read_first>
  <behavior>
    - Test (RankChip rank 1): renders 24x24 circle with accent emerald border + low-alpha emerald surface + Fira Code tabular text "1"
    - Test (RankChip rank 2): emerald → cyan border + cyan surface; text "2"
    - Test (RankChip rank 3): muted-foreground border, transparent surface, muted text
    - Test (RankChip ranks 4-10): hairline border (--border), transparent surface, foreground text; rank 10 uses 24x32 rect (two-digit)
    - Test (LeaderboardRow): renders left rail per rank rule (2px emerald for 1, 2px cyan for 2, 2px muted for 3, none for 4-10); masked playerId in Mono Data; net profit + sign + trend icon; win count "Nw" suffix muted
    - Test (LeaderboardRow, own-row): own row gets emerald-rail override + emerald low-alpha row tint + `YOU (first5hex…)` prefix
    - Test (LeaderboardPanel, own-row detection via shared mask): given a fixture UUID `OWN_SUB = "3f29b1a2-4d5e-6f7a-8b9c-0d1e2f3a4b5c"` and a server-emitted entries list containing `{ playerIdMasked: maskPlayerId(PlayerId(OWN_SUB)), ... }` (computed once at test setup via the shared-kernel helper from Task 1), the panel correctly identifies the own row — proving the FE-imported helper produces the SAME 8-hex prefix as the server side
    - Test (LeaderboardRow, positive profit): accent emerald color + TrendingUp icon
    - Test (LeaderboardRow, negative profit): muted-foreground (NOT red — UI-SPEC §Color) + TrendingDown icon; leading `−` sign present
    - Test (LeaderboardRow, zero profit): muted, no icon
    - Test (LeaderboardRow, rank-up transition): when row's rank improves (e.g., 4 → 2), the rank chip border briefly flashes accent for 200ms then transitions back over 600ms (CSS-only; verify via class addition + getComputedStyle timing OR via assertion that the transient CSS variable / className is added on the rank-change render and removed after the duration). Under prefers-reduced-motion: snap (no transition)
    - Test (LeaderboardPanel, heading): "Top {sizeN} · last {windowHours}h" (env-driven; default "Top 10 · last 24h")
    - Test (LeaderboardPanel, refresh button): RefreshCw icon ≥44x44px hit area; click → calls useLeaderboard().refetch()
    - Test (LeaderboardPanel, empty state): when entries.length === 0 → centered text "No rounds settled in the last 24h yet." + body "The leaderboard updates as players cash out."
    - Test (LeaderboardPanel, loading state): when isLoading → 5 Skeleton rows matching loaded dimensions (MCP "Match skeleton dimensions")
    - Test (LeaderboardPanel, error state): when isError → shadcn Alert variant="destructive" + Retry button
    - Test (LeaderboardPanel, footer): "Updated Ns ago" footer updates every getConfig().leaderboard.relativeRefreshMs (5000ms default); renders "Just now" for < 5s, "Ns ago" / "Nm ago" / "Nh ago" per UI-SPEC
    - Test (LeaderboardPanel, footer pauses when tab not active): the setInterval used to tick the relative timestamp must clear / not run when the Leaderboard tab is NOT active (Phase 7/8 isolation principle — verify by mounting in a Tabs context with Live Feed active and asserting no setInterval body runs)
    - Test (LeaderboardPanel, aria-live for own-rank-up): when own row's rank improves, an aria-live="polite" region announces "You moved up to rank N" (UI-SPEC §Accessibility); rank-down: no announcement
    - Test (Touch targets): every interactive element (refresh button, retry button) ≥ 44px (UI-SPEC §Accessibility)
  </behavior>
  <action>
    Create frontend/src/components/rank-chip.tsx per UI-SPEC §Color rank-color rule:
      - Props: `{ rank: number }`
      - Renders a circle (rank 1-9) or rounded rect (rank 10) with the rank-color border + surface from the inherited semantic ramp (CSS variables only — NO hardcoded hex per CLAUDE.md §Configuration)
      - Mono Data 14px Fira Code tabular for the rank number
    Create frontend/src/components/leaderboard-row.tsx per UI-SPEC §Surface B.1:
      - Props: `{ entry: LeaderboardEntryWire, isOwnRow: boolean, previousRank: number | null }`
      - Left rail per rank rule (2px emerald/cyan/muted for 1/2/3, none for 4-10; always emerald for own-row regardless of rank)
      - 2-line layout: top line = rank chip + masked playerId (or `YOU (first5hex…)` for own-row) + right-aligned net profit + Trend icon; bottom line = "Nw" win count muted
      - Rank-up transition: if `previousRank !== null && entry.rank < previousRank`, apply a transient className that triggers a 200ms ease-out border-color flash to accent on the rank chip, then transitions back over 600ms; reduced-motion: snap. Use CSS transitions with the inherited token (NO new dependency). Duration sourced from getConfig().leaderboard.rankUpTransitionMs.
      - Net profit color rule: positive accent + TrendingUp; negative muted + TrendingDown (NOT red); zero muted no icon. Sign always rendered (`+` or `−` prefix). Use Money.fromSnapshot(entry.netProfit).toFormat() (or whichever Phase 7 Money toString helper is canonical).
    Create frontend/src/components/leaderboard-panel.tsx per UI-SPEC §Surface B.1:
      - Top: heading "Top {sizeN} · last {windowHours}h" + RefreshCw button right-aligned (≥44×44 hit area, aria-label "Refresh leaderboard", tooltip "Refresh now")
      - Body: shadcn ScrollArea capped at rail height; renders 5 Skeleton rows when isLoading; renders empty-state copy when entries.length === 0; renders Alert destructive + Retry button on error; renders LeaderboardRow list otherwise
      - Footer: "Updated Ns ago" relative timestamp; setInterval that ticks every getConfig().leaderboard.relativeRefreshMs, BUT only when the panel is the active tab (use IntersectionObserver OR a parent-prop `isActive` OR React.useDeferredValue-style gating — pick the cleanest given the shadcn Tabs API. Simplest: parent index.tsx passes isActive boolean from Tabs context; panel runs interval only if isActive).
      - aria-live="polite" region for own-rank-up announcements (one polite announcement per rank improvement; NOT per rank-down)
      - Detect own playerId via the Phase 7 OIDC user info (the sub claim) and compare against `entry.playerIdMasked` by hashing-and-comparing. Per Task 1 (this plan): `import { maskPlayerId } from "@crash/shared-kernel"` — the SAME helper the server uses (SHA256 hex prefix, NOT `uuid.replace(/-/g,'').slice(0,8)` which would mismatch). Cast the OIDC sub claim to a `PlayerId` brand and call: `const ownMask = maskPlayerId(PlayerId(sub)); const isOwnRow = entry.playerIdMasked === ownMask;`. Per UI-SPEC §Accessibility this comparison is necessary for the own-row override. The cross-environment determinism test in Task 1 gates that the FE-computed mask matches the server-emitted mask for any fixture UUID.
      - Track previousRank per playerId across renders via a useRef map so the rank-up transition only fires on actual rank improvement (not on initial render). The map persists across renders but resets on tab unmount; that's acceptable per UI-SPEC.
    Create frontend/src/components/leaderboard-panel.test.tsx with the ~18 cases above. Mock useLeaderboard to inject specific top-N states (empty, loaded, isLoading, isError, rank changes between renders).
    Per CLAUDE.md §Money: Money VO at the FE row boundary; never `number`. Per CLAUDE.md §Configuration: no hardcoded sizeN / windowHours / refresh / transition / threshold — all via getConfig(). Per UI-SPEC §Color: red reserved for crash (negative profit uses muted, NOT red). Per UI-SPEC §Motion: rank-up is sub-300ms band, NOT a juice moment, reduced-motion gated.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bunx vitest run src/components/leaderboard-panel.test.tsx</automated>
  </verify>
  <done>
    18 component cases green; rank chip color rule verified; own-row override verified; net profit color rule verified (negative is muted, NOT red); rank-up transition fires on rank improvement only + reduced-motion snap; relative footer ticks only while tab active; aria-live announces own-rank-up; touch targets verified; bunx tsc --noEmit + bun run lint clean; existing 169+ FE tests still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Right rail tabbed refactor in routes/index.tsx (Live Feed | Leaderboard)</name>
  <files>frontend/src/routes/index.tsx</files>
  <read_first>
    - frontend/src/routes/index.tsx (the existing layout — locate the `feed-rail` <aside> that mounts <LiveFeed /> at line ~90; the surrounding grid structure must stay byte-stable per UI-SPEC §"Layout Contract")
    - frontend/src/components/ui/tabs.tsx (Tabs / TabsList / TabsTrigger / TabsContent)
    - .planning/phases/09-auto-features-leaderboard/09-UI-SPEC.md §"Surface B" (entire right-rail Tabs structure, defaultValue="live-feed", trigger labels + Trophy icon for Leaderboard, mobile stacked-column preservation)
    - frontend/src/components/live-feed.tsx (preserved verbatim — NO changes)
  </read_first>
  <behavior>
    - Test (routes/index.tsx, integration via RTL): right-rail aside renders Tabs root with defaultValue="live-feed"; TabsList has 2 triggers ("Live Feed", "Leaderboard" with Trophy icon)
    - Test: Live Feed tab content renders the existing <LiveFeed /> verbatim (presence of LiveFeed test-id or known role)
    - Test: clicking "Leaderboard" trigger swaps content to <LeaderboardPanel />; LiveFeed unmounts from DOM
    - Test: mobile breakpoint (<768px) — the right rail remains stacked-below per Phase 7 D-01 (UI-SPEC §Surface B); the Tabs structure is preserved inside the stacked card with width-full
    - Test: keyboard navigation — left/right arrows swap tabs; tab focus moves to TabsContent; UI-SPEC §Accessibility (shadcn Tabs is Radix-backed; the behavior comes free, but assert)
    - Test: tab swap respects prefers-reduced-motion (shadcn Tabs default is content swap, no slide — no animation budget cost; just assert no animation runs)
    - Test (regression): bet panel grid + history strip + curve stage all render unchanged (Phase 7 layout preserved); 169+ FE tests still green
  </behavior>
  <action>
    Modify frontend/src/routes/index.tsx:
      - Inside the `feed-rail` <aside>, wrap the existing <LiveFeed /> in a shadcn `<Tabs defaultValue="live-feed">`:
        - `<TabsList class="grid grid-cols-2">` with two `<TabsTrigger>`s: `value="live-feed"` (label "Live Feed", no icon) and `value="leaderboard"` (label "Leaderboard" + lucide Trophy icon 14px to the LEFT of the label per UI-SPEC §Surface B)
        - `<TabsContent value="live-feed"><LiveFeed /></TabsContent>` — preserved verbatim
        - `<TabsContent value="leaderboard"><LeaderboardPanel /></TabsContent>` — the new panel from Task 2
      - Pass `isActive={activeTab === 'leaderboard'}` to LeaderboardPanel so its footer setInterval gating works (Task 2 contract). Use the controlled Tabs API: `const [activeTab, setActiveTab] = useState('live-feed'); <Tabs value={activeTab} onValueChange={setActiveTab}>`.
      - DO NOT touch the bet-rail aside (09-08 owns it).
      - DO NOT touch the curve-stage or history-strip sections.
      - The grid layout (`grid-cols-[320px_minmax(0,1fr)_320px]`) stays byte-identical per UI-SPEC "same width, same height, same dark-casino tokens" requirement.
    Extend frontend/src/routes/index.test.tsx (or sibling) with the 7 cases above.
    Per UI-SPEC §"Critical invariant — opening the right-rail Leaderboard tab MUST NOT pause the live game": the tab swap is a render swap; WS connection + rAF loop + round FSM keep running. Verify by asserting useRoundStore + useConnectionStore subscriptions still mount across tab swaps (the test mounts both states across the tab change and asserts no unmount of the live-game subscribers).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; bunx vitest run src/routes/index.test.tsx</automated>
  </verify>
  <done>
    7 routes/index test cases green; LiveFeed preserved byte-unchanged; LeaderboardPanel mounted on tab swap; mobile stacked behavior preserved; keyboard nav works; full 169+ FE test suite still green; bunx tsc --noEmit + bun run lint clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS leaderboard:updated → FE | Zod-validated against @crash/contracts/ws schema before consumption; malformed payload dropped |
| GET /games/leaderboard → FE | response Zod-validated against the same schema as the WS payload; consistent gate |
| FE → DOM | playerIdMasked rendered as text only; no raw player UUIDs ever reach DOM (server masks before serialization) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-60 | Tampering | Malicious WS payload with invalid shape | mitigate | Zod validation in ws-dispatch drops invalid payloads; logs the error; cache stays at last valid state. |
| T-09-61 | Information Disclosure | FE leaks own full player UUID via DOM attribute | mitigate | Own-row detection uses the shared `maskPlayerId` helper from `@crash/shared-kernel` (Task 1 of this plan; SHA256 hex prefix — IDENTICAL to the server-side mask). The full UUID is held only in memory via the OIDC sub claim and never serialised to a rendered DOM attribute. Audit at code review. |
| T-09-62 | Availability | LeaderboardPanel re-render storm on rapid WS events (server failed to throttle) | mitigate | TanStack Query's `setQueryData` is idempotent + memoized; React renders only when data identity changes. The server-side throttle (09-06 Pitfall 7) is the primary gate. |
| T-09-63 | Availability | Footer setInterval runs forever, leaking timers across tab swaps | mitigate | Panel clears the interval in useEffect cleanup + the interval only registers when `isActive` prop is true (Task 2 contract). |
</threat_model>

<verification>
- All ~32 vitest cases green (api + hook + components + routes/index)
- 169+ existing FE tests still green (regression gate — Phase 7/8 + 09-08 untouched)
- bunx tsc --noEmit clean for frontend
- bun run lint clean
- grep `frontend/src/components/leaderboard-row.tsx` for `bg-destructive` or `text-destructive` on the negative-profit branch: returns 0 (UI-SPEC §Color — muted, NOT red)
- grep `frontend/src/components/leaderboard-panel.tsx` for hardcoded sizes/intervals (`5000`, `200`, `10`, `24`): returns 0 (all from getConfig())
- grep `frontend/src/features/leaderboard/leaderboard-api.ts` for `z.parse|safeParse`: confirms Zod gate present
- grep `frontend/src/components/leaderboard-panel.tsx` for `replace(/-/g` OR `slice(0, *8)` (the buggy uuid-prefix mask shape): returns 0 (must use shared `maskPlayerId` from `@crash/shared-kernel` per Task 1; mismatch would silently break own-row detection)
- grep `frontend/src/components/leaderboard-panel.tsx` for `from "@crash/shared-kernel"`: returns ≥1 (own-row detection imports the shared helper)
- grep `services/games/src/application/use-cases/get-current-round.use-case.ts services/games/src/application/use-cases/get-ws-snapshot.use-case.ts services/games/src/application/use-cases/verify-round.use-case.ts services/games/src/infrastructure/messaging/ws-bridge.consumer.ts services/games/src/presentation/gateways/game-ws.gateway.ts` for `from ".*application/use-cases/mask-player-id"`: returns 0 (all consumers migrated to @crash/shared-kernel)
- bun test packages/shared-kernel/tests/identity/mask-player-id.test.ts passes (5 cases including cross-environment determinism)
- Manual smoke (phase verify): open the right-rail Leaderboard tab → seed leaderboard via test data → observe top-N renders with masked IDs + correct rank chip colors + own-row highlight if applicable; trigger a rank-changing event server-side and observe the 200ms border flash (reduced-motion off)
</verification>

<success_criteria>
REQ-LEAD-03 + REQ-LEAD-04 delivered: GET /games/leaderboard fetched via TanStack Query; WS leaderboard:updated inline-updates the cache; tabbed right rail (Live Feed | Leaderboard) preserves the Phase 7 D-01 grid byte-identically; LeaderboardPanel renders top-N per UI-SPEC §Surface B.1 with full color rule, own-row override, rank-up transition, accessibility, and state coverage (empty, loading, error). LiveFeed byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/09-auto-features-leaderboard/09-09-SUMMARY.md` when done.
</output>
