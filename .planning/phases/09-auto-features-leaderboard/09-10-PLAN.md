---
phase: 09-auto-features-leaderboard
plan: 10
type: execute
wave: 5
depends_on:
  - 09-05
  - 09-06
  - 09-07
  - 09-08
  - 09-09
files_modified:
  - .planning/adrs/ADR-032-light-cqrs-leaderboard-read-model.md
  - .planning/adrs/ADR-033-server-enforced-auto-cashout.md
  - .planning/adrs/ADR-034-per-session-auto-bet-no-persist.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-AUTO-01
  - REQ-AUTO-02
  - REQ-AUTO-03
  - REQ-AUTO-04
  - REQ-AUTO-05
  - REQ-LEAD-01
  - REQ-LEAD-02
  - REQ-LEAD-03
  - REQ-LEAD-04

must_haves:
  truths:
    - "Three ADRs landed at next-free numbers ADR-032 (Light CQRS read-model), ADR-033 (Server-enforced auto-cashout — ratifies ADR-023 boundary), ADR-034 (Per-session auto-bet config — no persist)"
    - "STATE.md updated: Phase 9 marked COMPLETE; v1-complete count 67/95 → 76/95 (adds REQ-AUTO-01..05 + REQ-LEAD-01..04 = 9 reqs); ADRs landed 31 → 34"
    - "ROADMAP.md Phase 9 marked complete + traceability rows for 9 REQ-IDs updated to Done with plan citations (09-02..09-09)"
    - "REQUIREMENTS.md Phase 9 traceability table: all 9 rows flipped to Done with citations"
    - "Anticipated ADR-025/026/027 labels in ROADMAP §Phase 9 reconciled with a renumber note (Phase 5/6/7/8 consumed 019..031 before Phase 9 closed; shipped ADRs never renumbered — same precedent set by Phase 7 (ADR-019..022 → 024..027) and Phase 8 (ADR-023/024 → 028..031))"
  artifacts:
    - path: ".planning/adrs/ADR-032-light-cqrs-leaderboard-read-model.md"
      provides: "Context/Decision/Consequences/Alternatives for D-05 + REQ-LEAD-01/02"
      contains: "Light CQRS"
    - path: ".planning/adrs/ADR-033-server-enforced-auto-cashout.md"
      provides: "Context/Decision/Consequences for D-06 + REQ-AUTO-01 + AutoCashoutTickService split; ratifies ADR-023"
      contains: "AutoCashoutTickService"
    - path: ".planning/adrs/ADR-034-per-session-auto-bet-no-persist.md"
      provides: "Context/Decision/Consequences for D-01 + D-03 + REQ-AUTO-04"
      contains: "no persist"
    - path: ".planning/STATE.md"
      provides: "Current focus advanced past Phase 9"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 checkbox flipped, traceability updated"
    - path: ".planning/REQUIREMENTS.md"
      provides: "9 REQ-IDs flipped to Done"
  key_links:
    - from: "ADR-033"
      to: "ADR-023 (server-authoritative cashout)"
      via: "explicit ratification / generalization"
      pattern: "ADR-023"
---

<objective>
Close out Phase 9 with the three ADRs at next-free numbers (032..034) and the STATE/ROADMAP/REQUIREMENTS rotation. Phase 9 is the second-to-last phase (only Phase 10 remains); the closeout must accurately reflect that 9/10 phases are complete and that 76/95 v1 requirements have shipped.

Purpose: CLAUDE.md §ADRs requires one ADR per significant decision; Phase 9 has three (D-05 light CQRS, D-06 server-enforced auto-cashout, D-01+D-03 per-session no-persist auto-bet). The STATE/ROADMAP/REQUIREMENTS rotation is the standard end-of-phase audit gate established by Phases 1-8.
Output: 3 ADRs + STATE/ROADMAP/REQUIREMENTS updates + a single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.planning/phases/09-auto-features-leaderboard/09-CONTEXT.md
@.planning/phases/09-auto-features-leaderboard/09-RESEARCH.md

# ADR style reference — read the most recent Phase 8 ADRs to match format verbatim
@.planning/adrs/ADR-028-client-seed-deterministic-reaffirmation.md
@.planning/adrs/ADR-029-driver-injection-seam.md
@.planning/adrs/ADR-030-provably-fair-browser-subpath.md
@.planning/adrs/ADR-031-replay-modal-speed-selector.md
@.planning/adrs/ADR-023-server-authoritative-cashout-accepted-at.md

# Plan summaries — required input for the closeout citations
@.planning/phases/09-auto-features-leaderboard/09-01-SUMMARY.md
@.planning/phases/09-auto-features-leaderboard/09-02-SUMMARY.md
@.planning/phases/09-auto-features-leaderboard/09-03-SUMMARY.md
@.planning/phases/09-auto-features-leaderboard/09-04-SUMMARY.md
@.planning/phases/09-auto-features-leaderboard/09-05-SUMMARY.md
@.planning/phases/09-auto-features-leaderboard/09-06-SUMMARY.md
@.planning/phases/09-auto-features-leaderboard/09-07-SUMMARY.md
@.planning/phases/09-auto-features-leaderboard/09-08-SUMMARY.md
@.planning/phases/09-auto-features-leaderboard/09-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-032 (Light CQRS), ADR-033 (Server-enforced auto-cashout), ADR-034 (Per-session auto-bet)</name>
  <files>.planning/adrs/ADR-032-light-cqrs-leaderboard-read-model.md, .planning/adrs/ADR-033-server-enforced-auto-cashout.md, .planning/adrs/ADR-034-per-session-auto-bet-no-persist.md</files>
  <read_first>
    - .planning/adrs/ADR-023-server-authoritative-cashout-accepted-at.md (the canon ADR-033 ratifies)
    - .planning/adrs/ADR-028-client-seed-deterministic-reaffirmation.md (most recent ADR — match format verbatim)
    - .planning/adrs/ADR-029-driver-injection-seam.md, ADR-030, ADR-031 (Phase 8 ADR shape; the Status / Date / Phase header, the Context / Considered / Decision / Consequences / Alternatives Rejected sections)
    - .planning/phases/09-auto-features-leaderboard/09-CONTEXT.md (D-01..D-06 locked decisions; the three ADRs each codify one decision cluster: D-05 → ADR-032, D-06 → ADR-033, D-01 + D-03 → ADR-034)
    - .planning/phases/09-auto-features-leaderboard/09-RESEARCH.md §"Architecture Patterns" (Pattern 1 / Pattern 2 / Pattern 3 — directly summarized in each ADR's Decision section)
    - .planning/STATE.md (current ADR count 31 — confirms next-free numbers are 032..034 with no collision)
  </read_first>
  <action>
    Create .planning/adrs/ADR-032-light-cqrs-leaderboard-read-model.md following the Phase 8 ADR format:
      - Status: Accepted | Date: 2026-05-30 | Phase: 9
      - Context: REQ-LEAD-01/02 + D-05. Need a 24h rolling top-N leaderboard derived from game events without introducing event sourcing.
      - Considered: (A) Full event sourcing + on-demand fold; (B) Light CQRS with denormalized read model + projector (CHOSEN); (C) On-the-fly aggregation from bets table via JOIN at query time
      - Decision: Light CQRS — denormalized `leaderboard_24h` table + `LeaderboardProjectorService` via `@IdempotentSubscribe` consuming bet.cashed_out + bet.refunded + bet.lost. On-read window filter (no cron). Top-N diff via pure `LeaderboardSnapshot.diff` gates `leaderboard:updated` emit.
      - Consequences: Projector failure does NOT block writes (SC5 proven via integration test from 09-06). Adds 1 table + 1 service + 1 queue + 1 in-process event constant; no event sourcing tax. Throttled WS emits avoid lobby flood.
      - Alternatives Rejected: (A) overengineered for v1 — no audit-trail business need; (C) JOIN-at-query is O(round_count × player_count) — slow at scale even with indexes.
      - References: Plan 09-04 (table + repo), Plan 09-06 (projector), Plan 09-07 (controller), Pitfalls 6, 7, 8, 12.
    Create .planning/adrs/ADR-033-server-enforced-auto-cashout.md:
      - Status: Accepted | Date: 2026-05-30 | Phase: 9
      - Context: REQ-AUTO-01 + D-06. Need server enforcement of player-set auto-cashout target so disconnects don't cost the player. The 30Hz tick loop is the right hook point but the broadcast service must stay single-responsibility.
      - Considered: (A) Inline check in `MultiplierBroadcastService.fireTick()`; (B) In-process `GAME_EVENTS.ROUND_TICK` event from fireTick → new `AutoCashoutTickService` @OnEvent listener (CHOSEN); (C) Per-tick callback on `RoundLoopService`
      - Decision: Option B. `MultiplierBroadcastService.fireTick()` emits in-process ROUND_TICK after volatile.emit; `AutoCashoutTickService` @OnEvent({ async: true }) listens, stamps `acceptedAt = new Date()` as literal first statement (ratifies ADR-023), queries `BetRepository.findAutoCashoutCandidates(roundId, ceilingCentiX)` (single indexed SELECT), invokes existing `CashOutUseCase` with `bet.autoCashoutTarget` as the multiplier (NOT current tick — honors player contract).
      - Consequences: ADR-023's race-resolution invariant generalizes to the tick-loop entry. Broadcast loop never blocks on listener (`{ async: true }`). SC1 disconnect E2E (Plan 09-05) is the recorded proof. The cashout pays the player's target (e.g., 2.0x even if tick reads 2.05x).
      - Alternatives Rejected: (A) couples broadcast SRP + blocks volatile tick on DB query — rejected per CONTEXT canonical-refs; (C) RoundLoopService doesn't tick at 30Hz (transitions BETTING/RUNNING/CRASHED/SETTLED only) — wrong cadence source.
      - References: ADR-023, Plans 09-02 + 09-05, Pitfalls 2, 3, 4, 9, 11.
    Create .planning/adrs/ADR-034-per-session-auto-bet-no-persist.md:
      - Status: Accepted | Date: 2026-05-30 | Phase: 9
      - Context: REQ-AUTO-02/03/04/05 + D-01 + D-03. Auto-bet strategy config + cumulative session P/L stops must work safely. Persisting across reloads could surprise users (continue strategies they don't remember starting); server-side stops add infrastructure tax for marginal UX value.
      - Considered: (A) DB-persisted per-player strategy config + server-enforced stops; (B) Per-session FE Zustand store, NO persist middleware, FE-driven cumulative-since-Start stops (CHOSEN); (C) localStorage-persisted FE config
      - Decision: Option B. Auto-bet state lives in `useAutoBetStore` (Zustand slice-per-concern, NO persist). Stops are cumulative since the Start click; the FE driver computes session P/L from `bet:my_cashed_out`/`bet:my_refunded` events and halts on threshold. Backend stays stateless on stops; the autoCashoutTarget IS server-enforced (separate concern per ADR-033). Manual tab disabled while Auto running (Lock affordance per UI-SPEC).
      - Consequences: Every reload = fresh session; explicit "no auto-bet continuity" UX (defensible: prevents runaway sessions on page reload). Simpler architecture; no inter-session state for the recruiter to ask about. Tradeoff: no auto-bet across browser reloads — accepted.
      - Alternatives Rejected: (A) infra/UX tax + state-sync complexity not justified for play-money; (C) localStorage surprises the user on reload — explicit no-persist is the safer default.
      - References: Plans 09-08 (FE store + driver + form), Pitfall 5 (martingale wallet overflow), Pitfall 10 (driver state syncs from server events).
    Each ADR ~50-80 lines. Reuse the Phase 8 ADR file template verbatim for header / section ordering / phrasing style. NO emojis, NO AI attribution per CLAUDE.md.
  </action>
  <verify>
    <automated>ls .planning/adrs/ADR-032-light-cqrs-leaderboard-read-model.md .planning/adrs/ADR-033-server-enforced-auto-cashout.md .planning/adrs/ADR-034-per-session-auto-bet-no-persist.md &amp;&amp; grep -c "^## " .planning/adrs/ADR-032-light-cqrs-leaderboard-read-model.md</automated>
  </verify>
  <done>
    Three ADRs exist at .planning/adrs/ADR-032..034 with Context / Considered / Decision / Consequences / Alternatives Rejected / References sections. ADR-033 explicitly cites ADR-023 in its Decision + References. No collision with existing ADRs 001..031.
  </done>
</task>

<task type="auto">
  <name>Task 2: STATE/ROADMAP/REQUIREMENTS rotation + Phase 9 closeout</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/STATE.md (current state at end of Phase 8; the "Last updated" footer pattern; the v1-complete count 67/95; the ADR-landed count 31)
    - .planning/ROADMAP.md §"Phase 9: Auto Features & Leaderboard" (the bracket "Plans:" section is empty until this plan; the per-phase Plans Complete table; the anticipated ADR-025/026/027 labels that need a renumber note)
    - .planning/REQUIREMENTS.md §"Phase 9 — Auto Features & Leaderboard (9 reqs)" (the 9 Pending rows that need to be flipped to Done with citations) + the §"Coverage summary" v1-complete bullet that needs the count incremented from 67/95 to 76/95
    - All 9 plan summaries (09-01..09-09) for citation strings
    - The closeout precedent from Phase 8 (P08-10) and Phase 7 (P07-09): use the same pattern for the renumber note + the traceability citation format + the STATE footer update
  </read_first>
  <action>
    Update .planning/STATE.md:
      - Update "Current Position" — Phase 9 to COMPLETE (10/10 plans), 9/10 phases done overall, progress bar `▰▰▰▰▰▰▰▰▰▱` 9/10
      - Update "Performance Metrics" table: Phases complete 8 → 9; v1 requirements complete 67/95 → 76/95; ADRs landed 31 → 34
      - Add a fresh "Next action:" entry pointing to `/gsd:verify-phase 9` + `/gsd:ui-review` then `/gsd:plan-phase 10`
      - Update the "Status:" paragraph with a Phase 9 closeout summary — name each plan's contribution (09-01 env vars + getConfig; 09-02 autoCashoutTarget field + DTO + migration + repo; 09-03 per-bet bet.lost outbox; 09-04 leaderboard_24h table + repo + snapshot VO; 09-05 AutoCashoutTickService + ROUND_TICK + SC1 disconnect E2E; 09-06 LeaderboardProjectorService + WS emit + SC5 chaos; 09-07 LeaderboardController + @crash/contracts/ws payload schema; 09-08 FE auto-bet store + driver + form + tabbed BetPanel; 09-09 FE LeaderboardPanel + tabbed right rail). State the three Phase 9 ADRs at next-free 032..034 and the renumber-note reconciliation (anticipated 025/026/027 stale because Phase 5/6/7/8 ADRs consumed 019..031). State the SC1 + SC5 proofs are recorded as their respective integration/E2E tests.
      - Append a "Last updated" footer entry — match the Phase 8 P08-10 footer style (terse summary of what landed + what changed counts).
    Update .planning/ROADMAP.md:
      - Flip the §Phases checkbox: `- [ ] **Phase 9: Auto Features & Leaderboard**` → `- [x] **Phase 9: Auto Features & Leaderboard**`
      - Inside the Phase 9 §"Phase Details" block, add a "Plans Complete: 10/10" line and a "Plans:" sub-list with `- [x] 09-01-PLAN.md — ...` through `- [x] 09-10-PLAN.md — ...` matching Phase 7/8 closeout style.
      - Add a renumber-note paragraph after the "Key decisions to make" list (mirrors Phase 7/8 renumber notes verbatim): "These three were anticipated under the labels ADR-025/026/027, but Phase 5/6/7/8 ADRs consumed 019..031 before Phase 9 closed. Shipped ADRs are never renumbered; the Phase 9 decisions took the next-free range ADR-032..034: ADR-032 (D-05 light CQRS read-model), ADR-033 (D-06 server-enforced auto-cashout — ratifies ADR-023), ADR-034 (D-01+D-03 per-session auto-bet config — no persist)."
      - Update the §"Progress" table row: `| 9. Auto Features & Leaderboard | 10/10 | Complete | 2026-05-30 |`
    Update .planning/REQUIREMENTS.md:
      - In §"Phase 9 — Auto Features & Leaderboard (9 reqs)" traceability table, flip each row's Status from `Pending` to `Done` with the corresponding plan citation:
        - REQ-AUTO-01 → Done (Plan 09-02 field + Plan 09-05 tick service + SC1 disconnect E2E)
        - REQ-AUTO-02 → Done (Plan 09-08 strategy.ts + tests; Martingale base = configured initial, locked)
        - REQ-AUTO-03 → Done (Plan 09-08 driver stops; FE-only per D-01)
        - REQ-AUTO-04 → Done (Plan 09-08 auto-bet.store has NO persist middleware)
        - REQ-AUTO-05 → Done (Plan 09-08 tabbed BetPanel Manual | Auto + 5-field form)
        - REQ-LEAD-01 → Done (Plan 09-04 leaderboard_24h + 09-06 projector consumes deltas)
        - REQ-LEAD-02 → Done (Plan 09-06 LeaderboardProjectorService via @IdempotentSubscribe + SC5 chaos proof)
        - REQ-LEAD-03 → Done (Plan 09-07 LeaderboardController)
        - REQ-LEAD-04 → Done (Plan 09-09 LeaderboardPanel + ws-dispatch leaderboard:updated + Plan 09-06 server emit + Plan 09-07 contract schema)
      - In the top-of-file checkbox list (§v1 Requirements → Auto Features + Leaderboard sections), flip each of the 9 boxes from `[ ]` to `[x]` and add the same one-line citation suffix (mirrors Phase 7/8 idiom — see how REQ-FE-01..REQ-FE-08 + REQ-FE-09/10 + REQ-REPLAY-* were marked Done).
      - Update §"Coverage summary" → "v1 complete" bullet: increment from 67/95 to 76/95 and add the 9 new REQ-IDs to the listed completions string (mirroring the Phase 8 P08-10 increment style).
      - Update the §Traceability footer "Last updated:" line with a Phase 9 P09-10 closeout summary entry.
    Per CLAUDE.md: no emojis, no AI attribution; commit message in EN matching the existing prefix style (`docs(09-10): close out Phase 9 — three ADRs landed + STATE/ROADMAP/REQUIREMENTS rotation`).
  </action>
  <verify>
    <automated>grep -c "REQ-AUTO-01.*Done" .planning/REQUIREMENTS.md &amp;&amp; grep -c "76 / 95" .planning/REQUIREMENTS.md &amp;&amp; grep -c "9 / 10" .planning/STATE.md &amp;&amp; grep -c "ADR-032\\|ADR-033\\|ADR-034" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    STATE.md reflects Phase 9 COMPLETE + 9/10 phases done + 76/95 v1 reqs + 34 ADRs landed; ROADMAP.md flips the Phase 9 checkbox + adds the 10-plan list + renumber note + Progress table row update; REQUIREMENTS.md flips all 9 Phase 9 traceability rows + 9 checkboxes to Done + increments coverage summary count + footer update; all three files cite the three new ADRs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | No runtime impact; closeout updates are advisory artifacts read by gsd-verify + future planners |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-70 | Repudiation | Phase 9 decisions not documented → future planner re-litigates | mitigate | Three ADRs at 032..034 establish the canon; ADR-033 explicitly ratifies ADR-023 to make the lineage explicit. |
| T-09-71 | Information Disclosure | STATE.md or ROADMAP.md leaks credentials or secrets | accept | Phase 9 closeout text contains no secrets; the demo user `player/player123` is already documented publicly per Phase 1. |
</threat_model>

<verification>
- 3 ADR files exist at .planning/adrs/ADR-032..034
- STATE.md grep `9 / 10` (phases complete) returns ≥ 1 match
- REQUIREMENTS.md grep `REQ-AUTO-01.*Done` returns ≥ 1 match (and similar for the other 8 IDs)
- REQUIREMENTS.md grep `76 / 95` returns ≥ 1 match (coverage count)
- ROADMAP.md grep `09-01-PLAN.md.*✓\|09-01-PLAN.md` returns ≥ 1 match (plan list landed)
- No accidental mutations to ADR-001..031 (they are immutable per CLAUDE.md §ADRs)
- Atomic single commit `docs(09-10): close out Phase 9 — three ADRs landed + STATE/ROADMAP/REQUIREMENTS rotation`
</verification>

<success_criteria>
Phase 9 closed out per the precedent set by Phases 1-8. Project state truthfully reflects 9/10 phases done + 76/95 v1 reqs + 34 ADRs landed + Phase 10 (Quality Hardening) as the next phase. Three new ADRs (032..034) document the locked decisions for future replay. The recruiter's arguição can defend every Phase 9 decision via the ADRs + the inline plan citations.
</success_criteria>

<output>
Create `.planning/phases/09-auto-features-leaderboard/09-10-SUMMARY.md` when done.
</output>
