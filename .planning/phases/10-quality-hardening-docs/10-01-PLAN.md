---
phase: 10-quality-hardening-docs
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - e2e/playwright.config.ts
  - e2e/fixtures/auth.fixture.ts
  - e2e/specs/bet-cashout.spec.ts
  - e2e/specs/bet-crash.spec.ts
  - e2e/.gitignore
  - services/games/.env.example
  - services/games/src/config/defaults.ts
  - services/wallets/.env.example
  - services/wallets/src/config/defaults.ts
  - .env.example
  - .planning/phases/10-quality-hardening-docs/d03a-diagnosis.md
autonomous: false
requirements:
  - REQ-TEST-05
  - REQ-OBS-01
  - REQ-OBS-02
  - REQ-OBS-04

must_haves:
  truths:
    - "Playwright config + auth fixture + 2 spec scaffolds exist (intentionally skipped pending Wave 3 wiring)"
    - "OTel/pino/metrics env vars flow through typed config + .env.example in BOTH services"
    - "Keycloak crash-game realm redirect URIs include http://localhost:3000/* (verified, fix if absent)"
    - "D-03a Sheet/Dialog invisible-after-click root cause diagnosed and recorded BEFORE any fix attempt"
  artifacts:
    - path: "e2e/playwright.config.ts"
      provides: "Playwright test runner config with webServer + storageState"
      contains: "defineConfig"
    - path: "e2e/fixtures/auth.fixture.ts"
      provides: "OIDC PKCE login -> storageState global setup"
      contains: "storageState"
    - path: "e2e/specs/bet-cashout.spec.ts"
      provides: "REQ-TEST-05(a) spec scaffold (test.skip until Wave 3)"
      contains: "test.skip"
    - path: "e2e/specs/bet-crash.spec.ts"
      provides: "REQ-TEST-05(b) spec scaffold (test.skip until Wave 3)"
      contains: "test.skip"
    - path: "services/games/src/config/defaults.ts"
      provides: "Typed env vars: OTEL_EXPORTER_OTLP_ENDPOINT, OTEL_SERVICE_NAME, LOG_LEVEL, CRASH_RTP_WINDOW_ROUNDS"
      contains: "OTEL_EXPORTER_OTLP_ENDPOINT"
    - path: "services/wallets/src/config/defaults.ts"
      provides: "Typed env vars: OTEL_EXPORTER_OTLP_ENDPOINT, OTEL_SERVICE_NAME, LOG_LEVEL"
      contains: "OTEL_EXPORTER_OTLP_ENDPOINT"
    - path: ".planning/phases/10-quality-hardening-docs/d03a-diagnosis.md"
      provides: "Live-DOM diagnosis report ranking the 3 hypotheses with observed evidence"
      contains: "Root cause:"
  key_links:
    - from: "e2e/playwright.config.ts"
      to: "e2e/fixtures/auth.fixture.ts"
      via: "globalSetup field"
      pattern: "globalSetup.*auth.fixture"
    - from: "services/games/src/config/defaults.ts"
      to: ".env.example"
      via: "every new key documented at repo root"
      pattern: "OTEL_EXPORTER_OTLP_ENDPOINT"
---

<objective>
Lay the Phase 10 Wave 0 scaffolding so downstream waves do not block on missing test infrastructure, missing env-var typing, or undiagnosed polish-defect root causes. Three concerns: (1) Playwright project layout + auth fixture + 2 failing/skipped spec scaffolds satisfy the Nyquist rule (`<verify><automated>` targets pre-exist for later waves). (2) New OTel/pino/metrics env vars land in BOTH services' typed `defaults.ts` + `.env.example` BEFORE any code consumes them — eliminates "config missing" surprises in Wave 1. (3) The D-03a Sheet/Dialog invisible-after-click defect gets a "live diagnose first" task (per RESEARCH Assumption A1) so Wave 3 applies the correct fix rather than guessing.

Purpose: Unblock Waves 1-3. Eliminate "missing fixture / env / diagnosis" rework. Lock the Keycloak realm redirect URI assumption (Pitfall 5) before Wave 3 Playwright authors the spec body.
Output: Playwright project tree (config + fixture + 2 skipped specs) + extended env-var schemas in both services + D-03a diagnosis report with concrete root cause + (optional) Keycloak realm patch if redirect URIs are missing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-quality-hardening-docs/10-CONTEXT.md
@.planning/phases/10-quality-hardening-docs/10-RESEARCH.md
@CLAUDE.md
@services/games/src/config/defaults.ts
@services/wallets/src/config/defaults.ts
@docker-compose.yml
@frontend/src/components/verification-drawer.tsx
@frontend/src/components/replay-modal.tsx
@frontend/src/components/ui/sheet.tsx
@frontend/src/components/ui/dialog.tsx
@frontend/src/styles
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend typed config and .env.example in BOTH services with OTel/pino/metrics env vars</name>
  <files>services/games/src/config/defaults.ts, services/games/.env.example, services/wallets/src/config/defaults.ts, services/wallets/.env.example, .env.example</files>
  <read_first>
    services/games/src/config/defaults.ts (current zod schema + how env keys are typed and exported — Phase 7 + Phase 9 added prior keys following the same pattern)
    services/wallets/src/config/defaults.ts (mirror structure)
    .env.example (repo-root canonical env documentation table)
    .planning/REQUIREMENTS.md "Open Configuration Values" table (no Phase 10 keys yet — this task adds them)
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Project Constraints" (per CLAUDE.md: every observability env var MUST flow through config/defaults.ts zod schema and live in .env.example)
  </read_first>
  <action>
    Extend the zod schema in BOTH `services/games/src/config/defaults.ts` and `services/wallets/src/config/defaults.ts` to type the following keys (with defaults that work for local docker:up out of the box per D-05/D-06):

    Games + Wallets (both services):
    - `OTEL_EXPORTER_OTLP_ENDPOINT` (string URL, default `http://jaeger:4318/v1/traces`)
    - `OTEL_SERVICE_NAME` (string, default `games-service` / `wallets-service` respectively)
    - `LOG_LEVEL` (z.enum trace/debug/info/warn/error/fatal, default `info`)

    Games-only (custom metric tuning per D-06 and RESEARCH Open Q2):
    - `CRASH_RTP_WINDOW_ROUNDS` (z.coerce.number int positive, default 100)

    Each key follows the established `defaults.ts` parse-on-boot pattern. Export through the existing typed `env` object. Mirror entries into each service's `.env.example` AND the repo-root `.env.example` (canonical "Open Configuration Values" surface — append a new OTEL/LOGS section). Use the same comment style + section header as existing groups.

    Do NOT consume these env vars anywhere yet — Wave 1 plan 10-03 will read `OTEL_EXPORTER_OTLP_ENDPOINT` + `OTEL_SERVICE_NAME` in `tracing.ts` and `LOG_LEVEL` in `observability.module.ts`; Wave 2 plan 10-05 will read `CRASH_RTP_WINDOW_ROUNDS` in the RTP metric.

    Per CLAUDE.md "no hardcoded business constants" — every Wave-1+ consumer reads from `env.*`, never from a literal.
  </action>
  <verify>
    <automated>cd services/games && bun tsc --noEmit && grep -q OTEL_EXPORTER_OTLP_ENDPOINT src/config/defaults.ts && grep -q CRASH_RTP_WINDOW_ROUNDS src/config/defaults.ts && cd ../wallets && bun tsc --noEmit && grep -q OTEL_EXPORTER_OTLP_ENDPOINT src/config/defaults.ts && cd ../.. && grep -q OTEL_EXPORTER_OTLP_ENDPOINT .env.example</automated>
  </verify>
  <done>Both services' typed env schemas parse OTEL_EXPORTER_OTLP_ENDPOINT + OTEL_SERVICE_NAME + LOG_LEVEL; games also parses CRASH_RTP_WINDOW_ROUNDS; defaults work for local docker compose with jaeger hostname; all three .env.example files updated with section comments; both services typecheck clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Diagnose D-03a Sheet/Dialog invisible-after-click root cause LIVE before any fix</name>
  <what-built>RESEARCH Pitfall 6 + Assumption A1 require LIVE DOM diagnosis BEFORE applying any fix to Wave 3 plan 10-06. Three ranked hypotheses (per probability): (a) missing `tw-animate-css` Tailwind v4 plugin OR pruned `data-[state=open]:*` utility classes; (b) z-index collision with game canvas / Phase 7 D-01 sticky bottom rail; (c) shadcn sheet.tsx defaults out of sync. Without this diagnosis, Wave 3 risks applying the wrong fix.

  This task is a developer-run diagnosis step. Claude has automated everything pre-checkpoint that it can — created the env scaffolding, the Playwright skeleton, etc. The browser DOM inspection must be performed by the developer because Claude cannot drive `bun run dev` + Chrome DevTools + click + inspect computed styles in this environment.</what-built>
  <how-to-verify>
    1. From repo root: `bun run docker:up --wait` then in second terminal `cd frontend && bun run dev` (assumes both come up green per the existing Phase 7 quickstart).
    2. Open http://localhost:3000 in Chrome, log in as `player/player123`.
    3. Open Chrome DevTools (Cmd+Option+I). Switch to the Console tab. Clear it.
    4. **Test the Fairness drawer (Sheet)**: Click the "Fairness" badge in the header.
       - Expected: drawer slides in from the right.
       - Observed: drawer state changes but content does not appear visually.
    5. In DevTools Elements panel, expand the body and locate the rendered `<div data-state="open" data-side="right" ...>` (it WILL be in the DOM — only invisible). Note its full class list.
    6. With that element selected, inspect the Computed panel:
       - What is `transform`? If still `translate(100%, 0)` or `translate3d(100%, 0, 0)`, hypothesis (a) is correct — `slide-in-from-right` animation class never applied.
       - What is `z-index`? If anything in the game shell sits above it (z > 50), hypothesis (b) is correct.
       - Search Console for any "Unknown utility class" or "could not resolve" warnings — those confirm (a).
    7. **Repeat steps 4-6 for the Replay modal (Dialog)** — click any history strip entry's Replay button. Note the same computed style observations.
    8. **Inspect `frontend/src/styles/app.css`** (or wherever the global CSS imports live): does it contain `@plugin "tw-animate-css";`? Run `grep -r "tw-animate-css\|@plugin" frontend/src/styles/` to confirm.
    9. Write the diagnosis findings to `.planning/phases/10-quality-hardening-docs/d03a-diagnosis.md` with this structure:
       - **Observed (Sheet)**: <DOM state, computed transform, computed z-index, console warns>
       - **Observed (Dialog)**: <same>
       - **app.css `tw-animate-css` plugin present?**: yes / no / path
       - **Ranked verdict**: which hypothesis (a) / (b) / (c) the evidence supports
       - **Root cause**: <one-line plain-English root cause>
       - **Recommended fix for Wave 3 plan 10-06**: <single concrete action>
    10. Commit the diagnosis file: `docs(10-01): D-03a Sheet/Dialog root-cause diagnosis`.

    Resume only after diagnosis file is committed and root cause is concrete (not "unclear" / "could be any of three").
  </how-to-verify>
  <resume-signal>Type "approved" once .planning/phases/10-quality-hardening-docs/d03a-diagnosis.md is committed with a concrete root cause. Wave 3 plan 10-06 will read this file as its primary source.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create Playwright project tree (config + auth fixture + 2 spec scaffolds) and verify Keycloak realm redirect URIs</name>
  <files>e2e/playwright.config.ts, e2e/fixtures/auth.fixture.ts, e2e/specs/bet-cashout.spec.ts, e2e/specs/bet-crash.spec.ts, e2e/.gitignore</files>
  <read_first>
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md (Pattern 4 Playwright config with webServer + storageState; Pattern "Playwright OIDC login fixture (storageState pattern)"; Pitfall 5 OIDC redirect CORS — verify Keycloak realm BEFORE writing spec body; Validation Architecture Wave 0 Gaps)
    docker/keycloak/<realm>.json or docker/keycloak/realms/<realm>.json (Phase 1 realm import — list of allowed redirect URIs; planner-side assumption A7 says `http://localhost:3000/*` is present)
    frontend/package.json (confirm `bun run dev` boots on port 3000)
    package.json root (confirm `bun run docker:up` exists)
  </read_first>
  <action>
    1. Create `e2e/` directory at repo root (NOT under frontend/) per D-07 Claude's-discretion call — keeps Playwright a peer of services + frontend per RESEARCH Recommended Project Structure.

    2. `e2e/playwright.config.ts`: `defineConfig` with `testDir: ./specs`, `fullyParallel: false`, `workers: 1` (saga state is shared — serialize), `forbidOnly: !!process.env.CI`, `retries: process.env.CI ? 1 : 0`, `globalSetup: ./fixtures/auth.fixture.ts`, `use: { baseURL: "http://localhost:3000", trace: "on-first-retry", video: "retain-on-failure", screenshot: "only-on-failure" }`, `projects: [{ name: "chromium", use: { ...devices.Desktop Chrome, storageState: STORAGE_STATE_PATH } }]`, `webServer: { command: "cd frontend && bun run dev", url: "http://localhost:3000", reuseExistingServer: !process.env.CI, timeout: 120_000 }`, `reporter: process.env.CI ? [["github"], ["html", { open: "never" }]] : "list"`. Per D-07 the CI workflow boots docker:up BEFORE Playwright; the webServer block ONLY boots the FE dev server.

    3. `e2e/fixtures/auth.fixture.ts`: exports `STORAGE_STATE_PATH = join(__dirname, "../.auth/player.json")` and a default-exported `globalSetup` async fn matching RESEARCH "Playwright OIDC login fixture" snippet. Drives `chromium.launch()` -> `page.goto("http://localhost:3000")` -> `page.waitForURL(/\/realms\/crash-game\/protocol\/openid-connect\/auth/, { timeout: 30_000 })` -> `page.fill('input[name="username"]', "player")` -> `page.fill('input[name="password"]', "player123")` -> `page.click('input[type="submit"]')` -> `page.waitForURL("http://localhost:3000/", { timeout: 30_000 })` -> `page.context().storageState({ path: STORAGE_STATE_PATH })` -> close browser. Use the Phase 1 demo credentials per CONTEXT canonical-refs Keycloak section.

    4. `e2e/specs/bet-cashout.spec.ts`: import `test` from `@playwright/test`, write a single `test.skip("login -> BETTING -> bet -> RUNNING -> cashout -> balance updated [Wave 3]", async ({ page }) => {})` body referencing the RESEARCH Pattern "Playwright OIDC login fixture" usage idiom. Wave 3 plan 10-07 will replace `test.skip` with `test` and add the body. Document the deferred implementation in a top-of-file comment naming plan 10-07.

    5. `e2e/specs/bet-crash.spec.ts`: same skip-scaffold for "login -> bet -> wait crash -> bet lost [Wave 3]".

    6. `e2e/.gitignore`: ignore `.auth/` (storageState OIDC tokens — must not be committed per Pitfall 5 mitigation) and `playwright-report/` and `test-results/`.

    7. Verify Keycloak realm allowed redirect URIs (Pitfall 5). Read `docker/keycloak/realms/*.json` (or wherever realm is — find with `find docker/keycloak -name '*.json'`). Grep for `redirectUris` blocks and confirm `http://localhost:3000/*` AND `http://localhost:3000/silent-sso.html` (or equivalent oidc-spa silent-renew callback) are present. If absent, add them in-place and document the patch in the commit message — keep this realm-edit atomic and reversible.

    Do NOT add `playwright` package install in this task — that lands in the package-legitimacy + install plan 10-02. `@playwright/test` is referenced via type-only import here; tsc may complain until 10-02 installs the package. That is expected — the scaffold ships intentionally pending install (the next plan in this same wave).
  </action>
  <verify>
    <automated>test -f e2e/playwright.config.ts && test -f e2e/fixtures/auth.fixture.ts && test -f e2e/specs/bet-cashout.spec.ts && test -f e2e/specs/bet-crash.spec.ts && grep -q "test.skip" e2e/specs/bet-cashout.spec.ts && grep -q "test.skip" e2e/specs/bet-crash.spec.ts && grep -q "STORAGE_STATE_PATH" e2e/fixtures/auth.fixture.ts && grep -q "webServer" e2e/playwright.config.ts && grep -q ".auth/" e2e/.gitignore && find docker/keycloak -name "*.json" -exec grep -l "localhost:3000" {} \; | head -1</automated>
  </verify>
  <done>e2e/ project tree exists with config + auth fixture + 2 skipped spec scaffolds; .gitignore excludes .auth/ + reports; Keycloak realm verified or patched to include localhost:3000 redirect URIs; tsc errors on `@playwright/test` are EXPECTED and will resolve in plan 10-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Keycloak (during Playwright auth fixture) | OIDC PKCE redirect; storageState captures OIDC tokens which MUST NOT be committed |
| Repo file -> CI runner | .env.example is template only; no real secrets land in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | e2e/.auth/player.json (OIDC tokens) | mitigate | e2e/.gitignore excludes .auth/; storageState path is local-only; never written under tracked paths |
| T-10-02 | Tampering | Keycloak realm allowed redirect URIs | mitigate | Realm patch (if needed) limits to fixed http://localhost:3000/* — no wildcards beyond the dev host |
| T-10-03 | Information Disclosure | services/*/.env.example | accept | Templates contain only defaults (jaeger:4318, info log level) — no secrets |
</threat_model>

<verification>
- `bun --cwd services/games tsc --noEmit` and `bun --cwd services/wallets tsc --noEmit` both clean
- `grep -q OTEL_EXPORTER_OTLP_ENDPOINT services/games/src/config/defaults.ts services/wallets/src/config/defaults.ts .env.example`
- `grep -q CRASH_RTP_WINDOW_ROUNDS services/games/src/config/defaults.ts`
- `test -f e2e/playwright.config.ts && test -f e2e/fixtures/auth.fixture.ts && test -f e2e/specs/bet-cashout.spec.ts && test -f e2e/specs/bet-crash.spec.ts`
- `test -f .planning/phases/10-quality-hardening-docs/d03a-diagnosis.md && grep -qi "root cause" .planning/phases/10-quality-hardening-docs/d03a-diagnosis.md`
- `find docker/keycloak -name "*.json" -exec grep -l "localhost:3000" {} \;` returns the realm file path
</verification>

<success_criteria>
1. Plan 10-02 can install @playwright/test and immediately have a valid project tree to anchor against.
2. Plan 10-03 reads `env.OTEL_EXPORTER_OTLP_ENDPOINT` and `env.OTEL_SERVICE_NAME` from already-typed schemas; no "missing env" errors.
3. Plan 10-06 reads `d03a-diagnosis.md` as its single source of truth for the D-03a fix; no guessing between 3 hypotheses.
4. Plan 10-07 replaces test.skip with test bodies in the existing spec files; no scaffold work in Wave 3.
</success_criteria>

<output>
Create `.planning/phases/10-quality-hardening-docs/10-01-SUMMARY.md` when done. Cite the d03a-diagnosis.md root cause and the Keycloak realm verification result (verified-as-is vs patched).
</output>
