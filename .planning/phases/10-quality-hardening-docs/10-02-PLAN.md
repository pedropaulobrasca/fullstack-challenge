---
phase: 10-quality-hardening-docs
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - services/games/package.json
  - services/games/bun.lock
  - services/wallets/package.json
  - services/wallets/bun.lock
  - frontend/package.json
  - frontend/bun.lock
autonomous: false
requirements:
  - REQ-TEST-05
  - REQ-OBS-01
  - REQ-OBS-02
  - REQ-OBS-04

must_haves:
  truths:
    - "All 13 [ASSUMED] packages gate-verified via blocking-human checkpoint BEFORE install"
    - "Scoped @willsoto/nestjs-prometheus installed; unscoped nestjs-prometheus stale package is NOT installed"
    - "All OTel + pino + Prometheus packages present at pinned versions in BOTH services"
    - "@playwright/test installed under frontend (chromium browser downloaded via bunx playwright install --with-deps chromium)"
  artifacts:
    - path: "services/games/package.json"
      provides: "OTel SDK + nestjs-otel + nestjs-pino + @willsoto/nestjs-prometheus deps"
      contains: "@willsoto/nestjs-prometheus"
    - path: "services/wallets/package.json"
      provides: "Mirror set"
      contains: "@willsoto/nestjs-prometheus"
    - path: "frontend/package.json"
      provides: "@playwright/test devDependency"
      contains: "@playwright/test"
  key_links:
    - from: "services/games/package.json"
      to: "node_modules/@willsoto/nestjs-prometheus"
      via: "bun install"
      pattern: "@willsoto/nestjs-prometheus"
---

<objective>
Install all Phase 10 npm dependencies (13 [ASSUMED] packages) in BOTH services + Playwright in the frontend, gated by a blocking-human Package Legitimacy checkpoint per the GSD package-legitimacy gate protocol (slopcheck unavailable at research time per RESEARCH "Package Legitimacy Audit"). Critical distinction enforced: install the SCOPED `@willsoto/nestjs-prometheus@^11.0.0`, NOT the unscoped stale `nestjs-prometheus` package.

Purpose: Wave 1 (OTel SDK + pino) and Wave 2 (metrics) cannot import packages that are not installed. This plan removes that block.
Output: Updated package.json + bun.lock in both services + frontend; Playwright chromium browser installed locally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-quality-hardening-docs/10-RESEARCH.md
@.planning/phases/10-quality-hardening-docs/10-CONTEXT.md
@CLAUDE.md
@services/games/package.json
@services/wallets/package.json
@frontend/package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package Legitimacy Gate — verify all 13 [ASSUMED] packages before install (slopcheck unavailable)</name>
  <what-built>RESEARCH "Package Legitimacy Audit" tagged all 13 packages as [ASSUMED] because slopcheck was not available in the agent environment. Per the GSD package-legitimacy gate protocol, every [ASSUMED] package MUST be human-verified BEFORE install. Two highest-risk distinctions:

  1. `@willsoto/nestjs-prometheus` (SCOPED, v11) vs `nestjs-prometheus` (unscoped, STALE 2024) — RESEARCH "Common Pitfalls / Standard Stack / Alternatives Considered" repeats this 3 times. Installing the wrong one wastes downstream wave time.
  2. `nestjs-otel` (single-maintainer pragmaticivan, ~100k/wk downloads) — well-known NestJS community package; verify the GitHub repo + latest release date.

  T-10-SC (supply-chain Tampering threat) is mitigated by this checkpoint. The other 11 packages are CNCF-graduated (OpenTelemetry org) or industry-standard (pino, prom-client, microsoft/playwright).</what-built>
  <how-to-verify>
    Open each registry page in a browser and confirm: (a) package author/org matches the expected name, (b) latest version matches the pinned version in 10-RESEARCH.md, (c) weekly downloads are not orders of magnitude below the RESEARCH-cited number, (d) the source repo link goes to the expected GitHub org.

    1. https://www.npmjs.com/package/@opentelemetry/sdk-node — org `@opentelemetry`, repo `open-telemetry/opentelemetry-js`, expect ~5M+/wk downloads, version `0.218.0` available.
    2. https://www.npmjs.com/package/@opentelemetry/api — same org, repo `open-telemetry/opentelemetry-js`, ~35M+/wk, `1.9.1`.
    3. https://www.npmjs.com/package/@opentelemetry/auto-instrumentations-node — repo `open-telemetry/opentelemetry-js-contrib`, `0.76.0`.
    4. https://www.npmjs.com/package/@opentelemetry/exporter-trace-otlp-http — same org, `0.218.0`.
    5. https://www.npmjs.com/package/@opentelemetry/resources — same org, `2.1.0`.
    6. https://www.npmjs.com/package/@opentelemetry/semantic-conventions — same org, `1.41.1`.
    7. https://www.npmjs.com/package/nestjs-otel — maintainer `pragmaticivan`, repo `pragmaticivan/nestjs-otel`, `6.1.0`, active in last 6 months (verify the "Last publish" timestamp).
    8. https://www.npmjs.com/package/pino — repo `pinojs/pino`, `10.3.1`.
    9. https://www.npmjs.com/package/nestjs-pino — maintainer `iamolegga`, repo `iamolegga/nestjs-pino`, `4.6.1`.
    10. https://www.npmjs.com/package/@willsoto/nestjs-prometheus — SCOPED package, maintainer `willsoto`, repo `willsoto/nestjs-prometheus`, `11.0.0`. Confirm the URL bar shows the `@willsoto/` prefix. DO NOT confuse with https://www.npmjs.com/package/nestjs-prometheus (unscoped, last release 2024 — STALE).
    11. https://www.npmjs.com/package/prom-client — repo `siimon/prom-client`, `15.1.3`.
    12. https://www.npmjs.com/package/@playwright/test — official Microsoft Playwright, repo `microsoft/playwright`, `1.60.0`.

    If any package fails verification (wrong maintainer, very low downloads, suspicious recent ownership change, name typo-squat), STOP. Report the finding and discuss replacement before resume.
  </how-to-verify>
  <resume-signal>Type "approved" once all 12 registry pages confirm expected maintainer + repo + downloads + version. Reply with the specific concern + package name if any fails.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install OTel + pino + nestjs-otel + @willsoto/nestjs-prometheus in BOTH services and @playwright/test in frontend</name>
  <files>services/games/package.json, services/games/bun.lock, services/wallets/package.json, services/wallets/bun.lock, frontend/package.json, frontend/bun.lock</files>
  <read_first>
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Standard Stack" — exact package names + pinned versions + the installation block at the end
    services/games/package.json (current dep shape; do NOT bump existing deps; ADDITIVE only)
    services/wallets/package.json (mirror)
    frontend/package.json (current devDeps)
  </read_first>
  <action>
    From `services/games/` run a single `bun add` for the 11 runtime packages at pinned versions per RESEARCH §"Installation":

    `bun add @opentelemetry/sdk-node@^0.218.0 @opentelemetry/api@^1.9.1 @opentelemetry/auto-instrumentations-node@^0.76.0 @opentelemetry/exporter-trace-otlp-http@^0.218.0 @opentelemetry/resources@^2.1.0 @opentelemetry/semantic-conventions@^1.41.1 nestjs-otel@^6.1.0 nestjs-pino@^4.6.1 pino@^10.3.1 @willsoto/nestjs-prometheus@^11.0.0 prom-client@^15.1.3`

    Then from `services/wallets/` run the IDENTICAL `bun add` command — same package set, same pinned versions, so both services share the same OTel/pino/metrics surface.

    Then from `frontend/` run: `bun add -d @playwright/test@^1.60.0` (devDep — only Playwright; do not install OTel/pino in the frontend).

    Then from `frontend/`: `bunx playwright install --with-deps chromium` to download the chromium browser into Playwright's cache (required by Wave 3 plan 10-07 spec runs and by the CI workflow in plan 10-08).

    Critical: install ONLY the scoped `@willsoto/nestjs-prometheus` — the unscoped `nestjs-prometheus` is STALE per RESEARCH "Alternatives Considered" / "Common Pitfalls" / "Critical disambiguation". If a typo lands the unscoped one, `bun remove nestjs-prometheus` + reinstall scoped.

    Run `bun install` per workspace if bun.lock needs reconciliation. Verify no other deps changed via `git diff services/games/package.json` and `git diff services/wallets/package.json` — both diffs should be ADDITIVE only.

    Per CLAUDE.md "atomic commits per logical change" — split into 3 commits: (a) games install, (b) wallets install, (c) frontend Playwright install. Each commit message in the project's existing prefix style (`chore(games-10-02): install OTel + pino + Prometheus deps` etc.). NO AI attribution.
  </action>
  <verify>
    <automated>cd services/games && grep -q '"@willsoto/nestjs-prometheus":' package.json && grep -q '"nestjs-pino":' package.json && grep -q '"@opentelemetry/sdk-node":' package.json && ! grep -q '"nestjs-prometheus":' package.json && cd ../wallets && grep -q '"@willsoto/nestjs-prometheus":' package.json && grep -q '"nestjs-pino":' package.json && grep -q '"@opentelemetry/sdk-node":' package.json && cd ../../frontend && grep -q '"@playwright/test":' package.json && bunx playwright --version</automated>
  </verify>
  <done>All 11 OTel/pino/Prometheus packages installed in BOTH services; the unscoped nestjs-prometheus is NOT present in either package.json (grep -v confirms); @playwright/test installed under frontend; `bunx playwright --version` prints a version; bun.lock updated in 3 workspaces; 3 atomic commits land.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local node_modules | Supply-chain trust boundary — install only audited package names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SC | Tampering | npm package installs | mitigate | Blocking-human Package Legitimacy checkpoint verifies maintainer + repo + downloads + version for every [ASSUMED] package BEFORE install. workflow.auto_advance is IGNORED for this checkpoint per GSD spec. |
| T-10-04 | Tampering | nestjs-prometheus typo-squat (unscoped stale) | mitigate | Task action repeats the SCOPED package name 3 times; verify grep gate runs `! grep nestjs-prometheus` (without scope) returning success |
</threat_model>

<verification>
- `grep -q '@willsoto/nestjs-prometheus' services/games/package.json services/wallets/package.json`
- `! grep -E '"nestjs-prometheus":' services/games/package.json services/wallets/package.json` (unscoped MUST NOT be present)
- `grep -q '@opentelemetry/sdk-node' services/games/package.json services/wallets/package.json`
- `grep -q '@playwright/test' frontend/package.json`
- `bunx playwright --version` succeeds
- Three commits land: games install, wallets install, frontend Playwright install
</verification>

<success_criteria>
1. Wave 1 plan 10-03 can `import { NodeSDK } from "@opentelemetry/sdk-node"` and `import { LoggerModule } from "nestjs-pino"` in both services.
2. Wave 2 plan 10-05 can `import { makeCounterProvider, InjectMetric } from "@willsoto/nestjs-prometheus"` in games.
3. Wave 3 plan 10-07 can `bunx playwright test --config=e2e/playwright.config.ts` against the existing skipped spec scaffolds.
</success_criteria>

<output>
Create `.planning/phases/10-quality-hardening-docs/10-02-SUMMARY.md` when done. Cite the Package Legitimacy verification outcome (all 12 approved / any flagged) and confirm the SCOPED-vs-unscoped distinction was honored.
</output>
