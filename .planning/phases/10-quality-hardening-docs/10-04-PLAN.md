---
phase: 10-quality-hardening-docs
plan: 04
type: execute
wave: 2
depends_on:
  - 10-03
files_modified:
  - docker-compose.yml
  - docker/prometheus/prometheus.yml
  - docker/grafana/provisioning/datasources/prometheus.yml
  - docker/grafana/provisioning/datasources/jaeger.yml
  - docker/grafana/provisioning/dashboards/dashboards.yml
  - docker/grafana/provisioning/dashboards/games-service.json
  - docker/grafana/provisioning/dashboards/wallets-service.json
  - docker/grafana/provisioning/dashboards/crash-domain.json
  - scripts/smoke-health.sh
autonomous: true
requirements:
  - REQ-OBS-03

must_haves:
  truths:
    - "bun run docker:up brings up Jaeger + Prometheus + Grafana healthy alongside existing stack"
    - "Prometheus successfully scrapes games:4001/metrics + wallets:4002/metrics every 15s after Wave 2 plan 10-05 mounts the endpoints"
    - "Grafana opens at http://localhost:3001 with Prometheus + Jaeger datasources pre-provisioned (UID-pinned per Pitfall 4) and 3 dashboards loaded"
    - "Jaeger UI at http://localhost:16686 receives OTLP HTTP spans on port 4318 from both services (visible after Wave 1 plan 10-03 init)"
    - "smoke-health.sh extended with probes 45-47 covering the 3 new containers"
  artifacts:
    - path: "docker-compose.yml"
      provides: "jaeger + prometheus + grafana service blocks with healthchecks + depends_on"
      contains: "jaegertracing/all-in-one"
    - path: "docker/prometheus/prometheus.yml"
      provides: "Static scrape configs for games + wallets"
      contains: "static_configs"
    - path: "docker/grafana/provisioning/datasources/prometheus.yml"
      provides: "Prometheus datasource with pinned UID prometheus-main"
      contains: "uid: prometheus-main"
    - path: "docker/grafana/provisioning/dashboards/crash-domain.json"
      provides: "Custom Crash domain dashboard (bet_volume, RTP, multiplier_drift, ws_*)"
      contains: "crash_bet_volume_total"
    - path: "scripts/smoke-health.sh"
      provides: "Probes 45-47 for jaeger + prometheus + grafana"
      contains: "16686"
  key_links:
    - from: "docker/prometheus/prometheus.yml"
      to: "services games + wallets /metrics"
      via: "scrape target hostnames games:4001 + wallets:4002"
      pattern: "targets:"
    - from: "docker/grafana/provisioning/dashboards/*.json"
      to: "docker/grafana/provisioning/datasources/prometheus.yml"
      via: "datasource uid prometheus-main"
      pattern: "prometheus-main"
---

<objective>
Bake the observability infrastructure (Jaeger + Prometheus + Grafana) into the docker-compose stack with pre-provisioned datasources + dashboards so `bun run docker:up` brings up the full observability surface zero-config. Land the 3 new containers, the Prometheus scrape config, the Grafana provisioning tree (datasources + dashboard provider + 3 dashboards), and extend the smoke-health.sh probe suite to 47 probes covering the new endpoints.

Purpose: REQ-OBS-03 + D-01 + D-06 — Jaeger receives the OTLP traces emitted by Wave 1 plan 10-03 immediately; Prometheus stands ready to scrape `/metrics` from both services as soon as Wave 2 plan 10-05 lands the endpoint; Grafana datasources + dashboards work on first boot per Pitfall 4 (datasource UID pinning).
Output: 3 new compose services with healthchecks + depends_on; prometheus.yml with both service scrape targets; 2 Grafana datasource YAMLs (Prometheus + Jaeger) with explicit UIDs; 1 dashboard provider YAML + 3 dashboard JSONs (games-service, wallets-service, crash-domain); smoke probes 45-47.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-quality-hardening-docs/10-RESEARCH.md
@.planning/phases/10-quality-hardening-docs/10-CONTEXT.md
@.planning/phases/10-quality-hardening-docs/10-03-SUMMARY.md
@CLAUDE.md
@docker-compose.yml
@scripts/smoke-health.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Jaeger + Prometheus + Grafana service blocks to docker-compose.yml with healthchecks + depends_on</name>
  <files>docker-compose.yml, docker/prometheus/prometheus.yml</files>
  <read_first>
    docker-compose.yml (existing service blocks — note healthcheck patterns, depends_on patterns from Phase 1; observe the FE port 3000 conflict that drives Grafana to 3001 per RESEARCH Pitfall "Mounting Grafana on port 3000")
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Code Examples / docker-compose additions" — canonical Jaeger + Prometheus + Grafana blocks with healthchecks, ports, env vars, depends_on, image tags
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Common Pitfalls #3" — Prometheus scraper hits services before healthy → depends_on with service_healthy required
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Pitfall — Mounting Grafana on port 3000" — collides with FE :3000; use `3001:3000` mapping
  </read_first>
  <action>
    Append 3 new service blocks to `docker-compose.yml` (do NOT touch existing service blocks — additive only):

    **jaeger** per D-01 (RESEARCH "Code Examples / docker-compose additions"):
    - image `jaegertracing/all-in-one:1.63` (PINNED — not `:latest`; RESEARCH "Standard Stack / Supporting" cites 1.63 stable)
    - ports `16686:16686` (UI) + `4318:4318` (OTLP HTTP receiver)
    - env `COLLECTOR_OTLP_ENABLED: "true"` (required to enable the OTLP HTTP listener on 4318 per Assumption A5)
    - healthcheck `wget -qO- http://localhost:14269/` (Jaeger admin port) every 10s, 5s timeout, 10 retries

    **prometheus** per D-06:
    - image `prom/prometheus:v3.0.1` (PINNED)
    - port `9090:9090`
    - volume mount `./docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro`
    - depends_on `games: { condition: service_healthy }` AND `wallets: { condition: service_healthy }` per Pitfall 3
    - healthcheck `wget -qO- http://localhost:9090/-/healthy` every 10s

    **grafana** per D-06:
    - image `grafana/grafana:11.3.1` (PINNED)
    - port `3001:3000` per Pitfall — avoid FE :3000 collision
    - env `GF_AUTH_ANONYMOUS_ENABLED: "true"`, `GF_AUTH_ANONYMOUS_ORG_ROLE: "Viewer"` (recruiter-friendly read-only per Open Question 9 + Security T-10 EoP mitigation), `GF_USERS_DEFAULT_THEME: "dark"`
    - volume mount `./docker/grafana/provisioning:/etc/grafana/provisioning:ro`
    - depends_on `prometheus: { condition: service_healthy }`
    - healthcheck `wget -qO- http://localhost:3000/api/health | grep -q ok` every 10s

    Create `docker/prometheus/prometheus.yml` with:
    - `global: { scrape_interval: 15s, scrape_timeout: 10s }`
    - `scrape_configs:` two jobs `games-service` and `wallets-service` with `static_configs: [{ targets: ["games:4001"] }]` and `["wallets:4002"]` respectively (in-compose hostnames; Prometheus runs inside the same docker network), `metrics_path: /metrics` on each.

    Do NOT mount Kong route for `/metrics` — RESEARCH Security V13 confirms metrics endpoint is internal-network only; Prometheus scrapes via docker network, never via Kong public gateway.

    The frontend container stays out of compose per RESEARCH Open Question 5 / Phase 7 boundary — do NOT add it here.
  </action>
  <verify>
    <automated>bun run docker:up --wait && curl -sf http://localhost:9090/-/healthy && curl -sf http://localhost:16686/ -o /dev/null && curl -sf http://localhost:3001/api/health | grep -q ok && docker compose ps prometheus jaeger grafana | grep -c healthy | grep -q '^3$'</automated>
  </verify>
  <done>3 new containers boot healthy via `docker:up`; Prometheus + Jaeger + Grafana reachable on their host ports; FE port 3000 unaffected; existing services unmodified in compose file (verified via diff).</done>
</task>

<task type="auto">
  <name>Task 2: Pre-provision Grafana datasources (UID-pinned per Pitfall 4) + 3 dashboards (games + wallets + crash-domain)</name>
  <files>docker/grafana/provisioning/datasources/prometheus.yml, docker/grafana/provisioning/datasources/jaeger.yml, docker/grafana/provisioning/dashboards/dashboards.yml, docker/grafana/provisioning/dashboards/games-service.json, docker/grafana/provisioning/dashboards/wallets-service.json, docker/grafana/provisioning/dashboards/crash-domain.json</files>
  <read_first>
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Common Pitfalls #4" — Grafana datasource UID mismatch breaks pre-provisioned dashboards; pin `uid: prometheus-main` (and `uid: jaeger-main` for traces); EVERY panel JSON references the datasource by uid
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Recommended Project Structure" — provisioning tree layout `docker/grafana/provisioning/{datasources,dashboards}/`
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Architectural Responsibility Map / Grafana dashboards" — 3 dashboards (one per service + one custom domain)
    docker-compose.yml as modified in Task 1 (grafana volume mount path `/etc/grafana/provisioning`)
  </read_first>
  <action>
    Per D-06 + Pitfall 4 (UID pinning), create the Grafana provisioning tree:

    **datasources/prometheus.yml** (apiVersion: 1, datasources: name "Prometheus", type prometheus, `uid: prometheus-main` PINNED, access proxy, url `http://prometheus:9090` in-compose hostname, isDefault true).

    **datasources/jaeger.yml** (apiVersion: 1, datasources: name "Jaeger", type jaeger, `uid: jaeger-main` PINNED, access proxy, url `http://jaeger:16686`). The Jaeger datasource enables trace-to-logs correlation in Grafana for the pino traceId field.

    **dashboards/dashboards.yml** (apiVersion: 1, providers: name "default", type file, options.path `/etc/grafana/provisioning/dashboards`, allowUiUpdates false, foldersFromFilesStructure true). This makes Grafana scan the dashboards directory and auto-import all JSON files.

    **dashboards/games-service.json** — built-in NestJS HTTP + AMQP panels:
    - Request rate (rate(http_server_requests_total[1m])) per route
    - Request duration p95/p99 (histogram_quantile against http_server_request_duration_seconds_bucket)
    - AMQP consumer lag / publish rate from auto-instrumentation metrics
    - Active WS connections (`active_ws_connections` — emitted by Plan 10-05)
    All panels: `datasource: { type: "prometheus", uid: "prometheus-main" }` (Pitfall 4).

    **dashboards/wallets-service.json** — mirror panels for wallets HTTP + AMQP (no WS / domain custom metrics for wallets).

    **dashboards/crash-domain.json** — the differential observability claim per RESEARCH "specifics":
    - `crash_bet_volume_total` rate by status (counter)
    - `crash_rtp_window` current value (gauge — env CRASH_RTP_WINDOW_ROUNDS rolling)
    - `multiplier_drift_seconds` p50/p95 (histogram)
    - `ws_broadcast_latency_seconds` p50/p95 (histogram)
    - `active_ws_connections` over time (gauge)
    All panels: `datasource: { type: "prometheus", uid: "prometheus-main" }`.

    Use Grafana panel JSON v2/v3 — match the major version of the pinned grafana image (11.3.1 → schemaVersion 38 or current). Panel queries can use minimal PromQL; the dashboards must LOAD without "Datasource not found" errors even if no metric data has been scraped yet (empty panels are OK). Until Plan 10-05 lands the custom metrics emit, the crash-domain panels render empty — expected.

    Important: dashboard JSON `id` field MUST be null (Grafana assigns on import); use `uid` strings for the dashboards themselves (e.g. `uid: "games-service-overview"`). Per Pitfall 4: do NOT rely on auto-generated UIDs anywhere.
  </action>
  <verify>
    <automated>test -f docker/grafana/provisioning/datasources/prometheus.yml && test -f docker/grafana/provisioning/datasources/jaeger.yml && test -f docker/grafana/provisioning/dashboards/dashboards.yml && test -f docker/grafana/provisioning/dashboards/games-service.json && test -f docker/grafana/provisioning/dashboards/wallets-service.json && test -f docker/grafana/provisioning/dashboards/crash-domain.json && grep -q "uid: prometheus-main" docker/grafana/provisioning/datasources/prometheus.yml && grep -q "uid: jaeger-main" docker/grafana/provisioning/datasources/jaeger.yml && grep -q "prometheus-main" docker/grafana/provisioning/dashboards/crash-domain.json && grep -q "crash_bet_volume_total\|crash_rtp_window" docker/grafana/provisioning/dashboards/crash-domain.json && (docker compose restart grafana && sleep 8 && curl -sf http://localhost:3001/api/datasources/uid/prometheus-main | grep -q '"uid":"prometheus-main"')</automated>
  </verify>
  <done>Provisioning tree exists with UID-pinned datasources + 3 dashboards; Grafana restart loads all 3 dashboards under "Default" folder; `curl /api/datasources/uid/prometheus-main` returns 200 with the pinned uid; no "Datasource not found" errors in Grafana logs.</done>
</task>

<task type="auto">
  <name>Task 3: Extend scripts/smoke-health.sh with probes 45-47 for jaeger + prometheus + grafana</name>
  <files>scripts/smoke-health.sh</files>
  <read_first>
    scripts/smoke-health.sh (existing 44 probes — note the probe numbering, output format `PROBE NN`, success/fail pattern, exit code convention; Phase 1-9 progressively appended probes 1-44)
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Validation Architecture / Phase Requirements → Test Map" REQ-OBS-03 row — `curl -sf http://localhost:9090/-/healthy && curl -sf http://localhost:3001/api/health && curl -sf http://localhost:16686/`
  </read_first>
  <action>
    Append 3 probes to scripts/smoke-health.sh, following the existing function/echo/curl-with-exit-code pattern verbatim:
    - **Probe 45**: jaeger UI reachable — `curl -sf -o /dev/null http://localhost:16686/` → expect 200
    - **Probe 46**: prometheus healthy — `curl -sf http://localhost:9090/-/healthy` → expect HTTP 200 + body contains "Prometheus" or "Healthy"
    - **Probe 47**: grafana healthy — `curl -sf http://localhost:3001/api/health` → expect 200 + JSON body contains `"database":"ok"`

    Match the existing probe registration style (numeric label + section header). Do not reorder or renumber existing probes. The script must continue to exit non-zero on any failed probe (preserve the existing exit convention).
  </action>
  <verify>
    <automated>bun run docker:up --wait && bash scripts/smoke-health.sh | grep -E "PROBE 45|PROBE 46|PROBE 47" | grep -c PASS | grep -q '^3$'</automated>
  </verify>
  <done>scripts/smoke-health.sh runs end-to-end with probes 1-47 all PASS after `bun run docker:up`; probes 45-47 specifically exercise the 3 new observability containers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Grafana :3001 | Anonymous Viewer role only — read-only dashboards |
| Browser -> Prometheus :9090 | Local dev access — Prometheus UI exposes scrape state |
| Prometheus -> games/wallets /metrics | In-compose internal docker network only — NEVER routed via Kong externally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Elevation of Privilege | Grafana anonymous role | mitigate | GF_AUTH_ANONYMOUS_ORG_ROLE: "Viewer" — read-only; cannot edit dashboards or query datasources outside the provisioned set |
| T-10-10 | Information Disclosure | /metrics endpoint exposed externally | mitigate | Kong routing audit: `/metrics` is NOT in docker/kong/kong.yml; Prometheus reaches it only via internal `games:4001` / `wallets:4002` docker hostnames |
| T-10-11 | Information Disclosure | OTLP exporter target | mitigate | jaeger:4318 in-compose hostname only; external public OTLP collectors never reached |
</threat_model>

<verification>
- `bun run docker:up --wait` brings all 3 new containers up healthy
- `bash scripts/smoke-health.sh` returns exit 0 with probes 1-47 PASS
- `curl -sf http://localhost:3001/api/datasources/uid/prometheus-main` returns 200
- `curl -sf http://localhost:3001/api/datasources/uid/jaeger-main` returns 200
- `curl -s http://localhost:9090/api/v1/targets | grep -c UP` returns at least 2 (games + wallets) after Plan 10-05 mounts /metrics
- Manual: open http://localhost:3001 → see 3 dashboards under Dashboards → Browse
- Manual: open http://localhost:16686 → Jaeger UI loads
</verification>

<success_criteria>
1. After this plan, a recruiter running `bun run docker:up` opens Grafana at :3001 and sees 3 dashboards rendered (panels may be empty until Plan 10-05 lands custom metric emit).
2. Plan 10-05 can scrape its custom metrics immediately — Prometheus targets games:4001 + wallets:4002 already configured.
3. Wave 1 plan 10-03's OTLP traces immediately have a receiver (jaeger:4318) and a UI (16686) waiting.
</success_criteria>

<output>
Create `.planning/phases/10-quality-hardening-docs/10-04-SUMMARY.md` when done. Cite the smoke-health.sh probe count post-extension (47) and any Grafana panel JSON details worth documenting.
</output>
