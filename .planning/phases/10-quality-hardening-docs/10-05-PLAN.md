---
phase: 10-quality-hardening-docs
plan: 05
type: execute
wave: 2
depends_on:
  - 10-03
files_modified:
  - services/games/src/observability/observability.module.ts
  - services/games/src/observability/metrics/bet-volume.metric.ts
  - services/games/src/observability/metrics/crash-rtp-window.metric.ts
  - services/games/src/observability/metrics/multiplier-drift.metric.ts
  - services/games/src/observability/metrics/ws-broadcast-latency.metric.ts
  - services/games/src/observability/metrics/active-ws-connections.metric.ts
  - services/games/src/application/use-cases/crash-round.use-case.ts
  - services/games/src/application/use-cases/settle-round.use-case.ts
  - services/games/src/application/handlers/wallet-debit-rejected.handler.ts
  - services/games/src/application/services/saga-timeout-sweeper.service.ts
  - services/games/src/application/use-cases/cash-out.use-case.ts
  - services/games/src/presentation/gateways/multiplier-broadcast.service.ts
  - services/games/src/presentation/gateways/game-ws.gateway.ts
  - services/games/tests/unit/observability/metrics/bet-volume.metric.test.ts
  - services/games/tests/unit/observability/metrics/active-ws-connections.metric.test.ts
  - services/games/tests/unit/observability/metrics/crash-rtp-window.metric.test.ts
  - services/games/tests/integration/metrics-endpoint.test.ts
  - services/wallets/src/observability/observability.module.ts
  - services/wallets/tests/integration/metrics-endpoint.test.ts
autonomous: true
requirements:
  - REQ-OBS-02

must_haves:
  truths:
    - "GET /metrics on both services returns Prometheus exposition format with HTTP 200"
    - "bet_volume_total counter labeled by status increments when bets cash out / refund / lose"
    - "crash_rtp_window gauge updates on every round settle with payout/bet ratio over the last CRASH_RTP_WINDOW_ROUNDS rounds"
    - "multiplier_drift_seconds histogram observes ticker drift inside MultiplierBroadcastService.tick()"
    - "ws_broadcast_latency_seconds histogram observes tick-to-emit time inside MultiplierBroadcastService"
    - "active_ws_connections gauge increments on handleConnection and decrements on handleDisconnect"
    - "No high-cardinality labels (no playerId / no UUIDs as label values per Security V4)"
  artifacts:
    - path: "services/games/src/observability/metrics/bet-volume.metric.ts"
      provides: "makeCounterProvider with labelNames=['status']"
      contains: "makeCounterProvider"
    - path: "services/games/src/observability/metrics/crash-rtp-window.metric.ts"
      provides: "makeGaugeProvider for the rolling RTP gauge"
      contains: "makeGaugeProvider"
    - path: "services/games/src/observability/metrics/multiplier-drift.metric.ts"
      provides: "makeHistogramProvider with buckets [0.001, 0.005, 0.01, 0.05, 0.1, 0.5, 1]"
      contains: "makeHistogramProvider"
    - path: "services/games/src/observability/metrics/ws-broadcast-latency.metric.ts"
      provides: "makeHistogramProvider"
      contains: "makeHistogramProvider"
    - path: "services/games/src/observability/metrics/active-ws-connections.metric.ts"
      provides: "makeGaugeProvider"
      contains: "makeGaugeProvider"
    - path: "services/games/tests/integration/metrics-endpoint.test.ts"
      provides: "GET /metrics returns 200 + content-type text/plain + expected metric names present"
      contains: "/metrics"
  key_links:
    - from: "services/games/src/application/use-cases/crash-round.use-case.ts"
      to: "BET_VOLUME_TOTAL counter"
      via: "InjectMetric"
      pattern: "InjectMetric.*BET_VOLUME_TOTAL"
    - from: "services/games/src/presentation/gateways/multiplier-broadcast.service.ts"
      to: "MULTIPLIER_DRIFT_SECONDS + WS_BROADCAST_LATENCY_SECONDS"
      via: "histogram.observe() per tick"
      pattern: "InjectMetric.*MULTIPLIER_DRIFT"
    - from: "services/games/src/presentation/gateways/game-ws.gateway.ts"
      to: "ACTIVE_WS_CONNECTIONS gauge"
      via: "inc()/dec() on connect/disconnect"
      pattern: "ACTIVE_WS_CONNECTIONS"
---

<objective>
Land the 5 custom Prometheus metrics per D-06 (RESEARCH "Open Questions / crash_rtp_window window size" / "Where does multiplier_drift_seconds get observed?") + auto-mount the `/metrics` endpoint via `@willsoto/nestjs-prometheus` (NOT the unscoped stale package per RESEARCH "Standard Stack / Alternatives Considered") in BOTH services. Wallets gets only the built-in HTTP/AMQP defaults (no domain metrics — domain metrics are games-specific). Games gets the full 5 custom metrics instrumented at the observation sites named in the `critical_planning_inputs` (CrashRoundUseCase + CashOutUseCase + WalletDebitRejectedHandler + SagaTimeoutSweeper for bet_volume; SettleRoundUseCase for crash_rtp_window; MultiplierBroadcastService.tick for multiplier_drift + ws_broadcast_latency; GameWsGateway.handleConnection/handleDisconnect for active_ws_connections).

Purpose: REQ-OBS-02 closure. Wave 2 plan 10-04 has Prometheus already scraping games:4001 and wallets:4002 every 15s — as soon as `/metrics` mounts, the targets flip from "DOWN" to "UP" and the crash-domain dashboard populates.
Output: 5 metric provider files in games + PrometheusModule registered in BOTH ObservabilityModules + 5 instrumentation sites injecting via @InjectMetric + unit tests for the metric providers + integration tests for the /metrics endpoint contract in both services.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-quality-hardening-docs/10-RESEARCH.md
@.planning/phases/10-quality-hardening-docs/10-CONTEXT.md
@.planning/phases/10-quality-hardening-docs/10-03-SUMMARY.md
@CLAUDE.md
@services/games/src/observability/observability.module.ts
@services/games/src/application/use-cases/crash-round.use-case.ts
@services/games/src/application/use-cases/settle-round.use-case.ts
@services/games/src/application/use-cases/cash-out.use-case.ts
@services/games/src/application/handlers/wallet-debit-rejected.handler.ts
@services/games/src/application/services/saga-timeout-sweeper.service.ts
@services/games/src/presentation/gateways/multiplier-broadcast.service.ts
@services/games/src/presentation/gateways/game-ws.gateway.ts
@services/games/src/config/defaults.ts
@services/wallets/src/observability/observability.module.ts

<interfaces>
From @willsoto/nestjs-prometheus (RESEARCH Pattern 3):
- `PrometheusModule.register({ path: "/metrics", defaultMetrics: { enabled: true } })` — auto-mounts the GET /metrics controller
- `makeCounterProvider({ name, help, labelNames })` returns a Provider
- `makeGaugeProvider({ name, help })` returns a Provider
- `makeHistogramProvider({ name, help, buckets })` returns a Provider
- `@InjectMetric(NAME)` decorator: `private readonly counter: Counter<string>`

From prom-client types (transitive):
- `Counter<string>` -> `.inc(labels, value)`
- `Gauge<string>` -> `.set(value)`, `.inc()`, `.dec()`
- `Histogram<string>` -> `.observe(value)`, `.startTimer()`

Naming convention (RESEARCH Pattern 3 example uses prefix):
- crash_bet_volume_total (counter)
- crash_rtp_window (gauge)
- crash_multiplier_drift_seconds (histogram)
- crash_ws_broadcast_latency_seconds (histogram)
- crash_active_ws_connections (gauge)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create 5 metric providers + register PrometheusModule in BOTH services + /metrics endpoint integration tests</name>
  <files>services/games/src/observability/observability.module.ts, services/games/src/observability/metrics/bet-volume.metric.ts, services/games/src/observability/metrics/crash-rtp-window.metric.ts, services/games/src/observability/metrics/multiplier-drift.metric.ts, services/games/src/observability/metrics/ws-broadcast-latency.metric.ts, services/games/src/observability/metrics/active-ws-connections.metric.ts, services/games/tests/integration/metrics-endpoint.test.ts, services/wallets/src/observability/observability.module.ts, services/wallets/tests/integration/metrics-endpoint.test.ts</files>
  <behavior>
    Test (games metrics-endpoint.test.ts INTEGRATION=1): boots a Nest app with ObservabilityModule + AppModule (or a slim test module); `GET /metrics` returns HTTP 200, content-type `text/plain; version=0.0.4; charset=utf-8` (Prometheus exposition default), body contains `crash_bet_volume_total`, `crash_rtp_window`, `crash_multiplier_drift_seconds`, `crash_ws_broadcast_latency_seconds`, `crash_active_ws_connections` AND default Node.js process metrics (e.g., `process_cpu_seconds_total`, `nodejs_eventloop_lag_seconds`) — proves `defaultMetrics.enabled: true` works.
    Test (wallets metrics-endpoint.test.ts): same minus the custom names — only default metrics present (wallets has no domain metrics).
    Tests gated on `INTEGRATION=1` env per existing integration test convention (search services/games/tests/integration/*.test.ts for the prevailing gate).
  </behavior>
  <read_first>
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Pattern 3: Custom Prometheus metrics via @willsoto/nestjs-prometheus" — canonical makeCounterProvider + @InjectMetric usage
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Open Questions #2" — CRASH_RTP_WINDOW_ROUNDS default 100 env-tunable; consumed by crash-rtp metric
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Open Questions #3" — multiplier_drift_seconds histogram buckets [0.001, 0.005, 0.01, 0.05, 0.1, 0.5, 1]
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Security Domain V4" — no playerId / UUID labels (cardinality explosion mitigation)
    services/games/src/observability/observability.module.ts (Plan 10-03 created this; we EXTEND it by adding PrometheusModule to imports + the 5 metric providers to providers array)
    services/wallets/src/observability/observability.module.ts (mirror — add PrometheusModule only; no custom metrics)
    services/games/tests/integration/*.test.ts (existing INTEGRATION=1 gate convention; reuse the same boot pattern)
  </read_first>
  <action>
    Create 5 metric provider files under `services/games/src/observability/metrics/`:

    **bet-volume.metric.ts**: exports `BET_VOLUME_TOTAL = "crash_bet_volume_total"` const + `betVolumeTotalProvider = makeCounterProvider({ name: BET_VOLUME_TOTAL, help: "Total bet volume in cents, labeled by terminal status", labelNames: ["status"] as const })`. Label values restricted to terminal-status enum (`cashed_out` / `lost` / `refunded`) — no playerId / UUID per Security V4.

    **crash-rtp-window.metric.ts**: exports `CRASH_RTP_WINDOW = "crash_rtp_window"` + `crashRtpWindowProvider = makeGaugeProvider({ name: CRASH_RTP_WINDOW, help: "Rolling RTP (payout/bet) over the last CRASH_RTP_WINDOW_ROUNDS rounds" })`. No labels.

    **multiplier-drift.metric.ts**: exports `MULTIPLIER_DRIFT_SECONDS = "crash_multiplier_drift_seconds"` + `multiplierDriftSecondsProvider = makeHistogramProvider({ name: MULTIPLIER_DRIFT_SECONDS, help: "Difference between expected tick time and actual emit time, in seconds", buckets: [0.001, 0.005, 0.01, 0.05, 0.1, 0.5, 1] })`. Per RESEARCH Open Q3 — recorded inside MultiplierBroadcastService.tick().

    **ws-broadcast-latency.metric.ts**: exports `WS_BROADCAST_LATENCY_SECONDS = "crash_ws_broadcast_latency_seconds"` + `wsBroadcastLatencySecondsProvider = makeHistogramProvider({ name: WS_BROADCAST_LATENCY_SECONDS, help: "Time between tick computation and socket.io emit() return, proxy for true client RTT (see metric help — Socket.IO ack-RTT requires per-client ping)", buckets: [0.0005, 0.001, 0.005, 0.01, 0.05, 0.1] })`. Per RESEARCH Open Q1 — proxy metric documented in help string.

    **active-ws-connections.metric.ts**: exports `ACTIVE_WS_CONNECTIONS = "crash_active_ws_connections"` + `activeWsConnectionsProvider = makeGaugeProvider({ name: ACTIVE_WS_CONNECTIONS, help: "Currently connected WebSocket clients" })`.

    Edit `services/games/src/observability/observability.module.ts` (Plan 10-03 created it):
    - Add `PrometheusModule.register({ path: "/metrics", defaultMetrics: { enabled: true } })` to the imports array.
    - Add the 5 metric providers to the `providers` array.
    - Add `BET_VOLUME_TOTAL`, `CRASH_RTP_WINDOW`, `MULTIPLIER_DRIFT_SECONDS`, `WS_BROADCAST_LATENCY_SECONDS`, `ACTIVE_WS_CONNECTIONS` names to the `exports` array so other modules can resolve via `@InjectMetric(NAME)`.

    Edit `services/wallets/src/observability/observability.module.ts`: add `PrometheusModule.register({ path: "/metrics", defaultMetrics: { enabled: true } })` to imports. Wallets has no custom metrics — default node + http metrics only.

    Create `services/games/tests/integration/metrics-endpoint.test.ts` and `services/wallets/tests/integration/metrics-endpoint.test.ts` using the existing INTEGRATION=1 boot pattern (cite the test that already does this — e.g. `tests/integration/wallet-debit-rejected.integration.test.ts` or similar). Test bodies follow the contract in <behavior>.
  </action>
  <verify>
    <automated>cd services/games && bun tsc --noEmit && INTEGRATION=1 bun test tests/integration/metrics-endpoint.test.ts && cd ../wallets && bun tsc --noEmit && INTEGRATION=1 bun test tests/integration/metrics-endpoint.test.ts && cd .. && bun run docker:up --wait && curl -sf http://localhost:4001/metrics | grep -E "crash_bet_volume_total|crash_rtp_window|crash_multiplier_drift_seconds|crash_ws_broadcast_latency_seconds|crash_active_ws_connections" | wc -l | grep -qE '^[5-9]|^[0-9]{2,}'</automated>
  </verify>
  <done>5 metric providers exist; ObservabilityModule (games) registers PrometheusModule + all 5 providers + exports the metric name constants; ObservabilityModule (wallets) registers PrometheusModule only; tsc clean in both services; metrics-endpoint integration tests green in both services; live `curl /metrics` from running games container exposes all 5 custom metric names plus defaultMetrics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument bet_volume + crash_rtp_window + active_ws_connections at their observation sites with unit tests</name>
  <files>services/games/src/application/use-cases/crash-round.use-case.ts, services/games/src/application/use-cases/cash-out.use-case.ts, services/games/src/application/use-cases/settle-round.use-case.ts, services/games/src/application/handlers/wallet-debit-rejected.handler.ts, services/games/src/application/services/saga-timeout-sweeper.service.ts, services/games/src/presentation/gateways/game-ws.gateway.ts, services/games/tests/unit/observability/metrics/bet-volume.metric.test.ts, services/games/tests/unit/observability/metrics/active-ws-connections.metric.test.ts, services/games/tests/unit/observability/metrics/crash-rtp-window.metric.test.ts</files>
  <behavior>
    Test 1 (bet-volume.metric.test.ts): given a CrashRoundUseCase processing a round with 3 LOST bets each 5000 cents, the betVolumeTotal counter is invoked 3 times with `{ status: "lost" }` and value 5000. Mirror coverage: CashOutUseCase invokes inc with `{ status: "cashed_out" }`; WalletDebitRejectedHandler invokes inc with `{ status: "refunded" }`; SagaTimeoutSweeper invokes inc with `{ status: "refunded" }` per refunded bet.
    Test 2 (crash-rtp-window.metric.test.ts): given SettleRoundUseCase invoked after N rounds with known payout/bet totals, the gauge is set to `sum(payouts) / sum(bets)` over the last `env.CRASH_RTP_WINDOW_ROUNDS` rounds. Uses repo method to fetch the rolling-window aggregate (add the repo method if missing).
    Test 3 (active-ws-connections.metric.test.ts): given a GameWsGateway with mocked Socket.io server, `handleConnection` calls `gauge.inc()` exactly once; `handleDisconnect` calls `gauge.dec()` exactly once; reentrant connect/disconnect leaves the gauge net zero.
  </behavior>
  <read_first>
    services/games/src/application/use-cases/crash-round.use-case.ts (Phase 9 plan 09-03 made this the per-bet bet.lost outbox emit site — the same loop is the right place for bet_volume_total label=lost inc)
    services/games/src/application/use-cases/cash-out.use-case.ts (Phase 5 plan 05-06 — single-TX commit + outbox bet.cashed_out; bet_volume_total label=cashed_out at the same boundary)
    services/games/src/application/use-cases/settle-round.use-case.ts (SC5/Phase 9 — this is the round-settle path; crash_rtp_window updates here after settlement)
    services/games/src/application/handlers/wallet-debit-rejected.handler.ts (Phase 5 plan 05-05 — Bet.refund on INSUFFICIENT_FUNDS; bet_volume_total label=refunded)
    services/games/src/application/services/saga-timeout-sweeper.service.ts (Phase 5 plan 05-07 — auto-refund path; bet_volume_total label=refunded per refunded bet in the sweep loop)
    services/games/src/presentation/gateways/game-ws.gateway.ts (Phase 6 plan 06-03 — handleConnection auto-joins lobby + user:{playerId}; handleDisconnect cleanup; both already touched per Phase 6 — append gauge inc/dec without disrupting existing behavior)
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Open Questions #2" — crash_rtp_window = sum(payouts) / sum(bets) over last CRASH_RTP_WINDOW_ROUNDS via a repo aggregate query
    .planning/research/PITFALLS.md (Phase 6 raw-SQL `toDate()` pitfall — if the rolling RTP query uses raw SQL or `INTERVAL` it MUST coerce via toDate; reuse the Phase 9 projector's coercion pattern)
  </read_first>
  <action>
    **bet_volume_total instrumentation (4 sites):**
    - `CrashRoundUseCase`: in the per-bet sweep loop where bet.lost outbox events are emitted (Phase 9 plan 09-03), inject `@InjectMetric(BET_VOLUME_TOTAL) private readonly betVolume: Counter<string>` via the constructor and call `this.betVolume.inc({ status: "lost" }, Number(bet.amount.toCents()))` per bet. The `Number(...)` cast on the bigint cents is acceptable in metrics per CLAUDE.md / RESEARCH "Project Constraints" — Prometheus has no bigint type.
    - `CashOutUseCase`: at the bet.cashed_out outbox emit boundary in the em.transactional block, inject + call `this.betVolume.inc({ status: "cashed_out" }, Number(bet.amount.toCents()))`.
    - `WalletDebitRejectedHandler`: where Bet.refund is invoked + bet.refunded outbox written, inject + call `this.betVolume.inc({ status: "refunded" }, Number(bet.amount.toCents()))`.
    - `SagaTimeoutSweeper`: in the loop emitting bet.refunded with reason SAGA_TIMEOUT, inject + call `this.betVolume.inc({ status: "refunded" }, Number(bet.amount.toCents()))` per refunded bet.

    **crash_rtp_window instrumentation (1 site):**
    - `SettleRoundUseCase`: after settlement persists, inject `@InjectMetric(CRASH_RTP_WINDOW) private readonly crashRtp: Gauge<string>` AND inject `env` (typed config). Add a repo method (extend BetRepository or RoundRepository — choose whichever owns aggregate queries) like `getRollingRtp(windowRounds: number): Promise<{ payoutTotalCents: bigint; betTotalCents: bigint }>` that computes the aggregate over the last N rounds via SQL. If the SQL uses raw `INTERVAL` or `toDate()`, follow the Phase 6 raw-SQL `toDate()` pattern from the Phase 9 leaderboard projector to avoid the pitfall. Compute `rtp = Number(payoutTotalCents) / Number(betTotalCents)` (guarded against div-by-zero) and call `this.crashRtp.set(rtp)`. Read window from `env.CRASH_RTP_WINDOW_ROUNDS` (typed in Plan 10-01) — NEVER hardcode.

    **active_ws_connections instrumentation (1 site / 2 handlers):**
    - `GameWsGateway`: inject `@InjectMetric(ACTIVE_WS_CONNECTIONS) private readonly wsConnections: Gauge<string>`. In `handleConnection`, AFTER successful auth + room join, call `this.wsConnections.inc()`. In `handleDisconnect`, call `this.wsConnections.dec()`. Match the existing logging idiom in the file. Do NOT introduce any await on the metric call — gauge.inc/dec is synchronous.

    **multiplier_drift + ws_broadcast_latency** are instrumented in Task 3 — they live in MultiplierBroadcastService which has its own discipline boundary (30Hz hot path).

    Write unit tests per the behavior block above under `services/games/tests/unit/observability/metrics/`. Mock the Counter/Gauge with a minimal stub asserting call shape. Tests should be fast (no boot needed).

    Per CLAUDE.md domain rule: ALL injection is in `application/` (use cases + handlers + sweeper) or `presentation/` (gateway). NEVER touch `domain/` aggregates with metric concerns.
  </action>
  <verify>
    <automated>cd services/games && bun tsc --noEmit && bun test tests/unit/observability/metrics/bet-volume.metric.test.ts tests/unit/observability/metrics/active-ws-connections.metric.test.ts tests/unit/observability/metrics/crash-rtp-window.metric.test.ts && grep -q "InjectMetric(BET_VOLUME_TOTAL)" src/application/use-cases/crash-round.use-case.ts src/application/use-cases/cash-out.use-case.ts src/application/handlers/wallet-debit-rejected.handler.ts src/application/services/saga-timeout-sweeper.service.ts && grep -q "InjectMetric(ACTIVE_WS_CONNECTIONS)" src/presentation/gateways/game-ws.gateway.ts && grep -q "InjectMetric(CRASH_RTP_WINDOW)" src/application/use-cases/settle-round.use-case.ts && grep -q "env.CRASH_RTP_WINDOW_ROUNDS" src/application/use-cases/settle-round.use-case.ts</automated>
  </verify>
  <done>4 bet_volume inc sites instrumented (cash-out + crash + reject + sweeper); SettleRoundUseCase computes RTP via env-driven window + sets gauge; GameWsGateway inc/dec on connect/disconnect; 3 unit test files green; tsc clean; no playerId / UUID labels anywhere; no hardcoded RTP window.</done>
</task>

<task type="auto">
  <name>Task 3: Instrument multiplier_drift + ws_broadcast_latency inside MultiplierBroadcastService.tick() (30Hz hot path discipline)</name>
  <files>services/games/src/presentation/gateways/multiplier-broadcast.service.ts</files>
  <read_first>
    services/games/src/presentation/gateways/multiplier-broadcast.service.ts (Phase 6 plan 06-04 created this; sole owner of `volatile.emit('round:tick', ...)` at ~30Hz; recursive setTimeout loop with self-scheduled tick; existing tick state tracks expected next-tick time via `expectedNextTickAt` or equivalent)
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Open Questions #3" — multiplier_drift = (Date.now() - expectedNextTickAt) seconds, observed in tick BEFORE emit
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Project Constraints / WebSocket discipline" — do NOT add OTel-related awaits in the gateway tick path; instrumentation must be SYNCHRONOUS span-recording or out-of-band
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Open Questions #1" — ws_broadcast_latency proxy = tick computed -> emit returned delta inside MultiplierBroadcastService (NOT real client-ack RTT; documented in metric help)
  </read_first>
  <action>
    In `MultiplierBroadcastService` constructor add `@InjectMetric(MULTIPLIER_DRIFT_SECONDS) private readonly multiplierDrift: Histogram<string>` and `@InjectMetric(WS_BROADCAST_LATENCY_SECONDS) private readonly wsBroadcastLatency: Histogram<string>`.

    In the `tick()` method (or equivalent recursive-setTimeout entry):
    - At the very top, compute `const driftMs = Date.now() - this.expectedNextTickAt` (use whichever field name the service already uses for expected-next-tick time). Convert to seconds and observe via `this.multiplierDrift.observe(Math.max(0, driftMs / 1000))`. Synchronous, sub-microsecond — no impact on the 30Hz loop budget.
    - Wrap the `emit('round:tick', ...)` call in a synchronous timing: `const emitStart = performance.now()` immediately before the emit call; `const emitDurationMs = performance.now() - emitStart` immediately after; `this.wsBroadcastLatency.observe(emitDurationMs / 1000)`.
    - Both observations are SYNCHRONOUS — no await, no Promise. Honor the CLAUDE.md WebSocket discipline rule.

    Do NOT change the tick scheduling logic, the existing volatile.emit behavior, or the volatile.emit recipient. Diff should be additive only (3 new lines + 2 injected dependencies).
  </action>
  <verify>
    <automated>cd services/games && bun tsc --noEmit && grep -q "InjectMetric(MULTIPLIER_DRIFT_SECONDS)" src/presentation/gateways/multiplier-broadcast.service.ts && grep -q "InjectMetric(WS_BROADCAST_LATENCY_SECONDS)" src/presentation/gateways/multiplier-broadcast.service.ts && ! grep -E "await.*Drift|await.*Latency|\\.observe\\(.*await" src/presentation/gateways/multiplier-broadcast.service.ts && bun test tests/unit/ -t "MultiplierBroadcastService" 2>&1 | tee /tmp/multiplier-test-out.txt | grep -E "fail|pass" | tail -5 && ! grep -q fail /tmp/multiplier-test-out.txt</automated>
  </verify>
  <done>multiplierDrift + wsBroadcastLatency injected and observed once per tick; observations are synchronous; existing 30Hz tick volatile.emit behavior preserved (Phase 6 unit test for the service still green); tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prometheus scraper -> games/wallets /metrics | Internal docker network only — Kong does NOT route /metrics externally |
| Metric label values -> Prometheus TSDB | Cardinality risk — labels must be enumerable sets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12 | Denial of Service | High-cardinality label explosion | mitigate | bet_volume labelNames=['status'] enum only (3 values); zero playerId / UUID labels anywhere; integration test asserts /metrics response size stays bounded |
| T-10-13 | Information Disclosure | /metrics endpoint internal scope | mitigate | Prometheus reaches via internal docker network only; Kong routes audit excludes /metrics (no kong.yml route for /metrics path) |
| T-10-14 | Performance regression | 30Hz tick path metric overhead | mitigate | Metric observations are synchronous Histogram.observe + Gauge.inc/dec calls (sub-microsecond); no awaits in MultiplierBroadcastService.tick — verified by grep gate |
</threat_model>

<verification>
- `curl -sf http://localhost:4001/metrics | grep -c "^crash_"` returns ≥5 (5 custom metric names)
- `curl -sf http://localhost:4002/metrics | grep -c "^crash_"` returns 0 (wallets has no domain metrics)
- `curl -sf http://localhost:9090/api/v1/targets | grep -c '"health":"up"'` returns ≥2 after first scrape
- After a manual bet+cashout flow: `curl -sf http://localhost:4001/metrics | grep crash_bet_volume_total` shows non-zero counter with `status="cashed_out"` label
- After ≥1 round settles: `curl -sf http://localhost:4001/metrics | grep crash_rtp_window` shows a non-zero gauge value
- After WS clients connect/disconnect: `curl -sf http://localhost:4001/metrics | grep crash_active_ws_connections` reflects current count
</verification>

<success_criteria>
1. Plan 10-04's Grafana crash-domain dashboard populates with real data within 30s of any bet activity.
2. Recruiter opens http://localhost:3001 and sees bet volume rising in real time during a live game.
3. No high-cardinality label values landed (verified by integration test bounded /metrics size + manual grep for any UUID-shaped label).
</success_criteria>

<output>
Create `.planning/phases/10-quality-hardening-docs/10-05-SUMMARY.md` when done. Cite the live `curl /metrics` output snippet for each of the 5 custom metrics + the Prometheus target health.
</output>
