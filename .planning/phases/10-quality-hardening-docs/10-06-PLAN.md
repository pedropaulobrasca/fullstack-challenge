---
phase: 10-quality-hardening-docs
plan: 06
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - frontend/src/styles/app.css
  - frontend/package.json
  - frontend/bun.lock
  - frontend/src/components/ui/sheet.tsx
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/verification-drawer.tsx
  - frontend/src/components/replay-modal.tsx
  - frontend/src/components/history-strip.tsx
  - packages/contracts/src/ws/ws-event.payloads.ts
  - packages/contracts/tests/ws/round-snapshot-nullable.test.ts
  - frontend/src/stores/ws-dispatch.ts
autonomous: false
requirements:
  - REQ-OBS-04

must_haves:
  truths:
    - "D-03a: Fairness drawer (Sheet) AND Replay modal (Dialog) visually open within ~300ms of click; verified live + via the diagnosed fix recommended in d03a-diagnosis.md"
    - "D-03b: roundSnapshotPayloadSchema accepts null via .nullable(); FE dispatcher treats null as no-op (waits for next round:started); no zod warn in console at reconnect-between-rounds"
    - "D-03c: HistoryStrip renders without any React key warn; verified live first (the warn may already be fixed) before applying any fix"
    - "Existing FE + contracts test suites stay green; no Phase 7/8/9 regression"
  artifacts:
    - path: "packages/contracts/src/ws/ws-event.payloads.ts"
      provides: "roundSnapshotPayloadSchema relaxed via .nullable()"
      contains: ".nullable()"
    - path: ".planning/phases/10-quality-hardening-docs/d03a-diagnosis.md"
      provides: "Plan 10-01 Task 2 already created this — referenced HERE as the SoT for the D-03a fix"
      contains: "Root cause:"
  key_links:
    - from: "frontend (WS dispatcher under frontend/src/lib or frontend/src/features)"
      to: "packages/contracts roundSnapshotPayloadSchema"
      via: "import + result.data === null no-op branch"
      pattern: "=== null"
---

<objective>
Close the 3 polish defects surfaced by the Phase 8 live smoke (per D-03):

- **D-03a**: Radix Sheet (Fairness drawer) + Dialog (Replay modal) click handler flips state but content never visually opens. Plan 10-01 Task 2 produced `d03a-diagnosis.md` with a concrete root cause. THIS PLAN applies the diagnosis's recommended fix + verifies live + adds a regression guard.
- **D-03b**: WS `round:snapshot=null` at connect between rounds → FE zod safeParse rejects with "Expected object, received null". Apply RESEARCH Assumption A2 fix: FE-side schema relaxation via `.nullable()` + dispatcher null-branch no-op. Cleaner than server defer-emit (preserves always-emit invariant, no gateway state).
- **D-03c**: HistoryStrip React `key` prop warn. RESEARCH Pitfall 10 + Assumption A3: the current file uses `key={entry.roundId}` which IS valid — verify live first. If still warning, apply minimal disambiguator.

Purpose: A senior-level submission cannot ship visible defects. Playwright Wave 3 plan 10-07 also won't pass SC1 if Radix portals don't open visually.
Output: D-03a fix applied per diagnosis + visual smoke confirmation; D-03b schema relax + dispatcher branch + regression test; D-03c verified live + minimal fix (or documented as already-resolved).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-quality-hardening-docs/10-RESEARCH.md
@.planning/phases/10-quality-hardening-docs/10-CONTEXT.md
@.planning/phases/10-quality-hardening-docs/10-01-SUMMARY.md
@.planning/phases/10-quality-hardening-docs/d03a-diagnosis.md
@.planning/phases/08-provably-fair-history-replay
@CLAUDE.md
@frontend/src/components/verification-drawer.tsx
@frontend/src/components/replay-modal.tsx
@frontend/src/components/history-strip.tsx
@frontend/src/components/ui/sheet.tsx
@frontend/src/components/ui/dialog.tsx
@packages/contracts/src/ws/ws-event.payloads.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply D-03a fix per d03a-diagnosis.md root cause (Sheet + Dialog visibility)</name>
  <files>frontend/src/styles, frontend/package.json, frontend/bun.lock, frontend/src/components/ui/sheet.tsx, frontend/src/components/ui/dialog.tsx</files>
  <read_first>
    .planning/phases/10-quality-hardening-docs/d03a-diagnosis.md (Plan 10-01 Task 2 produced this — SINGLE SOURCE OF TRUTH for what to fix; read first, then act on the "Recommended fix for Wave 3 plan 10-06" line)
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Common Pitfalls #6" — the three hypothesis tree (a) tw-animate-css missing or pruned data-attr variants, (b) z-index collision, (c) shadcn defaults; the diagnosis file says which one
    frontend/src/styles (whatever the global CSS file is — likely app.css or globals.css; the diagnosis file cites the path)
    frontend/src/components/ui/sheet.tsx + ui/dialog.tsx (the shadcn primitives — only edit if diagnosis says hypothesis (c))
  </read_first>
  <action>
    1. Open `.planning/phases/10-quality-hardening-docs/d03a-diagnosis.md` (Plan 10-01 Task 2 created it).
    2. Read the "Ranked verdict" and "Recommended fix" lines.
    3. Apply EXACTLY the recommended fix. The fix path differs per hypothesis — do not deviate from the diagnosis recommendation:
       - **If hypothesis (a) missing `tw-animate-css` plugin**: `cd frontend && bun add -d tw-animate-css@latest` + add `@plugin "tw-animate-css";` to the global CSS file at the path cited in the diagnosis (likely `frontend/src/styles/app.css`). Do not edit other CSS.
       - **If hypothesis (a) pruned utility classes via `@source` directive override**: edit the global CSS `@source` block to re-include the component-scanning paths the diagnosis names, so `data-[state=open]:*` variants are discovered. Do not remove any other `@source` line.
       - **If hypothesis (b) z-index collision**: edit ONLY the colliding selector identified in the diagnosis. Sheet + Dialog Content default `z-50` should stay; the colliding element gets `z-40` or lower per UI-SPEC docs.
       - **If hypothesis (c) shadcn sheet.tsx/dialog.tsx defaults**: run `bunx shadcn@latest add sheet dialog --overwrite` to refresh both component files. Note any non-trivial diff to confirm only animation/transform CSS changed (no behavior change).
    4. After applying the fix, restart the dev server and visually confirm:
       - Click Fairness badge → drawer slides in from right → content visible within ~300ms.
       - Click any history strip Replay → modal opens centered → content visible within ~300ms.
       - Close + reopen each twice — animation works on both open and close.
    5. Take a screenshot of each opened component and commit them to `.planning/phases/10-quality-hardening-docs/d03a-fix-verified/{fairness-drawer-open.png,replay-modal-open.png}` as visual evidence (per CLAUDE.md no AI attribution in the commit msg, no emojis).
    6. Commit message: `fix(10-06): D-03a Sheet/Dialog visibility per d03a-diagnosis.md` (no AI attribution).

    Do NOT modify `verification-drawer.tsx` or `replay-modal.tsx` — those Phase 8 components correctly set state; the root cause is in the primitives layer (per diagnosis).
  </action>
  <verify>
    <automated>cd frontend && bun tsc --noEmit && bun test && test -f .planning/phases/10-quality-hardening-docs/d03a-fix-verified/fairness-drawer-open.png && test -f .planning/phases/10-quality-hardening-docs/d03a-fix-verified/replay-modal-open.png</automated>
  </verify>
  <done>Fix applied per diagnosis recommendation; tsc + existing FE tests still green (Phase 7/8/9 regression-free); both screenshots committed proving visual open; no edits to verification-drawer.tsx / replay-modal.tsx (Phase 8 state-management correct).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-03b schema relax + FE dispatcher null no-op + regression test</name>
  <files>packages/contracts/src/ws/ws-event.payloads.ts, packages/contracts/tests, frontend/src/lib, frontend/src/features</files>
  <behavior>
    Test 1 (packages/contracts/tests/ws/round-snapshot-nullable.test.ts NEW): `roundSnapshotPayloadSchema.safeParse(null).success === true`; `roundSnapshotPayloadSchema.safeParse({ /* valid prior shape */ }).success === true`; `roundSnapshotPayloadSchema.safeParse({ /* malformed */ }).success === false` — proves nullable accepts BOTH null AND prior valid shapes; rejects garbage.
    Test 2 (extends existing frontend WS dispatcher test): when dispatcher receives `round:snapshot` with payload `null`, NO error is thrown, NO toast surfaces, NO console warn emitted, AND the round store is NOT mutated; the dispatcher returns from the null branch silently.
  </behavior>
  <read_first>
    packages/contracts/src/ws/ws-event.payloads.ts (current roundSnapshotPayloadSchema — locate the object schema for round:snapshot; it is currently strict object; we wrap with `.nullable()`)
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Common Pitfalls #9" — exact snippet for the schema + dispatcher null branch
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Architectural Responsibility Map" D-03b row — FE-side schema fix; FE dispatcher no-ops on null
    frontend's WS dispatcher (search: grep -r "roundSnapshotPayloadSchema\|round:snapshot" frontend/src/ — likely under frontend/src/features/socket/ or frontend/src/lib/ws-dispatch.ts; reuse the existing dispatcher pattern)
  </read_first>
  <action>
    1. Edit `packages/contracts/src/ws/ws-event.payloads.ts`: the existing `roundSnapshotPayloadSchema` (currently a strict z.object) becomes the same object wrapped with `.nullable()`. Update the inferred type `RoundSnapshotPayload = z.infer<typeof roundSnapshotPayloadSchema>` — it now permits `RoundSnapshotObject | null`. Per RESEARCH snippet: introduce a `roundSnapshotObjectSchema` alias for the original object-only schema (used by tests that still want strict validation of populated snapshots) + export both.

    2. Add `packages/contracts/tests/ws/round-snapshot-nullable.test.ts` covering Test 1 above.

    3. Update the frontend WS dispatcher (the file imported from `packages/contracts` for `roundSnapshotPayloadSchema` — find it by grep). In the `round:snapshot` branch, after `safeParse`, add an explicit `if (result.data === null) return;` BEFORE the `roundStore.applySnapshot(result.data)` call. This preserves the "wait for next round:started" behavior per RESEARCH Pitfall 9.

    4. Extend the existing dispatcher test (locate via grep — likely `frontend/src/lib/ws-dispatch.test.ts` or under frontend/src/features/socket/) with Test 2 above.

    5. Run the live game: `docker compose restart games` + reload http://localhost:3000 between rounds (catch the empty-snapshot window). The console must show NO zod warn; the FE simply waits for the next `round:started` event.

    Critical: do NOT relax any other payload schema; only the snapshot one. Plan-wide grep gate ensures the `.nullable()` is on the snapshot schema only, not somewhere else.
  </action>
  <verify>
    <automated>cd packages/contracts && bun tsc --noEmit && bun test tests/ws/round-snapshot-nullable.test.ts && grep -c ".nullable()" src/ws/ws-event.payloads.ts | grep -qE '^[1]$' && cd ../../frontend && bun tsc --noEmit && bun test 2>&1 | grep -E "fail|pass" | tail -3 && grep -q "=== null" $(grep -rl "roundSnapshotPayloadSchema" src/ | head -1)</automated>
  </verify>
  <done>roundSnapshotPayloadSchema accepts null; FE dispatcher branches on null + no-ops; contracts test + extended FE dispatcher test green; tsc clean in both packages; no other payload schema relaxed (`.nullable()` count remains exactly 1 in ws-event.payloads.ts).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-03c HistoryStrip React key warn — verify live FIRST, fix only if still warning</name>
  <what-built>RESEARCH Pitfall 10 + Assumption A3: the current `history-strip.tsx` already uses `key={entry.roundId}` which IS valid. The warn may be stale from an earlier version. The diagnosis-first protocol from D-03a applies here too — verify live before applying any fix.

  Claude has already pre-positioned for the fix path if needed: if the live console still shows the warn, the disambiguator is a single-character commit (`key={entry.roundId}-${entry.crashedAt}` or move key to outer wrapper).</what-built>
  <how-to-verify>
    1. From repo root: `bun run docker:up --wait` then `cd frontend && bun run dev`.
    2. Open http://localhost:3000, log in.
    3. Open Chrome DevTools Console (Cmd+Option+J). Clear it.
    4. Watch ≥3 rounds complete (so new history entries push to the head of the strip via WS `round:crashed`).
    5. Search the console for any warn matching "Each child in a list should have a unique 'key' prop" OR "Encountered two children with the same key".
    6. **If no warn**: defect is already resolved. Document in commit message: `chore(10-06): D-03c verified resolved — no React key warn observed across 3+ rounds`. Skip the fix code change.
    7. **If warn appears**: identify the specific key collision from the warn's component stack trace. Likely causes (per RESEARCH Pitfall 10):
       - Duplicate `roundId` values (history store dedup bug) — apply `key={`${entry.roundId}-${entry.crashedAt}`}` as a deterministic disambiguator
       - Key on wrong wrapper (Radix Tooltip unmount-remount) — move `key` from `<Tooltip>` to the inner `<button>` per RESEARCH recommendation
       Apply the minimal fix that resolves the warn. Re-test live ≥3 rounds to confirm warn never reappears.
    8. Commit either the verification-only message OR the fix commit (`fix(10-06): D-03c HistoryStrip key warn — <root cause> resolved`).
    9. Resume after either path lands.
  </how-to-verify>
  <resume-signal>Type "verified-no-warn" if the warn was never observed in step 5; type "fixed" if the warn was observed and a fix landed. Quote the warn text + commit hash either way for the SUMMARY.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS server -> FE schema validation | Schema relaxation must remain restrictive enough to reject malformed payloads — only null is added as a valid value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-15 | Tampering | roundSnapshotPayloadSchema relaxation | mitigate | `.nullable()` accepts null OR the strict object — does NOT accept arbitrary garbage; regression test asserts malformed payloads still rejected |
| T-10-16 | Information Disclosure | D-03a hypothesis (b) z-index | accept | Z-index value changes are CSS-only; no data flows through the change |
</threat_model>

<verification>
- `bun --cwd packages/contracts test` green; `bun --cwd frontend test` green
- `grep -c "\.nullable()" packages/contracts/src/ws/ws-event.payloads.ts` returns exactly 1 (only the snapshot schema)
- D-03a fix screenshots exist under `.planning/phases/10-quality-hardening-docs/d03a-fix-verified/`
- Live smoke: open the Fairness drawer, the Replay modal, watch 3 rounds — all three defects do not reproduce
- Phase 7/8/9 unit + integration tests still green (regression check)
</verification>

<success_criteria>
1. Plan 10-07 Playwright spec for cashout can navigate to the Fairness drawer + Replay modal without timing out on a never-visible element.
2. Wave 4 plan 10-09 can claim "Phase 10 ships zero visible defects" in the README troubleshooting section without lying.
3. WS reconnect between rounds is silent (no zod warn in console) — recruiter-friendly.
</success_criteria>

<output>
Create `.planning/phases/10-quality-hardening-docs/10-06-SUMMARY.md` when done. Cite (a) the d03a-diagnosis recommended-fix line and the actual applied diff, (b) the .nullable() schema change, (c) the D-03c verify-vs-fix outcome.
</output>
