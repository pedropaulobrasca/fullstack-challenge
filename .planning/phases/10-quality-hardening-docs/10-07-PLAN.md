---
phase: 10-quality-hardening-docs
plan: 07
type: execute
wave: 4
depends_on:
  - 10-01
  - 10-02
  - 10-06
files_modified:
  - e2e/specs/bet-cashout.spec.ts
  - e2e/specs/bet-crash.spec.ts
  - e2e/fixtures/auth.fixture.ts
  - e2e/playwright.config.ts
  - frontend/src/components
autonomous: true
requirements:
  - REQ-TEST-05

must_haves:
  truths:
    - "Spec (a) login -> wait BETTING -> place bet -> wait RUNNING -> cashout -> balance updated PASSES against live docker stack"
    - "Spec (b) login -> bet -> wait crash -> bet lost PASSES against live docker stack"
    - "Both specs use storageState fixture so OIDC login runs ONCE in globalSetup (Pattern 4 — 10× faster CI)"
    - "Specs run serially (workers=1) and complete in well under the 30-min CI timeout"
    - "Tests assert via data-testid hooks added in frontend if needed (no brittle text selectors)"
  artifacts:
    - path: "e2e/specs/bet-cashout.spec.ts"
      provides: "REQ-TEST-05(a) full implementation"
      contains: "place bet"
    - path: "e2e/specs/bet-crash.spec.ts"
      provides: "REQ-TEST-05(b) full implementation"
      contains: "crash"
  key_links:
    - from: "e2e/specs/*.spec.ts"
      to: "e2e/fixtures/auth.fixture.ts STORAGE_STATE_PATH"
      via: "test.use({ storageState })"
      pattern: "storageState"
    - from: "e2e/specs/*.spec.ts"
      to: "frontend testid hooks (data-testid balance-pill, bet-amount-input, connection-badge)"
      via: "page.getByTestId"
      pattern: "getByTestId"
---

<objective>
Replace the Plan 10-01 spec scaffolds (test.skip) with the two real Playwright specs satisfying REQ-TEST-05: (a) login → wait BETTING → place bet → wait RUNNING → cashout → verify balance changed; (b) login → wait BETTING → place bet → wait crash → verify bet lost. Both run against the live docker:up stack (no mocks).

Purpose: REQ-TEST-05 closure. Wave 4 plan 10-08 CI workflow runs these specs as the final gate. Wave 3 plan 10-06 fixed the polish defects, so Sheet/Dialog visibility no longer blocks any spec (this plan does NOT cover those surfaces per D-02 — strictly the cashout-happy + crash-lost flows).
Output: 2 fully implemented Playwright specs + any frontend data-testid attribute additions needed for robust selectors (no brittle text matching).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-quality-hardening-docs/10-RESEARCH.md
@.planning/phases/10-quality-hardening-docs/10-CONTEXT.md
@.planning/phases/10-quality-hardening-docs/10-01-SUMMARY.md
@.planning/phases/10-quality-hardening-docs/10-06-SUMMARY.md
@CLAUDE.md
@e2e/playwright.config.ts
@e2e/fixtures/auth.fixture.ts
@e2e/specs/bet-cashout.spec.ts
@e2e/specs/bet-crash.spec.ts
@frontend/src/components
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-testid hooks to FE components consumed by Playwright (balance-pill, connection-badge, bet-amount-input, bet-place-button, cashout-button, round-status, bet-row)</name>
  <files>frontend/src/components</files>
  <read_first>
    frontend/src/components/ (Phase 7 BalancePill, ConnectionBadge, BetPanel, CashoutButton, HistoryStrip — Phase 7 plan 07-04..07-08 created them; locate via grep for "BalancePill\|ConnectionBadge\|BetPanel\|CashoutButton" — Phase 7 already used some testids; we ADD only where missing)
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Code Examples / e2e/specs/bet-cashout.spec.ts" — exact testid names the spec depends on: `connection-badge`, `balance-pill`, `bet-amount-input`, `[data-status="BETTING"]`, `[data-status="RUNNING"]`
    frontend's index.tsx / game page that owns the round-status data attribute (where the BETTING/RUNNING/CRASHED status renders — likely a wrapper around CrashCurve or the page-level shell)
  </read_first>
  <action>
    Add `data-testid` attributes (NOT classnames; testids are stable selectors per Playwright best practice) to the following FE elements. If a testid already exists with a different name, keep the existing name and update Task 2's spec selectors instead. Run `grep -r "data-testid" frontend/src/` first to inventory existing testids.

    - `BalancePill`: outermost element gets `data-testid="balance-pill"`
    - `ConnectionBadge`: outermost element gets `data-testid="connection-badge"`
    - `BetPanel` / bet amount input: input element gets `data-testid="bet-amount-input"`
    - `BetPanel` / "Place Bet" button: button gets `data-testid="bet-place-button"` (optional — `getByRole("button", { name: /place bet/i })` works too, but testid is more stable)
    - `CashoutButton`: button gets `data-testid="cashout-button"`
    - Page-level round status wrapper: `data-status={round.status}` attribute (so the spec can `waitForSelector('[data-status="BETTING"]')` and `[data-status="RUNNING"]`)
    - Optional: `BetRow` in LiveFeed: `data-testid="bet-row"` + `data-bet-status={bet.status}` for the crash-lost spec to verify the player's bet flipped to LOST.

    Add NO new components; only attribute additions. Existing styling + behavior must be byte-stable per Phase 7 D-01 grid invariant. Run the existing FE test suite to confirm no regression.
  </action>
  <verify>
    <automated>cd frontend && bun tsc --noEmit && bun test 2>&1 | tail -5 && grep -rq 'data-testid="balance-pill"' src/components/ && grep -rq 'data-testid="connection-badge"' src/components/ && grep -rq 'data-testid="bet-amount-input"' src/components/ && grep -rq 'data-status=' src/ </automated>
  </verify>
  <done>All 6 testids added (or existing equivalents documented); FE tsc + tests still green; no visual or behavior regression.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the 2 Playwright specs against the live stack</name>
  <files>e2e/specs/bet-cashout.spec.ts, e2e/specs/bet-crash.spec.ts, e2e/fixtures/auth.fixture.ts, e2e/playwright.config.ts</files>
  <read_first>
    e2e/playwright.config.ts (Plan 10-01 — webServer + globalSetup configured)
    e2e/fixtures/auth.fixture.ts (Plan 10-01 — OIDC login → storageState pattern; we may need to extend to capture starting balance pre-login OR after first login on the game page)
    e2e/specs/bet-cashout.spec.ts + e2e/specs/bet-crash.spec.ts (Plan 10-01 scaffolds with test.skip — we replace skip with real test)
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Code Examples / e2e/specs/bet-cashout.spec.ts" — the canonical spec body for the cashout flow; the crash spec mirrors it minus the cashout click
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Validation Architecture / Phase Requirements → Test Map" REQ-TEST-05 rows — both spec contracts
    .planning/REQUIREMENTS.md "Open Configuration Values" — BETTING_WINDOW_MS (5000) drives the BETTING timeout floor in the spec; reference env not literal where possible
  </read_first>
  <action>
    Replace the `test.skip` scaffold in `e2e/specs/bet-cashout.spec.ts` with a real `test` body matching RESEARCH "Code Examples / e2e/specs/bet-cashout.spec.ts" verbatim, adapted to the testids confirmed in Task 1:

    ```
    test("login -> BETTING -> bet -> RUNNING -> cashout -> balance updated", async ({ page }) => {
      await page.goto("/");
      await expect(page.getByTestId("connection-badge")).toContainText(/live|connected/i, { timeout: 15_000 });
      const startBalance = (await page.getByTestId("balance-pill").innerText()).trim();
      await page.waitForSelector('[data-status="BETTING"]', { timeout: 30_000 });
      await page.getByTestId("bet-amount-input").fill("5.00");
      await page.getByTestId("bet-place-button").click();
      await page.waitForSelector('[data-status="RUNNING"]', { timeout: 15_000 });
      await page.getByTestId("cashout-button").click();
      await expect(page.getByTestId("balance-pill")).not.toContainText(startBalance, { timeout: 10_000 });
    });
    ```

    Use `test.use({ storageState: STORAGE_STATE_PATH })` at the top of the file (RESEARCH Pattern 4).

    Implement `e2e/specs/bet-crash.spec.ts` similarly. The crash spec waits for `[data-status="CRASHED"]` after placing the bet (NEVER click cashout), then asserts the player's bet row appears with `data-bet-status="LOST"` (or whichever bet status string the FE uses post-crash for non-cashed bets — confirm via Phase 7 plan 07-07 LiveFeed component):

    ```
    test("login -> bet -> wait crash -> bet lost", async ({ page }) => {
      await page.goto("/");
      await expect(page.getByTestId("connection-badge")).toContainText(/live|connected/i, { timeout: 15_000 });
      await page.waitForSelector('[data-status="BETTING"]', { timeout: 30_000 });
      await page.getByTestId("bet-amount-input").fill("5.00");
      await page.getByTestId("bet-place-button").click();
      await page.waitForSelector('[data-status="CRASHED"]', { timeout: 60_000 });
      await expect(page.locator('[data-testid="bet-row"][data-bet-status="LOST"]').first()).toBeVisible({ timeout: 10_000 });
    });
    ```

    The crash timeout is generous (60s) because the crash point distribution can land anywhere from instant (1.00x at 1-in-101) to ~100x — the spec must wait for whatever crash time the round draws.

    Both specs run serially (Plan 10-01 set workers=1, fullyParallel=false). The cashout spec runs first by alphabetic order, then crash; both bets are 5.00 CRD against a 1000.00 CRD wallet — wallet doesn't deplete.

    After implementing both specs, run them locally end-to-end:
    - `bun run docker:up --wait` + `cd frontend && bun run dev` in second terminal
    - `bunx playwright test --config=e2e/playwright.config.ts`
    - BOTH specs must PASS without retries needed locally

    If the first spec fails, investigate the failure (videos in `test-results/`); do NOT loosen assertions to make them pass. The only acceptable change is to swap one stable selector for another stable selector. If the live game itself reveals a missing data-testid not anticipated in Task 1, add it back to the FE component then re-run.
  </action>
  <verify>
    <automated>cd /Users/pedro/Projetos/fullstack-challenge && bun run docker:up --wait && (cd frontend && bun run dev &) && sleep 8 && bunx playwright test --config=e2e/playwright.config.ts 2>&1 | tee /tmp/playwright-out.txt && grep -E "2 passed" /tmp/playwright-out.txt</automated>
  </verify>
  <done>Both specs PASS against the live stack with zero retries; videos/screenshots ONLY captured on failure (none in the success run); spec files reference the testids from Task 1; auth.fixture.ts unchanged (existing globalSetup still works); no `test.skip` remains in either spec file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright headless browser -> Keycloak / FE / Kong | Same boundaries as real users; spec drives real OIDC PKCE flow |
| storageState JSON file -> CI artifacts | OIDC tokens must NEVER be uploaded to artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-17 | Information Disclosure | e2e/.auth/player.json with OIDC tokens | mitigate | Plan 10-01 .gitignore excludes .auth/; Wave 4 plan 10-08 CI workflow does NOT upload .auth/ to artifacts (artifact path is e2e/playwright-report/ + e2e/test-results/) |
| T-10-18 | Tampering | Spec selectors using text matching | mitigate | All assertions use data-testid (stable hooks) — text matching is for assertion-only on balance content, not for element selection |
</threat_model>

<verification>
- `bunx playwright test --config=e2e/playwright.config.ts` exits 0 with "2 passed"
- `grep -c "test\.skip" e2e/specs/*.spec.ts` returns 0
- `grep -c "getByTestId" e2e/specs/*.spec.ts` returns at least 4 (testids used in both specs)
- Local recruiter-style smoke: `bun run docker:up + frontend dev + bunx playwright test` works end-to-end in well under 5 min
</verification>

<success_criteria>
1. Wave 4 plan 10-08 CI workflow can run `bunx playwright test --config=e2e/playwright.config.ts` and expect zero failures.
2. REQ-TEST-05 closes — both Phase 10 traceability rows flip from Pending to Done.
3. The submission's "every claim is provable by automation" property is demonstrably true for the headline player flows.
</success_criteria>

<output>
Create `.planning/phases/10-quality-hardening-docs/10-07-SUMMARY.md` when done. Cite the wall-clock duration of each spec + any data-testid additions to the FE.
</output>
