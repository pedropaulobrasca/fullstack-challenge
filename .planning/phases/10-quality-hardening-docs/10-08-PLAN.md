---
phase: 10-quality-hardening-docs
plan: 08
type: execute
wave: 5
depends_on:
  - 10-01
  - 10-02
  - 10-03
  - 10-04
  - 10-05
  - 10-06
  - 10-07
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - REQ-CI-01
  - REQ-CI-02

must_haves:
  truths:
    - "Single .github/workflows/ci.yml triggers on push to main + every pull_request, with concurrency-cancel per ref"
    - "Job runs on ubuntu-latest with timeout-minutes: 30 (Pitfall 8 safety margin)"
    - "Free-disk-space pre-step prunes the runner image cache (Pitfall 8 — runner ~14GB free vs ~10GB compose images)"
    - "Pipeline: checkout -> setup-bun (.bun-version) -> bun install --frozen-lockfile -> bun run lint -> bun run typecheck -> unit tests -> bun run docker:up --wait -> integration (INTEGRATION=1) -> bunx playwright install chromium -> bunx playwright test -> always() bun run docker:down -> upload playwright artifacts on failure"
    - "Workflow proves on a fresh clone the zero-step bootstrap claim (REQ-CI-02 surface)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Single CI workflow that boots full docker stack + runs unit + integration + Playwright + tears down"
      contains: "docker:up"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/smoke-health.sh + e2e/playwright.config.ts + bun run docker:up"
      via: "step ordering in jobs.full-stack.steps"
      pattern: "playwright"
---

<objective>
Land the single GitHub Actions workflow (per D-07) that proves on every push/PR the REQ-CI-02 zero-step bootstrap claim: clean ubuntu-latest runner pulls the repo, installs bun, runs lint + typecheck + unit tests, boots the full docker compose stack, runs integration tests + the 2 Playwright specs against the live stack, then tears down + uploads failure artifacts. No matrices, no sharding, no self-hosted runners — single Ubuntu job, single Playwright shard, single concurrency group per branch (cancel-in-progress on a new push).

Purpose: REQ-CI-01 + REQ-CI-02 closure. README badge (REQ-CI-03) wires up in Wave 4 plan 10-09 once the workflow file lives.
Output: One workflow file at `.github/workflows/ci.yml` per RESEARCH "Pattern 5: GitHub Actions workflow" + Pitfall 8 free-disk-space guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-quality-hardening-docs/10-RESEARCH.md
@.planning/phases/10-quality-hardening-docs/10-CONTEXT.md
@CLAUDE.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/ci.yml per RESEARCH Pattern 5 with Pitfall 8 disk-space guard</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Pattern 5: GitHub Actions workflow" — canonical workflow YAML body with steps, concurrency, retention-days, upload-artifact paths
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Common Pitfalls #8" — free-disk-space step pruning /usr/share/dotnet + /opt/ghc + /usr/local/share/boost + AGENT_TOOLSDIRECTORY + docker image prune; required because ubuntu-latest ships ~14GB free and the full compose stack consumes ~10GB
    package.json (root scripts — confirm `docker:up` / `docker:down` / `lint` / `typecheck` / `test` script names; ci.yml MUST invoke the existing names exactly)
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Assumption A4" — ubuntu-latest preinstalled docker compose v2 (no setup-docker step needed)
    e2e/playwright.config.ts (Plan 10-01 path — `--config=e2e/playwright.config.ts`)
    .bun-version (bun version pin for setup-bun action — match exactly per RESEARCH Pattern 5 example shows bun-version: 1.3.11)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` per RESEARCH Pattern 5, augmented with Pitfall 8 disk-cleanup step. Structure:

    - `name: CI`
    - `on: { push: { branches: [main] }, pull_request: {} }` (REQ-CI-01)
    - `concurrency: { group: ${{ github.workflow }}-${{ github.ref }}, cancel-in-progress: true }` — same-ref pushes cancel earlier runs
    - `jobs.full-stack:` on `runs-on: ubuntu-latest`, `timeout-minutes: 30` (Pitfall 8 safety margin)
    - Steps in order:
      1. `actions/checkout@v4`
      2. **Free disk space** (Pitfall 8): `sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/share/boost "$AGENT_TOOLSDIRECTORY" && docker image prune -af`
      3. `oven-sh/setup-bun@v2` with `bun-version: <read from .bun-version>` (DO NOT hardcode; either read the file or use the `bun-version-file: .bun-version` action input — RESEARCH cites 1.3.11; verify against the current `.bun-version` file)
      4. `name: Install deps` `run: bun install --frozen-lockfile`
      5. `name: Lint` `run: bun run lint`
      6. `name: Typecheck` `run: bun run typecheck`
      7. `name: Unit tests` `run: bun test` (runs root workspace unit tests across services/games + services/wallets + packages/contracts + packages/shared-kernel + packages/messaging-spine + frontend)
      8. `name: Boot full docker stack` `run: bun run docker:up --wait` with env `OTEL_EXPORTER_OTLP_ENDPOINT: http://localhost:4318/v1/traces` (RESEARCH Pattern 5 example)
      9. `name: Smoke probes (1-47)` `run: bash scripts/smoke-health.sh` (Plan 10-04 extended to 47 probes; CI fast-fails if any infra probe fails before Playwright runs)
      10. `name: Integration tests (INTEGRATION=1)` `run: INTEGRATION=1 bun --cwd services/games test tests/integration && INTEGRATION=1 bun --cwd services/wallets test tests/integration` (mirror the existing local integration command)
      11. `name: Install Playwright browsers` `run: cd frontend && bunx playwright install --with-deps chromium`
      12. `name: Playwright E2E` `run: bunx playwright test --config=e2e/playwright.config.ts` (the FE dev server boots via the webServer block in the config — `reuseExistingServer: !process.env.CI` honors the CI=true env)
      13. **always() tear down**: `name: Tear down stack` `if: always()` `run: bun run docker:down`
      14. `actions/upload-artifact@v4` `if: failure()` with `name: playwright-report` `path: |\n  e2e/playwright-report/\n  e2e/test-results/` `retention-days: 14`

    DO NOT include any step that uploads `e2e/.auth/` (Plan 10-07 Threat T-10-17 mitigation). DO NOT add Codecov per RESEARCH Deferred Ideas. DO NOT add OS or Node matrix per D-07.

    The lint / typecheck / unit / integration steps follow the same commands a developer uses locally — preserves the "what CI runs is what dev runs" invariant.

    Per CLAUDE.md commit-hygiene: commit message `ci(10-08): full-stack workflow with docker:up bootstrap proof` — no AI attribution.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q "docker:up --wait" .github/workflows/ci.yml && grep -q "playwright test" .github/workflows/ci.yml && grep -q "oven-sh/setup-bun" .github/workflows/ci.yml && grep -q "concurrency:" .github/workflows/ci.yml && grep -q "cancel-in-progress: true" .github/workflows/ci.yml && grep -q "if: always()" .github/workflows/ci.yml && grep -q "if: failure()" .github/workflows/ci.yml && grep -q "free.*disk\|/usr/share/dotnet" .github/workflows/ci.yml && grep -q "INTEGRATION=1" .github/workflows/ci.yml && ! grep -q ".auth/" .github/workflows/ci.yml && (command -v actionlint && actionlint .github/workflows/ci.yml || echo "actionlint not installed — schema check skipped")</automated>
  </verify>
  <done>ci.yml exists; all required steps present in correct order; concurrency cancel + 30-min timeout + disk-cleanup + always() teardown + failure() artifact upload all present; .auth/ NOT referenced anywhere in the workflow; actionlint (if available) reports clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Push a branch / open a draft PR to trigger the CI workflow and verify a green run end-to-end before merge</name>
  <what-built>The workflow file lives but the only true verification is a real GitHub Actions run against the repo. RESEARCH "deep_work_rules" requires the CI gate include "a green build run on the PR before merge" — this checkpoint enforces it.</what-built>
  <how-to-verify>
    1. From a clean branch off main: `git push -u origin HEAD` (or open a draft PR via `gh pr create --draft --title "ci: phase 10 workflow proof" --body "Verifies REQ-CI-02 zero-step bootstrap"`).
    2. In the GitHub UI Actions tab, watch the `CI` workflow run for the new push.
    3. Confirm the run hits each step:
       - Free disk space (logs show >10GB freed)
       - Install deps (bun install completes)
       - Lint + Typecheck (both green)
       - Unit tests (bun test green)
       - Boot full docker stack (docker:up --wait returns within ~2-3 min)
       - Smoke probes 1-47 (all PASS)
       - Integration tests (INTEGRATION=1 green in both services)
       - Install Playwright chromium (~30s)
       - Playwright E2E (2 specs pass)
       - Tear down (always step runs cleanly)
       - No failure artifacts uploaded (no failure)
    4. If any step fails, download the failure artifacts (playwright-report) to diagnose. Apply the minimal fix (likely a step ordering, env var, or selector tweak) and push again.
    5. The workflow run must turn GREEN before merging. Capture the workflow run URL for the SUMMARY.

    Resume after the workflow shows a fully green run (all steps successful).
  </how-to-verify>
  <resume-signal>Type "ci-green" with the workflow-run URL (e.g. https://github.com/<org>/<repo>/actions/runs/<id>) once the run is fully successful end-to-end.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub-hosted runner -> repo + npm registry | Runner pulls only first-party code + audited deps from Plan 10-02; no third-party network calls beyond the locked package set |
| CI workflow output -> repo artifacts | Failure artifacts MUST NOT include .auth/ (OIDC tokens) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-19 | Information Disclosure | Failure artifact upload paths | mitigate | upload-artifact path explicitly lists e2e/playwright-report/ + e2e/test-results/; .auth/ excluded; grep gate `! grep -q ".auth/" .github/workflows/ci.yml` |
| T-10-20 | Tampering | Pull-request workflow runs untrusted code | accept | Workflow runs on `pull_request` (which uses base-ref permissions by default for forks); the `secrets:` block is not consumed; no credentialed actions called |
| T-10-21 | Denial of Service | Concurrent runs exhausting Actions minutes | mitigate | concurrency cancel-in-progress per ref → only the latest commit per branch runs; older runs auto-cancel |
| T-10-22 | DoS via disk exhaustion | Runner disk fills during docker:up | mitigate | Pitfall 8 free-disk-space step before docker:up prunes runner image cache; sudo rm -rf well-known paths frees ~10GB |
</threat_model>

<verification>
- `.github/workflows/ci.yml` exists and matches all grep gates from Task 1
- GitHub UI shows a green workflow run on the test branch / draft PR
- Failure artifact upload path does NOT reference e2e/.auth/
- Workflow run wall-clock duration well under 30-min timeout (expected 8-15 min including docker:up + Playwright)
</verification>

<success_criteria>
1. Wave 4 plan 10-09 README badge URL (https://github.com/<org>/<repo>/actions/workflows/ci.yml/badge.svg) renders green on first push.
2. REQ-CI-01 + REQ-CI-02 close — Phase 10 traceability rows flip to Done.
3. The "zero-step bootstrap" claim is proven by automation: a fresh checkout on a clean runner brings the whole stack up and runs the player flow end-to-end.
</success_criteria>

<output>
Create `.planning/phases/10-quality-hardening-docs/10-08-SUMMARY.md` when done. Cite the workflow-run URL + wall-clock duration of the green run.
</output>
