---
phase: 10-quality-hardening-docs
plan: 09
type: execute
wave: 6
depends_on:
  - 10-04
  - 10-05
  - 10-06
  - 10-07
  - 10-08
files_modified:
  - README.md
  - scripts/build-adr-index.ts
  - scripts/build-adr-index.test.ts
  - .planning/adrs/ADR-035-otel-jaeger-stack.md
  - .planning/adrs/ADR-036-adr-catalogue-in-readme.md
  - .planning/adrs/ADR-037-ci-runs-full-stack.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - package.json
autonomous: true
requirements:
  - REQ-CI-03
  - REQ-DOC-01
  - REQ-DOC-02

must_haves:
  truths:
    - "README has Quick Start (one command) + Architecture mermaid + Saga Flow mermaid + Provably-Fair link preserved + ADR Catalogue table generated + Scripts + Env Vars + Troubleshooting + CI badge"
    - "scripts/build-adr-index.ts auto-generates the ADR markdown table between sentinel markers (no hand-maintenance)"
    - "ADR-035 / ADR-036 / ADR-037 authored at next-free numbers (031 was last per Phase 8, 032-034 from Phase 9, so Phase 10 takes 035-037 per the renumber precedent set Phase 7/8/9)"
    - "STATE.md + ROADMAP.md + REQUIREMENTS.md reflect Phase 10 complete + 10/10 phases done"
    - "build-adr-index runs in CI (added to workflow) OR via pre-commit (chosen path documented in ADR-036)"
  artifacts:
    - path: "README.md"
      provides: "Full README with all D-08 sections + CI badge + generated ADR table"
      contains: "ADR-INDEX:START"
    - path: "scripts/build-adr-index.ts"
      provides: "Bun-run script reading .planning/adrs/ADR-*.md frontmatter -> markdown table"
      contains: "ADR-INDEX:START"
    - path: ".planning/adrs/ADR-035-otel-jaeger-stack.md"
      provides: "OTel SDK + nestjs-otel + Jaeger choice ADR"
      contains: "OpenTelemetry"
    - path: ".planning/adrs/ADR-036-adr-catalogue-in-readme.md"
      provides: "ADR catalogue location + generator script ADR"
      contains: "build-adr-index"
    - path: ".planning/adrs/ADR-037-ci-runs-full-stack.md"
      provides: "CI runs full docker:up stack on every push ADR"
      contains: "REQ-CI-02"
  key_links:
    - from: "README.md"
      to: ".planning/adrs/ADR-*.md (34 prior + 3 new)"
      via: "build-adr-index.ts output between sentinel markers"
      pattern: "ADR-INDEX:START"
    - from: "README.md CI badge"
      to: ".github/workflows/ci.yml"
      via: "actions/workflows/ci.yml/badge.svg URL"
      pattern: "ci\\.yml/badge\\.svg"
---

<objective>
Closeout Phase 10 and the entire 10-phase project:

1. **README** — add the D-08 sections (Quick Start, Architecture mermaid, Saga Flow mermaid, ADR Catalogue table marker, Scripts, Env Vars, Troubleshooting, CI badge) around the existing Provably-Fair Phase 8 section (preserved verbatim).
2. **ADR Catalogue generator** — `scripts/build-adr-index.ts` reads `.planning/adrs/ADR-*.md` frontmatter (status / date / phase / title) and emits the markdown table between `<!-- ADR-INDEX:START -->` / `<!-- ADR-INDEX:END -->` sentinel markers in README (RESEARCH "Code Examples / ADR catalogue index script"). Add the script to CI so README ADR table is always in sync with `.planning/adrs/`.
3. **Three Phase 10 ADRs** at next-free numbers (031 was last Phase 8 → 032-034 Phase 9 → Phase 10 takes 035-037, per the renumber precedent set Phase 7/8/9 closeouts):
   - **ADR-035**: OpenTelemetry SDK + nestjs-otel + Jaeger all-in-one (D-05 + D-01; alternatives commercial APM and Tempo+Grafana rejected with rationale)
   - **ADR-036**: ADR catalogue lives in README via generator script (D-08; alternatives `.planning/adrs/INDEX.md` only or hand-maintained README table rejected)
   - **ADR-037**: CI runs full `docker:up` stack on every push (D-07; alternative mocked-deps unit-only pipeline rejected because it does NOT prove the zero-step bootstrap claim)
4. **State rotation** — STATE.md current-focus updated to "Phase 10 complete / project shippable"; ROADMAP.md Phase 10 marked `[x]` + Progress table updated; REQUIREMENTS.md flips REQ-TEST-05 + REQ-OBS-01..04 + REQ-CI-01..03 + REQ-DOC-01..02 from Pending to Done with plan citations.

Purpose: REQ-CI-03 + REQ-DOC-01 + REQ-DOC-02 closure. The submission is recruiter-ready after this plan.
Output: README ships D-08 sections + generated ADR table + CI badge; 3 new ADRs filed; STATE/ROADMAP/REQUIREMENTS reflect the closeout; package.json gains a `bun run docs:adr-index` script for the generator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-quality-hardening-docs/10-RESEARCH.md
@.planning/phases/10-quality-hardening-docs/10-CONTEXT.md
@.planning/phases/10-quality-hardening-docs/10-04-SUMMARY.md
@.planning/phases/10-quality-hardening-docs/10-05-SUMMARY.md
@.planning/phases/10-quality-hardening-docs/10-06-SUMMARY.md
@.planning/phases/10-quality-hardening-docs/10-07-SUMMARY.md
@.planning/phases/10-quality-hardening-docs/10-08-SUMMARY.md
@.planning/adrs
@README.md
@package.json
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author scripts/build-adr-index.ts + unit test + wire as bun script + add to CI workflow</name>
  <files>scripts/build-adr-index.ts, scripts/build-adr-index.test.ts, package.json, .github/workflows/ci.yml</files>
  <behavior>
    Test 1 (build-adr-index.test.ts): given a fixture directory with 3 ADR files at well-formed names (ADR-001-*.md, ADR-002-*.md, ADR-010-*.md) carrying expected Status / Date / Phase headers + `# ADR-NNN: Title` first line, the script's `renderTable` output contains 3 rows with correctly parsed number / title / phase / date / status; rows sorted by ADR number ascending.
    Test 2: given a README fixture with sentinel markers `<!-- ADR-INDEX:START -->` / `<!-- ADR-INDEX:END -->`, calling the script replaces ONLY the content between markers; content outside markers byte-stable.
    Test 3: malformed ADR file (missing status line) → row renders with status `(unknown)` rather than throwing.
  </behavior>
  <read_first>
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Code Examples / ADR catalogue index script (sentinel-marker pattern)" — full canonical script code including sentinel constants, readAdrs + renderTable functions, replace logic
    .planning/adrs/ (existing 34 ADRs — verify file naming pattern `ADR-NNN-slug.md`; verify the frontmatter convention: `# ADR-NNN: Title` first line + `**Status**: ...` + `**Date**: ...` + `**Phase**: N` lines; use one existing ADR like ADR-032-light-cqrs-leaderboard-read-model.md as the format reference)
    package.json root (existing scripts section — add `docs:adr-index` script)
    .github/workflows/ci.yml (Plan 10-08 — add a step that runs build-adr-index in --check mode and fails the build if README drift is detected; OR run the script and fail if `git diff README.md` shows changes after the run)
  </read_first>
  <action>
    1. Create `scripts/build-adr-index.ts` per RESEARCH "Code Examples / ADR catalogue index script" verbatim with these constants:
       - `ADRS_DIR = join(import.meta.dir, "..", ".planning", "adrs")`
       - `README_PATH = join(import.meta.dir, "..", "README.md")`
       - `START_MARKER = "<!-- ADR-INDEX:START -->"` / `END_MARKER = "<!-- ADR-INDEX:END -->"`
       - `readAdrs()`: `readdir(ADRS_DIR)`, filter `/^ADR-\d{3}-/`, parse number + title + status + date + phase + slug
       - `renderTable(adrs)`: header `| # | Title | Phase | Date | Status |` + sorted-by-number rows, each with `[Title](.planning/adrs/<slug>.md)` link
       - Replace between sentinel markers, throw if markers absent
       - Final `console.log(`ADR index rendered: ${adrs.length} entries`)`

    2. Add `--check` mode (optional but recommended): when invoked as `bun scripts/build-adr-index.ts --check`, the script computes the would-be README and exits non-zero if it differs from on-disk README — without writing. This is what CI consumes.

    3. Create `scripts/build-adr-index.test.ts` covering the 3 behaviors above. Use Bun.tempDir or `fs.mkdtemp` for the fixture ADR dir + README; tests must be hermetic (no real .planning/adrs reads in tests).

    4. Add to root `package.json` scripts:
       - `"docs:adr-index": "bun scripts/build-adr-index.ts"`
       - `"docs:adr-index:check": "bun scripts/build-adr-index.ts --check"`

    5. Edit `.github/workflows/ci.yml` (Plan 10-08): insert a new step after "Typecheck" and before "Unit tests": `name: ADR index sync check` `run: bun run docs:adr-index:check` — fails the build if README is stale relative to `.planning/adrs/`. This guarantees the README ADR table never drifts.
  </action>
  <verify>
    <automated>bun tsc --noEmit scripts/build-adr-index.ts && bun test scripts/build-adr-index.test.ts && grep -q "docs:adr-index" package.json && grep -q "ADR-INDEX:START" scripts/build-adr-index.ts && grep -q "docs:adr-index:check" .github/workflows/ci.yml</automated>
  </verify>
  <done>Script + test landed; package.json exposes both scripts; CI workflow includes the --check step; tests green; script handles 34+ ADRs without throwing.</done>
</task>

<task type="auto">
  <name>Task 2: Author ADR-035 + ADR-036 + ADR-037 at next-free numbers</name>
  <files>.planning/adrs/ADR-035-otel-jaeger-stack.md, .planning/adrs/ADR-036-adr-catalogue-in-readme.md, .planning/adrs/ADR-037-ci-runs-full-stack.md</files>
  <read_first>
    .planning/adrs/ADR-032-light-cqrs-leaderboard-read-model.md (Phase 9 format reference — Status / Date / Phase headers + Context / Considered / Decision / Consequences sections + Alternatives Rejected per CLAUDE.md ADR template)
    .planning/adrs/ADR-033-server-enforced-auto-cashout.md (ratifies-prior-ADR pattern reference; ADR-035 follows similar voice)
    CLAUDE.md "ADRs" section — exact template format
    .planning/phases/10-quality-hardening-docs/10-CONTEXT.md (D-05 OTel SDK rationale, D-01 Jaeger over Tempo rationale, D-08 ADR catalogue rationale, D-07 CI strategy rationale)
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Standard Stack / Alternatives Considered" + "Don't Hand-Roll" — the explicit rationale for the Phase 10 stack choices
  </read_first>
  <action>
    Author each ADR following the CLAUDE.md template (Status / Date / Phase / Context / Considered / Decision / Consequences / Alternatives Rejected sections).

    **ADR-035-otel-jaeger-stack.md** — OpenTelemetry SDK + nestjs-otel + Jaeger all-in-one
    - Phase: 10 / Status: Accepted / Date: today
    - Context: REQ-OBS-01..03 demand traces + metrics + dashboards on the player flow; needed an observability stack that proves saga correlation visually
    - Considered: (a) `@opentelemetry/sdk-node` + auto-instrumentations + Jaeger all-in-one (chosen); (b) commercial APM (Datadog / Splunk — rejected per RESEARCH Deferred Ideas: cost, lock-in, recruiter-unfamiliar credentials); (c) Tempo + Grafana for traces (rejected per D-01: 2 extra containers for marginal UX gain when Grafana already exists for metrics)
    - Decision: Adopted (a). Init order is Footgun #1 — `tracing.ts` is literal first import of main.ts in both services
    - Consequences: 3 new compose containers (Jaeger, Prometheus, Grafana); recruiter familiarity high; OTel SDK 0.x semver requires periodic minor-version bumps but breaking changes rare
    - Alternatives rejected with full rationale per RESEARCH "Alternatives Considered"
    - Cite: D-01 + D-05 + D-06 from 10-CONTEXT.md; RESEARCH "Standard Stack"

    **ADR-036-adr-catalogue-in-readme.md** — ADR catalogue lives in README via generator script
    - Phase: 10 / Status: Accepted / Date: today
    - Context: REQ-DOC-02 requires ADRs surfaced in README; 34 (now 37) ADRs across 10 phases would drift if hand-maintained
    - Considered: (a) generated table between sentinel markers in README via `scripts/build-adr-index.ts`, CI --check gate (chosen); (b) `.planning/adrs/INDEX.md` only with README link (rejected — recruiter scans README, not subdirs); (c) hand-maintained README table (rejected — drift inevitable; one missing row signals carelessness)
    - Decision: (a). Sentinel markers `<!-- ADR-INDEX:START -->` / `<!-- ADR-INDEX:END -->`; script runs in CI as `bun run docs:adr-index:check`
    - Consequences: README ADR table always in sync with `.planning/adrs/`; CI fails on drift; one new build script; ADR frontmatter format frozen (Status / Date / Phase / # ADR-NNN: Title pattern)
    - Cite: D-08 from 10-CONTEXT.md; RESEARCH "Code Examples / ADR catalogue index script"

    **ADR-037-ci-runs-full-stack.md** — CI runs full docker:up on every push
    - Phase: 10 / Status: Accepted / Date: today
    - Context: REQ-CI-02 demands the zero-step bootstrap claim be provable by automation; mocked-deps unit-only pipelines do not exercise compose healthchecks, realm import, migration ordering, AMQP topology, or end-to-end OIDC
    - Considered: (a) GitHub Actions ubuntu-latest, single job, bun → docker:up --wait → unit + integration + Playwright → docker:down (chosen); (b) unit-only with mocked external services (rejected — does not prove the headline submission claim); (c) self-hosted runner (rejected — operational burden disproportionate for a submission)
    - Decision: (a). Pitfall 8 disk cleanup pre-step required (runner ~14GB free vs ~10GB compose images)
    - Consequences: ~8-15 min wall-clock per CI run; Actions minutes consumption acceptable for the submission scope; concurrency cancel keeps in-flight runs under control; failure artifacts (Playwright videos) uploaded for triage
    - Cite: D-07 from 10-CONTEXT.md; RESEARCH "Pattern 5: GitHub Actions workflow" + "Pitfall 8"

    Per RESEARCH Open Question 4 + the renumber precedent set Phase 7/8/9 — these take 035/036/037 not the stale ROADMAP labels (028/029/030). Phase closeout reconciliation note added in Task 4.

    No AI attribution in commit messages (CLAUDE.md). Commit each ADR individually: `docs(10-09): ADR-035 OTel + Jaeger stack`, etc.
  </action>
  <verify>
    <automated>test -f .planning/adrs/ADR-035-otel-jaeger-stack.md && test -f .planning/adrs/ADR-036-adr-catalogue-in-readme.md && test -f .planning/adrs/ADR-037-ci-runs-full-stack.md && grep -q "^# ADR-035:" .planning/adrs/ADR-035-otel-jaeger-stack.md && grep -q "^# ADR-036:" .planning/adrs/ADR-036-adr-catalogue-in-readme.md && grep -q "^# ADR-037:" .planning/adrs/ADR-037-ci-runs-full-stack.md && grep -q "^\*\*Status\*\*:" .planning/adrs/ADR-035-otel-jaeger-stack.md && grep -q "^\*\*Phase\*\*: 10" .planning/adrs/ADR-035-otel-jaeger-stack.md && grep -q "Alternatives Rejected\|## Considered" .planning/adrs/ADR-035-otel-jaeger-stack.md</automated>
  </verify>
  <done>3 new ADRs exist at next-free numbers with full CLAUDE.md template structure; each cites D-XX decisions + RESEARCH sections + the alternatives rejected with rationale; Phase 10 ADR count reaches 37 total.</done>
</task>

<task type="auto">
  <name>Task 3: Author README sections (Quick Start, Architecture mermaid, Saga Flow mermaid, ADR sentinel markers, Scripts, Env Vars, Troubleshooting, CI badge) preserving Phase 8 Provably-Fair section</name>
  <files>README.md</files>
  <read_first>
    README.md (current — Phase 8 P08-09 added "Provably Fair: Verify Outside the App" section; Phase 1 P1.9 created the initial structure; treat existing sections as preserved unless replaced)
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Architecture Patterns / System Architecture Diagram" mermaid graph TD — use verbatim (it's accurate to the current stack)
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Saga Trace Propagation Diagram" mermaid sequenceDiagram — use verbatim
    .planning/REQUIREMENTS.md "Open Configuration Values" table — Env Vars section sources from here
    package.json root scripts section — Scripts section enumerates from here (docker:up, docker:down, docker:prune, test, lint, typecheck, docs:adr-index)
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Common Pitfalls" + "Open Questions / Frontend container" + "Environment Availability" — Troubleshooting items
    .planning/phases/10-quality-hardening-docs/10-08-SUMMARY.md (Plan 10-08 — workflow URL for badge)
  </read_first>
  <action>
    Edit `README.md` to add the D-08 sections AROUND the existing Provably-Fair section. The Phase 8 P08-09 "Provably Fair: Verify Outside the App" section is preserved byte-stable (do NOT edit it).

    Final README structure (top to bottom):

    1. **Title + tagline** — Crash Game (Jungle Gaming Challenge); senior-level fullstack submission demonstrating real-time correctness + fairness + observability.
    2. **CI badge** (REQ-CI-03): `![CI](https://github.com/<repo>/actions/workflows/ci.yml/badge.svg)`. Insert the actual repo path from `git remote -v` (if remote not set, use placeholder `<org>/<repo>` and note in the commit message that the badge URL must be updated post-fork). Do NOT add a Codecov badge per RESEARCH Deferred Ideas — defer or mark "pending Codecov enroll".
    3. **Quick Start** (D-08): single command `bun run docker:up` then `cd frontend && bun run dev`; demo creds `player / player123`; live URLs (game http://localhost:3000, Keycloak :8080, Jaeger :16686, Prometheus :9090, Grafana :3001 anonymous Viewer).
    4. **Architecture** (D-08): mermaid `graph TD` from RESEARCH "System Architecture Diagram" verbatim.
    5. **Saga Flow** (D-08): mermaid `sequenceDiagram` from RESEARCH "Saga Trace Propagation Diagram" verbatim — illustrates the one trace_id across HTTP→AMQP→WS for a single bet.
    6. **Provably Fair: Verify Outside the App** (Phase 8 P08-09 — PRESERVED VERBATIM; do not edit).
    7. **Observability** (NEW per D-05 + D-06): Jaeger at :16686 (trace any bet end-to-end), Prometheus at :9090 (scrape targets + custom metrics), Grafana at :3001 with 3 pre-provisioned dashboards (games / wallets / crash-domain showing bet_volume + RTP + multiplier_drift + ws_*).
    8. **ADR Catalogue** (D-08 + REQ-DOC-02): the sentinel-bounded section with markers `<!-- ADR-INDEX:START -->` and `<!-- ADR-INDEX:END -->` — leave empty between markers; Task 4 runs `bun run docs:adr-index` to populate.
    9. **Scripts** (D-08): table of every root `package.json` script with one-line purpose.
    10. **Env Vars** (D-08): link to `.env.example` + REQUIREMENTS.md Open Configuration Values; tabulate the NEW Phase 10 keys (OTEL_EXPORTER_OTLP_ENDPOINT / OTEL_SERVICE_NAME / LOG_LEVEL / CRASH_RTP_WINDOW_ROUNDS).
    11. **Troubleshooting** (D-08): RESEARCH "Common Pitfalls" distilled — port 3000 collision (FE vs Grafana — Grafana on 3001), `bun run docker:up` fails first time (run `docker compose pull` then retry — Pitfall 8 disk on macOS), Jaeger no spans (verify `head -1 services/games/src/main.ts` is `import "./tracing"`), Grafana panels empty (Prometheus targets DOWN — check `/api/v1/targets`), Playwright OIDC redirect (Pitfall 5 — verify Keycloak realm allowed redirect URIs), polish-defect status (Phase 10 plan 10-06 closes all three; if they reproduce file an issue).
    12. **Roadmap status** (one-line): "Phases 1-10 complete (10/10). 95/95 v1 REQs satisfied."

    Use no emojis. Mermaid diagrams render natively on GitHub (no plugin needed). Tabularize Scripts + Env Vars for readability.
  </action>
  <verify>
    <automated>grep -q "Quick Start" README.md && grep -q "## Architecture" README.md && grep -q "## Saga Flow\|sequenceDiagram" README.md && grep -q "Provably Fair" README.md && grep -q "ADR-INDEX:START" README.md && grep -q "ADR-INDEX:END" README.md && grep -q "## Scripts\|## Troubleshooting" README.md && grep -q "ci\.yml/badge\.svg" README.md && grep -q "OTEL_EXPORTER_OTLP_ENDPOINT" README.md && grep -q "graph TD\|graph TB" README.md</automated>
  </verify>
  <done>README has all 12 sections in order; Phase 8 Provably-Fair section byte-stable (verified via `git diff README.md` showing only ADDITIONS around the preserved block); 2 mermaid diagrams present; CI badge URL placeholder OR real repo URL; sentinel markers ready for Task 4 to fill.</done>
</task>

<task type="auto">
  <name>Task 4: Run docs:adr-index to populate ADR table + rotate STATE.md + ROADMAP.md + REQUIREMENTS.md + final closeout commit</name>
  <files>README.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/STATE.md (current — Phase 9 closeout notes; update Current Focus to "Phase 10 complete — project shippable"; append "Phase 10 complete" summary citing 9 plans, ADR-035..037, REQ-IDs closed)
    .planning/ROADMAP.md (Phase 10 entry currently `[ ]`; flip to `[x]` + check off each of the 9 plans + update Progress table row + add 1-line completion note in the "Last updated" footer following Phase 9's voice)
    .planning/REQUIREMENTS.md (Pending rows for REQ-TEST-05, REQ-OBS-01..04, REQ-CI-01..03, REQ-DOC-01..02 — flip each to Done with the plan citation; update Coverage summary "v1 complete" count from 76/95 to 86/95)
    .planning/phases/10-quality-hardening-docs/10-04-SUMMARY.md / 10-05 / 10-06 / 10-07 / 10-08 (cite each plan's deliverable in the closeout note per Phase 9's precedent)
    .planning/phases/10-quality-hardening-docs/10-RESEARCH.md "Open Question #4" — renumber-reconciliation note pattern from Phase 7/8/9 closeout precedent
  </read_first>
  <action>
    1. Run `bun run docs:adr-index` from repo root. This populates the README table between the sentinel markers Task 3 added. Verify the output table has 37 rows (1..037) with correct titles + phases + dates + status. Commit: `docs(10-09): generate ADR catalogue table (37 entries)`.

    2. Update `.planning/STATE.md`:
       - "Current focus": replace Phase 9 closeout note with "Phase 10 complete — Quality Hardening & Docs (9/9 plans). All 10 phases done; v1 complete 86/95 (REQ-TEST-05 + REQ-OBS-01..04 + REQ-CI-01..03 + REQ-DOC-01..02 closed); ADRs landed 34 → 37. Submission ready: `bun run docker:up` brings the full observability stack (Jaeger + Prometheus + Grafana) alongside services; CI proves zero-step bootstrap via GitHub Actions; README has Quick Start + Architecture + Saga Flow mermaids + auto-generated ADR table + CI badge."
       - Append `## Current Position` updates per existing STATE.md voice.
       - No retrospective — Phase 10 itself IS the retrospective surface (README + ADRs).

    3. Update `.planning/ROADMAP.md`:
       - Flip `- [ ] **Phase 10:** ...` to `- [x] **Phase 10:** ...` in the Phases list.
       - Phase 10 details: replace placeholder Plans count with `**Plans:** 9 plans` + the 9 plan entries with one-line objectives + flip each to `[x]`.
       - Progress table Phase 10 row: 9/9 / Complete / today's date.
       - "Last updated" footer: append a Phase 10 closeout note following the Phase 9 voice (cite ADR-035..037 reconciliation; 9 plans landed; all 10 REQ-IDs closed; submission shippable).

    4. Update `.planning/REQUIREMENTS.md`:
       - Flip REQ-TEST-05 to Done (cite plan 10-07).
       - Flip REQ-OBS-01..04 to Done (cite plan 10-03 + 10-04 + 10-05).
       - Flip REQ-CI-01 + REQ-CI-02 to Done (cite plan 10-08).
       - Flip REQ-CI-03 to Done (cite plan 10-09).
       - Flip REQ-DOC-01 + REQ-DOC-02 to Done (cite plan 10-09).
       - Update Coverage summary `v1 complete`: 76/95 → 86/95 (with the +10 REQ-IDs enumerated in the Phase 10 closeout sentence).
       - Phase 10 traceability table: all rows Pending → Done with plan citations.
       - Update `Last updated` footer per Phase 9 voice with the Phase 10 increment list.

    5. Re-run `bun run docs:adr-index:check` to confirm README still in sync.

    6. Final commit: `docs(10-09): Phase 10 closeout — STATE + ROADMAP + REQUIREMENTS rotation` (no AI attribution).

    Per CLAUDE.md: atomic commits, EN messaging matching project style, no emojis, no AI attribution.
  </action>
  <verify>
    <automated>bun run docs:adr-index:check && grep -q "ADR-037" README.md && grep -q "Phase 10 complete" .planning/STATE.md && grep -q "- \[x\] \*\*Phase 10:" .planning/ROADMAP.md && grep -q "v1 complete.*86/95\|86 / 95" .planning/REQUIREMENTS.md && grep -q "REQ-TEST-05.*Done\|REQ-TEST-05.*P10\|REQ-TEST-05.*10-07" .planning/REQUIREMENTS.md && grep -q "REQ-OBS-01.*Done\|REQ-OBS-01.*10-03" .planning/REQUIREMENTS.md && grep -q "REQ-CI-01.*Done\|REQ-CI-01.*10-08" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>README ADR table populated with 37 entries; STATE / ROADMAP / REQUIREMENTS reflect Phase 10 closeout; v1 complete 86/95; ADRs 34 → 37; ROADMAP Phase 10 marked [x] with 9 plan rows; all closing commits land atomically with no AI attribution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README -> external repo viewers | Public-facing surface; no secrets, no internal hostnames beyond the documented dev ports |
| CI badge URL -> github.com | Points to ci.yml badge endpoint; no secrets exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-23 | Information Disclosure | README troubleshooting / env vars section | accept | All disclosed values are dev defaults (player/player123, localhost ports, jaeger:4318) — production deployment is out of scope per the submission framing |
| T-10-24 | Tampering | ADR catalogue generator script | mitigate | CI `docs:adr-index:check` step fails the build on drift — ADR table is always in sync with `.planning/adrs/` |
</threat_model>

<verification>
- `bun run docs:adr-index:check` returns exit 0 (README in sync with ADRs)
- README sections all present per Task 3 verify gate
- 3 new ADRs (035 / 036 / 037) exist with Phase 10 + Accepted status
- STATE.md says Phase 10 complete
- ROADMAP.md Phase 10 row marked [x] + 9 plans listed
- REQUIREMENTS.md v1 complete count is 86/95 + Phase 10 traceability rows all Done
- Final CI run on the closeout PR is green (badge in README will reflect this)
</verification>

<success_criteria>
1. Recruiter opens README in browser → CI badge green → Quick Start works → Architecture diagram renders → ADR table fully populated.
2. All 95 v1 REQ-IDs accounted for: 86 Done + 9 [stretched: none — all v1 closed; wait, actually 86 because some are explicitly out-of-scope-Done per other phase increments — verify the final count matches the existing traceability accounting].
3. `/gsd:verify-phase 10` can run and find every must-have artifact + observable truth from this plan + the 8 prior Phase 10 plans satisfied.
</success_criteria>

<output>
Create `.planning/phases/10-quality-hardening-docs/10-09-SUMMARY.md` when done. Cite the final ADR count (37), the v1 complete count (86/95), and the ADR-035..037 renumber-reconciliation note per the Phase 7/8/9 precedent.
</output>
